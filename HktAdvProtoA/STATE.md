# STATE.md

## 구현 현황

목적 트리 1차 작성 완료.

- `data/objective-graph.json` — 목적 트리 데이터 (Design-ObjectiveTree.md 근거).
  - 노드 스키마: §5 표준 목적 노드 구조 (목적 주체·문장·현재/목표 상태·생존과의 관계·발생 원인·해결/대체 방법·필요 기능/기호·재료 제공 단위·접근 조건·방해 요소·발견 방식·완료 조건·완료/실패 결과·파생 목적).
  - 최상위 목적 §2 + 생존 7하위(G1.1~G1.8).
  - 예시 트리 §10(추위에서 생존한다, `C1*`) · §11(거대 신을 제거한다, `D1*`) 전개.
  - 다주체 목적 §2(늑대 무리·나무 군락·상인 연합·왕국·거대 신·질병) 을 별도 루트로 배치.
  - `links` = 주체 간 관계(충돌·협력·의존) — §2 충돌 관계 반영.
  - 규모: 65 노드 · 10 관계 · 7 루트.
- `objective-tree.html` — 브라우저 관찰·편집기 (의존성 없음, 단일 파일).
  - 트리 뷰(접기/펼치기·검색·주체 색상) + 그래프 뷰(tidy-tree SVG, 드래그 이동·휠 확대/축소, 주체 간 관계 점선).
  - 노드 선택 시 §5 표준 필드 전체를 관찰·편집. 하위/최상위 목적 추가·복제·삭제, 상위 목적(파생) 재지정, 관계(충돌/협력/의존) 추가·삭제.
  - 편집본은 브라우저 localStorage 자동 저장. `내보내기(JSON)` → `data/objective-graph.json` 에 저장하면 원본 갱신. `원본 복원` 으로 파일 원본 재로드.
  - 로컬 서버 권장: `python3 -m http.server` 후 `objective-tree.html` 열기 (file:// 은 fetch 제약 → `가져오기`로 JSON 직접 선택).

## TODO

- [ ] §16 검증 규칙(1~15) 자동 점검 기능 — 미연결 요소·단일 해결 방법 노드 등 경고.
- [ ] §8 기호 기록(모든 고유 기호의 존재 근거) 을 별도 데이터로 분리·연결.
- [ ] 완료/실패에 따른 세계 상태 전환(§3.3) 시뮬레이션.
- [ ] 예시 트리 외 나머지 최상위 하위 목적(G1.3~G1.8) 전개.
