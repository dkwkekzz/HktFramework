# step-0228 — 월드 영속 fsync durable barrier(worldFsync/worldRecoverDurable)

> 직전 step-0227 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/worldlog.js` — `worldFsync`: durableSeq=jseq(fsync 로 디스크 *물리 확정*된 최대 seq). `worldRecoverDurable`: seq≤durableSeq 인 tail 만 replay(fsync 이후 tail 은 crash 시 미보장). 0227 flush(페이지캐시 적층)와 fsync(물리 확정)의 구분 = durability 의 진짜 경계.
- 계층: 6(데이터·월드영속) · OFF 플래그: worldFsync 미수신(durableSeq 0·recoverDurable 미발화 → 0227 비트 동일=회귀 0) · verify.js 새 모드 `worldfsync`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `worldfsync`: 5/5 OK — 3 append→fsync(durableSeq 3)→2 append → recoverDurable 3개(e4/e5 lost)·full replay 5개
- spine ALL OK

## 척추 5항 + 한계

①tick: fsync 는 tick 밖 데이터 계층. ②결정론: seq 순서·시드 PRNG. ③권위: 로그는 월드 상태 유일 쓰기 경로. ④은닉: 명시 메시지만. ⑤headless: Node·원격 재현. — 한계: fsync 는 인프로세스 모델(실제 디스크 sync 아님) — 디스크 확정 *경계*를 durableSeq 로 모델링한 것·C++ 호스트에서 실 fsync 로 교체.
