# step-0169 — 아이템 우편 saga 회계 정합 capstone(sagaConsistent)

> 직전 step-0168 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail-core.js` — `sagaConsistent()` 단언용 읽기 accessor: 두 항등식 AND — ① gives==ackedGives+pendingGives(보낸 give 는 정확히 acked 또는 pending·새는 give 0) ② ackedGives==giveOks+giveFails(회신은 ok/fail 분류·누락 0). 정상·회신손실·재전송 *모든 체제*서 성립. 거래소 0128 의 우편 판. 계층:게임 서비스.
- 미호출 accessor → 0168 비트 동일(회귀 0·0128·0140 capstone 패턴). verify.js 새 모드 `exmlsagac`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlsagac`(3 체제: 정상·회신손실·재전송): normal g/a/p 4/4/0·loss 4/3/1·retry 4/4/0·3체제 전부 sagaConsistent 예(3/3) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — saga 회계가 새지 않음을 단언(보낸 give 의 종착 회계 닫힘) ④은닉 우편 박스 내부 읽기 ⑤headless `node run.js` 통과 — 이상 없음. 아이템 우편 saga 정합층 완성(0128 의 우편 판). 이 capstone 은 *우편 내부* 회계 닫힘 — 우편·가방 *두 서비스* 회계 합치(giveOks==가방 escrowXfers)는 transfers capstone 0170 후속.
