# step-0169 — 아이템 우편 교차 정합(mailXfers — mail ackedOk == 가방 mailcustody transfers)

> 직전 step-0168 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-inventory-core.js`·`-txn.js` — `mailXfers` 계측: from/to 중 `'mailcustody'` 인 성공 give 수(거래소 0130 `escrowXfers` 의 우편 판). 우편 give 는 `escrowXfers`(거래소 판)를 오염시키지 않음(custody 의제 소유자 분리). 우편 `ackedOk`(성공 회신)와 교차 정합. 계층:게임 서비스/데이터(우편↔가방 2-서비스).
- OFF: 우편 give 부재(mailcustody 미등장) → mailXfers 0·digest/net.log 무영향 = 0168 비트 동일(회귀 0). verify.js 새 모드 `exmlxfer`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlxfer`(발신3+수령1+반환1 leg): gives=5·ackedOk=5·inv mailXfers=5·ackedOk==mailXfers(`예`)·escrowXfers=0(거래소 무오염) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0 ③권위 가방 단독(mailXfers 는 가방측 성공 transfer 계측·우편 ackedOk 는 우편측 회신 계측·두 독립 회계가 합치) ④은닉 의제 소유자 'mailcustody' 가 거래소 'escrow' 와 분리(회계 오염 0) ⑤headless `node run.js` 통과 — 이상 없음. 무손실(인프로세스 FIFO)서 ackedOk==gives==mailXfers — 회신 손실 시 ackedOk<gives 이나 mailXfers 는 실행 기준이라 ackedOk≤mailXfers 가능(재전송 dedup 이 ackedOk 따라잡게 함·#9 무대). liveness capstone(0170)이 전 정합층 동시 성립을 닫는다.
