# step-0384 — #65 양방향 동기 4: coordCoherent/run 가드가 coordDesync 채택

> 직전 step-0383 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — run 루프 매-tick 가드(0374)와 `coordCoherent`(0380)가 `driver.clusterDesync`(orch plan 기준) 대신 `coordDesync`(placement 기준) 채택. 0380 capstone 은 orch plan stale 때문에 migrate/failover 를 제외해야 했으나, placement 권위가 lifecycle 을 따라가므로 *migrate 포함* 연속 루프·capstone 이 정합. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordmigcap`. 새 박스·run() 미사용 → reg 0(정상 경로 placement==orch plan 이라 결과 동일).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmigcap 5/5**: run(5)+migrate z1 A→B → maxDesync 0·placedHost('z1')==hostB·**coordCoherent Y**(0380 이 제외했던 migrate 를 포함해도 정합).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 가드는 tick 밖 ②결정론: maxDesync 0 ③권위: placement 기준 정합으로 migrate 후 단일 소유 유지 ④은닉: cluster API 만 ⑤headless: 실 host. — 이상 없음(failover 의 lost 처리는 0385~·grand capstone 0390).
