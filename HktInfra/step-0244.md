# step-0244 — 배치 SSOT 실배선(#51) 4: executed placeDrain(host 드레인 실 이주)

> 직전 step-0243 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `_drain` 의 매 paper `placement.set` 옆에 `placeExecute` ON 이면 `_migrate`(0242) 호출 — host 드레인이 실 존 런타임도 release+acquire 로 이주 → 드레인 후 그 host running 0(*실제* 비워짐·0224 퇴역 안전 이주의 집행 판). 계층:코디네이션.
- OFF 플래그 `placeExecute`(OFF→_drain 은 paper 만=0243 비트 동일=회귀 0).
- verify.js 새 모드 `placedrainexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placedrainexec`**: 5시드 모두 running A 0·B 2·C 2·rtMig 2 — z1·z2@A·z3@B·z4@C 에서 A 드레인 후 A 가 *실제* 비고(z1→B·z2→C)·결정==집행 전 존.
- **결정론/권위/수렴**: `run.js all` ALL OK.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면 ②결정론: 시드 PRNG·매 존 최소부하 재계산 결정론 ③권위 단일 소유: 매 _migrate 원자 교체 ④은닉: orch 내부 ⑤headless: node 검증. — 이상 없음(placeZone/Migrate/Rebalance/Drain 4종 executed 완비·reconcile capstone 0245 후속).
