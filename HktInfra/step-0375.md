# step-0375 — #62 runMulti 통합 5: 상주 migrate

> 직전 step-0374 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `migrate(zone, fromHost, toHost)` 추가: driver.migrateZone(snapshot from→toHost spawn/zoneadd→loadstate→from zonedel) 을 코디네이터 상주 lifecycle 메서드로 감쌈. entity 무손실·release+acquire(이중 쓰기 0)·`migrations` 계측. verify 가 직접 부르던 lifecycle 을 broker 측 제어 평면으로. 계층: 코디네이션(#62).
- verify.js 새 모드 `coordmigrate`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmigrate 5/5**: run(3) 후 migrate z1 A→B → a1 상태 보존(realPos==zoneEntityPos 권위)·hostA z1 release Y·coord.migrations==1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: migrate 는 tick 밖 제어 평면 lifecycle ②결정론: snapshot+loadstate 상태 이전 무손실 ③권위: release+acquire 쌍 거래·hostA release 로 이중 쓰기 0 ④은닉: driver/cluster API 만 ⑤headless: 실 host.js 프로세스 경계 넘어 이주. — 이상 없음.
