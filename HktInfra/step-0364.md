# step-0364 — #57 실 데이터 평면 4: 실 host.js migrate 상태 보존

> 직전 step-0363 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `migrateZone(cluster, zone, fromHost, toHost, specOf)`: snapshot(from)→ toHost spawn/zoneadd→ loadstate(이전 ents/sessions 주입)→ zonedel(from). orch `_bridgeMigrate`(같은 핸들 원자 교체·상태 보존)의 *실 child_process* 판 — 실 프로세스 경계를 넘어 entity 무손실·권위 단일 소유(release+acquire).
- 계층: 코디네이션/월드(⑤②). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostmigratereal`. host.js loadstate cmd(0013·기존) 재사용.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0363) 비트 동일.
- `hostmigratereal` 5/5 OK — z1@hostA·a1 enter 뒤 `migrateZone` z1 A→B: hostB snapshot 에 z1·a1 `{x:5,y:5}` 보존(== in-proc 권위·무손실)·hostA snapshot zones==(none)(release). 실 프로세스 경계 넘어 entity 보존.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: migrate 는 제어 평면(snapshot/loadstate/zonedel rpc). ② 결정론: 5종 동일. ③ 권위: from release + to acquire 쌍(실 프로세스 둘 다에 동시 존재 0·이중 쓰기 0). ④ 은닉: 상태 직렬화(snapshot→loadstate). ⑤ headless: 실 host.js 2 프로세스 간 상태 이전·snapshot 검증 — 원격.
- 한계: migrate 중 in-flight frame 의 실 소켓 재라우팅(이주 직후 도착분)은 후속(현재 정적 상태 이전까지). 이상 없음.
