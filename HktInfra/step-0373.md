# step-0373 — #62 runMulti 통합 3: ClusterCoordinator.run (연속 tick 루프)

> 직전 step-0372 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `run(ticks)` 추가: start()(미시작 시) 후 tick(t)을 1..ticks 반복. cluster-run.js runMulti 핵심(broker 측 제어 평면이 *매 tick* 실 cluster 전체 데이터 평면을 구동)을 상주 코디네이터 한 호출로 환산. 계층: 코디네이션(#62 — runMulti 의 연속 tick 루프).
- verify.js 새 모드 `coordrun`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordrun 5/5**: run(5)→views 2(첫 tick 에 z1·z2 move view_delta·이후 무이동 0)·clusterCoherent Y(5 tick 내내 desync 0)·coord.ticks==5.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 연속 루프가 매 tick 실 host zone.onTick 만 구동(시뮬 외 작업 미혼입) ②결정론: 같은 입력 반복→매 tick desync 0 ③권위: in-proc orch SSOT ④은닉: driver/cluster API 만 ⑤headless: 실 host.js child_process 연속 tick. — 한계: 이번 시나리오는 첫 tick 후 신규 intent 없음(정적 수렴 확인). 매-tick 신규 intent 피드는 #61 업스트림 실 클라 경로 후속.
