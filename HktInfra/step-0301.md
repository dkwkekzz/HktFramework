# step-0301 — #9 잔여(실 host.js 물리 분리) 1: host 1급 컨테이너(zoneHosts) 레지스트리

> 직전 step-0300 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `zoneHostProc` 플래그 + `zoneHosts: Map<host,{zones:Set}>` 필드. 계층: 코디네이션(⑤). OFF→0300 비트 동일.
- `src/orch-zonebridge.js` — `_hostSet(zoneId, host)`(zone 을 정확히 한 host 컨테이너에 귀속·어디 있든 먼저 떼고 새 host 에 붙임·빈 컨테이너 제거=roster 자동·host==null 이면 떼기만) + 질의 `hostRuntimeCount`·`zoneHostOf`·`zoneHostHosts`. `_bridgeStart/_bridgeMigrate/_bridgeHostDown/_bridgeStop` 가 _hostSet 호출(zoneHostProc 가드).
- `src/topo-build.js` — `zoneHostProc` opt 배선(orch 전달).
- verify.js 새 모드 `zonehostreg`. **한 조각**: flat zoneRuntimes 위에 host=*1급 컨테이너*(자기 존 집합 소유) — 실 host.js 프로세스(여러 존 소유·자기 소켓 수신·자기 루프 tick)의 씨앗.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneHostProc OFF → baseline(0300) 비트 동일(maxDiff 0·전 카테고리).
- `zonehostreg` 5/5 OK — 0300 혼합 lifecycle(hostdown C·stop z2·migrate z1·rebalance·drain) 후: host 컨테이너 == running(hc== Y·hosts 2·총귀속==running.size 2) + directFlowCoherent Y·entityConserved Y·total 1·ledger 5/1/2/1.
- `node run.js` = ALL OK · `spine` = ALL OK(전 역사 누적 회귀).

## 척추 5항 + 한계

- ① tick: host 컨테이너 유지는 배치 집행 경로(tick 밖)·시뮬 tick 무침습. ② 결정론: 자료구조 귀속만(난수 0)·시드 5종 동일. ③ 권위: zone→host 정확히 한 컨테이너(_hostSet 멱등 단일 귀속). ④ 은닉: orch 내부 레지스트리(클라 무관). ⑤ headless: Node 순수·reg 0.
- 한계: zoneHosts 는 아직 *읽기 묶음*일 뿐 — 프레임 전달은 여전히 orch flat 경로(per-runtime mbox). host 컨테이너가 *자기 inbox 로 수신*하고 *자기 루프로 tick* 하는 것은 후속(0302~). 이상 없음.
