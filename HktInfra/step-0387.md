# step-0387 — #65 양방향 동기 7: placement-aware report

> 직전 step-0386 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `report()` 가 placement 기준으로 전환: hosts/zones=placement(migrate/failover 반영)·desync=coordDesync·coherent=coordDesync===0·`lost`(lostZones.size) 추가. 0379 report 는 orch.hostSpawnPlan + driver.clusterDesync 라 migrate 후 발산(0379 가 migrate 제외한 이유) → placement 권위로 정합. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordreport2`. 새 박스·run() 미사용 → reg 0(정상 경로 결과 동일).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordreport2 5/5**: run(3)+migrate z1 A→B → report hosts 2·zones 3·entities 2·desync 0·migrations 1·lost 0·**coherent Y**(0379 가 못했던 migrate 후 대시보드 정합).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: report 는 읽기 관측 ②결정론: 동일 ③권위: placement(where)+coordDesync 로 migrate 반영 ④은닉: driver/cluster API 만 ⑤headless: 실 host 집계. — 이상 없음.
