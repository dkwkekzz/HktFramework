# step-0477 — 실 host.js child 경계 업스트림 7: 다중 UpClient

> 직전 step-0476 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 코드(박스) 무변경 — 0475 `driveUpstream` 의 다중 클라 지원을 2 존(zone1·zone2·한 host)·2 UpClient(a1·b1)로 검증. 계층: 엣지/월드(#70). verify.js 새 모드 `upcmulti`.
- 각 클라가 자기 존으로 경계 배달·자기 존 egress 뷰 수신 → 각자 자기 존 authSig 로 수렴. 교차 존 격리(a1 은 b1 을 안 봄·다른 존).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (코드 무변경).
- `upcmulti` 5/5 OK — applied 6(각 enter+2 move)·a1 seenSig==zone1 authSig·b1 seenSig==zone2 authSig 전 시드(예: seed42 a1@4,6·b1@0,10).
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 이상 없음 ②결정론: 각 존 독립 재현 ③권위 단일: 각 존 자기 entity 소유 ④은닉: 각 클라 게이트웨이-형·자기 세션 뷰만 ⑤headless: 실 child_process 다중 존.
- 한계: 이상 없음(두 클라 별 존·AOI 비겹침). 손실 하 멱등 수렴(0478)로 확장.
