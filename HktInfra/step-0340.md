# step-0340 — #9 후속: 다운스트림 다중 존 격리(클라별 교차 누수 0)

> 직전 step-0339 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway.js` — 클라별 전달 세션 회계 `downDelivered`(client addr→Set<sessionId>) 를 `_recvZoneView` 라우팅 시 기록(읽기 전용 회계·메시지 무영향). 격리 술어 `gatewayDeliveryIsolated()`(모든 클라가 정확히 자기 1세션 frame 만) + 질의 `gatewayClientSessions(client)`.
- 계층: 엣지(④⑤·SPINE §4 경로2 격리). 플래그 없음·읽기 전용 회계(맵 채움만·미참조 메시지) → baseline 비트 동일. verify.js 새 모드 `gwiso`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 회계 추가 → baseline(0339) 비트 동일.
- `gwiso` 5/5 OK — z1@A(a1@dc0·a2@dc1)·z2@C(b1@dc2) 동시: `gatewayDeliveryIsolated` **Y**·dc0→[s:a1]·dc1→[s:a2]·dc2→[s:b1](교차 누수 0)·존별 egress 격리(zoneViewSessions z1=[s:a1,s:a2]·z2=[s:b1]).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 회계는 읽기. ② 결정론: 5종 동일. ③ 권위: 무영향. ④ 은닉: 각 클라는 자기 게이트웨이 연결로 자기 세션 뷰만(타 세션·존 무지). ⑤ headless: Node 순수·reg 0.
- 한계: 격리는 세션→클라 바인딩의 단사성에 의존(0334) — 바인딩 정확성은 0334 가 보장. 이상 없음.
