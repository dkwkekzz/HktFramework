# step-0323 — 정리 분할: 다운스트림 뷰 질의 → orch-views.js

> 직전 step-0322 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js`(신규)·`src/orch-zonebridge.js`·`src/orchestrator.js` — orch-zonebridge.js 가 30.6KB>30KB 박스 트리거를 넘겨, 다운스트림 데이터 평면 뷰 질의 6종(0319~0322·`zoneViewBuf`·`zoneViewEntered`·`zoneViewStats`·`zoneVisibleIds`·`zoneViewsFor`·`zoneViewFrames`)을 `orch-views.js` 믹스인으로 분리. `Object.assign(Orchestrator.prototype, OrchViews)` 로 되섞어 this 해소 동일 = 동작 비트 불변(orch-placement 0251·orch-control 0267·orch-hostproc 0305 동형).
- 계층: 코디네이션(⑤). **기능 0·투명 분할** → 0322 비트 동일. verify.js 새 모드 `zoneviewsplit`(분할 스모크).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 위치만 이동(기능 0) → baseline(0322) 비트 동일(분할이 동작 불변임의 본 증거).
- `zoneviewsplit` 5/5 OK — 옮긴 6 질의가 분할 후에도 해소·동작: zoneViewFrames **4**==zoneViewsFor·zoneViewBuf 4·zoneViewStats 1/2(reset/update)·zoneVisibleIds⊇a1·zoneViewEntered⊇a1.
- 박스 크기: orch-zonebridge.js 30.6KB→28.1KB·orch-views.js 4.9KB·**>30KB 박스 0개**.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 질의(시뮬 외 0). ② 결정론: 5종 동일. ③ 권위: 무변경(위치만 이동). ④ 은닉: 무변경. ⑤ headless: Node 순수·reg 0.
- 한계: 순수 정리(기능 추가 0)·다음 기능 step 의 박스 헤드룸 확보. 이상 없음.
