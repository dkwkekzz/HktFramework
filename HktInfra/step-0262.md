# step-0262 — 정리(#49 wiring): topo-run crash/failover 복구 주입 분리

> 직전 step-0261 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/topo-run.js` → `run()` 루프에 남아 있던 *crash/failover 복구 주입*을 새 박스 파일 `src/topo-failover.js`(`applyFailover(opts, i, ctx)`)로 분리. 계층: 배선(wiring·구동 루프). **OFF 플래그 없음 — 투명 분할**(코드 위치만 이동·동작 비트 불변). 0261(topo-inject·메시지 주입열) 분할의 짝.
- 옮긴 복구 핸들러(verbatim): `persistRestart`(primary persist crash+N-replica kill)·`invRestart`(가방 crash→quorum-merge/persist2 replay)·`clientResync`(in-flight give 재발행)·`rankRestart`(읽기모델 reconstruct)·`spawnReconstruct`(대체 소비자 late-join)·`chatRestart`(채팅 커맨드 replay)·`busReSub`(동적 sub/unsub)·`busRestart`(bus crash+구독 재협상·busResendReq/busResend·rankRenegAt). 잔류(run 루프): 빌드/핸들 셋업·메시지 주입 위임(0261)·net.step·trace 집계.
- 배선: topo-run.js 가 `__p('topo-failover')` 1줄 require · 루프 전 핸들 묶음 `__fctx`(박스 + `quorumMergeJournals` 헬퍼) 1회 구성 · 루프 안은 `applyFailover(opts, i, __fctx)` 한 줄 위임. verify.js 새 모드 `fosplit`.
- 결과 크기: topo-run.js 22.7KB→13.1KB·topo-failover.js 11.1KB. **#49 의 topo-run(35.9KB) 완전 해소**(셋업+위임 2줄로 슬림).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `fosplit`(가설): 5/5 — 가방 invRestart(crash@60+PersistStore replay) 복구판 ledger size == 무crash base(51/53/54/53/56)·전 시드 일치. 옮긴 복구 경로가 원장을 투명 복구(위임 무결).
- spine: ALL OK(전 역사 누적 회귀 + fosplit 편입).

## 척추 5항 + 한계

①tick: 복구 주입은 net.step *직전* 제어 평면(존 tick 밖)·무변경 ②결정론: 코드 이동만·시드 동일 해시 ③권위: 복구가 원장에 투명(단일 소유 보존) ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
