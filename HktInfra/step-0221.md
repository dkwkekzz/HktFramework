# step-0221 — 인스턴스 플레이어 이탈(instanceLeave·occupancy 감소·권위 release)

> 직전 step-0220 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/instance.js` — `instanceLeave{player}`: 배정된 player 의 route 해제(occupancy 감소·권위 release·0216 acquire 의 짝). 미배정 player 는 멱등 no-op(`leaveMisses`). 비운 인스턴스는 0222 수요 자동 despawn 회수 대상이 된다.
- 계층: 2(월드·인스턴스) · OFF 플래그: `instanceLeave` 미수신(미발화 → 0220 비트 동일=회귀 0) · verify.js 새 모드 `instanceleave`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 persistReplicas 0 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `instanceleave`: 5/5 OK — d1 occupancy 2→1(p1 이탈·p2 잔류)·p1 route null·left 1·leaveMisses 1(미배정 pX)
- spine ALL OK

## 척추 5항 + 한계

①tick: 인스턴스는 존 tick 밖 순수 반응형 — 위반 0. ②결정론: route Map 단일 키 delete·시드 PRNG 만. ③권위: route 해제 = 권위 단일 소유의 release(0216 acquire 짝·이탈 후 무권위는 정상). ④은닉: 명시 메시지(instanceLeave)만. ⑤headless: Node·원격 재현. — 이상 없음.
