# step-0497 — svcmailexpire 우편 메시지 통수 회계 편입 (#16 라운드 3차 7)

> 직전 step-0496 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 우편 메시지 통수 회계 capstone(옛 0150 `mailConsistent` 판)을 재작성 편입: `run()` 을 mail+mailTtl opts 로 구동(mailSend 2통→mailFetch 1→mailSweep 만료 1)하고 `mailConsistent`(sent == 보유 held + 수령 fetched + 만료 expired·세 종결 분할·공백/중복 0)를 단언하는 새 mode `svcmailexpire`. 아이템 saga(0493/0494)의 *메시지 레벨* 짝 — 만료 TTL 종결 커버. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 7/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcmailexpire`: 5/5 OK — sent 2 == fetched 1 + expired 1 + held 0(mailConsistent)·bob 수령·carol 만료(TTL 4).
- spine ALL OK — svcmailexpire 포함(우편 saga 2 + 메시지 통수 회계·서비스 계층 누적 회귀).

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 통수 회계·만료 대조·Math.random 0. ③권위: 우편 1통 매 순간 정확히 한 상태(held/fetched/expired)로 분할 — 공백·중복 0. ④은닉: 우편 박스 경유. ⑤headless: node in-proc. — 이상 없음.
