# step-0500 — promotedsvc 서비스 capstone 등록 가드·#16 라운드 3차 arc 닫기 (10/10)

> 직전 step-0499 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — #16 라운드 3차 arc(0491~0500) 정리: ⒜ 재작성 편입한 서비스 saga capstone 9종(svcexchangecap·svcexchangexfer·svcmailcap·svcmailxfer·svcguildcap·svcbankcap·svcmailexpire·svcsvccombined·svcexchangecancel)이 ORDER/MODES 누적 회귀에 항구 등록됐는지 단언하는 가드 mode `promotedsvc` 추가(향후 우발 제거 방지·"no silent cap") ⒝ 헤더 모드 카탈로그에 라운드 3차 목록 명기. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. **#16 라운드 3차 arc 닫기(10/10)** — 서비스 계층(거래소·우편·길드) 실행 검증이 이제 verify-kit ORDER 상주(감사 §2 ①-b 해소).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `promotedsvc`: OK — 서비스 saga capstone 9종 전부 ORDER∧MODES 등록(누락 시 즉시 FAIL·시드 무관 구조 가드).
- spine ALL OK — 서비스 capstone 9종 + promotedsvc 가드 전부 누적 회귀(거래소·우편·길드 정합 불변을 현재 src 에 항구 단언).

## 척추 5항 + 한계

①tick: 검증 가드·박스 무변. ②결정론: ORDER/MODES 구조 단언·Math.random 0. ③④⑤: 편입한 9 capstone 이 각각 서비스 계층의 소유 단일(escrow 쌍 거래·single-master·금고 원장)·은닉(버스 경유)·headless 를 재단언(promotedsvc 는 그 상시 실행 보장). — **정직한 한계**: 재작성 capstone 은 *정상·취소·만료·종합 격리* 체제 커버 — 옛 0121~0140/0161~0180 의 완전한 saga liveness(회신 손실 주입→abandon→재admission→permFailed 3분할 발현)는 loss 주입 seam 이 필요해 후속(현 sagaLivenessConsistent 는 정상 흐름 pending 0 로 성립). 금고↔가방 escrow 연동(#46)·금고 배지 feed 정합은 별도 트랙.
