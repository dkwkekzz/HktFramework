# step-0392 — #66 tick placement-aware 2: deliver 재생도 placement 권위 host

> 직전 step-0391 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `tick()` 의 deliver 재생이 `driver.commands` 의 `c.host`(번역 당시 host) 대신 `this.placement[c.zoneId]`(코디네이터 where 권위)로 현 host 를 조회해 frame 재생. mid-run migrate 후 c.host 는 stale 일 수 있으나 placement 는 lifecycle 마다 갱신돼 항상 옳은 host. 계층: 코디네이션(#66).
- OFF 동치: 정상 경로(placement[zoneId]==c.host)에선 같은 host 로 deliver → 0391 동치. verify.js 새 모드 `coorddeliverplace`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 20.6KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coorddeliverplace 5/5**: run(5) 후 a1 이 placement[z1] host 에 적용(pos {8,8})·coordDesync 0(deliver 가 placement host 로 라우팅).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 재생/순회는 시뮬 tick 밖 ②결정론: 시드 5종 동일 a1 {8,8} ③권위: deliver 가 placement(where) 단일 권위로 라우팅 ④은닉: cluster.rpc API 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: mid-run migrate 발현(deliver+tick 가 루프 도중 migrate 따라감)은 0393·orch 이중 권위 합류 #67 은 0394~.
