# step-0395 — #67 orch 이중 권위 합류 2: authoritiesAgree() 술어·migrate 후 발산 노출

> 직전 step-0394 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `authoritiesAgree()` 술어: 코디네이터 `placement`(where 권위) == `orchWhere()`(orch 집행 where-view) 가 모든 존에서 일치하는가. 참이면 단일 where 권위. lifecycle write-back(0396~)이 없는 지금은 migrate 후 orch.running 이 stale → 두 권위 *발산*(#67 의 구체 증거). 계층: 코디네이션(#67).
- 읽기 전용·새 메서드. verify.js 새 모드 `coordauthsplit`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 22.1KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordauthsplit 5/5**: run(5) 후 authoritiesAgree **Y**(일치) → migrate z1 A→B 후 authoritiesAgree **N**(orch stale·이중 권위 노출)·coordDesync 0(코디네이터 placement 권위는 정확).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 술어는 시뮬 tick 밖 ②결정론: 시드 5종 동일 ③권위: 두 where 권위 발산을 *측정*(아직 합류 전·placement 가 정확한 권위) ④은닉: orch 공개 API 만 ⑤headless: 실 host.js child_process E2E. — **한계(의도된·후속)**: migrate 후 authoritiesAgree N 은 #67 이중 권위의 노출이며 write-back 미구현 탓 — 0396(migrate)·0397(failover) write-back 이 합류시켜 Y 로.
