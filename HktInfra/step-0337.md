# step-0337 — #9 후속: 다운스트림 재전송(egress 손실을 resync 로 복구)

> 직전 step-0336 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway.js` `_recvZoneView` 재작성 — 인오더 게이팅: dseq==기대면 전진·전달·ack, 미래(앞 유실)면 gap·`zoneResync{from}` 요청(에피소드당 1회·`downResyncPending`), 과거(중복)면 드롭. 인오더 frame 만 클라 전달(순서 보장·중복 0). 질의 `gatewayResyncsSent`.
- `src/orch-views.js` `_resendEgress(sid,from)` — zoneResync 응답: 미-ack 버퍼의 dseq≥from 재전송(드롭분 복구). `src/orch-control.js` onMsg `zoneResync` 분기. `src/orch-bridge-init.js`·`src/topo-build.js` — 손실 주입 `egressDrop`(["sid#dseq"]·첫 전송만 드롭·재전송 통과).
- 계층: 코디네이션/엣지(②⑤·0008 ack/NAK 재전송의 다운스트림 판). 플래그 없음(egressDrop 미설정·zoneEgress OFF→손실/재전송 0). verify.js 새 모드 `gwloss`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — egressDrop 미설정·zoneEgress OFF → 재전송 경로 dead → baseline(0336) 비트 동일.
- `gwloss` 5/5 OK — `s:a1#1` 전송 손실(`zoneEgressDropped` **1**)에도: 게이트웨이 gap **2** 감지→`gatewayResyncsSent` **1**→orch `zoneResent` **3**→최종 `downSeqNext('s:a1')` **5** == egressed 5(전부 인오더 복구·무손실).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 손실/재전송 모두 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 재전송이 권위 무영향·중복은 드롭(belief 무오염). ④ 은닉: resync 는 게이트웨이↔orch 명시 메시지. ⑤ headless: Node 순수·직렬화·reg 0.
- 한계: 1회 손실 모델(재전송 항상 통과)·gap 은 *뒤 frame 도착*이 트리거 → 세션 마지막 frame 손실은 타임아웃 재전송 필요(후속). 이상 없음.
