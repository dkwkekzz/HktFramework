# step-0422 — #61 업스트림 intent 실 클라 2: UpClient move 발신 (plan zoneMove)

> 직전 step-0421 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/client.js` — `UpClient.onTick` 확장: joinAt 의 zoneEnter 이후 매 tick `plan`([[dx,dy]…]) 한 발씩 `zoneMove` 발신. 클라가 *자기 plan 으로 intent 를 생성*(합성 entityOps 의 스크립트 move 대체).
- 계층: 엣지/월드(#61).
- OFF 플래그: upClients=null(기본)→스폰 0→회귀 0.
- verify.js 새 모드: `upmove`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK.
- 가설 `upmove`: 5/5 — uc0 가 enter+plan[[3,2],[1,1]] 발신(sent 3)→a1 최종 {9,8} == enter(5,5)+Σplan(클램프 0).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 이상 없음. 수신(양방향)은 0423.
