# step-0154 — MailFeed 영속·late-join(reconstruct — 우편 op 저널 replay 로 배지 복원)

> 직전 step-0153 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mailfeed.js` — `reconstruct(journal)` 추가: 자기 영속 0 인 MailFeed 가 우편 박스 durable op 저널(0145·send/fetch/expire)을 seq 순 replay 해 배지(unread/sent/read/expired)를 재계산. 매핑은 우편 reconstruct 와 동형(send→unread++·fetch→그 시점 보유분 unread→read·expire→unread--). MarketFeed 0113·ranking 0020 의 우편 판(CQRS late-join). 계층:게임 서비스.
- 토폴로지 무변경 — reconstruct 는 verify 모드가 직접 호출(MarketFeed 0113 과 동일·run 훅 없음).
- OFF: reconstruct 미호출(restart 미주입) = 0153 비트 동일(회귀 0). verify.js 새 모드 `exmlfrec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일(digest 0x37591eab) · spine ALL OK.
- 가설 `exmlfrec`(h1 수령·h2 만료·h3 생존): crash 후 reconstruct digest == 라이브 digest(`예`)·feed==우편 권위(`예`)·복원 h3 unread=1 전 시드 OK.

## 척추 5항 + 한계

- ①tick 반응형(onTick 0) ②결정론 난수 0·digest 일치 ③권위 우편함 권위 불변(feed 파생) ④은닉 우편 저널은 명시 복구원(MarketFeed 0113 과 동형 경로) ⑤headless `node run.js` 통과 — 이상 없음. 우편 저널이 스냅샷 압축(0146)으로 가지쳐지면 가지친 head 이력은 복원 불가(MarketFeed 0113 과 같은 한계) — 완전 복원은 mailSnapshot 0 전제.
