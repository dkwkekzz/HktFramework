# step-0363 — #57 실 데이터 평면 3: 실 host.js tick (move 적용 + egress 산출)

> 직전 step-0362 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `tickZone(cluster, host, zone, tick)`: 실 host.js `{cmd:'tick'}` → 실 프로세스 `zone.onTick`(pending move 적용 + AOI `view_delta` 산출)·그 존이 낸 send(다운스트림 egress) 반환. orch `_tickRuntimes` 의 실 host 판.
- 계층: 코디네이션/월드(⑤②·SPINE §4 경로2 egress). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hosttickreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0362) 비트 동일.
- `hosttickreal` 5/5 OK — z1@hostA·a1 enter+6 move(1,1): 실 deliver(enter 적용+move pending) 후 `tickZone` → 실 zone.onTick 이 pending move 적용 → 실 a1 `{x:11,y:11}` == in-proc 권위 `{x:11,y:11}`(enter{5,5}+6×(1,1))·tick 산출 send 에 view `egress`==1(다운스트림 실 출력).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: tickZone 이 실 zone.onTick 을 구동(시뮬 tick = 존 자기 tick·외부 작업 0). ② 결정론: 5종 동일·배치 move 적용=순차와 동일(mod 덧셈). ③ 권위: 실 프로세스 zone 이 move 후에도 권위 재현(desync 0). ④ 은닉: tick items·view send 직렬화. ⑤ headless: 실 host.js onTick·view 산출·snapshot — 원격 검증.
- 한계: tick 산출 view 를 게이트웨이/클라까지 *라우팅*하는 실 소켓 경로는 후속(현재는 산출 확인까지). 이상 없음.
