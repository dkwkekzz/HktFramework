# step-0183 — 길드 멤버십 변경 발행(guildChangePublish·svc.guild.changed)

> 직전 step-0182 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `_publishChange(guildId,kind,member)`: 실 멤버십 변경(가입/탈퇴) 시 `svc.guild.changed` 버스 발행. no-op 변경(중복 가입·없는 탈퇴·master 탈퇴 거부)은 발행 안 함(발행==실 변경). published 계측. 파티 0084 의 길드 판. 계층: **3 게임 서비스**.
- 배선: `topo-build.js`(`guildChangePublish` 플래그·guild opts·buildSubs 인자)·`topo-subs.js`(audit→svc.guild.changed 구독).
- OFF 플래그 `guildChangePublish`(OFF·bus 부재 → 발행 0 = 0182 비트 동일=회귀 0) · verify.js 새 모드 `guildpub`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildChangePublish OFF → 0182 비트 동일.
- **가설 `guildpub`**: 5/5 시드 OK — published 3 == audit 수신 3 == 실 변경 3(c2·c3 가입·c2 탈퇴). no-op 3건(중복/없음/master) 발행 0. OFF published 0(비-침습).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 존 tick 밖 발행. ②결정론: 전 시드 발행 3. ③권위 단일 소유: 발행은 관측만·로스터 권위 불변. ④은닉: 버스 토픽 발행·구독자 주소 무지(audit 무수정 추가). ⑤headless·원격: Node·run.js 증명. — 이상 없음.
