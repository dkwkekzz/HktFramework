# step-0348 — #9 후속: 다운스트림 late-join 수렴(중도 합류 클라 keyframe 수렴)

> 직전 step-0347 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- **통합 검증 step(src 무변경)** — 세션이 *중도에* 입장하면 런타임 존이 reset keyframe(전체 AOI)을 산출 → egress→게이트웨이→클라 전파 → late-join 클라가 즉시 현재 세계로 수렴. 척추 "복제=재현·snapshot 은 late-join 최후 수단" 의 다운스트림 판. 새 메커니즘 없음 → src == baseline 비트 동일.
- 계층: 월드/엣지(②⑤·수렴 desync 0). verify.js 새 모드 `dcjoin`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — src 무변경 → baseline(0347) 비트 동일(자명).
- `dcjoin` 5/5 OK — a1 선입장·(5,5)→(13,13) 이동 뒤 a2 가 tick 14 중도 입장(13,15·a1 반경 안): `dc1.resets` **1**(keyframe 수신)·`dc1.seenSig` == `zoneAuthSig('z1','a2')`(즉시 수렴)·`[a1,a2]` 둘 다 봄.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: keyframe 산출 onTick·전파는 메시지(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 클라 읽기전용 뷰가 keyframe 재현으로 즉시 수렴. ④ 은닉: 클라 게이트웨이만. ⑤ headless: Node 순수·reg 0.
- 한계: 통합 검증(새 메커니즘 0). late-join keyframe 은 입장 시 자연 발생(전체 AOI reset). 이상 없음.
