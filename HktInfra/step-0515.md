# step-0515 — guildbanksaga 금고↔가방 saga 회신 닫힌 고리 (#46 금고↔가방 escrow 5)

> 직전 step-0514 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild.js`+`svc-guild-txn.js`+`topo-*` — 금고 escrow give 의 결과를 금고가 비동기 수신(거래소 0121·우편 0166 의 금고 판). 새 OFF 플래그 `guildBankSaga`: ON 이면 `_custody` 가 give 에 `replyTo`(금고 주소)+`gid` 동봉 → 가방이 `item_result` 를 금고로도 echo → `_onGiveReply` 가 ackedGives/giveOks/giveFails 집계. gid·ackedGives·giveOks·giveFails 추가. 계층: 3 게임 서비스. verify 새 모드 `guildbanksaga`. `saga` OFF→give 에 replyTo 없음→가방 echo 휴면→0514 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — saga OFF→echo 0·박스 비트 동일.
- 가설 `guildbanksaga`: 5/5 OK — gives 3·ackedGives 3·giveOks 3·giveFails 0·closed(닫힌 고리 liveness·무손실서 모든 give acked·성공).
- spine ALL OK — #46 arc 5모드(saga 회신).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 회신 집계 5시드 동일. ③권위: give 결과 수신은 관측·아이템 권위는 가방(중복 0). ④은닉: 금고↔가방 replyTo 명시 인터페이스(가방 내부 모름). ⑤headless: node in-proc. — 이상 없음(무손실 닫힌 고리만·회신 손실 시 pending 잔존은 0516).
