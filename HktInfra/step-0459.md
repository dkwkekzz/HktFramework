# step-0459 — #4 실 net.step 배리어 실제 치환 9: 다운스트림 뷰 수렴

> 직전 step-0458 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/verify.js` — `barview`: 배리어 손실+resync(업스트림 월드 입력)이 *다운스트림 뷰*에도 정합함을 단언 — 클라 AOI 뷰(seenSig)·마지막 seenTrace 가 lockstep 과 수렴(desync 0). 상류 손실을 배리어가 복원하므로 하류 뷰 스트림도 최종 일치. 코드 박스 무변경 → reg 구조적 0.
- 계층: 코디네이션(#4). 배리어 상류 손실 복원이 다운스트림 클라 뷰 수렴까지 정합.
- OFF 플래그: `asyncBarrier`(미설정→net.step 그대로 = **reg 구조적 0**). verify.js 새 모드 `barview`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `barview` 5/5 OK — 4 client·단일 존·20% 상류 move 손실: `run({asyncBarrier:{loss,resync}})` worldDigest == lockstep Y·최종 클라 seenSig == lockstep Y·마지막 seenTrace == lockstep Y·**desync 0**.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅ ③권위 단일 ✅ ④은닉 ✅(클라는 게이트웨이 뷰만) ⑤headless ✅. 이상 없음 — 상류 손실 복원이 하류 뷰 수렴(desync 0)까지 정합. asyncBarrier OFF→reg 구조적 0.
