# step-0173 — 아이템 우편 saga 재시도 상한(mailMaxRetries)

> 직전 step-0172 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`(`_resendPending`)에 gid 당 재전송 상한 — `maxRetries>0` 이면 retryCount 누적, N 도달 시 그 give 포기(pendingGive 제거→이후 sweep 비-순회·giveAbandoned++)·pending(Set) 잔존. 계층: 게임 서비스(우편)·서비스간 saga.
- 필드: `maxRetries`/`retryCount`/`giveAbandoned` + 테스트 seam `ackDropAlways`(지속 회신 손실). `svc-mail-txn.js`: item_result 핸들러에 ackDropAlways 드롭(안 지움) + ack 시 retryCount 제거. `svc-mail-persist.js` crash(): retryCount/giveAbandoned 소실. `src/topo-build.js`: `mailMaxRetries`/`mailAckDropAlways` 배선.
- OFF 플래그 `mailMaxRetries=0`(무제한 = 0172 비트 동일)·`ackDropAlways` 미사용(production 무영향). verify.js 새 모드 `mailmax`.
- 포기는 *재전송 중단*일 뿐 abort 아님(give 가 실제 성공했을 수 있어 pending 미해결 유지 = 낙관적·안전). 거래소 0131·0059 recoverMaxRetries 의 우편 판.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: maxRetries 0·ackDropAlways 미사용 = 0172 비트 동일(전 시스템 5시드) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailmax`**: 5시드 전부 — 지속 손실서 무제한 retries/abandoned=4/0(매 sweep 재전송)·상한2 2/1(2회 후 포기)·양 체제 pending 1 잔존·sagaConsistent 예.
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖·sweep 외부 주기 op — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경(재전송 멱등). ③권위 단일 소유: 포기는 pending 잔존(미해결 보존)·원장 권위 불변. ④은닉·단일 연결: 클라 와이어 무변경(saga 서비스간). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (다음: saga 포기 발행 mailAbandonPublish·거래소 0132 의 우편 판.)
