# step-0451 — #4 실 net.step 배리어 실제 치환 1: 배리어 stepper seam 투명

> 직전 step-0450 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 신규 박스 `src/async-barrier.js` — async-net(0441~0450·in-proc 등가) substrate 를 **실 `run()` 전송 배선**에 꽂는 arc 의 첫 조각(seam). `makeAsyncBarrier(net, cfg)` — run() 이 매 tick `net.step()`(중앙 lockstep 배리어) 대신 부를 stepper. 0451 은 *투명 pass-through*(net.step() 그대로 위임·아직 가로채지 않음) — 이후 step 이 월드 입력 스탬프/holdback/재정렬/resync 를 이 seam 에 얹는다.
- `src/topo-run.js` — gated 배선: `opts.asyncBarrier` ON 이면 `ab.step()`, OFF(기본·미설정)면 `net.step()` 그대로 + 루프 뒤 `ab.flush()`(투명 no-op). 계층: 코디네이션(#4).
- OFF 플래그: `asyncBarrier`(미설정→ab=null→net.step() 그대로 = **baseline 비트 동일·reg 구조적 0**). verify.js 새 모드 `barpass`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK(asyncBarrier 미설정→net.step() 경로 비트 동일).
- `barpass` 5/5 OK — 4 client·30 move·48 tick: `run({asyncBarrier:true})` worldDigest == `run({})`(lockstep) Y·logDigest == Y(투명 seam·world/log 불변).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅(stepper 는 배달 seam·tick 안 동기 작업 0) ②결정론 ✅(투명·world 불변) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅. 한계(정직): 이 조각은 *seam*만 — 아직 net.step() 에 투명 위임(월드 입력 substrate 배달은 0452~). async-barrier 는 asyncBarrier OFF 면 run() 미호출 → reg 구조적 0.
