# step-0216 — 인스턴스 플레이어 라우팅(instanceRoute·배정 SSOT)

> 직전 step-0215 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/instance.js` — `instanceRoute{player,instanceId}` → player 를 active 인스턴스에 배정(routes 맵·한 player=한 인스턴스·권위 단일 소유 척추③). 죽은 인스턴스 거부(no-op). 같은 인스턴스 재배정 멱등. 다른 인스턴스로 옮기면 release(기존)+acquire(신규) 쌍(원자 재배정). 질의 `instanceOf`/`occupancyOf`.
- 계층: 월드(계층2·인스턴스). OFF 플래그: `instanceRoute` 미주입(미발화 → 0215 비트 동일·reg 0). verify.js 새 모드 `instanceroute`.
- 2차 고도화 인스턴스 #2 — 수명주기·수요 spawn(0201~0215) 위에 게이트웨이 던전 입장 라우팅 토대.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (instanceRoute 미주입 → 0215 비트 동일).
- instanceroute: 5/5 — p1 d1→d2 재배정·d1 occ 1·d2 occ 2·routed 3/rerouted 1/reject 1(죽은 d3)·routedCount 3.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 인스턴스는 존 tick 밖·onTick 0. ②결정론: 라우팅 결정론(같은 ops→같은 배정). ③권위: 한 player 정확히 한 인스턴스(Map 단일 키·재배정=release+acquire 쌍). ④은닉: 클라 접점 무변(게이트웨이 경유). ⑤headless: Node·원격 재현. — 이상 없음.
