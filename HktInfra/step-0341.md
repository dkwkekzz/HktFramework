# step-0341 — #9 후속 capstone: 다운스트림 전파 전 정합(downstreamDeliverCoherent)

> 직전 step-0340 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 정착 술어 `downstreamSettled()` = 모든 세션 미-ack egress 버퍼가 빔(= 산출된 모든 다운스트림 frame 이 게이트웨이에 닿아 ack 됨·손실 시 gap-resync 0337/타임아웃 0338 재전송이 복구 후 정착). 읽기 전용.
- capstone(verify) = 손실(`s:a1#1`)+혼합 lifecycle(enter/move/leave/migrate) 뒤: `downstreamSettled` && `gatewayDeliveryIsolated`(0340) && 활성 세션 downSeqNext==egressSeq(무손실 인오더) && `downstreamCoherent`(z1·z2·host 뷰 0330).
- 계층: 코디네이션/엣지/월드(②④⑤). 읽기 전용 → baseline 비트 동일. verify.js 새 모드 `downdeliver`. **전파 sub-arc 0331~0341 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 술어 추가 → baseline(0340) 비트 동일.
- `downdeliver` 5/5 OK — z1@A(a1·a2)·z2@C(b1)·z1 A→B migrate·a2 leave + s:a1#1 손실 뒤: `downstreamSettled` **Y**·`gatewayDeliveryIsolated` **Y**·a1 delivered **4**==produced 4·b1 **3**==3·`downstreamCoherent` z1·z2 **Y**.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 술어는 읽기. ② 결정론: 5종 동일. ③ 권위: 다운스트림이 업스트림 보존 무영향. ④ 은닉: 각 클라 자기 세션 뷰만(게이트웨이 단일 연결). ⑤ headless: Node 순수·직렬화·reg 0.
- 한계: 전파 sub-arc 는 게이트웨이→클라(spectator addr)까지 — 실 Client 객체 수신·적용은 0334 limitation(실 host.js OS 프로세스 spawn 과 함께 후속). 이상 없음.
