# step-0200 — 길드 금고 arc capstone (bankCapstone·원장+배지 정합 결합·arc 0191~0200 닫기)

> 직전 step-0199 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `bankCapstone(feed)`: 금고 박스 전체 정합을 한 진입점으로 결합 단언 — bankConsistent()(원장 권위 단일 소유·itemId 이중 소유 0·0199) AND feed.bankFeedConsistent(this)(읽기 모델 배지==vault·0198). 외부 모니터/감사용 한 줄 건강검진. 순수 읽기(권위 0·실행 경로 무변경). 거래소 0140·우편 0180·길드 0190 capstone 의 *금고* 판. **guild bank arc(0191~0200) 닫기**.
- OFF: 순수 읽기 메서드(미호출 시 baseline 비트 동일=회귀 0). verify.js 새 모드 `guildbankcapstone`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — 메서드 추가만 → 0199 비트 동일.
- **가설 `guildbankcapstone`**: 5/5 시드 OK — 풍부한 연산(create/join/deposit/withdraw·비멤버 no-op)×세 체제(A 정상·B guild crash→reconstruct·C feed crash→reconstruct) 모두 bankCapstone true(3/3)·g1/g2 vault 2/2.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 정합 검사 존 tick 밖. ②결정론: 전 시드 3/3 true. ③권위 단일 소유: bankCapstone 이 모든 연산·체제서 아이템 권위(원장)+배지 정합 결합 단언(capstone). ④은닉: 배지는 발행 스트림 파생·SSOT 직접 접근 0. ⑤headless·원격: run.js 증명. — guild bank arc 0191~0200 닫힘(SPINE 계층3 길드 금고 박스 완성). 후속: 가방 escrow 연동(예치 시 멤버 가방 인출·인출 시 입금)·비동기 결정론.
