# step-0246 — 배치 SSOT 실배선(#51) 6: executed placeStop(존 운영 퇴역)

> 직전 step-0245 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placeStop{zoneId}` op + `_stop`: 존을 운영 퇴역 — 결정(`placement`)에서 제거 + `placeExecute` ON 이면 실 런타임(`running`)도 종료(instance.js `_despawn` 의 존 판). 드레인(host 의 *모든* 존 이주)과 달리 *그 존 자체*를 내린다. 없는 존 멱등 no-op. 계수 `stops`/`zonesRetired`. 계층:코디네이션.
- OFF: `placeStop` 미수신이면 미발화=0245 비트 동일=회귀 0(새 op).
- verify.js 새 모드 `placestopexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placestopexec`**: 5시드 모두 placed 2·running 2·z2 제거·drift 0·zonesRetired 1·stops 2 — z2 운영 퇴역 후 결정/집행 모두서 사라짐·미존 zX 멱등.
- **결정론/권위/수렴**: `run.js all` ALL OK(16모드+classics).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면 ②결정론: 시드 PRNG ③권위 단일 소유: stop 은 결정·집행 동시 제거로 drift 0 보존 ④은닉: orch 내부 ⑤headless: node 검증. — 이상 없음(executed placeAuto·런타임 crash/recover 는 0247~ 후속).
