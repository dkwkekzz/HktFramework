# step-0193 — 길드 금고 변경 발행 (guildBankPublish·svc.guild.bank.changed)

> 직전 step-0192 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `_publishBank()`: 예치/인출 *성사* 시 `svc.guild.bank.changed{guildId,kind:deposit|withdraw,itemId,member}` 버스 발행 → 발행자 무수정 소비자(audit) 관측. no-op(중복 예치·없는 인출·비멤버)은 발행 안 함(발행==실 변경). 거래소 체결 발행 0108·길드 멤버십 변경 발행 0183 의 금고 판.
- `src/topo-build.js`·`src/topo-subs.js` — `guildBankPublish` 플래그 → guild opts.bankPublish + audit `svc.guild.bank.changed` 구독(buildSubs 인자에 guildBank/guildBankPublish 추가).
- OFF: `bankPublish` OFF·bus 부재면 발행 0 = 0192 비트 동일(reg). verify.js 새 모드 `guildbankpublish`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — bankPublish OFF → 0192 비트 동일.
- **가설 `guildbankpublish`**: 5/5 시드 OK — ON pub 3==audit rx 3==실 변경(예치 2·인출 1)·OFF pub 0.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 발행 존 tick 밖. ②결정론: 5/5 동일 발행 수. ③권위 단일 소유: 발행은 관측 사본(원장 권위 불변). ④은닉: 소비자 주소 무지(버스 토픽). ⑤headless·원격: run.js 증명. — 이상 없음. 후속: bank 영속·failover(0194).
