# step-0224 — 오케스트레이터 host 드레인(placeDrain·정비/퇴역 안전 이주)

> 직전 step-0223 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/orchestrator.js` — `placeDrain{host,hosts[]}`: 정비/퇴역할 host 의 *모든* 존을 나머지 host 중 최소부하로 차례차례 이주(release+acquire 연쇄·매 존마다 최소부하 재계산=고른 분산·드레인 후 그 host 부하 0). 다른 host 없으면 보류.
- 계층: 5(코디네이션·오케스트레이터) · OFF 플래그: `placeDrain` 미수신(미발화 → 0223 비트 동일=회귀 0) · verify.js 새 모드 `placedrain`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `placedrain`: 5/5 OK — A 2존·B/C 1존 → drain A → A 0·B 2·C 2·drainMoves 2(z1→B·z2→C)
- spine ALL OK

## 척추 5항 + 한계

①tick: 오케는 존 tick 밖 제어 평면. ②결정론: placement/host 순서 tie-break·시드 PRNG. ③권위: 존마다 placement 단일 키 원자 교체(release+acquire 연쇄·공백/중복 0). ④은닉: 명시 메시지(placeDrain)만. ⑤headless: Node·원격 재현. — 이상 없음(어떤 host 도 영구 중심 아님·SPINE §2 충족).
