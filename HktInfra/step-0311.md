# step-0311 — #9 잔여: host 프로세스 부하 불균형 질의 hostLoadSkew

> 직전 step-0310 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 질의 `hostLoadSkew()` = 전 host 컨테이너 존 수 분포 {hosts, min, max, skew:max−min}. zoneHostCensus(0307·*무엇이 어디*)의 *얼마나 고른가* 판 — 오케스트레이터가 어느 host 프로세스를 비우거나 채울지 판단하는 실 단위. graceful 재배치(placeRebalance/placeDrain) 뒤 skew 수렴을 단언하는 기초.
- 계층: 코디네이션(⑤). 읽기 전용 → 0310 비트 동일. verify.js 새 모드 `hostloadskew`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0310) 비트 동일.
- `hostloadskew` 5/5 OK — A 에 몰린 배치(z1·z2·z3@A·z4@B): rebalance 없으면 skew0 **2**(A=3,B=1), placeRebalance([A,B,C]) 후 skew1 **≤1**(A=2,B=1,C=1)·총존 4 보존·hostContainerCoherent Y·zoneHostSingleOwner Y.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 질의(시뮬 외 0). ② 결정론: 시드 5종 동일·rebalance 결정론 host/zone 순서. ③ 권위: 단일 소유 유지(재배치=release+acquire). ④ 은닉: orch 내부 컨테이너 질의. ⑤ headless: Node 순수·reg 0.
- 한계: 부하 척도는 *존 수*만(entity 수·tick 비용 가중 미반영). zoneHosts 는 orch 인프로세스 논리 컨테이너(실 OS 프로세스 spawn 후속). 이상 없음.
