# step-0481 — mze2ecap grand capstone 누적 회귀 승격 (#16 라운드 2차 1)

> 직전 step-0480 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0470 의 bespoke `mze2ecap`(다중 존 이주 하 유계 resync grand capstone)을 누적 회귀 키트로 승격(MODES+ORDER 등록·본문 `run`/`worldDigest`/`check`/`pad` 지역 helper 재사용). 계층: 검증 하니스(박스 코드 무수정). OFF 플래그 없음 — 박스 `.js` 0줄 수정이므로 reg 0 자명(verify-kit·verify 는 baseline 비대상). `src/verify.js` 는 순수 셸로 정리(splice 없음·kit.cli 위임). 정리·도구 step(#16): 생성 step(0470) 한정이던 grand capstone 을 매 spine 항구화 — 옛 "수치=verify 출력" 이 HEAD 재검증 불가였던 격차 해소 시작(감사 2026-07 §2 권고 ①).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `mze2ecap`: 5/5 OK — world==lockstep·exactly-once(moveDup0·lost0·pendingAtEnd0)·뷰수렴·유계(span3<4·across0)·handoffs 4~15.
- spine ALL OK — mze2ecap 가 이제 누적 회귀에 포함(전 역사 불변 + 0461~0470 다중 존 async 유계 resync 를 현재 src 에 단언).

## 척추 5항 + 한계

①tick: 검증 모드 승급일 뿐 박스 onTick 무변(시뮬 외). ②결정론: run 결과만 단언·Math.random 0. ③권위: mze2ecap 이 world==lockstep·exactly-once 로 소유 보존 재단언. ④은닉: 영향 없음. ⑤headless: node 단언, 변동 없음. — 이상 없음(승급 라운드는 코드 무변경·검증 항구화만).
