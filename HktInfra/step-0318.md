# step-0318 — #9 잔여: host 프로세스 부하 균형 술어 hostBalanced (부하 sub-arc capstone)

> 직전 step-0317 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 술어 `hostBalanced(zoneTol=2, entTol=2)` = `hostLoadSkew().skew ≤ zoneTol && hostEntitySkew().skew ≤ entTol`. 부하 균형 sub-arc(0311~0317·skew 관측·생애주기·failover·entity 가중 배치/재배치)의 종합 술어 — 재배치 후 참·전 거짓을 단언하고, 균형 달성이 정합 불변(entityConserved·hostContainerCoherent)을 깨지 않음을 확인.
- 계층: 코디네이션(⑤). 읽기 전용 → 0317 비트 동일. verify.js 새 모드 `hostbalancecap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0317) 비트 동일.
- `hostbalancecap` 5/5 OK — z1·z2@A(각 entity 2 → A 부하 4)·z3@C(0)·entity skew 4: `hostBalanced(2,2)` **false**(bal0) → `placeRebalanceE([A,C])` 로 z1→C → entity skew **0**·`hostBalanced(2,2)` **true**(bal1)·entityConserved Y·hostContainerCoherent Y·총 entity 4.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 술어(시뮬 외 0). ② 결정론: 시드 5종 동일. ③ 권위: 균형 후에도 단일 소유·무손실(conserved). ④ 은닉: orch 내부 부하 질의. ⑤ headless: Node 순수·reg 0.
- 한계: 균형 술어는 *수* 가중만(tick 비용 미반영)·zoneTol/entTol 은 정책 파라미터. 자동 트리거(임계 초과 자동 발화)는 후속. 이상 없음.
