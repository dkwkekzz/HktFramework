# step-0281 — #56 브리지 존 데이터 평면 1: enter 흐름(실 EntityZone 핸들로 entity 라우팅)

> 직전 step-0280 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_bridgeEnter(zoneId, avatar, sessionId, gateway)`: 실 EntityZone 핸들(`zoneRuntimes.get(zoneId).zone`)에 `onMsg('enter')` 호출 → 실제 avatar 가 그 존의 `ents` 에 산다(미가동 존 거부·멱등). 질의 `zoneEntityCount`/`zoneHasEntity`(실 zone.js ents 직접 읽기). 계층: 코디네이션↔월드(데이터 평면).
- `src/orch-control.js` `onMsg` — `zoneEnter` 분기(`zoneEntityFlow` 가드 후 `_bridgeEnter`). `src/orchestrator.js` — `zoneEntityFlow` 플래그 + `zoneEnters` 계측. `src/topo-build.js` — `zoneEntityFlow` opt 배선. `src/topo-inject.js` — `entityOps` 주입열(게이트웨이→orch).
- OFF 플래그 `zoneEntityFlow`(OFF→0280 비트 동일=회귀 0·zoneBridge OFF 면 런타임 자체 0). verify.js 새 모드 `zoneenter`.
- 배경: 0272~0280 의 `zoneRuntimes` 는 *빈 핸들*(entity 0·onTick 0)이라 migrate "상태 보존"이 구조적이되 행동적이지 않았다(리뷰 0271~0280 #56). 이 step 이 entity 데이터 평면을 시작한다.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zoneenter` 5/5: z1·z2 배치 후 a1·a2→z1·a3→z2 enter → 실 zone.js ents `z1cnt=2`·`z2cnt=1`·`zoneHasEntity` Y. 미가동 존 z9 enter 거부(`z9cnt=0`). `zoneEnters=3`(수락분·미가동 거부 제외).
- `node run.js spine` ALL OK(src 누적 회귀·전 역사 불변 현재 코드 단언).

## 척추 5항 + 한계

- ①신성한 tick: enter 라우팅은 orch 제어 평면(onMsg)·존 tick 밖 동기 Map 연산. ②결정론: 런타임 시드=`fnv1a(zoneId)`·`Math.random` 0. ③권위 단일 소유: enter 는 한 존 핸들에만 적재(다중 소유는 0287 에서 단언). ④은닉: orch 가 zone.js 직접 import 0(팩토리 주입)·게이트웨이는 zoneEnter 메시지로만. ⑤headless: 실 EntityZone 헤드리스 인스턴스·UE 0.
- 정직한 한계: entity 트래픽이 게이트웨이→orch→runtime 경로다(orch 가 런타임 핸들 보유·#51b 구조). 실 아키텍처의 게이트웨이→실 존 직접 enter 는 #9(실 프로세스 분리) 후속. move/leave·migrate 무손실·hostdown 소실은 이 arc 후속(0282~).
