# step-0506 — mailsagareadmitpub 재admission 발행 E2E 관측 (#16 라운드 4차 6)

> 직전 step-0505 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagareadmitpub`: 재admission 발행 `mailReadmitPublish`+audit. 재admission 시 `svc.mail.saga_readmitted` 1회 발행 → 버스 → audit 수신. `readmitPublished==readmitted`(1:1) AND `audit.seen('svc.mail.saga_readmitted')==readmitted`. 포기 발행(0504)의 짝 — 복구 사건도 은닉 통신으로 가시. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 6/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagareadmitpub`: 5/5 OK — readmitted 1·readmitPublished 1·auditSeen 1·sagaConsistent+liveness Y. 재admission 발행 == 재admission 수 == 감사 수신.
- spine ALL OK — 손실 체제 6모드 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 발행/수신 5시드 동일. ③권위: 재admission 발행은 관측일 뿐 상태 변경 아님. ④은닉: 우편→버스→audit 토픽 pub/sub. ⑤headless: node in-proc. — 이상 없음(포기 0504·재admission 0506 발행 대칭 — saga 수명주기 발행 삼종 중 둘 완비·종결 발행은 0508).
