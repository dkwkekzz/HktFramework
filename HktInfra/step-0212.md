# step-0212 — 캐시 무효화(cacheInvalidate·write 시 캐시 일관성)

> 직전 step-0211 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheInvalidate{key}` → 소스(SSOT) 변경 통지에 캐시 사본을 끊는다(store/setAt 제거). 다음 get 은 miss → read-through 로 fresh 값 재적재. stale 사본을 즉시 차단(write 시 캐시 일관성). 없는 키는 멱등 no-op.
- 계층: 데이터(계층6). OFF 플래그: `cacheInvalidate` 미주입(미발화 → 0211 비트 동일·reg 0). verify.js 새 모드 `cacheinval`.
- 2차 고도화 캐시 박스 #2 — TTL(0211·시간 기반 회수) 위에 명시적(소스 기반) 무효화.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (cacheInvalidate 미주입 → 0211 비트 동일).
- cacheinval: 5/5 — k1 stale set → invalidate → get(k1) miss → 소스 fresh('v1-fresh') 재적재. invalidated 1·misses 1·k2 생존.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 캐시는 존 tick 밖·onTick 0. ②결정론: 결정론 reducer·시드 의사난수만. ③권위: 소스가 SSOT·캐시는 사본(무효화는 사본을 SSOT 로 재동기). ④은닉: 클라 접점 무변. ⑤headless: Node·원격 재현. — 이상 없음.
