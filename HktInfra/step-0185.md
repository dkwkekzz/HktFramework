# step-0185 — 길드 저널 스냅샷 압축(guildSnapshot·snapshot+tail replay)

> 직전 step-0184 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `_journalChange` 에 압축: snapInterval 개 변경마다 현재 로스터 스냅샷(upToSeq)+그 이하 저널 가지치기(tail 유계). `reconstruct` 가 스냅샷에서 출발해 tail(seq>upToSeq)만 replay → 전체 저널 replay 와 비트 동일(무손실). snapshot/snapshots 계측. 파티 0086 의 길드 판. 계층: **6 데이터**(영속 압축).
- 배선: `topo-build.js`(`guildSnapshot` 플래그·snapInterval: guildPersist?guildSnapshot:0).
- OFF 플래그 `guildSnapshot=0`(0 → 압축 0·무계 저널 = 0184 비트 동일=회귀 0) · verify.js 새 모드 `guildsnap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildSnapshot 0 → 0184 비트 동일.
- **가설 `guildsnap`**: 5/5 시드 OK — full 저널 8 → snap tail 2(유계)·snapshots 2·스냅샷+tail digest `2f062538` == 전체 저널 digest == 죽기 전(무손실).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 압축은 존 tick 밖. ②결정론: 전 시드 동일 digest·tail 2. ③권위 단일 소유: 스냅샷이 master 보존→single-master 불변. ④은닉: 자기 저널 압축·외부 무지. ⑤headless·원격: Node·run.js 증명. — 이상 없음.
