# step-0322 — #9 후속: host 산출 뷰의 상호 가시 (AOI enter 델타)

> 직전 step-0321 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — 읽기 전용 질의 `zoneViewEntered(zoneId, sessionId)` = 그 세션 뷰들의 enter 를 누적한 id 집합(언젠가 한 번이라도 AOI 안에서 본 entity). 두 avatar 가 가까워지면 서로의 집합에 들어온다(상호 가시·동적 enter 델타).
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0321 비트 동일. verify.js 새 모드 `hostzonemutual`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0321) 비트 동일.
- `hostzonemutual` 5/5 OK — a1(5,5)·a2(13,15) 처음 반경 4 밖 → a1 을 (1,1)씩 6회 이동(→(11,11)·a2 와 Chebyshev 4=반경 안): `zoneViewEntered(s:a1)` ⊇ a2·`zoneViewEntered(s:a2)` ⊇ a1(상호 가시·enter 델타)·최종 `zoneVisibleIds` 둘 다 [a1,a2].
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 존 시드 고정 → 위치·뷰 5종 동일. ③ 권위: 권위 존이 동적 AOI 뷰 산출. ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·reg 0.
- 한계: 정적 enter 만(멀어질 때 exit 델타는 0323)·아직 클라 전파 미연결. 이상 없음.
