# step-0261 — 정리(#49 wiring): topo-run 제어 평면 주입열 분리

> 직전 step-0260 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/topo-run.js`(35.9KB>30KB 트리거) → `run()` 의 *per-tick 제어 평면 메시지 주입열*을 새 박스 파일 `src/topo-inject.js`(`applyInjections(opts, i, ctx)`)로 분리. 계층: 배선(wiring·인프로세스 구동 루프). **OFF 플래그 없음 — 투명 분할**(코드 위치만 이동·동작 비트 불변).
- 옮긴 주입 핸들러(verbatim): `rankDie`·`rankStall`·`producerInject`·`presenceFailover`·`whispers`·`wrouterRestart`·`mboxStraggler`·`mboxDrain`·`mboxDrainAck`·`presAnnounceStraggler`·`whisperAnnounceStraggler`·`exchangeOps`·`invOps`·`mailOps`·`mailFeedQuery`·`parties`·`partyCreate`·`partyOps`·`partyTo`·`guildOps`·`instanceOps`·`placementOps`·`cacheOps`·`worldOps`·`loginOps`·`inject`. 잔류(run 루프): 빌드/핸들 셋업·crash/failover 복구 주입(persistRestart·invRestart·busRestart 등)·net.step·trace 집계.
- 배선: topo-run.js 가 `__p('topo-inject')` 1줄 require · 루프 전 박스 핸들 묶음 `__ctx` 1회 구성 · 루프 안은 `applyInjections(opts, i, __ctx)` 한 줄 위임. verify.js 새 모드 `injsplit`.
- 결과 크기: topo-run.js 35.9KB→22.7KB(<30KB)·topo-inject.js 15.3KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `injsplit`(가설): 5/5 — instanceOps(인스턴스 spawn 2)+cacheOps(캐시 set k1/k2)를 한 시나리오로 동시 구동 → active 2·cache k1=v1·k2=v2. 옮긴 주입 핸들러가 정규 박스에 도달(위임 무결).
- spine: ALL OK(전 역사 누적 회귀 + injsplit 편입).

## 척추 5항 + 한계

①tick: 주입열은 net.step *직전* 제어 평면(존 tick 밖)·무변경 ②결정론: 코드 이동만·시드 동일 해시 ③권위: 주입 경로 무변경(단일 소유 보존) ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
