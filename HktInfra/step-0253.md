# step-0253 — 캐시 bulk get (cacheMget)

> 직전 step-0252 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheMget{keys[]}` 핸들러 추가. 계층: 데이터(캐시). 플래그 없음(새 메시지 타입·미수신→0252 비트 동일=reg 0).
- 메커니즘: 여러 키를 cacheGet(0206)과 동일 로직(hit/miss/read-through·setAt·lruTouch·hits/misses 계측)으로 일괄 조회 → `cacheMReply{values[]}` 한 회신(라운드트립 N→1·플레이어 N명 핫데이터 배치 페치). `_lastMget` 보관(검증용).
- verify.js 새 모드 `cachemget`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(cacheMget 미수신 → 비트 동일).
- `cachemget`(가설): 5/5 — set k1=v1,k2=v2·소스 k3=s3·k4 없음 → mget[k1,k2,k3,k4] = [v1,v2,s3,undefined]·hits 2·misses 2(store hit 2 + source read-through 1 + 미존재 1).
- spine: ALL OK(누적 회귀 + cachemget 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: 읽기 경로(소스 SSOT 미변경) ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음.
