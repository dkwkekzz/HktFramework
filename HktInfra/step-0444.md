# step-0444 — #4 실 net.step 배리어 치환 4: 실 actor.onMsg 디스패치

> 직전 step-0443 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `makeZoneActor(seed, avatars)`(실 Net 계약 `onMsg(m){ m.payload.type==='intent' → 동결 sim tick }`) + `deliverToActor(orderedMsgs, actor)`. net.step() 이 하던 두 일 중 **배달**(actor.onMsg)을 중앙 배리어에서 떼어낸다: 존 메일박스 holdback 방출열을 실 수신 actor 로 흘려, 전역 FIFO 큐 대신 substrate 재구성 전순서로 배달 → actor 의 실 sim 상태 == canonical.
- 계층: 코디네이션(#4). net.step 배달 절반을 substrate 로 치환(진행 onTick 은 아직 lockstep — 0447).
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netdispatch`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netdispatch` 5/5 OK — 4 client·40 intent·K=6 인터리빙: 실 actor.digest == canonical Y·applied == 40(전 intent 배달) Y·전 인터리빙 불변 Y.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅(디스패치는 tick 밖 제어) ②결정론 ✅ ③권위 단일 ✅(존 actor 1) ④은닉 ✅(실 Net from/to/payload 계약) ⑤headless ✅(검증 전용). 한계(정직): actor 진행(onTick)은 아직 lockstep — 배리어-free 진행은 0447. 손실 미도입(0445). async-net 은 run() 밖 substrate.
