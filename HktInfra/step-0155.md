# step-0155 — MailFeed 회계 정합 capstone(feedConsistent — unread==sent−read−expired)

> 직전 step-0154 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mailfeed.js` — `feedConsistent()` 단언용 읽기 accessor: 모든 수신자에 `unread == sent − read − expired`·`unread≥0`(공백·중복 0). 0151~0154 가 더한 배지 전이(입금·읽음·만료·replay)의 *창발 불변*을 단언. 0150 mailConsistent(우편 박스 권위 판)의 읽기 모델 판. 계층:게임 서비스.
- 미호출 accessor → 0154 비트 동일(회귀 0·코드 더하지 않고 불변 단언·0150·0140 capstone 패턴). verify.js 새 모드 `exmlfcons`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlfcons`(4체제: 수령만·만료만·혼합·crash 복구): 4체제 feedConsistent 예(4/4)·feed totalUnread==우편 totalHeld·crash→reconstruct 후 정합+digest 보존 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — *바로 이것을 단언*(배지 미읽음=입금−읽음−만료·분할 보존) ④은닉 배지 내부 읽기 ⑤headless `node run.js` 통과 — 이상 없음. MailFeed arc(0151~0155) 닫힘 — 거래소 MarketFeed(0112~0116)와 동형 읽기 모델 골격. 배지 질의 인터페이스(0156)가 다음 자연 확장.
