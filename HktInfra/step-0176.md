# step-0176 — 아이템 우편 saga 포기 give 재admission(mailReadmit)

> 직전 step-0175 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`: `_readmit()` — 포기(abandonedGive)된 give 를 pendingGive 로 되돌리고 retryCount 리셋(상한 재충전·readmitted++). `_resendPending` 포기 분기에 `abandonedGive.set(gid, g)` 추가(재admission 소스 간직). 계층: 게임 서비스(우편)·서비스간 saga.
- 필드: `abandonedGive`/`readmitted`. `svc-mail-txn.js`: `mailReadmit` op → `_readmit()`. `svc-mail-persist.js` crash(): abandonedGive/readmitted 소실.
- OFF 게이트: mailReadmit op 부재·포기 미발생 = 0175 비트 동일(abandonedGive.set 은 cap+지속손실서만·reg 시나리오 무영향). verify.js 새 모드 `mailreadmit`. 거래소 0134·0048 busLeaseLife 재admission 의 우편 판.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: readmit op 부재·포기 미발생 = 0175 비트 동일(전 시스템 5시드) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailreadmit`**: 5시드 전부 — 포기 후 ON readmitted/abandonedGive=1/0(재무장)·OFF 0/1(잔존)·giveAbandoned 1·양 체제 sagaConsistent 예.
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖·readmit 외부 op — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경(재전송 멱등). ③권위 단일 소유: 재admission 은 pending 재무장·원장 권위 불변. ④은닉·단일 연결: 클라 와이어 무변경(saga 서비스간). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (한계: 무한 abandon↔readmit 루프 가능 — readmitMax 차단 후속·거래소 0137 의 우편 판. 다음: 재admission 발행 mailReadmitPublish·거래소 0135.)
