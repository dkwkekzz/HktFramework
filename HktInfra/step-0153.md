# step-0153 — MailFeed 만료 반영(mailFeedExpire — svc.mail.expired 구독→unread--)

> 직전 step-0152 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mailfeed.js` — MailFeed 가 `svc.mail.expired`(0149)도 소비: 그 수신자 `unread--`·`expired++`. `expiredOf` accessor 추가. 미수령 만료 우편이 배지에서 사라진다 → 회계 unread==sent−read−expired 로 닫힘. 계층:게임 서비스.
- 배선: `topo-build.js`(`mailFeedExpire` 플래그·buildSubs 전달) · `topo-subs.js`(`mailFeedExpire && mailExpirePublish`→`svc.mail.expired`→mailfeed 구독).
- OFF 플래그 `mailFeedExpire`(기본 false) → expired 토픽 미구독 = 0152 비트 동일(회귀 0). verify.js 새 모드 `exmlfexp`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · 권위 보존 · spine ALL OK.
- 가설 `exmlfexp`(h1 3통 입금+수령·h2 2통 입금+TTL10 sweep 만료): h1 u/r/e=0/3/0·h2=0/0/2·totalUnread=0·unread==sent−read−expired(`예`) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 반응형(onTick 0) ②결정론 난수 0 ③권위 우편함 권위 불변 ④은닉 버스 구독만 ⑤headless `node run.js` 통과 — 이상 없음. 0151~0153 으로 배지가 입금(+)·읽음(−)·만료(−) 전 수명주기 반영. 단언은 0155 capstone(mailFeedConsistent)에서 모든 체제로 닫는다.
