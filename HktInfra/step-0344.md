# step-0344 — #9 후속: 손실 하 다운스트림 클라 수렴(재전송 후 desync 0)

> 직전 step-0343 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- **통합 검증 step(src 무변경)** — 전 신뢰 스택(0337 gap-resync + 0338 타임아웃 재전송 + 게이트웨이 인오더 게이팅)이 실 `DownClient`(0342)에게 *손실에도* 클린 인오더 스트림을 배달해 host 권위 AOI 로 수렴(desync 0)함을 단언. 새 src 메커니즘 없음 → src == baseline 비트 동일.
- 계층: 월드/엣지(②⑤·수렴 desync 0). verify.js 새 모드 `dcloss`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — src 무변경 → baseline(0343) 비트 동일(자명).
- `dcloss` 5/5 OK — a1@dc0 4회 이동(dseq 0..4) 중 `s:a1#2` 손실(`zoneEgressDropped` **1**)에도: gap-resync(`gatewayResyncsSent` **1**) 복구 → 게이트웨이 인오더 게이팅 → `dc0.seenSig` == `zoneAuthSig('z1','a1')`(**desync 0**).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 전부 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 클라 읽기전용 뷰가 권위 재현으로 수렴(손실 복구 후). ④ 은닉: 클라는 게이트웨이만. ⑤ headless: Node 순수·reg 0.
- 한계: 통합 검증(새 메커니즘 0) — 0337·0338·0342 의 합성. 인프로세스 DownClient. 이상 없음.
