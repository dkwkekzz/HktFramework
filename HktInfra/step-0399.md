# step-0399 — #66+#67 통합 정합 술어: unifiedCoherent()

> 직전 step-0398 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `unifiedCoherent()` 술어: `syncedCoherent()`(0390·#65/#66: maxDesync0 && coordDesync0(lost 제외) && placementCoherent) && `authoritiesAgree()`(0395·#67: 코디네이터 placement == orch 집행 where-view). 즉 *양방향 동기·tick placement-aware·orch 이중 권위 합류*가 모두 한 몸. orch.running 에 stale host 가 남으면 authoritiesAgree 가 실측 검출(N) — by-construction 아님. 계층: 코디네이션(#66+#67).
- 읽기 전용·새 메서드. verify.js 새 모드 `coordunified`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 23.9KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordunified 5/5**: run(5)+migrate+failover → unifiedCoherent **Y** — 대조로 orch.running 에 stale host(hostGHOST) 주입 시 **N**(authoritiesAgree 실측 검출).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 술어는 시뮬 tick 밖 ②결정론: 시드 5종 동일 ③권위: 단일 술어가 양방향 동기+두 where 권위 합치를 동시 단언 ④은닉: orch 공개 Map 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: 이번엔 lifecycle 을 run 루프 *뒤*에 적용 — mid-run migrate(루프 *중*)+failover 복합 capstone 은 0400(unifiedCoherent 로 #66+#67 종합·0391~0400 닫기).
