# step-0189 — 마스터 이양(guildTransfer·single-master 보존 쌍 거래)

> 직전 step-0188 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `guildTransfer{guildId,from,to}` 핸들러: from 이 현재 master·to 가 멤버일 때만 master 원자 교체(release+acquire 쌍 거래·공백 0·이중 0). from 잔류·로스터 크기 불변. 거부(to 비멤버·from 비마스터) no-op. 이양 발행(kind 'transfer'·GuildFeed 무시=배지 불변)·저널(reconstruct 에 transfer replay). transfers 계측. 존 핸드오프 0006·escrow 0117 의 마스터십 판. 계층: **3 게임 서비스**.
- OFF: guildTransfer 미주입 → 휴면 → 0188 비트 동일(회귀 0) · verify.js 새 모드 `guildtransfer`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildTransfer 미주입 → 0188 비트 동일.
- **가설 `guildtransfer`**: 5/5 시드 OK — transfers 3(성사 1+거부 2)·최종 master c1·from x 잔류·로스터 {c1,c2,x} 불변·crash→reconstruct master c1 보존·single-master 보존.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 이양 존 tick 밖. ②결정론: 전 시드 동일 결과. ③권위 단일 소유: release+acquire 쌍 거래로 master 원자 교체(공백/이중 0·거부 no-op)→single-master 항상 보존. ④은닉: 명령 계약만·내부 무지. ⑤headless·원격: Node·run.js 증명. — 이상 없음.
