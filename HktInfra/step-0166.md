# step-0166 — 아이템 우편 발신 실패 보상(mailCompensate — give 실패→우편 롤백·phantom 0)

> 직전 step-0165 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `compensate`(mailCompensate) opt + `_gidRef`(gid→발신 우편 참조) + `_compensateSend(ref)`: 발신 leg(cause=mailsend) give 가 실패 회신하면 그 우편을 롤백(box 제거·`sent--`·`itemSent--`·`compensated++`). `_custody` 가 발신 시 `ref={kind:'send',mailId,rcpt}` 보관·`onMsg` item_result 실패 분기서 보상. 거래소 0122 exchCompensate(list 인출 실패→매물 abort)의 우편 판. 계층:게임 서비스(우편↔가방 saga).
- 배선: `topo-build.js` — `mailCompensate` 플래그·`compensate` 를 mail opts 로.
- OFF 플래그 `mailCompensate`(기본 false)·give 성공 → 롤백 0 = 0165 비트 동일(회귀 0). verify.js 새 모드 `exmlcomp`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlcomp`(x 가 item0 만 소유·item0+itemX 발신): sent=1(itemX 롤백)·itemHeld=1·ackedFail=1·compensated=1·h1 우편함=1(phantom itemX 없음) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0 ③권위 가방 단독(우편 적재 롤백은 우편 박스 자기 상태·아이템 권위 불변) ④은닉 item_result 회신 경유 ⑤headless `node run.js` 통과 — 이상 없음. 보상은 *발신 leg(0161)만* — 수령(0162)·만료 반환(0163) leg 실패 보상은 미구현(거래소도 list-leg 만 보상·buy/cancel-leg 은 무대 드묾). 롤백은 우편이 *아직 보유(held)* 일 때만(수령 전 실패 회신 도착 전제) — 회신 지연/손실 시 재전송(0167)·dedup(0168) 필요.
