# step-0166 — 아이템 우편 saga 회신 비동기 수신(mailSaga·ackedGives)

> 직전 step-0165 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail-core.js` — `_custody` 가 saga ON 이면 give 에 `replyTo`(우편 주소)+`cause` 동봉 → 가방 `_sagaReply` 가 item_result 를 우편으로 echo. 계측 `ackedGives`·`giveOks`·`giveFails`. `src/svc-mail-txn.js` — onMsg 가 `item_result{op:'give'}` 수신 → 집계. 거래소↔가방 saga(0121)의 우편 판. 계층:게임 서비스.
- `src/topo-build.js` — mail 박스에 `saga`(mailSaga) 배선. OFF 플래그 `mailSaga`(기본 false) → replyTo 부재·가방 echo 휴면 = 0165 비트 동일(회귀 0). verify.js 새 모드 `exmlsaga`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlsaga`(3 레그 자극·give 4건): ON gives/acked/oks/fails 4/4/4/0(닫힌 고리 gives==ackedGives)·OFF acked 0(fire-and-forget) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖) ②결정론 난수 0 ③권위 가방 원장 권위·우편은 회신 집계만(쓰기 0) ④은닉 item_result 명시 인터페이스(replyTo) ⑤headless `node run.js` 통과 — 이상 없음. 회신 *손실*은 아직 무대비 — 잃은 회신의 give 가 ackedGives<gives 로 새는 것(미해결 추적·gid·재전송)은 후속(0167~·거래소 0125~0126 류). 현재 무손실 가정. give↔가방 transfers capstone(giveOks==escrowXfers 0170) 후속.
