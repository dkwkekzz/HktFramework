# step-0470 — grand capstone: 다중 존 이주 하 유계 resync E2E (0461~0470 닫기)

> 직전 step-0469 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 코드(박스) 무변경 — verify 모드 `mze2ecap`(grand capstone) 추가. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`.
- 다중 존(grid24·zones2) 결합 loss+delay+핸드오프를 한 시나리오로 돌려 arc 전체를 단언: ⒜ **world==lockstep**(유계 resync·이주 타이밍 불변) ⒝ **exactly-once**(moveDup0·lost0·pendingAtEnd0) ⒞ **다운스트림 desync 0**(전 클라 뷰==lockstep) ⒟ **유계 resync**(maxSpan<horizon·deferredAcrossHandoff0·deferN>0·handoffs>0).
- #4 완전 async 전환 sub-arc(0461~0470) 닫기.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → net.step·baseline 비트 동일).
- `mze2ecap` 5/5 OK — world **Y**·exactly-once **Y**·뷰수렴 **Y**·유계 span3<4·deferredAcrossHandoff **0**·handoffs 4~15 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: 결합 섭동 하 world/뷰 lockstep 재현 ③권위 단일: exactly-once·이주 무손실 ④은닉: 클라 게이트웨이 뷰만·barrier 존 읽기 ⑤headless 이상 없음.
- 한계(정직): 유계 resync 는 *interior* 섭동만 흡수 — 경계 근처 move 는 즉시 배달(async 안 됨). 완전-ON(항상 async·경계 포함)은 lockstep 등가가 불가능(lockstep 자체가 이주 경계 move-drop 을 타이밍 의존적으로 함·0461 발견) → interior 유계가 이 모델의 정합 최대치. 잔여 후속: 실 host.js child 경계 업스트림(#70).
