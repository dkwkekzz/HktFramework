# step-0456 — #4 실 net.step 배리어 실제 치환 6: move exactly-once 회계

> 직전 step-0455 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-barrier.js` — exactly-once 회계 계수(`moveDeliv`=존에 고유 배달·`moveDup`=이미 배달된 move 재배달 시도). 손실+resync 하에서도 배리어가 move 를 존에 *정확히 한 번* 배달했나를 단언 — moveDeliv == 발신 move 수·moveDup 0. net.delivered dedup 이 중복 적용을 막고 resync 가 유실을 메워 닫힌 장부.
- 계층: 코디네이션(#4). 배리어 손실 복원이 exactly-once(중복 0·유실 0)임을 회계로 확증.
- OFF 플래그: `asyncBarrier`(미설정→net.step 그대로 = **reg 구조적 0**). verify.js 새 모드 `baraccount`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `baraccount` 5/5 OK — 4 client·단일 존·20% move 손실: `run({asyncBarrier:{loss,resync}})` worldDigest == lockstep Y·moveDeliv **120** == 발신 move **120**·moveDup **0**(exactly-once).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅ ③권위 단일 ✅(exactly-once·중복 적용 0) ④은닉 ✅ ⑤headless ✅. 이상 없음 — 손실+resync 하에서도 move 발신==적용(닫힌 장부). asyncBarrier OFF→reg 구조적 0.
