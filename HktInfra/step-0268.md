# step-0268 — 정리(#49 인접·선제): svc-mail-core saga 헬퍼 믹스인 분리

> 직전 step-0267 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`(25.3KB·30KB 근접·성장 박스 선제 정리) → `MailService` 의 *saga 헬퍼*(`_custody`·`_resendPending`·`_readmit`: 우편↔가방 escrow custody 레그 + 미해결 give 재전송/상한/포기 + 재admission)를 새 박스 파일 `src/svc-mail-saga.js` 믹스인으로 분리. 계층: 게임 서비스(우편). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변). 0171 txn/persist 증강과 같은 패턴.
- 배선: svc-mail-core.js 가 `require('./svc-mail-saga.js')` 1줄 · 클래스 정의 직후 `Object.assign(MailService.prototype, MailSaga)`. _custody 는 txn(onMsg)이 호출. 잔류(코어): 생성자·_box·accessors(held·totalHeld·mailConsistent·sagaConsistent·digest 등). verify.js 새 모드 `mailsplit`.
- 결과 크기: svc-mail-core.js 25.3KB→19.9KB·svc-mail-saga.js 6.8KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `mailsplit`(가설): 5/5 — _custody(escrow give→pending 1·gives 1·escrow 진입)·_resendPending×2(상한 도달 포기·giveAbandoned 1)·_readmit(재admission·readmitted 1) 각 단계 sagaConsistent 유지. 옮긴 saga 헬퍼 무결.
- spine: ALL OK(전 역사 누적 회귀 + mailsplit 편입).

## 척추 5항 + 한계

①tick: saga 헬퍼는 존 tick 밖 우편 제어 평면·무변경 ②결정론: 코드 이동만·시드 동일 ③권위: escrow custody 단일 소유·sagaConsistent 보존 ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
