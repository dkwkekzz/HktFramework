# step-0388 — #65 양방향 동기 8: syncPlan 이 placement 권위 기준

> 직전 step-0387 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `syncPlan()` 이 orch.hostSpawnPlan 대신 *코디네이터 placement*(실 위치)로 host→존 차분. 0377 syncPlan 은 orch plan 으로 차분 → migrate 후엔 stale(z1@A)이라 잘못된 host 에 복원하려 들었다. placement(실 위치)로 차분해 *옳은* host 에 누락 존 복원. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordsync2`. 새 박스·run() 미사용 → reg 0(정상 경로 결과 동일).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordsync2 5/5**: run(3)+migrate z3(빈 존) A→B → z3 drift(hostB zonedel)→ syncPlan → z3 **hostB 복원**(onB Y)·**hostA 미복원**(onA N·orch plan 따랐으면 잘못 hostA)·coordDesync 0.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: reconcile 은 tick 밖 ②결정론: placement 정렬 차분 ③권위: placement(실 위치) 기준 복원으로 단일 소유 유지 ④은닉: cluster API 만 ⑤headless: 실 host. — **한계**: syncPlan 은 토폴로지 복원만(entity 상태 미복구) — populated 존 drift 시 entity 손실(0377 와 동일·영속 후속), 그래서 빈 존 z3 로 검증.
