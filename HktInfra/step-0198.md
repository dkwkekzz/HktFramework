# step-0198 — 길드 금고 배지 정합 capstone (bankFeedConsistent·배지==vault 크기)

> 직전 step-0197 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guildfeed.js` — `bankFeedConsistent(guildSvc)`: 금고 배지 정합 불변 명시 단언 — 전 길드 `bankCountOf(id) == vault 크기 bankOf(id).length`(고아 배지 0·누락 0). 순수 읽기(권위 0·실행 경로 무변경). 0188 멤버 배지 정합(feedConsistent)의 금고 판.
- OFF: 순수 읽기 메서드(미호출 시 baseline 비트 동일=회귀 0). verify.js 새 모드 `guildbankfeedconsistent`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — 메서드 추가만 → 0197 비트 동일.
- **가설 `guildbankfeedconsistent`**: 5/5 시드 OK — g1/g2 vault 2/2·정상(A)·guild crash→reconstruct(B)·feed crash→reconstruct(C) 모두 배지==vault(3/3).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 정합 검사 존 tick 밖. ②결정론: 5/5 동일. ③권위 단일 소유: 배지는 vault 권위와 독립한 사본. ④은닉: 배지==vault 대조(SSOT 직접 접근 0). ⑤headless·원격: run.js 증명. — 이상 없음. 후속: bank 원장 정합(0199·itemId 단일 길드 소유)→arc capstone(0200).
