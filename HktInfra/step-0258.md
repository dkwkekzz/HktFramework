# step-0258 — 캐시 stats 관측 (INFO)

> 직전 step-0257 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheStats{}` 질의 핸들러 + `hitRate()`·`stats()` 읽기 accessor. 계층: 데이터(캐시). 플래그 없음(새 메시지 타입·순수 읽기·미수신→0257 비트 동일=reg 0).
- 메커니즘: cacheStats 질의 → `cacheStatsReply{stats}`(hits·misses·hitRate·size·sets·evicted·capEvicted·negHits·deleted) 회신(운영 대시보드가 캐시 효율 폴링). Redis INFO 더미판. 쓰기 무변경.
- verify.js 새 모드 `cachestats`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(cacheStats 미수신 → 비트 동일).
- `cachestats`(가설): 5/5 — set k=v·get k(hit)·get k(hit)·get x(miss) → hits 2·misses 1·hitRate 2/3(0.667)·size 1.
- spine: ALL OK(누적 회귀 + cachestats 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: 순수 읽기(관측·쓰기 0) ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음.
