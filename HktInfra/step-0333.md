# step-0333 — #9 후속: 다운스트림 — 게이트웨이가 zoneView 수신(세션별 버퍼)

> 직전 step-0332 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway-msg.js` onMsg — `m.from === 'orch'` 분기에 `zoneView` 추가 → `_recvZoneView(p)` 호출. orch egress 한 host 산출 AOI 뷰(zoneView·zoneId·sessionId·frame)를 게이트웨이가 받는다(존→게이트웨이 경로 완성).
- `src/gateway.js` — `_recvZoneView`: frame 을 세션별 버퍼 `zoneViewIn`(sessionId→[frame]) 에 적재(도착 순서 보존·sessionId 없으면 무시). 질의 `gatewayViewsFor`/`gatewayDownstreamCount`/`gatewayViewSessions`·계측 `zoneViewsRx`.
- 계층: 엣지/월드(④⑤·SPINE §4 경로2 다운스트림의 게이트웨이 종단). 플래그 없음 — `zoneEgress` OFF 면 zoneView 가 영영 안 옴(분기 dead) = baseline 비트 동일. verify.js 새 모드 `gwdown`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneEgress OFF → zoneView 미수신 → baseline(0332) 비트 동일.
- `gwdown` 5/5 OK — z1@A 에 a1·a2 enter+이동 뒤 `gatewayDownstreamCount` **4** == `orch.zoneEgressCount` 4(egress→게이트웨이 무손실)·세션 **2**(s:a1·s:a2 각 frame 2).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 게이트웨이 수신은 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 수신·버퍼링이 업스트림 권위 무영향. ④ 은닉: orch→게이트웨이 명시 메시지(클라는 게이트웨이만 봄). ⑤ headless: Node 순수·frame 직렬화 가능·reg 0.
- 한계: 게이트웨이가 *받아 버퍼링*까지 — 세션→실 클라 라우팅·전달은 후속(0334+). 브리지 세션(s:avatar)은 registry validate 경로 밖이라 bySession 에 없다(세션→클라 디렉토리는 후속). 이상 없음.
