# step-0411 — #62 코드 합류 1: coordSetup — 코디네이터 zone-cluster 배선 단일 함수

> 직전 step-0410 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `coordSetup(opts, deps)` 추가: verify 가 0401~0410 내내 손으로 짜던 4단 배선(`deps.run(opts)`→orch/orch.clusterDriver→`new Cluster([])` spawn→`makeClusterCoordinator`)을 한 함수로 흡수. 반환 `{orch, cluster, coord, drv}`. `require('./cluster-coord.js')` 1줄 추가.
- 계층: 코디네이션(#62 — 옛 runMulti zone-cluster 복원력을 cluster-run.js 소유 단일 진입점으로 합류하는 첫 조각).
- OFF 플래그: 미부착(verify 만 호출·runMulti 옛 경로 불변·run() 데이터 평면 미수정) → 회귀 0.
- verify.js 새 모드: `coordsetup`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일 — coordSetup 미부착·새 require 는 run 비-침습).
- 결정론 전파·권위/수렴: 기존 모드 전부 통과(e2e/repro/sacred ALL OK).
- 가설 `coordsetup`: 5/5 — coordSetup 으로 구성한 coord.run(6) 후 placement==orch plan Y·maxDesync 0·coordDesync 0 (손배선 0410 과 동치).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉 ⑤headless·원격 검증 — 이상 없음(coordSetup 은 in-proc 권위를 *읽어* 실 cluster 를 수렴시키는 배선일 뿐·시뮬 침습 0·실 child_process 검증).
