# step-0410 — #62 runMulti 합류 9·grand capstone: runMultiCoherent 종합 복원력

> 직전 step-0409 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `runMultiCoherent()` grand capstone 술어: `unifiedCoherent`(데이터 평면+두 where 권위 한 몸·0399) && 등록 미러 standby 가 실 cluster 에 실재(따뜻한 사본 alibi). 종합 복원력 시나리오(runScenario migrate+reprovision → killHost primary → promoteStandby 따뜻한 failover) 뒤에도 참 → 코디네이터가 runMulti 의 *펜싱(0401)·lease-timeout(0402)·restart(0403)·reprovision/mirror(0405~0406)·warm-failover(0409)·시나리오 루프(0408)·clusterInfo(0407)* 를 모두 zone cluster 상주 제어 평면으로 보유(#62 능력 합류·병존 reg 0). **#62 runMulti 합류 복원력 sub-arc(0401~0410) 닫기.** 부수: `clusterInfo` 에 `promotions` 필드 추가. 계층: 코디네이션(#62).
- OFF 동치: runMultiCoherent 미호출이면 0409 동치. verify.js 새 모드 `coordmulticap`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 25.0KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmulticap 5/5**: runScenario(6,{migrate z3 A→B@2·reprovision z1@hostA_s@3})+killHost(hostA)+promoteStandby(z1) → **runMultiCoherent Y**·a1 보존·migrations 1·reprovisions 1·promotions 1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 술어/시나리오는 제어 평면 ②결정론: 시드 5종 동일 ③권위: 종합 lifecycle(migrate+reprovision+kill+promote) 뒤도 단일 where 권위·warm-standby 상태 보존 ④은닉: cluster/driver/orch 공개 API 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: ⒜ 능력 합류(병존)까지 — 옛 cluster-run.js runMulti 가 *이 코디네이터를 호출하도록* 코드 합류(runMulti delegate)는 미수행(능력은 superset·플립만 잔여) ⒝ 코디네이터는 *zone* cluster 범위 — runMulti 의 full topology(gateway/services/partition) 복원력은 미포함 ⒞ orch zoneHost entity 컨테이너 동기(#68)·업스트림 intent 실 클라(#61)·진짜 비동기(#4) 후속.
