# step-0308 — #9 잔여(실 host.js 물리 분리) 8: host 컨테이너 정합 불변 primitive

> 직전 step-0307 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 술어 `hostContainerCoherent()`: host 프로세스 층이 깨지지 않았는가의 단일 판정 — ⒜ `zoneHostSingleOwner`(어떤 존도 두 host 없음·0303) ⒝ `zoneHostDrift()===0`(컨테이너==집행 SSOT running·0303) ⒞ roster 회계 닫힘(`hostRegisters−hostDeregisters === zoneHosts.size`·0304 spawn/despawn 이 정확히 상쇄). `bridgeCoherent`(0278·실 핸들)의 *host 프로세스 컨테이너* 판.
- 계층: 코디네이션(⑤). 읽기 전용 → 0307 비트 동일. verify.js 새 모드 `zonehostcoh`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0307) 비트 동일.
- `zonehostcoh` 5/5 OK — 혼합 lifecycle 후: hostContainerCoherent Y(single Y·drift 0·reg−dereg 2 == hosts 2) + directFlowCoherent Y·entityConserved Y·total 1·ledger 5/1/2/1.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 읽기 전용 술어(tick 무관). ② 결정론: 술어 합성·시드 5종 동일. ③ 권위: 존이 정확히 한 host 프로세스에·컨테이너==집행·spawn/despawn 회계 닫힘을 한 술어로. ④ 은닉: orch 내부. ⑤ headless: Node 순수·reg 0.
- 한계: 정합 술어는 by-construction(_hostSet 단일 귀속·roster 상쇄). 전 데이터 평면 + host 층 capstone(hostProcCoherent = directFlowCoherent && hostContainerCoherent)은 후속(0310). 이상 없음.
