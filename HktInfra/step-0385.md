# step-0385 — #65 양방향 동기 5: failover 가 placement 갱신 + lost 추적

> 직전 step-0384 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `failover(deadHost,toHost)` 가 ⒜ 죽은 host 의 존을 *코디네이터 placement*(orch plan 아님)로 찾고 ⒝ 재가동 후 `placement[zone]=toHost` 갱신 ⒞ `lostZones`(새 Set 필드)에 기록(failover 는 상태 소실·#63). lostZones 는 0386 coordDesync 가 *기대된 부재*로 제외할 근거. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordfosync`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordfosync 5/5**: run(3)+failover hostA→hostB → placedHost('z1')==hostB·placedHost('z3')==hostB·lostZones={z1,z3}.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: failover 는 tick 밖 ②결정론: placement 정렬 조회 ③권위: where 권위 생존 host 로 갱신·단일 소유 유지 ④은닉: cluster API 만 ⑤headless: 실 host SIGKILL+재가동. — **한계**: failover 상태 소실은 여전(#63·lostZones 로 *명시 추적*만)·영속 연동 복구는 후속.
