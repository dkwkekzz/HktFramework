# step-0460 — #4 실 net.step 배리어 실제 치환 10·grand capstone: run() E2E

> 직전 step-0459 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/verify.js` — grand capstone `bare2ecap`: 실 `run()` 의 net.step 중앙 배리어를 `async-barrier` 로 치환해 한 시나리오로 단언 — ⒜ 단일 존 손실+지연+resync 하 world==lockstep·다운스트림 뷰 수렴(seenSig==lockstep)·exactly-once(moveDup 0) ⒝ 다중 존+핸드오프 투명(world/log==lockstep·handoffs>0). **#4 실 net.step 배리어 실제 치환 sub-arc(0451~0460) 닫기** — 실 run() 전송에서 중앙 lockstep 배리어를 async substrate 로 치환해도 월드/뷰가 lockstep 에 수렴. 코드 박스 무변경 → reg 구조적 0.
- 계층: 코디네이션(#4). 실 run() 배리어 치환 E2E 완결.
- OFF 플래그: `asyncBarrier`(미설정→net.step 그대로 = **reg 구조적 0**). verify.js 새 모드 `bare2ecap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `bare2ecap` 5/5 OK — ⒜ 단일 존·손실 20%+지연 30%: world/뷰 == lockstep·exactly-once(moveDup 0)·resyncs 24~32·delayed 32~43 ⒝ 다중 존(2)·핸드오프: world/log == lockstep.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(손실+지연 교란 무관·world==lockstep) ③권위 단일 ✅(exactly-once) ④은닉 ✅ ⑤headless ✅. 한계(정직): 손실/지연 *섭동*은 단일 존(무이주)에서 검증 — 다중 존 이주 경계는 *투명 통합*만(섭동 하 이주 전 resync 지연 유계는 후속). 배리어는 net.step 을 *대체 실행*하나 aync-barrier 는 asyncBarrier OFF 면 run() 미호출(기본 lockstep 보존·reg 구조적 0) — 완전 async 전환(항상 ON·downstream 손실/재접속)은 후속.
