# step-0270 — 정리(#49 인접·선제): gateway 메시지 라우팅 핸들러 믹스인 분리

> 직전 step-0269 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/gateway.js`(22.8KB·엣지 핵심 박스·성장 선제 정리) → `Gateway` 의 *메시지 라우팅 핸들러*(`onMsg`: 클라 move/item/chat 업스트림 라우팅 + 존/서비스/버스 다운스트림 중계 + 세션 bind/unbind, ~105줄)를 새 박스 파일 `src/gateway-msg.js` 믹스인으로 분리. 계층: 엣지(게이트웨이). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변).
- 배선: gateway.js 가 `require('./gateway-msg.js')` 1줄 · 클래스 정의 직후 `Object.assign(Gateway.prototype, GatewayMsg)`. onMsg 가 _svcSend/_itemReq/_relayItemResult/_relayChatOut/_relayRank(코어 잔류) 호출. 잔류(코어): 생성자·worldTargets·_svcSend·_itemReq·resendIn·_onItemAck·_ackOut·_relayX 헬퍼. verify.js 새 모드 `gwsplit`.
- 결과 크기: gateway.js 22.8KB→16.5KB·gateway-msg.js 7.7KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `gwsplit`(가설): 5/5 — 6 클라 40 tick move 가 게이트웨이 onMsg 로 존 라우팅 → 두 동일 run 의 worldDigest 일치(결정론 재현)·last-tick live 엔티티 6. 옮긴 라우팅 핸들러가 업스트림 라우팅·결정론 수렴 무결.
- spine: ALL OK(전 역사 누적 회귀 + gwsplit 편입).

## 척추 5항 + 한계

①tick: 라우팅은 존 tick 밖 엣지 중계·무변경 ②결정론: 코드 이동만·worldDigest 재현 ③권위: 클라는 게이트웨이만 앎(은닉 보존)·존 권위 무변경 ④은닉·단일 연결: 게이트웨이 단일 연결 계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
