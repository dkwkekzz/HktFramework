# step-0419 — #62 코드 합류 9: runMulti OFF-게이트 위임 — opts.viaCoord→runMultiViaCoord

> 직전 step-0418 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `runMulti(opts, deps)` 최상단에 OFF-게이트 분기 추가: `if (opts.viaCoord) return runMultiViaCoord(opts, deps);`. *옛 runMulti 가 코디네이터 단일 진입점을 호출* — #62 "코드 합류"의 플립. 기존 호출(`NET.runMulti`·`viaCoord` 미설정)은 분기를 건너뛰어 옛 전 토폴로지 lockstep 경로 그대로.
- 계층: 코디네이션(#62 코드 합류 — runMulti↔코디네이터 병존을 *호출*로 합류).
- OFF 플래그: `opts.viaCoord`(미설정=OFF) → 옛 경로 비트 동일 → 회귀 0·e2e 멀티프로세스 보존.
- verify.js 새 모드: `coorddelegate`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (OFF 경로 — run() 및 runMulti 옛 경로 비트 동일).
- e2e: ALL OK (OFF 경로 멀티프로세스 = 인프로세스 비트 동일 — viaCoord 미설정이라 분기 미진입).
- 가설 `coorddelegate`: 5/5 — `runMulti(viaCoord:true, full deps)` → coord 결과 shape(.net 부재·coherent 보유)·runMultiCoherent Y·migrations 1·reprovisions 1.
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 이상 없음. 위임은 `deps` 에 `run`·`zoneSpecOf` 가 있을 때만 동작(공개 `NET.runMulti` 의 deps={buildTopology,Net,fnv1a} 엔 없음 → viaCoord 설정해도 zone-cluster 전용·verify 가 직접 주입). 옛 runMulti(전 토폴로지)와 코디네이터(zone-cluster)는 *범위*가 달라 위임은 zone-cluster 시나리오에 한정 — 전 토폴로지 흡수는 범위 밖(정직한 한계).
