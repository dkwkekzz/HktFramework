# step-0206 — 캐시 read-through (cacheGet·miss→소스)

> 직전 step-0205 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cache.js` — `cacheGet{key}` → `cacheReply{key,value,hit}`: hit 면 캐시서 즉답, **miss 면 소스(backing DB)서 읽어 캐시 채운 뒤** 답(다음 get 은 hit). 계층: **데이터(계층6)**. `source`/`hits`/`misses`/`_lastGet` 추가. read-through 가 매 조회의 DB 직행을 흡수(첫 miss 만 소스 1홉).
- 배선: 0205 `cacheOps` seam 재사용(cacheGet 도 같은 경로)·소스는 `cacheSource` 파라미터→cache opts. cacheGet 미주입→미발화 = 0205 비트 동일(reg 0).
- verify.js 새 모드 **`cachereadthrough`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(cacheGet 미주입 → 0205 비트 동일).
- **가설 `cachereadthrough`**: 5/5 시드 OK — get 4(session hit·price miss→fill·price refill hit·absent miss) → **hits 2·misses 2·size 2**(price 채워짐)·absent=undefined(소스도 없음).
- **결정론**: 5 시드 동일. **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(cacheGet onMsg 반응·존 tick 밖) ② 결정론 ✅(`Math.random` 0) ③ 권위 단일 소유 ✅(캐시는 파생 핫뷰·소스가 권위·read-through 는 사본 채움) ④ 은닉 ✅(cacheGet request/reply·소스 1홉) ⑤ headless ✅(Node·UE 0).
- 한계: 캐시 박스 *기본 통신 완비*(set+get/read-through). write-behind·TTL/eviction·소스를 실 PersistStore 박스로 연결은 2차(현 소스=주입 backing map). 멀티프로세스 미배선(#9).
