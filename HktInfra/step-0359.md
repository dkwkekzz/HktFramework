# step-0359 — #57 실 host.js OS 프로세스 spawn 9: 실 host 프로세스 다중 존 incremental 가동

> 직전 step-0358 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/host.js` — `zoneadd` cmd 추가: 가동 중 host 에 존 액터를 *증분* 추가(full-reset 인 init 과 달리 기존 액터 보존). 한 host.js 프로세스가 여러 존을 incremental 로 소유. 기존 cmd(init/deliver/tick/snapshot…) 무변경.
- `src/cluster-hostdriver.js` — `flush` 의 specOf init 송신을 `{cmd:'zoneadd',specs:[spec]}` 로(증분) 변경 → 다중 존이 한 host 에 누적 가동.
- 계층: 코디네이션/월드(⑤②·SPINE §5 ⑤). OFF 플래그 `clusterDriverReal`(미설정→실 spawn 0). host.js 는 run() 인프로세스 경로 밖(reg/spine in-proc 무관)·기존 cmd 무변경 → e2e/multiproc 비트 동일. verify.js 새 모드 `hostmultizone`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입·host.js 신규 cmd 만 추가 → baseline(0358) 비트 동일.
- `hostmultizone` 5/5 OK — z1·z2@hostA: 드라이버 commands==[spawnOne:hostA,init:z1,init:z2]·flush(specOf)→실 host.js 1개 spawn(`livePids`==1)·zoneadd×2·snapshot 에 z1·z2 둘 다 인스턴스(kind 'zone'). 한 실 OS 프로세스가 다중 존 소유.
- `node run.js spine` = ALL OK (실 child_process·기존 e2e 무변경).

## 척추 5항 + 한계

- ① tick: zoneadd 는 제어 평면 cmd(tick 밖). ② 결정론: 5종 동일·zoneSpec 시드=zoneId 해시. ③ 권위: 두 존 한 host 프로세스 단일 소유. ④ 은닉: 직렬화 spec(소켓). ⑤ headless: 실 host.js 자식 프로세스·다중 존 snapshot — 원격 검증.
- 한계: deliver/egress·migrate 의 실 소켓 집행은 후속(0360 capstone 에서 in-proc 계약 닫고 실 데이터 평면은 다음 arc). 이상 없음.
