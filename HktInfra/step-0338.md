# step-0338 — #9 후속: 다운스트림 타임아웃 재전송(마지막 frame 손실 복구)

> 직전 step-0337 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` `_retransmitStale(tick)` — 매 tick 미-ack egress 버퍼를 훑어 ack 없이 `egressTimeout` tick 경과한 frame 재전송(재전송 후 sentAt 갱신). egress 버퍼 entry 에 `sentAt` 추가. 0337 게이트웨이 gap-resync 는 *뒤 frame 도착*이 트리거라 세션 *마지막* frame 손실은 못 잡는 구멍을 메운다(zone heartbeat·bus recoverRetry 0058 의 다운스트림 판).
- `src/orch-control.js` onTick — `_drainZoneEgress` 뒤 `if (this.zoneEgress && this.egressTimeout) this._retransmitStale(tick)`. `src/orch-bridge-init.js`·`src/topo-build.js` — `egressTimeout`(0=off)·계측 `zoneEgressTimeoutResent`.
- 계층: 코디네이션/엣지(②⑤). 플래그 없음(egressTimeout 0·zoneEgress OFF→재전송 0). verify.js 새 모드 `gwtimeout`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — egressTimeout 0·zoneEgress OFF → `_retransmitStale` 미실행 → baseline(0337) 비트 동일.
- `gwtimeout` 5/5 OK — a1 의 *마지막* dseq3 손실(`zoneEgressDropped` 1·뒤 frame 없음→`gatewayResyncsSent` **0** 으로 gap 기전 못 함 확인)에도 `zoneEgressTimeoutResent` **1** 로 능동 재전송→`downSeqNext('s:a1')` **4** == egrA1(전부 복구).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: onTick 재전송은 전송(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 재전송 무영향·중복은 게이트웨이가 드롭. ④ 은닉: orch→게이트웨이 명시. ⑤ headless: Node 순수·직렬화·reg 0.
- 한계: 고정 타임아웃(적응형 아님). ack 오면 가지쳐 종료(무한 재전송 없음). 이상 없음.
