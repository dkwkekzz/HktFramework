# step-0239 — loginauth 모드 spine 승급 (#16 — 3차 라운드 누적 회귀화 9/10)

> 직전 step-0238 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0229 의 bespoke `loginauth`(로그인 계정 검증) 검증 모드를 누적 키트로 승급(OPS/BASE 함수 내 지역화). 계층: 검증 하니스(박스 코드 무수정). OFF 플래그 없음 — 박스 `.js` 0줄 → reg 0 자명. #16 정리·도구 step 9/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `loginauth`: 5/5 OK — p1·p2 enqueue(큐 2·authed 2)·미인증 pX 거부(authRejects 1·pos -1).
- spine ALL OK — loginauth 누적 회귀 편입(0229 엣지 검증 차단을 현재 src 에 단언).

## 척추 5항 + 한계

①tick: 검증 모드 추가(시뮬 외)·인증은 엣지(존 tick 밖). ②결정론: 검증 결정론·Math.random 0. ③권위: 큐는 엣지 SSOT. ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음.
