# step-0376 — #62 runMulti 통합 6: 상주 failover

> 직전 step-0375 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `failover(deadHost, toHost)` 추가: 죽은 host killHost(child_process 종료) 후 그 host 가 소유하던 존(orch plan 기준)을 생존 toHost 에 새 빈 인스턴스로 재가동(driver.failoverZone). 죽은 host 는 snapshot 불가라 entity 소실(정직한 한계·migrate 의 graceful 보존과 대조). `failovers` 계측. 계층: 코디네이션(#62).
- verify.js 새 모드 `coordfailover`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordfailover 5/5**: run(3) 후 failover hostA→hostB → hostA socketDead Y·z1·z3 hostB 재가동 Y·a1 소실 Y(정직한 한계)·coord.failovers==1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: failover 는 tick 밖 제어 평면 lifecycle ②결정론: 재가동 빈 존(소실은 비결정 아님·구조적) ③권위: killHost→재가동으로 단일 소유 유지(이중 권위 0) ④은닉: driver/cluster API 만 ⑤headless: 실 host.js SIGKILL+재spawn. — **정직한 한계**: failover 는 상태 소실(죽은 host snapshot 불가) — 영속 연동(월드 영속 replay)으로 무손실 failover 는 후속.
