# step-0283 — #56 브리지 존 데이터 평면 3: leave 흐름(entity 제거)

> 직전 step-0282 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_bridgeLeave(zoneId, avatar)`: 실 EntityZone 핸들로 `onMsg('leave')`(sessionId='s:'+avatar) → ents/sessions 제거(멱등·실존 avatar 만 계측). 계층: 코디네이션↔월드(데이터 평면).
- `src/orch-control.js` — `onMsg` `zoneLeave` 분기(`zoneEntityFlow` 가드). `src/orchestrator.js` — `zoneLeaves` 계측.
- OFF 플래그 `zoneEntityFlow`(OFF→0282 비트 동일=회귀 0). verify.js 새 모드 `zoneleave`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zoneleave` 5/5: z1 에 a1·a2 enter(count 2) → a1 leave → `cnt=1`·a1 `N`·a2 `Y`·`zoneLeaves=1`.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: leave 라우팅은 orch onMsg·존 tick 밖 동기 Map 제거. ②결정론: 제거는 결정론(시드 무관)·`Math.random` 0. ③권위 단일 소유: leave 는 그 존 핸들에서만 제거(공백 정상). ④은닉: 게이트웨이는 zoneLeave 메시지로만. ⑤headless: 실 EntityZone onMsg·UE 0.
- 한계: enter/move/leave 기본 데이터 평면 완성. migrate 무손실·hostdown 소실·stop 폐기·단일 소유·census 는 0284~ 후속.
