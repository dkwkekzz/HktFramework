# step-0391 — #66 tick placement-aware 1: tick() 존 순회를 placement 권위로

> 직전 step-0390 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `tick()` 의 존 순회를 `orch.hostSpawnPlan()`(stale 가능) 대신 `this.placement`(코디네이터 where 권위·삽입 순·결정론)로 전환. 0390 capstone 은 lifecycle(migrate)을 run 루프 *뒤*에 둬 #66(tick 이 stale orch plan 사용)을 미발현시켰다 — tick 이 placement 를 직접 순회하면 run *도중* migrate 도 즉시 따라간다(0393 발현). 계층: 코디네이션(#66).
- OFF 동치: 정상 경로(placement==orch plan·mid-run migrate 없음)에선 같은 (host,zone) 집합 → views/egress 동일 = 0390 동치. verify.js 새 모드 `coordtickplace`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 20.4KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordtickplace 5/5**: run(5) placement 권위 순회 → views 2·coordDesync 0·placementCoherent Y·egressByZone ⊆ placement Y(placement 집합만 tick).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 순회는 시뮬 tick 밖 ②결정론: 시드 5종 동일·placement 삽입 순 순회 ③권위: placement(where) 순회로 tick 이 단일 where 권위 추종 ④은닉: driver/cluster API 만(zone.js import 0) ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: tick 의 deliver 재생은 여전히 `c.host`(driver.commands) 사용(0392 에서 placement 조회로)·mid-run migrate 발현 테스트는 0393·orch 이중 권위 합류 #67 은 0394~.
