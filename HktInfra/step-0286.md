# step-0286 — #56 브리지 존 데이터 평면 6: stop 폐기(계획적 퇴역 entity 폐기)

> 직전 step-0285 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` `_bridgeStop` — 런타임 제거 *전* 그 핸들의 `ents.size` 를 `zoneEntitiesDiscarded` 에 더한다(존 운영 퇴역=계획적 폐기·hostdown 비자발 소실과 구분·계측). `src/orchestrator.js` — `zoneEntitiesDiscarded` 계측. 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0285 비트 동일=회귀 0). verify.js 새 모드 `zonestopent`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zonestopent` 5/5: z1@hostA(a1·a2)·z2@hostB(b1·b2·b3) → z1 stop → `z1cnt=0`(미가동)·`z2cnt=3`(무사)·`discarded=2`·`stops=1`·`rtCount=1`·`total=3`.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: stop 은 orch 제어 평면·존 tick 밖. ②결정론: 폐기 계측 결정론·`Math.random` 0. ③권위 단일 소유: z1 퇴역 후 잔존 존만 단일 소유(z1 entity 공백 정상). ④은닉: 게이트웨이는 placeStop 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 한계: entity 데이터 평면 lifecycle 3종(migrate 무손실·hostdown 소실·stop 폐기) 의미 완비. 단일 소유 불변·census·capstone 은 0287~ 후속.
