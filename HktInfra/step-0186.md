# step-0186 — 길드 멤버 수 배지 읽기 모델(guildFeed·GuildFeed)

> 직전 step-0185 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/svc-guildfeed.js`(GuildFeed) — svc.guild.changed 구독 읽기 모델: guildId 별 memberCount 배지(create=로스터 크기·join +1·leave −1). 발신 0·권위 0(CQRS 파생·비-침습). 우편 MailFeed 0151 의 길드 판. 계층: **3 게임 서비스**(읽기 모델).
- `src/svc-guild.js` — 배지 정확도: guildCreate 도 svc.guild.changed(kind 'create'·members) 발행(changePublish ON 시). 0183 join/leave 발행에 create 추가.
- 배선: `net-core.js`·`topo-actors.js`(import+case)·`topo-build.js`(`guildFeed` 플래그·박스·buildSubs)·`topo-subs.js`(guildfeed→svc.guild.changed 구독)·`topo-run.js`(노출).
- OFF 플래그 `guildFeed`(OFF·길드 부재 → 박스 0 = 0185 비트 동일=회귀 0) · verify.js 새 모드 `guildfeed`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildFeed OFF → 0185 비트 동일.
- **가설 `guildfeed`**: 5/5 시드 OK — g1 배지 3==로스터 3·g2 배지 2==로스터 2·total 배지 5·OFF 박스 0(비-침습).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 배지는 존 tick 밖 순수 관찰. ②결정론: 전 시드 동일 배지. ③권위 단일 소유: 읽기 모델·권위 0(로스터 SSOT 불변). ④은닉: 버스 구독만·로스터 저장 무지. ⑤headless·원격: Node·run.js 증명. — 이상 없음.
