# step-0171 — 정리: svc-mail-core 영속·failover 부품 분할(→ svc-mail-persist.js)

> 직전 step-0170 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`(34.7KB>30KB·비대화 트리거)에서 **영속·failover 메서드**(`_snapState`/`_restore`/`_journal`/`crash`/`reconstruct`)를 신규 `src/svc-mail-persist.js` 로 추출 — `Object.assign(MailService.prototype, …)` 프로토타입 증강(가방 svc-inventory-persist 0053·우편 txn 0165 와 동일 패턴). **기능 0·바이트 동일·reg 0**.
- 진입점 `src/svc-mail.js`: `require('./svc-mail-persist.js')` 추가 → core→txn→persist 순 로드. 계층: 게임 서비스(우편)·데이터(영속) — 새 동작 없음.
- OFF 플래그 없음(정리 step·새 항 0). verify.js 새 모드 `mailsplit` — crash→reconstruct 비트 보존 단언.
- core 34.7KB → **30.4KB**(svc-exchange-core 30.8KB 와 동급으로 유계화).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 전 시스템 5시드×시나리오 cur==prev 비트 동일(persist+rel+rst 포함) — `결과: ALL OK`. 분할은 내부 파일 구조만 변경.
- **결정론 전파**: repro 멀티프로세스 2회+인프로세스 동일(0x7a122947 등 5시드) OK.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경(run.js ALL OK).
- **가설 `mailsplit`**: 무압축 pre==post 예·스냅샷 pre==post 예·tail 1<full 5(5시드 전부 OK). 분할된 persist 부품으로 crash→reconstruct 가 죽기 전과 비트 동일(무압축 전체 replay·스냅샷 _snapState/_restore→tail replay 양 경로).
- **spine**: ALL OK(백그라운드 사슬).

## 척추 5항 + 한계

- ①신성한 tick: 우편은 존 tick 밖 별 박스 — 무변경. ②결정론 코어: 쓰기 경로 무변경(분할은 메서드 위치만). ③권위 단일 소유: 우편함 권위 MailService 유지. ④은닉·단일 연결: 클라 와이어 계약 무변경(hide 누설 0). ⑤headless·원격 검증: Node require 체인(core→txn→persist)으로 무변경, live.js는 서버 사이드.
- 이상 없음. (core 30.4KB — 30KB 경계 근접·svc-exchange-core 동급. 다음 기능 arc 누적 시 saga/custody 추가 분할 후보.)
