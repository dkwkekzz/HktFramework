# step-0327 — #9 후속: host 이주 중 다운스트림 뷰 연속성

> 직전 step-0326 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 질의 `zoneViewReport(zoneId)` = 그 존 다운스트림 데이터 평면 요약 {frames, bytes, sessions, serializable}(0319~0326 지표 집계). 운영 한눈 + migrate 같은 lifecycle 전후로 뷰 산출이 *끊기지 않는지*(연속성) 보는 단위.
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0326 비트 동일. verify.js 새 모드 `hostzonemigrateview`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0326) 비트 동일.
- `hostzonemigrateview` 5/5 OK — z1@A 에 a1 enter·2회 이동 → `placeMigrate(z1, hostB)`(같은 EntityZone 핸들·상태 보존) → a1 2회 이동: 뷰 산출 끊김 없음(reset 1 + update **4**=이주 전 2+후 2)·entity **1 보존**·런타임 host **hostB**·pos (9,9)·`zoneViewReport` serializable·frames 5.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 5종 동일. ③ 권위: migrate=같은 핸들(release+acquire)·상태·세션·뷰 스트림 보존. ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·reg 0.
- 한계: migrate(자발·같은 핸들)만 연속(hostdown=새 인스턴스는 prevSeen 소실→reset keyframe 재시작·정상). 이상 없음.
