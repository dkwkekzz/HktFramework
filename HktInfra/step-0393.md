# step-0393 — #66 tick placement-aware 3·발현: run(ticks, onTick) mid-loop migrate

> 직전 step-0392 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `run(ticks, onTick)` 에 mid-loop lifecycle 훅(매 tick·coordDesync 가드 후 `onTick(t,this)` await). 루프 *도중* migrate 가 일어나도 placement-aware tick 순회(0391)+deliver(0392)가 새 host 를 추종해 maxDesync 0 유지. 0390 capstone 은 lifecycle 을 루프 *뒤*로 미뤄 #66(tick 이 stale orch plan)을 미발현시켰다 — 이제 루프 *중* migrate 를 발현시켜 정합 증명. 계층: 코디네이션(#66).
- OFF 동치: onTick 미제공이면 루프 몸통 무변경 = 0392 동치. verify.js 새 모드 `coordmidmigrate`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 21.0KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmidmigrate 5/5**: run(5, t=2 에 migrate z1 A→B) → **maxDesync 0**·coordDesync 0·placementCoherent Y·migrations 1 — 대조로 **driver.clusterDesync 1**(옛 orch plan stale·#66 발현 증거).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 훅은 시뮬 tick 밖(제어 평면 lifecycle) ②결정론: 시드 5종 동일 ③권위: mid-loop migrate 후도 placement(where) 단일 권위·release+acquire ④은닉: driver/cluster API 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: orch 내부 zoneHost/running 은 여전히 mid-migrate 미반영(이중 권위 #67)·0394~ 에서 orchWhere 노출→write-back 합류.
