# step-0321 — #9 후속: host 산출 뷰의 증분 델타 정확성

> 직전 step-0320 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — 읽기 전용 질의 `zoneViewStats(zoneId, sessionId)` = 그 세션에 산출된 view_delta 분포 {resets, updates, enters, total}. 증분 전파의 핵심을 본다: 초기 keyframe(reset) 1 + 이동마다 update — *매 tick 전체 전송이 아니라 변경분만*(대역 절감). total ≪ tick 수면 증분이 동작.
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0320 비트 동일. verify.js 새 모드 `hostzonedelta`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0320) 비트 동일.
- `hostzonedelta` 5/5 OK — a1 enter 후 3회 이동(tick 6·7·8): 산출 뷰 = reset **1**(초기 keyframe) + update **3**(이동마다) = total **4** ≪ 14 tick(정지 tick 은 frame 0·매 tick 전체 아님)·update frame 은 자기(a1) 이동 담음.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출은 onTick 안·검증은 읽기. ② 결정론: 존 시드 고정 → 5종 동일. ③ 권위: 권위 존이 증분 뷰 산출. ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·reg 0.
- 한계: 단일 세션 자기 이동만(겹친 뷰 enter/exit·다중 세션 상호 가시 증분은 후속)·아직 클라 전파 미연결. 이상 없음.
