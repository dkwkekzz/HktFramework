# step-0412 — #62 코드 합류 2: coordScenarioFromOpts — runMulti 열화 스펙→코디네이터 시나리오 번역기

> 직전 step-0411 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `coordScenarioFromOpts(opts)` 추가: 옛 runMulti 의 스크립트 열화(`wire.kill`/`reprovision` 등 tick 루프 주입·cluster-run.js:176·202)를 코디네이터 `runScenario` 선언 시나리오로 번역. 입력 `opts.coordSc`(0412 는 `migrate`·`reprovision` case)→`{migrate, reprovision}` 객체. 순수 함수.
- 계층: 코디네이션(#62 코드 합류 — opts 어휘를 코디네이터 시나리오 어휘로 잇는 번역기·이후 step 이 case 확장).
- OFF 플래그: 미부착(verify 만 호출) → 회귀 0.
- verify.js 새 모드: `coordscenfromopts`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일).
- 가설 `coordscenfromopts`: 5/5 — 순수 번역 일치 Y + coordSetup→runScenario(번역결과) 후 unifiedCoherent Y·migrations 1·reprovisions 1 (0408 coordscenario 동치).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 이상 없음(순수 번역 함수·시뮬 비-침습).
