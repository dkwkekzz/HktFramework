# step-0190 — 길드 정합 capstone(rosterConsistent·single-master·guild arc 0181~0190 닫기)

> 직전 step-0189 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `rosterConsistent()`: single-master 불변(척추 ③) 명시 단언 — 전 길드 정확히 한 master(공백 0)·master ∈ members(고아 0)·멤버 중복 0. 순수 읽기(권위 0·실행 경로 무변경). feedConsistent(0188)와 결합. 거래소 0140·우편 0180 capstone 의 길드 판. **guild arc(0181~0190) 닫기**. 계층: **3 게임 서비스**.
- OFF: 순수 읽기 메서드(미호출 시 baseline 비트 동일=회귀 0) · verify.js 새 모드 `guildcapstone`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — 메서드 추가만(실행 경로 불변) → 0189 비트 동일.
- **가설 `guildcapstone`**: 5/5 시드 OK — 풍부한 연산(create/join/leave/transfer)×세 체제(A 정상·B guild crash→reconstruct·C feed crash→reconstruct) 모두 rosterConsistent AND feedConsistent true(3/3)·g1 이양 성사 master c2·g2 이양 거부 master c4.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 정합 검사 존 tick 밖. ②결정론: 전 시드 3/3 true·동일 master. ③권위 단일 소유: rosterConsistent 가 모든 연산·체제서 single-master 보존 단언(capstone). ④은닉: 배지는 발행 스트림 파생·SSOT 직접 접근 0. ⑤headless·원격: Node·run.js 증명. — guild arc 0181~0190 닫힘(SPINE 계층3 길드 박스 골격 완성). 후속: 발행 게이트 통합·길드 bank(escrow).
