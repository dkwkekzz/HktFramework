# step-0174 — 아이템 우편 saga 포기 발행(mailAbandonPublish)

> 직전 step-0173 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`(`_resendPending` 포기 분기): 상한 도달 포기 시 `svc.mail.saga_abandoned{gid,itemId,cause}` 1회 발행(abandonPublished++·운영 가시화·거래소 0132 의 우편 판). 계층: 게임 서비스(우편)·버스(발행 스트림).
- 필드: `abandonPublish`/`abandonPublished`. `svc-mail-persist.js` crash(): abandonPublished 소실(발행은 replay 에서 안 함). `src/topo-build.js`: `mailAbandonPublish` 기본 + 박스 opts 배선 + buildSubs 설정에 추가. `src/topo-subs.js`: audit 가 `svc.mail.saga_abandoned` 구독(ON 일 때만).
- OFF 플래그 `mailAbandonPublish`(OFF→발행 0·구독 미추가 = 0173 비트 동일). verify.js 새 모드 `mailabandon`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: abandonPublish OFF = 0173 비트 동일(토폴로지 구독 미추가) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailabandon`**: 5시드 전부 — ON abandoned 1·published/audit 1/1(발행==포기==관측 1:1)·OFF published 0(포기는 하되 발행 0).
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖·발행은 파생 스트림 — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경(발행=관찰 스트림). ③권위 단일 소유: 우편함 권위 불변(발행 비-침습). ④은닉·단일 연결: audit 가 발행자 무수정으로 구독(버스 경유·서버간 명시 인터페이스). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (다음: saga 포기 give 재admission·거래소 0134 의 우편 판.)
