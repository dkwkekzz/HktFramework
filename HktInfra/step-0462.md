# step-0462 — 다중 존 loss 유계 resync 가드(ownerZone+wrap-aware interior)

> 직전 step-0461 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/async-barrier.js` — **wrap-aware interior 유계 resync 가드** 추가. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`(미설정→net.step·비트 동일). verify.js 새 모드 `mzlossguard`.
- `ownerZone(av)`: net.actors 를 훑어 avatar 를 현재 소유한 authoritative 존 반환(barrier ON 경로 전용 오라클).
- `interior(wm)`: 엔티티가 자기 region 양 끝(`region.lo` 경계 + `region.hi-1` wrap 경계 둘 다)에서 `horizon=max(resyncDelay,delayMax)+1` 이상 떨어졌는가. interior 면 deferred move 가 재배달되기 전 엔티티가 이주 경계에 닿을 수 없다.
- *loss/resync* 경로를 `interior(wm)` 로 게이트(단락 평가 → non-interior move 는 lrnd() 미소비·rng 보존). 경계 근처 move 는 in-order 배달(손실 흡수 보류) → 이주 타이밍 불변 → world==lockstep. (delay 게이트는 0463.)

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → barrier 미생성·net.step·baseline 비트 동일).
- `mzlossguard` 5/5 OK — 다중 존(grid24·zones2) loss-only: handoffs 4~15(실 이주)·resyncs 17~32(실 손실 복원)·world==lockstep **Y** 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 월드 입력 배달만(interior 판정은 소유 존 읽기 peek·쓰기 0) ②결정론: 가드가 이주 경계 섭동을 제거해 lockstep 재현 ③권위 단일: 이주 불변 보존 ④은닉: barrier 는 존 상태를 *읽기만*(전송 substrate 내부) ⑤headless 이상 없음.
- 한계: 이번 step 은 loss 만 게이트(delay 는 0463). interior peek 은 barrier 가 존 ents/region 을 직접 읽음 — 실 분산에선 세션→존 디렉토리(게이트웨이)가 이 라우팅 정보를 제공(더미 in-proc 근사).
