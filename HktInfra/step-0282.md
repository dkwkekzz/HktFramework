# step-0282 — #56 브리지 존 데이터 평면 2: move 흐름 + 런타임 tick 구동

> 직전 step-0281 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_bridgeMove(zoneId, avatar, dx, dy)`: 실 EntityZone 핸들로 `onMsg('move')`(pending). `_tickRuntimes(tick)`: orch 가 자기 zoneRuntimes 의 실 EntityZone `onTick` 구동 → pending move 를 위치에 적용(실 zone.js 시뮬). 질의 `zoneEntityPos`. 계층: 코디네이션↔월드(데이터 평면).
- `src/orch-control.js` — `onMsg` `zoneMove` 분기 + `onTick` 에서 `_tickRuntimes` 호출(`zoneEntityFlow` 가드). `src/topo-actors.js` — 런타임 EntityZone 팩토리에 net 싱크 + addr 부착(onTick view send 흡수·위치 적용만·클라 직접 전파는 #9 후속). `src/orchestrator.js` — `zoneMoves` 계측.
- OFF 플래그 `zoneEntityFlow`(OFF→0281 비트 동일=회귀 0). verify.js 새 모드 `zonemove`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zonemove` 5/5: enter-only(기준 위치 `5,5`) vs enter+move(3,1)+(2,-1)=(5,0) → `moved 10,5` == 기대 `10,5`(격자 wrap·결정론). `zoneMoves=2`. 런타임 시드=`fnv1a(zoneId)`·run seed 무관 → 전 seed 동일 위치.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: move 라우팅은 orch onMsg·위치 적용은 런타임 onTick(orch 제어 평면이 자기 소유 런타임 구동)·존 tick 밖. ②결정론: 위치=시드 PRNG enter + 정수 delta wrap·`Math.random` 0. ③권위 단일 소유: move 는 그 존 핸들에만 적용. ④은닉: net 싱크는 런타임 내부(클라 직접 전파 0·#9 후속)·게이트웨이는 zoneMove 메시지로만. ⑤headless: 실 EntityZone onTick 헤드리스 구동·UE 0.
- 정직한 한계: 런타임 존 view send 는 no-op 싱크로 버려진다(클라 전파는 #9 실 프로세스 후속). leave·migrate 무손실/hostdown 소실은 0283~ 후속.
