# step-0167 — 아이템 우편 give 회계 정합(mailGiveConsistent — gives==ackedOk+ackedFail+pending)

> 직전 step-0166 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailGiveConsistent()` 단언용 읽기 accessor: 발신한 custody give 는 매 순간 정확히 한 상태(회신성공 `ackedOk`·회신실패 `ackedFail`·미해결 `pending`)에 분할(공백·중복 0)·`gives == 셋의 합`. 0165~0166 give·회신·보상 전이의 창발 불변. 거래소 0128 sagaConsistent 의 우편 판. 계층:게임 서비스(우편↔가방 saga).
- 미호출 accessor → 0166 비트 동일(회귀 0). verify.js 새 모드 `exmlgc`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlgc`(3체제): normal g/ok/f/p=4/4/0/0·comp=2/1/1/0(보상 1)·mixed=2/2/0/0·3체제 mailGiveConsistent 예(3/3)·무손실 pending 0 drain 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — give 분할 불변 단언(가방 권위 불변) ④은닉 우편 박스 내부 읽기 ⑤headless `node run.js` 통과 — 이상 없음. 무손실(인프로세스 FIFO·#9)이라 pending 항상 0 drain — 회신 손실은 멀티프로세스 무대서 발현(#9). 그때 재전송(0168 멱등)이 pending 을 되살린다. 교차 정합(0169)·liveness capstone(0170) 후속.
