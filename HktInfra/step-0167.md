# step-0167 — 아이템 우편 saga 미해결 추적 + 회신 손실 감지(pendingGives·gid)

> 직전 step-0166 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail-core.js` — `_custody` 가 saga ON 이면 give 마다 단조 `gid` 부여·미해결 집합 `pending` add·재전송 소스 `pendingGive` 보관. accessor `pendingGives()`. 테스트 seam `ackDrop`(수신 시 드롭할 gid 집합·회신 손실 모의·미제공이면 무손실·production 무영향). `src/svc-mail-txn.js` — item_result 수신 시 ackDrop 이면 드롭(처리 0), 아니면 pending/pendingGive delete. `src/topo-build.js` — `mailAckDrop` 배선. 거래소 0125 의 우편 판. 계층:게임 서비스.
- saga OFF·gid 부재·ackDrop 미제공이면 추적 0 = 0166 비트 동일(회귀 0). verify.js 새 모드 `exmlpend`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlpend`: 무손실 gives/acked/pending 4/4/0(닫힌 고리 drain)·회신 손실[gid1] 4/3/1(잃은 gid 잔존)·gives−acked==pending(격차 가시) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖) ②결정론 난수 0·gid 단조 ③권위 가방 원장 권위·우편은 추적만 ④은닉 gid/replyTo 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. 지금은 손실을 *감지*만 — 재전송(idempotent dedup·가방 sagaDedup 재사용)은 0168 후속. ackDrop 은 테스트 seam(실 손실 주입 대용·production 경로 무영향). 정합 capstone(sagaConsistent 0169)·transfers capstone(0170) 후속.
