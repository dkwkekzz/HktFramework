# step-0432 — #4 진짜 비동기 2: send/recv 인과 규칙·clock condition

> 직전 step-0431 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `lamportExchange(seed, {sites, rounds})`: N site 가 Lamport 클럭으로 메시지를 주고받는 결정론 스케줄(링크 FIFO·순서 보존 배달)을 돌려 *이벤트 로그 + happens-before 간선*(program order + send→recv)을 낸다. + `clockConditionViolations(events, edges)`: 모든 간선 a→b 에서 C(a)<C(b) 위반 수.
- 계층: 코디네이션(#4). 0431 의 단일 클럭을 *교차-site 인과 규칙*으로 확장 — 0433~ 전순서/holdback 이 소비할 substrate.
- OFF 플래그: 없음 — run() 경로 미호출 검증 전용 박스 → **reg 구조적 0**. verify.js 새 모드 `lcrecv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK (async-core run() 미호출).
- `lcrecv` 5/5 OK — 3 site·30 라운드 교환에서 clock condition 위반 **0**·send→recv 포함 인과 간선 31~36·recv 4~9·결정론(같은 시드→같은 로그) Y.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(시드 PRNG·Math.random 0·같은 입력 같은 로그) ③권위 ✅(무관) ④은닉 ✅(무관) ⑤headless ✅(순수 JS·검증 전용). 한계: 배달이 FIFO 순서 보존(재정렬 없음) — *물리 재정렬* 하의 전순서 복원은 0433(전순서)·0434(holdback). clock condition 은 인과의 *필요조건*(역 아님) — 전순서 tie-break(site)는 0433.
