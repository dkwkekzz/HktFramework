# step-0379 — #62 runMulti 통합 9: report 운영 대시보드

> 직전 step-0378 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `report()` 추가: 실 cluster + 코디네이터 누계를 단일 스냅샷으로 종합 — {ticks·hosts·zones·entities(실 host entity 합)·desync·maxDesync·egressTotal·migrations·failovers·coherent}. broker 측 제어 평면의 현재 건강을 한 호출로 관측(운영 대시보드). 계층: 코디네이션(#62).
- verify.js 새 모드 `coordreport`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordreport 5/5**: run(3)→report → ticks 3·hosts 2·zones 3·entities 2(a1·b1)·desync 0·maxDesync 0·egressTotal 2·coherent Y.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: report 는 tick 밖 읽기 관측 ②결정론: 같은 run→같은 대시보드 ③권위: entities=실 host 합·desync=권위 대조 ④은닉: driver/cluster API 만 ⑤headless: 실 host snapshot 집계. — **한계**: clusterDesync 는 orch plan(SSOT) 기준이라 *실 migrate 후 orch 측 placement 미갱신*이면 desync 가 뜬다(migrate 는 0375 가 realPos 로 별도 검증) — orch-side placement 갱신은 capstone/후속에서 다룸.
