# step-0377 — #62 runMulti 통합 7: syncPlan 상주 reconcile(비파괴 자가 치유)

> 직전 step-0376 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `syncPlan()` 추가: orch.hostSpawnPlan(SSOT) 대비 실 cluster 의 현 snapshot 을 차분해 *누락 존만* zoneadd(기존 존 상태 보존·idempotent). driver.reconcile(0366)은 전 존 재-zoneadd 라 entity 를 리셋(초기 spawn 전용) — 상주 제어 평면은 언제든 호출돼도 보존해야 하므로 비파괴 차분으로 토폴로지 drift 자가 치유. 계층: 코디네이션(#62).
- verify.js 새 모드 `coordsync`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordsync 5/5**: run(3) 후 hostA z3 소실(zonedel drift)→driftGone Y→syncPlan()→z3 복원 Y·acted 1(누락 1개만)·clusterCoherent Y(기존 z1/a1 보존).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: reconcile 은 tick 밖 제어 평면 ②결정론: plan.order 정렬·차분 ③권위: orch plan SSOT·누락만 복원 ④은닉: cluster API 만 ⑤headless: 실 host snapshot 차분. — **발견**: driver.reconcile 은 파괴적(zoneadd 가 makeActor 리셋) — 상주 재호출엔 비파괴 차분이 필수. 죽은 host 부활은 socketDead 미해소로 orphan 위험(범위 밖·존 단위 drift 만).
