# step-0187 — GuildFeed 영속·late-join(guildFeedPersist·op 저널 replay)

> 직전 step-0186 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guildfeed.js` — 소비한 svc.guild.changed 를 durable 저널 append·`_apply`(배지 적용 분기 1곳·onMsg/reconstruct 공용)·`crash()`(투영 소실·저널 보존)·`reconstruct()`(저널 replay 로 배지 재구성). 투영 휘발/저널 durable. 우편 MailFeed 0154 의 길드 판. 계층: **6 데이터**(읽기 모델 영속).
- 배선: `topo-build.js`(`guildFeedPersist` 플래그·guildfeed opts persist).
- OFF 플래그 `guildFeedPersist`(OFF → 저널 0·reconstruct 빈 배지 = 0186 비트 동일=회귀 0) · verify.js 새 모드 `guildfeedpersist`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildFeedPersist OFF → 0186 비트 동일.
- **가설 `guildfeedpersist`**: 5/5 시드 OK — ON crash→reconstruct 배지 == 죽기 전(pre==post `dd0ca09f`)·배지==로스터. OFF reconstruct 빈 배지(소실).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 배지 저널은 존 tick 밖. ②결정론: 전 시드 동일 배지 digest. ③권위 단일 소유: 읽기 모델·권위 0(로스터 SSOT 불변). ④은닉: 자기 op 저널만으로 복원·외부 무지. ⑤headless·원격: Node·run.js 증명. — 이상 없음.
