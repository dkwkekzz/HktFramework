# step-0420 — #62 코드 합류 10·grand capstone: 단일 진입점 종합 warm-failover 복원력

> 직전 step-0419 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 검증만(코드 박스 무변경·reg 구조적 0) — `src/cluster-run.js` 헤더에 capstone 1줄. 단일 진입점(`runMultiViaCoord`·`runMulti viaCoord` 위임·0419)이 종합 warm-failover 복원력 시나리오(migrate z3 off hostA + reprovision z1@hostA_s + kill hostA + promote z1)를 한 호출로 구동해 `runMultiCoherent` + 옛 runMulti `clusterInfo` parity 를 동시에 만족.
- 계층: 코디네이션(#62 코드 합류 sub-arc 0411~0420 종합 — 옛 runMulti zone-cluster 복원력이 코디네이터 단일 진입점으로 합류 완료).
- OFF 플래그: 검증 전용·viaCoord 위임은 OFF-게이트(0419) → 회귀 0.
- verify.js 새 모드: `coordmergecap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK · e2e: ALL OK (OFF 경로 보존).
- 가설 `coordmergecap`: 5/5 — 종합 시나리오 한 호출 → runMultiCoherent Y·migrations 1·reprovisions 1·promotions 1·a1 보존(==권위)·clusterInfo parity 키 15종 완비.
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유(kill→promote 원자) ④은닉 ⑤headless·원격 검증(실 host.js child_process E2E) — 이상 없음.
- 한계(범위): 위임은 *zone-cluster 시나리오*에 한정(코디네이터 범위). 옛 runMulti 의 *전 토폴로지*(bus·inventory·chat·rank 등) 경로는 viaCoord 미설정으로 그대로 유지 — 전 토폴로지 흡수는 #62 범위 밖. #62 "코드 합류"는 zone-cluster 복원력의 단일 진입점화로 완료. 잔여: ⒞ 업스트림 intent 실 클라(#61)·⒟ 진짜 비동기(#4).
