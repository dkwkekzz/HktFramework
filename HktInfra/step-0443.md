# step-0443 — #4 실 net.step 배리어 치환 3: 존 수신 메일박스 스트리밍 holdback 재정렬

> 직전 step-0442 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `makeZoneMailbox(nsites)`: 실 Net intent 를 **교차-client 재정렬**로 스트림 수신, low-water-mark 안정성 holdback(async-core.makeHoldback 재사용)으로 *도착하는 족족* 안전분만 점진 방출 → close 잔여 flush. 0442 는 전체 집합 totalOrder 정규화였지만, 실 존은 스트림으로 받으므로 점진 재정렬이 필요. 어떤 인터리빙이든 방출열 == totalOrder → 그 방출 simFold == canonical(수렴).
- 계층: 코디네이션(#4). net.step 중앙 큐(FIFO 배달) 대신 존별 홀드백이 재정렬을 흡수.
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netreorder`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netreorder` 5/5 OK — 4 client·40 intent·K=6 인터리빙: 방출 sig **불변** Y·방출 simFold == canonical(**수렴**) Y·close前 방출 16~32(**점진 holdback** 실동작·전체 대기 아님).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅ ③권위 단일 ✅ ④은닉 ✅(존이 client 링크 FIFO 만 가정) ⑤headless ✅(검증 전용). 한계(정직): 아직 *손실 없는* 재정렬만 — per-client sseq gap-resync 는 0445, 실 actor.onMsg 디스패치는 0444. async-net 은 run() 밖 substrate.
