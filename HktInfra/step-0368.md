# step-0368 — #57 실 데이터 평면 8: driveCluster 통합 E2E (#62)

> 직전 step-0367 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `driveCluster(orch, cluster, specOf, tick)`: orch 드라이버 커맨드로 실 cluster 전체 데이터 평면을 한 호출에 — ① `reconcile`(plan→spawn/zoneadd) ② deliver 커맨드 재생(실 zone.onMsg) ③ 전 존 1 tick(move 적용 + view 산출). orch.clusterDriver 가 broker 측 제어 평면처럼 실 cluster 를 구동(#62 runMulti analog). 반환=tick 산출 view 총수.
- 계층: 코디네이션(⑤·SPINE §5 ⑤). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostdrivereal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0367) 비트 동일.
- `hostdrivereal` 5/5 OK — z1@hostA(a1+4move)·z2@hostB(b1+3move) → 한 `driveCluster` 호출 뒤 실 host.js a1 `{x:9,y:9}`·b1 `{x:10,y:5}` == in-proc orch 권위(2 host·desync 0)·`views`==2(전 존 tick view 산출).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: driveCluster 의 tick 단계가 실 zone.onTick 구동. ② 결정론: 5종 동일. ③ 권위: 2 host 의 entity 전부 in-proc 권위 재현(desync 0). ④ 은닉: 직렬화 deliver/tick. ⑤ headless: 실 host.js 2 프로세스 통합 구동·snapshot — 원격 검증(#62 통합 진전).
- 한계: 단발 구동(reconcile+deliver+1tick) — 연속 tick 루프·migrate/kill 혼합 E2E 는 capstone(0370). runMulti 코어 자체 배선(broker 측 orch 상주)은 후속. 이상 없음.
