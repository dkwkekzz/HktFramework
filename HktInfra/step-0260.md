# step-0260 — 캐시 정합 capstone (arc 0252~0260 닫기)

> 직전 step-0259 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `coherent()` 읽기 accessor 추가 + 무효화(0212) 핸들러가 `keyTtl` 도 정리(고아 per-key TTL 누수 차단). 계층: 데이터(캐시). 플래그 없음(읽기 accessor·keyTtl 비면 무효화 무변경=reg 0).
- 불변: 캐시 메커니즘 전체(write-through·bulk·negative·SETNX·SETEX·delete·prefix)가 섞여 돌아도 ① store↔setAt 1:1(모든 캐시값에 recency) ② store∩negatives=∅(present 키는 known-absent 불가) ③ keyTtl⊆store(고아 TTL 0)를 유지. 4차 고도화 캐시 arc(0252~0260) 닫기.
- verify.js 새 모드 `cachecohere`(혼합 시퀀스 매 단계 coherent 단언).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(coherent 읽기 전용·keyTtl 빈 reg 시나리오서 무효화 비트 동일).
- `cachecohere`(가설): 5/5 — 14-op 혼합 시퀀스 매 단계 coherent()==true + 최종 a=1·c 제거·x∈negatives·session:* 제거·소스 정합(source.a=1·source 에 c 없음).
- spine: ALL OK(누적 회귀 + cachecohere 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: 구조 불변 + 소스 SSOT 정합 단언 ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음(캐시 정합 arc capstone).
