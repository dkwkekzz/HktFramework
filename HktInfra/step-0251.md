# step-0251 — 정리(#49 인접): 오케스트레이터 배치 런타임 분리

> 직전 step-0250 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orchestrator.js`(34KB>30KB 트리거) → 배치 SSOT 런타임 메서드를 새 박스 파일 `src/orch-placement.js` 믹스인으로 분리. 계층: 코디네이션(오케스트레이터). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변).
- 옮긴 메서드: `_start`·`_migrate`·`_hostDown`·`_stop`·`_rebalance`·`_drain`(executed lifecycle)·`hostLoad`·`_leastLoaded`(load helper)·`placementOf`·`placedCount`·`runningHostOf`·`runningOn`·`runningCount`·`runningHosts`·`placementDrift`(질의). 잔류: `onMsg`·`onTick`·`_track`·`_presence`·`monitor`·`_survivorOf`(프레즌스/failover 제어 평면).
- 배선: orchestrator.js 가 `Object.assign(Orchestrator.prototype, OrchPlacement)` 로 되섞음(this 바인딩·메서드 해소 동일) · `net-core.js` 진입점에 `__p('orch-placement')` 1줄 · verify.js 새 모드 `orchsplit`.
- 결과 크기: orchestrator.js 34KB→27.5KB(<30KB)·orch-placement.js 9.1KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `orchsplit`(가설): 5/5 분리 후 executed lifecycle(z1@hostA+z2@hostB 가동→z1 hostC 이주) → z1 running==hostC·z2 running==hostB·running수 2·drift 0(결정==집행·단일 소유). 믹스인 메서드(runningHostOf·placementDrift·runningCount)가 prototype 에서 정상 해소.
- spine: ALL OK(전 역사 누적 회귀 + orchsplit 편입).

## 척추 5항 + 한계

①tick: 배치 제어 평면(존 tick 밖)·무변경 ②결정론: 코드 이동만·시드 동일 해시 ③권위: running 단일 소유 보존(drift 0) ④은닉: 내부 구조 변경만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
