# step-0163 — 아이템 우편↔가방 leg3: 만료 시 escrow→발신자 가방 반환(mailInv)

> 직전 step-0162 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailSweep` 만료 시 아이템 실은 통마다 `_custody(item, 'escrow', mm.from)` 호출 → 가방에 `item_req{op:'give'}`(escrow→발신자). 미수령 만료 아이템이 발신자 가방으로 회수(거래소 0119 expire leg3 의 우편 판·증발 0). 아이템 경로 분기 완성: 발신자→escrow → {수령 시 수신자(leg2) | 만료 시 발신자 반환(leg3)}. 계층:게임 서비스.
- 기존 OFF 플래그 `mailInv` 재사용 → give 0·가방 무변경 = 0162 비트 동일(회귀 0). verify.js 새 모드 `exmlin3`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlin3`(혼합: item0 수령→h1·item1 만료→발신자 x 반환): ON gives/escrowXfers 4/4·item0 소유자 h1·item1 소유자 x(발신자 반환)·OFF item0/item1 x/x·fetched/expired 1/1 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖) ②결정론 난수 0 ③권위 가방 원장 권위·쌍 거래 원자 ④은닉 item_req 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. 3 레그 완비됐으나 give 회신(item_result)은 여전히 fire-and-forget — give 실패(발신자 미소유 등) 시 회계가 어긋날 수 있음(saga 회신 수신·보상 0165~ 후속). 2-서비스 보존 capstone(0164·가방 minted==최종 분포·escrow 잔액==itemHeld) 후속.
