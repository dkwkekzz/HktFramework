# step-0163 — 아이템 우편 만료 반환 leg3(mailSweep 만료 → 우편 custody→발신자 가방 give)

> 직전 step-0162 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailSweep` 만료 시 그 우편의 아이템을 `_custody(item, 'mailcustody', mm.from, 'mailexpire')`(거래소 0119 cancel/expire 반환 leg 의 우편 판). 받는 이가 안 가져가면 *발신자*(mm.from)에게 반환. 세 leg(인출 0161·입금 0162·반환 0163) 완비. 계층:게임 서비스(우편↔가방).
- OFF: `invMode` OFF·아이템 미첨부 → give 0 = 0162 비트 동일(회귀 0). verify.js 새 모드 `exmlinv3`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlinv3`(x→h1·h2 발신·h1 수령·h2 만료): item0 owner=`h1`(수령)·item1 owner=`x`(만료 반환)·gives=4(발신2+수령1+반환1)·itemExpired=1 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0·정렬 순회 ③권위 아이템 권위 가방 단독(custody→발신자 쌍 거래·소유자=1·아이템은 발신자/custody/수령자 중 한 곳에만 — 보존) ④은닉 `item_req` 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. 세 leg 모두 *fire-and-forget*(결과 회신 0165·실패 보상 0166 전까지 낙관적) — give 실패 시 우편 회계와 가방이 어긋날 수 있음(0166 보상이 닫음). 2-서비스 보존(0164)이 held≡custody 를 단언.
