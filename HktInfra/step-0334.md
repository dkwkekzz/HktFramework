# step-0334 — #9 후속: 다운스트림 — 게이트웨이가 zoneView 를 바인딩된 클라로 라우팅

> 직전 step-0333 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway-msg.js` zoneEnter 핸들러 — 클라가 게이트웨이로 zoneEnter 할 때 `downClients.set(p.sessionId || 's:'+avatar, m.from)` 로 세션→클라 바인딩(orch 의 sessionId 기본과 같은 키). 맵 write 뿐(메시지 무변경).
- `src/gateway.js` `_recvZoneView` — 수신 frame 을 버퍼링 후 `downClients` 로 클라를 해소해 그 클라로 frame 전달(존→게이트웨이→클라 완성). 미바인딩 세션 frame 은 드롭(계측). 디렉토리 `downClients`·계측 `zoneViewsRouted`/`zoneViewDropped`·질의 `gatewayRoutedCount`/`gatewayDroppedCount`/`downClientOf`.
- 계층: 엣지(④⑤·SPINE §4 경로2 다운스트림 종단 전달). 플래그 없음 — zoneEgress OFF→zoneView 미수신→전달 송신 0 = baseline 비트 동일. verify.js 새 모드 `gwroute`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneEgress OFF → zoneView 0·downClients write 만 → baseline(0333) 비트 동일.
- `gwroute` 5/5 OK — a1=클라 dc0·a2=클라 dc1 로 zoneEnter 후: `gatewayRoutedCount` **4** == rx 4(무드롭 라우팅)·`gatewayDroppedCount` 0·`downClientOf('s:a1')`=dc0·`downClientOf('s:a2')`=dc1.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 게이트웨이 라우팅은 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 다운스트림 읽기 전달이 권위 무영향. ④ 은닉: 클라는 게이트웨이만 안다(게이트웨이가 세션→클라 해소·클라는 존/host 무지). ⑤ headless: Node 순수·frame 직렬화 가능·reg 0.
- 한계: 전달은 best-effort(per-세션 seq·ack·재전송·중복 억제는 후속 0335+). leave/disconnect 시 downClients 정리는 후속(stale 바인딩 누적 가능). 이상 없음.
