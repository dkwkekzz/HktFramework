# step-0211 — 캐시 TTL 만료(cacheExpire·휘발 캐시 메모리 유계)

> 직전 step-0210 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheExpire{ttl}` → `setAt+ttl≤now` 인 캐시 키 회수(스윕·store/setAt 제거). set 시 `setAt`(tick) 기록(read-through 채움 포함). 휘발 캐시가 stale 핫 데이터를 영영 안 들고 있게(메모리 유계·Redis TTL 더미판).
- 계층: 데이터(계층6). OFF 플래그: `cacheExpire` 미주입(미발화 → 0210 비트 동일·reg 0). verify.js 새 모드 `cachettl`.
- 2차 고도화 캐시 박스 #1 — 0205/0206 기본 통신(set/get·read-through) 위에 만료 능력.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (cacheExpire 미주입 → 0210 비트 동일).
- cachettl: 5/5 — k1(setAt2+ttl3=5≤7) 회수·k2(setAt5+ttl3=8>7) 생존 → size 1·evicted 1·expires 1.
- spine: ALL OK (전 시리즈 누적 회귀).

## 척추 5항 + 한계

①신성한 tick: 캐시는 존 tick 밖·순수 반응형(onTick 0). ②결정론: setAt=m.tick·시드 의사난수만. ③권위: 캐시는 휘발 사본(소스가 SSOT)·만료는 사본 회수. ④은닉: 클라 접점 무변(게이트웨이 경유). ⑤headless: Node·원격 재현. — 이상 없음.
