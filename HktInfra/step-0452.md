# step-0452 — #4 실 net.step 배리어 실제 치환 2: 월드 입력 per-site Lamport 스탬프

> 직전 step-0451 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-barrier.js` — stepper 가 net.step() 배달을 *직접 인라인*(dedup·delay·stats·onMsg·onTick verbatim·같은 순서 → world/log 불변) + **월드 입력**(zone 행 enter/move/leave)에 **per-site Lamport 스탬프** 부여(site = intent 원발신 sessionId/avatar). 0452 는 스탬프만 기록(재정렬 없음) → world/log 불변(스탬프는 배리어 내부 기록·메시지/log 미변경).
- `src/topo-run.js` — run() 반환에 `asyncBarrier: ab ? ab.stats() : null`(OFF→null·기존 digest 헬퍼 미참조 → reg 무영향). 계층: 코디네이션(#4).
- OFF 플래그: `asyncBarrier`(미설정→ab=null→net.step() 그대로 = **baseline 비트 동일·reg 구조적 0**). verify.js 새 모드 `barstamp`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK(asyncBarrier 미설정→net.step 경로 비트 동일).
- `barstamp` 5/5 OK — 4 client·30 move·48 tick: `run({asyncBarrier})` worldDigest == lockstep Y·logDigest == Y·stamped **244**(월드 입력 스탬프)·sites **4**(4 client 원발신·per-site 클럭).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅(배달 seam·인라인은 net.step verbatim) ②결정론 ✅(스탬프 월드 중립·world/log 불변) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅. 이상 없음 — 스탬프는 배리어 내부 기록(메시지/log 불변). 한계: 아직 재정렬/holdback 없음(0453~). asyncBarrier OFF→run() 미호출→reg 구조적 0.
