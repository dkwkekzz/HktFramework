# step-0165 — 아이템 우편 give 결과 비동기 수신(mailSaga — replyTo+gid·pending drain)

> 직전 step-0164 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `saga`(mailSaga) opt: ON 이면 `_custody` 가 give 에 `replyTo`(자기 주소)+단조 `gid` 를 실어 보내고 `pending` 집합에 추가. `onMsg` 가 가방 `item_result`(op:give) 회신을 gid 로 매칭해 pending 에서 빼고 `acked`/`ackedOk`/`ackedFail` 집계(거래소 0121 exchSaga 의 우편 판). 계층:게임 서비스(우편↔가방 saga).
- 배선: `topo-build.js` — `mailSaga` 플래그·`saga:mailSaga` 를 mail opts 로. 가방 `_sagaReply`(0121)·`item_result` 회신 경로 재사용.
- OFF 플래그 `mailSaga`(기본 false) → replyTo 부재 → 가방 회신 0(`_sagaReply` no-op) = 0164 비트 동일(회귀 0). verify.js 새 모드 `exmlsaga`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlsaga`(item0·item1 h1 발신 후 수령·무손실): gives=4(발신2+수령2)·acked=4·ackedOk=4·pending=0 drain·`예` 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0·단조 gid ③권위 가방 단독(우편은 결과 수신만) ④은닉 replyTo 명시 인터페이스(가방→우편 회신) ⑤headless `node run.js` 통과 — 이상 없음. 회신은 받지만 *손실 대비·실패 보상 없음* — 회신 손실 시 pending 잔존(0167 가시·재전송)·give 실패(ackedFail) 시 우편 적재 롤백 미수행(0166 보상). 닫힌 고리는 0166~0170 에서.
