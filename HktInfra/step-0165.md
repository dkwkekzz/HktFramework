# step-0165 — 정리: svc-mail.js 박스-부품 분할(core/txn/entry·기능 0·reg 0)

> 직전 step-0164 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 정리(기능 0) — `src/svc-mail.js`(30.9KB·박스 1개=파일 1개 30KB 유계 초과·비대화 트리거)를 세 부품으로 재분할: `svc-mail-core.js`(원장 코어 — 생성자·헬퍼 `_snapState/_restore/_journal/_box/_custody`·`crash`·`reconstruct`·조회/회계 accessor·`digest`)·`svc-mail-txn.js`(onMsg 핸들러 — mailSend/mailFetch/mailSweep·`Object.assign` 프로토타입 증강)·`svc-mail.js`(진입점·core→txn 로드·동일 export). 거래소 svc-exchange 0124·가방 svc-inventory 0053·whisper 0094 와 같은 패턴.
- verbatim 이동 — export 집합·동작 불변 → 0164 비트 동일(회귀 0). verify.js 새 모드 `mailsplit`(분할 동작 보존 단언). 다음 기능 step(아이템 우편 saga)을 위한 헤드룸 확보.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK. 파일 크기: core 25.6KB · txn 5.9KB · entry 1.1KB.
- 가설 `mailsplit`(우편 전 기능 — 발신·수령·만료·아이템 custody 3 레그): NET(분할)·NETPREV(0164 동결) mail digest 0x421ff2a9 비트 동일·gives·escrowXfers 일치 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖·불변) ②결정론 난수 0 ③권위 불변(코드 이동만) ④은닉 불변 ⑤headless `node run.js` 통과 — 이상 없음. 정리 step(거래소 0124·가방 0053 계보) — 새 기능 0·구조만. 코드는 src/ 제자리 분할(0049 단일 src/ 모델·디렉토리 복사 없음). 아이템 우편 saga(give 회신 비동기 수신 0166~) 후속.
