# step-0242 — 배치 SSOT 실배선(#51) 2: executed placeMigrate(실 런타임 release+acquire)

> 직전 step-0241 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `_migrate(zoneId, toHost)`: 도는 존 런타임을 release(기존 host)+acquire(toHost) 쌍으로 *실제 이주*(running 단일 키 원자 교체·instance.js `_route` 의 존 판). `placeMigrate` 핸들러가 `placeExecute` ON 이면 paper `placement` 갱신에 더해 `_migrate` 호출 — 0218 paper Map.set 의 집행 판(advisory→executed migrate). 계수 `runtimeMigrations`. 계층:코디네이션.
- OFF 플래그 `placeExecute`(OFF→placeMigrate 는 paper 만=0241 비트 동일=회귀 0).
- verify.js 새 모드 `placemigrexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline). placeExecute OFF 경로 무변경.
- **가설 `placemigrexec`**: 5시드 모두 z1 run hostC·z2 run hostA·hostA run 1·hostC run 1·rtMig 1 — z1 이 hostA→hostC 로 *실제* 이주·한 존 정확히 한 host(중복/공백 0)·결정(placement)==집행(running).
- **결정론/권위/수렴**: `run.js all` ALL OK(멀티프로세스 비트 동일 유지).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 집행은 코디네이션 제어 평면(존 tick 밖) ②결정론: 시드 PRNG ③권위 단일 소유: running 원자 교체로 한 존 정확히 한 host(executed release+acquire) ④은닉: orch 내부 ⑤headless: node 검증. — 이상 없음(rebalance/drain 의 executed 집행은 0243~0244 후속·현재 _rebalance/_drain 은 아직 paper placement.set 만).
