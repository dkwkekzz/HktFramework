# step-0330 — #9 후속 capstone: 다운스트림 데이터 평면 전 정합

> 직전 step-0329 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 capstone 술어 `downstreamCoherent(zoneId)` = `zoneViewConserved`(모든 frame 한 세션 귀속·고아 0·0329) && `zoneViewAllKeyed`(모든 활성 세션 keyframe·무굶김·0328) && `zoneViewWire().serializable`(전부 와이어 준비·0325). 참이면 "host 산출 AOI 뷰가 빠짐없이 주소를 갖고·아무도 안 굶고·소켓 탈 준비".
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0329 비트 동일. verify.js 새 모드 `downstreamcap`. **#9 후속 downstream sub-arc 0319~0330 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0329) 비트 동일.
- `downstreamcap` 5/5 OK — z1@A(a1·a2)·z2@C(b1) 혼합 lifecycle(이동 + z1 A→B migrate + a2 leave): 두 존 `downstreamCoherent` Y(뷰 무손실 회계+무굶김+와이어 준비)·`hostProcCoherent` Y(배치 3층+게이트웨이 직접 라우팅+host 프로세스 컨테이너 정합·0310)·`entityConserved` Y(3 enter−1 leave=2)·total **2**.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 5종 동일. ③ 권위: 다운스트림이 업스트림 단일 소유·보존을 안 깸. ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·직렬화 가능·reg 0.
- 한계: 다운스트림 sub-arc 는 host *산출·포착* 까지(게이트웨이→실 클라 전파·재전송·ack 는 후속·실 클라 하니스 필요). zoneHosts·뷰 버퍼는 orch 인프로세스(실 host.js OS 프로세스 spawn 후속). 이상 없음.
