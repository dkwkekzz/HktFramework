# step-0513 — guildbankconserved 금고↔가방 2-서비스 보존 (#46 금고↔가방 escrow 3)

> 직전 step-0512 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `guildbankconserved`(pure-verify·박스 무수정): 금고↔가방 2-서비스 보존 capstone(거래소 0120·우편 0164 의 금고 판). 금고 `escrowIds`(길드가 escrow 로 보낸 itemId) == 가방서 `'escrow'` 소유 itemId(두 서비스 escrow 회계 합치) AND `Σvault==escrowIds.size` AND `bankConsistent`(itemId 단일 길드 소유). 다중 길드(g1·g2)·예치/인출 혼합서 성립. 계층: 검증 하니스. OFF 플래그 없음·박스 무변→reg 0 자명.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `guildbankconserved`: 5/5 OK — escrowIds ["item1","item2"]==invEscrow·Σvault 2·match Y·bankConsistent. g1 예치2−인출1=잔여1·g2 예치1(격리).
- spine ALL OK — #46 arc 3모드(보존 capstone).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 다중 길드 보존 5시드 동일. ③권위: escrow itemId 가 두 서비스서 정확히 일치(공백·중복 0)·bankConsistent(itemId 한 길드). ④은닉: give 경유·교차 회계는 관측 대조. ⑤headless: node in-proc. — 이상 없음(유효 예치[멤버 소유 아이템] 전제 — 미소유 예치 give 실패는 saga 0515~가 화해·거래소 optimistic escrow 동형).
