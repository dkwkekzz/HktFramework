# step-0401 — #62 runMulti 합류 1·복원력: 코디네이터 epoch 펜싱 fence/presumedDead

> 직전 step-0400 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — 코디네이터에 epoch 펜싱 능력: 필드 `epoch`/`presumedDead`/`fencedTicks` + `fence(host)`(presumedDead 추가 + epoch++) + `tick()` 이 presumedDead host 의 존을 건너뜀(fenced·tick/egress 0). runMulti(`cluster-run.js:69,76`)가 stale host 발신을 막던 epoch 펜싱의 zone cluster 상주 제어 평면 판 — 코디네이터를 runMulti 호환 복원력 코어로 승격하는 #62 능력 합류의 첫 조각. 계층: 코디네이션(#62).
- OFF 동치: presumedDead 비면 tick 의 skip 분기 도달 0 → 0400 동치. verify.js 새 모드 `coordfence`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 27.6KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordfence 5/5**: run(5)(fencedTicks 0) → fence(hostB) → epoch 1·presumedDead={hostB}·tick(6) 이 z2(hostB) 건너뜀(fencedTicks 0→1).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 펜싱은 제어 평면(시뮬 tick 밖) ②결정론: 시드 5종 동일 ③권위: 펜싱이 추정 사망 host 발신 차단(stale 권위 발신 0·split-brain 방지) ④은닉: cluster API 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: fence 는 *수동* 트리거 — silence 기반 자동 추정 사망(lease-timeout)은 0402·snapshot-restart(0403)·reprovision+mirror(0404~0405)·clusterInfo(0406)·runScenario(0407~)·capstone(0410).
