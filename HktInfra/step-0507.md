# step-0507 — mailsagapermfail 재admission 상한 → 영구 실패 종결 (#16 라운드 4차 7)

> 직전 step-0506 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagapermfail`: 재admission 상한 `mailReadmitMax`=1 도달 → 영구 실패. readmitMax 회 재admission 된 give 가 또 포기되면 abandonedGive 에 안 넣어 재admission 차단(`permFailed`++) → 무한 abandon↔readmit 루프 방지. `pending` 잔존·`pending==pendingGive+abandonedGive+permFailed`(perm 항 nonzero)·sagaLivenessConsistent. 미해결의 세 번째(종결) 분할 발현. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 7/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagapermfail`: 5/5 OK — permFailed 1·pendingGive 0·abandonedGive 0·pending 1·readmitted 1·sagaConsistent+liveness Y. 재admission 1회 후 재포기 → 영구 종결(재admission 차단·pending 무공백 잔존).
- spine ALL OK — 손실 체제 7모드 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 영구 실패 5시드 동일. ③권위: permFailed 도 pending 낙관적 잔존(give 성공했을 수 있어 abort 아님·아이템 이중적용 0). ④은닉: give 경유. ⑤headless: node in-proc. — 이상 없음(readmitMax 가 무한 루프를 끊는 안전판 — 종결 상태도 회계에 무공백 편입).
