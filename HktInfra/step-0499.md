# step-0499 — svcexchangecancel 거래소 release 경로 escrow 반환 편입 (#16 라운드 3차 9)

> 직전 step-0498 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 거래소 release 경로(취소·만료 TTL) escrow 반환 capstone 을 재작성 편입: `run()` 을 exchange+saga+exchangeTtl opts 로 구동(2 매물 list→exchCancel 1→exchSweep 만료 1)하고 escrow 아이템이 판매자 가방으로 *반환*(cancelled 1·expired 1·둘 다 seller 복귀)되며 `sagaLiveConsistent` 가 보존됨을 단언하는 새 mode `svcexchangecancel`. buy(0491) 의 짝인 *반환* 레그(취소/만료) 커버. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 9/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcexchangecancel`: 5/5 OK — cancelled 1·expired 1·item0(취소)/item1(만료) 둘 다 seller 복귀·pending 0·`sagaLiveConsistent` Y.
- spine ALL OK — svcexchangecancel 포함(거래소 3종·서비스 계층 누적 회귀).

## 척추 5항 + 한계

①tick: 거래소 존 tick 밖·박스 무변. ②결정론: 반환·정합 대조·Math.random 0. ③권위: cancel/expire release 쌍 거래로 escrow→판매자 아이템 단일 소유 복귀 재단언(escrow 잔류 0). ④은닉: 거래소↔가방 give 경유. ⑤headless: node in-proc. — 이상 없음.
