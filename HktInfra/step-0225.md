# step-0225 — 캐시 용량 LRU 회수(cacheCapacity·개수 유계)

> 직전 step-0224 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/cache.js` — `cacheCapacity{cap}`: 캐시 키 수 상한 설정. set 으로 store.size>cap 이 되면 가장 오래된(recency=setAt 최소·동률 iteration 순) 키부터 회수(`_evictToCapacity`). 0211 TTL(시간 유계)의 짝 = *개수* 유계(Redis allkeys-lru 더미판).
- 계층: 6(데이터·캐시) · OFF 플래그: capacity=∞(cacheCapacity 미수신 → `_evictToCapacity` 루프 미진입 → 0224 비트 동일=회귀 0) · verify.js 새 모드 `cachecapacity`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `cachecapacity`: 5/5 OK — cap 2 후 k1·k2·k3 set → 가장 오래된 k1 회수·size 2·k2/k3 생존·capEvicted 1
- spine ALL OK

## 척추 5항 + 한계

①tick: 캐시는 존 tick 밖 순수 반응형. ②결정론: setAt 최소 + iteration 순 tie-break·시드 PRNG. ③권위: 캐시는 사본(권위는 소스 SSOT)·회수는 사본 폐기. ④은닉: 명시 메시지(cacheCapacity)만. ⑤headless: Node·원격 재현. — 한계: recency=setAt(set 시각)이라 get 은 recency 갱신 안 함 → 진짜 LRU(get touch)는 0226.
