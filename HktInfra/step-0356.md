# step-0356 — #57 실 host.js OS 프로세스 spawn 6: clusterDriver onEgress 다운스트림 송출

> 직전 step-0355 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-bridge-init.js` — recorder 드라이버에 `onEgress(host,key)` + 회계 `driverEgress`. host→게이트웨이 다운스트림 view 송출마다 = 실 host 프로세스 소켓 송신.
- `src/orch-views.js` — `_drainZoneEgress` 의 host→게이트웨이 송출 지점(0331·zoneViewEgressed++ 직후)에서 `clusterDriver.onEgress(rt.host, key)` 호출. 미부착→호출 0.
- 계층: 코디네이션/월드/엣지(⑤②①·SPINE §4 경로2). OFF 플래그 `clusterDriverRecord`(드라이버 null→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostegress`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미부착 → baseline(0355) 비트 동일.
- `hostegress` 5/5 OK — z1@hostA·a1 enter+6회 이동 뒤 `driverEgress`==`zoneViewEgressed`==`egress.length`==7(>0·모든 송출이 드라이버로)·모든 egress host==hostA.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: onEgress 는 _drainZoneEgress(onTick egress·tick 경계) 안·시뮬 tick 밖. ② 결정론: 5종 동일·송출 7 일정. ③ 권위: 단일 존 단일 host(모든 egress hostA). ④ 은닉: host:key(sid#dseq)만. ⑤ headless: Node 순수·reg 0.
- 한계: onEgress 는 송출 *이벤트*만 — 실 host 소켓 송신 집행은 cluster-run.js 후속(0357). 이상 없음. 이로써 orch 드라이버 계약(spawn/despawn·assign/unassign·frame·egress)이 완비 — 다음은 실 cluster 배선.
