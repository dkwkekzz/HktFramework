# step-0484 — asynce2ecap grand capstone 누적 회귀 승격 (#16 라운드 2차 4)

> 직전 step-0483 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0440 의 bespoke `asynce2ecap`(진짜 비동기 substrate in-proc grand capstone: M 복제 순열+손실→인과 정렬→전 복제 desync0)을 누적 회귀로 승격(MODES+ORDER·지역 helper siteOf/shuffle/arrivalFor/replica 함수 내부 지역화·`NET.lamportExchange`/`makeResyncSite`/`totalOrder`/`applyDigest`/`clockConditionViolations`/`totalOrderSound`/`accountDelivered`/`mulberry32` 재사용). 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. `src/verify.js` 순수 셸. #16 라운드 2차 4/N — 자기완결(NET.run/NET.* 기반) capstone 5종 마지막.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `asynce2ecap`: 5/5 OK — clock 위반0·전복제 수렴·인과 존중(sound strict+causal)·exactly-once(complete)·lossy·이벤트56.
- spine ALL OK — asynce2ecap 포함 #4 async 사슬 4종(async-core 0431~40 + async-net 0441~50 + async-barrier 0451~70) 전부 항구 회귀.

## 척추 5항 + 한계

①tick: 검증 모드 승급·박스 무변. ②결정론: canonical digest 대조·clock 위반0·Math.random 0(mulberry32 시드 PRNG). ③권위: 전복제 exactly-once 로 소유 보존 재단언. ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음.
