# step-0263 — 정리(#49 wiring): topo-build 서비스 박스 add 시퀀스 분리

> 직전 step-0262 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/topo-build.js`(31.5KB>30KB 트리거) → `buildTopology()` 의 *서비스/데이터 박스 add() 시퀀스*를 새 박스 파일 `src/topo-boxes.js`(`addServiceBoxes(ctx, add)`)로 분리. 계층: 배선(wiring·토폴로지 빌더). **OFF 플래그 없음 — 투명 분할**(코드 위치만 이동·동작 비트 불변).
- 옮긴 add 시퀀스(verbatim): gateway·bus(buildSubs)·persist·persist2·N-replica·inventory·chatpersist·chat·audit·presmon·wrouter·mbox·mbox2·pservice·presence·presence2·ranking·exchange·market·mail·mailfeed·guild·guildfeed·ranking2·instance·cache·worldlog·loginqueue. 잔류(buildTopology): opts destructure/기본값·파생 주소 계산·zones/orch/followers/clients add·return.
- 배선: topo-build.js 가 `require('./topo-boxes.js')` 1줄 · buildTopology 가 destructure/derive 한 옵션·파생 주소를 `__bctx`(150 이름·기계 생성)로 묶어 `addServiceBoxes(__bctx, add)` 위임 · topo-boxes 는 자체 `buildSubs` require. verify.js 새 모드 `boxsplit`.
- 결과 크기: topo-build.js 31.5KB→14.7KB(<30KB)·topo-boxes.js 23.3KB. **#49 의 topo-build(31.5KB) 해소.**

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `boxsplit`(가설): 5/5 — 멀티 서비스 토폴로지(bus+inventory+chat+audit+ranking+exchange+mail+guild+instance+cache+worldlog+loginqueue+gateway)의 13/13 박스가 spec order 에 존재. 옮긴 add 시퀀스가 전 박스를 정상 구성(위임 무결).
- spine: ALL OK(전 역사 누적 회귀 + boxsplit 편입).

## 척추 5항 + 한계

①tick: 박스 add 는 토폴로지 빌드(존 tick 밖)·무변경 ②결정론: 코드 이동만·시드 동일 해시 ③권위: 박스 구성 무변경(단일 소유 보존) ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할·ctx 150 이름은 buildTopology 가 이미 계산한 값의 verbatim 전달).
