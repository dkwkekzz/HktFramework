# step-0473 — 실 host.js child 경계 업스트림 3: egress 뷰→UpClient

> 직전 step-0472 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — `feedViews(sends, upclient)` 추가. 계층: 엣지/월드(#70·SPINE §4 경로2 되먹임). 드라이버 미부착→run() reg 0. verify.js 새 모드 `upcrecv`.
- 실 host.js 존 `tick` 이 낸 게이트웨이-향 `view`/`view_delta` 중 자기 세션(`s:`+avatar) 것을 골라 실 UpClient 의 `onMsg` 로 배달 — 다운스트림 라우팅(0333)의 *경계 업스트림 판*. 실 UpClient 가 자기 AOI 뷰를 *경계 넘어* 수신.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향).
- `upcrecv` 5/5 OK — enter+tick 후 뷰 배달 1·UpClient.seen 에 a1(자기)·seenSig=a1@enter pos 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 실 존 tick 이 뷰 산출·되먹임은 배달만 ②결정론: 뷰=월드 함수 ③권위 단일: 실 존 소유 ④은닉: 존→게이트웨이-향 뷰→클라(클라는 게이트웨이만) ⑤headless: 실 child_process.
- 한계: 이번 step 은 뷰 *수신* 확인까지(권위 대조 desync 0 은 0474). 되먹임이 게이트웨이 대신 seam 경유(라우팅 계약 동일·sessionId 필터).
