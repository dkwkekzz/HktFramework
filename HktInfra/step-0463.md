# step-0463 — 다중 존 delay 유계 resync 가드(interior→delay 경로)

> 직전 step-0462 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/async-barrier.js` — 0462 의 wrap-aware `interior()` 유계 resync 가드를 *delay(교차-tick 지연 jitter)* 경로로 확장. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`. verify.js 새 모드 `mzdelayguard`.
- delay 경로 조건에 `interior(wm)` 추가(단락 평가 → non-interior move 는 drnd() 미소비·rng 보존). 경계 근처 move 는 지연 없이 in-order 배달 → 이주 타이밍 불변 → world==lockstep.
- 이제 loss·delay 두 섭동 모두 interior 게이트(경계 근처는 즉시 배달·interior 만 async 흡수).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → net.step·baseline 비트 동일).
- `mzdelayguard` 5/5 OK — 다중 존(grid24) delay-only: handoffs 4~15(실 이주)·delayed 27~60(실 지연)·world==lockstep **Y** 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 배달만·interior peek(읽기) ②결정론: 가드가 이주 경계 지연 섭동 제거→lockstep 재현 ③권위 단일 보존 ④은닉: 존 상태 읽기만 ⑤headless 이상 없음.
- 한계: 이상 없음(다음 0464 는 loss+delay 결합 + exactly-once).
