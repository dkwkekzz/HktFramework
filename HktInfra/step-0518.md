# step-0518 — guildbanksagacons 금고 saga 회계 정합 (#46 금고↔가방 escrow 8)

> 직전 step-0517 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild.js` — 순수 accessor `bankSagaConsistent()`(거래소 0128·우편 0169 의 금고 판): 두 불변 ① `gives==ackedGives+pending`(보낸 give 는 정확히 acked 또는 pending·새는 give 0) ② `ackedGives==giveOks+giveFails`(회신 ok/fail 분류·누락 0). 세 체제(정상 drain·지속 손실 잔존·재전송 회복) 모두서 성립. 계층: 3 게임 서비스. verify 새 모드 `guildbanksagacons`. 순수 읽기(권위 0·실행 경로 무변경)→0517 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — accessor 순수 읽기·박스 비트 동일.
- 가설 `guildbanksagacons`: 5/5 OK — normal Y(pending 0)·loss Y(pending 1)·resend Y(pending 0) 세 체제 bankSagaConsistent 성립.
- spine ALL OK — #46 arc 8모드(saga 정합).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 세 체제 5시드 동일. ③권위: 회계 불변이 "보낸 give 는 acked 또는 pending 정확히 하나"(새는 give 0·중복 0)를 손실 하에서도 단언. ④은닉: 회계는 관측. ⑤headless: node in-proc. — 이상 없음(교차 회계 giveOks==가방 escrowXfers 는 0519·grand capstone 0520).
