# step-0488 — coordmergecap grand capstone 누적 회귀 승격 (#16 라운드 2차 8)

> 직전 step-0487 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0420 의 bespoke `coordmergecap`(#62 코드 합류 grand capstone: 단일 진입점 `runMultiViaCoord` 가 종합 warm-failover[migrate+reprovision+kill+promote]를 한 호출로 → runMultiCoherent·a1 보존·clusterInfo parity)을 누적 회귀로 승격. 공유 `coordScenario()`(2 host·3 zone)를 verify-kit 모듈 함수로 승격(coordcap 0489 와 재사용). 지역 helper zoneSpecOf/RUNMULTI_KEYS 함수 내부 지역화·ctx dep `runMultiViaCoord`/`coordAuthEquiv`. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 2차 8/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `coordmergecap`: 5/5 OK — runMultiCoherent·migrations1·reprovisions1·promotions1·a1 보존(match==total)·clusterInfo parity(15키 전부).
- spine ALL OK — coordmergecap 포함(#62 코디네이터 단일 진입점 복원력 항구 회귀).

## 척추 5항 + 한계

①tick: 검증 모드 승급·박스 무변. ②결정론: coherent/보존 대조·Math.random 0. ③권위: warm-failover(migrate+promote) 후 a1 보존이 소유 단일 재단언. ④은닉: 코디네이터 단일 진입점(runMultiViaCoord)·클라 무관. ⑤headless: 실 runMulti child_process·node 단언. — 이상 없음.
