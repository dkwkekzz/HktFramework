# step-0479 — 실 host.js child 경계 업스트림 9: 업스트림 경계 회계

> 직전 step-0478 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — `zoneEntity(cluster, host, zone, id)`(실 존 한 entity 위치 읽기) 추가. 계층: 엣지/월드(#70·SPINE §4 경로1 회계). 드라이버 미부착→run() reg 0. verify.js 새 모드 `upcaccount`.
- 업스트림 경계 회계: 실 UpClient 가 발신한 intent(`intentLog`·`intentDelta`) == 실 host.js 존 반영 — enter 위치 + Σ발신 move 변위 == 최종 실 존 위치(경계 넘어 발신 손실 0·클램프 0).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향).
- `upcaccount` 5/5 OK — intentLog 4(enter+3 move)·enterPos + Δ발신(4,3) == 최종 실 존 pos 전 시드(예: seed42 15,5+4,3→3,8·wrap).
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 이상 없음 ②결정론: 발신==반영 재현 ③권위 단일: 실 존 유일 소유 ④은닉: 게이트웨이-형 발신 ⑤headless: 실 child_process·경계 넘어 회계.
- 한계: 이상 없음. 다음 0480 grand capstone 이 발신→존→egress→클라 desync0·생애주기·회계·손실 멱등을 한 시나리오로 종합.
