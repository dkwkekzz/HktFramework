# step-0314 — #9 잔여: host 프로세스 entity 가중 부하 hostEntitySkew

> 직전 step-0313 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 질의 `hostEntitySkew()` = host 컨테이너별 *entity 수* 분포의 불균형 {hosts,min,max,skew}. hostLoadSkew(0311·존 수)와 달리 부하를 entity(≈동접 플레이어)로 — 존 1개라도 만원이면 무겁다. 존 수만 보면 균형으로 착각하는 실 부하를 드러내는 재배치의 더 정직한 척도.
- 계층: 코디네이션(⑤). 읽기 전용 → 0313 비트 동일. verify.js 새 모드 `hostentityskew`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0313) 비트 동일.
- `hostentityskew` 5/5 OK — z1@A·z2@B·z3@C(존 수 1·1·1=균형)인데 entity 는 z1(A)에 3·z2(B)에 1·z3(C)에 0: `hostLoadSkew().skew` **0**(존 수 균형 착각) vs `hostEntitySkew().skew` **3**(max 3·min 0·A 만원)·총 entity 4.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 질의(시뮬 외 0). ② 결정론: 시드 5종 동일. ③ 권위: 단일 소유 유지. ④ 은닉: orch 내부 컨테이너. ⑤ headless: Node 순수·reg 0.
- 한계: entity 가중도 *수*만(tick 비용·대역 가중 미반영). 가중 기반 자동 재배치(hostEntitySkew 트리거)는 후속. 이상 없음.
