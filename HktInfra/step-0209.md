# step-0209 — 로그인 큐 박스: enqueue/dequeue 기본

> 직전 step-0208 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/loginqueue.js`(LoginQueue) — 로그인 대기열 FIFO + 세션 티켓 발급. 계층: **엣지(계층1)**. `loginEnqueue{player}`(중복 멱등) → 큐 적층·`loginDequeue` → 맨 앞 입장+티켓 발급(`admitted` SSOT)·`queueLength`/`positionOf`/`ticketOf` 질의. 접속 폭주를 *엣지에서* 흡수(0001 LoginServer 일회 티켓 스텁의 대기열 실체화·§3 격차 메움)·존 tick 밖.
- 배선: `net-core.js` `__p('loginqueue')` · `topo-actors.js` · `topo-build.js`(`loginQueue` 플래그) · `topo-run.js`(handle+`loginOps` seam+return).
- OFF 플래그 **`loginQueue`**(OFF→박스 0·loginOps 주입 0 = 0208 비트 동일=회귀 0) · verify.js 새 모드 **`loginqueue`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(loginQueue 미제공 → 박스 0 → 0208 비트 동일).
- **가설 `loginqueue`**: 5/5 시드 OK — enqueue 4(p1·p2·p3·p1 중복 멱등) + dequeue 1 → **큐 길이 2·admitted 1·p1 tkt-1·p2 pos 0**·enq/deq 4/1.
- **결정론**: 5 시드 동일(FIFO 순서·티켓 단조). **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(loginEnqueue/Dequeue onMsg 반응·존 tick 밖) ② 결정론 ✅(`Math.random` 0·FIFO+티켓 단조) ③ 권위 단일 소유 ✅(대기열·admitted 가 엣지 입장 SSOT) ④ 은닉 ✅(loginOps gateway 발신·대기열은 엣지서 끝남) ⑤ headless ✅(Node·UE 0).
- 한계: enqueue/dequeue 만(줄서기+입장). 티켓 *만료*(TTL)·계정 검증·재접속은 후속(0210 만료·이후 2차). 멀티프로세스 미배선(#9).
