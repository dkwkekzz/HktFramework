# step-0250 — 배치 SSOT 실배선(#51) 10: placeQuery executed host(읽기 경로 완성)

> 직전 step-0249 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placeQuery` 회신(0204)에 실 가동 host `running` 필드 추가 — 게이트웨이가 존이 *실제로 도는 곳*(집행)으로 라우팅한다(0204 는 결정 host 만 회신했음). 읽기 전용(배치 무변경). 계층:코디네이션.
- OFF: `placeQuery` 미수신이면 reply 미발화=0249 비트 동일=회귀 0(reply 필드 추가뿐·baseline 모드는 placeQuery 미전송).
- verify.js 새 모드 `placequeryexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placequeryexec`**: 5시드 모두 reply host hostC·running hostC·rx 1·sent 1 — z1@A→C 이주 후 질의가 결정==집행 실 위치(hostC)를 회신.
- **결정론/권위/수렴**: `run.js all` ALL OK(20모드+classics).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면·순수 읽기 ②결정론: 시드 PRNG ③권위 단일 소유: 질의는 무변경(읽기) ④은닉: 게이트웨이는 placeReply 로만 위치 획득(orch 내부 무지) ⑤headless: node 검증. — 한계: running 은 orch 내 executed SSOT(실 EntityZone host 이주는 #51 잔여)·멀티프로세스 패리티(#9). 🔎 0241~0250 묶음 리뷰가 #51 잔여·다음 방향(#49 wiring 분할/#9 멀티프로세스/실 zone.js 핸드오프) 평결.
