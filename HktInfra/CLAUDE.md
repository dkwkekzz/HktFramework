# CLAUDE.md — HktInfra

> 루트 라우터. HktInfra 시리즈의 **궁극 목표 · 불변 규칙 · step 작성법**만 보관한다.
> 네트워크 인프라 큰 그림(척추)은 [SPINE.md](SPINE.md), 현재 위치는 [STATE.md](STATE.md).
> 프로젝트 전체 절대 원칙은 [../CLAUDE.md](../CLAUDE.md) · 시뮬 도메인은 [../HktGameplay/CLAUDE.md](../HktGameplay/CLAUDE.md).

---

# 큰 목표

**[HktGameplay](../HktGameplay/CLAUDE.md)를 시뮬레이션 모듈로 삼아, 오픈월드 MMORPG 네트워크 인프라를 점진적·단계별로 구축한다.**

HktGameplay 는 이미 토대를 준다 — **서버 권위**, **HktCore(순수 C++ 결정론 VM, UObject 0)**, 클라가 받는 **읽기 전용 `FHktWorldView`**, **GGPO 롤백(30Hz)**. 그러나 이 토대는 *작은 세션*(락스텝 격투 모델)에 맞춰져 있다. 단일 VM + 전역 락스텝은 수천 명이 거대한 세계를 공유하는 오픈월드로 **확장되지 않는다**. HktInfra 는 이 간극을 메운다 — 단일 인스턴스를 *공간으로 분할되고, 참여자마다 자기 관심 영역만 보며, 권위가 서버 사이를 매끄럽게 이주하고, 세계가 세션보다 오래 사는* MMO 인프라로 키운다.

핵심 신념(척추, [SPINE.md](SPINE.md) 필독): **한 프로세스가 모든 일을 하면 모든 일이 서로의 병목이다.** 오늘은 로그인·가방(인벤토리)·이벤트·영속·시뮬이 전부 UE 서버 한 프로세스 안에 산다 — 이를 상용 오픈월드 MMORPG 의 **6계층**(엣지 · 월드 · 게임 서비스 · 이벤트 버스 · 코디네이션 · 데이터)으로 분리한다. 단 두 가지를 신성하게 지키며: **시뮬 tick 에는 시뮬만**(시뮬 외 작업은 전부 비동기 서비스/버스 경유), **클라이언트는 내부를 모른다**(게이트웨이 단일 연결). 목표 토폴로지·계층별 책임·씨앗 매핑은 [SPINE.md](SPINE.md).

이 목표는 한 번에 도달하지 않는다. **큰 목표(이 문서)** 를 향해 **작은 실행 단위(step)** 로 나아가며, **상태 파일([STATE.md](STATE.md))** 이 "지금 어디까지 왔는가"를 한 곳에 기록한다.

---

# 작업 구조 — 3축

작업은 항상 세 축이 함께 굴러간다. 역할이 겹치지 않게 분리한다.

| 축 | 파일 | 역할 | 갱신 주기 |
|---|---|---|---|
| **큰 목표** | `CLAUDE.md` (이 문서) | 궁극 목표 · 불변 규칙 · step 작성법. *무엇을 향해, 어떻게* | 거의 불변 |
| **작은 실행 단위** | `step-NNNN.md` + 프로토타입/검증 | 한 step = 한 조각. 직전 step을 잇고 *하나만* 더한다 | step마다 새로 추가 |
| **상태 파일** | `STATE.md` | 현재 위치 · 다음 가설 · 열린 격차 · 정전 제약 · 시리즈 인덱스. *지금 어디까지* | step마다 갱신 |

> **단일 진실 원천(SSOT)**: "지금 어디까지 왔고 다음은 무엇인가"는 **오직 `STATE.md`** 에만 산다. 닫은 step 문서(`step-NNNN.md`)는 그 step의 *기록*이므로 이후 수정하지 않는다.
>
> **STATE.md 는 고정 크기 대시보드(에이전트 효율)**: §1~6(NOW·NEXT·OPEN GAPS·DURABLE CONSTRAINTS·6요소·빠른참조)은 step 마다 **덮어쓴다(rewrite)** — 누적하지 않는다. 오직 §7 INDEX 만 1행 append. *의외의 발견·정직한 한계의 전문*은 STATE 가 아니라 `step-NNNN.md` 에 산다(STATE = 현재의 SSOT, step 문서 = 역사의 SSOT). 발견/한계가 다음 결정에 아직 작용(load-bearing)하면 STATE §3 OPEN GAPS 또는 §4 DURABLE CONSTRAINTS 에 *현재 상태로만* 반영하고, 후속 step 이 해소하면 인덱스 한 줄로 떨어진다.

---

# 불변 규칙

- 각 step에서는 궁극적 목표(오픈월드 MMO 인프라)를 향해 나아가야 한다.
- 각 step은 분명하게 정의될 수 있고 **검증될 수 있는** 작업으로 정의한다.
- 한번에 많은 작업을 하지 않는다(한 step = 한 조각). 나머지는 다음 step으로 전가한다 — 단 *서로 독립인 여러 조각*은 한 라운드에 동시에 제안·처리할 수 있다(아래 "다음 step 제안 — 병렬 팬아웃"). 팬아웃은 조각의 *수*를 늘릴 뿐 *크기*는 키우지 않는다.
- 새 step 프로토타입은 이전 step 을 잇고, 기존 파라미터에서 **회귀 0**(비트 단위 동일)을 verify `reg`로 증명한다.
- 모든 수치는 시드 [42, 7, 1234, 99, 2026] 평균으로 재현 가능해야 한다. **문서의 수치 = verify 출력**.
- **결정론 전파**: 같은 intent 로그를 먹은 모든 참여자(서버·클라·리전)는 같은 월드 해시로 수렴해야 한다 — `Math.random` 금지, 시드 의사난수만. (이것이 netcode 의 토대 — [SPINE.md](SPINE.md) §5 ②)
- **권위 보존**: 모든 엔티티는 매 tick *정확히 한* 권위 소유자를 가진다 — 공백(무권위)도 중복(이중쓰기)도 없다. 권위 이동은 release+acquire **쌍 거래**(닫힌 장부의 netcode 판). ([SPINE.md](SPINE.md) §5 ③)
- **수렴(desync 0)**: 뷰가 겹치는 두 참여자는 겹친 영역에서 (조정 후) 일치해야 한다. 클라 예측 뷰는 권위 재현으로 desync 0 에 수렴한다.
- **HktGameplay 원칙 상속(위반 금지)**: ① HktCore 순수성(UObject 0, 순수 결정론 VM) ② 서버 권위(클라는 읽기전용 `FHktWorldView` 만) ③ ISP 3-Layer(Intent→Sim→Presentation 역방향 의존 금지) ④ VM 직접쓰기 금지(`SetPropertyDirty` 경유). 인프라는 이 원칙들을 *확장*하되 *깨지 않는다*.
- **척추 준수 ([SPINE.md](SPINE.md))**: 새 기능은 큰 그림 6계층의 박스/계약을 채우는 것이어야 한다. 새 step 은 **척추 체크 4항**(① 신성한 tick ② 결정론 코어 ③ 권위 단일 소유 ④ 은닉·단일 연결)을 가설 4기둥과 함께 통과시킨다. 하나라도 어기면 척추에서 벗어난 것 — 재설계한다.
- 설계하지 않았는데 떠오른 결과는 "의외의 발견"으로, 실패는 원인 규명과 함께 "정직한 한계"로 *그 step 문서(`step-NNNN.md`)에* 기록한다. STATE.md 는 이를 중복 누적하지 않는다.

---

# 작은 실행 단위 — step 작성법

**산출물 형태 = 헤드리스 프로토타입 먼저, 검증된 설계만 UE 승격**. HWS 가 64×64 웹 시뮬로 동역학을 먼저 증명하고 UE 로 올렸듯, HktInfra 는 *netcode 프로토콜·토폴로지*를 결정론적 헤드리스 하니스로 먼저 증명한 뒤 HktInfra UE C++ 모듈로 승격한다. 프로토타입의 "VM" 은 실제 HktCore 가 아니라 *최소 결정론 커널*(netcode 불변을 자극하기에 충분한 스텁) — 실제 HktCore 결합은 UE 승격 단계의 일이다.

step마다 다음 산출물을 만든다.

- `step-NNNN.md` — 문서. 도입부에 "6요소 지도" 표, 가설·검증 결과·다음 예고를 담는다.
- `step-NNNN/net-core.js` — 브라우저/Node 겸용 프로토타입 코어. **이 step 이 더한 인프라 로직은 여기에만 산다.** 서버 박스들(로그인·게이트웨이·존·서비스… — 인프로세스 액터 + 메시지 큐) + 전송 모델(지연·손실·순서) + 이번 step 의 계약.
- `step-NNNN/verify.js` — 헤드리스 검증. 회귀(`reg`)·결정론 전파·권위보존·수렴(desync)을 수치로 출력.
- (선택) `step-NNNN/panel.js` + `step-NNNN.html` — 시각 관찰 셸(토폴로지·대역폭·desync 타임라인). 관찰이 통찰을 주는 step 에서만.

> **공통 하니스 엔진**: 결정론 VM 커널 · 전송 모델 · 참여자 스케줄러는 복제하지 않는다. HWS 가 step-0007 에서 `engine/` 으로 공통화했듯, 초기 몇 step 뒤 `engine/` 한 곳으로 모은다(중복 방지). 매 step 은 그 위에 net-core.js(새 프로토콜) + verify.js 만 더한다.

검증의 4기둥(매 step 통과해야 닫을 수 있다):

1. **회귀 0** — 새로 더한 항을 끄면(계수/플래그=0) 직전 step과 비트 단위 동일.
2. **결정론 전파** — 같은 시드·같은 intent 로그 → 모든 참여자 전 셀 비트 비교 + 상태 해시 일치(서버 1회 + 클라/리전 재현).
3. **권위 보존 + 수렴** — 매 tick 모든 엔티티 소유자 수 = 1(공백·중복 0), 겹친 뷰 desync = 0(조정 후).
4. **가설 검증** — 이 step이 더한 한 조각이 의도한 현상을 수치로 보였는가(예: AOI 가 대역폭 X% 절감, 핸드오프 N tick 내 완료·desync 0).

---

# 다음 step 제안 — 병렬 팬아웃 (STATE §2)

step 을 닫을 때 STATE §2 "다음"은 **한 가설이 아니라 *동시에 처리 가능한 여러 독립 조각*** 을 제안한다. 작업자(에이전트)가 여럿이면 한 라운드에 여러 조각을 병렬로 집어 처리량을 올린다. 이때 다음 다섯 규칙으로 팬아웃이 방법론을 깨지 않게 한다.

1. **조각 크기 불변** — 팬아웃은 *한 step = 한 조각*을 바꾸지 않는다. 한 번에 *여러 조각을 제안*할 뿐, 각 조각은 그대로 작고 검증 가능하다.
2. **독립 판정(한 줄)** — 두 조각이 *같은 코어 이음새*(`engine/` 의 같은 함수·박스, 특히 존 VM tick)를 변형하면 **직렬**, 아니면 **병렬**. [SPINE.md](SPINE.md) 6계층에서 서로 다른 박스 + 서로 다른 verify 차원을 채우면 대개 독립이다.
3. **임계 경로는 하나** — 같은 이음새를 사슬로 잇는 조각(특히 존 VM 결정론을 직접 전진시키는 단계)은 한 라운드에 **🔴 임계 경로 하나만** 둔다. 같은 이음새의 나머지는 다음 라운드로.
4. **공통 토대 = 동결 `engine/`** — 동시 라운드의 조각들은 직전 닫힌 step 이 아니라 *동결된 `engine/`* 을 공통 토대로 잇고, **회귀 0 을 `engine/` 기준선 대비** 증명한다. (`engine/` 미추출이면 그 라운드 첫 직렬 작업으로 추출 — 안 그러면 각 조각이 직전 step 을 포크해 코어가 충돌하고 reg 기준선이 갈라진다.)
5. **닫기는 한 번에(SSOT 보존)** — 병렬 조각은 닫을 때 STATE §1~6 을 *각자* 덮어쓰지 않는다. 라운드의 조각이 다 닫히면 STATE 를 **한 번** 갱신하고, §7 INDEX 에 조각마다 1행 append 한다(재선형화). 각 조각의 *의외의 발견·정직한 한계 전문*은 각 `step-NNNN.md` 에 남긴다.

> 요컨대: **"한 조각씩"의 엄격함은 그대로 두고, *동시성*만 더한다.** STATE §2 가 매 라운드 "임계 1 + 독립 병렬 N" 형태로 다음을 제시한다.

---

# Step 루프 — 작업 절차

매 작업은 이 순환을 한 바퀴 돈다.

1. **읽기 (필독 3종)** — `CLAUDE.md`(목표·규칙) · **[SPINE.md](SPINE.md)(척추)** · `STATE.md`(현재 위치·다음 가설). **SPINE.md 숙지 없이 step 을 시작하지 않는다** — 한 조각이 큰 그림의 어느 박스/계약을 채우는지부터 본다.
2. **계획** — `STATE.md` §2 "다음"이 제안한 *서로 독립인 조각들* 중 이번에 처리할 조각을 고른다(작업자가 여럿이면 독립 조각 여러 개를 동시에 집어도 좋다 — "다음 step 제안" 참조). 각 조각은 여전히 *하나의 조각*. 더 떠올라도 다음 라운드로 전가. 척추 체크 4항에 어긋나면 조각을 고쳐 잡는다.
3. **구현** — 직전 step 프로토타입을 잇고 항을 *하나만* 더한다. 산출물 작성.
4. **검증** — `verify.js` 로 가설 4기둥 + **척추 체크 4항**을 모두 통과시킨다. 통과 못 하면 step을 닫지 않는다.
5. **갱신** — 발견/한계의 *전문*은 이번 `step-NNNN.md` 에 기록한 뒤 `STATE.md` 를 갱신한다(§1~6 덮어쓰기 / §7 인덱스만 append). **§2 "다음"은 단일 가설이 아니라 다음 라운드의 *임계 1 + 독립 병렬 N* 팬아웃으로 적는다**("다음 step 제안"). 병렬 라운드면 조각이 다 닫힐 때까지 §1~6 갱신을 미루고 한 번에 덮어쓴다.
6. **닫기** — step 문서 도입부 "6요소 지도" 표를 이번 step 기준으로 확정. 닫은 step 문서는 이후 수정하지 않는다.

> 각 step 문서의 "다음" 절이 다음 조각들을 제안하지만, **현재 위치와 다음 팬아웃의 권위는 `STATE.md`** 다. 둘이 어긋나면 `STATE.md` 를 따른다.
