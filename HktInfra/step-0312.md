# step-0312 — #9 잔여: host 프로세스 생애주기 이벤트 로그 hostLifecycle

> 직전 step-0311 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js`·`src/orch-hostproc.js`·`src/topo-build.js` — `_hostSet` 의 host 컨테이너 spawn(첫 존)/despawn(마지막 존)을 순서 있는 이벤트 스트림 `hostLifecycleLog`[{host,kind,seq}]로 기록. hostRegisters/hostDeregisters(누적 *수*)가 *얼마나*라면, 이건 *언제 어느 host*가 떴다/졌다 — 실 `cluster.spawnOne(host)`/`killHost(host)` 호출이 들어갈 지점·운영 타임라인의 씨앗. 질의 `hostLifecycle()`·`hostSpawnCount()`·`hostDespawnCount()`·`hostLifecycleNet()`(로그를 접은 현 가동 host 집합).
- 계층: 코디네이션(⑤). OFF 플래그 `zoneHostLifecycle`(OFF→로그 0·0311 비트 동일). verify.js 새 모드 `hostlifecycle`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneHostLifecycle OFF → baseline(0311) 비트 동일.
- `hostlifecycle` 5/5 OK — 3 host(A·B·C) spawn 후 hostA 드레인 → z1 이주(A 마지막 존 잃음) → A despawn: spawn **3**·despawn **1**·hostLifecycleNet() 집합 == zoneHostHosts() (net==live)·spawn−despawn(2) == 현 host 수.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 로그 append 만(시뮬 외 0). ② 결정론: 시드 5종 동일·이벤트 순서 결정론. ③ 권위: 로그가 roster 단일 소유의 정직한 역사(net==live). ④ 은닉: orch 내부 컨테이너. ⑤ headless: Node 순수·reg 0.
- 한계: 로그는 *논리* 컨테이너 spawn/despawn 만(실 OS 프로세스 spawn 미연결 — 이 지점이 cluster.spawnOne 훅 자리). 이상 없음.
