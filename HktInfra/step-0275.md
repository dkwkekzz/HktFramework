# step-0275 — #51b 실 zone.js 브리지 4: _hostDown 실 런타임 재가동

> 직전 step-0274 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `_bridgeHostDown(zoneId,target)` 추가: 죽은 host 의 실 EntityZone 런타임을 생존 host 에 **새 인스턴스**로 재가동(zoneRuntimes 의 zone 핸들 교체·host=target). migrate(자발·같은 핸들·상태 보존)와 **결정적 구분** — 죽은 host 런타임은 소실이므로 graceful 이주 불가·상태 보존 불가(복구는 영속서 후속·범위 밖). + `runtimeOn(host)` 질의(host별 실 런타임 수).
- `orchestrator.js` — `zoneRescued` 계측.
- `orch-placement.js` `_hostDown` 루프에 가드 1줄: `zoneBridge` ON 이면 `_bridgeHostDown` 도 호출. OFF→0274 비트 동일.
- OFF 플래그 `zoneBridge`(기존). verify.js 새 모드 `zonehostdown`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonehostdown`**: 5/5 seed — hostA(z1·z2) 장애 후 `runtimeOn(hostA)`=0·z1·z2 생존 host(hostC) 재가동·`zoneRescued`=2·runtimeCount=3 보존(공백/중복 0·단일 소유).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 제어 평면·존 tick 밖. ②결정론 — 재가동 host=최소부하 결정론 tie-break. ③권위 단일 소유 — 한 존 정확히 한 host 회복(죽은 host release 불가→재배치). ④은닉 — orch 집행 책임. ⑤headless — 실 EntityZone. 한계: 비자발 장애는 상태 *소실*(새 인스턴스)이 정상 — 잃은 entity 복구는 영속/스냅샷 계층(범위 밖·후속). migrate(0273)와 의미 분리 명확. 이상 없음.
