# step-0152 — MailFeed 읽음 반영(mailFeedRead — svc.mail.read 구독→unread--)

> 직전 step-0151 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mailfeed.js` — MailFeed 가 `svc.mail.read`(0147)도 소비: 그 수신자 `unread--`·`read++`. `readOf` accessor 추가. 0151 의 단조 증가(읽어도 안 줆) 해소 → 미읽음 배지가 *읽으면 준다*(MarketFeed 0116 만료 반영의 우편 판). 계층:게임 서비스.
- 배선: `topo-build.js`(`mailFeedRead` 플래그·buildSubs 전달) · `topo-subs.js`(`mailFeedRead && mailReadPublish`→`svc.mail.read`→mailfeed 구독).
- OFF 플래그 `mailFeedRead`(기본 false) → read 토픽 미구독 = 0151 비트 동일(회귀 0). verify.js 새 모드 `exmlfread`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · 권위 보존(feed 권위 0) · spine ALL OK.
- 가설 `exmlfread`(h1 3통 입금+수령·h2 2통 미수령): h1 unread/read=0/3·h2=2/0·totalUnread=2·unread==sent−read(`예`) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 반응형(onTick 0) ②결정론 난수 0 ③권위 우편함 권위 불변 ④은닉 버스 구독만 ⑤headless `node run.js` 통과 — 이상 없음. unread==sent−read 는 만료(0153) 반영 전까지 성립 — 만료가 더해지면 unread==sent−read−expired(0155 capstone 에서 닫음).
