# step-0254 — 캐시 negative caching (침투 방어)

> 직전 step-0253 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheNegative{on}` 토글 추가. 계층: 데이터(캐시). OFF 플래그 `negativeCache`(미설정→negatives 미사용·miss 경로 무변경=0253 비트 동일).
- 메커니즘: ON 이면 read-through miss 가 *소스에도 없을* 때 그 키를 known-absent(`negatives` Set)로 기억 → 같은 미존재 키 재조회는 소스 조회 없이 `negHits` 즉답(DB 침투 폭주 방어·Redis cache penetration 더미판). 키가 set 되면 negatives 에서 제거(`_set`).
- verify.js 새 모드 `cacheneg`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(cacheNegative 미수신·negativeCache=false → 비트 동일).
- `cacheneg`(가설): 5/5 — ON: 미존재 x 2회 get → negHits 1·x∈negatives·set 후 해제 true. OFF(대조군): negHits 0·negatives 빔.
- spine: ALL OK(누적 회귀 + cacheneg 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: 읽기 경로(소스 SSOT 미변경·known-absent 는 set 시 가역) ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음.
