# step-0255 — 캐시 put-if-absent (SETNX)

> 직전 step-0254 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheAdd{key,value}` 핸들러 추가. 계층: 데이터(캐시). 플래그 없음(새 메시지 타입·미수신→0254 비트 동일=reg 0).
- 메커니즘: 키가 *없을 때만* `_set` 하고 added=true, 이미 있으면 무변경·added=false(`cacheAddReply` 회신·`_lastAdd` 보관). Redis SETNX 더미판·최초-기록-승(first-writer-wins)·분산 락/유일 점유 primitive(예: 인스턴스 좌석 선점).
- verify.js 새 모드 `cacheadd`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(cacheAdd 미수신 → 비트 동일).
- `cacheadd`(가설): 5/5 — add k=v1(없음)→added true·store=v1. add k=v2(있음)→added false·store 여전히 v1(최초-기록-승).
- spine: ALL OK(누적 회귀 + cacheadd 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: 최초-기록-승(단일 점유 보장) ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음.
