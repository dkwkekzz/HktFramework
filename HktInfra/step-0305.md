# step-0305 — 정리 분할: host 프로세스 컨테이너 층(0301~0304) → orch-hostproc.js

> 직전 step-0304 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- **정리 step(기능 0·reg 0)** — `src/orch-zonebridge.js` 가 29.4KB 로 >30KB 트리거에 근접(다음 기능 step 이 넘김). host 프로세스 컨테이너 층(0301~0304·#9 잔여)을 새 박스 `src/orch-hostproc.js` 믹스인으로 분리.
  - 옮긴 것: `_hostSet`·`hostRuntimeCount`·`zoneHostOf`·`zoneHostHosts`·`hostRegistered`·`zoneHostSingleOwner`·`zoneHostDrift`.
  - 남긴 것: 실 zone.js 브리지 lifecycle(`_bridgeStart/migrate/hostdown/stop`)·전송 seam(`_zoneDeliver`)·런타임 tick(`_tickRuntimes`)·#56 데이터 평면 질의.
- `src/orchestrator.js` — `OrchHostProc` require + `Object.assign(prototype, OrchHostProc)`. 같은 prototype 에 되섞으므로 `this._hostSet` 등 그대로 해소(투명 분할·orch-placement 0251·orch-control 0267 동형). 계층: 코디네이션(⑤). 기능 플래그 없음.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 분할 후에도 baseline(0304) 비트 동일(정의 위치만 이동·동작 불변).
- `zonehostroster` 5/5 OK — 분리된 `_hostSet`·roster 질의가 prototype 으로 그대로 해소(reg5−dereg3=net2==hosts==running·data 평면 보존).
- 박스 크기: orch-zonebridge.js 29.4KB→26.7KB·orch-hostproc.js 5.0KB(둘 다 ≤30KB).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick ② 결정론 ③ 권위 ④ 은닉 ⑤ headless — 전부 동작 불변(reg 0 이 5항 보존을 직접 증명·정의 위치만 이동). 이상 없음.
