# step-0205 — 캐시 박스 분리: set/get 기본

> 직전 step-0204 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/cache.js`(CacheStore) — 핫 데이터(세션·가방·시세) 1홉 캐시 계층. 계층: **데이터(계층6)**. `cacheSet{key,value}` → store(같은 key 덮어씀·최신)·`get`/`has`/`size` 질의. 매 조회가 DB 직행하지 않게(Redis 의 더미판)·존 tick 밖·onTick 없음.
- 배선: `net-core.js` `__p('cache')` · `topo-actors.js`(import+`case 'cache'`) · `topo-build.js`(`cacheService` 플래그) · `topo-run.js`(handle+`cacheOps` seam+return).
- OFF 플래그 **`cacheService`**(OFF→박스 0·cacheOps 주입 0 = 0204 비트 동일=회귀 0) · verify.js 새 모드 **`cacheset`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(cacheService 미제공 → 박스 0 → 0204 비트 동일).
- **가설 `cacheset`**: 5/5 시드 OK — set 4(session/inv/price + session 덮어씀) → **size 3·session:h1=gw2(최신)·sets 4**.
- **결정론**: 5 시드 동일. **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(onTick 없음·cacheSet onMsg 반응·존 tick 밖) ② 결정론 ✅(`Math.random` 0·캐시=cacheOps 시드 순서) ③ 권위 단일 소유 ✅(캐시는 파생 핫뷰·권위는 원장/DB·캐시 자체는 휘발 사본) ④ 은닉 ✅(cacheOps gateway 발신) ⑤ headless ✅(Node·UE 0).
- 한계: set/get(hit)만. read-through(miss→소스 조회)·write-behind·TTL/eviction 은 후속(0206 read-through·이후 2차). 멀티프로세스 미배선(#9).
