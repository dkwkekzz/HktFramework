# step-0213 — 월드 영속 스냅샷 압축(worldSnapshot·무손실·로그 저장 유계)

> 직전 step-0212 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/worldlog.js` — `worldSnapshot` → ① replay 로 투영 현재화 → ② 투영을 스냅샷(깊은 복사)으로 굳히고 `snapshotSeq=jseq` → ③ 로그를 tail(seq>snapshotSeq)만 남기고 절단. `replay()` 는 스냅샷서 출발 후 tail 만 재적용. intent 로그가 무한히 안 자란다(저장 유계·가방0018/우편0146/길드0185 의 월드 판).
- 계층: 데이터(계층6). OFF 플래그: `worldSnapshot` 미주입(미발화 → 0212 비트 동일·reg 0). verify.js 새 모드 `worldsnap`.
- 2차 고도화 월드영속 #1 — append/replay(0207~0208·event sourcing) 위에 스냅샷+tail 압축.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (worldSnapshot 미주입 → 0212 비트 동일).
- worldsnap: 5/5 — 압축 replay digest(0x4c635da6) == 전체 replay digest(무손실)·tail len 2 < full len 5·snapshots 1·snapshotSeq 3.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 월드영속은 존 tick 밖·onTick 0. ②결정론: 로그 reducer 결정론(스냅샷+tail==전체)·시드 의사난수만. ③권위: intent 로그가 월드 상태 유일 쓰기 경로(스냅샷은 그 파생 사본). ④은닉: 클라 접점 무변. ⑤headless: Node·원격 재현(digest 비트 동일). — 이상 없음.
