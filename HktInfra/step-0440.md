# step-0440 — #4 진짜 비동기 10·grand capstone: async substrate E2E

> 직전 step-0439 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/verify.js` — 검증 전용 grand capstone `asynce2ecap`: M 복제 site 가 *서로 다른 순열+~20% 손실* 도착을 각자 `makeResyncSite`+holdback 으로 재구성·재전송 채움 → 0431~0439 의 모든 substrate(Lamport 클럭·인과 규칙·전순서·holdback·인과 배달·수렴·배리어-free·손실 resync·회계)를 한 시나리오로 단언. 코드 박스 무변경(async-core 동결) → reg 구조적 0.
- 계층: 코디네이션(#4). **#4 진짜 비동기 in-proc substrate sub-arc(0431~0440) 닫기** — 중앙 lockstep 없이 인과 복원으로 결정론 수렴하는 기계를 완성·검증.
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `asynce2ecap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `asynce2ecap` 5/5 OK — 4 site·56 이벤트·3 복제: ⒜ clock condition 위반 **0** ⒝ 전 복제 다이제스트 일치 == 정전 전순서(**desync 0**) ⒞ 인과 존중(전순서 sound: 엄격+간선 보존) ⒟ exactly-once(전 복제 complete·dups0·missing0) ⒠ 손실 실제 발생(resyncs>0).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(순열+손실 교란 무관·정전 수렴) ③권위 단일 ✅(exactly-once) ④은닉 ✅ ⑤headless ✅(순수 JS·검증 전용). 한계(정직): 이 arc 는 *in-proc substrate* — 실 `net.step()` lockstep 배리어를 아직 *치환하지 않음*(DownClient/UpClient 가 in-proc 먼저였듯 후속 arc 가 실 전송 배선). async-core 는 run() 밖에 살아 reg 구조적 0 — 통합 시 회귀 게이트 재확인 필요.
