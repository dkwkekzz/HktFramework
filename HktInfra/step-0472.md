# step-0472 — 실 host.js child 경계 업스트림 2: move intent 배달

> 직전 step-0471 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — `intentToZoneMsg` 에 `zoneMove`→존 move(`{type:'move', avatar, d:{dx,dy}}`) 번역 추가. 계층: 코디네이션/엣지(#70). 드라이버 미부착→run() reg 0. verify.js 새 모드 `upcmove`.
- 실 UpClient 의 이동 intent 가 경계 넘어 실 host.js 존에 적용(존 `onTick` 이 pending move 적용) → entity 가 (dx,dy) 만큼 이동. wrap 포함 결정론.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향 → baseline 비트 동일).
- `upcmove` 5/5 OK — enter 위치 + (2,1) == move+tick 후 실 존 위치 전 시드(wrap 예: seed42 15,5→1,6·seed2026 12,15→14,0).
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 실 존 별 프로세스 onTick 이 move 적용 ②결정론: enter rng+move 가산 재현 ③권위 단일: 실 host.js 존 유일 소유 ④은닉: intent→seam→실 존 ⑤headless: 실 child_process.
- 한계: 이상 없음. 위치 대조는 enter pos 를 snapshot 으로 읽어 (dx,dy) 가산 예측(rng 복제 불요).
