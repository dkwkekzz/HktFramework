# step-0223 — 오케스트레이터 부하 재배치 자동 트리거(placeRebalance·자동 균형)

> 직전 step-0222 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/orchestrator.js` — `placeRebalance{hosts[]}`: 후보 host 부하 불균형(최대−최소 ≥ 2)이면 최대부하 host 의 존(placement 삽입 순 첫 존)을 최소부하 host 로 자동 이주(0218 placeMigrate 의 자동 트리거판·release+acquire 쌍). 균형(gap<2)까지 한 패스 수렴·결정론 host/zone 순서.
- 계층: 5(코디네이션·오케스트레이터) · OFF 플래그: `placeRebalance` 미수신(미발화 → 0222 비트 동일=회귀 0) · verify.js 새 모드 `placerebalance`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `placerebalance`: 5/5 OK — 3존 모두 hostA(3/0/0) → rebalance → A1/B1/C1 균형·rebalanceMoves 2(z1→B·z2→C·z3 잔류)
- spine ALL OK

## 척추 5항 + 한계

①tick: 오케는 존 tick 밖 제어 평면. ②결정론: host/zone 순서 tie-break·시드 PRNG. ③권위: 이주는 placement Map 단일 키 원자 교체(release+acquire·공백/중복 0). ④은닉: 명시 메시지(placeRebalance)만. ⑤headless: Node·원격 재현. — 한계: 임계 gap=2 결정론 상수(부하 hysteresis·oscillation 회피는 후속).
