# step-0180 — 아이템 우편 saga liveness 회계 정합 capstone(sagaLivenessConsistent)

> 직전 step-0179 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`: 단언용 읽기 accessor `sagaLivenessConsistent()` = `pending.size == pendingGive.size + abandonedGive.size + permFailed` AND `sagaConsistent()`. 0172~0179 liveness arc 의 창발 불변(거래소 0140 의 우편 판). 계층: 게임 서비스(우편)·서비스간 saga.
- 미호출 read accessor — 동작 무영향(0179 비트 동일). verify.js 새 모드 `mailliveness`(네 체제 분할 단언). 코드 외 변경 없음(persist/topo 무변경).
- 의미: 미해결(미-ack) give 는 매 순간 정확히 한 상태 — 재전송 중(pendingGive)·재admission 대기(abandonedGive)·영구 종결(permFailed). 공백·중복 0. 아이템 우편 saga liveness arc(0166~0180) 닫기.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 미호출 accessor = 0179 비트 동일(전 시스템 5시드) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailliveness`**: 5시드 전부 — 네 체제 분할 항등식 성립: A 정상 0=0+0+0·B 재전송중 1=1+0+0·C abandon대기 1=0+1+0·D 영구종결 1=0+0+1·sagaLivenessConsistent 예(4/4).
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖 — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경(accessor 는 읽기). ③권위 단일 소유: 미해결 give 분할 단언(공백·중복 0)·원장 권위 불변. ④은닉·단일 연결: 클라 와이어 무변경. ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. 아이템 우편↔가방 saga liveness arc(0166~0180)가 거래소 0121~0140 의 우편 판으로 완성 — 회신/추적/재전송/정합/transfers/autoRetry/재시도상한/포기/재admission/발행 3종/readmitMax/liveness capstone.
