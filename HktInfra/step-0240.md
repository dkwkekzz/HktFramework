# step-0240 — loginabandon 모드 spine 승급 (#16 — 3차 라운드 누적 회귀화 10/10 완료)

> 직전 step-0239 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0230 의 `loginabandon`(로그인 큐 이탈) 모드를 누적 키트로 승급. 이 모드는 직전까지 `src/verify.js` 셸의 *라이브 bespoke* 였으므로, verify.js 에서 **제거**(중복 등록 방지)하고 verify.js 를 얇은 셸(ctx+kit.cli)로 정리. 계층: 검증 하니스(박스 코드 무수정). 박스 `.js` 0줄 → reg 0 자명. **#16 정리·도구 라운드 10/10 — 3차 균형 라운드(0221~0230) bespoke 10모드 전부 누적 회귀화 완료.**

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `loginabandon`: 5/5 OK — p2 이탈→큐 [p1,p3]·abandoned 1·미줄 pX miss 1. `node run.js all` 에서 정확히 1회 실행(중복 0).
- spine ALL OK — loginabandon 누적 회귀 편입. 0221~0230 10모드 전부 매 spine 양성 단언.

## 척추 5항 + 한계

①tick: 검증 모드 이동(시뮬 외)·이탈은 엣지(존 tick 밖). ②결정론: 결정론·Math.random 0. ③권위: 큐는 엣지 SSOT(이탈=슬롯 release). ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음. 의외의 발견: verify.js 가 처음으로 *새 bespoke 모드 0*인 순수 셸이 됨(승급으로 라이브 가설이 키트로 흡수).
