# step-0514 — guildbankcrash 금고 crash→reconstruct escrow 보존 (#46 금고↔가방 escrow 4)

> 직전 step-0513 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild.js` — `reconstruct()` 에 escrow 집합 재구성 추가(guildBankInv gated): crash 로 휘발한 `escrowIds`(가방 escrow 추적)를 예치/인출 저널 replay 로 복원한 vault 에서 재계산. 가방은 별 박스라 crash 무관·여전히 `'escrow'` 소유 → 재구성 escrowIds == Σvault == 가방 escrow(2-서비스 보존 0513 이 crash 후에도 성립). 거래소 0120·우편 0164 crash 체제의 금고 판. 계층: 3 게임 서비스. verify 새 모드 `guildbankcrash`. `invMode` OFF→skip→0513 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — invMode OFF→reconstruct escrow 블록 skip·박스 비트 동일.
- 가설 `guildbankcrash`: 5/5 OK — crash 전 escrowIds ["item1"]==crash 후·==invEscrow·vault ["item1"] 복원·bankConsistent.
- spine ALL OK — #46 arc 4모드(crash 보존).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: crash→reconstruct 5시드 동일. ③권위: 재구성 후 itemId 단일 길드 소유·escrow 집합 == 가방 권위(공백·중복 0). ④은닉: 재구성은 자기 저널만·가방 상태 미참조(escrowIds 를 vault 에서·가방 대조는 verify 관측). ⑤headless: node in-proc. — 이상 없음(crash 모의는 verify 가 휘발 projection 소실 후 reconstruct·저널/스냅샷 durable).
