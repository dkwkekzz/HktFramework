# step-0247 — 배치 SSOT 실배선(#51) 7: executed placeAuto(부하 기반 실 가동)

> 직전 step-0246 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placeAuto` 핸들러가 `placeExecute` ON 이면 최소부하 host 선택 + paper `placement` 갱신에 더해 실 존 런타임도 그 host 에 가동(`_start`) — 0217 advisory 자동 배치의 집행 판. 계층:코디네이션.
- OFF 플래그 `placeExecute`(OFF→placeAuto 는 paper 만=0246 비트 동일=회귀 0).
- verify.js 새 모드 `placeautoexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placeautoexec`**: 5시드 모두 running A2/B1/C1·starts 4·drift 0 — z1@A 후 z2→B·z3→C·z4→A 로 *실제* 부하 분산 가동(결정론 tie-break)·결정==집행.
- **결정론/권위/수렴**: `run.js all` ALL OK(17모드+classics).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면 ②결정론: 시드 PRNG·최소부하 tie-break 후보 순서 결정론 ③권위 단일 소유: _start 는 신규 가동(중복 0) ④은닉: orch 내부 ⑤headless: node 검증. — 이상 없음(런타임 crash/recover 는 0248~ 후속·그 뒤 게이트 #49 wiring 분할).
