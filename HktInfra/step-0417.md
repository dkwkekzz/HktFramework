# step-0417 — #62 코드 합류 7: fence/sweepSilence — epoch 펜싱 시나리오 번역·구동

> 직전 step-0416 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `coordScenarioFromOpts` 에 `fence`(수동 epoch 펜싱)·`sweepSilence`(매 tick lease-timeout 자동 펜싱 sweep) case 추가. coord `runScenario` 는 이미 둘을 처리(0401·0402) — 번역만 잇는다.
- 계층: 코디네이션(#62 코드 합류 — 옛 runMulti presumedDead/epoch 펜싱·observeSilence 를 선언 시나리오로).
- OFF 플래그: 미부착(verify 만 호출) → 회귀 0.
- verify.js 새 모드: `coorddelegfence`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일).
- 가설 `coorddelegfence`: 5/5 — 순수 번역 일치 Y + fence hostB 구동 후 epoch 1·presumedDead⊇{hostB}·fencedTicks 3(z2 펜싱·stale 발신 차단).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 이상 없음. 한계: fence 는 산 zone(z2)을 의도적으로 펜싱하므로 그 존 entity 가 실에서 사라져 coordDesync>0(=runMultiCoherent false 가 *정상*) — 이 모드는 coherence 가 아니라 펜싱 계측(epoch/presumedDead/fencedTicks)을 단언. split-brain 차단의 본질(0401).
