# step-0284 — #56 브리지 존 데이터 평면 4: migrate 무손실(행동적 보존) + census

> 직전 step-0283 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `totalEntities()`: 전 실 EntityZone 핸들 ents.size 합(census 기초). `_bridgeMigrate`(0273·같은 핸들 host 교체)가 entity 를 *행동적으로* 무손실 보존함을 데이터 평면으로 단언(0273 은 구조적=zoneStarts 불변만 증명). 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0283 비트 동일=회귀 0·`totalEntities` 는 미호출 읽기). verify.js 새 모드 `zonemigent`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zonemigent` 5/5: nomig(a1·a2·a3 enter + a1 move·hostA) vs mig(z1→hostC) → `total=3`·`cnt=3`·`a1pos동일=Y`(이주가 위치 안 건드림)·`rtHost A→C`·`zoneMigrations=1`. migrate 가 같은 핸들이므로 entity·위치 무손실.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: migrate 는 orch 제어 평면 host 원자 교체·존 tick 밖. ②결정론: 위치 시드 PRNG·`Math.random` 0. ③권위 단일 소유: 같은 핸들·단일 키 host 교체(중간 공백/중복 0). ④은닉: 게이트웨이는 placeMigrate 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 의외의 발견: 0273 이 "상태 보존"을 zoneStarts 불변(구조적)으로만 증명했는데, 이제 entity 수·위치(행동적)로도 확인 — 리뷰 #56 "migrate 무손실 vacuous" 해소. hostdown 소실·stop 폐기는 0285~ 후속.
