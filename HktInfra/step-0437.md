# step-0437 — #4 진짜 비동기 7: 배리어-free 진행

> 직전 step-0436 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `makeAsyncSite(nsites)`: `receive`(도착=버퍼)와 `tick`(진행=받은 것 1개 holdback 반영)을 분리해 site 가 *독립 속도*로 전진. lockstep(net.step 의 한 동기 tick)과 대비 — 한 site 가 5보 가는 동안 다른 site 는 1보 가도(진행 skew>0=비-lockstep) holdback 안정성은 페이스 무관.
- 계층: 코디네이션(#4). 0436 수렴(같은 도착 다른 순열)을 *진행 입도 독립*으로 확장 — 중앙 배리어 없이 site 들이 제각각 굴러도 desync 0.
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `asyncprogress`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `asyncprogress` 5/5 OK — 4 site·48 이벤트·3 복제, 불균등 스케줄(임의 복제 1~4보 burst)에서 진행 skew 16~32(명백한 비-lockstep)인데도 복제 3개 최종 다이제스트 전부 일치 == 정전 전순서 → desync 0(진행 입도 무관).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅(오히려 *시뮬 tick 의 동기 배리어 의존을 줄이는* substrate) ②결정론 ✅(최종=내용 함수·페이스 무관) ③권위 ✅ ④은닉 ✅ ⑤headless ✅. 한계: 진행은 독립이나 *손실 0* 가정 유지 — 손실 하 gap-resync 수렴은 0438. 이 substrate 가 실 net.step 배리어를 치환하는 배선은 후속 arc.
