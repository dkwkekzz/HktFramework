# step-0266 — 정리(#49 인접·선제): svc-inventory-core 생성자 필드 초기화 믹스인 분리

> 직전 step-0265 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-inventory-core.js`(28.5KB·30KB 근접·성장 박스 선제 정리) → `InventoryService` 의 *생성자 필드 초기화*(가방 원장·역인덱스·영속·quorum·신뢰 전달·버스 failover·saga dedup ~120 필드)를 `_init(opts)` 메서드로 빼 새 박스 파일 `src/svc-inventory-init.js` 믹스인으로 분리. 계층: 게임 서비스(가방). **OFF 플래그 없음 — 투명 분할**(필드 초기화 verbatim 이동·동작 비트 불변). 생성자는 `this._init(opts)` 한 줄.
- 배선: svc-inventory-core.js 가 `require('./svc-inventory-init.js')` 1줄 · 클래스 정의 직후 `Object.assign(InventoryService.prototype, InventoryInit)`(생성 시 호출). 0053 txn/persist/bus 증강의 init 판. 잔류(코어): 생성자(위임)·_own·_unown·crash·itemCount·ownerOf. verify.js 새 모드 `invsplit`.
- 결과 크기: svc-inventory-core.js 28.5KB→5.7KB·svc-inventory-init.js 24.5KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `invsplit`(가설): 5/5 — `new InventoryService({persist,quorumW:2,reliable,snapshot:6})` 후 quorumW 2·reliable true·snapInterval 6·durableSeq -1·원장/역인덱스 빈 Map·crash 후 재초기화 정합. 옮긴 _init 가 생성/재초기화 무결.
- spine: ALL OK(전 역사 누적 회귀 + invsplit 편입).

## 척추 5항 + 한계

①tick: 생성자 초기화는 박스 구성(존 tick 밖)·무변경 ②결정론: 코드 이동만·시드 동일 ③권위: 원장 소유자=1 불변 무변경 ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할·생성자 본문 외부화).
