# step-0362 — #57 실 데이터 평면 2: 실 host.js zonedel 존 제거

> 직전 step-0361 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/host.js` — `zonedel` cmd: 가동 중 host 에서 존 액터 제거(`actors.delete`·`specByAddr.delete`·다른 액터 보존). stop/migrate-out 의 실 host 집행. 기존 cmd 무변경.
- `src/cluster-hostdriver.js` — `flush` 가 `stop`(onUnassign 산출) 명령을 specOf(실 모드)일 때 `cluster.rpc(host,{cmd:'zonedel',addr:zone})` 로 집행.
- 계층: 코디네이션/월드(⑤②). OFF 플래그 `clusterDriverReal`(미주입→호출 0 = baseline 비트 동일)·host.js 신규 cmd 는 기존 무변경(e2e 동일). verify.js 새 모드 `hostzonedelreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0361) 비트 동일.
- `hostzonedelreal` 5/5 OK — z1·z2@hostA·placeStop z1: 드라이버 commands==[spawnOne:hostA,init:z1,init:z2,stop:z1]·flush 후 실 host.js snapshot zones==[z2](z1 제거·z2 보존)·in-proc `running`==[z2](정합).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: zonedel 은 제어 평면 cmd. ② 결정론: 5종 동일. ③ 권위: 존 제거가 in-proc running 과 실 host snapshot 둘 다 일치(단일 소유). ④ 은닉: addr 만. ⑤ headless: 실 host.js cmd·snapshot — 원격 검증.
- 한계: 단순 제거(상태 이전 없음) — migrate 의 상태 보존(snapshot+loadstate)은 후속(0364). 이상 없음.
