# step-0354 — #57 실 host.js OS 프로세스 spawn 4: clusterDriver 존 roster (onAssign/onUnassign)

> 직전 step-0353 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-bridge-init.js` — recorder 드라이버에 `onAssign(host,zone)/onUnassign(host,zone)` + 회계 `driverAssigns/driverUnassigns`. 존↔host 귀속(실 cluster.init/loadstate·migrate-in/out 입력).
- `src/orch-hostproc.js` — `_hostSet` 에서 존을 컨테이너에서 떼면 `onUnassign(h,zone)`, 새 컨테이너에 붙이면 `onAssign(host,zone)` 호출(spawn/despawn 프로세스 lifecycle 과 직교한 *존-수준* 귀속). 미부착→호출 0.
- 계층: 코디네이션(⑤). OFF 플래그 `clusterDriverRecord`(드라이버 null→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostroster`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미부착 → baseline(0353) 비트 동일.
- `hostroster` 5/5 OK — z1@hostA·z2@hostA·z2 migrate→hostB 뒤 `assigns`==[hostA:z1,hostA:z2,hostB:z2]·`unassigns`==[hostA:z2]·net roster(assign−unassign) A=[z1]·B=[z2]==`hostSpawnPlan`·`driverAssigns`−`driverUnassigns`==`runningCount`.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 귀속 훅은 배치 집행(_hostSet) 안·시뮬 tick 밖. ② 결정론: 5종 동일·assign/unassign 순서=배치 op 순서. ③ 권위: net 귀속(assign−unassign)==running(존 단일 소유). ④ 은닉: host:zone 키만(직렬화 가능). ⑤ headless: Node 순수·reg 0.
- 한계: 아직 귀속 *이벤트*만 — 실 cluster.init/loadstate 집행은 cluster-run.js 후속(0357). 이상 없음.
