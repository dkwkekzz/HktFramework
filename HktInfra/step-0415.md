# step-0415 — #62 코드 합류 5: coordAuthEquiv — runMultiViaCoord 실 cluster == in-proc 권위 동치

> 직전 step-0414 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `coordAuthEquiv(coord, cluster, ents)` 추가: 코디네이터가 구동한 *실 cluster* 의 entity 위치가 *in-proc run() 권위*(`coord.orch.zoneEntityPos`)와 일치하는 수를 센다. in-proc run() 은 옛 runMulti 가 비트 동일(e2e)로·runMultiViaCoord 가 coordDesync 0 으로 각각 귀착하는 공유 기준 → 둘 다 같은 권위로 수렴 ⇒ runMultiViaCoord ≡ runMulti(zone subset). 반환 `{match,total}`.
- 계층: 코디네이션(#62 코드 합류 — 합류 경로가 옛 runMulti 와 동치임을 권위 기준으로 단언).
- OFF 플래그: 미부착(verify 만 호출) → 회귀 0.
- verify.js 새 모드: `coordvsauth`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일).
- 가설 `coordvsauth`: 5/5 — migrate+reprovision 한 호출 후 a1@z1·b1@z2 실 위치 == 권위(match 2/2)·coherent Y.
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉 ⑤headless — 이상 없음. 한계: 동치는 *권위 귀착*(transitive)으로 증명 — 옛 runMulti(in-proc actor host)와 runMultiViaCoord(실 host.js child)를 *직접* 비트 대조하지 않는다(서로 다른 substrate). 공유 기준 in-proc run() 권위에 둘 다 수렴함이 동치의 근거.
