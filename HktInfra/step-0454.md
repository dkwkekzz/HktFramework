# step-0454 — #4 실 net.step 배리어 실제 치환 4: move 손실+resync 복원

> 직전 step-0453 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-barrier.js` — cfg `{loss, resync, resyncDelay, seed, ticks}`: 배리어가 *move* 만 확률 손실(enter/leave 는 rng/존재 민감이라 무손실)·`resync` 로 resyncDelay tick 뒤 net.queue 재enqueue → 이후 배달·적용. move 는 위치 가산(가환)이라 늦게 적용돼도 최종 월드 동일 → 복원만 하면 world==lockstep. 재전송분은 재드롭 안 함(체인 길이 1·유계·확실 배달)·마지막 여유 tick 무손실(past-end 방지).
- 계층: 코디네이션(#4). net.step 중앙 배리어 없이 손실을 배리어가 복원.
- OFF 플래그: `asyncBarrier`(미설정→net.step 그대로 = **reg 구조적 0**). verify.js 새 모드 `barlossy`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK(asyncBarrier 미설정→net.step 경로 비트 동일).
- `barlossy` 5/5 OK — 4 client·30 move·48 tick·단일 존·20% move 손실: `run({asyncBarrier:{loss,resync}})` worldDigest == lockstep Y·resyncs 18~26(손실 실발생·복원)·lost 0.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(시드 PRNG 손실·복원 후 world==lockstep) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅. 한계(정직): 단일 존(핸드오프/이주 없음) 시나리오 — 다중 존은 지연 move 가 *이주 경계*를 못 넘으면 손실(move 는 이주 전 적용돼야). 이주 하 resync 지연 유계화는 후속(0455 는 무-resync 대조·0457 교차-tick 지연). asyncBarrier OFF→reg 구조적 0.
