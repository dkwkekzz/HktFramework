# step-0215 — 인스턴스 수요 spawn(instanceDemand·탄력 확장)

> 직전 step-0214 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/instance.js` — `instanceDemand{kind,target}` → active(kind) < target 이면 부족분만큼 자동 spawn(결정론 auto-id `kind-auto-N`). 이미 충족이면 멱등 0개. 수요 따라 던전 인스턴스를 탄력적으로 채운다(0201 수동 spawn 위 수요 기반 스케일링).
- 계층: 월드(계층2·인스턴스). OFF 플래그: `instanceDemand` 미주입(미발화 → 0214 비트 동일·reg 0). verify.js 새 모드 `instancedemand`.
- 2차 고도화 인스턴스 #1 — spawn/despawn 수명주기(0201~0202) 위에 수요 기반 자동 확장.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (instanceDemand 미주입 → 0214 비트 동일).
- instancedemand: 5/5 — d1(1개)→demand(target3)=+2→재요청 멱등 0→despawn→demand=+1 → active 3·demandSpawns 3·demands 3·retired 1.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 인스턴스는 존 tick 밖·onTick 0(일회성 수명). ②결정론: auto-id 단조 시퀀스·시드 의사난수만. ③권위: active 맵이 "지금 무엇이 도나" 단일 SSOT(수요 spawn 도 멱등). ④은닉: 클라 접점 무변(오케/게이트웨이 경유). ⑤headless: Node·원격 재현. — 이상 없음.
