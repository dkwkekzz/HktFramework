# step-0517 — guildbankresend 금고 saga 재전송+멱등 dedup (#46 금고↔가방 escrow 7)

> 직전 step-0516 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild.js`+`svc-guild-txn.js` — 회신 손실 회복(거래소 0126·우편 0168 의 금고 판). `_resendPending()` + `guildBankRetry` op: pendingGive 에 남은(회신 손실) give 를 *같은 gid* 로 재발신 → 가방 `sagaDedup`(replyTo,gid)이 재실행 없이 저장된 결과 재회신 → pending drain·escrowXfers 무증가(이중적용 0·아이템 안전). retries 계측 추가. 계층: 3 게임 서비스. verify 새 모드 `guildbankresend`. `guildBankRetry` op 미주입→_resendPending 무호출→0516 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — op 미주입→no-op·박스 비트 동일.
- 가설 `guildbankresend`: 5/5 OK — 일시 손실(ackDrop) 후 retries 1·pending 0 drain·acked 1==gives·escrowXfers 1(재실행 0·이중적용 없음)·ownerOf escrow.
- spine ALL OK — #46 arc 7모드(재전송+dedup).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 재전송+dedup 5시드 동일. ③권위: 재전송은 *재회신* 유도(재실행 아님)·가방 dedup 이 이미 이동한 아이템의 재-give 오판 실패를 막음(escrowXfers 무증가·이중적용 0). ④은닉: gid 매칭·give 경유. ⑤headless: node in-proc. — 이상 없음(재시도 상한·포기/영구실패 등 완전 liveness 는 2차 arc·우편 0172~0180 판).
