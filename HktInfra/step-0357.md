# step-0357 — #57 실 host.js OS 프로세스 spawn 7: ClusterHostDriver 명령 번역 + flush

> 직전 step-0356 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js`(신규) — `makeClusterHostDriver()`: orch 드라이버 계약(0353~0356) 이벤트(on*)를 *실 cluster 명령*으로 번역(동기 큐 `commands`): onSpawn→spawnOne·onDespawn→killHost·onAssign→init·onUnassign→stop·onFrame→deliver·onEgress→egress. `flush(cluster)`(async)가 큐를 실 Cluster(spawnOne/killHost=child_process·rpc=소켓)에 집행. 번역(동기)과 집행(async)을 분리 = #4 비동기 경계 격리.
- `src/topo-run.js` — `clusterDriverReal` ON 이면 orch 구성 직후 `orch.clusterDriver = makeClusterHostDriver()` 주입(recorder 와 배타).
- `src/topo-build.js`·`src/net-core.js` — `clusterDriverReal` opt 배선 + 진입점 part 등록.
- 계층: 코디네이션(⑤·SPINE §2 오케스트레이터→§5 ⑤ headless·원격). OFF 플래그 `clusterDriverReal`(미설정→null·호출 0 = baseline 비트 동일). verify.js 새 모드 `hostcmd`(async).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0356) 비트 동일.
- `hostcmd` 5/5 OK — z1@hostA·a1 enter+6 이동 뒤 commands: spawnOne(hostA)×1·init(z1)×1==driverAssigns·deliver×7==driverFrames·egress×7==driverEgress. `flush`→mock cluster: spawnOne:hostA×1·init:hostA#z1×1·deliver:hostA#z1×7·executed==전체·큐 비움.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 번역은 on* 훅(배치/전송 seam)·집행 flush 는 tick 밖 제어 평면. ② 결정론: 5종 동일·번역 큐 순서=이벤트 순서. ③ 권위: spawnOne/init 이 zoneHosts 단일 소유와 1:1. ④ 은닉: 명령은 host/zone id·cmd 만(직렬화 가능). ⑤ headless: Node 순수·mock cluster 동기·reg 0.
- 한계: flush 의 실 Cluster(child_process) 집행은 multiproc-proof 경로(0358~)·stop/egress 실 소켓 집행은 후속. 이상 없음.
