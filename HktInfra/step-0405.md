# step-0405 — #62 runMulti 합류 4·복원력: reprovisionStandby 따뜻한 대기

> 직전 step-0404 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `reprovisionStandby(zone, standbyHost)` + 필드 `mirrors`·`reprovisions`: zone 의 현 상태를 `snapshot`→standbyHost 에 `spawnOne`+`zoneadd`+`loadstate`(따뜻한 사본·primary 와 같은 상태) + `mirrors` 에 `{zone,dstHost}` 등록. runMulti(`cluster-run.js:202` rep + `:219` mirrors)가 kill→승격 후 새 standby 를 띄우고 권위 입력을 미러해 N=1 복제를 유지하던 것의 zone cluster 판. 입력 복제(미러 replay)는 0406. 계층: 코디네이션(#62).
- OFF 동치: reprovisionStandby 미호출이면 mirrors 비고 0404 동치. verify.js 새 모드 `coordreprov`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 20.6KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordreprov 5/5**: run(5)→reprovisionStandby(z1, hostA_s) → standby z1 의 a1 {8,8} == primary a1 {8,8}(따뜻한 사본)·mirrors=[{z1,hostA_s}]·reprovisions 1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: reprovision 은 제어 평면 ②결정론: 시드 5종 동일 a1 {8,8} ③권위: standby 는 shadow(발신 0·이중 쓰기 0)·primary 가 권위 유지 ④은닉: cluster API 만 ⑤headless: 실 host.js child_process standby spawn E2E. — **한계(후속)**: 0405 는 standby 를 *띄우고 사본*까지 — 입력 미러 replay(standby 가 primary 와 계속 동기)는 0406·미러 승격(failover→standby promote)은 후속. clusterInfo(0407)·runScenario(0408)·capstone(0410).
