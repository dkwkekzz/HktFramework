# step-0299 — #9 멀티프로세스 배선 9: 디렉토리 bijection (다중존 rebalance/drain churn)

> 직전 step-0298 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/gateway.js` — `zoneDirSnapshot()` 읽기 질의 추가: 게이트웨이가 보유한 전 라우팅 테이블 `{zoneId:host}`(운영 대시보드·orch.running 과 bijection 대조). 계층: 엣지(#9). 읽기 전용·동작 무변경.
- OFF 경로 무관(질의만 추가·기존 플래그 OFF→이전 비트 동일 = 회귀 0). verify.js 새 모드 `gwdirbij`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdirbij` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdirbij`: 5/5 4존 A 몰림→rebalance([A,B,C])→drain B 의 다중 동시 이주 후 게이트웨이 dir == running 정확한 bijection(dirN 4==runN 4)·직접 enter a1·a2 graceful 보존(total 2)·entitiesSingleOwner·entityDirectCoherent·entityConserved·zoneDirStale 0·gatewayZoneMisses 0.
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 라우팅 테이블·이주는 코디네이션·엣지(존 tick 밖). ②결정론: rebalance/drain·디렉토리 갱신 전부 op 함수·순수. ③권위 단일 소유: 다중 graceful 이주(같은 핸들)에도 entitiesSingleOwner·zone 당 단일 host·dir 단일 엔트리. ④은닉: 게이트웨이는 zoneLoc push 로만 라우팅 테이블 학습·orch 내부 무지·dir==running bijection. ⑤headless·원격: 대량 churn 의 라우팅 테이블 추적은 실 서비스 디스커버리 동형.
- 한계: bijection 은 모든 이주가 _bridgeMigrate→_pubZoneLoc 으로 push 되기 때문(by-construction)·push 손실/순서 역전은 net 신뢰 전제(전송 신뢰성은 별 트랙). destructive(stop/hostdown)+entity 보존 회계 종합은 capstone(0300). zone-host 는 여전히 orch 보유. 이상 없음.
