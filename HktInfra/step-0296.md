# step-0296 — #9 멀티프로세스 배선 6: 이주 중 직접 라우팅 정합 + stale 거부

> 직전 step-0295 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/topo-inject.js` — `zoneStaleProbe` 테스트 seam: at tick 에 *낡은 host* 를 단 zoneDeliver 를 게이트웨이 발신으로 orch 에 직접 보냄(이주 직후 게이트웨이 디렉토리가 뒤처졌을 때의 frame 모델·straggler 류). orch 가 host!=running 으로 거부(0294 stale-reject 경로 검증). 계층: 코디네이션(#9).
- 새 박스 로직 없음 — 0293 디렉토리 갱신(_pubZoneLoc on migrate)·0294 stale-reject 가 이미 한 일을, 이주 시나리오에서 *정합 불변*으로 증명. OFF 플래그 `gatewayDirectZone`/`zoneStaleProbe` 미제공(OFF→probe 0·라우팅 0 = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `gwdircoh`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdircoh` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdircoh`: 5/5 migrate z1 A→B(같은 핸들·a1 보존) 후 직접 enter a2 가 갱신 dir 로 z1@hostB 도달 — z1 a1+a2·total 2·runtimeHost hostB·dir==running·낡은 host probe(ax) 거부(zoneDirStale 1·ax 부재)·entityConserved.
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 라우팅/디렉토리 갱신은 코디네이션·엣지(존 tick 밖). ②결정론: 이주·디렉토리·거부 전부 op 함수·순수. ③권위 단일 소유: 이주는 같은 핸들(중복 0)·stale frame 거부로 옛 host 이중 적용 0. ④은닉: 게이트웨이는 zoneDir 로만 해소. ⑤headless·원격: stale 거부는 분산 디렉토리 지연의 현실적 안전망(원격 환경 동형).
- 한계: zoneStaleProbe 는 합성 frame(실 게이트웨이는 항상 현재 dir 사용 — 실제 stale 창은 1-tick push 지연뿐). 게이트웨이가 거부 통보를 받아 *재해소·재전송* 하는 폐루프는 미구현(거부=드롭·상위 재시도 후속). zone-host 는 여전히 orch 보유. 이상 없음.
