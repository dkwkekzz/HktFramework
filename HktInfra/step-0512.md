# step-0512 — guildbankwithdraw 금고 인출 escrow 복귀 (#46 금고↔가방 escrow 2)

> 직전 step-0511 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild-txn.js` — guildWithdraw 이 `_custody('escrow'→멤버)` give 를 가방에 보내 아이템이 escrow 서 멤버 가방으로 *실제* 복귀(거래소 buy leg 0118·우편 fetch 0158 의 금고 판·0511 예치 leg 의 짝). vault·escrowIds 에서 제거. 계층: 3 게임 서비스. verify 새 모드 `guildbankwithdraw`. `guildBankInv` OFF→_custody no-op→give 0→0511 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — guildBankInv OFF→give 0·박스 비트 동일.
- 가설 `guildbankwithdraw`: 5/5 OK — 예치+인출 gives 2·inv.ownerOf(item0)==alice(복귀)·vault []·escrowIds 0·escrowXfers 2.
- spine ALL OK — #46 arc 2모드(예치+인출 왕복).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 왕복 give 5시드 동일. ③권위: 아이템이 escrow→멤버 단일 이동(공백·이중 0)·vault 원장에서 이탈. ④은닉: `item_req` give 요청만. ⑤headless: node in-proc. — 이상 없음(예치·인출 왕복 완결·2-서비스 보존 명시 단언은 0513).
