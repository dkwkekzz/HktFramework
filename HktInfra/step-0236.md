# step-0236 — cachetouch 모드 spine 승급 (#16 — 3차 라운드 누적 회귀화 6/10)

> 직전 step-0235 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0226 의 bespoke `cachetouch`(캐시 recency touch·진짜 LRU) 검증 모드를 누적 키트로 승급(OPS/BASE 함수 내 지역화). 계층: 검증 하니스(박스 코드 무수정). OFF 플래그 없음 — 박스 `.js` 0줄 → reg 0 자명. #16 정리·도구 step 6/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `cachetouch`: 5/5 OK — get k1 후 k3 진입 시 k2 회수·k1 생존·touches 1.
- spine ALL OK — cachetouch 누적 회귀 편입(0226 진짜 LRU 를 현재 src 에 단언).

## 척추 5항 + 한계

①tick: 검증 모드 추가(시뮬 외). ②결정론: recency 갱신 결정론·Math.random 0. ③권위: 캐시는 읽기 모델. ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음.
