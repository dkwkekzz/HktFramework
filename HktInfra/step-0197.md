# step-0197 — 길드 금고 배지 영속·late-join (guildFeedPersist 의 금고 배지 확장)

> 직전 step-0196 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guildfeed.js` — 0187 의 배지 영속을 금고 배지로 확장: 소비한 svc.guild.bank.changed 도 durable 저널에 기록. `crash()` 가 bankCounts 도 비우고, `reconstruct()` 가 저널을 kind 분기 replay(deposit/withdraw→_applyBank·그 외→_apply) 해 금고 배지 재구성 → late-join 동일.
- OFF: `guildFeedPersist` OFF 면 저널 0·crash 후 빈 배지(소실) = 0196 비트 동일(reg). verify.js 새 모드 `guildbankfeedpersist`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — persist OFF·금고 미사용 시 0196 비트 동일.
- **가설 `guildbankfeedpersist`**: 5/5 시드 OK — crash→reconstruct 배지 g1 2·g2 1==pre·OFF post 빈 배지(소실).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 영속/복원 존 tick 밖. ②결정론: 5/5 동일 배지. ③권위 단일 소유: 배지는 vault 권위와 독립한 사본. ④은닉: 자기 저널만으로 복원. ⑤headless·원격: run.js 증명. — 이상 없음. 후속: bank 배지 정합 capstone(0198·배지==vault 크기).
