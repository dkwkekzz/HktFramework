# step-0495 — svcguildcap 길드 로스터 single-master 정합 편입 (#16 라운드 3차 5)

> 직전 step-0494 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 길드 로스터 single-master 정합 capstone(옛 0190 `rosterConsistent` 판)을 재작성 편입: `run()` 을 guildService opts 로 구동(guildCreate→guildJoin→guildTransfer 쌍 거래→guildLeave master 보호)하고 `rosterConsistent`(정확히 한 master·master∈members·멤버 중복 0)를 단언하는 새 mode `svcguildcap`. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 5/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcguildcap`: 5/5 OK — 이양 성사(master alice→bob·쌍 거래)·from(alice) 멤버 잔류(members 2)·master(bob) 탈퇴 no-op(single-master 보존)·`rosterConsistent` Y.
- spine ALL OK — svcguildcap 포함(거래소 2·우편 2·길드 로스터·서비스 계층 누적 회귀).

## 척추 5항 + 한계

①tick: 길드 존 tick 밖 반응형·박스 무변. ②결정론: 로스터 술어 대조·Math.random 0. ③권위: master 이양 release+acquire 쌍 거래·정확히 한 master(공백·이중 0)·master 탈퇴 보호로 권위 단일 소유 재단언. ④은닉: 클라→길드 버스 경유. ⑤headless: node in-proc. — 이상 없음.
