# step-0424 — #61 업스트림 intent 실 클라 4: UpClient 수렴 desync 0

> 직전 step-0423 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 검증 전용(코드 박스 무변경·reg 구조적 0) — uc0 가 *발신한 intent 가 권위에 반영*되고 *그 권위 AOI 뷰로 수렴*함을 단언: `uc0.seenSig() == orch.zoneAuthSig('z1','a1')`·`convergedTo`(desync 0). 다운스트림 DownClient 수렴(0342~0350)의 업스트림 실 클라 판.
- 계층: 엣지/월드(#61).
- OFF 플래그: 검증 전용·upClients=null→회귀 0.
- verify.js 새 모드: `upconverge`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK.
- 가설 `upconverge`: 5/5 — uc0 seenSig a1@9,8 == 권위 AOI sig a1@9,8·convergedTo Y·desync 0.
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉 ⑤headless — 이상 없음. 한계: 단일 클라·단일 entity 수렴(다중 클라·entityOps 동치는 0425~0426).
