# step-0448 — #4 실 net.step 배리어 치환 8: exactly-once 회계

> 직전 step-0447 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `accountReplicas(events, M, seed, avatars, nsites)`: M 복제가 순열+손실 도착을 makeZoneResync 재구성 후 async-core.accountDelivered(배달열 vs 전체 집합)로 {complete, dups, missing} 대조 + 실 월드 digest. 수렴·배리어-free·손실 복원이 *맞게* 동작하려면 회계가 닫혀야 한다: 발신한 모든 실 intent 가 결국 *정확히 한 번* 적용(손실 0·중복 0)되고 최종 digest 불변. 닫힌 장부의 논리 클럭 판.
- 계층: 코디네이션(#4). 배리어 없이도 발신==적용(exactly-once)을 회계로 단언.
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netaccount`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netaccount` 5/5 OK — 4 client·40 intent·M=4 복제·~20% 손실: 전 복제 complete Y·**dups 0**·**missing 0**·digest == canonical Y·손실 실발생(resyncs>0).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅ ③권위 단일 ✅(exactly-once) ④은닉 ✅ ⑤headless ✅(검증 전용). 이상 없음 — 순열+손실 교란에도 전 복제 회계 닫힘(발신==적용). async-net 은 run() 밖 substrate.
