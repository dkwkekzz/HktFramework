# step-0310 — #9 잔여(실 host.js 물리 분리) capstone: host 프로세스 컨테이너 전 정합

> 직전 step-0309 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 capstone 술어 `hostProcCoherent()` = `directFlowCoherent()`(배치 3층 정합 + entity 단일 소유/orphan0 + 게이트웨이 직접 라우팅 stale0·0300) && `hostContainerCoherent()`(존이 정확히 한 host 프로세스에 + 컨테이너==집행 + roster 회계 닫힘·0308). 참이면 "게이트웨이 직접 라우팅 데이터 평면 전부 정합 + host 프로세스 컨테이너(자기 inbox 수신·자기 루프 tick·spawn/despawn roster·stale 거부)가 집행 SSOT 와 완전 정합".
- 계층: 코디네이션(⑤). 읽기 전용 → 0309 비트 동일. verify.js 새 모드 `hostproccap`. **실 host.js 물리 분리 arc 0301~0310 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneHostProc OFF → baseline(0309) 비트 동일.
- `hostproccap` 5/5 OK — 0300 혼합 lifecycle(hostdown C·stop z2·migrate z1·rebalance·drain)을 host 프로세스 컨테이너 라우팅으로: hostProcCoherent Y·entityConserved Y·total 1·recv==drained+stale(정상 흐름 stale 0)·census.total 1·ledger 5/1/2/1.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: host 자기 루프가 자기 inbox drain 후 자기 존만 onTick — 시뮬 외 0. ② 결정론: 시드 5종 동일·직렬화 round-trip. ③ 권위: entity 단일 소유 + 존이 정확히 한 host 프로세스에 + stale 거부로 이중 쓰기 차단. ④ 은닉: 게이트웨이 직접 라우팅·orch 내부 컨테이너(클라 무관). ⑤ headless: Node 순수·reg 0.
- 한계: zone-host 핸들은 여전히 orch 인프로세스 `zoneRuntimes`/`zoneHosts`(논리 컨테이너) — 실 host.js *OS 프로세스/소켓* spawn(cluster-run.js 통합)은 후속. 정합 술어는 by-construction(같은 인프로세스). 이상 없음.
