# step-0182 — 길드 증분 가입/탈퇴(guildJoin/guildLeave·멱등·master 보호)

> 직전 step-0181 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `guildJoin{guildId,member}`(증분 추가·이미 멤버면 no-op·멱등)·`guildLeave{guildId,member}`(증분 제거·없으면 no-op·멱등). **master 보호**: master 의 guildLeave 는 no-op(마스터 탈퇴 거부·이양 0189 선결) → single-master 불변 보존. 미존재 길드 join 은 graceful 무시. joins/leaves 계측(no-op 포함). 계층: **3 게임 서비스**. 파티 0084 의 길드 판.
- OFF 플래그: 증분 명령(guildOps join/leave) 미주입 → 휴면 → 0181 비트 동일(회귀 0). 핸들러는 guildService 박스 존재 시에만 도달(reg 시나리오는 guild 박스 0).
- verify.js 새 모드 `guildjoin`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guild 미주입 → 0181 비트 동일.
- **가설 `guildjoin`**: 5/5 시드 OK — join/leave 호출 2/3(no-op 포함)·최종 로스터 `["c2","x"]`(c1 가입후탈퇴·중복가입 no-op·없는탈퇴 no-op·master x 탈퇴 거부)·master 유지 예·single-master 보존.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 존 tick 밖 비동기 멤버십 변경. ②결정론: 전 시드 동일 로스터. ③권위 단일 소유: master 보호로 single-master 불변 보존(마스터 공백 0). ④은닉: 증분 델타 명령만·내부 무지. ⑤headless·원격: Node·run.js 증명. — 이상 없음.
