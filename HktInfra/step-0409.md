# step-0409 — #62 runMulti 합류 8·복원력 payoff: promoteStandby 상태 보존 failover

> 직전 step-0408 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `promoteStandby(zone)` + `promotions` 계측: primary host 사망 시 그 zone 의 따뜻한 standby(mirror·0405/0406 으로 lockstep 동기)를 primary 로 승격 — placement[zone]=standbyHost·orch where-view 갱신·미러 해제. standby 가 이미 동기됐으므로 *상태 손실 0*. 0376 failover(죽은 host 의 존을 *빈* 인스턴스로 재가동·entity 소실·#63)와 대조 — 따뜻한 standby 로 #63 완화. runMulti 의 kill→추종자 승격(mirrors→primary)의 zone cluster 판. 계층: 코디네이션(#62·복원력 payoff).
- OFF 동치: promoteStandby 미호출이면 0408 동치. verify.js 새 모드 `coordpromote`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 24.0KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordpromote 5/5**: run(5)+migrate(z3 A→B·hostA 비움)+reprovisionStandby(z1,hostA_s)+killHost(hostA)+promoteStandby(z1) → a1 보존(pre {8,8} == post {8,8}·상태 손실 0)·placement[z1]=hostA_s·unifiedCoherent Y·promotions 1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 승격은 제어 평면 ②결정론: 시드 5종 동일 a1 {8,8} ③권위: 승격으로 단일 where 권위 유지(release dead+acquire standby)·standby 가 권위 인수 ④은닉: cluster API 만 ⑤headless: 실 host.js child_process kill+promote E2E. — **한계**: standby 가 없는 존의 failover 는 여전히 상태 소실(0376·#63)·따뜻한 standby 를 *사전 reprovision* 한 존만 보존. capstone runMultiCoherent(0410)·#62 sub-arc 닫기.
