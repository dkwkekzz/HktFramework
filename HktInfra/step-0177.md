# step-0177 — 아이템 우편 saga 재admission 발행(mailReadmitPublish)

> 직전 step-0176 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`(`_readmit`): 포기 give 재개마다 `svc.mail.saga_readmitted{gid,itemId,cause}` 1회 발행(readmitPublished++·0174 포기 발행의 짝·거래소 0135 의 우편 판). 계층: 게임 서비스(우편)·버스.
- 필드: `readmitPublish`/`readmitPublished`. `svc-mail-persist.js` crash(): readmitPublished 소실. `src/topo-build.js`: `mailReadmitPublish` 기본 + 박스 opts + buildSubs 설정. `src/topo-subs.js`: audit 가 `svc.mail.saga_readmitted` 구독(ON 일 때만).
- OFF 플래그 `mailReadmitPublish`(OFF→발행 0·구독 미추가 = 0176 비트 동일). verify.js 새 모드 `mailreadmitpub`. 포기(0174)↔재개(0177) 수명주기 발행 한 쌍 완비.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: readmitPublish OFF = 0176 비트 동일(전 시스템 5시드) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailreadmitpub`**: 5시드 전부 — ON readmitted 1·published/audit 1/1(발행==재admission==관측 1:1)·OFF published 0.
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖·발행 파생 스트림 — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경. ③권위 단일 소유: 우편함 권위 불변(발행 비-침습). ④은닉·단일 연결: audit 가 발행자 무수정으로 구독(버스 경유). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (다음: 재admission 횟수 상한 mailReadmitMax·거래소 0137 의 우편 판 — 무한 abandon↔readmit 루프 차단.)
