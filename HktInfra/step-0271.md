# step-0271 — 도구 갭 #43: close-step src/ 박스 >30KB 가드(#49 재발 방비)

> 직전 step-0270 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `engine/close-step.js` — 닫기 게이트에 **src/*.js 박스 크기 점검**(#43)을 더했다. src 디렉토리를 스캔해 >30KB 박스를 *비실패 경고*로 나열하고, 없으면 최대 박스명·크기를 한 줄로 보고한다. 계층: 도구(engine·tooling). **비실패** — 박스를 키운 기능 step 의 닫기를 막지 않되(다음 *기능* step 전 정리가 CLAUDE.md 규칙) #49(>30KB 방치)가 조용히 재발하지 않게 매 close 마다 가시화.
- net-core/src 박스는 **무변경** → OFF 플래그 불요(tooling-only). verify.js 새 모드 없음(이전 가설 모드 gwsplit 유지).

## 검증 (수치 = `node run.js` / `node engine/close-step.js` 출력)

- **reg 0(자명)**: src 박스 무변경 → baseline 과 비트 동일. `node run.js` = **ALL OK**(전 누적 모드 통과·gwsplit 포함).
- **가드 동작**: `node engine/close-step.js` → `src/ 박스 크기 — 최대 svc-exchange-core.js(26.0KB) ≤ 30KB (>30KB 0개)`. 현재 >30KB 박스 0개(#49 arc 0261~0270 해소 상태 확인).
- spine ALL OK(verify all = 전 역사 불변 현재 코드 단언).

## 척추 5항 + 한계

①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 모두 무영향(도구 변경·런타임 박스 무변경). 한계: 비실패 경고이므로 강제력은 없다(설계 의도 — 닫기 차단 대신 가시화). 이상 없음.
