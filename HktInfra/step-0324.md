# step-0324 — #9 후속: host 산출 뷰의 AOI exit 델타 (동적 가시 상실)

> 직전 step-0323 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 질의 `zoneViewExited(zoneId, sessionId)` = 그 세션 뷰들의 exit 를 누적한 id 집합(언젠가 AOI 에서 빠져나간 entity). 두 avatar 가 멀어지면 서로의 exit 집합에 들어온다(동적 가시 상실·`zoneViewEntered`·0322 의 짝).
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0323 비트 동일. verify.js 새 모드 `hostzoneexit`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0323) 비트 동일.
- `hostzoneexit` 5/5 OK — a1 을 a2 쪽으로 6회 접근(반경 안·enter) 후 6회 멀어짐(원위치 (5,5)·반경 밖): `zoneViewExited(s:a1)` ⊇ a2·`zoneViewExited(s:a2)` ⊇ a1(상호 exit)·최종 `zoneVisibleIds` 둘 다 자기만([a1]·[a2]).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 존 시드 고정 → 5종 동일. ③ 권위: 권위 존이 동적 시야 이탈 산출. ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·reg 0.
- 한계: 단일 존 내 AOI exit 만(존 경계 핸드오프 후 뷰 전환은 별개)·아직 클라 전파 미연결. 이상 없음.
