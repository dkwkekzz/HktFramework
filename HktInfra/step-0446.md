# step-0446 — #4 실 net.step 배리어 치환 6: M 복제 존 수렴 desync 0

> 직전 step-0445 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `convergeReplicas(events, M, seed, avatars, nsites)`: M 복제 존이 각자 *다른 순열+손실* 도착을 makeZoneResync 로 재구성·동결 sim 에 fold → 각 복제의 실 월드 digest. net.step lockstep 은 중앙 큐 하나라 자명히 같은 배달을 보지만, 배리어를 없애면 복제마다 물리적으로 다르게 받는다 — 이 조각은 그래도 전 복제 digest 가 서로 같고 == canonical(totalOrder) 임을 보인다.
- 계층: 코디네이션(#4). 배리어 없는 다중 복제 존의 결정론 수렴(desync 0).
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netconverge`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netconverge` 5/5 OK — 4 client·40 intent·M=4 복제·~20% 손실: 전 복제 digest 일치 Y·== canonical Y·**desync 0**(배리어 없이 수렴).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(전 복제 정전 수렴) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅(검증 전용). 이상 없음. 한계: 복제 진행(tick)은 아직 동기 페이스 — 독립 페이스(배리어-free 진행)는 0447. async-net 은 run() 밖 substrate.
