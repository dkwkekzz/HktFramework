# step-0188 — GuildFeed 회계 정합 capstone(feedConsistent·배지==로스터)

> 직전 step-0187 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guildfeed.js` — `feedConsistent(guildSvc)`: 배지 정합 불변(전 길드 countOf==로스터 members.length AND 고아 배지 0). 순수 읽기(권위 0·실행 경로 무변경). 우편 MailFeed 0155 의 길드 판. GuildFeed arc(0186~0188) 닫기. 계층: **3 게임 서비스**(읽기 모델 정합).
- OFF: 순수 읽기 메서드(미호출 시 baseline 비트 동일=회귀 0) · verify.js 새 모드 `guildconsistent`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — 메서드 추가만(실행 경로 불변) → 0187 비트 동일.
- **가설 `guildconsistent`**: 5/5 시드 OK — A 정상·B feed crash→reconstruct·C guild crash→reconstruct·D 영속 네 체제 모두 feedConsistent true·배지 총합 5==로스터 총합 5.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 정합 검사 존 tick 밖. ②결정론: 전 시드 4/4 true·총합 5==5. ③권위 단일 소유: 읽기 모델·권위 0(로스터 SSOT 만 권위). ④은닉: 배지는 발행 스트림 파생·SSOT 직접 접근 0. ⑤headless·원격: Node·run.js 증명. — 이상 없음.
