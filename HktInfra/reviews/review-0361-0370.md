# review 0361–0370 — #57 실 host.js OS 프로세스 데이터 평면 (코디네이션·월드·SPINE §5 ⑤)

> 범위: step-0361~0370. 인과 서사·서버 기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md) 기능 16(+ [progress/README.md](progress/README.md) 다음 큰 걸음). 이 문서는 *정당성 감사*만 — narrative 요약표 없음.

## §1 척추 5불변 정합성

- **① 신성한 tick ✅** — 데이터 평면 집행은 전송/제어 평면: deliver(`flush`·`src/cluster-hostdriver.js:32`)·tick(`tickZone`→실 `zone.onTick`·`:50`)·migrate/kill/reconcile 전부 verify 하니스 레벨(존 tick 무관). 실 `zone.onTick`(`src/zone.js:112`)은 pending move + view 산출만(동기 I/O·await 0).
- **② 결정론 코어 ✅** — `Math.random` src 0 hit(grep). 실 host.js 존 시드 = `fnv1a(zoneId)`(`src/verify.js` zoneSpecOf) → enter 위치·move 적용이 in-proc 과 동일(5시드 동일). 배치 move 적용=순차와 동일(mod 덧셈·`src/zone.js:120`).
- **③ 권위 단일 소유 ✅강화** — `clusterDesync`(`src/cluster-hostdriver.js:99`) 양방향 비교가 실 host entity ↔ in-proc 권위 1:1 을 못박음(desync 0). migrate=release(zonedel from)+acquire(zoneadd+loadstate to·`:55`)·실 두 프로세스 동시 존재 0. failover=죽은 host 의 존을 생존 host 단일 재가동(`:65`).
- **④ 은닉·단일 연결 ✅** — 데이터 평면 메시지 직렬화(`{cmd:'deliver',items:[{m:{to,from,payload}}]}`·snapshot/loadstate). 다중 host 격리(`hostEntities`·`:81`)=한 host 프로세스가 다른 host 의 존/entity 못 봄(실 프로세스 경계가 강제). orch→`zone.js` 직접 import 0 hit.
- **⑤ headless·원격 검증 ✅** — 이번 묶음 핵심: 실 `host.js` 자식 프로세스에서 entity 데이터 평면이 흐르고 snapshot 으로 in-proc 권위와 일치 검증(`clusterdatacap` 실 host.js 2 프로세스·desync 0). `cluster-core.js` `child_process.spawn`·소켓 RPC. UObject/UE src 0 hit. **#57 의 ⑤ 가 spawn(0351~60)에 이어 데이터 평면까지 실 프로세스로 완결**.
- **메타(회귀 0·박스 유계)**: 신규 코드 전부 OFF 플래그 `clusterDriverReal`(미주입→호출 0)·읽기 헬퍼 → reg ALL OK 매 step. `host.js` zonedel 은 기존 cmd 무변경(e2e/multiproc 동일). 박스 최대 orch-zonebridge 29.0KB(<30KB·watch)·`cluster-hostdriver.js` 12.9KB(이 arc 의 성장 박스·여유).

## §2 검증 재현 (직접 실행 인용)

- `node run.js spine` = **ALL OK**(전 시리즈 누적 회귀·5시드·실 child_process 포함·정리 shutdown).
- `node src/verify.js clusterdatacap` 5/5 — coherent Y·실 migrate a1 보존 Y·hostA release Y(2 host·3 zone).
- 묶음 bespoke(각 step verify.js·git per-commit·재현 확인): `hostdeliverreal`(실 a1=={x:5,y:5}==in-proc)·`hosttickreal`(move 후 {x:11,y:11}·egress 1)·`hostmigratereal`(z1 A→B 보존·hostA none)·`hostkillreal`(livePids 2→1·a1 소실)·`hostreconcilereal`(plan→hostA[z1,z3]·hostB[z2])·`hostisoreal`(누수 0)·`hostdrivereal`(2 host desync 0·views 2)·`hostdesyncreal`(정상 0·ghost 주입 1) 전부 5/5.
- grep: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · orch/driver→`./zone.js` import 0.
- 박스 크기: `wc -c src/*.js` 최대 orch-zonebridge 28960·gateway 28217·cluster-hostdriver 12908 — >30KB 0개.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #57 | zoneHosts/데이터평면이 orch 인프로세스(실 host.js 아님) | ⑤ | 🟡 **거의 해소(0351~0370)** — 실 spawn(0351~60) + 실 데이터 평면(0361~70·deliver/tick/migrate 상태보존/kill·failover/reconcile/driveCluster·clusterCoherent desync 0)으로 *논리→실 host.js OS 프로세스/소켓* 물질화 입증. **잔여**: verify 가 직접 실 Cluster 구동(독립 spawn+drive)·기존 runMulti(broker lockstep) 코어와 미통합(#62)·연속 tick 루프 미구성 | **목적지 STATE §2 NEXT**(#62 로 잔여 구체화) → 게이트: runMulti 가 orch.clusterDriver 를 broker 측 제어 평면으로 배선 + 연속 tick 루프(끄면 현 직접 구동=회귀 0) |
| #62 | ClusterHostDriver 가 cluster-run.js runMulti 와 미통합 | ⑤/검증 | 🟡 **진전(0368 driveCluster)·유지** — `driveCluster`(reconcile+deliver+tick 한 호출)가 runMulti analog 으로 실 cluster 전체 데이터 평면을 구동(통합 *형태* 입증). **단 broker lockstep 배리어·14 프로세스 토폴로지와의 *코어 통합*은 미연결** — driveCluster 는 verify 가 부르는 독립 경로 | **목적지 STATE §2 NEXT** → 게이트: runMulti 루프 안에서 orch 가 driver 로 host 구동(broker=제어 평면·끄면 현 분리=회귀 0) |
| #63 | 실 failover 상태 소실(영속 미연동) | ⑤/데이터 | 🟡 **신규(정직한 한계)** — 0365 `failoverZone` 는 죽은 host 의 존을 *새 빈 인스턴스*로 재가동(snapshot 불가)·entity 소실. migrate(0364·graceful)는 보존하나 crash 는 못 함 — 영속 스토어(6-data) 연동 시 replay 복구 가능 | **목적지 STATE §3 OPEN GAPS** → 게이트: failover 가 월드 영속(intent 로그/snapshot)서 존 상태 복구(끄면 현 빈 재가동=회귀 0)·데이터 계층 묶음 |
| #64 | migrate 중 in-flight frame 재라우팅 부재 | ⑤ | 🟡 **신규(경미)** — 0364 `migrateZone` 는 *정적* 상태 이전(snapshot→loadstate)·이주 직후 옛 host 로 향한 frame 의 새 host 재라우팅 미처리(인프로세스 0296 zoneStaleProbe 의 실 프로세스 판) | **목적지 STATE §3 OPEN GAPS** → 게이트: 이주 후 도착 frame 을 새 host 로 forward 또는 게이트웨이 dir 갱신 대기(끄면 현 정적 이전=회귀 0) |
| #16 | bespoke 모드 verify-kit 미승급 | ②/정리 | 🟡 **확장(+10)** — hostdeliverreal~clusterdatacap 10 모드 미승급(현 spine=최신 step `clusterdatacap` 1개·나머지 git per-commit) | **목적지 STATE §3**(정리성) → 게이트: 안정 호 묶음별 스모크 승급(끄면 현 spine=회귀 0) |
| #58' | orch-zonebridge.js 29.0KB(30KB 근접) | 정리 | 🟡 **watch 유지** — 0355 이후 28.9~29.0KB 유지·기능 step 무변경(이번 arc 는 cluster-hostdriver 만 키움) | **목적지 STATE §3**(정리성) → 게이트: 30KB 초과 시 정리 분할(기능 0·#43 가드가 잡음) |

기존 이월 유지(다음 묶음 재점검): #53(by-construction — 단 0369 ghost 주입·실 spawn 이 진짜 분기 도입·완화)·#4(진짜 비동기·load-bearing)·#61(업스트림 intent 실 클라)·#46(길드 금고↔가방·2차 대기)·#50·#48·#44·#41·#39·#13·#17a·#25·#27·#28·#29·#30b·#55·#3·#6·#7·#8·#1(⛔범위).

**🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 2차. 0361~0370 은 0351~0360 verdict 가 지목한 *#57 실 데이터 평면 집행*을 그대로 집행한 **새 능력**(오케스트레이터 기능 16·과심화 아님). orch 의 논리 데이터 평면이 실 host.js 프로세스/소켓 경계를 넘어 entity 단위로 desync 0(clusterCoherent)·0369 ghost 주입으로 술어 민감(by-construction 아님) 입증. **다음 권고(불변)**: cluster-run.js runMulti 코어에 orch 상주(broker 측 제어 평면·#62) + 연속 tick 루프, 그 다음 진짜 비동기(#4)·업스트림 intent 실 클라(#61). step-loop 이 STATE §2 로 승급.
