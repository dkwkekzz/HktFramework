# review 0421–0430 — #61 업스트림 intent 실 클라 (UpClient)

> 범위: step-0421~0430(엣지·월드). 인과 서사·서버 기능 리스트는 [progress/2-world.md](progress/2-world.md) 존 서버 기능 9. 이 문서는 *정당성 감사*만(narrative 금지).

## §1 척추 정합성 (SPINE §5 5불변)

다운스트림 실 `DownClient`(0342~0350)에 이어 *업스트림*도 실 클라: `UpClient`(`client.js:264`·kind 'upclient') 액터가 자기 plan 으로 intent(zoneEnter/zoneMove/zoneLeave)를 게이트웨이로 *발신*(`onTick`·`:292`)하고 자기 AOI 뷰를 *수신*(`onMsg`·`:277`)해 권위로 수렴. 합성 entityOps 주입(topo-inject)을 실 클라 액터 발신으로 대체.

- **① 신성한 tick** ✅ — UpClient 는 net 액터(net.register 가 net/addr 주입·net.step() 이 onTick 구동·`engine/index.js:121`)일 뿐 존 tick 침습 0. 발신/수신은 게이트웨이 경유.
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). UpClient 는 자기 plan(고정 배열)으로 결정론 발신·intent 순서는 등록 순서(this.order)로 결정론.
- **③ 권위 단일 소유** ✅ — leave→권위 존서 entity 제거(`upleave` a1 제거)·보존 회계(`upe2ecap` enters−leaves==present 5/5). 발신 회계로 발신 intent 전부 권위 반영(`upaccount` intentLog==sent·{9,9}==enter+Δ).
- **④ 은닉·단일 연결** ✅ — UpClient._emit 은 *오직 게이트웨이로*(`client.js:299` `this.net.send(this.addr,'gateway',op)`)·존/orch 직접 접근 0(클라는 게이트웨이만 안다). enter 가 uc0 발신이라 게이트웨이가 세션→uc0 다운스트림 바인딩.
- **⑤ headless·원격 검증** ✅ — `UObject|CoreUObject|GameplayTags` src 0 hit. UpClient 는 순수 JS 액터(in-proc)·실 OS 프로세스 경계는 #57 짝(후속).

**메타(회귀 규율·박스 유계)**: reg 0 — `upClients`=null(기본)→upclient 액터 미스폰→`this.order` 불변→run() 비트 동일(`reg` ALL OK). 박스 >30KB 0개(최대 cluster-coord 29.3KB·client.js 20.8KB). 코드 델타는 client.js(UpClient)·topo-actors/build/run 배선뿐(0421~0423·0427·0429), 나머지는 검증 전용(0424~0426·0428·0430·코드 박스 무변경).

## §2 검증 재현 (직접 실행·수치 인용)

- `node run.js 0430 reg` → **ALL OK** (upClients 미설정→액터 0→0429 비트 동일).
- `node run.js 0430 upe2ecap` → **5/5 OK**: 양방향 실 클라 E2E — uc0(a1@z1 체류) 수렴 Y·a1 체류·uc1(b1@z2 leave@9) 후 b1 제거·보존 Y(enters2−leaves1==present1)·발신 회계 Y(전 시드).
- `node run.js spine` (백그라운드) → **ALL OK** (전 시리즈 누적 회귀).
- grep 감사: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · UpClient 알리바이(`client.js:264·277·292·299`) 확인.
- 단계별(자기 step 재현): upclient 5/5(enter·a1@z1 {5,5})·upmove 5/5(a1 {9,8}==enter+Σplan)·uprecv 5/5(sent3+deltas3·seenSig a1@9,8)·upconverge 5/5(seenSig==zoneAuthSig·desync 0)·upvsscript 5/5(up a1=={script} {9,8})·upmulti 5/5(uc0·uc1 각자 존 수렴)·upleave 5/5(sent4→a1 제거)·uplossy 5/5(gaps1·resyncs1·수렴)·upaccount 5/5(intentLog4==sent).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #61 | 업스트림 intent 실 클라 경로 미연결(in-proc) | ②/⑤ | 🟡 **대폭 진전(in-proc ✅·잔여=실 OS 프로세스)** | in-proc 브리지 데이터 평면에서 UpClient 발신→게이트웨이→존→egress→수신 수렴(desync 0·`upe2ecap` 5/5). **잔여**: 실 host.js child 경계 업스트림(다운스트림 0361~0370 의 짝·#57 동반)·STATE §3 OPEN GAPS(🟡) → 게이트: UpClient 발신을 실 cluster RPC 로(끄면 in-proc=회귀 0) |
| #70 | UpClient E2E 가 in-proc 액터(실 OS 프로세스/소켓 아님) | ⑤ | 🟡 신규(경미·#57 family) | STATE §3 OPEN GAPS → UpClient/DownClient 는 net 액터·실 child_process spawn 미연결(다운스트림 #57 짝과 같은 묶음) → 게이트: 실 cluster 업스트림 RPC(끄면 in-proc=회귀 0) |
| #69 | 동치가 transitive(직접 비트 대조 아님) | 검증 | 🟡 유지(경미·#53 family) | STATE §3 — upvsscript 도 *최종 권위 위치* 동치(in-tick 발신 시점 차이로 비트 동일 아님·누적 plan 결과 동일) |
| #4 | broker lockstep = 강제 동기(진짜 비동기 미해제) | ① | 🟡 유지(load-bearing) | STATE §2 — 논리/벡터 클럭·인과 순서 |
| #68 | orch zoneHost entity 컨테이너 미동기(where-view 만) | ③/정리 | 🟡 유지(경미) | STATE §3 — 이번 묶음 미접촉 |

> **🎯 단계 평결 (0421-0430·SKILL §3.6)**: **#61 업스트림 intent 실 클라(0421~0430·직전 0411-0420 verdict 가 STATE §2 로 올린 load-bearing #61)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *verdict 가 지목한 #61* → **과심화 아님**(존 서버 기능 9·다중 클라 전파의 업스트림 짝). 다운스트림 실 DownClient(0342~0350)에 이어 업스트림도 실 UpClient — 골격(0421)·move(0422)·양방향 수신(0423)·수렴 desync0(0424)·entityOps 동치(0425)·다중 클라 인터리빙(0426)·leave 생애주기(0427)·손실 하 수렴(0428)·업스트림 회계(0429)·grand capstone 양방향 E2E(0430). **재현 증거**: `upe2ecap` 5/5(uc0 수렴·b1 제거·보존·발신 회계)·`upvsscript` 5/5(실 클라 ≡ 합성 entityOps)·`reg`/`spine` ALL OK·`Math.random`/UObject src 0·>30KB 박스 0(client.js 20.8KB). **#61 🟡대폭 진전(in-proc ✅·잔여=실 OS 프로세스 #57 짝=#70)**. **다음 권고: 진짜 비동기(#4·lockstep 해제) — 이제 양방향 실 클라가 섰으니 다음 큰 load-bearing 은 #4; 또는 실 host.js child 경계 업스트림(#70·#57 짝).** step-loop 이 STATE §2 로 승급.
