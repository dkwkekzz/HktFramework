# review-0091-0100 — 파티 전달 종결/관측 + 멤버별 수신함 + 수신함 메모리 유계화

> 범위 0091~0100(닫힘). *무엇을 했나*의 점적·박스별 성장은 [progress/](progress/)(특히 [3-services.md](progress/3-services.md))·STATE §7. 이 문서는 *정합 감사*만 — 척추 5불변 재현·이슈→후속 연결.
> 알리바이: 현재 누적 코드 `../src/*.js` + `node run.js spine`. 닫힌 step 문서는 불변.

이 묶음은 0081~0090 의 전달 dedup 유계화/관측/파티 집계 위에 ⒜ *파티 전송 세 종결*(done·acked·**incomplete**)과 *발행 양 끝*(complete·incomplete) ⒝ *전달 결말 셋째*(bounced 발행) ⒞ *멤버별 수신함*(0088 §9 해소) ⒟ *수신함 메모리 3차원 유계화*(seq·epoch·**inbox**: cap+drain)를 더하고, 정리 step 2건(0094 svc-whisper·0098 topo-build 분할)으로 30KB 예산을 유계 복귀시켰다.

## §1 척추 정합성 (SPINE §5)

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 새 로직 전부 반응형 박스 — `svc-mailbox.js:41` `drain()`(순수·tick 무관)·`:44` `_pruneEpoch`·`:97` inbox cap; 라우터 포기/발행은 `svc-whisper-handlers.js` `onTick`(제어평면 벽시계 timeout). grep `await|fs.|.connect(` svc 박스 = 0 hit |
| ② | 결정론 코어 | ✅ | 매 step verify `nonInvasive`(minted ON==OFF) 통과·세계 쓰기 무변경. straggler/drain 주입은 `topology.js:179·181`(net.log 밖·digest 불변). `Math.random` 신규 0(잔존 1건은 #8 panel-kit·헤드리스 경로 밖) |
| ③ | 권위 단일 소유 | ✅ | 새 항은 전부 dedup/영수증/관측 *회계* — 원장 권위 불촉(minted 불변). `svc-whisper-core.js:78` `partyIncomplete`·`:70` `_partyFail`(파티 원장 failed 집계, 전달 권위 무관) |
| ④ | 은닉·단일 연결 | ✅(긴장 watch) | audit 는 버스 토픽 구독(발행자 무수정)·`topo-build.js:155~157` `svc.party.incomplete/complete`·`svc.whisper.bounced` 행. 단 멤버→`p.consumer`(=수신함 주소) 직결 가정(0096) — member→session→mailbox 한 겹 부재 → #27(watch) |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | `node run.js`·`spine` 수치 증명·UE 0. 단 mbox/mbox2/wrouter/party 는 인프로세스 전용(host.js 미배선·straggler/drain 훅은 `topology.js` run 루프) → #9 확장 |

**메타(회귀 규율·박스 예산)**: 10/10 step 모두 OFF=baseline 비트 동일(`epochGrace||0`·`partyAckGiveup||false`·`inboxBound||0`·`mailbox2 false`·구독 행 가드 — `svc-*.js` OFF-guard 라인 확인). 정리 step 2건이 30KB 예산을 *능동적으로* 지킴(0094 svc-whisper 33→core 14/handlers 11/entry 1·0098 topo-build 32→28.6/actors 4.9) — #22 해소. 분할은 Object.assign 프로토타입(svc-inventory 패턴)·verbatim 이동으로 reg 0.

## §2 검증 재현

- **spine 누적 회귀** (`node run.js spine`·src 전 가설 모드 + reg): **ALL OK** — 100-step 전 역사 불변이 현재 코드에서 하나도 안 깨짐(월드해시 `0x7a122947`(seed42) 등 보존·E2E 14프로세스 비트동일).
- **현재 step 모드** (`node run.js 0100 pdrain`): **ALL OK** — ON inbox 0/drained 8 vs OFF inbox 8·received 8 보존·minted ON==OFF. (0091~0099 모드는 verify.js 셸이 0100 으로 덮였으나 각 close 시 5시드 OK 확인·spine reg 가 비트 동일 보증.)
- **grep 감사**: `Math.random` → `engine/panel-kit.js:29` 1건(#8·헤드리스 밖)·svc 박스 동기 I/O 0·`UObject|CoreUObject|GameplayTags` 0·`host.js` 새 박스 0(채팅 스냅샷 필드 1건은 무관) — ①②④⑤ 위반 0.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #22 | 박스 30KB 예산 근접 | ⑤/정리 | ✅ **해소** | **0094**(svc-whisper 분할)+**0098**(topo-build 분할) — 박스 전부 ≤30KB·verbatim·reg 0 |
| #23 | 멤버별 Mailbox 부재→파티 ack 반쪽 | 서비스 | ✅ **해소** | **0096** mailbox2(`topo-build.js:68`) — up 멤버 둘 다 수신함→delivered 2/acked true. 잔여(동적 N·게이트웨이 은닉)→#27 |
| #24 | epoch 가지치기 straggler 가정 | ④ | ✅ **해소** | **0091** deliverEpochGrace(`svc-mailbox.js:35`) — 최근 N개 닫힌 epoch 유예→straggler dedup. 잔여(N 초과 straggler)→#26 흡수(watch) |
| #21 | svc-whisper 헤더 주석 stale | ②/정리 | ✅ **해소** | **0094** 분할이 헤더 재작성(`svc-whisper-core.js:5` "onTick 은 제어평면 timeout"·코드 정합) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 **확장**(+10 모드·~30) | STATE §3 OPEN GAPS → 안정 호(전달 신뢰·파티 종결·유계화) 스모크 1~2 모드 verify-kit 승격(끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장**(+0091~0100) | STATE §3 → mbox/wrouter/party host.js 배선 + 라우팅/전달/drain 핸드셰이크(현 FIFO·인프로세스라 미발현=회귀 0) |
| #25 | 파티 종결 이벤트 단일성 미강제 | 서비스 | 🟡 신규(경미·watch) | STATE §3 → complete/incomplete 가 같은 partyId 에 상호 배타 보장(현 토폴로지 동시 참 불가라 무발현·끄면 현 거동=회귀 0) |
| #26 | inbox 드레인 읽음 영수증 부재 | 서비스 | 🟡 신규(경미) | STATE §3 → drain→ack→안전 제거 고리(0076 영수증의 inbox 판)+클라 게이트웨이 경유 read E2E(0099 cap·0100 drain 의 손실 정합·끄면 현 거동=회귀 0) |
| #27 | 멤버별 수신함 고정 2개·consumer=수신함 주소 가정 | ④/서비스 | 🟡 신규(경미·#6 family) | STATE §3 → 파티 크기에 따른 동적 N 수신함 + member→session→mailbox 한 겹(게이트웨이 은닉 정합·동적 토폴로지 #6 무대) |

**이월 유지**(이 묶음 무변): #1 결정론 이원화·#3 cross-zone AOI·#4 broker lockstep·#6 동적 경계/N존·#7 채팅 홉 신뢰·#8 panel-kit Math.random·#10 give×result-ahead·#11 per-producer ack/다중GW·#13 HealService 추출·#15 적응 lease 정밀화·#17(a) self-hb 메아리 — 전부 STATE §2/§3 목적지·게이트 기존대로(상세 README §2).
