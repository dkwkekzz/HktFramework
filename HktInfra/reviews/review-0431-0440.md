# review 0431–0440 — #4 진짜 비동기 substrate (논리 클럭·인과 정렬)

> 범위: 0431~0440 = #4(broker lockstep·0001 이래 최상위 미해소 load-bearing)를 *해제할 기계*를 신규 박스 `src/async-core.js`(run() 밖 검증 전용 substrate)에 in-proc 으로 세움. 인과 서사·기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md)(비동기 실행 substrate 항목)·[progress/README.md](progress/README.md). 이 문서는 *정당성 감사*만.

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick** ✅ — async-core 는 *시뮬 tick 밖* substrate(오히려 net.step 동기 배리어 의존을 *줄이는* 기계). run() tick 루프 무변경 → tick 안 동기 작업 0. 알리바이: `src/topo-run.js:92~116`(tick 루프·async-core 호출 0)·async-core 는 `src/net-core.js:28` export 1줄만.
- **② 결정론 코어** ✅ — `Math.random` src 0 hit. 전 substrate 가 시드 PRNG(`mulberry32`·`async-core.js:36 lamportExchange`)만 사용·다이제스트=내용 함수(순열/손실 교란 무관). clock condition(a→b⇒C(a)<C(b)) 위반 0(`asynce2ecap` clock열). 알리바이: `async-core.js:17 makeLamportClock`·`:27 recv`(max+1)·`:172 applyDigest`.
- **③ 권위 단일 소유** ✅ — exactly-once(각 이벤트 정확히 한 번 적용·dups 0·missing 0=중복 쓰기/공백 0). 알리바이: `async-core.js:231 accountDelivered`(complete=dups0&&missing0&&unique==emitted)·`asynce2ecap` exactly-once 열 5/5 Y.
- **④ 은닉·단일 연결** ✅ — async-core 는 순수 함수 substrate(타 박스 내부/DB 직접 접근 0·import 는 `common.js`(PRNG/FNV)만). 알리바이: `async-core.js:13` 진입(`__c=common.js`)·다른 src/ 박스 require 0.
- **⑤ headless·원격 검증** ✅ — 순수 JS·UE/UObject src 0 hit·`node run.js 0440 asynce2ecap` 으로 수치 증명. 한계(정직): **in-proc substrate** — 실 `net.step` lockstep 배리어를 *아직 치환 안 함*(DownClient/UpClient 가 in-proc 먼저였듯 후속 arc). substrate 가 run() 밖에 살아 reg 구조적 0 — 통합 시 회귀 게이트 재확인 필요(§3 #4).

**메타(회귀 규율·박스 유계)**: reg 구조적 0 — async-core 는 run() 경로가 호출하지 않음(grep `makeLamportClock|makeHoldback|lamportExchange` in topo-run/build/actors/topology = 0 hit)→run() 비트 불변(`reg` ALL OK 전 step). 박스 >30KB 0개(async-core 최종 16.3KB). 코드 델타는 0431~0439 async-core.js 증분(각 step 1 메커니즘)+net-core.js export 1줄·0440 은 verify.js capstone 만(코드 박스 무변경).

## §2 검증 재현 (직접 실행·수치 인용)

- `node run.js 0440 asynce2ecap` → **5/5 OK**: M(3) 복제 순열+~20% 손실 도착을 각자 resync+holdback 재구성 → clock 위반 **0**·전 복제 다이제스트 일치(desync 0)·인과 존중(전순서 sound)·exactly-once(complete)·손실 발생(전 시드).
- `node run.js 0440 reg` → **ALL OK**(async-core 미호출→step-0027 비트 동일).
- `node run.js spine` → **ALL OK**(전 시리즈 누적 회귀·async-core 비-침습).
- grep 감사: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · async-core run() 경로 호출 0 hit.
- 직접 재현(단일 src 모델상 0431~0439 모드는 git 태그 동결·현재 코드로 재현): `makeHoldback` 6 인터리빙 동일 전순서 true · `totalOrderSound` strict/causal true · `clockConditionViolations` 0(`node -e` async-core 직접 구동).
- 알리바이 라인: `async-core.js` 17(Lamport)·36(exchange)·90(totalOrder)·110(holdback)·142(causalDeliver)·172(applyDigest)·183(asyncSite)·205(resyncSite)·231(account).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #4 | broker lockstep = 강제 동기(진짜 비동기 미해제) | ① | 🟡 **대폭 진전(in-proc substrate ✅·잔여=실 net.step 치환)** | 0431~0440 이 `async-core.js` 로 *해제할 기계*(논리 클럭·전순서·holdback·인과 배달·수렴·배리어-free·손실 resync·회계·`asynce2ecap` 5/5)를 in-proc 세움. **잔여**: substrate 를 실 `net.step` 배리어 치환에 배선 → **목적지 STATE §2 NEXT**(다음 큰 load-bearing) → 게이트: net.step 동기 tick 을 논리 클럭+holdback 배선으로(끄면 현 lockstep=회귀 0) |
| #71 | async-core substrate 가 실 전송/run() 미배선(검증 전용·by-construction) | ⑤/검증 | 🟡 **신규(#53 family·#4 의 substrate 판)** | 0431~0440 substrate 는 run() 밖 순수 함수·합성 이벤트 로그로 검증(실 net 메시지 아님·#53 by-construction 의 비동기 판) → **목적지 STATE §3 OPEN GAPS**(#4 실 치환 묶음과 동반) → 게이트: 실 net.step/transport 이벤트를 substrate 에 흘려 진짜 분기 도입(끄면 합성=회귀 0) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/정리 | 🟡 **확장(+async 10 모드 미승급)** | 이번 묶음 bespoke 10 모드(lcstamp~asynce2ecap)도 미승급·현 spine 모드=최신 `asynce2ecap` 1개(나머지 git per-commit·단일 src 모델상 과거 step verify.js 재실행 불가) → **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 안정 substrate 스모크 1~2 모드 점진 승급(끄면 현 spine=회귀 0) |
| #70 | UpClient/DownClient in-proc 액터(실 host.js OS 프로세스 아님) | ⑤ | 🟡 유지(이번 묶음 미접촉·#57 짝) | STATE §3 — 실 cluster 업스트림/다운스트림 RPC |
| #68 | orch zoneHost entity 컨테이너 미동기(where-view 만) | ③/정리 | 🟡 유지(경미·미접촉) | STATE §3 — #62 합류 묶음 |
| #69 | runMultiViaCoord 동치 transitive | 검증 | 🟡 유지(경미·미접촉) | STATE §3 — 동일 substrate 비교 경로 |

> **🎯 단계 평결 (0431-0440·SKILL §3.6)**: **#4 진짜 비동기 substrate(0431~0440·직전 0421-0430 verdict 가 STATE §2 로 올린 load-bearing #4)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *verdict 가 지목한 #4*(0001 이래 최상위 미해소 load-bearing) → **과심화 아님**(코디네이션 비동기 substrate 기능 7·신규 박스 `async-core.js`). DownClient/UpClient 가 *in-proc 먼저* 였듯, 실 net.step 치환에 앞서 토대(Lamport 논리 클럭 0431·인과 규칙 0432·전순서 0433·holdback 0434·인과 배달 0435·수렴 desync0 0436·배리어-free 0437·손실 resync 0438·회계 0439·capstone 0440)를 in-proc 으로 증명. **재현 증거**: `asynce2ecap` 5/5(M 복제 순열+손실→clock0·전복제 desync0·인과존중·exactly-once)·직접 재현(holdback 인터리빙 불변·totalOrderSound·clockCondition 0)·`reg`/`spine` ALL OK(async-core run() 미호출→비트 동일)·`Math.random`/UObject src 0·async-core run() 호출 0 hit·>30KB 박스 0(async-core 16.3KB). **#4 🟡대폭 진전(in-proc substrate ✅·잔여=실 net.step 배리어 치환)**·부수 **#71 신규(substrate 실 전송 미배선·by-construction)**. **다음 권고: #4 실 net.step 배리어 치환(substrate 를 실 전송에 배선·다음 큰 load-bearing) 또는 실 host.js child 업스트림(#70·#57 짝), 그 다음 경미 정리(#68·#69·#16 승급).** step-loop 이 STATE §2 로 승급.
