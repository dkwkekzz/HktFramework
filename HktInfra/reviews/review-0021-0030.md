# review-0021–0030 — 묶음 감사 (서비스 영속 완성 → write-behind 신뢰성 4부작 → PersistStore quorum → 박스 분할)

> 범위: step-0021~0030(10개·전부 닫힘·십진 경계 21–30). 서사(왜·이론·진척)는 계층 서사 — *미작성*. 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 본 감사는 *정합성*(척추·재현·이슈)만.
> 호(arc): 채팅 영속(0021 커맨드 로그)·압축(0022) → **write-behind 신뢰성 4부작**(0023 저널 홉 중간갭 NAK·0024 tail heartbeat·0025 in-flight give resend·0026 in-flight mint id-reconciliation) → **PersistStore failover**(0027 이중쓰기 backup·0028 N-replica quorum-read·0029 quorum-write ack·durableSeq) → **정리 step**(0030 박스 1개=파일 1개 분할·verify-kit engine 승격·기능 0).

---

## §1 척추 정합성 (SPINE §5 5불변)

이 묶음은 *데이터 계층(6)을 프로덕션 형태로 닫는다* — 서비스 영속(가방 0017·채팅 0021)을 압축·신뢰성·복제로 완성하고, 마지막에 누적 비대를 박스 분할로 정리한다. 비판적 판정:

- **① 신성한 tick — ✅(미묘점 정직 공개)**. 채팅·버스·persist·ranking·audit 박스는 여전히 `onTick` 부재(`step-0030/svc-bus.js:8`·`svc-chat.js:8`·`persist.js:8`). **단 0024 부터 InventoryService 가 `onTick` 을 가졌다** — *저널 heartbeat*. 이는 *존 시뮬 tick 이 아니라 가방 자체 제어 평면 tick*이며 ⒜ off/조건 미달이면 early-return(`step-0030/svc-inventory.js:88-93` — `journalHb` OFF → 메시지 0 → 0023 비트 동일) ⒝ persist 로만 발신(존 도달 0·`sacred` svcMsgsToZones 0). 신성한 tick 위반 아님 — *서비스 제어 평면*은 존 박자와 직교. 클라 give-resend(0025)·id-reconcile(0026)도 존 tick 밖 제어 평면. 알리바이: `step-0030/svc-inventory.js:88`(heartbeat 게이트).
- **② 결정론 코어 — 🟡(이원화 지속·복구=재현)**. write-behind 복구 전부 §4 "복제=재현"(저널/커맨드 로그 replay·상태 전송 아님) — 채팅은 *커맨드* 리듀서 재실행(0021), 가방은 *효과* 저널 replay(0017), 둘 다 같은 PersistStore 스냅샷 압축(0018/0022). re-mint id = `mintTotal` 단조(시드 함수·`step-0026.md §5`). 시뮬 `Math.random` **0**(0021~0030 grep). `engine/panel-kit.js:29` 잔존(이 묶음 코드 아님·이슈 #8). 비트-결정론 VM C++ 대기(#1). 알리바이: `step-0030/svc-chat.js`(replay 리듀서)·`step-0029 §2`(durableSeq 결정론 집계).
- **③ 권위 단일 소유 — ✅(데이터 복제로 강화)**. 원장 쓰기 권위는 InventoryService *단일* — 복제(persist N개)는 전부 *수신만*(읽기전용 store·쓰기 권위 이동 아님·`step-0028.md §4-3`). quorum *write* ack 은 durable *확인*이지 권위 이동 아님(`step-0029`). give-resend/id-reconcile 도 클라는 *요청*일 뿐 가방이 `owner==from` 멱등 판정(이중 전송 0·`step-0025.md §2`). `durableSeq` 워터마크가 (N+1−W) 죽음 견디는 durable 프런티어를 *정확히* 표시(crash 복구 프런티어 예측·`step-0029 wquorum`). 알리바이: `step-0030/svc-inventory.js`(durableSeq while 루프)·`step-0028/net-core.js`(quorumMergeJournals).
- **④ 은닉·단일 연결 — ✅**. `persist2`..`persistN+1`·NAK·heartbeat·`journal_ack`·reconcile 전부 *서버간 제어 평면*(클라 비가시·전 step `hide` 누설 0). **발견(메타)**: 0025 가 `toAvatar` 게이트웨이 은닉(`_relayItemResult`) 때문에 give-resend 수신자를 *발신 시점*에 캡처해야 했다 — 은닉(④)이 복구 설계를 *제약*한 사례(`step-0025.md §9`). 알리바이: `step-0030/gateway.js`(toAvatar 은닉).
- **⑤ headless·원격 검증 — ✅(분할로 토폴로지 정렬)**. 전 step `node run.js <NNNN>` 한 줄·멀티프로세스 14 프로세스 E2E 비트 동일. UE 모듈 링크 **0**(grep). 0030 박스 분할이 *파일 경계=서버 경계*로 정렬(박스→독립 프로세스 승격 시 파일이 곧 서버)·검증 진입(`node run.js`·`close-step.js`) headless 한 줄 유지. 알리바이: `step-0030/`(박스 1개=파일 1개·`net-core.js` 진입점 4KB).

**회귀 0 규율(메타)**: 0021~0030 전 step `reg` 25/25 — 새 항 OFF = 휴면(chatpersist·journalReliable·journalHeartbeat·clientResend·mintRecon·persistBackup·persistReplicas·quorumW 전부 플래그=0 → 직전 비트 동일). **직전 묶음 리뷰(0011-0020 §1)가 메타로 지적한 복사 전진 비대**(단일 net-core 88KB→0029 128KB·verify 76KB)를 **0030 정리 step 이 정확히 해소** — 진입점 4KB + 박스 13개(최대 topology 28KB) + verify→`engine/verify-kit.js`(76KB·1곳 승격) + 셸 4KB·dir 288→248KB. **코드 verbatim 이동**(reg 비트 동일이 손-재작성 0 을 증명) — 비대화 트리거(박스 30KB·디렉토리 300KB)의 시행. 리뷰 전방 연결(메타 지적→정리 step)의 실증.

> **메타(두 🟡 = 위반 아닌 연기된 채무)**: ② 결정론 이원화·broker lockstep(#4) 모두 *설계 순서*로 매 step §9·STATE 🔴 에 명시. 신규 척추 위반 0.

---

## §2 검증 재현 (실행 출력 인용)

대표 step `node run.js <NNNN>` **ALL OK** — 회귀 0·결정론 전파·권위 보존·수렴을 직접 재현(문서 PASS 주장을 베끼지 않음):

| step | 핵심 수치(라이브 재현) |
|---|---|
| 0021 | ALL OK · 14 프로세스 · 채팅 kill→커맨드 78개 replay@60·복구투명 true(배달 211~229)·누설 0 · `chatDigest` 7=**0xe46f106a** |
| 0023 | ALL OK · 14 프로세스 · 저널홉 손실 ON 무손실(NAK 166~187·재전송 223~239) vs OFF 갭 16~18 · `invDigest` 42=**0x7a122947** |
| 0024 | ALL OK · tail 손실 heartbeat ON 완전·복구 무손실 / NAK-only tailNAK 0(미감지) · persist killed=false |
| 0026 | ALL OK · in-flight mint: recon OFF itemDesync 6 → ON **0**(투영==truth)·reconcile 6·own≤1·역인덱스 정합 |
| 0028 | ALL OK · N=3 quorum-read: 완전복제 union==base·부분쓰기 union==base(merge>단일)·생존1 부분=손실(정족수 경계) |
| 0029 | ALL OK · quorum-write: 무손실 durableSeq=total−1/윈도 0·tail 미달 durableSeq 앞에서 정지·윈도 23~24·crash 윈도 정확 소실 |
| 0030 | ALL OK · reg 25/25(0029 비트 동일)·18모드 ALL OK·net-core 128→4KB·verify→engine 76KB·dir 288→248KB |

- **회귀 사슬(골든 등가)**: `node run.js spine` → **`결과: ALL OK (47 step 비트 사슬 통과)`**(라이브). 묶음 10 step 자동 모드 전부 OK — 어느 step 해시도 불변(0030 분할·verify-kit 승격이 0001~0029 회귀를 1비트도 안 깸의 증거·0031~0047 하류도 회귀 0).
- **grep 감사**: 시뮬 `Math.random(` 실호출 → step-0021~0030 스냅샷(0030 분할 박스 포함) **0건**(`engine/panel-kit.js:29` 1건은 이 묶음 코드 아님·이슈 #8). `UObject|CoreUObject|GameplayTags` → **0건**(⑤). 서비스 박스 `onTick` → persist·bus·chat·ranking·audit **부재**·InventoryService 만 *heartbeat 제어 평면 onTick*(off=no-op·존 도달 0·`svc-inventory.js:88`).

---

## §3 이슈 → 후속 step 연결 (전파 고리)

> 먼저 이월 원장(0001-0010·0011-0020) 재점검 — 이 묶음 코드가 해소한 이슈를 ✅떨군다.

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #7 | 채팅 best-effort — per-message ack/resend·홉 신뢰 부재 | 서비스 | 🟡 유지 | 0021/0022 는 채팅 *영속·압축*(커맨드 로그)을 더했으나 *전달 신뢰*(홉 ack/resend)는 미적용 — 0023/0024 신뢰화는 *가방 홉 한정*(0024 §9 ⓒ "채팅 저널 홉 tail 미적용"). **STATE §3** 추적 → 게이트: 채팅 홉 NAK/resend(0023 의 채팅 판) |
| #1 | 결정론 이원화(C++ 승격 대기) | ② | 🟡 유지 | **STATE §3** → ISimCore 뒤 C++ HktCore 승격. 이 묶음 미착수 |
| #3 | cross-zone AOI 지연 | ④/성능 | 🟡 유지(미착수) | **STATE §3** → 증분 ghost·heartbeat 적응. 이 묶음 데이터 계층만 |
| #4 | broker lockstep = 강제 동기 | ① | 🟡 유지(load-bearing) | **STATE §2** → 비동기 실행+논리/벡터 클럭. 매 step §9·STATE 🔴 |
| #6 | 동적 경계·N 존 오케스트레이션 | ③/성능 | 🟡 유지(미착수) | **STATE §3** → 오케스트레이터 동적 배치 |
| #8 | `engine/panel-kit.js:29` Math.random | ②/정리 | 🟡 유지(경미) | **STATE §3 OPEN GAPS** → 시드/결정론 id 로 교체(시각 위젯만·회귀 0) |
| #9 | **give-resend 견고성 — in-order 의존 + 멀티프로세스 패리티 갭** | ③/⑤ | 🟡 **신규** | 0025 §9 ⓕⓖ: ⒜ `_confirmGive` 가 클라 홉 *in-order* 암묵 의존(재정렬 시 itemId 첫-매칭이 틀린 toAvatar 기록→seed 1234 itemDesync 2 재현) ⒝ `clientResync`/`resendGives` 가 `run()`(인프로세스)에만·`runMulti`/host/cluster 미구현 → in-flight give/mint 복구가 *멀티프로세스 경로 부재*. **목적지 STATE §3** → 게이트: opSeq 키 매칭 견고화 + 멀티프로세스 클라 resend 핸드셰이크(현 시나리오 FIFO·인프로세스라 미발현=회귀 0) |

**이미 후속 게이트가 메운 신규 이슈(기록만·원장 비축적)**:
- **정합성 윈도 *해소* 부재**(0029 §9 — `durableSeq` 가 W 미달 윈도를 *감지*만·durable 전환 안 함) → **0031 quorum-fill** + **0032 유계 sweep/retry** 가 메움(STATE §3 ✅).
- **활성 중 서비스 다운타임 후 재발행**(0021/0025/0026 — quiescent restart 만) → STATE §2/§3 백로그(활성 중 다운타임+재발행) 추적 중.

**전파 요지**: 이 묶음은 *데이터 계층(6) 신뢰성을 거의 완성*했다 — write-behind 4부작(0023~0026)이 저널 홉 전송 손실·in-flight give/mint 손실의 사각을 모두 닫고(0026 §6 "사각지대 모두 닫힘"), PersistStore failover 3부작(0027~0029)이 단일점을 N-replica + quorum read/write 로 일반 해소했다. 또 **0030 정리 step 이 직전 묶음 리뷰가 메타로 지적한 복사 전진 비대를 정확히 갚았다**(전방 연결 실증). 신규 #9(give-resend 견고성)만 원장 이월 — 좁은 사각(재정렬·멀티프로세스 클라 resend)이라 현 시나리오 미발현(회귀 0). 남은 큰 부채는 여전히 **#1(C++ 이원화)·#4(lockstep 해제)**·#7(채팅 홉 신뢰)로 이 묶음 범위 밖. 신규 척추 위반 0. **0021~0030 전 닫힌 step 감사 완료(셋째 묶음).**
