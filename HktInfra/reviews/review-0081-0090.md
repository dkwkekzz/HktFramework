# review-0081-0090 — 귓속말/파티 전달 신뢰·관측·멤버십 영속 호

> 범위: step-0081~0090 (닫힘). *무엇을 했나*의 박스별 직관 진행은 [progress/3-services.md](progress/3-services.md)·[progress/5-coordination.md](progress/5-coordination.md). 이 문서는 *정합 감사*(척추·재현·이슈)만.
>
> 호 한 줄: 0071~0080 이 세운 라우터(wrouter)·수신함(mbox)·멤버십(pservice) 위에 — **전달 dedup 메모리 유계화(seq·epoch 두 차원)·전달 관측(성공/실패 발행)·파티 1:N 영수증/ack 집계·파티 멤버십 영속(증분·저널·압축)** 을 더해 0071-0080 리뷰가 연 #18·#19·#20 을 메웠다.

---

## §1 척추 정합성 (SPINE §5 5불변)

| # | 불변 | 판정 | 코드 알리바이 (현재 `src/`) |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 전 박스 onTick 0 또는 *제어 평면 timeout* 만 — `src/svc-whisper.js:131` onTick 은 deliverTimeout 재발신(벽시계·존 tick 무관). 박스 3종 `grep await/readFileSync/fetch` = 0 hit. crash/reconstruct/restart 는 주입 훅(`src/topology.js:175`)·tick 밖. |
| ② | 결정론 코어 | ✅ | 세계 쓰기 무변경 — 전 step 비침습 단언(minted ON==OFF·각 verify `nonInvasive`). 박스 신규 코드 `Math.random` 0(잔존 1건은 `engine/panel-kit.js:29`·#8·헤드리스 경로 밖). |
| ③ | 권위 단일 소유 | ✅ | dedup·집계·발행·저널 전부 *원장 권위 0* — `src/svc-whisper.js:70` `_partyAck`·`:121` `_partyTally` 는 회계만·`src/svc-party.js:69` crash 는 멤버십 projection 만 비움(원장 무관). 멤버십 권위는 PartyService 단일. |
| ④ | 은닉·단일 연결 | ✅ | 발행은 `bus` 주소만 알고 구독자 무지 — `src/svc-whisper.js:138`·`:95`·`src/svc-party.js:46` 전부 `net.send(addr, this.bus, {type:'pub'…})`. epoch 는 whisperDeliver 계약 필드(라우터↔mbox 만)·`src/svc-mailbox.js:81`. audit 는 구독 행만 추가(`src/topo-build.js` decouple). |
| ⑤ | headless·원격 검증 | ✅ | `grep -riE 'uobject\|coreuobject\|gameplaytags\|#include' src/` = 0 hit. 전 가설 `node run.js`/`spine` 로 수치 증명. |

**메타(구현 품질)**:
- **회귀 0 규율** — 매 step 새 항이 단일 플래그(`deliverDedupBound`·`failedPublish`·`partyReceipt`·`partyChange`·`partyPersist`·`partySnapshot`·`deliveredPublish`·`epochKeyed`·`epochBound`)에 게이트, OFF → 직전 비트 동일. 특히 0089 의 *epoch 미부착 시 키=정확히 `m.from`*(`src/svc-mailbox.js:81` 삼항)·0088 의 *비-파티 시 inflight.party=undefined* 가 reg-0 을 보존하는 정밀 가드. spine 90-step ALL OK 가 전 역사 불변 무손상 증거.
- **박스 유계(30KB 예산)** — `src/topo-build.js` **30477B**(29.76KB)·`src/svc-whisper.js` **28725B**(28.05KB) 둘 다 *예산 근접*(헤드룸 topo-build ~240B). 다음 기능 step 전 정리 압력(→ #22 신규).
- **직교 유계화 설계** — 0081(seq 차원·연속 워터마크)+0090(epoch 차원·현재-epoch-만)이 dedup 메모리를 두 축으로 독립 유계화(`src/svc-mailbox.js:49`·`:34`). 깔끔한 분해.

## §2 검증 재현

- `node run.js spine` (src 누적 회귀 — reg + 보유 가설 모드, 전 역사 불변을 현재 코드에 단언) → **ALL OK** (90-step). `/tmp/review_spine.log` 꼬리: `결과: ALL OK`.
- `node run.js` (현재 step=0090 `pepochbnd` ON-경로 + reg) → **ALL OK**: 3재시작·`epochKeys ON 1` vs `OFF 4`(∝epoch)·둘 다 received 8/dup 0.
- grep 감사: `Math.random` → `engine/panel-kit.js:29` 1건(#8·헤드리스 무관)·`UObject/UE/#include` → 0·박스 3종 `await/sync-IO` → 0.
- 코드 알리바이 확인(현재 `src/`): 0081 `_seenAdd`/연속 흡수 `svc-mailbox.js:49~57` · 0089 epoch 부착 `svc-whisper.js:117,142`·restart `:149` · 0090 prune `svc-mailbox.js:34~40` · 0082/0087 발행 `svc-whisper.js:138,95` · 0083/0088 집계 `svc-whisper.js:66~72,93,121` · 0084~0086 멤버십 `svc-party.js:33~41,46,55,61,69,72`.

> **재현 한계(구조적·#16)**: verify-kit `ORDER` 가설 모드는 `…reliable·tail·inflight·degrade…`(≈0024)에서 멈춘다(`engine/verify-kit.js:702`). 0081~0090 이 더한 10개 bespoke 모드(`wsbound·wfpublish·pagg·pchange·ppersist·pcompact·wdpublish·pack·pepoch·pepochbnd`)는 *close 시 1회*만 ON-의미 단언·spine 은 reg-0 만 보증. archive 폐기로 옛 step 의 ON-경로는 git checkout 외 재현 불가 → ON-의미 미보증 빚이 라우팅/전달 호 길이에 비례 누적(#16 확장).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #18 | 전달 신뢰 1:1 한정 → 파티 1:N 집계 | 서비스 | ✅ **해소** | **0083** partyReceipt(라우팅 결정 집계 routed/bounced·done·`svc-whisper.js:66`) + **0088** partyAckTally(영수증 집계 delivered·acked·`:70,93`). 멤버별 *수신함* 토폴로지(다중 Mailbox)는 잔여 → #23 |
| #19 | 파티 멤버십 영속·증분 가입/탈퇴 | 서비스/데이터 | ✅ **해소** | **0084** partyJoin/Leave 델타+svc.party.changed 발행 + **0085** partyPersist(변경 저널 crash→reconstruct·`svc-party.js:72`) + **0086** partySnapshot(스냅샷+tail 압축·`:37`). cluster kill→replay 통합은 잔여 → #9 흡수 |
| #20 | dedup seen 무계 + 전달 실패 발행 | 서비스/④ | ✅ **해소** | **0081** deliverDedupBound(연속 워터마크 O(gap)·`svc-mailbox.js:49`) + **0082** failedPublish(svc.whisper.failed→audit) + **0087** deliveredPublish(성공+tries 발행). epoch 차원까지 0089/0090 으로 확장 유계 |
| #9 | 라우팅·전달·영속 호 멀티프로세스 미배선 | ③/⑤ | 🟡 **확장** | `grep mailbox\|whisper\|pservice\|party src/host.js` = 0 hit — wrouter/mbox/pservice + **0085 영속·0089 epoch restart 까지 전부 인프로세스 전용**(주입은 `src/topology.js:174~181` run 루프만). **목적지 STATE §3** → 게이트: host.js 박스 배선 + 멀티프로세스 failover/restart 핸드셰이크(현 FIFO·인프로세스라 미발현=회귀 0) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 **확장**(+10) | 0081~0090 의 10 모드 추가 미승격(`engine/verify-kit.js:702` ORDER 정지). **목적지 STATE §3 OPEN GAPS** → 게이트: 안정 호(wrouter/mbox/pservice 스모크 1~2 모드) verify-kit 승급(끄면 현 spine=회귀 0) |
| #22 | `topo-build.js`·`svc-whisper.js` 30KB 예산 근접 | ⑤/정리 | 🟡 **신규** | topo-build 30477B(헤드룸 ~240B)·svc-whisper 28725B — 다음 기능 step 이 넘길 위험. **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 정리 step(박스 재분할/engine 승격·기능 0·reg 비트 동일·0030/0035/0043 패턴) |
| #23 | 멤버별 Mailbox 토폴로지 부재 → 파티 ack 반쪽 | 서비스 | 🟡 **신규**(경미) | 0088 partyAcked 는 *Mailbox 가진 up 멤버*만 delivered 기여 — 현 토폴로지 mbox 1개라 up 멤버 여럿 중 mbox 만 ack(0088 §9). **목적지 STATE §3** → 게이트: 파티원마다 수신함 박스(끄면 현 1-mbox 거동=회귀 0) |
| #24 | epoch 가지치기 straggler 가정 | ④ | 🟡 **신규**(경미·watch) | 0090 prune 은 *옛 epoch 지연 도착 0* 가정(`svc-mailbox.js:38`) — 가지친 뒤 옛 epoch straggler 도착 시 신규 오인 재수신 가능(현 토폴로지 restart 가 inflight 비워 무발현). **목적지 STATE §3** → 게이트: prune grace 윈도(0051 lease grace 패턴·끄면 현 거동=회귀 0) |

**유지(이월·이번 묶음 무변)**: #1(결정론 이원화·STATE §3)·#3(cross-zone AOI·§3)·#4(broker lockstep·STATE §2)·#6(동적 경계·§3)·#7(채팅 홉 신뢰·§3)·#8(panel-kit Math.random·§3)·#10(give×result-ahead·§2/§3)·#11(per-producer ack 풀 토폴로지·§3)·#13(HealService 분리 잔여·§3)·#15(적응 lease 정밀화·§3)·#17a(self-hb 메아리·§3).
