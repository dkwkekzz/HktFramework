# review 0391–0400 — #66 tick placement-aware + #67 orch 이중 권위 합류

> 범위 0391~0400(코디네이션·`src/cluster-coord.js`). 인과 서사·서버 기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md) 기능 19. 이 문서는 *정당성 감사*만(narrative 0).

## §1 척추 5불변 정합성 (실물 코드 알리바이)

직전 묶음(0381~0390·#65)이 placement 권위로 *측정*은 정합시켰으나 데이터 평면 *구동*(#66)과 orch 집행 뷰(#67)는 placement 와 갈렸다 — 이 묶음이 둘을 닫는다. 전부 새 박스 `cluster-coord.js`(run() 데이터 평면 미사용·require 0 순수 주입).

| # | 불변 | 판정 | 알리바이 (현재 코드) |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 코디네이터 lifecycle/술어는 *제어 평면*(시뮬 tick 밖). `run(ticks, onTick)`(`cluster-coord.js:105`)의 onTick 훅도 tick 사이에 실행 — 시뮬 tick 내 동기 I/O 0. |
| ② | 결정론 코어 | ✅ | `src/` `Math.random` 0 hit. tick 순회가 `Object.keys(this.placement)` 삽입 순(`:94`·결정론). 시드 5종 동일(`coordunifiedcap` 5/5 maxDesync 0). |
| ③ | 권위 단일 소유 | ✅**강화** | #66/#67 의 핵심. placement(where 권위)가 데이터 평면 구동(`:89,94`)과 orch 집행 where-view(`_orchWriteBack:67`·migrate `:120`·failover `:131`) 둘 다를 끈다 → 두 where 권위가 *단일*로 합류(`authoritiesAgree:65`·`unifiedCoherent:182`). migrate=release+acquire·failover=lost 명시 추적. |
| ④ | 은닉·단일 연결 | ✅**강함** | cluster-coord.js `require` 0 hit(순수 주입)·`zone.js` 직접 import 0 hit. orch 접근은 공개 API/자료구조(`runningHostOf`·`running`/`placement` Map)만. |
| ⑤ | headless·원격 검증 | ✅ | 실 `host.js` child_process E2E(`coordunifiedcap` 가 `Cluster.spawn()`→실 프로세스 migrate/failover 구동). `UObject|CoreUObject|GameplayTags` src 0 hit. |

**메타(회귀 0·박스 유계)**: reg 5/5 비트 동일(write-back 은 run() *종료 후* orch 객체에만 작용 → run() 기록 출력 불변·구조적 reg 0). `src/` >30KB 박스 **0개**. cluster-coord.js 26.7KB(<30KB).

## §2 검증 재현 (직접 실행·인용)

- **`node run.js 0400`(coordunifiedcap·arc capstone)**: 5/5 OK — 루프 *중* t=2 migrate z1 A→B + t=3 failover hostA→hostB 후 **unifiedCoherent Y·maxDesync 0·rpt.coherent&&authoritiesAgree Y**, 대조 **옛 driver.clusterDesync 1**(orch plan stale). #66(mid-loop migrate 추종)+#67(write-back 합의)를 한 시나리오로 동시 입증.
- **`node src/verify.js reg`**: ALL OK(5/5 baseline 비트 동일).
- **`node run.js spine`**(전 시리즈 누적 회귀): **ALL OK** — 0391~0400 의 어떤 변경도 0001~0390 불변을 깨지 않음.
- **grep 감사**: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · cluster-coord `require` 0(순수 주입) · cluster-coord→`zone.js` import 0 · >30KB 박스 0.

> **by-construction 주의(#53 family)**: `unifiedCoherent` 의 `authoritiesAgree` 부분은 orch.running stale host 주입 시 N 으로 떨어짐을 0399 가 실측(`coordunified` 5/5·by-construction 아님). 단 `coordDesync`/`placementCoherent` 는 여전히 인프로세스 orch 권위와 실 cluster snapshot 비교(0369 ghost 주입으로만 진짜 분기 입증·#53 잔존).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #66 | tick()/deliver 재생 placement-aware 아님 | ⑤/검증 | ✅**해소(0391~0393)** | tick 순회(`:94`)·deliver(`:89`)·run(onTick) mid-loop(`:105`)로 placement 추종·`coordmidmigrate` maxDesync 0(대조 clusterDesync 1). 원장서 제거. |
| #67 | placement↔orch zoneHost 이중 권위 | ③/코디네이션 | ✅**해소(0394~0400·where-view)** | migrate/failover write-back(`_orchWriteBack:67`)으로 orch.running/placement 동기·`authoritiesAgree`/`unifiedCoherent` Y. **단 잔여 #68**(아래). 원장서 #67 제거. |
| #68 | orch zoneHost *entity 컨테이너* 미동기(write-back 은 where-view 만) | ③/정리 | 🟡**신규(경미)** | `_orchWriteBack`(`:67`)이 orch.running/placement Map 만 동기 — orch 내부 `zoneHosts` 컨테이너(0301~·entity 권위)는 미갱신. where 권위는 합류됐고 coordDesync/syncedCoherent 가 entity 정합을 별도 보장 → 경미. **목적지 STATE §3 OPEN GAPS** → 게이트: write-back 이 zoneHost 컨테이너도 동기(끄면 현 where-view 만=회귀 0)·#62 합류 묶음. |
| #62 | 코디네이터↔옛 cluster-run.js runMulti 코드 합류 | ⑤/검증 | 🟡유지 | 능력은 섰음(상주 제어 평면)·두 별개 runner 코드 합류만 잔여. **목적지 STATE §2 NEXT** → 게이트: 두 runner 합류(끄면 병존=회귀 0). |
| #16 | bespoke 모드 spine 미승급 | 정리 | 🟡유지·확장 | 이 묶음 10 모드(coordtickplace~coordunifiedcap)도 미승급(현 spine 모드=최신 coordunifiedcap 1개·나머지 git per-commit) → cluster-coord spine 안전망 얇음. **목적지 STATE §3**. |
| #61·#4·#53 | 업스트림 실 클라·진짜 비동기·정합 술어 by-construction | ②/⑤/검증 | 🟡유지 | 변동 없음(이 묶음 무관). 원장 유지. |

> **🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 **0**(너비 1차 완료 유지·0210 이래) 위 2차. 0391~0400 은 *직전 리뷰(0381~0390) verdict 가 지목한 load-bearing #66·#67* 을 그대로 집행 — **과심화 아님**(코디네이션 기능 19·승인된 다음 절). #65(0381~0390)가 placement 권위로 *측정*을 정합시킨 뒤, 이 묶음이 그 권위를 *데이터 평면 구동*(#66)과 *orch 집행 뷰*(#67)에까지 일원화. **#66·#67 ✅해소**, 잔여는 경미한 #68(entity 컨테이너 동기)로 분리. **다음 권고**: 코디네이터↔cluster-run.js runMulti 코드 합류(#62·STATE §2 NEXT), 그 다음 업스트림 intent 실 클라(#61)·진짜 비동기(#4). step-loop 이 원장을 읽어 STATE §2 로 승급.
