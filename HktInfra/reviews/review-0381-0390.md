# review 0381–0390 — #65 양방향 동기: 코디네이터 placement 권위 분리

> 범위: step-0381~0390(닫힘). 인과 서사·기능 분해는 [progress/5-coordination.md](progress/5-coordination.md) 오케스트레이터 서버 기능 18. 이 문서는 *정당성 감사*(척추 5불변·재현·이슈)만.

## §1 척추 정합성 (SPINE §5 5불변)

이 묶음은 `src/cluster-coord.js`(19.9KB·require 0·순수 주입) 한 박스만 키웠다. #62(0371~0380)가 남긴 #65 — 코디네이터 migrate/failover 가 *실 cluster* 만 바꾸고 placement 권위는 갱신 안 해 실 migrate 후 `clusterDesync` 가 stale orch plan 으로 발산 — 을, 코디네이터가 *placement 권위(where)*를 직접 소유(orch=entity 권위/what 분리)하는 설계로 해소했다.

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | `coordDesync`/`placementCoherent`/`report` 모두 tick 밖 제어 평면(`cluster-coord.js:54,138,126`)·데이터 평면 tick 은 실 host `zone.onTick` 만(`:68`) |
| ② | 결정론 코어 | ✅ | `coordsyncedcap` 5/5 시드 무관 동일·`Math.random` src 0 hit |
| ③ | 권위 단일소유 | ✅(강화·재정의) | migrate=release+acquire(`:96` driver.migrateZone)+placement 갱신(`:97`)·failover=killHost+재가동+placement 갱신+lost 기록(`:104~108`)·placement⟷실 1:1 bijection(`placementCoherent:138`)로 단일 소유 *입증*(이주/장애 후도) |
| ④ | 은닉·단일연결 | ✅강함 | `cluster-coord.js` **require 0**·zone.js 직접 import 0·driver/cluster API(rpc·killHost) 로만·orch→zone import 0 hit |
| ⑤ | headless·원격검증 | ✅ | `coordsyncedcap` 가 `new Cluster([])`+실 `host.js` child_process 구동·UObject/UE src 0 hit |

**메타**:
- **reg 0 — 구조적**: cluster-coord 는 run() 데이터 평면 미사용 새 박스 → reg by-construction 0(신호 0·#16 family·아래).
- **박스 유계**: cluster-coord 19.9KB·>30KB 박스 0(최대 orch-zonebridge 28.96KB).
- **#53 부분 진짜화 지속**: 이 묶음 술어는 *진짜 refutation 분기*를 갖는다 — `coordmigsync`(0383)는 coordDesync 0 vs clusterDesync 1 *대조*로 stale 검출·`coordfocoh`(0386) ghost 주입 desync 1·`coordplacecoh`(0389) 불일치 주입 N. **단** syncedCoherent 의 maxDesync==0 은 *정적 수렴 루프*(첫 tick 후 신규 intent 0) 위 측정이라 정상 경로는 여전히 구조적.

## §2 검증 재현 (실행·인용)

- `node run.js 0390 coordsyncedcap` → **5/5 ALL OK**: syncedCoherent Y·placementCoherent Y·coordDesync 0 — 대조 **driver.clusterDesync 1**(옛 #65·orch plan stale·0380 이 migrate/failover 제외한 이유).
- `node run.js 0390 reg` → **ALL OK**(net.log+상태 baseline 비트 동일·구조적).
- `node run.js spine` → **ALL OK**(전 시리즈 누적 회귀).
- grep: `Math.random` src **0**·`UObject|CoreUObject|GameplayTags` src **0**·`cluster-coord require` **0**(순수 주입)·`orch→zone.js require` **0**·>30KB 박스 **0**.
- 알리바이 라인: placement SSOT `cluster-coord.js:38,46`·coordDesync `:54`·migrate 갱신 `:97`·failover+lost `:103~108`·lost 제외 `:62`·report `:127`·syncPlan placement `:115`·placementCoherent `:138`·syncedCoherent `:153`.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #65 | 코디네이터 migrate/failover↔placement 권위 단방향 | ③/검증 | ✅ **해소(0381~0390)** — 코디네이터가 placement 권위(where)를 직접 소유·lifecycle 마다 갱신(migrate `:97`·failover `:107`)·`coordDesync`/`syncedCoherent` 가 placement 로 조회 → **migrate/failover 후 desync 0**(`coordsyncedcap` 5/5·옛 clusterDesync 1 대조). *단 ledger 가 제안한 "orch zoneHost 갱신" 이 아니라 where/what 권위 분리로 해소* → 잔여는 #67 로 분리 | 원장서 제거·해소 기록만 |
| #66 | tick()/deliver 재생이 placement-aware 아님(orch.hostSpawnPlan 사용) | ⑤/검증 | 🟡 **신규(latent)** — `tick()`(`cluster-coord.js:72`)·deliver 재생(`:69`)은 여전히 orch.hostSpawnPlan 으로 host/zone 순회 → *run 루프 중* migrate 발생 시 옛 host 를 tick(새 host 미tick). capstone 은 lifecycle 을 run 루프 *뒤* 실행해 미발현 | **목적지 STATE §3 OPEN GAPS**(무르익으면 §2·연속 운영 중 migrate) → 게이트: tick/deliver 가 placement 순회(끄면 placement==orch plan 정상=회귀 0) |
| #67 | placement 권위가 orch zoneHost 와 *이중 권위*(decouple·orch 내부 미동기) | ③/코디네이션 | 🟡 **신규** — 코디네이터 placement(where) 가 orch 의 zoneHost/zoneRuntimes(0301~) 와 별개 권위 → 둘이 갈릴 수 있고 orch 자체 placement API 와 미합류(0390 한계 ⒝). #65 를 *write-back* 아닌 *decouple* 로 푼 대가 | **목적지 STATE §2 NEXT**(#62 동반) → 게이트: 코디네이터 placement↔orch zoneHost 합류 또는 단일 권위화(끄면 현 decouple=회귀 0) |
| #62 | 코디네이터↔cluster-run.js runMulti 코어 미통합 | ⑤/검증 | 🟡 **유지**(이 묶음 무접촉) — 코디네이터(`cluster-coord.js`)와 옛 lockstep runMulti(14-프로세스) 두 별개 runner·합류 미수행 | **목적지 STATE §2 NEXT** → 게이트: 코디네이터를 cluster-run.js runMulti 코어로 승격/합류(끄면 현 병존=회귀 0) |
| #63 | 실 failover 상태 소실(영속 미연동) | ⑤/데이터 | 🟡 **유지·명시화** — 0385 lostZones 가 손실을 *명시 추적*·0386 이 coherence 정의에 *용인*(reverse 제외)만·복구 아님 | **목적지 STATE §3 OPEN GAPS** → 게이트: failover 가 월드 영속서 상태 복구(데이터 계층 묶음·끄면 현 빈 재가동=회귀 0) |
| #16 | bespoke 모드 spine 미승급 | ②/⑤/정리 | 🟡 **확장** — coordplace~coordsyncedcap 10모드 미승급·현 spine 모드=coordsyncedcap 1개 → reg 구조적 0 와 겹쳐 cluster-coord spine 안전망 얇음 | **목적지 STATE §3 OPEN GAPS** → 게이트: 안정 후 capstone 1모드 spine 승급(끄면 현 spine=회귀 0) |

> 그 외 유지(무접촉): #64(migrate in-flight 재라우팅)·#61(업스트림 intent 실 클라)·#4(진짜 비동기)·#50·#46·#48·#44·#39·#55·#37·#38·#36·#3·#6·#7·#8·#10·#11·#13·#17a·#25·#27·#28·#29·#30b·#32·#41·#1(⛔범위)·#58'(zonebridge 28.96KB watch).

> **🎯 단계 평결 (0381-0390·SKILL §3.6)**: **#65 양방향 동기 — 코디네이터 placement 권위 분리(0381~0390·prior 리뷰가 지목한 load-bearing #65 해소)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *prior 리뷰 verdict 가 지목한 #65* 집행 → **과심화 아님**. 코디네이터가 placement 권위(where)를 직접 소유(orch=entity 권위/what 분리)하고 lifecycle 마다 갱신: placement SSOT(0381)·coordDesync(0382)·migrate placement 갱신(0383 핵심 fix)·coordCoherent 가 coordDesync 채택(0384)·failover placement+lost(0385)·lost 기대 부재 제외(0386)·placement-aware report(0387)·syncPlan placement 기준(0388)·placementCoherent bijection(0389)·capstone syncedCoherent(0390·migrate/failover 포함). **재현 증거**: `coordsyncedcap` 5/5(syncedCoherent Y·coordDesync 0 vs 옛 clusterDesync 1)·`reg`/`spine` ALL OK·`Math.random`/UObject src 0·cluster-coord require 0·>30KB 박스 0(19.9KB). **세 잔여(다음 권고)**: ⒜ **#67 이중 권위** — placement 가 orch zoneHost 와 decouple(write-back 아닌 분리로 #65 해소)·orch 내부 미동기 ⒝ **#66 tick 미-placement-aware** — 데이터 평면 tick 이 orch.hostSpawnPlan 사용(run 중 migrate latent) ⒞ **#62 코드 합류** — 코디네이터↔cluster-run.js runMulti 병존. **다음 권고: 코디네이터 placement↔orch zoneHost 합류(#67·#62) + tick placement-aware(#66), 그 다음 업스트림 intent 실 클라(#61)·진짜 비동기(#4).** step-loop 이 STATE §2 로 승급.
