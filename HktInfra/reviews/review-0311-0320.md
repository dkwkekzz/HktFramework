# review 0311–0320 — host 프로세스 부하 균형 + 다운스트림 AOI 뷰 산출(#9 잔여 심화 · #9 후속)

> 범위 0311~0320. 인과 서사(무슨 서버·어떤 기능·왜/어떻게/했나)는 [progress/5-coordination.md](progress/5-coordination.md)(오케스트레이터 기능 12)·[progress/2-world.md](progress/2-world.md)(존 기능 8). 이 문서는 *정당성 감사*만 — 실물 코드(`src/*.js`)·`run.js`/inline 재현·grep.

## §1 척추 5불변 정합성 (SPINE §5)

- **① 신성한 tick** ✅ — 부하 균형 결정(`placeAutoE`/`placeRebalanceE`)은 코디네이션 제어 평면(`orch-control.js:24,40`·시뮬 tick 밖). 다운스트림 뷰는 런타임 존 `onTick`(시뮬 산출물)이 만들고, 포착은 *읽기 전용 질의*(`orch-views.js`·tick 무관). 재배치 루프 `_rebalanceByEntities`(`orch-hostproc.js:57`)는 동기 I/O·await 0.
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). `_rebalanceByEntities` 종료는 *gap strict 감소* 보장(`orch-hostproc.js:65` `Math.abs((maxL-e)-(minL+e)) < (maxL-minL)` 인 존만 이주 → 단조)·tie-break 후보 배열 순서(결정론). 런타임 존 시드=`fnv1a(zid)`(`topo-actors.js:70`·run 시드 무관) → 5 시드 동일 위치·동일 뷰.
- **③ 권위 단일 소유** ✅강화 — `placeAutoE`/`placeRebalanceE` 는 `_start`/`_migrate`(release+acquire 쌍·`orch-placement.js:18`)로만 가동/이주 → 공백/중복 0. 다중/동시 host 장애(0313·0315)는 죽은 host 의 존을 생존 host 로 재가동(`_hostDown`·`orch-placement.js:25`)·죽은 host 비움. `entityConserved`(보존 회계) 균형/장애 후에도 참(재현).
- **④ 은닉·단일 연결** ✅ — orch 가 `zone.js` 직접 import 0 hit(grep·EntityZone 은 `makeActor` 팩토리 주입). 부하 질의(`hostLoadSkew`·`hostEntitySkew`)는 orch 내부 컨테이너만 읽음. 다운스트림 뷰는 *게이트웨이 주소*로 발신(`zoneViewBuf` 의 `to==='gateway'`·재현 확인)·클라 직접 아님.
- **⑤ headless·원격 검증** ✅(🟡 인프로세스) — Node 순수·`UObject|CoreUObject|GameplayTags` src 0 hit. 모든 신규 기능 OFF→reg 0(spine ALL OK). 다운스트림 뷰는 버퍼링 싱크가 *전역 net 무오염*(버퍼 미읽으면 비트 동일)이라 prior 모드/baseline 영향 0. **🟡**: `zoneHosts`·뷰 버퍼는 여전히 orch 인프로세스(실 host.js OS 프로세스/소켓 아님·#57).

**메타(구현 품질)**: reg 0 규율 유지(매 step OFF→직전 비트 동일). 박스 유계: 0319~0322 가 뷰 질의를 `orch-zonebridge.js` 에 쌓아 30.6KB>30KB 트리거 → **0323 정리 분할**(`orch-views.js`·다음 묶음)로 해소 예정(이 묶음 말미 `orch-zonebridge.js` 일시 초과는 0323 에서 해소·#43 가드가 close 경고로 포착). `orchestrator.js` 29.3→29.7KB(생성자 필드 4개 추가·#58 watch 악화·<30KB).

## §2 검증 재현 (주장 아님 — 실행 인용)

- `node run.js spine` = **ALL OK**(전 시리즈 src 누적 회귀·현재 코드에 전 역사 불변 단언·exit 0).
- **grep 감사**: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · orch→`zone.js` 직접 import 0 · `wc -c src/*.js` >30KB 박스 0(최대 `orchestrator.js` 29.7KB).
- **inline 재현 5/5**(현재 `src/` 의 박스 메서드 직접 구동·시드 [42,7,1234,99,2026]):
  - 0311 `hostLoadSkew`: z1·z2·z3@A·z4@B → skew **2**, +`placeRebalance` → skew **1**(균형).
  - 0316 `placeAutoE`: z1@A(엔티티 5)·z2@B(0)·존 수 균형 → `placeAuto` z3@**hostA**(만원에 또), `placeAutoE` z3@**hostB**(한가) — entity 가중이 만원 host 회피.
  - 0318 `hostBalanced(2,2)`: z1·z2@A(ent 4)·z3@C(0) → bal **false**, +`placeRebalanceE` → bal **true**·`hostContainerCoherent` Y·`entityConserved` Y.
  - 0319/0320 다운스트림: z1@A·a1·a2 enter → `zoneViewFrames` **2**·`zoneVisibleIds(a1)`==[a1]·(a2)==[a2](반경 밖 → 자기만)·모든 frame `to`=='gateway'·산출 enter==`zoneVisibleIds`(AOI 정확).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #57 | zoneHosts=orch 인프로세스 논리 컨테이너(실 host.js OS 프로세스 아님) | ⑤ | 🔴 **유지·최상위 load-bearing** — 이 묶음은 그 *위에* 부하 균형(0311~0318)을 쌓았을 뿐 #57 미진전. 부하 균형·생애주기 로그(`hostLifecycle`·실 spawn/killHost *지점 씨앗*)는 실 프로세스의 *자리*만 표시 | **STATE §2 NEXT**(불변) → 게이트: zone 런타임·host inbox 를 실 `host.js` 프로세스/소켓·`cluster-run.js` 실 spawn 통합(끄면 현 인프로세스=회귀 0) |
| #60 | 다운스트림 뷰 *포착*까지 — 게이트웨이→실 클라 전파 미연결 | ②/⑤ | 🟡 **신규** — 0319~0320 은 host 산출 AOI 뷰를 버퍼링 싱크로 *잡고 검증*만. `gateway-msg.js:43~50` 은 `m.from.startsWith('zone')`+`bySession` 바인딩 있는 *실 클라* 만 forward → 브리지 합성 세션(`s:a1`·실 Client actor 0)은 전파 대상 없음. 다운스트림 절반의 *마지막 홉* 미배선 | **STATE §3 OPEN GAPS**(0321~0330 이 직렬화·무손실까지 이으나 실 클라 전파는 그 후) → 게이트: 게이트웨이가 포착 뷰를 실 클라로 forward + 실 클라 하니스(끄면 현 포착-only=회귀 0) |
| #58 | `orchestrator.js` 30KB 근접(생성자 필드 재성장) | 정리 | 🟡 **악화 후 완화 예정** — 이 묶음이 필드 4개(autoEPlacements·rebalanceEMoves·zoneHostLifecycle·hostLifecycleLog) 추가 29.3→29.7KB. 0323(다음 묶음)이 `orch-views.js` 분할로 뷰 질의를 뺌(코어 필드는 잔존) | **STATE §3 OPEN GAPS** → 게이트: 코어 필드 init 을 mixin init 헬퍼로(기능 0·reg 0) |
| #53 | 정합 술어 by-construction(분기 시나리오 부재) | 검증 | 🟡 **확장(부하·뷰 판)** — `hostBalanced`(0318)·AOI 정확성(0320·`zoneVisibleIds`==산출 enter 가 *같은* `zone.visibleFor` 비교라 부분 순환)도 같은 인프로세스 구조 정합 | **STATE §3 OPEN GAPS** → 게이트: #9 실 프로세스가 진짜 분기(소켓 지연·뷰 유실) 도입 시 술어 load-bearing 화 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | 검증 | 🟡 **확장(+10 모드)** — 이 묶음 bespoke 모드(hostloadskew~hostzoneaoi)도 spine 누적 키트 미승급(최신 step 1개만 spine·나머지 git per-commit) | **STATE §3 OPEN GAPS** → 게이트: 양성 의미 모드를 `engine/verify-kit.js` 로 승급(1 step 1개·reg 0) |

> **🎯 단계 평결(SKILL §3.6)**: **host 프로세스 부하 균형(0311~0318·승인된 심화) + 다운스트림 AOI 뷰 산출·포착(0319~0320·새 능력)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) → 2차 심화 허용. 0311~0318 은 0301~0310 host 컨테이너(load-bearing #57 의 논리 층)의 *운영 심화*(부하 인지 배치·재배치·failover), 0319~0320 은 SPINE §4 경로2 *월드 다운스트림*(예전 드롭하던 뷰)을 처음 세운 것 → **과심화 아님**. **단 최상위 load-bearing 은 여전히 #57(실 host.js OS 프로세스)** — 이 묶음은 그 위 심화라, 다음 권고는 부하 균형 더 파기가 아니라 **#57(실 host.js OS 프로세스 spawn·cluster-run.js 통합) + 진짜 비동기(#4)**. step-loop 이 원장을 읽어 STATE §2 로 승급.
