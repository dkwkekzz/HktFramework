# review-0291-0300 — #9 멀티프로세스 배선 arc

> 범위: 0291~0300(닫힘). 인과 서사·서버별 기능 리스트는 [progress/](progress/)(① 엣지 게이트웨이 기능4·⑤ 코디네이션 오케스트레이터 기능10). 이 문서는 *정당성 감사*만 — narrative 요약표 없음.
> 판정 권위 = 실물 코드(`../src/*.js`·`../engine/*.js`) + `node run.js spine` 재현 + grep. step 문서 PASS 는 주장.

한 줄: entity 데이터 평면을 orch 인프로세스 method 경로(0290)에서 *직렬화 전송 seam + 서비스 디스커버리 + 게이트웨이 직접 라우팅*으로 분리(브리지 핸들→실 host.js 소켓의 씨앗). orch=라우팅 결정 보유 → 게이트웨이=결정·orch=검증·집행.

---

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick** ✅ — 전송 seam·mailbox drain·디렉토리 push·직접 라우팅 전부 코디네이션/엣지(존 시뮬 tick 밖). mailbox drain 은 *런타임 존* onTick 경계(`orch-zonebridge.js` `_tickRuntimes`)이지 권위 존 tick 아님. seam 내 동기 I/O·DB·await 0(`_zoneDeliver` 는 순수 JSON round-trip).
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). frame 직렬화(`JSON.stringify`/`parse`)·라우팅(디렉토리 조회)·stale 판정 전부 op(시드 함수) 종속·순수. 같은 시드 같은 바이트(`zoneFrameBytes` 5/5 동일=546).
- **③ 권위 단일 소유** ✅ 강화 — 직접 라우팅 churn(migrate/rebalance/drain/hostdown) 아래 `entitiesSingleOwner` 유지(`gwdirsingle`·`gwdircap` 5/5). host 불일치 frame 은 *적용 전 거부*(`orch-control.js` zoneDeliver `if (p.host !== liveHost) { this.zoneDirStale++; return; }`) → 옛 host 이중 적용 0. 디렉토리는 zone 당 단일 host(bijection·`gwdirbij`).
- **④ 은닉·단일 연결** ✅ 강화 — 게이트웨이는 orch 내부를 *모른다*: 위치를 명시 `zoneLoc` 메시지로만 학습(`gateway-msg.js` orch 블록)·자기 `zoneDir` 로만 해소. orch→`zone.js` 직접 import 0 hit(grep — `orchestrator.js`는 `orch-zonebridge.js` 믹스인만 require·EntityZone 은 `makeActor` 팩토리 주입). 라우팅 결정이 게이트웨이로 이동 = SPINE §4 경로(게이트웨이가 직접 중계) 정합 강화.
- **⑤ headless·원격 검증** 🟡→✅(설계 진전) — 전 데이터 평면이 *직렬화 가능 frame*(`zoneDeliver`·`zoneLoc`)으로 흐르고(`zoneFrameBytes`>0 이 와이어 증거), 게이트웨이가 host 로 라우팅(소켓 라우팅 동형)·mailbox 가 수신 버퍼·디렉토리가 서비스 디스커버리 — **실 host.js 프로세스 분리의 완전한 씨앗**. 단 아직 인프로세스(아래 #9 잔여).

**메타(구현 품질)**: 매 step OFF 플래그(`zoneHostHandle`/`zoneHostMailbox`/`gatewayZoneDir`/`gatewayDirectZone`) → 직전 step 비트 동일(reg 0·10/10). 박스 유계: 최대 `orchestrator.js` 27.0KB ≤30KB(>30KB 0). 0298~0300 은 읽기 질의만(`entityDirectCoherent`/`zoneDirSnapshot`/`directFlowCoherent`·동작 무변경).

---

## §2 검증 재현 (실행·인용)

- `node run.js spine` → **ALL OK**(reg + 전 누적 가설 모드 + `gwdircap` — 전 역사 불변이 현재 코드에서 0 깨짐).
- `node src/verify.js gwdircap` → 5/5 `directFlowCoherent` Y·`entityConserved` Y·total 1·ledger 5/1/2/1·dir==running Y·routes 7==applied 7·stale 0·miss 0(0290 destructive+graceful 혼합 lifecycle 을 게이트웨이 직접 라우팅만으로).
- grep 감사: `Math.random` src **0 hit** · `UObject|CoreUObject|GameplayTags` src **0 hit** · `require.*zone` in `orch*.js` → `orch-zonebridge.js`(믹스인)만, `zone.js` 직접 0 · `wc -c src/*.js` >30KB **0행**(최대 orchestrator 27.0KB).
- 단계 판정(SKILL §3.6): progress 맵 ⬜/🌱 박스 **0**(0210~ 유지) — 너비 1차 완료 위 *승인된 심화*(0271~0290 verdict 가 연속 지목한 최상위 load-bearing 게이트) → **과심화 아님**.

---

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #9 | 멀티프로세스 패리티(브리지=orch 인프로세스 핸들·실 host.js 소켓 0) | ③/⑤ | 🟡 **대폭 진전(재정의)** — *게이트웨이→실 존 직접 라우팅·서비스 디스커버리·전송 seam·host mailbox* ✅(0291~0300). **잔여 축소: 실 host.js *물리* 프로세스/소켓 분리만**(zone-host 핸들=orch 인프로세스 `zoneRuntimes` Map·게이트웨이 host 태깅→orch 적용·`cluster-run.js` 실 spawn 미통합) | **목적지 STATE §2 NEXT**(최상위 load-bearing) → 게이트: 존 런타임을 `host.js` 실 프로세스로 분리·게이트웨이 zoneDeliver 를 실 소켓으로(끄면 현 인프로세스 레지스트리=회귀 0) |
| #53 | 정합-0 by-construction(분기 시나리오 부재) | 검증 | 🟡 **확장(#9 판)** — `directFlowCoherent`·dir bijection 도 structural: 게이트웨이·orch 가 같은 인프로세스·이주마다 `_pubZoneLoc` 동시 push→실제 stale 창은 1-tick 지연뿐·`zoneStaleProbe`(0296)만 *합성* stale frame 으로 거부 경로 발화 | **목적지 STATE §3 OPEN GAPS** → 게이트: #9 실 프로세스가 진짜 분기(소켓 지연·디렉토리 비동기 lag·적용 실패) 도입 시 정합 술어 load-bearing 화(끄면 구조적 0=회귀 0) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🟡 **확장** — 이번 묶음 9 모드(`zonehandle`·`zonembox`·`gwzonedir`·`gwdirenter`·`gwdirmove`·`gwdircoh`·`gwdirdown`·`gwdirsingle`·`gwdirbij`)도 미승급(현 spine #9 모드=`gwdircap` 만·0291~0299 는 git per-commit·단일 src 모델상 verify.js 교체로 재실행 불가) | **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 안정 호를 묶음별 스모크 1~2 모드로 점진 승급(끄면 현 spine=회귀 0) |
| #41 | 테스트 seam 이 production wiring(topo-build/inject)에 위치 | ④/정리 | 🟡 **확장(+zoneStaleProbe)** — 합성 stale frame seam `zoneStaleProbe`(0296)가 `topo-inject.js`(production 주입열)에 gated(미제공=무효·reg 0)로 추가 | **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 전용 테스트 주입 경로 분리(끄면 현 거동=회귀 0) |
| #54 | 캐시 집중 편중(평결) | 단계 | ✅ **해소(자연)** — #9 가 세 라운드 연속 보류되던 최상위 load-bearing 을 0291~0300 으로 집행(0271~0290 verdict 가 지목한 방향대로) → "편중" 평결의 우선순위 권고가 실현됨 | 인덱스서 제거(기록만) |

> 그 외 유지(미해소·이번 묶음 미접촉): #4(broker lockstep·진짜 비동기)·#50·#46·#48·#44·#39·#55·#37·#38·#36·#3·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#1(⛔범위밖). 다음 묶음 리뷰가 재점검.
