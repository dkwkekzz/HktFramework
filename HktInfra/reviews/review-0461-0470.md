# review 0461–0470 — #4 완전 async 전환: 다중 존 이주 하 유계 resync

> 범위 + 인과 서사는 **[progress/5-coordination.md](progress/5-coordination.md)**(비동기 실행 substrate #4·기능 10). 이 문서는 *정당성 감사*만 — narrative 요약표 없음.
> 대상: `async-barrier.js` wrap-aware interior 유계 resync 가드(0462~) + verify 모드 mzdiverge~mze2ecap. 판정 권위 = 실물 코드(`../src/async-barrier.js`) + `node run.js` 재현 + grep.

---

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick — ✅**. barrier stepper 는 월드 입력 *배달*만 한다(`async-barrier.js:59` step). interior 판정은 소유 존을 *읽기*(peek)만 — `ownerZone`(`:68`)이 `a.ents.has(av)`·`interior`(`:70`)가 `e.x`·`z.region` 을 읽을 뿐 존 상태 쓰기 0. tick 안 동기 I/O·트랜잭션 0.
- **② 결정론 코어 — ✅**. 섭동(loss/delay)은 시드 PRNG(`lrnd`/`drnd`·`common.mulberry32`)만·`Math.random` src 0 hit(주석 제외). interior 게이트는 loss/delay roll 앞 단락 평가(`:89`·`:95`)라 non-interior move 는 rng 미소비 → ON 경로 재현성 보존. 핵심 명제 `worldDigest(run{asyncBarrier}) == worldDigest(run{})` 를 5 시드 5/5 로 단언(`mze2ecap` world Y).
- **③ 권위 단일 소유 — ✅**. 이주 release+acquire 쌍 거래(zone.js)는 불변·barrier 는 관여 안 함. exactly-once(moveDup0·lost0·pendingAtEnd0·`mze2ecap` once Y)로 move 유실·중복 0 → 권위 상태(엔티티 위치) 정확히 한 번 반영. `deferredAcrossHandoff`==0(`:78`·이주 관측 시 미결 deferred move 0)으로 이주 경계에 걸친 미결 쓰기 0.
- **④ 은닉·단일 연결 — ✅**. barrier 는 전송 substrate 내부(net.actors·존 상태 읽기)·클라는 게이트웨이 뷰만 소비(다운스트림 수렴 `mze2ecap` view Y). orch→zone.js 직접 import 0 hit.
- **⑤ headless·원격 검증 — ✅**. UObject/UE 모듈 src 0 hit·`node run.js`/`spine` ALL OK(헤드리스). barrier ON 경로만 변경·OFF→net.step 비트 동일(reg 구조적 0).

**메타(구현 품질)**: 회귀 0 규율 — `opts.asyncBarrier` OFF→`topo-run.js:88` ab 미생성·net.step() 경로·baseline 비트 동일(`reg` ALL OK). 박스 유계 — `async-barrier.js` 11.3KB(≤30KB 예산·8.4KB→11.3KB 회계 추가분). 정직성 — 완전-ON 불가(lockstep 등가 아님) 한계를 step-0461·0470 문서·progress 에 명시(숨기지 않음).

**긴장(이슈 승격)**: interior/ownerZone peek 은 barrier 가 존 ents/region 을 *직접* 읽음(실 분산에선 세션→존 디렉토리가 라우팅 제공·더미 in-proc 근사) → #53 확장(by-construction·단 handoff 관측 `deferredAcrossHandoff`·가드 OFF 발산 대조는 *진짜 분기*라 순수 by-construction 아님). 완전-ON 이 lockstep 등가 불가 → #73 신규(별 정합 기준 필요·경미).

---

## §2 검증 재현 (실행 출력 인용)

- **`node run.js`(현재 verify=mze2ecap) + `reg`**: `mze2ecap` 5/5 OK — seed별 world Y·exactly-once Y·뷰수렴 Y·유계 span3<4·deferredAcrossHandoff a0·handoffs 4/15/14/7/7. `reg` ALL OK(asyncBarrier OFF→net.step 비트 동일). `결과: ALL OK`.
- **가드 load-bearing 인라인 재현(mzguardctl 등가)**: 5 시드 전부 `가드ON 수렴=true · 가드OFF(mzGuard:false) 발산=true` — interior 유계 resync 가드가 load-bearing(없으면 발산·있으면 수렴). 발산은 0461 negative control(`mzdiverge`)과 동일 원인(이주 경계 move-drop 집합 변동).
- **`node run.js spine`**: `결과: ALL OK`(전 시리즈 누적 회귀 — 전 역사 불변을 현재 코드에 단언·FAIL 0).
- **grep 감사**: `Math.random` src 0 hit(주석 제외)·`UObject|CoreUObject|GameplayTags` src 0 hit·`orch→zone.js` 직접 import 0 hit.
- **코드 알리바이**: `ownerZone`(`async-barrier.js:68`)·`interior`(`:70`)·`horizon`(`:67`)·loss 게이트(`:89`)·delay 게이트(`:95`)·deferSpan 회계(`:90`·`:96`)·`deferredAcrossHandoff`(`:78`)·`pendingMoves`(`:42`·set `:90`/`:96`·delete `:99`)·stats(`:105`)·gating(`topo-run.js:88`).

---

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #72 | 다중 존 이주 하 손실/지연 섭동 미검증(유계 resync 필요) | ②/⑤ | ✅ **해소(0461~0470)** | wrap-aware interior 유계 resync 가드로 다중 존 loss+delay world/뷰==lockstep·exactly-once·deferredAcrossHandoff0(`mze2ecap` 5/5). 원장서 제거·기록만 |
| #4 | broker lockstep = 강제 동기(진짜 비동기 미해제) | ① | 🟡 **대폭 진전(다중 존 이주 유계 resync ✅·잔여=완전-ON·downstream 재접속)** | 목적지 STATE §2/§3 → 게이트: 완전-ON(경계 포함)은 별 정합 기준(#73)·끄면 현 gated OFF=회귀 0 |
| #73 | 완전-ON(경계 포함 async)은 lockstep 등가 불가 | ①/② | 🟡 **신규(경미)** — interior 가드는 경계 근처 move 를 즉시 in-order 배달(async 안 됨)·lockstep 자체가 이주 경계 move-drop 을 타이밍 의존적으로 함(0461 발견) → "완전 async" 는 lockstep 아닌 별 정합 기준 필요 | 목적지 STATE §3 OPEN GAPS → 게이트: 경계 move 도 async 흡수 시 새 정합 술어(수렴/보존 기반·끄면 현 interior 유계=회귀 0) |
| #70 | UpClient/DownClient 가 in-proc 액터(실 host.js OS 프로세스 아님) | ⑤ | 🟡 유지(#57 family·업스트림 짝·다음 큰 load-bearing) | 목적지 STATE §2 NEXT(무르익음) → 게이트: 실 cluster 업스트림/다운스트림 RPC(끄면 in-proc=회귀 0) |
| #53 | 정합-0 가 by-construction | 검증 | 🟡 **확장(0461~0470)** — interior/ownerZone peek 은 barrier 가 존 상태 직접 읽음(in-proc)·단 `deferredAcrossHandoff`(실 handoff 메시지 관측)·가드 OFF 발산 대조는 진짜 분기 | 목적지 STATE §3 → 게이트: 실 프로세스가 진짜 라우팅 lag 도입 시 load-bearing 화(끄면 구조적=회귀 0) |
| #71 | async substrate 실 net.transport 미배선 | ⑤/검증 | 🟡 유지(#72 해소로 다중 존 이주까지 검증·실 net.transport loss 는 여전 합성 드라이버) | 목적지 STATE §3 → 게이트: 실 net.transport loss 를 배리어에 흘려 진짜 분기(끄면 합성=회귀 0) |
| #16 | bespoke 모드 verify-kit 미승격 | ②/정리 | 🟡 유지·확장(+mzdiverge~mze2ecap 10 모드·현 spine=mze2ecap 1개·나머지 git) | 목적지 STATE §3 → 게이트: 안정 호 스모크 1~2 모드 점진 승급(끄면 현 spine=회귀 0) |
| #68·#69 | orch zoneHost entity 컨테이너 동기·동치 transitive | ③/검증 | 🟡 유지(경미·미접촉) | 목적지 STATE §3 OPEN GAPS |

**🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *직전 verdict 가 지목한 load-bearing #4/#72* → **과심화 아님**(코디네이션 substrate 기능 10·기존 박스 심화·신규 능력=다중 존 이주 하 유계 resync). **#72 ✅해소·#4 대폭 진전**. **다음 권고: 실 host.js child 경계 업스트림(#70·#57 짝) — 양방향/다중 존 in-proc 결정론이 모두 섰으니 남은 최상위 load-bearing 은 실 OS 프로세스 경계. 그 다음 경미 정리(#68·#69·#73·#16 승급).** step-loop 이 STATE §2 로 승급(review 는 기록만).
