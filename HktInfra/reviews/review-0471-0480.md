# review 0471–0480 — #70 실 host.js child 경계 업스트림

> 범위 + 인과 서사는 **[progress/5-coordination.md](progress/5-coordination.md)**(오케스트레이터/cluster-hostdriver 기능 22). 이 문서는 *정당성 감사*만 — narrative 요약표 없음.
> 대상: `cluster-hostdriver.js` 업스트림 seam(`intentToZoneMsg`·`deliverIntent`·`feedViews`·`upstreamAuthSig`·`driveUpstream`·`zoneEntity`) + verify 모드 upclenter~upce2ecap. 판정 권위 = 실물 코드 + `node run.js` 재현 + 실 child_process spawn + grep.

---

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick — ✅**. intent 배달·뷰 되먹임은 존 tick *밖*(별 프로세스). `deliverIntent`(`cluster-hostdriver.js:127`)는 소켓 rpc·`feedViews`(`:136`)는 부모 프로세스 클라 onMsg — 실 host.js 존의 onTick(pending move 적용)은 자기 프로세스에서만. 존 tick 안 동기 I/O 0.
- **② 결정론 코어 — ✅**. 경계 넘어 UpClient.seenSig == 실 존 authSig(`upcconverge`/`upce2ecap`)·enter rng+move 가산 재현. `Math.random` src 0 hit(주석 제외). 손실 하에서도 결정론 재전송(`_dropHash` fnv1a·시드)으로 재현.
- **③ 권위 단일 소유 — ✅**. 실 host.js 존이 entity 유일 소유(`upcaccount`·enterPos+Σmove==실 존 pos·발신 손실 0). 손실 하 exactly-once — rpc 재전송+host reqId 멱등(`host.js` replyCache)으로 재적용 0(`upclossy`·dupCmds>0 인데 회계 정확). leave 로 release(`upcleave`·존 size 0).
- **④ 은닉·단일 연결 — ✅**. UpClient 는 게이트웨이-형 intent(zoneEnter/Move/Leave)만 발신(`client.js:298`·`net.send(addr,'gateway',op)`)·자기 세션 뷰만 수신(`feedViews` sessionId 필터 `:138`). seam 이 게이트웨이 번역을 재현(존은 게이트웨이-form 만 봄). cluster-hostdriver→zone.js 직접 import 0 hit.
- **⑤ headless·원격 검증 — ✅ 강화**. 실 `child_process` host.js spawn(livePid≠부모·smoke 6684≠6664)·소켓(길이-프리픽스 프레이밍)으로 통신·UObject/UE src 0 hit. 다운스트림(#57)에 이어 업스트림도 실 OS 프로세스 경계 — SPINE §5 ⑤(실 OS 프로세스/소켓)를 업스트림까지 만족.

**메타(구현 품질)**: 회귀 0 — 업스트림 seam 은 `clusterDriverReal` OFF(`topo-build.js:187` 기본 false)·verify 모드가 직접 호출(run() 무관)→run() 비트 동일(`reg` ALL OK). 박스 유계 — `cluster-hostdriver.js` 17.3KB(≤30KB). 정직성 — 실 GW 프로세스 아님 한계를 step-0480 문서·progress 에 명시.

**긴장(이슈 승격)**: 업스트림 seam 이 실 게이트웨이 *프로세스*가 아니라 드라이버 seam·부모 프로세스(클라·게이트웨이 부모·존만 자식)→#74 신규(경미). 경계 넘어 수렴은 실 소켓 왕복·실 프로세스 상태(snapshot)를 읽으므로 #53(by-construction) 이 여기선 *강화*(실 분기·합성 아님).

---

## §2 검증 재현 (실행 출력 인용)

- **`node run.js`(현재 verify=upce2ecap) + `reg`**: `upce2ecap` 5/5 OK — seed별 uc0 수렴 Y·회계 Y·exactly-once(resends 18/17/6/13/15·dup 10/7/0/9/8)·uc1 leave 제거 Y. `reg` ALL OK(clusterDriverReal OFF→run() 비트 동일). `결과: ALL OK`.
- **실 child_process 증거**: 실 host.js child pid **6684** ≠ 부모 **6664**·경계 넘어 uc.seenSig `a1@1,6` == authSig `a1@1,6`(별 프로세스 왕복 후 수렴).
- **`node run.js spine`**: `결과: ALL OK`(FAIL 0·전 역사 불변 현재 코드에 단언).
- **grep 감사**: `Math.random` src 0 hit(주석 제외)·`UObject|CoreUObject|GameplayTags` src 0 hit·`orch/cluster-hostdriver → zone.js` 직접 import 0 hit.
- **코드 알리바이**: `intentToZoneMsg`(`cluster-hostdriver.js:120`·enter/move/leave 번역)·`deliverIntent`(`:127`)·`feedViews`(`:136`·sessionId 필터)·`upstreamAuthSig`(`:148`)·`zoneEntity`(`:154`)·`driveUpstream`(`:162`)·gating(`topo-build.js:187` clusterDriverReal=false).

---

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #70 | UpClient/DownClient 가 in-proc 액터(실 host.js OS 프로세스/소켓 아님) | ⑤ | ✅ **해소(0471~0480)** | 실 UpClient intent 가 소켓 넘어 실 host.js 존→egress 뷰 되먹임·경계 넘어 desync0·손실 exactly-once·생애주기(`upce2ecap` 5/5·실 child pid≠부모). 다운스트림 실 DownClient(0342~50)와 짝 완성. 원장서 제거·기록만 |
| #74 | 업스트림 seam 이 실 게이트웨이 *프로세스* 아님(드라이버 seam·부모) | ④/⑤ | 🟡 **신규(경미)** — `intentToZoneMsg`(`cluster-hostdriver.js:120`) 가 게이트웨이 번역을 재현·클라·게이트웨이는 부모 프로세스(존만 자식). 번역/라우팅 계약은 게이트웨이 동일·실 GW 프로세스 분리 미수행 | 목적지 STATE §3 OPEN GAPS → 게이트: 실 host.js 게이트웨이 프로세스에 seam 이식(끄면 현 드라이버 seam=회귀 0) |
| #61 | 업스트림 intent 실 클라(SPINE §4 경로1) | ②/⑤ | ✅ **해소(in-proc 0421~30 + 실 경계 0471~80)** — #70 로 실 OS 프로세스 경계까지 완결. 원장서 제거 |
| #53 | 정합-0 가 by-construction | 검증 | 🟡 **강화(0471~0480)** — 경계 넘어 수렴은 실 소켓 왕복·실 프로세스 snapshot 을 읽으므로 합성 아님(진짜 분기)·손실 wire 도 진짜 유실 | 목적지 STATE §3 → 게이트: 유지(실 프로세스 분기가 load-bearing) |
| #73 | 완전-ON(경계 포함 async) lockstep 등가 불가 | ①/② | 🟡 유지(경미·미접촉) | 목적지 STATE §3 OPEN GAPS |
| #16 | bespoke 모드 verify-kit 미승격 | ②/정리 | 🟡 유지·확장(+upclenter~upce2ecap 10 모드·현 spine=upce2ecap 1개·나머지 git) | 목적지 STATE §3 → 게이트: 안정 호 스모크 1~2 모드 승급(끄면 현 spine=회귀 0) |
| #68·#69 | orch zoneHost entity 컨테이너 동기·동치 transitive | ③/검증 | 🟡 유지(경미·미접촉) | 목적지 STATE §3 OPEN GAPS |

**🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *직전 verdict 가 지목한 load-bearing #70* → **과심화 아님**(코디네이션 cluster-hostdriver 기능 22·기존 박스 심화·신규 능력=실 host.js child 경계 업스트림). **#70·#61 ✅해소** — DownClient(#57 다운스트림)·UpClient(#70 업스트림) 둘 다 실 OS 프로세스 경계 확보. **다음 권고: 실 게이트웨이 프로세스 분리(#74·seam→실 GW·업스트림/다운스트림 경로의 마지막 인프로세스 잔재) 또는 경미 정리(#68·#69·#73·#16 승급).** step-loop 이 STATE §2 로 승급(review 는 기록만).
