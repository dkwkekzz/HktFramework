# review-0131–0140 — 거래소↔가방 saga liveness arc (유계화·자율 복구·관측·정합 capstone)

> 범위: 0131~0140(닫힘). 인과 서사·서버별 기능 리스트는 [progress/3-services.md](progress/3-services.md)(거래소·가방 박스). 이 문서는 *정당성 감사*만(척추 5불변 재현 + 이슈→후속). 점적 기록은 `../step-NNNN.md`·STATE §7.
>
> 한 줄: 0130 이 saga *회계*(물리·회계·교차 3정합층)를 닫은 위에, 이 묶음은 **회신이 영구 손실될 때의 liveness**(유계 재시도→포기→재admission→자동 트리거→2단 유계 종결→수명주기 발행→liveness 정합 capstone)를 한 호로 세운다. 전 step OFF-게이트(reg 0). 정리 1건(0133 topo-subs 분할).

---

## §1 척추 5불변 판정 (실물 코드 알리바이 — 현재 `src/`)

| # | 불변 | 판정 | 근거(코드 알리바이 + 비판) |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 거래소·가방 둘 다 onTick 0(순수 반응형). 재시도/포기/재admission/공지는 외부 op(`exchSweep`·`exchReadmit`·`announceUp`) 또는 구독 ev(`svc.inventory.up`) 반응 — 존 tick 밖. grep `await\|fs\.\|\.connect(` on 4 박스 = **0 hit**. |
| ② | 결정론 코어 | ✅(🟡#29) | 상한·카운트 전부 정수/단조 gid 비교(`svc-exchange-core.js:101-114` `_resendPending`·`readmitCount`). grep `Math.random` src/ = **0 hit**. 잔여 긴장: epoch/클럭 단일성(#29·이 묶음 무접촉). |
| ③ | 권위 단일 소유 | ✅ **강화(liveness 분할)** | 포기/종결도 *abort 아님* — give 가 실제 성공했을 수 있어(dedup→escrow 소유) 낙관적 open 유지 → 4체제 모두 `open==escrow`(exsagalive 안전열). 미해결 give 는 정확히 한 liveness 상태(`sagaLiveConsistent` `:186` `pending==pendingGive+abandonedGive+permFailed`·공백·중복 0). 권위 이동/이중쓰기 0. |
| ④ | 은닉·단일 연결 | ✅ | 0136 autoReadmit: 거래소가 가방 회복을 *직접 안 보고* 구독 토픽(`svc.inventory.up`)으로만 반응(`svc-exchange-txn.js:41`). 0139 발행자: 가방→버스→거래소(`svc-inventory-txn.js:15`·발행→버스→구독 실 체인·발행자 무수정). 클라 와이어 무관(전부 서버간 제어 평면). |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | 전 step `node run.js`(4기둥)+`spine`(누적 회귀) ALL OK. grep `UObject\|CoreUObject\|GameplayTags` = **0 hit**. 잔여: saga liveness 전 경로 *인프로세스 전용*(host.js saga/escrow/invUp = **0**·#9 확장). |

**메타(구현 품질)**: ⒜ **회귀 0 규율** — 매 step 새 플래그/op OFF→baseline 비트 동일(sagaMaxRetries 0·readmitMax 0·*Publish false·autoReadmit false·announceUp 부재). spine 사슬이 이를 현재 코드에 단언(§2). ⒝ **헬퍼 추출** — 0131 `_resendPending`·0134/0136 `_readmit` 가 수동 op·자동 트리거·재전송 3경로를 한 곳으로(중복 0·byte 동치로 reg 0). ⒞ **정리 0133** — topo-build 33.1→25.5KB(#34 절반 해소·§2). ⒟ **0139 절차 사고** — new-step.js 미실행으로 baseline 이 0137 스냅샷이던 것을 발견·`step-0138` 태그서 박스 복원해 baseline=0138 정정 후 닫음(reg 무영향·모든 변경 OFF-게이트). 절차 게이트(close-step)가 막지 못한 누락 — #36.

---

## §2 검증 재현 (실행 출력 인용 — 주장 아님)

- **`node run.js spine`(src 누적 회귀·전 역사 불변을 현재 코드에 단언)** → **ALL OK**. "0001~0140 어느 불변도 현재 코드에서 안 깨졌다"의 증거. saga 60+ 가설 모드는 각 step close 1회 단언이지만(#16) reg-0 사슬은 전수 보증.
- **`node run.js`(현재 step 0140)** → **ALL OK**. capstone `exsagalive` 4체제 분할: 정상 `0/0/0/0`·포기 `1/0/1/0`·재admission 회복 `0/0/0/0`·영구실패 `1/0/0/1` — 모두 `pending==pg+ab+pf`·sagaLiveConsistent 예(4/4)·open==escrow.
- **grep 5불변 감사** (src/): `Math.random` 0 · 4 박스 `await\|fs\.\|.connect(` 0 · `UObject\|CoreUObject\|GameplayTags` 0.
- **#9 알리바이**: `grep -c "exchange|saga|escrow|invUp" src/host.js` = **0**(saga liveness 주입은 `topology.js` run 루프 seam `exchangeOps`/`invOps` 만·멀티프로세스 미배선).
- **#34 알리바이**: `topo-build.js` 26052B(25.5KB ✅·0133 분할)·`topo-subs.js` 10793B(10.5KB)·`topology.js` 31549B(**30.8KB 🟡 잔여**).
- **#33 알리바이**: STATE 30KB 예산이 *이번 묶음 10/10 step* 옛 §7 행 압축을 강제(매 close-step `✗ STATE.md` → 트림 반복 실측).

---

## §3 이슈 → 후속 step 연결 (전파 고리·불가침)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #35 | saga 자동 재전송 무차별·영구 손실 무한 재시도 | 서비스 | ✅ **해소** | **0131** sagaMaxRetries(재전송 상한·0059 의 saga 판) + **0137** readmitMax(2단 유계·영구 실패) + **0129** §9 의 무차별 재전송은 잔존(나이 무관 전 pending 재전송·경미)→#37 로 분리 |
| #34 | wiring 박스 30KB 초과 | 정리 | 🟡 **절반 해소** | (a) topo-build ✅ **0133**(구독 테이블→`topo-subs.js`·33.1→25.5KB·기능 0·reg 0). (b) **잔여 topology 30.8KB** → STATE §3 OPEN GAPS·게이트: run() 또는 액터 구성 추출(정리 step·기능 0·끄면 회귀 0) |
| #37 | 자동 재전송 나이 무관 무차별(대역) | 서비스 | 🟡 신규(경미·#35 잔여) | `_resendPending`(`svc-exchange-core.js:101`)·`exchSweep` autoRetry 가 매 sweep 에 *모든* pendingGive 재전송(마지막-시도-시각 무관). → STATE §3·게이트: per-give last-sent + backoff(끄면 현 무차별=회귀 0) |
| #38 | saga liveness 자동 트리거(announceUp) 회복 시점 수동 seam | ⑤/서비스 | 🟡 신규(#9 family) | 0139 announceUp 은 *외부 op* — 가방 reconstruct 직후 *자동* 발행 아님(`svc-inventory-txn.js:15`). → STATE §3·게이트: 클러스터 reconstruct hook→자동 svc.inventory.up(멀티프로세스 #9 무대·끄면 수동 seam=회귀 0) |
| #36 | new-step 미실행→baseline 표류(close-step 미탐지) | 정리/도구 | 🟡 신규(경미·도구) | 0139 가 new-step.js 없이 진행돼 baseline 이 직전-2 step 스냅샷이던 것을 수동 정정. close-step 이 baseline==직전 src 를 검증 안 함. → STATE §3 OPEN GAPS·게이트: close-step 에 baseline-freshness 체크(가독/안전만·회귀 무관) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 **확장**(+saga liveness 10 모드·총 ~70) | 0131~0140 의 exsagamax/aband/read/readpub/autoread/permfail/failpub/invup/live ON-의미는 close 1회만 단언. → STATE §3·게이트: 안정 호(saga liveness) verify-kit 승격(스모크 1~2 모드·끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장** | +0131~0140 saga liveness(상한·재admission·자동 트리거·발행·announceUp) 전부 host.js 0(인프로세스 전용·위 알리바이). → STATE §3·게이트: 멀티프로세스 saga liveness 배선 |
| #33 | STATE §7 INDEX 30KB 천장 | 정리/도구 | 🟡 **유지(10/10 발현)** | 140행 INDEX·이번 묶음 매 step 트림 강제(실측). → STATE §3 OPEN GAPS·게이트: INDEX 묶음당 1행 압축(전문 reviews/) 또는 예산 상향(가독성만·회귀 무관) |

**유지(이 묶음 무접촉)**: #1·#3(AOI)·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32. 전부 직전 원장대로(목적지·게이트 불변).

> **arc 정합**: 이 묶음은 SPINE §6 계층3(게임 서비스) 거래소↔가방 saga 를 *liveness* 차원에서 닫았다(0121~0130 회계 차원에 이어). 네 정합층(물리 0120·회계 0128·교차 0130·liveness 0140) 완성. 신규 이슈(#36·#37·#38)는 전부 경미/정리·백로그 — 6계층 우선순위(다음 큰 걸음: 우편/길드 박스·#30b 분산 영속·#9 멀티프로세스)에 끼어들지 않는다.
