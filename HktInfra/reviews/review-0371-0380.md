# review 0371–0380 — #62 runMulti 코어 통합: broker 측 제어 평면 상주(ClusterCoordinator)

> 범위: step-0371~0380(닫힘). 인과 서사·기능 분해는 [progress/5-coordination.md](progress/5-coordination.md) 오케스트레이터 서버 기능 17. 이 문서는 *정당성 감사*(척추 5불변·재현·이슈)만.

## §1 척추 정합성 (SPINE §5 5불변)

새 박스 `src/cluster-coord.js`(`ClusterCoordinator`·10.9KB·require 0·주입된 orch/cluster/driver/specOf 로만 동작). 0361~0370 이 실 데이터 평면을 *verify 하니스가 ad-hoc 구동*(driveCluster 0368)했다면, 이 묶음은 그 구동을 **broker 측 제어 평면 상주 객체**로 옮겼다 — start·tick·연속 run 루프·매-tick desync 가드·상주 migrate/failover·syncPlan 비파괴 자가 치유·egress 집계·report·grand capstone.

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | `cluster-coord.js:54` run 루프·`:38` tick 은 실 host `zone.onTick` 만 구동(tickZone)·코디네이터 자신은 tick *밖* 제어 평면(rpc snapshot/deliver/tick) — 시뮬 tick 내 동기 I/O 0 |
| ② | 결정론 코어 | ✅ | `coordcap` 5/5 maxDesync 0(시드 5종 동일)·`Math.random` src 0 hit·plan.order 정렬 spawn |
| ③ | 권위 단일소유 | ✅(긴장) | `:65` migrate=driver.migrateZone(from zonedel **release** + to zoneadd+loadstate **acquire**)·in-proc orch SSOT·실 cluster 재현. **단 코디네이터 migrate/failover 가 orch 권위 placement(zoneHosts)를 갱신 안 함**→실 migrate 후 orch plan stale(아래 #65) |
| ④ | 은닉·단일연결 | ✅강함 | `cluster-coord.js` **require 0**(grep)·zone.js 직접 import 0·driver/cluster API(rpc·spawnOne·killHost)로만 — orch→zone.js import 0 hit |
| ⑤ | headless·원격검증 | ✅ | `coordcap` 가 `new Cluster([])`+driver 로 실 `host.js` child_process 구동·UObject/UE 모듈 src 0 hit |

**메타(구현 품질)**:
- **reg 0 — *구조적*(OFF 플래그 불필요)**: cluster-coord 는 run() 데이터 평면이 *결코 호출하지 않는* 새 박스라 reg 가 by-construction 0(net-core 진입점 등록은 export 만·`src/net-core.js:27`). 정직히 — reg 는 이 박스 정합에 *신호 0*. 검증은 bespoke 모드(coordstart~coordcap)에만 의존.
- **박스 유계**: cluster-coord.js 10.9KB·>30KB 박스 0(최대 orch-zonebridge 28.96KB).
- **#16 family**: 이 묶음 bespoke 10모드(coordstart~coordcap)도 미승급(verify.js 가 step 마다 1개로 교체·git per-commit)·현 spine 모드=최신 coordcap 1개 → **cluster-coord 회귀를 spine 이 못 잡는다**(아래 #16 확장).

## §2 검증 재현 (실행·인용)

- `node run.js 0380` (coordcap) → **ALL OK** 5/5: maxDesync 0·drift heal Y·coordCoherent Y·report coh Y(시드 42·7·1234·99·2026).
- `node run.js 0380 reg` → **ALL OK**(net.log+상태 0x… baseline 비트 동일·구조적).
- `node run.js spine` → **ALL OK**(전 시리즈 누적 회귀·E2E 14 프로세스 비트동일 true).
- grep 감사: `Math.random` src **0**·`UObject|CoreUObject|GameplayTags` src **0**·`orch→zone.js require` **0**·`cluster-coord.js require` **0**(순수 주입 의존)·>30KB 박스 **0**.
- 알리바이 라인: run 루프 가드 `cluster-coord.js:54-61`·migrate `:65`·failover `:71`·syncPlan(비파괴 차분) `:80-88`·report `:91`·coordCoherent `:103`.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #57 | zoneHosts/데이터평면 실 host.js·runMulti 통합 | ⑤ | 🟡 **거의 해소 심화**(0371~0380) — 실 데이터 평면(0361~70) 위에 *broker 측 제어 평면 상주 + 연속 tick 루프*(ClusterCoordinator) 입증. **잔여 좁아짐**: cluster-run.js 옛 lockstep runMulti 와 코드 합류(=#62) | STATE §2 NEXT(잔여=#62) → 게이트: 두 runner 합류(끄면 현 병존=회귀 0) |
| #62 | ClusterHostDriver↔cluster-run.js runMulti 미통합 | ⑤/검증 | 🟡 **거의 해소·재정의**(0371~0380) — *orch 상주 broker 측 제어 평면 + 연속 tick 루프*(start/tick/run/desync 가드/migrate/failover/syncPlan/egress/report/coordCoherent·`cluster-coord.js`)가 **새 박스로** 섰다(#62 의 *능력* 충족). **잔여**: 이 코디네이터와 옛 `cluster-run.js` runMulti(14-프로세스 lockstep)가 *두 별개 runner* — 코드 합류 미수행 | STATE §2 NEXT → 게이트: 코디네이터를 cluster-run.js runMulti 코어로 승격/합류(끄면 현 병존=회귀 0) |
| #65 | 코디네이터 migrate/failover 가 orch 권위 placement 미갱신(단방향) | ③/검증 | 🟡 **신규** — `migrate`/`failover`(`cluster-coord.js:65,71`)는 *실 cluster* 만 바꾸고 in-proc orch zoneHost(SSOT)는 그대로 → 실 migrate 후 `clusterDesync`(orch plan 기준)가 stale 로 발산(0379 발견·**grand capstone 시나리오가 migrate/failover 를 뺀 이유**·migrate 는 0375 가 realPos 직접 대조로 별도 검증) | STATE §2 NEXT(#62 동반) → 게이트: 코디네이터 lifecycle op 가 orch zoneHost 도 갱신(양방향 동기·끄면 현 단방향=회귀 0) |
| #63 | 실 failover 상태 소실(영속 미연동) | ⑤/데이터 | 🟡 **유지·재확인** — 0376 `failover`(`cluster-coord.js:71`)도 죽은 host 존을 *빈 인스턴스* 재가동·entity 소실(정직한 한계·0365 와 동형) | STATE §3 OPEN GAPS → 게이트: failover 가 월드 영속서 상태 복구(데이터 계층 묶음) |
| #64 | migrate in-flight frame 재라우팅 부재 | ⑤ | 🟡 **유지** — 코디네이터 migrate 도 정적 이전(snapshot→loadstate)·이주 직후 frame 재라우팅 미처리 | STATE §3 OPEN GAPS → 게이트: 이주 후 forward 또는 dir 갱신 |
| #53 | 정합-0 by-construction | 검증 | 🟡 **부분 진짜화 지속** — `coorddesync`(0374) ghost entity 주입이 desync 1 검출(진짜 refutation 분기)·`coordsync`(0377) zonedel drift→syncPlan 복원도 진짜 분기. **단** coordCoherent 의 maxDesync==0 은 *정적 수렴 루프*(첫 tick 후 신규 intent 0) 위 측정이라 정상 경로는 구조적 | STATE §3 OPEN GAPS → 게이트: 업스트림 실 intent(#61)·진짜 비동기(#4)가 동적 분기 도입 시 load-bearing |
| #16 | bespoke 모드 spine 미승급 | ②/⑤/정리 | 🟡 **확장** — coordstart~coordcap 10모드 미승급·spine 현 모드=coordcap 1개 → **cluster-coord 회귀를 spine 이 못 잡음**(reg 구조적 0 = 신호 0 와 겹쳐 *이 박스 안전망 얇음*) | STATE §3 OPEN GAPS → 게이트: 안정 후 coordcap 1모드 spine 승급(끄면 현 spine=회귀 0) |
| #61 | 업스트림 intent 실 클라 경로 미연결 | ②/⑤ | 🟡 유지 — 이 묶음 무접촉(코디네이터는 다운스트림/데이터 평면만) | STATE §3 OPEN GAPS(#57 동반 무르익음) |

> 그 외 유지(무접촉): #4·#50·#46·#48·#44·#39·#55·#37·#38·#36·#3·#6·#7·#8·#10·#11·#13·#17a·#25·#27·#28·#29·#30b·#32·#41·#1(⛔범위). #58'(zonebridge 28.96KB watch) 유지.

> **🎯 단계 평결 (0371-0380·SKILL §3.6)**: **#62 runMulti 코어 통합 — broker 측 제어 평면 상주(0371~0380·연속 4묶음 verdict 가 지목한 #57 잔여/#62 게이트 집행)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *연속 지목된 최상위 게이트의 다음 절* → **과심심화 아님**. 0361~0370 verdict("cluster-run.js runMulti 코어에 orch 상주 + 연속 tick 루프")를 그대로 집행 — verify ad-hoc 구동을 `ClusterCoordinator`(start·tick·연속 run·매-tick desync 가드·상주 migrate/failover·syncPlan 비파괴 자가 치유·egress 집계·report·coordCoherent capstone)로 상주화(오케스트레이터 기능 17). **재현 증거**: `coordcap` 5/5(maxDesync 0·drift→syncPlan 치유·coordCoherent·report coh)·`reg`/`spine` ALL OK·`Math.random`/UObject src 0·cluster-coord require 0(순수 주입)·orch→zone.js import 0·>30KB 박스 0(cluster-coord 10.9KB). **세 잔여(다음 권고)**: ⒜ **#62 코드 합류** — 코디네이터와 옛 cluster-run.js lockstep runMulti 두 runner 병존(능력은 섰으나 *합류* 미수행) ⒝ **#65 양방향 동기** — 코디네이터 migrate/failover 가 orch 권위 placement 미갱신(단방향·capstone 이 migrate/failover 제외) ⒞ reg 구조적 0 + bespoke 미승급(#16)으로 cluster-coord 안전망 얇음. **다음 권고: 코디네이터↔cluster-run.js runMulti 합류(#62) + 코디네이터 lifecycle↔orch zoneHost 양방향 동기(#65), 그 다음 업스트림 intent 실 클라(#61)·진짜 비동기(#4).** step-loop 이 STATE §2 로 승급.
