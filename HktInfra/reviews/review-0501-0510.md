# review 0501–0510 — #16 라운드 4차: 완전 saga liveness *손실 체제* 편입

> 범위: 0501~0510(우편 saga 를 실 회신 손실 하 구동해 liveness 3분할을 *비자명* 검증·verify-kit ORDER 편입·박스 무수정). 인과 서사·서버 기능 리스트는 [progress/3-services.md](progress/3-services.md)(우편 서버 기능 11). 이 문서는 정당성 감사만.

이 묶음은 0491~0500(서비스 saga capstone *행복 경로* 편입)이 원장에 남긴 **#76**(완전 saga liveness 미발현·회신 손실 주입 seam 부재)를 집행한 *검증 인프라 라운드*다. 박스 코드 0줄 수정 — `engine/verify-kit.js` 에 손실 체제 capstone 9종 + 등록 가드 1종을 편입(공용 하니스 `runMailLoss`).

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick ✅** — 우편 saga 는 존 tick 밖 비동기 서비스. 편입은 검증 하니스뿐(박스 무변). 알리바이: `engine/verify-kit.js:1408~1586`(신규 함수 10종)·`src/svc-mail-*.js` diff 0.
- **② 결정론 코어 ✅** — 손실 주입(`mailAckDropAlways`)·재전송·포기·재admission·영구실패 전 경로가 시드 PRNG 결정론. 5시드 전부 동일 수치(pg1·ab1·pf1). `Math.random` src 0 hit. 알리바이: `src/svc-mail-saga.js:28`(`_resendPending` 결정론 순회)·`src/svc-mail-core.js:103,109`(sagaConsistent·sagaLivenessConsistent).
- **③ 권위 단일 소유 ✅ 강화(비자명)** — 미해결 give 가 매 순간 정확히 한 상태(pendingGive=재전송중·abandonedGive=재admission대기·permFailed=영구종결)로 분할: `pending==pendingGive+abandonedGive+permFailed`. 포기/종결도 *abort 아님* — give 가 실제 성공했을 수 있어(가방 dedup→escrow 소유) 낙관적 pending 유지(아이템 이중적용 0). 알리바이: `svc-mail-core.js:109`(`sagaLivenessConsistent`)·`svc-mail-saga.js:34,37`(abandon/permFail 분기).
- **④ 은닉·단일 연결 ✅** — saga 수명주기 발행 삼종(abandon/readmit/fail)이 우편→버스→audit 토픽 pub/sub(발행자 무수정·audit 는 신규 소비자). give 는 `item_req` 로만(가방 원장 권위). 알리바이: `svc-mail-saga.js:39,44,59`(3종 발행)·`svc-audit.js:19`(seen 집계).
- **⑤ headless·원격 검증 ✅** — 전부 node in-proc `run()`·UObject/UE src 0 hit. `spine` ALL OK(손실 체제 10모드 상주).

**회귀 규율**: `engine/verify-kit.js` 는 `baseline/` 대상 아님(net-core require 박스만)·박스 무수정 → reg 0 *구조적*(매 step). `git status` clean·src/·STATE·step 문서 리뷰 중 무변.

## §2 검증 재현 (실행·인용)

- `node run.js 0510 reg` → **ALL OK**(net-core+박스 = baseline 비트 동일).
- `node run.js spine` → **ALL OK** — 손실 체제 9 capstone + `promotedsagaloss`(9/9 ORDER∧MODES) 매 spine 재검증.
- 대표 모드 5/5: `mailsagapermfail`(permFailed 1·pending 1·pending==pg+ab+perm)·`mailsaga3way`(pg1·ab1·pf1 동시 nonzero·grand)·`mailsagaabandonpub`/`mailsagareadmitpub`/`mailsagafailpub`(발행==사건==audit.seen 삼자 일치).
- **load-bearing 확증(by-construction 아님)**: 동일 ops 를 손실 seam *없이* 돌리면 permFailed 0·pending 0·acked==gives(3분할 자명 0,0,0). 손실 seam ON 이면 permFailed 1·pending 1 → 주입 손실이 3분할을 *비자명하게* 만드는 원인(#76 게이트 "현 정상 흐름=회귀 0" 정확히 실증).
- grep 감사: `Math.random` src 0·`UObject|CoreUObject|GameplayTags` src 0·>30KB 박스 0(최대 cluster-coord 29.3KB).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| 76 | 완전 saga liveness 미발현(회신 손실 주입) | 검증 | ✅ **해소(0501~0510·우편 saga)** | 원장서 제거 — `mailAckDrop`/`mailAckDropAlways` 손실 seam 으로 재전송→포기→재admission→영구실패 발현·3분할 동시 nonzero(`mailsaga3way` 5/5)·발행 삼종 audit E2E. **잔여(경미)=#77 거래소 측 손실 seam 미배선** |
| 16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | ✅ **완전 해소** | grand 9종(0481~90)+서비스 행복 9종(0491~0500)+**손실 체제 9종(0501~09)**+가드 3종 전부 HEAD ORDER 상주. #76 잔여까지 닫혀 #16 종결(잔여 sub-arc 수백 모드는 비-load-bearing 정리성) |
| 77 | 거래소 saga 손실 seam 미배선(우편만 손실 검증) | 검증 | 🟡 **신규(경미)** | 목적지 STATE §3 OPEN GAPS → 게이트: `svc-exchange` 회신 경로에 ackDrop/ackDropAlways seam 추가 후 거래소 abandon/permFailed 3분할 발현(끄면 현 우편만=회귀 0). 우편 saga 가 패턴 실증·거래소 machinery(`svc-exchange-core.js:74~110`) 동형이라 배선만 남음 |
| 78 | readmit→drain(loss cleared) 미검증(정적 seam mid-run 미해소) | 검증 | 🟡 **신규(경미)** | 목적지 STATE §3 OPEN GAPS → 0505 는 재admission *전이*(abandonedGive→pendingGive)만 단언·정적 손실 seam 은 mid-run 해소 불가라 재개 후 실 drain 미구성(drain 자체는 0501 transient drop-once 가 커버). 게이트: 시각/카운트 기반 해소 seam(끄면 현 전이-only=회귀 0) |
| 41 | 테스트 seam 이 production wiring 에 위치 | ④/정리 | 🟡 **유지·재확인** | 이 arc 가 `mailAckDrop`/`mailAckDropAlways`(0167/0173 도입·`topo-build.js`)를 *검증에서 처음 능동 활용* — seam 자체는 무변(gated·미제공=reg 0). 목적지·게이트 무변(전용 테스트 주입 경로 이동) |

**🎯 단계 평결 (0501-0510·SKILL §3.6)**: progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *직전 0491-0500 verdict 가 원장에 올린 #76* 집행 → **과심화 아님**(코박기 아닌 검증 하니스 강화·박스 무수정→reg 0). 서비스 capstone 이 행복 경로만 봐 (0,0,0) 자명 참이던 `sagaLivenessConsistent` 를 실 손실 하 비자명 증명(9 국면 분해 + 3분할 grand + 발행 E2E). **#76·#16 ✅해소**·부수 #77·#78 신규(경미). **다음 권고**: ⒜ #74 실 GW 프로세스 분리(마지막 인프로세스 잔재) ⒝ #46 금고↔가방 escrow(2차 서비스 심화) ⒞ #77 거래소 손실 seam(우편 판 이식) ⒟ #75 프로덕션 프로파일. step-loop 이 STATE §2 로 승급.
