# review 0281–0290 — #56 브리지 존 데이터 평면 arc (entity 트래픽)

> 범위: 0281~0290(전부 코디네이션·오케스트레이터 — 브리지 존에 실 entity 트래픽 배선). 인과 서사·서버별 기능 진척은 [progress/5-coordination.md](progress/5-coordination.md)(오케 기능 8→9) · 이 문서는 *정당성 감사*만.

## §1 척추 5불변 정합성

| # | 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 전 entity op 는 orch 제어 평면·존 시뮬 tick 밖 동기 Map 연산. `src/orch-control.js:48,50,52` enter/move/leave 분기·`:79` `_tickRuntimes`. orch 가 *자기 소유* 런타임의 `onTick` 을 구동하는 것은 코디네이션의 존 lifecycle 정의 책임(SPINE §2)·은닉 위반 아님. I/O·await 0. |
| ② | 결정론 코어 | ✅ | 런타임 시드=`fnv1a(String(zid))`(`src/topo-actors.js:70`)·enter 위치=시드 PRNG·move=정수 delta wrap. `grep -n Math.random src/*.js`=**0 hit**. `zoneflowcap` 5/5 전 seed 동일 ledger(5/1/2/1·total 1)=run seed 무관 결정론. |
| ③ | 권위 단일 소유 | ✅강화 | entity 차원 단일 소유를 *수치로* 단언: `entitiesSingleOwner`(`orch-zonebridge.js:111`·avatar 두 존 동시 거주 0)·`entityCoherent`(`:117`·entity 보유 런타임은 모두 executed `running`·orphan 0)·`entityConserved`(`:125`·total=enters−leaves−lost−discarded). migrate=같은 핸들(중복 0)·hostdown=새 인스턴스(공백 회복). |
| ④ | 은닉·단일 연결 | 🟡 | orch 가 `zone.js` 직접 import **0 hit**(`require.*zone` = orch-zonebridge 자기 믹스인만)·`EntityZone` 은 `makeActor`(`topo-actors.js:67`)가 zoneBridge ON 시 팩토리 주입(코디네이션→월드 직접 결합 회피). 게이트웨이는 `zoneEnter/zoneMove/zoneLeave` 메시지로만(`topo-inject.js:70`). **단 긴장**: entity 트래픽이 게이트웨이→**orch**→런타임 경유(orch 가 런타임 핸들 보유)이지 게이트웨이→실 존 *직접*이 아니다 → 이슈(#9 흡수·§3). |
| ⑤ | headless·원격 검증 | 🟡 | 실 `EntityZone` 헤드리스 인스턴스·`grep -E 'UObject|CoreUObject|GameplayTags' src/*.js`=**0 hit**. `zoneflowcap`·`spine` 헤드리스 ALL OK. **단 인프로세스**(브리지=orch 메모리 내 런타임 핸들·실 host.js 소켓 0=#9)·런타임 view 는 net 싱크로 드롭(클라 전파 0·#9). |

**메타(회귀 0·박스 유계)**: `zoneEntityFlow` 기본 OFF → 전 entity 경로 가드(`orch-control.js:48,50,52,79` `if (this.zoneEntityFlow)`)·OFF 면 런타임 자체 0(zoneBridge OFF) → 0280 비트 동일(`reg` ALL OK·maxDiff 0). `Math.random` 0(②). 박스 유계: `orch-zonebridge.js` 16.4KB·`orch-control.js` 11.4KB·전 src/*.js ≤26KB(>30KB 0). 새 박스 0(전부 기존 오케 믹스인·진입점에 메서드 누적).

## §2 검증 재현 (직접 실행·인용)

- `node src/verify.js zoneflowcap` → **ALL OK**(5/5 seed). 혼합 lifecycle(enter5·move·leave1·hostdownC[소실2]·stop z2[폐기1]·migrate·rebalance·drain) 후 `flow`=Y·`consv`=Y·`total`=1(==5−1−2−1)·ledger `5/1/2/1`·`migs`=2.
- `node src/verify.js reg` → **ALL OK**(zoneEntityFlow OFF·전 시나리오 0280 비트 동일·maxDiff 0).
- `node run.js spine` → **ALL OK**(`spine.txt:322`·src 누적 회귀·전 역사 불변 현재 코드 단언).
- grep 감사: `Math.random` src **0**·`UObject|CoreUObject|GameplayTags` src **0**·`require.*zone` (orch 4파일)=orch-zonebridge 자기 믹스인 require 1줄뿐(orch 가 zone.js 직접 import 0·④ 은닉).
- **단일 src 모델 유의(#16)**: 0281~0289 의 bespoke 모드(zoneenter·zonemove·zoneleave·zonemigent·zonehostdownent·zonestopent·zoneowner·zoneentcoherent·zonegraceful)는 현 verify.js 에 부재(매 step 교체)·git per-commit 에만. 현 spine 모드=`zoneflowcap` 하나가 전 lifecycle(enter~drain) + 보존 회계를 한 번에 단언.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #56 | 브리지 EntityZone 비활성(entity 트래픽·tick 0·migrate 무손실 vacuous) | ③/⑤ | ✅ **해소(0281~0290)** | `orch-zonebridge.js` 가 게이트웨이→orch enter/move/leave 를 실 EntityZone 핸들로 라우팅(`_bridgeEnter:74`/`_bridgeMove:82`/`_bridgeLeave:90`)·런타임 `onTick` 구동(`_tickRuntimes:99`). migrate 무손실(행동적·0284)↔hostdown 소실(0285)↔stop 폐기(0286) 행동적 단언·단일 소유(0287)·정합(0288)·graceful census 보존(0289)·capstone(0290 entityFlowCoherent/entityConserved). 인덱스서 제거. **잔여는 #9(게이트웨이→실 존 *직접* 라우팅·실 프로세스)로 승계.** |
| #53 | 정합 술어 by-construction(분기 부재) | 검증 | 🟡 **확장(#56 판)** | 0288 entityCoherent·0290 entityFlowCoherent/entityConserved 도 by-construction — 정상 op 에서 graceful=같은 핸들·destructive(hostdown/stop)=런타임·entity 동시 정리 → 분기(정합 위반) 경로 0. **목적지 STATE §3** → 게이트: #9 실 프로세스가 *진짜 분기*(라우팅 OK·실 적재 실패) 도입 시 정합 술어 load-bearing 화(끄면 구조적 정합=회귀 0). |
| #9 | 멀티프로세스 패리티 갭 | ④/⑤ | 🟡 **확장(+브리지 데이터 평면)** | **+0281~0290: 브리지 존 entity 트래픽도 orch 경유 인프로세스**(`entityOps`=`topo-inject.js:70` run-loop 주입 seam·런타임 핸들=orch 메모리·실 host.js 소켓 0). 게이트웨이→실 존 *직접* enter/move 라우팅 0(현 게이트웨이→orch→런타임). **이제 최상위 load-bearing**(#51b·#56 으로 SSOT↔실물↔데이터 평면 닫혔으므로 다음은 실 프로세스). **목적지 STATE §2 NEXT** → 게이트: 브리지 핸들→host.js 소켓 + 게이트웨이→실 존 직접 라우팅(끄면 인프로세스 orch 경유=회귀 0)·arc 정합 SPINE §6 위. |
| #16 | bespoke 모드 verify-kit 미승급 | ②/⑤/정리 | 🟡 **확장(+9 모드)** | zoneenter~zonegraceful(0281~0289) 미승급·현 spine 모드=zoneflowcap 만. **목적지 STATE §3**(정리성) → 게이트: 안정 호 묶음별 스모크 승급(끄면 현 spine=회귀 0). |

> 그 외 유지(미접촉): #54·#55·#50·#46·#48·#44·#41·#39·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#37·#38·#36.

**🎯 단계 평결(SKILL §3.6)**: 0281~0290=#56 브리지 존 데이터 평면(load-bearing) — 0271~0280 verdict 가 지정한 게이트("게이트웨이→실 존 enter/move 라우팅 + 이주 시 entity 무손실 실증")를 집행. 너비 1차 완료(⬜/🌱 0)·새 박스 0(오케스트레이터 *심화*)→**과심화 아님**(승인된 심화). reg ALL OK·spine ALL OK. **다음 권고: #9 멀티프로세스 배선**(브리지 핸들→실 host.js 소켓·게이트웨이→실 존 직접 라우팅·현 orch 경유 인프로세스). review=기록·step-loop 이 STATE §2 로 승급.
