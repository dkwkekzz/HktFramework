# review-0211-0220 — 5박스 2차 고도화 균형 라운드 (정당성 감사)

> 범위 0211~0220 · 인과 서사·서버별 기능 리스트는 [progress/](progress/)(1-edge·2-world·5-coordination·6-data 갱신). 이 문서는 *정당성 감사*만(척추 5불변·검증 재현·이슈→후속). 진행 마커 권위 = [../STATE.md](../STATE.md) §5.

## §1 척추 5불변 정합성 (SPINE §5)

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 5박스 전부 *순수 반응형*(onTick 메서드 0·tick 내 동기 I/O 0). grep `onTick|await|fs.` = 주석 1줄씩만(`src/cache.js:11`·`worldlog.js:11`·`instance.js:10`·`loginqueue.js:11` "순수 반응형(onTick 없음)"). 만료/무효화/스냅샷/배치/재배치 전부 op 수신 시에만 실행 — 존 tick 밖. |
| ② | 결정론 코어 | ✅ | `Math.random` 0 hit(touched 5박스). TTL/만료 기준은 `m.tick`(`cache.js:_set(key,value,now)`·`loginqueue.js _dequeue(now)`)·스냅샷 digest 는 FNV 결정론(`worldlog.js:stateDigest`). 부하 tie-break 은 후보 배열 순서(`orchestrator.js:_leastLoaded`)·auto-id 는 단조 시퀀스(`instance.js:demandSeq`) — 난수 0. 전 모드 5시드 동일. |
| ③ | 권위 단일 소유 | ✅강화 | 이번 묶음이 *권위 쌍 거래*를 두 곳 더 입증: 인스턴스 재배정(`instance.js:_route` cur≠new 시 Map 단일 키 release+acquire·routedCount 보존)·존 재배치(`orchestrator.js placeMigrate` from≠toHost release+acquire·총 placedCount 불변=공백/중복 0). 캐시는 휘발 사본(소스 SSOT)·worldlog 스냅샷은 intent 로그의 파생(유일 쓰기 경로 불침). |
| ④ | 은닉·단일 연결 | ✅ | 5박스 전부 제어/서비스 평면 메시지로만 구동(클라 접점 무변=게이트웨이). 새 op(cacheExpire·worldCrash·instanceRoute·placeMigrate·loginCapacity…) 전부 서버간/운영 경계. 클라 와이어 계약 무변(reg 0 이 보증). |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | UE 모듈(`CoreUObject`·`GameplayTags`·`UObject`) 0 hit. 전 모드 `node run.js`/`spine` headless 재현. **단 2차 10조각 전부 인프로세스 op-injection seam(host.js 멀티프로세스 0)·bespoke 모드 누적 회귀 미승급** — #9·#16 확장(아래 §3). |

**메타(회귀 규율·박스 유계)**: 2차 10조각 전부 OFF 플래그(미주입 시 휴면)로 **reg 0**(직전 step 비트 동일) — `cacheExpire`·`cacheInvalidate`·`worldSnapshot`·`worldCrash/Recover`·`instanceDemand`·`instanceRoute`·`placeAuto`·`placeMigrate`·`loginCapacity`·`loginReconnect` 미주입 경로가 baseline 과 동일. 박스 크기 **건전**(cache 6.6·instance 7.8·loginqueue 7.5·worldlog 8.4·orch 17.7KB·전부 ≪30KB). **이번 묶음은 `topo-build`/`topo-run` 미접촉**(기존 op-injection 채널 `instanceOps`/`placementOps`/`cacheOps`/`worldOps`/`loginOps` 재사용) → #49(wiring >30KB) *미악화*(30.5/35.1KB·0210 과 동일).

## §2 검증 재현 (실행·인용)

- **`node run.js`**(현재 step 0220 = reg + loginreconnect): `reg: ... step-0027 와 비트 동일` + `loginreconnect: ... 2차 고도화 로그인 큐 #2` → **결과: ALL OK**. 유효 티켓 재개(p1 tkt-1·ticketSeq 1 불변=새 티켓 0)·미입장 재개 실패(p2 miss) 5/5.
- **`node run.js spine`**(src 누적 회귀 — 전 역사 불변을 *현재 코드*에 단언): **결과: ALL OK** (220-step 사슬·reg+전 가설 모드). "0211~0220 의 10조각이 0001~0210 의 어떤 불변도 안 깼다"의 증거.
- **grep 감사**: ② `Math.random` 0(5박스) · ⑤ UE 모듈 0(5박스) · ① tick 내 `onTick/await/fs` 0(주석만·4박스 확인) · 박스 size 전부 ≪30KB.
- **단일 src 한계(#16 재확인)**: `src/verify.js` 에 `kit.MODES['loginreconnect']` 만 — 0211~0219 의 9 bespoke 모드는 git 태그(step-0211~0219)에만 보존·재실행 불가("step-NNNN verify.js 없음"). ON-경로 의미는 각 close 시 1회 단언·spine 은 reg-0 만 보증.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트 |
|---|---|---|---|---|
| #50 | **instanceRoute orphan route**(despawn 시 routes 미정리) | ③/서비스 | 🟡 신규·경미 | `instance.js:_despawn` 가 active 만 제거·routes 잔류 → 죽은 인스턴스로 player 가 dangling(occupancyOf 과대). **STATE §3 OPEN GAPS** → 게이트: despawn 시 그 instance 로 라우팅된 routes 스윕(끄면 현 거동=회귀 0) |
| #51 | **오케 배치 SSOT 가 실 존 lifecycle 미배선(paper map·#46 family)** | ⑤/③ | 🔴 신규·load-bearing(**2차/3차 대기**) | placeZone/placeAuto/placeMigrate 는 `placement` 맵(zoneId→host)만 갱신·실제 존(orch.pairs/promotion·`zone.js`)은 안 움직임 → 배치/재배치가 *advisory*. 길드 금고 #46(가짜 escrow)의 코디네이션 판. **2차/3차 백로그**(이미 선 박스 심화) → 게이트: placeMigrate→실 존 핸드오프 트리거(끄면 현 paper map=회귀 0) |
| #49 | wiring 박스 >30KB(topo-build 30.5·topo-run 35.1KB) | 정리 | 🔴 **유지·미악화** | 이번 묶음이 op-injection 채널 재사용으로 미접촉(0210 과 동일 크기) → 0201-0210 권고 "다음 기능 step 전 정리"는 *기능 step 이 wiring 을 안 키웠으므로* 비-블로킹이었음이 입증. **단 다음 *너비/멀티프로세스 배선* 전엔 여전히 게이트**. **STATE §2 NEXT** → 게이트: per-tick Ops 핸들러→`topo-inject.js` 추출 + close-step src size 체크(#43 동반·기능 0·reg 0) |
| #16 | bespoke 모드 누적 회귀 미승급 | ②/⑤/정리 | 🔴 **확장**(+2차 10모드) | cachettl·cacheinval·worldsnap·worldcrash·instancedemand·instanceroute·placeload·placemigrate·logincapacity·loginreconnect 전부 verify-kit 밖·spine 미포함. **STATE §3 OPEN GAPS** → 게이트: 안정 호 스모크 1~2모드 verify-kit 승급(끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장**(+2차 10조각 host.js 0) | 2차 심화 전부 인프로세스 op-injection seam(crash/recover·재배치·재접속 핸드셰이크 멀티프로세스 미검증). **STATE §3** → 게이트: 멀티프로세스 배선 + 멱등/순서 보존(현 FIFO·인프로세스라 미발현=회귀 0) |

**단계 평결(§3.6)**: **2차 균형 라운드 완료** — 0211~0220 이 너비 1차 5박스를 *각 2조각씩* 심화(과심화 0·한 박스 코박기 없음). progress 맵 전 박스 🟡(기능 리스트 2차 항목 채움). 과거 0107~0200(서비스 한 박스 영속~capstone 코박기)과 대비되는 *균형* 패턴 — **이번 묶음은 과심화 아님**. 다음 방향(정리#49 / 3차 잔여 / 멀티프로세스#9·spine#16 / 금고#46·비동기#4) 은 STATE §2 가 승급. **신규 #50·#51 둘 다 후속 연결됨**(orphan route 정리·배치→실 존 배선).

> 미해소 이월(목적지·게이트 §2 원장): #51·#49(🔴 STATE §2 후보)·#16·#9(확장)·#50(신규 §3) + 기존 #46·#48·#44·#47·#45·#43·#41·#39·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#33·#36·#37·#38 유지.
