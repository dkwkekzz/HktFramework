# review 0261–0270 — #49 wiring 정리 arc (박스 크기 유계화·기능 0)

> 범위: step 0261~0270 (닫힘). 인과 서사·박스별 진행은 [progress/](progress/) — 이 문서는 *정당성 감사*(척추 5불변·재현·이슈)만. 묶음 성격: **순수 구조 정리**(기능 0·전 step 투명 분할·reg 0). 3대 >30KB 박스(topo-run·topo-build·svc-exchange-core) 해소 + 30KB 근접 7박스 선제 정리. **0241-0250 verdict 가 지정한 #49 게이트를 집행한 arc.**

## §1 척추 정합성 (SPINE §5 5불변)

이 묶음은 *코드를 옮기기만* 한다(믹스인 `Object.assign(prototype)` / 위임 함수 `apply*`·`addServiceBoxes`). 동작 비트 불변 → 5불변 모두 **무변경 ✅**(긴장·악화 0). 알리바이는 분할이 *동작을 안 바꿈*을 보이는 배선 라인.

- **① 신성한 tick ✅(무변경)** — 옮긴 코드 전부 존 tick 밖 제어 평면(주입열·복구·박스 add·서비스 핸들러). tick 내 신규 I/O 0. 알리바이: `topo-run.js:91,94`(applyFailover/applyInjections 는 `net.step()` *직전* 호출·루프 위치 동일).
- **② 결정론 코어 ✅(무변경)** — 코드 이동만·시드 PRNG 경로 불변. grep `Math.random` src/ = **0 hit**. 알리바이: `gwsplit` 두 동일 run worldDigest 일치(`src/verify.js`·재현 아래 §2).
- **③ 권위 단일 소유 ✅(무변경)** — 옮긴 메서드의 권위 로직 verbatim. 알리바이: `svc-exchange-core.js:158`(`Object.assign(...ExchangePersist)` — crash/reconstruct 가 프로토타입서 해소·xchsplit 보존 불변 5/5)·`svc-guild.js:194`(GuildTxn·single-master gldsplit 5/5).
- **④ 은닉·단일 연결 ✅(무변경)** — 게이트웨이 onMsg 분할(0270)도 *단일 연결 계약*(whisperDeliver/item_req/move 메시지) 무변경·내부 구조만. 알리바이: `gateway.js`(`Object.assign(Gateway.prototype, GatewayMsg)`·onMsg 가 `_svcSend`/`_relayX` 코어 잔류 헬퍼 호출·gwsplit live 6).
- **⑤ headless·원격 검증 ✅(무변경)** — 새 파일 11종 전부 dual-mode(Node require + `globalThis.__HktNetParts`). grep UE 모듈(`UObject`·`#include`) src/ = **0 hit**. 알리바이: `svc-mailbox-dedup.js` 등 말미 `if (module) … if (globalThis) …`.

**메타(구현 품질)**: **회귀 0 규율 전 step 준수**(reg ALL OK·maxDiff 0 — 투명 분할의 핵심 증거)·**박스 유계 ✅ 달성**(전 src/*.js ≤26.6KB·30KB 초과 0). 분할 패턴 일관: *클래스* 박스는 0251 `Object.assign(prototype)` 믹스인(0264~0270), *단일 거대 함수* wiring 은 `apply*(opts,i,ctx)` 위임 + ctx 핸들 묶음(0261~0263·#49 근거의 "단일 거대 함수라 클래스 패턴 미적용" 을 *위임* 패턴으로 해소).

## §2 검증 재현 (run.js·grep — 추정 금지)

- `node run.js spine` → **ALL OK**(전 역사 누적 회귀 + 이번 10 모드 중 현 셸 gwsplit 편입·전 불변 현재 코드에 단언·exit 0).
- `node run.js`(현 step 0270·gwsplit) → **ALL OK**(클라 move→게이트웨이 onMsg 존 라우팅·worldDigest 5/5 재현·live 6).
- `wc -c src/*.js | awk '$1>30000'` → **0 행**(30KB 초과 박스 0·#49 해소 증거). 최대 = `svc-exchange-core.js` 26.6KB.
- `grep -rn "Math\.random" src/`(주석 제외) → **0 hit**(②). `grep -rniE "UObject|#include" src/` → **0 hit**(⑤).
- 각 step reg(닫기 시) → ALL OK·maxDiff 0(투명 분할·step-0261~0270.md §검증).

> per-step 새 가설 모드 10종(injsplit·fosplit·boxsplit·xchsplit·gldsplit·invsplit·orchctlsplit·mailsplit·mboxsplit·gwsplit)은 각 step verify.js 에 bespoke·미승급(현 셸엔 gwsplit 만 — #16 패턴). 닫을 때 5/5 통과 확인됨(step 문서)·재현은 git `git show step-NNNN:src/verify.js`.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #49 | wiring 박스 >30KB | 정리 | ✅ **해소(0261~0264)** | topo-run→topo-inject(0261)/topo-failover(0262)·topo-build→topo-boxes(0263)·svc-exchange-core→svc-exchange-persist(0264). >30KB 박스 0(재현 §2). 기록만·인덱스서 제거 |
| #43 | close-step src 박스 size 미체크 | 도구 | 🔴 **승격(근거 강화)** — #49 를 10 step 수동 `wc -c` 로만 탐지(도구 무방비)·재성장 시 또 수동 | **STATE §3 OPEN GAPS**(도구성) → close-step 에 `src/*.js ≤30KB` 체크 추가(#36 baseline-freshness 와 동반·회귀 무관) |
| #54 | 캐시 집중 편중(0251~0260) | 단계 | ✅ **완화** — 이번 묶음이 #49(게이트)를 집행해 "load-bearing 보류" 압력 해소·균형 회복 | 기록만(0271+ 방향은 #51b/#9) |
| #16 | bespoke 모드 미승급 | 정리 | 🟡 **확장** — 이번 10 모드도 bespoke·미승급(잔여 0033~0270 수백 모드) | STATE §3(정리성) → 안정 호 스모크 점진 승급 |
| #9 | 멀티프로세스 패리티 | ③/⑤ | 🟡 **유지** — 정리 묶음(신규 host.js 0·기능 0이라 무악화) | STATE §3 → 멀티프로세스 핸드셰이크 배선 |
| #51b | running↔실 zone.js 미배선 | ⑤/③ | 🟡 **유지**(0261~0270 무영향·orchestrator 는 onMsg/onTick 만 이동) | STATE §2 NEXT(load-bearing·#49 게이트 해제로 이제 우선) |

기타 유지(무영향): #46·#48·#44·#47(이 중 #47 svc-guild 는 0265 분할로 24.4KB·임박 완화)·#50·#53·#55·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#36·#37·#38·#39·#41.

> **🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 **0 유지**(새 박스 0·기능 0). 이번 묶음은 *빈 박스도·이미 선 박스 심화도 아닌* **순수 구조 정리** — 0241-0250·0251-0260 verdict 가 세 라운드 연속 권고한 **#49 게이트를 집행**(load-bearing 보류 해소). **과심화 아님**(기능 0). **다음 방향**: #49 게이트 해제 → load-bearing **#51b(running↔실 EntityZone host 이주·zone.js 핸드오프)** + **#9 멀티프로세스 배선** 우선(원장 §2·목적지 STATE §2 NEXT). 도구 갭 #43(close-step src size)은 재발 방지로 동반 권고.
