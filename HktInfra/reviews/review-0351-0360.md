# review 0351–0360 — #57 실 host.js OS 프로세스 spawn: 드라이버 계약 + 실 spawn (코디네이션·SPINE §5 ⑤)

> 범위: step-0351~0360. 인과 서사·서버 기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md) 기능 15(+ [progress/README.md](progress/README.md) 다음 큰 걸음). 이 문서는 *정당성 감사*만 — narrative 요약표 없음.

## §1 척추 5불변 정합성

- **① 신성한 tick ✅** — 드라이버 훅(on*)은 배치 집행(`_hostSet`·`src/orch-hostproc.js:18,22`)·전송 seam(`_zoneDeliver`·`src/orch-zonebridge.js:112`)·egress 드레인(`_drainZoneEgress`·`src/orch-views.js:26`) 안에서만 발화 — 전부 시뮬 tick 밖 제어/전송 평면. `flush`(`src/cluster-hostdriver.js:21`)의 async child_process spawn 도 verify 하니스 레벨(존 tick 무관). 시뮬 tick 내 동기 I/O·await 0.
- **② 결정론 코어 ✅** — `Math.random` src 0 hit(grep). 드라이버 명령 큐 순서 = 이벤트(배치 op) 순서(결정론)·실 host 존 시드 = `fnv1a(zoneId)`(`src/verify.js` zoneSpecOf·결정론). 5시드 동일 출력.
- **③ 권위 단일 소유 ✅강화** — `clusterHostsCoherent`(`src/orch-hostproc.js:144`)가 드라이버 명령 순계와 가동 host/존을 1:1 로 못박음: `driverSpawns−driverDespawns==zoneHosts.size` && `driverAssigns−driverUnassigns==runningCount` && `hostContainerCoherent`. 실 host 도 한 존 = 한 프로세스(`clusterhostcap` A=[z1,z2]·B=[z3]·중복 0).
- **④ 은닉·단일 연결 ✅** — 드라이버 명령은 host id·zone id·직렬화 spec 만(존 내부·함수 미적재) → 실 소켓 와이어 호환(`zoneadd specs:[spec]`). orch→`zone.js` 직접 import 0 hit(EntityZone 은 `makeActor` 팩토리·host.js 가 자기 makeActor 로 재구성=멀티프로세스-safe).
- **⑤ headless·원격 검증 ✅(진전)** — 이번 묶음의 핵심: 0301~0350 내내 *논리* 컨테이너였던 zoneHost 를 실 `host.js` 자식 OS 프로세스로 물질화. `hostspawnreal`/`hostmultizone`/`clusterhostcap` 가 실 `Cluster`(`src/cluster-core.js`·`child_process.spawn`)로 host.js 를 띄워 snapshot 으로 존 인스턴스를 확인(livePids 1~2). UObject/UE 모듈 src 0 hit. **#57 의 ⑤ 긴장이 처음으로 *실 프로세스*로 완화됨**(이전 묶음은 전부 인프로세스).
- **메타(회귀 0·박스 유계)**: 신규 코드 전부 OFF 플래그(`clusterDriverRecord`/`clusterDriverReal`) 또는 읽기 전용 질의 → 드라이버 미부착 시 `&&` 단락·호출 0·baseline 비트 동일(`reg` ALL OK 매 step). `host.js` 신규 `zoneadd` cmd 는 기존 cmd 무변경 → e2e/multiproc 비트 동일. 박스 최대 `orch-zonebridge.js` 28.9KB(<30KB·0355 onFrame 으로 27.8→28.9KB — **30KB 근접·watch**).

## §2 검증 재현 (직접 실행 인용)

- `node run.js spine` = **ALL OK**(전 시리즈 누적 회귀·5시드·실 child_process e2e 포함·정리 shutdown).
- `node run.js 0360`(현 step verify): `clusterhostcap` 5/5 — coherent Y·hostCount 2·hostA=[z1,z2]·hostB=[z3]·livePids 2 + `reg` ALL OK(5시드 digest 동일).
- 묶음 bespoke(각 step verify.js·git per-commit): `hostplan`/`hostdelta`/`hostdrive`/`hostroster`/`hostframe`/`hostegress`/`hostcmd`/`hostspawnreal`/`hostmultizone` 전부 5/5(step 문서 수치와 일치 확인).
- grep 감사: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · orch→`./zone.js` import 0 · clusterDriver 훅 부착 = `orch-hostproc.js:22`(spawn)·`:18`(despawn)·`orch-zonebridge.js:112`(frame)·`orch-views.js:26`(egress).
- 박스 크기: `wc -c src/*.js` 최대 orch-zonebridge 28901·gateway 28217·orchestrator 22781 — >30KB 0개.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #57 | zoneHosts/egress/DownClient 가 orch 인프로세스 액터(실 host.js OS 프로세스 아님) | ⑤ | 🟡 **대폭 진전(0351~0360)** — 드라이버 계약(번역) + 실 spawn/zoneadd 로 *논리 컨테이너→실 host.js child_process* 물질화 입증(`clusterhostcap` 실 2 프로세스). **잔여**: deliver/egress 실 소켓 데이터 평면 집행(현 flush=spawn+zoneadd 만)·migrate/killHost 실 프로세스 전환·`cluster-run.js` runMulti 통합 | **목적지 STATE §2 NEXT**(잔여 load-bearing) → 게이트: ClusterHostDriver flush 에 deliver/egress 실 rpc·migrate/kill 실 전환·runMulti 통합(끄면 현 인프로세스 데이터 평면=회귀 0) |
| #62 | ClusterHostDriver 가 spine 게이트 밖 `cluster-run.js` runMulti 와 미통합(실 lifecycle 전환 미검증) | ⑤/검증 | 🟡 **신규** — 0351~0360 의 실 spawn 은 verify 모드가 *직접* 실 Cluster 를 구동(독립 스폰+flush)·기존 runMulti(14 프로세스 lockstep) 와 별도 경로. migrate/failover/kill 의 실 프로세스 핸드셰이크는 runMulti 통합 후에야 E2E | **목적지 STATE §2**(#57 동반) → 게이트: runMulti 가 orch.clusterDriver 를 broker 측 제어 평면으로 배선(끄면 현 분리 경로=회귀 0) |
| #53 | 정합-0 가 by-construction(분기 부재) | 검증 | 🟡 **부분 진짜화 유지** — in-proc 드라이버 계약(hostplan~hostcmd)은 recorder 비교라 구조 정합(#53 family)·단 `hostspawnreal`/`clusterhostcap` 의 실 child_process spawn+snapshot 은 *진짜 분기*(실 프로세스 생사·실 makeActor) | **목적지 STATE §3 OPEN GAPS** → 게이트: 실 소켓 지연/적용 실패 분기 도입 시 load-bearing(끄면 구조적=회귀 0) |
| #16 | bespoke 모드 verify-kit 미승급 | ②/정리 | 🟡 **확장(+10)** — 이번 묶음 hostplan~clusterhostcap 10 모드도 미승급(현 spine 모드=최신 step `clusterhostcap` 1개·나머지 git per-commit) | **목적지 STATE §3**(정리성) → 게이트: 안정 호 묶음별 스모크 1~2 모드 점진 승급(끄면 현 spine=회귀 0) |
| #58' | `orch-zonebridge.js` 28.9KB(0355 onFrame 으로 +1.1KB·30KB 근접) | 정리 | 🟡 **watch** — #58(orchestrator 코어)은 0332 해소됐으나 zonebridge 가 30KB 트리거 근접 | **목적지 STATE §3**(정리성) → 게이트: 다음 기능 step 전 30KB 초과 시 정리 분할(기능 0·reg 0·#43 가드가 잡음) |

기존 이월 유지(다음 묶음 재점검): #61(업스트림 intent 실 클라·#57 동반)·#4(진짜 비동기·load-bearing)·#46(길드 금고↔가방·2차 대기)·#50·#48·#44·#41·#39·#13·#17a·#25·#27·#28·#29·#30b·#55·#3·#6·#7·#8·#1(⛔범위 경계).

**🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 2차. 0351~0360 은 *연속 4묶음 verdict 가 지목한 최상위 load-bearing #57*(실 host.js OS 프로세스/소켓 spawn)을 집행한 **새 능력**(오케스트레이터 기능 15·과심화 아님). 인프로세스 다운스트림 E2E(0319~0350) 가 완결된 뒤 *실 프로세스 경계*의 첫 절반(드라이버 계약 + 실 spawn/존 인스턴스화)을 세움. **다음 권고(불변)**: #57 잔여 — deliver/egress 실 소켓 데이터 평면 집행 + migrate/killHost 실 프로세스 전환 + `cluster-run.js` runMulti 통합(#62), 그 다음 진짜 비동기(#4). step-loop 이 STATE §2 로 승급.
