# review-0181-0190 — 길드(Guild) 전용 박스 신규 arc (정당성 감사)

> 범위 0181~0190 · 인과 서사(무슨 서버·왜·어떻게)는 [progress/3-services.md](progress/3-services.md) "길드(Guild) 서버" 항목. 이 문서는 *그 인과가 척추에 정당한가*만 — 5불변 판정 + 코드 알리바이 + run.js 재현 + 이슈 전방 연결.
>
> 한 줄: 파티(0075)+우편(0142~0180) 의 검증된 박스 패턴(분리→증분→발행→영속→스냅샷→배지→정합)을 동형 적용하되, **길드 고유의 single-master 권위 불변**(척추 ③의 길드 적용·이양은 release+acquire 쌍 거래)을 더해 SPINE 계층3 마지막 미착수 박스를 세웠다.

## §1 척추 5불변 정합성

| # | 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | GuildService·GuildFeed 둘 다 `onMsg` 순수 반응형·`onTick` 없음·존 tick 밖. grep `await/fs/setTimeout` 0 hit(두 박스). 멤버십·배지는 *오래 사는 상태*로 SPINE §2 판정("tick 박자여야 하나? 아니오")을 통과. |
| ② | 결정론 코어 | ✅ | grep `Math.random` 0 hit(`svc-guild.js`·`svc-guildfeed.js`). `_normalize`(`svc-guild.js:41`) 가 master 선두·결정론적 삽입 순서로 로스터 정규화 → 전 시드 동일 형상(guild·guildcapstone 5/5 동일). 발행/저널/배지 전부 시드 무관. |
| ③ | 권위 단일 소유 | ✅ **강화(single-master)** | 길드 고유 불변을 *명시 단언*: ⒜ master 보호 `svc-guild.js:84`(`p.member !== g.master` — 마스터 guildLeave no-op·공백 0) ⒝ 마스터 이양 `svc-guild.js:88-95`(release+acquire 쌍 거래 — from==master·to∈members 일 때만 `g.master = p.to` 원자 교체·이중 0·거부 no-op) ⒞ capstone `rosterConsistent()` `svc-guild.js:109`(전 길드 한 master·master∈members·중복 0). 존 핸드오프 0006·escrow 0117 의 마스터십 판. |
| ④ | 은닉·단일 연결 | ✅ | GuildFeed 는 svc.guild.changed *구독*만(`svc-guildfeed.js:27` 토픽 가드)·로스터 SSOT 직접 접근 0(발행 스트림 파생). 소비자는 guildQuery request/reply 로만 로스터 소비(`svc-guild.js:99`)·저장 방식 무지. 발행자(GuildService) 무수정으로 audit·guildfeed 가 버스 구독 추가(`topo-subs.js:32-33`). |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | grep UE 모듈(`UObject`·`CoreUObject`·`GameplayTags`) 0 hit(두 박스). Node 전용·`node run.js` 수치 증명. **단** 길드 arc 전체 host.js 0 — guildOps 는 `topo-run.js` run-loop 주입 seam·crash/reconstruct 는 verify post-run 직접 호출(인프로세스만·#9 family). |

**메타(구현 품질)**: 회귀 0 규율 견고 — 신규 항 전부 OFF 게이트(`guildService`·`guildChangePublish`·`guildPersist`·`guildSnapshot`·`guildFeed`·`guildFeedPersist`; 증분/이양은 op 미주입 시 휴면). reg ALL OK(0189 비트 동일). capstone 2개(0188 feedConsistent·0190 rosterConsistent)는 순수 읽기 메서드(실행 경로 무변경)라 추가만으로 reg 0. **박스 유계**: svc-guild 18.3KB·svc-guildfeed 5.5KB ✅. **단 wiring 박스 재성장**: 길드 arc 가 6배선점(net-core·topo-actors·topo-build·topo-subs·topo-run)에 플래그/expose/inject 를 더해 `topo-run.js` 31.9→**33.3KB**(#34c 심화)·`topo-build.js` **29.5KB**(30KB 임박) — close-step 미탐지(#43).

> **긍정적 발견(#39 미상속)**: GuildFeed.reconstruct(`svc-guildfeed.js:43`)는 *자기* 소비-op 저널(`this.journal`)을 replay 한다 — MailFeed(#39)가 *우편 박스 저널*을 무압축 전제로 replay 한 것과 달리 외부 저널 의존 0(self-persist). 따라서 #39(읽기모델 무압축 전제)를 길드는 상속하지 않는다. create 이벤트가 `members` 배열을 실어 배지를 절대값으로 시드하므로 압축과 무관.

## §2 검증 재현 (직접 실행·인용)

- `node src/verify.js reg` → **ALL OK** (persistReplicas 미제공·0189 비트 동일).
- `node src/verify.js guildcapstone` → **5/5 OK** — g1 master c2(이양 성사)·g2 master c4(이양 거부)·A 정상·B guild crash→reconstruct·C feed crash→reconstruct **3체제 3/3**·배지 총합 5==로스터 5.
- `node run.js spine` → **ALL OK** (190-step 누적 회귀 사슬 — 전 역사 불변이 현재 코드서 하나도 안 깨짐).
- **grep 감사**(5불변 위반 탐지·`svc-guild.js`·`svc-guildfeed.js`): `Math.random` **0**(②) · `UObject`/UE 모듈 **0**(⑤) · 박스 tick 내 `await/fs/setTimeout` **0**(①). 권위 이중쓰기(③): 이양은 `g.master = p.to` 단일 대입 + 가드(from==master·to∈members) — 직접 소유 이전 외 경로 0.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #34c | topo-run.js 33.3KB>30KB (길드 arc expose+inject 로 31.9→33.3KB 심화) | 정리 | 🟡 **심화** | STATE §3 OPEN GAPS → per-tick 주입 핸들러 `topo-inject.js` 추출(정리 step·기능 0·reg 0) |
| #43 | close-step src 박스 size 미체크(topo-run 33.3KB·topo-build 29.5KB 미탐지) | 정리/도구 | 🔴 유지 | STATE §3 OPEN GAPS → close-step 에 src/*.js ≤30KB 체크 추가(회귀 무관) |
| #16 | 길드 가설 10모드 verify-kit 미승격(단일 src/ 모델상 0181~0189 모드 재실행 불가) | ②/⑤/정리 | 🔴 **확장**(+길드 10모드) | STATE §3 OPEN GAPS → 안정 호 스모크 1~2 모드 verify-kit 승격(끄면 현 spine=회귀 0) |
| #9 | 길드 arc 멀티프로세스 미배선(guildOps run-loop seam·crash/reconstruct post-run·host.js 0) | ③/⑤ | 🟡 **확장**(+길드 arc) | STATE §3 → 멀티프로세스 배선 + 멱등/순서 보존(현 인프로세스 미발현=회귀 0) |
| #44 | 길드 종료(disband) 경로 부재 — 솔로-마스터 길드 해소 불가 | 서비스 | 🟡 신규(경미) | STATE §3 OPEN GAPS → guildDisband op(저널 'disband' kind·끄면 현 거동=회귀 0)·arc 정합 대기 |
| #45 | topo-build.js 29.5KB — 30KB 임박(길드 플래그 누적) | 정리 | 🟡 신규(watch) | STATE §3 OPEN GAPS → #34c 분할 시 동반·또는 플래그 그룹 객체화(회귀 무관) |

> 신규 #44(disband)·#45(topo-build 임박)는 경미·정리성 → §3 OPEN GAPS 백로그(arc 정합 대기·SPINE §6 6계층 순서 우선). 심화 #34c·확장 #16·#9 는 기존 원장 이월. 길드 arc 는 새 load-bearing 척추 위반을 만들지 않았다(single-master 강화는 ③을 *굳혔다*).
