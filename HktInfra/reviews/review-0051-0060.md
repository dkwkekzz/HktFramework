# review-0051-0060 — 묶음 감사 (척추 정합성 · 재현 · 이슈→후속 step)

> 범위 **0051~0060** — *무엇을 했나*는 [progress/](progress/)(버스 [4-bus.md](progress/4-bus.md)·코디네이션 [5-coordination.md](progress/5-coordination.md)) + [STATE.md](../STATE.md) §7 INDEX. 이 문서는 *정합한가*만(감사 전용). 호(arc): cadence 적응 정밀화(grace prior·윈도 감쇠) → 정리(0053 txn 추출) → **lease 생애 관측→반응→self-healing 제어 루프**(0054 관측·0055 프레즌스 SSOT·0056~0060 반응·확인·재시도·상한·발행).
> 알리바이는 *현재 코드* `src/<box>.js:line`(누적·0049 src/ 전환)·정확한 델타는 git.

## §1 척추 정합성 (SPINE §5 5불변)

| # | 불변 | 판정 | 코드 알리바이 / 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | self-healing 제어 평면이 전부 존 tick 밖: lease cadence/sweep 는 `_onOutAck` ack-구동(`src/svc-inventory-bus.js:38-75`), 프레즌스 소비/발행은 orch.onMsg 반응(`src/orchestrator.js:30-48`), 재시도는 orch.onTick(`src/orchestrator.js:52-66`·존 시뮬과 별개 코디네이션 tick). grep: 닿은 박스(orchestrator·svc-ranking·svc-inventory-bus) `await`/sync I/O **0 hit**. reg=월드 비트 동일이 침습 0 의 증거. |
| ② | 결정론 코어 | ✅ (🟡 메타) | `src/` 실 `Math.random` **0 hit**·전 검증 시드[42,7,1234,99,2026] 재현(월드해시 `0x7a122947` 보존). 윈도 cadence(`consumerGaps` 링버퍼)·재시도 타이밍(tick 차)·프레즌스 발행 전부 결정론 ack/tick 순서의 함수. **`engine/panel-kit.js:29` Math.random 잔존**(브라우저 위젯 id·다이제스트 경로 밖) → #8 유지. |
| ③ | 권위 단일 소유 | ✅ | 이 묶음은 버스 lease·코디네이션 제어 평면 — 가방 원장 pickup/give 쌍 거래 *무변경*(reg 비트 동일). self-healing 전부 *비-침습*: recover/retry/cap/publish ON==OFF 의 `minted` 동일(0056~0060 각 §3 표), lease 축출/재admission 은 결과 버퍼 정의역 조정이지 권위 이전 아님(`src/svc-inventory-bus.js:34,75`). |
| ④ | 은닉·단일 연결 | ✅ (🟡 긴장) | 프레즌스/lease 는 *버스 이벤트*로만 흐른다: svc.item.lease(`src/svc-inventory-bus.js:110`)·svc.presence(`src/orchestrator.js:_presence`) 발행자 무수정 구독(audit). 소비자 재구독은 *구독자 자신*이 발신(`src/svc-ranking.js:38`·라우팅 권위=소비자)·recoverAck 는 m.from 회신(orch 주소 무지). **단 orch→소비자 recover 는 직접 주소 명령**(0009 promote/reroute 와 동형 코디네이션 제어 평면 — 데이터 평면 은닉은 보존, 제어 평면은 직접). |
| ⑤ | headless·원격 검증 | ✅ (🟡 메타) | `src`+`engine` UE 모듈(`UObject`·`CoreUObject`·`GameplayTags`) **0 hit**·`node run.js spine` ALL OK·`node run.js 0060` 멀티프로세스 14개 E2E 비트동일. **단 self-healing arc(0056~0060) 검증은 인프로세스 `run()` 가설 모드만**(cluster.js 무수정·멀티프로세스가 rankDie/dropRecover/recover 미주입) → #9 확장. |

**메타 — 박스 유계**: 0053 이 cadence 누적으로 31.9KB 넘긴 `svc-inventory-core.js` 의 트랜잭션 `onMsg` 를 `svc-inventory-txn.js`(6.5KB)로 바이트 동일 추출(다섯째 정리·reg 0). 현 최대 박스 **svc-inventory-core 26.1KB ≤30KB**(0054~0060 가 flag 누적 +0.6KB·여유). 전 박스 <30KB.

## §2 검증 재현 (주장 아님 — 직접 실행)

- **`node run.js spine` = ALL OK** — **60-step 누적 회귀 사슬**(reg + 전 가설 모드를 현재 src 에 단언). "어느 역사 불변도 현재 코드에서 안 깨졌다"의 증거.
- **`node src/verify.js publish`(0060) = ALL OK** — 치유 down+up 2건·영구 down+permanent 2건 발행→audit 1:1 무손실 수신·OFF 0·비-침습(전 시드).
- **`node src/verify.js reg` = ALL OK** — presencePublish=0 → 0059 비트 동일(전 시드).
- **grep 감사**: `src/` Math.random **0**(②)·`src+engine` UE 모듈 **0**(⑤)·닿은 박스 sync I/O **0**(①)·`engine/panel-kit.js:29` Math.random **1 잔존**(#8).
- **각 step OFF=직전 비트 동일**(STATE §7): busLeaseGrace/busCadenceWindow/busLeaseAudit/busLeasePresence/busPresenceRecover/recoverRetry/recoverMaxRetries/presencePublish 전부 플래그=0 → 직전 step 비트 동일(회귀 0 규율 일관). 0053 정리는 0052 바이트 동일.

## §3 이슈 → 후속 step 연결 (전파 고리)

**(a) 묶음 내 자체 해소** — §9 가 *다음* 게이트 step 으로 메워진 사슬(전파 고리가 도는 증거):

| 발견 | 이슈 | 해소 게이트 |
|---|---|---|
| #12(0041-0050 리뷰) | cadence EWMA 감쇠·시작 grace prior·bootstrap 1회 오축출 | ✅ 0051 busLeaseGrace(시작 prior floor·live ev 1→0 오축출 0) + 0052 busCadenceWindow(최근 K gap max·단조 증가 해소·일시 stall 후 죽음 감지 회복 deadEvict false→true) |
| 0052 §9 | core 31.9KB>30KB(박스 임계 위반) | ✅ 0053 txn 추출(25.5KB·reg 0) |
| 0054 §9 | 오케스트레이터가 lease 이벤트에 *반응* 미착수(관측만) | ✅ 0055 busLeasePresence(consumerDown SSOT) → 0056 busPresenceRecover(recover→자기 재구독→치유) |
| 0055 §9 | 프레즌스는 *상태*·행동 0 | ✅ 0056(액션 고리·OFF 영영 down vs ON 소비 119~128 vs 24 회복) |
| 0056 §9 | recover fire-and-forget(전달 미확인) | ✅ 0057 recoverAck(recoverAcks==recoversSent·분실 0) |
| 0058 §9 | 영구 분실에 재시도 무한(∝tick) | ✅ 0059 recoverMaxRetries(retries 3 유계 vs 17 무계·permanentDown 포기) |

> **요지**: 이 묶음은 *직전 리뷰가 이월한 #12*(cadence 정밀화)를 첫 두 step(0051·0052)이 곧장 메우고, 이어 **lease 생애의 관측→반응→완전한 self-healing 제어 루프**(0054~0060)를 한 호로 완성한 구간이다. 각 §9 한계가 바로 다음 step 의 가설이 되는 전파 고리가 6연쇄로 돌았다.

**(b) 미해소 — 원장 이월**(목적지·게이트·arc 정합):

| # | 이슈 | 항 | 상태 | 목적지 → 게이트 |
|---|---|---|---|---|
| #13(신규) | orch 三역할 겸함 → 전용 프레즌스/치유 박스 분리 | ⑤/정리 | 🟡 — orch 가 zone failover(0009)+소비자 프레즌스(0055)+치유(0056~0060) 겸함(0055·0059 §9). 규모 시 PresenceService/HealService(계층 5 둘째 박스) | STATE §3 → 정리 step: orch 에서 프레즌스/치유 메서드 추출(기능 0·reg 0) |
| #14(신규) | 대체 소비자 spawn(permanentDown→런타임 새 소비자) | ⑤ | 🟡 — 0059/0060 이 permanent 판정·발행까진 했으나 실제 동적 spawn 미착수(런타임 액터 생성·buildTopology 시점 등록의 한계) | STATE §2/§3 → 동적 토폴로지(svc.presence permanent 구독 spawn·#6 동적경계 family) |
| #15(신규·#12 잔여 흡수) | 적응형 lease 정밀화 — 둘째-결함 민감도·주기 인지·마진 jitter | ④ | 🟡 — full-max/윈도 둘 다 한 번의 긴 outage 가 *둘째 동일 결함* 감지를 억제(0057 §9 반복 사이클 좌절)·윈도 주기성 맹점(0052 §9)·마진(leaseSpan) 고정(jitter 적응 0) | STATE §3 → cadence EWMA 분포/주기 인지 + busLeaseMarginAdapt(끄면 현 거동=회귀 0) |
| #9 | give-resend 견고성 + **멀티프로세스 패리티**(self-healing 포함) | ③/⑤ | 🟡 유지(확장) — self-healing arc(0056~0060)도 인프로세스 가설 모드만(cluster.js 무수정·멀티프로세스 recover/retry/cap 미검증) | STATE §3 → 멀티프로세스 failover/heal 핸드셰이크 |
| #4 | broker lockstep = 강제 동기(비동기 결정론) | ① | 🔴 유지 — 이 묶음 미착수 | STATE §2 → 논리/벡터 클럭(🔴 최우선) |
| #8 | `engine/panel-kit.js:29` Math.random | ②/정리 | 🟡 유지 — 코드 재확인 잔존 | STATE §3 → 시드 카운터 id |
| #1·#3·#6·#7·#10·#11 | 결정론 이원화·cross-zone AOI·동적경계 N존·채팅 신뢰·give×result-ahead·per-producer ack/다중 GW | — | 🟡 유지 — 이 묶음(lease/코디네이션) 미착수 | 원장 §2 유지 |

> **#12 처리**: bootstrap/감쇠 부분은 0051·0052 가 해소(위 (a)) — 인덱스서 제거. 잔여(EWMA 분포·주기 인지·마진 jitter·둘째-결함 민감도)는 #15 로 승계.
