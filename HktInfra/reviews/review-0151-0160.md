# review 0151–0160 — 우편 미읽음 배지(MailFeed) + 아이템 첨부 우편

> 범위: step-0151~0160(닫힘). 인과 서사·서버별 기능 리스트는 [progress/3-services.md](progress/3-services.md)(우편·MailFeed 항목). 이 문서는 *정당성 감사*만 — 척추 5불변 판정 + 실물 코드 알리바이 + run.js 재현 + 이슈→후속 연결.
>
> 두 호: **MailFeed 미읽음 배지 읽기 모델**(0151~0156·MarketFeed 0112 의 우편 판) + **아이템 첨부 우편**(0157~0160·우편 메시지 회계 0142~0150 의 아이템 판). 둘 다 *발행자 무수정 소비자/단일 권위 박스* 패턴의 재현 — 새 박스 `src/svc-mailfeed.js` 1개 + `src/svc-mail.js` 제자리 확장.

## §1 척추 5불변 판정

| # | 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | MailFeed·MailService 둘 다 onTick 없음(존 tick 밖 반응형). `src/svc-mailfeed.js:onMsg`(구독 소비·질의 응답만)·`src/svc-mail.js:onMsg`(입금/수령/만료). grep: 박스 내 `await`·`fs`·`child_process` **0 hit**. |
| ② | 결정론 코어 | ✅ | grep `Math.random` `svc-mailfeed.js`·`svc-mail.js` **0 hit**. 배지·아이템 회계는 결정론 카운터(난수 0)·mailId 는 `_seq` 단조(`svc-mail.js:_seq`). reg 5시드 비트 동일·spine digest 0x4bc176e1 불변. |
| ③ | 권위 단일 소유 | ✅ | 우편함 권위 = MailService 단독(`svc-mail.js:boxes`). MailFeed 는 *파생 뷰*(`svc-mailfeed.js:badges`·쓰기 권위 0·feedConsistent 가 unread==sent−read−expired·unread≥0 단언). 아이템도 우편 1통에 묶여(itemConsistent `itemSent==itemHeld+itemFetched+itemExpired`·`svc-mail.js:itemConsistent`) 매 순간 보유/수령/만료 정확히 한 상태. |
| ④ | 은닉·단일 연결 | ✅ | MailFeed 는 버스 *구독*으로만 우편을 본다(발행자 주소 무지·`topo-subs.js:svc.mail.sent/read/expired→mailfeed`). 발신은 질의 reply 뿐(`svc-mailfeed.js:42` net.send(addr, m.from, …)·요청자에게만). 우편 박스 net.send 는 버스 pub 3종뿐(타 서버 내부 직접 접근 0). |
| ⑤ | headless·원격 검증 | ✅ | grep `UObject`·`CoreUObject`·`GameplayTags` **0 hit**. `node run.js`/`spine` 통과(UE 0). dual-mode 노출(`svc-mailfeed.js` 끝 module.exports + globalThis). |

**메타(구현 품질)**: ⒜ 회귀 0 규율 — 10 step 모두 플래그 OFF(mailFeed/mailFeedRead/mailFeedExpire/mailItem)·미호출 accessor(feedConsistent·itemConsistent·reconstruct)·미주입 seam(mailFeedQuery)으로 직전 비트 동일. digest 확장(item 동봉·`svc-mail.js` H/R 행)은 *item!=null 일 때만* 추가 → 미첨부 비트 불변(reg-safe). ⒝ **압축 step 문서 첫 묶음** — 0151~0160 이 새 압축 골격(한 일·검증·척추+한계 3절·8KB 게이트) 아래 첫 10 step·실측 평균 ~1.5KB(직전 0141~0150 ~4.8KB 의 1/3)·close-step 8KB 예산 강제 작동. ⒞ 박스 유계 — svc-mailfeed 신규 ~3KB(작음)·svc-mail 19.6KB(<30KB). 단 **topo-run 31.9KB>30KB**(#34c·이번에 mailfeed expose+mailFeedQuery 주입 2줄 더해 31.2→31.9·정리 step 별도 필요).

## §2 검증 재현 (직접 실행·인용)

- `node run.js`(현재 step-0160 `exmlitc` capstone): **ALL OK** — 4체제 itemConsistent 예(4/4)·아이템 분할 수령만 0/2/0·만료만 0/0/2·혼합 1/1/1·crash→reconstruct 정합(5시드 동일).
- `node run.js spine`(src 누적 회귀·전 역사 불변을 현재 코드에 단언): **ALL OK** — seed 2026 E2E 비트동일 true·저널홉 손실 복구 무손실(NAK 180·재전송 239)·digest `0x4bc176e1`. 160-step 누적 사슬에서 *어느 불변도 안 깨짐*(MailFeed/item 추가가 기존 박스 비트 불변).
- grep 감사(5불변 위반 탐지): `Math.random`·`await/fs`·`UObject/UE 모듈` 모두 `svc-mailfeed.js`·`svc-mail.js` 에서 **0 hit**.
- 각 step ON-경로는 close 시 1회 단언(태그 `step-0151`~`step-0160`·로컬): exmailfeed unread 3/2==sent · exmlfread unread==sent−read · exmlfexp unread==sent−read−expired · exmlfrec crash 후 reconstruct digest==라이브 · exmlfcons 4체제 feedConsistent · exmlfq queriesRx==repliesSent·회신==배지 · exmlitem itemHeld 2 · exmlitf itemFetched 2 · exmlitx itemExpired 1 · exmlitc 4체제 itemConsistent. **단 이 10 모드는 verify-kit 미승격**(spine 은 reg-0 만 보증·#16).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트 |
|---|---|---|---|---|
| #40 | **아이템 우편 가방 미연동** — 0157~0160 아이템이 *우편 박스 안 회계*(itemSent/Held/Fetched/Expired)로만 추적·발신자 가방 인출(give)·수령자 가방 입금·만료 시 발신자 반환 0. 진짜 아이템 우편 아님 | 서비스 | 🔴 신규(load-bearing) | **STATE §2 NEXT**(무르익음·거래소↔가방 0117~0120 2-서비스 보존의 우편 판이 확립 패턴) → 게이트: mailSend 시 발신자 가방 give→escrow·fetch 시 수령자 가방 입금·expire 시 발신자 반환 + 2-서비스 보존 단언(open mail-item ≡ 가방 mail-escrow·끄면 현 우편-박스-내 회계=회귀 0) |
| #39 | **MailFeed reconstruct 가 우편 저널 무압축 전제** — `svc-mailfeed.js:reconstruct(mail.journal)` 가 mail.snapshot 미소비 → mailSnapshot>0 면 가지친 head 배지 이력 복원 불가(MarketFeed #32 의 우편 판) | 서비스 | 🟡 신규(경미·#32 family) | **STATE §3 OPEN GAPS** → 게이트: MailFeed 자기 영속 또는 mail.snapshot+tail 동시 소비(끄면 무압축 정합=회귀 0). 무발현 전제: 0154 테스트는 mailSnapshot 0 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤ | 🔴 **확장**(+우편 read-model/item 10 모드·총 ~80) | STATE §3 OPEN GAPS → 게이트: 안정 호(우편·MailFeed) verify-kit 승격(스모크 1~2 모드·끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장**(+0151-0160 MailFeed/item 전부 host.js 0·`mailFeedQuery` run-loop seam `topo-run.js`·mailfeed crash/reconstruct post-run 직접 호출) + 읽기 모델 배지 정확성은 *순서 보존 전달* 가정(인프로세스 FIFO·재정렬 시 unread 음수 가능·feedConsistent 의 unread≥0 가 가드) | ③/⑤ | 🟡 확장 | STATE §3 → 게이트: 멀티프로세스 배선 + 멱등/순서 보존(현 FIFO·인프로세스라 미발현=회귀 0) |
| #33 | STATE §7 INDEX 30KB 천장 | 정리/도구 | 🟡 **유지(0151-0160 재발현)** — 이번 묶음서 close-step 30KB 예산이 2회 압축 강제(§7 결과열 정규형 + INDEX 중간열 em-dash 설명 일괄 제거로 47.8→28KB headroom 확보) | STATE §3 OPEN GAPS(도구성) → 게이트: INDEX 묶음당 1행 압축 또는 예산 상향(가독성만·회귀 무관) |
| #34 | wiring 박스 30KB 초과 | 정리 | 🟡 **(c) 유지·소폭 증가** — topo-run 31.2→**31.9KB**(이번 mailfeed expose+mailFeedQuery 주입) | STATE §3 OPEN GAPS → 게이트: per-tick 주입 핸들러→`topo-inject.js` 추출(정리 step·기능 0·reg 0) |

**유지(이번 묶음 미접촉)**: #30b·#32·#37·#38·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29.

> **전파**: #40 이 1차 — 우편 박스가 메시지·배지·아이템 세 축으로 섰으나 아이템이 *가방과 끊겨* 있어 "진짜 아이템 우편" 의 마지막 마디(거래소↔가방 saga 의 우편 판)가 비었다. step-loop 이 원장을 읽어 STATE §2 로 승급·게이트 step 으로 구현(review=기록·STATE=step-loop).
