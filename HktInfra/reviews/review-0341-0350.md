# review 0341–0350 — 다운스트림 전파 capstone + 실 클라 수렴 sub-arc(#9 후속·#60 해소 완결·E2E)

> 범위 0341~0350. 인과 서사(무슨 서버·어떤 기능·왜/어떻게/했나)는 [progress/1-edge.md](progress/1-edge.md)(게이트웨이 수신 유계화·대시보드)·[progress/2-world.md](progress/2-world.md)(실 클라 DownClient·수렴 desync 0). 이 문서는 *정당성 감사*만 — 실물 코드(`src/*.js`)·`run.js`/inline 재현·grep.

## §1 척추 5불변 정합성 (SPINE §5)

- **① 신성한 tick** ✅ — 실 클라 수렴 검증은 *읽기 전용 질의*(`zoneAuthSig`·`convergedTo`·`downstreamWorldCoherent`·tick 무관). DownClient 수신은 메시지 처리(`client.js` `DownClient.onMsg`·발신 0). 시뮬 tick 안 비-시뮬 작업 0.
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). DownClient seen 은 받은 view/view_delta 로만 갱신(`client.js`·결정론 전파). 5 시드 동일 수렴(재현: dc0.seen==aoi(a1) 5종·worldcap 5/5).
- **③ 권위 단일 소유** ✅강화 — DownClient 는 *읽기전용 뷰 소비자*(발신 0·seen 은 권위 재현)·권위 무영향. capstone(0346/0350)이 migrate(같은 핸들·`_migrate`)·손실·late-join 뒤 클라가 *권위로 수렴*(`convergedTo(zoneAuthSig)`)을 단언 — 클라 뷰가 권위 단일 소유를 재현.
- **④ 은닉·단일 연결** ✅ — 각 DownClient 는 *게이트웨이만* 통해 자기 세션 뷰만 받는다(`gatewayDeliveryIsolated`·0340·교차 누수 0·worldcap iso Y). 존/host/타 세션 무지. 수신 버퍼 유계화(0347·`downRecvWindow`)도 게이트웨이 내부 회계.
- **⑤ headless·원격 검증** ✅ — Node 순수·`UObject|...` src 0 hit. DownClient=수신 전용 인프로세스 액터(downClients 0→스폰 0·reg 0·spine ALL OK). **#60 완결**: 게이트웨이가 산출 뷰를 *실 DownClient 액터*로 전파 → `dc.seenSig == zoneAuthSig`(host 권위 AOI == 클라 뷰·**desync 0**). 손실·migrate·late-join·다중 클라에도 수렴(0344·0346·0348·0350). **🟡 잔여**: DownClient 는 인프로세스(실 OS 프로세스/소켓 spawn=#57).

**메타(구현 품질)**: reg 0 규율 유지. 새 박스 `DownClient`(`client.js` 끝·수신 전용·`topo-actors.js` case 'downclient'·`topo-build.js` downClients opt). 수신 버퍼 유계화(0347·`downRecvWindow` K·전달 후 클라 보유→최근 K·O(K) 상한). >30KB 박스 0(최대 orch-zonebridge 28.4KB·gateway 28.2KB).

## §2 검증 재현 (주장 아님 — 실행 인용)

- `node run.js spine` = **ALL OK**(전 시리즈 src 누적 회귀·exit 0) · `node src/verify.js reg` = ALL OK · `node src/verify.js worldcap`(0350 grand capstone) = **5/5**(downstreamWorldCoherent·dc0/dc1/dc2 convergedTo·isolated).
- **grep 감사**: `Math.random` src 0 · `UObject|...` src 0 · orch→`zone.js` 직접 import 0 · >30KB 박스 0.
- **inline 재현**(현재 `src/` 박스 메서드 직접 구동·시드 [42,7,1234,99,2026]):
  - 0342 수렴: `dc0.seenIds` **[a1]** == `zoneVisibleIds('z1','a1')`(host 권위 AOI == 클라 뷰·desync 0).
  - 0344 손실 하 수렴: `s:a1#2` 손실(dropped 1·resync 1·resent 3)에도 `dc0.convergedTo(zoneAuthSig)` **true**(재전송 복구 후 desync 0).
  - 0350 E2E grand capstone: 2존·3클라·손실·migrate·late-join 뒤 `downstreamWorldCoherent` Y·dc0/dc1/dc2 convergedTo Y·isolated Y(5/5).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #60 | 다운스트림 뷰 게이트웨이→실 클라 *전파* 미연결 | ②/⑤ | ✅ **해소(인프로세스)** — 0342 실 DownClient 액터 수신(전파 종단 spectator→실 클라)·0343 상호 가시 위치 수렴·0344 손실 하 수렴·0345 교차 관찰자 일치·0346 capstone(다중 클라·migrate·손실 뒤 convergedTo)·0348 late-join keyframe·0350 E2E grand capstone(downstreamWorldCoherent). host→게이트웨이→실 클라 **desync 0** 완결 | (해소·인프로세스·원장서 ✅한 줄로 떨굼·실 OS 프로세스는 #57) |
| #57 | zoneHosts/DownClient=orch 인프로세스(실 host.js/클라 OS 프로세스 아님) | ⑤ | 🔴 **유지·최상위 load-bearing** — 다운스트림 전파·수렴 완결로 *인프로세스 E2E* 는 닫혔으나, zoneHost·DownClient 모두 orch 인프로세스 액터(실 OS 프로세스/소켓 spawn 미연결)·`cluster-run.js` 통합 미착수 | **STATE §2 NEXT**(불변·최우선) → 게이트: zoneHost/egress/DownClient 를 실 `host.js`·소켓·`cluster-run.js` 실 spawn 통합(끄면 현 인프로세스=회귀 0) |
| #61 | 업스트림 intent 실 클라 경로(경로1) 미연결 | ②/⑤ | 🟡 **신규** — 다운스트림(경로2)은 실 DownClient 로 닫혔으나, *업스트림*(클라→게이트웨이→존 intent·SPINE §4 경로1)은 entityOps 합성 주입(실 클라 발신 0). 양방향 실 클라가 진짜 E2E | **STATE §3 OPEN GAPS**(무르익으면 §2·#57 과 같은 묶음) → 게이트: DownClient→실 클라(발신+수신)·업스트림 intent 실 발신 |
| #53 | 정합 술어 by-construction(분기 부재) | 검증 | 🟡 **부분 진짜화** — `zoneAuthSig` 비교는 산출원(`zone.visibleFor`)과 같은 소스라 부분 순환 잔존(#53). 단 0344 손실·0346 migrate 는 *진짜 분기*(유실·핸들 이주) 뒤 수렴 단언 | **STATE §3 OPEN GAPS** → 게이트: #57 실 소켓 지연/재정렬이 더 강한 분기 도입 시 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | 검증 | 🟡 **확장(+10 모드)** — downdeliver·dcconv·dcmutual·dcloss·dcagree·dccap·dcwindow·dcjoin·dcreport·worldcap 미승급(현 spine 모드=worldcap 1개·나머지 git per-commit) | **STATE §3 OPEN GAPS** → 게이트: 안정 호 묶음별 스모크 1~2 모드 점진 승급 |

> **🎯 단계 평결(SKILL §3.6)**: **다운스트림 전파 capstone(0341) + 실 클라 수렴 sub-arc 닫힘(0342~0350·E2E grand capstone downstreamWorldCoherent)**. **#60(다운스트림 뷰→실 클라 전파)이 인프로세스에서 완전히 해소** — host AOI 포착(0319~30)→신뢰 전파(0331~41)→실 DownClient 수렴 desync 0(0342~49)→월드 다운스트림 E2E(0350)가 한 사슬로 닫혔다(SPINE §4 경로2 완결·인프로세스). 수렴 검증(정적/상호가시/손실/다중클라/migrate/late-join·desync 0)은 *netcode 핵심 불변(수렴)의 다운스트림 실증*이라 과심화 아님 — 미배선이던 절반의 *마지막 홉*을 세운 **새 능력**. 0347(유계화)·0349(대시보드)는 운영 품질. progress 맵 ⬜/🌱 박스 0 유지. **다음(불변·최상위 load-bearing)**: **#57 실 host.js OS 프로세스/소켓 spawn(zoneHost·DownClient·egress 를 cluster-run.js 실 spawn 통합)** — 이제 인프로세스 다운스트림 E2E 가 완결됐으니, 그걸 *실 프로세스 경계*로 옮기는 게 다음 큰 걸음. 그 위 #61 업스트림 실 클라·#4 진짜 비동기. step-loop 이 원장을 읽어 STATE §2 로 승급.
