# review-0031–0040 — 묶음 감사 (정합성 윈도 해소 → 버스 동적 구독·failover·양경로 무손실 → replay 버퍼 유계·자기-크기조정)

> 범위: step-0031~0040(10개·전부 닫힘·십진 경계 31–40). 서사(왜·이론·진척)는 계층 서사 — *미작성*. 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 본 감사는 *정합성*(척추·재현·이슈)만.
> 호(arc): 정합성 윈도 *해소*(0031 quorum-fill·0032 유계 sweep+retry) → **이벤트 버스 계층(4) 본격화**: 동적 구독/해지(0033)·failover 재협상(0034) → 정리(0035 cluster 5분할) → 버스 failover 무손실 producer replay(0036 결과 경로·0037 요청 경로+reqId dedup) → 정리(0038 topology 분할) → replay 버퍼 유계화(0039 고정 K busWindow)·자기-크기조정(0040 ack 가지치기).

---

## §1 척추 정합성 (SPINE §5 5불변)

이 묶음은 *두 미완 데이터/버스 채무를 닫는다* — ⒜ 0029 가 *감지*만 한 정합성 윈도를 0031/0032 가 *durable 전환*으로 해소하고, ⒝ 0016 이 남긴 ServiceBus 단일점을 0033·0034 동적 구독/failover 로 제거한 뒤 0036·0037 producer replay 로 *무손실*화하고 0039·0040 으로 그 replay 버퍼를 *유계*화한다. 비판적 판정:

- **① 신성한 tick — ✅**. 버스는 여전히 `onTick` 부재(순수 반응형) — 동적 sub/unsub·crash/재협상·producer replay·ack 가지치기·윈도 sweep 이 *전부 존 tick 밖 제어 평면*. 알리바이: `step-0040/svc-bus.js:59`(`crash(){this.topics=new Map()}` — 반응형)·`step-0040/svc-inventory.js:68`(outBuffer push = 결과 발신 중·`onMsg`). 가방 윈도 sweep(0031)·heartbeat(0024)는 *서비스 자체 제어 평면 onTick*(존 net.log 비-기여·`sacred` svcMsgsToZones 0). 재현: 전 step `reg` 에서 존 world 다이제스트가 새 플래그 on/off 에 비트 동일.
- **② 결정론 코어 — 🟡(이원화 지속)**. replay·ack·sweep·dedup 전부 *발신/수신 순서*(시드 함수)의 함수 — 시뮬 `Math.random` **0**(0031~0040 grep). `reqId` = producer-local 단조(클라 op 순서·시드 함수)·`durableSeq`/`windowFills` = 결정론 ack 집계. `engine/panel-kit.js:29` 잔존(이 묶음 코드 아님·이슈 #8). 비트-결정론 VM C++ 대기(#1). 알리바이: `step-0040/gateway.js:25`(reqId 단조)·`step-0032.md §9`(retry=멱등 재-scan 결정론).
- **③ 권위 단일 소유 — ✅(강화)**. 버스는 *파생 라우팅 테이블*만 들고 원장 권위 아님 — crash 는 routing 만 비우고(`svc-bus.js:59`), producer replay 는 *읽기전용 fan-out*(원장 비-침습·전 step `itemConserved`·`ledgerConsistent`). 요청 재발행은 `reqId` dedup(`seenReqs`)으로 *이중 mint 0*(`minted==base` 정확)·`durableSeq` 윈도 해소(0031)가 W 미달 seq 를 durable *전환*해 crash{primary,p4} 에 *전 seq 생존*(0029 는 윈도 소실). 알리바이: `step-0040/svc-inventory.js:22`(durableSeq 워터마크)·`:24`(windowFill 전환).
- **④ 은닉·단일 연결 — ✅(서버간 재협상)**. 동적 sub/unsub·crash/재협상·`svc.item.ack`·producer replay 전부 *서버간 제어 평면*(클라 비가시·전 step `hide` 누설 0). 버스가 *같은 'bus' 주소*로 재시작 → 발행자(gateway/inventory/chat/ranking) *무수정*(주소 무지·`step-0034.md §2`). 알리바이: `step-0040/svc-bus.js:46-47`(런타임 sub/unsub seam).
- **⑤ headless·원격 검증 — ✅**. 전 step `node run.js <NNNN>` 한 줄·`spine` 47-step 비트 사슬·멀티프로세스 14 프로세스 E2E 비트 동일. UE 모듈 링크 **0**(grep). 0035(cluster 5분할)·0038(topology 2분할) 정리가 박스 파일 <30KB 유지(파일=서버 경계 정렬). **단(메타)**: 버스 failover·producer replay 가설(busRestart/busResend/busResendReq/busAck)은 *인프로세스 가설 모드*로만 검증(`cluster.js` 무수정·멀티프로세스 경로 미주입) — 멀티프로세스 버스 failover 는 미검증(이슈 #9 멀티프로세스 패리티 갭의 확장). 알리바이: `step-0035/cluster-core.js`(5분할·verbatim).

**회귀 0/정리 규율(메타)**: 0031~0040 전 step `reg` 25/25(windowFill·wfWindow·busReSub·busRestart·busResend·busResendReq·busWindow·busAck 전부 OFF=직전 비트 동일). 두 정리 step(0035·0038)이 비대 박스(cluster 45KB·topology 31KB)를 *verbatim 분할*로 <30KB 유계화(reg 0). **직전 묶음 리뷰(0021-0030 §3)가 "후속 게이트가 메움"으로 기록한 ① ServiceBus 단일점·정적 구독 → 0033/0034 ② 정합성 윈도 해소 → 0031/0032 가 *바로 이 묶음에서 실제로 지어졌다*** — 리뷰 전방 연결의 도착 확인(앞 리뷰가 가리킨 게이트가 이번 묶음).

> **메타(두 🟡 = 위반 아닌 연기된 채무)**: ② 결정론 이원화·broker lockstep(#4·STATE 🔴 최우선) 모두 매 step §9 에 명시. 신규 척추 위반 0.

---

## §2 검증 재현 (실행 출력 인용)

대표 step `node run.js <NNNN>` **ALL OK** — 회귀 0·결정론 전파·권위 보존·수렴을 직접 재현(문서 PASS 주장을 베끼지 않음):

| step | 핵심 수치(라이브 재현) |
|---|---|
| 0031 | ALL OK · 윈도 해소: OFF durableSeq T−1/윈도 23~24 → ON total−1/윈도 0·fills 46~48·crash{prim,p4} ON 전 seq 생존·dupe 0 |
| 0032 | ALL OK · fill 손실 retry: 무계·유계 K=8 둘 다 durableSeq total−1·fills 2×F0(92~96·retry 발동)·dupe 0·crash 생존 |
| 0033 | ALL OK · 동적 unsub@15→re-sub@18: audit gap 손실(A 60→30→42)·공동 구독자 ranking 비트 동일·발행자 무수정 |
| 0034 | ALL OK · 버스 crash@12→재협상@14: subN 3→0(crashOnly)→3(recover)·gap desync 6·원장 conserved/consistent |
| 0036 | ALL OK · 결과 경로 producer replay: recover desync 6 → resend **6→0**·outResends 18·원장 비-침습 |
| 0037 | ALL OK · 요청 경로 producer replay+dedup: minted base/recov/resendReq **51/33/51**·inResends 36·이중 mint 0(==base) |
| 0039 | ALL OK · 유계 K: bnd(K=24≥gap) minted==base·desync 0 vs tiny(K=8<gap) minted−10·desync 4(바운드 load-bearing) |
| 0040 | ALL OK · ack 자기-크기조정: peak unbnd 60/fixed8/ack 48·idle drain 0·가동길이 무관(unbnd 60→180 vs ack 고정)·minted==base |

- **회귀 사슬(골든 등가)**: `node run.js spine` → **`결과: ALL OK (47 step 비트 사슬 통과)`**(라이브). 묶음 10 step 자동 모드 전부 OK — 두 정리 step(0035·0038)·버스 failover·윈도 해소가 0001~0030 회귀를 1비트도 안 깸의 증거.
- **정리 step 재현**: 0035 `node run.js` ALL OK(cluster 45KB→진입점 1.3KB+부품 4·최대 19.7KB)·0038 ALL OK(topology 31KB→17.1KB+topo-build 15.9KB)·멀티프로세스 E2E 비트 동일.
- **grep 감사**: 시뮬 `Math.random(` 실호출 → step-0031~0040 박스 **0건**(`engine/panel-kit.js:29` 1건은 이 묶음 코드 아님·이슈 #8). `UObject|CoreUObject|GameplayTags` → **0건**(⑤). 버스 `onTick` 부재(svc-bus 순수 반응형)·재발행/ack 전부 제어 평면.

---

## §3 이슈 → 후속 step 연결 (전파 고리)

> 먼저 이월 원장(#1·#3·#4·#6·#7·#8·#9) 재점검 — 이 묶음 코드가 해소한 이슈를 ✅떨군다. (이 묶음은 버스/데이터 윈도 작업이라 이월 6건 중 직접 해소 0 — 전부 범위 밖.)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #1 | 결정론 이원화(C++ 승격 대기) | ② | 🟡 유지 | **STATE §3** → ISimCore 뒤 C++ HktCore 승격. 이 묶음 미착수 |
| #3 | cross-zone AOI 지연 | ④/성능 | 🟡 유지(미착수) | **STATE §3** → 증분 ghost·heartbeat 적응. 이 묶음 버스/데이터만 |
| #4 | broker lockstep = 강제 동기 | ① | 🟡 유지(load-bearing·🔴 최우선) | **STATE §2** → 비동기 실행+논리/벡터 클럭. 매 step §9·STATE 🔴 |
| #6 | 동적 경계·N 존 오케스트레이션 | ③/성능 | 🟡 유지(미착수) | **STATE §3** → 오케스트레이터 동적 배치. (0033/0034 는 *버스* failover·존 오케스트레이션 아님) |
| #7 | 채팅 best-effort — 홉 ack/resend 부재 | 서비스 | 🟡 유지 | **STATE §3** → 채팅 홉 NAK/resend. 이 묶음 가방/버스 홉만 |
| #8 | `engine/panel-kit.js:29` Math.random | ②/정리 | 🟡 유지(경미) | **STATE §3 OPEN GAPS** → 시드/결정론 id 교체 |
| #9 | give-resend 견고성 + 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 유지(범위 확장) | 0025 클라 give-resend 의 in-order 의존(미해소) **+ 이 묶음 버스 failover replay(busResend/busResendReq)도 인프로세스 가설 모드만**(`cluster.js` 무수정·멀티프로세스 버스 failover 미검증). **STATE §3** → opSeq 키 + 멀티프로세스 failover 핸드셰이크 |
| #10 | **give 요청 × result-ahead 결합 미해소(transfers≠base)** | 서비스/버스 | 🟡 **신규** | 0037/0039/0040 §9: 버스 gap 에 떨군 give 요청 재발행 시, 결과 미수신 클라가 같은 아이템을 *재-give* → 원 give 재도달 시 owner 바뀌어 실패(`failedOps`) → *다른 유효 결과*로 수렴(원장 자기-정합·desync 0 이나 transfers≠base). **목적지 STATE §2/§3**(이미 "give×result-ahead 0037 §9" 추적) → 게이트: 0025 클라 give-resend × 버스 요청 replay 통합 복구 |

**이미 후속 게이트가 메운 신규 이슈(기록만·원장 비축적)**:
- **결과 경로 outBuffer ack-가지치기 부재**(0040 §9 ⒜ — outBuffer 는 여전 고정 busWindow) → **0041 busOutAck**(게이트웨이 outSeq ack→가방 outBuffer 가지치기)가 메움(STATE §7 ✅).
- **`seenReqs`(가방 dedup 집합) 무계 성장**(0040 §9 ⒝) → **0042 busSeenBound**(게이트웨이 inAcked 역워터마크) + **0047 busSeenNs**(per-producer 워터마크)가 메움(STATE §7 ✅).
- **적응형 K/sweep 주기**(0031/0032/0039 §9 — 고정 K·고정 wfPeriod) → STATE 백로그(적응형 창·give×result-ahead·적응형 sweep) 추적 중(미해소).

**전파 요지**: 이 묶음은 *이벤트 버스 계층(4)을 단일점에서 분산-가능·무손실·유계로* 끌어올리고(0033~0040), 데이터 계층의 정합성 윈도를 *durable 전환*으로 닫았다(0031/0032). **직전 두 리뷰가 전방으로 가리킨 게이트(ServiceBus 단일점→0033/0034·윈도 해소→0031/0032)가 바로 이 묶음에서 도착** — 리뷰→step 전파 고리의 양방향 실증. 신규 #10(give×result-ahead)만 원장 이월 — 좁은 결합 사각이라 원장 자기-정합·desync 0(손실 아닌 *다른 유효 결과*). #9 는 *멀티프로세스 버스 failover 미검증*으로 범위가 확장됨. 남은 큰 부채는 여전히 **#1(C++ 이원화)·#4(lockstep 해제·🔴 최우선)**. 신규 척추 위반 0. **0031~0040 전 닫힌 step 감사 완료(넷째 묶음).**
