# review 0121–0130 — 거래소↔가방 2-서비스 saga 신뢰 전달 (#31 해소)

> 범위: 닫힌 step 0121~0130. *무엇을 했나*의 박스별 진행은 [progress/](progress/)(3-services 갱신). 이 문서는 *정합 감사*만 — 척추 5불변 + 재현 + 이슈→후속 연결. 권위: 실물 `../src/*.js` + `node run.js` 재현(주장 아님).

묶음 골격: **이전 묶음의 #31(give 낙관적·결과 미수신·2-서비스 원자성 부재)을 한 호로 닫는다** — 피드백 채널(0121)→실패 보상/saga 롤백(0122)→보상 발행(0123)→정리 분할(0124)→회신 손실 감지(0125)→재전송+멱등 dedup(0126)→dedup 유계화(0127)→회계 정합 불변(0128)→자동 재전송(0129)→give↔transfers 교차 정합 capstone(0130). 세 정합 층 합류: 물리(0120 open≡escrow 소유)·회계(0128 gives==acked+pending)·교차 transfer(0130 giveOks==escrowXfers).

## §1 척추 정합성 (SPINE §5 5불변)

| 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|
| ① 신성한 tick | ✅ | saga 전 경로 `onMsg`/내부 메서드·서비스 평면 net.send 만. `_custody` give 1발(`src/svc-exchange-core.js:65`)·item_result 회신 수신(`src/svc-exchange-txn.js:12`)·재전송(`:34 exchRetry`·`:53 autoRetry`)·가방 회신 `_sagaReply`(`src/svc-inventory-txn.js:90`)·dedup(`:65`). `grep "await\|fs\.\|readFileSync" src/svc-exchange-core.js src/svc-exchange-txn.js src/svc-inventory-txn.js` = **0 hit** — tick 경로 동기 I/O 0. autoRetry 는 주입 `exchSweep` 반응(존 tick 무관·`:51`). |
| ② 결정론 코어 | ✅(🟡 #29 불변) | `grep Math.random src/` = **0 hit**. gid 단조(`core.js:71`)·pending Set·sagaResults Map·escrowXfers 카운터 전부 결정론. **메타(번들 내 자체수정)**: 0126 dedup 키 구분자가 실수로 null 바이트였다(store/lookup 자기 일관이라 0126 동작 무해)·0127 가 prune 키와 정합 위해 공백으로 통일(`inventory-txn.js:13,65` 둘 다 `' '`·동작 불변·키는 어떤 digest 에도 미포함). 🟡 #29(공지 epoch 단일클럭) 이번 묶음 미접촉. |
| ③ 권위 단일 소유 | ✅ **강화(멱등·#31 해소)** | **이 묶음의 머리.** 0117~0120 의 escrow give 가 *낙관적 fire-and-forget*(#31)이었다 — 이 호가 *닫힌 고리*로 만든다. ⒜ give 결과 수신(`txn:12`)+회계 집계(giveOks/giveFails) ⒝ list 인출 실패 시 보상 롤백(`txn:23` listing abort·저널 정합 `core.js:111`) ⒞ **멱등 dedup**(`inventory-txn.js:65` (replyTo,gid) 키·이미 처리면 재실행 0·저장 결과 재회신)이 재전송의 *재실행*을 막는다 — dedup OFF 면 재전송→owner≠from 재실행→ok:false 오판→보상이 valid 매물 abort(안전 위반·exsagaretry 가 ON/OFF 대조 증명). ⒟ 교차 정합 `escrowXfers==giveOks`(`inventory-txn.js:74`·거래소 요청 회계 ≡ 가방 실행 회계·1:1) + 회계 닫힘 `sagaConsistent` `gives==acked+pending`(`core.js:119`). 권위가 **정확히 한 번** 옮겨짐을 양 서비스서 증거. |
| ④ 은닉·단일 연결 | 🟡 watch(#31-잔여) | abortPublish→audit 는 버스 무수정 구독 OK(`topo-build.js:182`). **🟡 두 긴장 유지**: ⒜ saga 피드백 채널(item_result·saga_done)은 거래소↔가방 *직접 주소*(replyTo=this.addr·`core.js:72`)·버스 아닌 명시 인터페이스(0117 `inv: inventoryAddr` 직접 결합과 동형·수용 가능하나 버스화 미완) ⒝ 재전송 트리거 `exchRetry`·`exchSweep` 는 topology 테스트 seam(`exchangeOps`)이지 클라→게이트웨이 E2E 아님 → ④ 긴장 + #9 로 흡수. |
| ⑤ headless·원격 검증 | ✅(🟡 #9) | `grep UObject\|CoreUObject\|GameplayTags src/` = **0 hit**·Node 전용. `node run.js spine` 130-step ALL OK. **🟡**: `grep "exchange\|saga\|escrow" src/host.js` = **0 hit** → saga 피드백·재전송·dedup 전부 인프로세스 전용(멀티프로세스 미배선) → #9 확장. |

**메타(회귀 0 규율)**: 10 조각 전부 OFF 플래그 + 가드 — `exchSaga`(`core.js:47`·`_custody:70` saga 분기)·`exchCompensate`(`txn:23`)·`abortPublish`(`txn:27`)·`sagaDedup`(`inventory-txn.js:65` sagaKey null 가드)·`sagaDedupBound`(`txn:17`)·`autoRetry`(`txn:53`)·`exchRetry`/`saga_done` op 부재(reg 시나리오 거래소 OFF). reg ALL OK = src=baseline 비트 동일. **박스 크기**: 0124 정리 분할(svc-exchange 32.4KB→core 12.5+txn 7.0+entry 1.1)·이후 saga 필드로 **core 19.4KB**·txn 8.8KB(전부 ≤30KB). 단 **topo-build 32.8KB·topology 31.5KB 여전 >30KB**(saga 옵션 6개 추가가 wiring 비대 가중) → 신규 #34.

## §2 검증 재현 (직접 실행·주장 아님)

- **`node run.js spine`** → **ALL OK** (130-step 누적 회귀 — 전 역사 불변이 *현재 src 코드*서 하나도 안 깨짐). seed42 월드해시 `0x7a122947` 보존.
- **`node src/verify.js reg`** → **ALL OK** (전 플래그 OFF=src=baseline 비트 동일·saga 전 경로 휴면).
- **`node src/verify.js exsagacap`**(현재 step 0130 가설) → **ALL OK** — giveOks 9 == escrowXfers 9 == 가방 transfers 9(요청 회계≡실행 회계)·minted 5·open==escrow `["item4"]`·conserved·sagaConsistent 전 시드.
- **grep 5불변**: `Math.random` 0 · `UObject|CoreUObject|GameplayTags` 0 · `await/fs`(saga 3박스) 0 · `host.js` exchange/saga/escrow 0(인프로세스 전용).
- **#16 알리바이**: 0121~0130 의 10 가설 모드(exsagaack·exsagacomp·exabortpub·exsplit·exsagapend·exsagaretry·exsagabound·exsagaconsist·exsagaauto·exsagacap)는 각 step verify.js 셸서 1회 단언 후 다음 step 이 교체 — verify-kit ORDER(`engine/verify-kit.js`·~0024 정지) 미승급. spine 은 reg-0 만 보증(ON-의미 미보증 빚 누적).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #31 | 거래소↔가방 give 낙관적·결과 미수신(2-서비스 원자성) | 서비스/버스 | ✅ **해소 0121~0130** | give 결과 수신(0121)+실패 보상 saga 롤백(0122·`txn:23`)+멱등 dedup(0126·`inventory-txn.js:65` 재실행 0)+자동 재전송(0129)+교차 정합 capstone(0130 escrowXfers==giveOks)+회계 닫힘(0128 sagaConsistent). dedup 없는 재전송이 valid 매물 오보상함을 exsagaretry 가 ON/OFF 대조 증명. **원장서 제거·기록만.** |
| #34 | wiring 박스(topo-build/topology) 30KB 초과 | 정리 | 🟡 **신규(경미·#22 family)** | 0124 가 svc-exchange 만 분할(core/txn/entry)·saga 옵션 6개(`topo-build.js:36-41`)+inventory opts 1줄 200자+가 topo-build **32.8KB**·topology **31.5KB** 로 밀어올림. **STATE §3 OPEN GAPS**(정리성) → 게이트: topo-build subs/add 블록 분할 또는 topology 액터 구성 추출(정리 step·기능 0·끄면 현 wiring=회귀 0) |
| #35 | saga 자동 재전송 무차별 + 영구 손실 무한 재시도 | 서비스 | 🟡 **신규(경미)** | 0129 §9: autoRetry 가 매 exchSweep 에 *모든* pending 을 나이 무관 재전송(`txn:53`·대역 낭비)·회신 영구 손실 시 재시도 상한 없음(0059 recoverMaxRetries 의 saga 판 부재). saga_done 도 ack 마다 1발(유계 발신이나 자체 ack 0=best-effort). **STATE §3** → 게이트: per-give 마지막-시도-시각 + 재시도 상한→포기 발행(끄면 현 무차별=회귀 0) |
| #30 | 거래소 op 저널 거래소 박스 내(분산 영속 0) | 서비스/데이터 | 🟡 **(b) 잔여 유지** | 이번 묶음 미접촉 — 거래소 저널 여전 `core.js:_journal` 내·별 PersistStore/N-replica 0. **STATE §3** → 게이트: 거래소 저널 PersistStore 박스화(끄면 현 단독 영속=회귀 0) |
| #16 | bespoke 가설 모드 미승격 | ②/⑤/정리 | 🔴 **확장 +10(~60)** | 0121~0130 10 모드 추가 미승급. **STATE §3 OPEN GAPS** → 게이트: 안정 호(거래소↔가방 saga) 스모크 1~2 모드 verify-kit 승급(끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장** | +saga 피드백/보상/재전송/dedup(0121~0130) 전부 host.js 0·인프로세스 전용(`grep exchange\|saga src/host.js`=0). **STATE §3** → 게이트: 거래소↔가방 박스 host 배선 + 멀티프로세스 회신 핸드셰이크(현 인프로세스 FIFO=회귀 0) |
| #33 | STATE §7 INDEX 30KB 천장 — 매 step 트림 강제 | 정리/도구 | 🟡 **유지(이번 묶음 10/10 발현)** | 130행째 INDEX·close-step 30KB 예산이 *이번 묶음 매 step* 옛 행 압축 강제(10/10 step 에서 반복·실측). **STATE §3 OPEN GAPS** → 게이트: INDEX 묶음당 1행 압축(전문 reviews/) 또는 예산 상향(가독성만·회귀 무관) |

기존 유지(변화 없음·grep 재확인): #1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#32 — 이번 묶음이 거래소↔가방 saga 외 계층 무변경(존·버스·프레즌스·채팅·가방 코어 grep 무변경). #32(시세 피드 발행 완전성)는 거래소 발행 4종 완비됐으나 시세 피드 aborted 미구독(0123 §9·무해)이라 유지.
