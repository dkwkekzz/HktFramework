# review 0201–0210 — 너비 우선 1차: 빈 박스 5종 기본 통신

> 범위: 0201~0210(인스턴스·오케스트레이터 배치·캐시·월드 영속·로그인 큐 — *빈 박스 5종*을 기본 통신 선까지). 인과 서사·서버별 기능 리스트는 [progress/](progress/) 각 계층 파일. 이 문서는 *정당성 감사*(척추·재현·이슈)만.

## §0 단계 평결 (너비 우선 — SKILL §3.6)

- **빈 박스(직전 progress 맵)**: 인스턴스 ⬜·캐시 ⬜·로그인 🌱·오케스트레이터 *배치* 미완·월드 영속 미착수 — 5종.
- **이번 묶음 판정 = ✅ 너비 충족(과심화 0).** 0201~0210 이 그 5종을 *각각 기본만*(최소 연산 + 척추 5항 + reg 0) 세웠다. **한 박스도 더 깊게 안 팠다** — 0107~0200 과심화 궤적의 정반대. 각 박스 2 step(spawn/despawn·place/query·set/read-through·append/replay·enqueue·expire) = 목적 달성 최소 연산에서 멈춤(영속·failover·escrow·saga capstone 0).
- **🎯 1차 너비 완료**: progress/README 박스 맵에 ⬜/🌱 박스 0(전 박스 🟡↑). SPINE 6계층 모든 박스가 "기본 통신 가능" 도달.
- **다음 방향**: **2차 고도화 개시 가능**(너비 충족). 단 *먼저 정리 step* — wiring 박스 30KB 초과(#49)가 다음 기능 step 전 분할 트리거. 2차 후보는 §3 원장(승급은 step-loop).

## §1 척추 정합성 (SPINE §5 5불변)

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 신규 5박스 전부 onTick 없음·순수 onMsg 반응(grep `onTick\|await\|fs\.` in instance/cache/worldlog/loginqueue = 주석 외 **0 hit**). spawn/set/append/enqueue 다 존 tick 밖 제어 평면. orch placeZone 도 onMsg(`orchestrator.js:66`). |
| ② | 결정론 코어 | ✅ | grep `Math.random` in 신규 5박스 = **0 hit**. 상태=주입 op 시드 순서 결정(active/placement/store/journal/queue). worldlog replay reducer 결정론(`worldlog.js:_apply`)·5/5 시드 동일 digest 0x7de275ff. |
| ③ | 권위 단일 소유 | ✅ | 각 박스가 자기 SSOT 단일 권위: instance.active(중복 spawn 멱등 `instance.js:_spawn`)·orch.placement(재배치 덮어씀)·worldlog.journal(append-only 유일 쓰기 경로·SPINE §4 ①)·loginqueue.admitted. 캐시는 *파생 핫뷰*(권위=소스·read-through 사본·`cache.js:onMsg miss`). |
| ④ | 은닉·단일 연결 | ✅ | 신규 박스 명령은 전부 주입 seam(instanceOps/placementOps/cacheOps/worldOps/loginOps)이 orch/gateway/zone 발신(클라 직접 0)·질의는 request/reply(placeQuery→placeReply `orchestrator.js`·cacheGet→cacheReply). 타 박스 내부 직접 접근 0. |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | grep `UObject\|CoreUObject\|GameplayTags` in 신규 5박스 = **0 hit**. `node run.js 0210`·`spine` Node 헤드리스 증명. 단 5박스 멀티프로세스 배선 0(host.js 0·*Ops run-loop/post-run seam) → #9 확장. |

**메타(구현 품질)**: 회귀 0 규율 — 매 step OFF 플래그(instanceService·cacheService·worldLog·loginQueue·placementOps 부재)로 직전 step 비트 동일(reg ALL OK 10/10·spine ALL OK). **박스 유계 위반(신규 이슈 #49)**: 10 너비 step 의 배선이 wiring 박스에 누적 — **topo-build 31.2KB·topo-run 35.9KB(둘 다 >30KB 예산)**. 신규 박스 자체는 4~5KB(건전). STATE §7 INDEX 천장(#33)은 0201 에서 0001~0100 묶음 압축으로 완화(31→24KB).

## §2 검증 재현 (실행 출력 인용)

- **`node run.js 0210 loginexpire`** — 5/5 시드 OK·p1(issuedAt4+ttl2≤7) 만료·p2(6+2>7) 생존·admitted 1·expired 1.
- **`node run.js 0210 reg`** — ALL OK(loginQueue 미제공 → 박스 0 → 0209 비트 동일).
- **`node run.js spine`** — **ALL OK**(210-step 누적 회귀 사슬·신규 5박스 OFF 휴면 비트 동일 확증).
- **grep 감사**: `Math.random` 0·`UObject`/UE 모듈 0·신규 박스 `onTick`/동기 I/O 0(주석만) — 5불변 위반 탐지 0.
- **각 step 가설 재현**(닫을 때 1회 단언·#16): instancespawn active 3/spawns 4·instancedespawn active 2/retired 1·zoneplace placed 2/zone1 hostC·zonequery rx 2/sent 2·cacheset size 3·cachereadthrough hits 2/misses 2·worldappend 길이 4·worldreplay 무손실 0x7de275ff·loginqueue 큐 2/admit 1·loginexpire p1 만료/p2 생존.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| **#49** | **wiring 박스 30KB 초과(topo-build 31.2KB·topo-run 35.9KB·10 너비 step 배선 누적)** | 정리 | 🔴 **신규·트리거** — `topo-build.js`(파라미터+add 9개)·`topo-run.js`(handle+Ops seam+return 5박스)가 예산 초과. #34c(topo-run) 심화 + topo-build 재초과. close-step src size 미체크(#43)라 수동 발견 | **목적지 STATE §2 NEXT**(정리성·다음 기능 step 전 게이트) → 게이트: per-tick Ops 핸들러→`topo-inject.js` 추출 + topo-build 박스-add 분할(기능 0·verbatim·reg 0) + close-step src 체크(#43) |
| #9 | 멀티프로세스 패리티 갭(host.js 0) | ③/⑤ | 🟡 **확장(+신규 5박스)** | instance/cache/worldlog/loginqueue/orch placement 전부 인프로세스 seam(host.js 0·crash/replay post-run) | STATE §3·게이트: 신규 박스 멀티프로세스 핸드셰이크(2차) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🟡 **확장(+신규 10 모드)** | instancespawn~loginexpire 10 모드 단일 src 재실행 불가(0209 모드는 0210 으로 교체) → spine 은 reg-0 만 보증 | STATE §3·게이트: 안정 박스(instance·cache·worldlog·loginqueue) verify-kit 승격(스모크·reg 0·2차) |
| #33 | STATE §7 INDEX 30KB 천장 | 정리/도구 | 🟡 **완화** — 0201 이 0001~0100 을 묶음 10행으로 압축(reviews/ 가 전문)·STATE 31→24KB. 0101~0210 은 1행/step 유지 | STATE §3·게이트: 추가 누적 시 0101~ 묶음도 압축(가독성·회귀 무관) |
| #46·#48 | 길드 금고↔가방 escrow·인출 권한 | 서비스 | 🟡 **2차 대기 유지** — 너비 완료로 *이제 2차 개시 가능*(0191~0200 재리뷰서 강등) | STATE §2(2차)·게이트: guildBankInv(0117~0120 동형) |

> 그 외 이월(코드 미변경·다음 묶음 재점검): #1(결정론 이원화·월드 영속 0207~0208 이 #1 결정론 복제와 합류 지점)·#3·#4(진짜 비동기·2차 최우선)·#6·#7·#8·#29·#30b·#34c(↑#49 로 흡수)·#37·#38·#39·#43(#49 와 동반)·#44·#45·#47 유지.
