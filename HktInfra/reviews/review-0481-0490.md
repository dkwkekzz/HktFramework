# review 0481–0490 — #16 승급 라운드 2차: 시대별 grand capstone verify-kit 누적 회귀 항구화

> 범위: step-0481~0490 (검증 인프라 라운드·서버 기능 0). 인과 서사·진행 지도는 [progress/README.md](progress/README.md) "다음 큰 걸음 — #16 승급 라운드 2차". 이 문서는 *정당성 감사*(척추 5불변·재현·이슈)만.
>
> 판정 권위 = 실물 코드(`engine/verify-kit.js`·`src/verify.js`) + `node run.js`/`spine` 재현 + grep. 문서 PASS 를 베끼지 않는다.

이 묶음은 **박스 코드를 0줄도 수정하지 않는다** — 감사(2026-07-02) §2 최우선 권고 ①의 집행으로, 각 시대 arc 의 grand capstone(월드 다운스트림·#4 async·cluster/coord) 9종을 `engine/verify-kit.js` 누적 회귀(MODES+ORDER)로 승급해 *HEAD 에서 매 spine 재검증*되게 했다. 옛 격차: grand capstone 이 생성 step 의 `verify.js`(git per-commit)에만 살아 HEAD 재검증 불가("수치=verify 출력" 실효 깨짐).

## §1 척추 5불변 정합성

이 라운드는 *검증 하니스*만 강화한다(박스 onTick·전송·권위 로직 무변). 따라서 5불변은 **승급한 capstone 들이 각각 재단언하는 불변을 매 spine 이 항구 검사하게 됐다**는 메타 강화다 — 위반 신설 0.

| 불변 | 판정 | 코드 알리바이 |
|---|---|---|
| ① 신성한 tick | ✅ | 승급 모드는 검증 함수일 뿐 박스 tick 미변경. 승급된 `worldcap`/`clusterdatacap` 등이 존 tick 내 시뮬 외 작업 부재를 매 spine 재확인(`engine/verify-kit.js:985`·`:1049`). |
| ② 결정론 코어 | ✅ | `Math.random` src 실호출 0(grep·주석만). 승급 capstone 전부 시드 [42,7,1234,99,2026] 재현·world/canonical digest 대조(`mze2ecap:882`·`nete2ecap` canonical fold·`asynce2ecap` clock0). |
| ③ 권위 단일소유 | ✅ | 승급 capstone 이 소유 보존을 매 spine 단언 — `upce2ecap`(exactly-once·회계 enterPos+Σmove==실 존·`:1012`)·`clusterdatacap`(migrate 상태 보존+hostA release·`:1049`)·`coordmergecap`(warm-failover 후 a1 보존·`:1094`). |
| ④ 은닉·단일연결 | ✅ | engine→src 직접 import 없이 `src/verify.js` 셸이 cluster deps 를 ctx 로 주입(`verify-kit.js:55` 파괴적 결합 회피). 승급 `worldcap` 이 게이트웨이 격리(`gatewayDeliveryIsolated`)를 매 spine 재확인. |
| ⑤ headless·원격검증 | ✅ | UE/UObject 링크 0(grep). cluster capstone 4종(upce2ecap·clusterdatacap·coordmergecap·coordcap)이 실 `host.js` child_process 를 spawn 해 원격-검증 경로를 *매 spine* 항구 구동(`:1012`~`:1147`). |

**reg 0 알리바이 (구조적)**: `engine/`·`src/verify.js` 는 `baseline/` 스냅샷 대상이 아니다(`ls baseline/verify-kit.js` → 없음·`engine/new-step.js:42` 가 verify.js/panel.js 제외·engine 은 baseline 밖). 따라서 검증 모드 추가는 `net-core`+박스 비트를 건드리지 않아 reg 0 자명. 매 step `node src/verify.js reg` = ALL OK 로 확인.

**메타(하니스 품질)**: `promoted16`(`:1147`)이 승급 9종의 ORDER/MODES 등록을 매 spine 단언 → 향후 우발 제거를 회귀로 잡는다("no silent cap" 자기-가드). 박스 파일 유계(>30KB 0) 불변·엔진 승격분(verify-kit) 크기만 성장(하니스는 30KB 예산 대상 아님).

## §2 검증 재현 (실행 출력 인용)

- `node run.js spine` = **ALL OK** (전 역사 불변 + 승급 9 capstone 을 현재 src 에 단언·background 실행).
- `node src/verify.js promoted16` → 9/9 OK (mze2ecap·bare2ecap·nete2ecap·asynce2ecap·worldcap·upce2ecap·clusterdatacap·coordmergecap·coordcap 전부 ORDER∧MODES 등록).
- 대표 재현(승급 전 현재 src 에서 5/5 확인·편입): `mze2ecap` 5/5(world==lockstep·유계 span3<4·handoffs4~15)·`upce2ecap` 5/5(경계 넘어 수렴·exactly-once resends6~18·leave 제거)·`clusterdatacap` 5/5(coherent·migrate 보존·release)·`coordcap` 5/5(maxDesync0·drift heal·coordCoherent).
- grep 감사: `Math.random` src 실호출 **0**(주석만)·`UObject|Core|GameplayTags` **0**·`baseline/verify-kit.js` **부재**(reg-0 구조 알리바이)·ORDER 마지막 줄에 승급 9종+promoted16 등재(`verify-kit.js:1164`).
- 알리바이 라인: 주입 배선 `:55`·함수 정의 mze2ecap `:882`·worldcap `:985`·upce2ecap `:1012`·clusterdatacap `:1049`·coordmergecap `:1094`·coordcap `:1119`·promoted16 `:1147`.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #16 | bespoke 검증 spine 미승급 | 전체/검증 | 🟡 **대폭 진전** — 시대별 **grand capstone 9종 승급 완료**(0481~0490). *잔여*: 서비스 saga capstone(거래소0140·우편0180·길드0200) 재작성 편입 미완 → 서비스 계층 실행 검증 여전히 0. | STATE §2 NEXT(무르익음·감사 §2 ①-b) → 게이트: 서비스 saga capstone 재작성 후 verify-kit ORDER 승급(박스 무수정→reg 0) |
| #75 | 스파인 런타임 성장(cluster capstone child_process ×5 시드) | 검증/도구 | 🟡 **신규(경미)** — 승급 4 cluster capstone 이 매 spine 실 host.js 프로세스를 spawn → spine 벽시계 증가(현 허용·ALL OK). 무계 성장 아님(9종 고정)이나 향후 승급 누적 시 감시. | STATE §3 OPEN GAPS → 게이트: 필요 시 spine 를 fast(reg+대표) / full(전 capstone) 계층화(끄면 현 전량=회귀 0) |
| #53 | 정합 술어 by-construction | 검증 | 🟡 유지 — 승급이 *진짜성*을 바꾸지 않음(cluster capstone 은 실 child_process 분기라 by-construction 아님·자기완결 async 는 합성). 승급으로 재검증 빈도만↑. | STATE §3(기존) |
| #74·#73·#68·#69 | 실 GW 프로세스 분리·완전-ON·entity 컨테이너 동기·동치 transitive | ④⑤/검증 | 🟡 유지(경미·이 라운드 무관) | STATE §3 OPEN GAPS(기존) |
| #46 | 금고↔가방 escrow | 서비스 | 🟡 유지(2차 대기) | STATE §2/§3(기존) |

> **🎯 단계 평결 (0481-0490·SKILL §3.6)**: **#16 승급 라운드 2차 — 시대별 grand capstone 항구화(직전 0471-0480 verdict 의 "#16 승급" 권고 + 감사 §2 최우선 ① 집행)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *검증 인프라 강화*(서버 기능 0·박스 무수정→reg 0) → **과심화 아님**(코박기 아닌 하니스 항구화·10-step 지도가 보는 "옛 검증이 HEAD 재검증되는가" 격차 해소). **재현 증거**: `promoted16` 9/9·`spine` ALL OK·대표 capstone 5/5·`Math.random`/UObject src 0·`baseline/verify-kit.js` 부재(reg 구조 0)·>30KB 박스 0. **#16 대폭 진전(grand capstone ✅·서비스 saga capstone 잔여)**·부수 **#75 신규(spine 런타임 성장·경미)**. **다음 권고: 서비스 saga capstone(거래소/우편/길드) 재작성 편입(#16 잔여·감사 §2 ①-b) → 그다음 #74 실 GW 프로세스 분리·#46 escrow·프로덕션 프로파일.** step-loop 이 STATE §2 로 승급.
