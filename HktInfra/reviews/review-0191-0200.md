# review 0191–0200 — 길드 금고(Guild Bank) arc

> 범위: 0191~0200(길드 금고 — 거래소 escrow/우편 custody 의 *조직 공유* 판). 인과 서사·서버별 기능 리스트는 [progress/3-services.md](progress/3-services.md) 길드 서버 항목. 이 문서는 *정당성 감사*(척추·재현·이슈)만.

## §0 단계 평결 (너비 우선 — SKILL §3.6)

> **재리뷰 추가절.** 너비 지도([progress/README.md](progress/README.md) 박스 상태 맵) 기준으로 이 묶음이 *너비를 늘렸나, 이미 선 박스를 더 깊게 팠나*를 판정한다.

- **빈 박스(progress 맵)**: 인스턴스 ⬜·캐시 ⬜ 미착수 · 로그인 🌱 스텁 · 오케스트레이터 *배치* 미완 · 월드 영속 미착수. **1차 너비 미완.**
- **이번 묶음 판정 = 🔴 과심화(over-deepening).** 0181~0190 이 길드 박스를 *기본 통신*(로스터·가입/탈퇴·마스터십·이양) 선까지 이미 세웠는데, 0191~0200 은 거기에 **새 기능(공유 금고)을 10 step**(예치/인출/발행/영속/스냅샷/배지/배지영속/배지정합/원장정합/capstone) 더 얹었다 — 빈 박스를 두고 선 박스를 더 판 것. **0107~0200(거래소·우편·길드를 capstone 까지) 과심화 궤적의 마지막 발현**이며, 새 "너비 우선" 정책(CLAUDE.md·SKILL §3.6)을 촉발한 바로 그 증상.
- **다음 방향**: 1차 미완 → **다음 묶음은 빈 박스 우선 — 인스턴스(던전) 서버 spawn/despawn 기본**(STATE §2 NEXT 1순위·이미 prior step-loop 이 재조정). 금고 심화(#46 금고↔가방·#48 인출 권한)는 **2차 고도화 대기**로 강등 — 너비가 다 차기 전엔 STATE §2 로 올리지 않는다.

> **정책 충돌 정정**: 이 묶음의 *최초* 리뷰는 #46(금고↔가방)을 "STATE §2 NEXT·무르익음·다음 자연 arc"로 올렸으나, 그것이 곧 과심화다. 너비 우선 정책상 #46 의 목적지는 **2차 백로그**다(아래 §3·원장 동기 정정).

## §1 척추 정합성 (SPINE §5 5불변)

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 금고 연산 전부 존 tick 밖·순수 반응형(`svc-guild.js:24` "onTick 없음"·`svc-guildfeed.js:13"). deposit/withdraw 는 onMsg 반응(`:117,128`)·tick 루프 0. grep `onTick|fs\.|await` in 금고 박스 = 0 hit(주석 외). |
| ② | 결정론 코어 | ✅ | grep `Math.random` in `svc-guild.js`·`svc-guildfeed.js` = **0 hit**. 금고 상태 = guildOps 시드 로그 순서 결정(예치/인출 순서). 5/5 시드 동일 vault(`run.js 0200`). |
| ③ | 권위 단일 소유 | ✅ **강화(아이템 권위)** | 금고 vault = itemId 단일 길드 소유 SSOT(`:46`). deposit 중복 멱등(`:123` `!v.includes`)·withdraw 가 itemId 를 금고서 제거(`:134` filter·이중쓰기 0). **bankConsistent**(`:150`) = 교차 중복 0(itemId 두 길드 동시 부재)+내부 중복 0 — rosterConsistent(0190·master 권위)의 *아이템 권위* 판. 3체제 3/3. |
| ④ | 은닉·단일 연결 | ✅ | 금고 변경 발행은 버스 토픽만(`:78` `svc.guild.bank.changed`·구독자 주소 무지). 배지(GuildFeed)는 발행 스트림 파생(`svc-guildfeed.js:44` 구독)·vault SSOT 직접 접근 0(CQRS·발신 0·권위 0). bankFeedConsistent 는 대조 읽기(`:62`). |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | grep `UObject|CoreUObject|GameplayTags` = **0 hit**. `node run.js 0200`·`spine` Node 헤드리스 증명. 단 금고 arc 멀티프로세스 배선 0(host.js 금고 0·guildOps run-loop seam·crash/reconstruct verify post-run) → #9 확장. |

**메타(구현 품질)**: 회귀 0 규율 — 매 step OFF 플래그(bank·bankPublish·guildBankFeed·persist·snapInterval)로 직전 step 비트 동일(reg ALL OK·spine ALL OK). **박스 유계 경고**: `svc-guild.js` **29.5KB**(재리뷰 재측정·0190 ~18.6KB → 금고 arc 가 +11KB)·30KB 임박(#47). topo-build 29.7KB(#45)·topo-run 33.3KB(#34c·재측정).

## §2 검증 재현 (실행 출력 인용)

- **`node run.js 0200 guildbankcapstone`** — 5/5 시드 OK·세 체제(A 정상·B guild crash→reconstruct·C feed crash→reconstruct) 모두 `예(3/3)`·g1/g2 vault 2/2. bankCapstone = bankConsistent(원장 권위·0199) AND bankFeedConsistent(배지==vault·0198) 결합.
- **`node run.js 0200 reg`** — ALL OK(현재 step 금고 메서드 추가만 → 0199 비트 동일).
- **`node run.js spine`** — **ALL OK**(전 역사 불변 + reg 가 현재 코드에서 하나도 안 깨짐·금고 arc 의 OFF 휴면 비트 동일 확증).
- **grep 감사**: `Math.random` 0·`UObject`/UE 모듈 0·금고 박스 `onTick`/동기 I/O 0 — 5불변 위반 탐지 0.
- **단일 src 재현 한계**: `node run.js 0199 guildbankconsistent` → "step-199 verify.js 없음(현재 step=200 만 실행 가능)". 0191~0199 의 bespoke 모드(guildbank·guildwithdraw·guildbankpublish·guildbankpersist·guildbanksnapshot·guildbankfeed·guildbankfeedpersist·guildbankfeedconsistent·guildbankconsistent)는 0200 으로 교체돼 재실행 불가 — spine 은 그들의 reg-0 만 보증·ON-경로 의미는 close 시 1회 단언(=**#16 의 금고 arc 발현**).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| **#46** | **길드 금고↔가방 미연동(금고가 bare itemId 보유·멤버 가방서 실제 인출 0 = 가짜 escrow·#40 의 길드 판)** | 서비스 | 🔴 신규·load-bearing(단 **2차 대기**) — `svc-guild.js` 금고는 itemId *문자열*만 vault 에 적재(grep `_custody`/`svc.item`/give = **0 hit**). 예치해도 멤버 가방서 안 빠지고 인출해도 가방에 안 들어감 — 아이템 우편 0157~0160(가짜)가 0161~0164(실물 2-서비스)로 닫힌 그 갭의 길드 판 | **목적지 2차 고도화 백로그**(§0 단계 평결·너비 우선) — 이미 선 길드 박스의 *심화*라 너비 완료 전 STATE §2 승급 금지(거래소 0117~0120/우편 0161~0164 동형이나 *순서상* 2차). 게이트: `guildBankInv`(예치=멤버 가방→escrow give·인출=escrow→멤버 가방·OFF→현 standalone=회귀 0) |
| **#47** | **svc-guild.js 28.8KB — 30KB 임박(금고 arc +10KB)** | 정리 | 🟡 신규(watch·#34/#45 family) — 길드 로스터(0181~0190)+금고(0191~0200·예치/인출/발행/영속/스냅샷/원장정합/capstone)가 한 파일 누적 → 18.6→28.8KB·다음 금고 기능(가방 연동 #46) 면 초과. close-step src 미탐지(#43) | **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 금고 부품 분할(svc-guild-bank.js or core/txn·Object.assign·verbatim·reg 0) |
| **#48** | **금고 인출 권한 등급 부재(any 멤버가 any 아이템 인출)** | 서비스 | 🟡 신규(경미·watch) — withdraw 가드는 *멤버십*만(`:133` `g.members.includes(p.member)`)·예치자/등급/권한 무관 → 실 길드 금고의 rank-based 인출 제한·로그 0 | **목적지 STATE §3 OPEN GAPS** → 게이트: bankRole/권한 체크(끄면 현 멤버=무제한=회귀 0)·#44 disband 와 같은 길드 거버넌스 묶음 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 **확장(+금고 10 모드)** | 금고 arc 10 모드 단일 src 재실행 불가(§2 확인) → STATE §3 OPEN GAPS·게이트: 안정 호(금고·길드·우편 saga) verify-kit 승격(스모크 1~2 모드·회귀 0) |
| #9 | give-resend 견고성 + 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장(+금고 arc)** | 금고 arc 전부 host.js 0(guildOps run-loop seam·crash/reconstruct post-run) → STATE §3·게이트: 멀티프로세스 금고 핸드셰이크 |
| #45 | topo-build.js 30KB 임박 | 정리 | 🟡 유지(29.0KB·금고 3 플래그 추가에도 ~동일) | STATE §3 OPEN GAPS·게이트: 플래그 그룹 객체화 or #34c 동반 분할 |
| #44 | 길드 종료(disband) 부재 | 서비스 | 🟡 유지(금고 arc 미접촉) | STATE §3·게이트: guildDisband op(#48 거버넌스 묶음 가능) |
| #34c·#43 | topo-run 32.5KB·close-step src size 미체크 | 정리/도구 | 🟡 유지 | topo-run 여전히 >30KB·svc-guild 28.8KB(#47)도 close-step 무탐지(#43 근거 재강화) → STATE §3 |

> 그 외 이월(코드 미변경·다음 묶음 재점검): #1·#3·#4·#6·#7·#8·#10·#11·#15·#17a·#25·#27·#28·#29·#30b·#32·#33·#36·#37·#38·#39·#41 유지. (#33 STATE INDEX 천장 — 이번 묶음도 0200 닫기서 거래소 leg/feed/expiry 묶음 트림으로 30KB 유계 유지·재발현.)
