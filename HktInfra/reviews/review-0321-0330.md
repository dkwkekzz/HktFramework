# review 0321–0330 — 다운스트림 AOI 뷰 검증 sub-arc 닫힘(#9 후속·capstone downstreamCoherent)

> 범위 0321~0330. 인과 서사(무슨 서버·어떤 기능·왜/어떻게/했나)는 [progress/2-world.md](progress/2-world.md)(존 기능 8·다운스트림 뷰)·정리 분할은 [progress/5-coordination.md](progress/5-coordination.md)(오케 믹스인 다분할 노트). 이 문서는 *정당성 감사*만 — 실물 코드(`src/*.js`)·`run.js`/inline 재현·grep.

## §1 척추 5불변 정합성 (SPINE §5)

- **① 신성한 tick** ✅ — 다운스트림 뷰는 런타임 존 `onTick`(시뮬 산출물)이 만들고, 포착·검증은 *읽기 전용 질의*(`orch-views.js`·tick 무관·동기 I/O·await 0). 버퍼링 싱크(`topo-actors.js:71` `{buf:[],send}`)는 단순 push — tick 안 비-시뮬 작업 0.
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). 런타임 존 시드=`fnv1a(zid)`(`topo-actors.js:70`·run 시드 무관) → 5 시드 동일 위치·동일 뷰(재현: 증분 reset1/update3·exit==[a2]·serializable 439B 가 5 시드 동일).
- **③ 권위 단일 소유** ✅강화 — 뷰는 *권위 존이 자기 세션에만* 산출(존 격리 0326·`zoneViewSessions` 누수 0). 이주 연속성(0327)은 migrate=*같은 핸들*(release+acquire·`_migrate`·`orch-placement.js:18`)이라 세션·prevSeen 보존→뷰 끊김 0·`entityConserved` 참. capstone(0330)이 혼합 lifecycle 뒤 `entityConserved`(total=enters−leaves−lost−discarded=2) 단언.
- **④ 은닉·단일 연결** ✅ — orch→`zone.js` 직접 import 0 hit(grep·`makeActor` 팩토리 주입). 뷰는 *게이트웨이 주소*로 발신(`zoneViewBuf` 의 `to==='gateway'`·재현 확인)·클라 직접 아님. 존 간 뷰 격리(0326·z1 세션={s:a1,s:a2}·z2={s:b1}·교차 0).
- **⑤ headless·원격 검증** ✅(🟡 전파 미연결) — Node 순수·`UObject|CoreUObject|GameplayTags` src 0 hit. 모든 기능 OFF/버퍼 미읽음→reg 0·spine ALL OK(버퍼링 싱크 전역 net 무오염). 직렬화 경계(0325·`zoneViewWire`·JSON round-trip 동일·439B>0)로 뷰가 *소켓 탈 준비*(원격-검증 토대). **🟡**: 게이트웨이→실 클라 *전파* 미연결(#60·실 Client actor 0).

**메타(구현 품질)**: reg 0 규율 유지(매 step OFF→직전 비트 동일). **정리 분할(0323·기능 0)**: 뷰 질의 6종(0319~0322)을 `orch-zonebridge.js`(30.6KB>30KB 트리거)에서 `orch-views.js` 믹스인으로 분리(`Object.assign(Orchestrator.prototype, OrchViews)`·`orchestrator.js:41,175`)→orch-zonebridge 28.1KB·orch-views 9.9KB·>30KB 박스 0(#43 가드 통과). `orchestrator.js` 29.7KB(#58 watch·코어 필드 잔존).

## §2 검증 재현 (주장 아님 — 실행 인용)

- `node run.js spine` = **ALL OK**(전 시리즈 src 누적 회귀·현재 코드에 전 역사 불변 단언·exit 0) · `node src/verify.js reg` = ALL OK · `node src/verify.js downstreamcap`(0330 capstone) = **5/5**(downstreamCoherent·hostProcCoherent·entityConserved·total 2).
- **grep 감사**: `Math.random` src 0 · `UObject|...` src 0 · orch→`zone.js` 직접 import 0 · >30KB 박스 0(최대 `orchestrator.js` 29.7KB·`orch-zonebridge.js` 28.1KB·`orch-views.js` 9.9KB).
- **inline 재현 5/5**(현재 `src/` 의 박스 메서드 직접 구동·시드 [42,7,1234,99,2026]):
  - 0321 증분 델타: a1 3 이동 → `zoneViewStats` reset **1**·update **3**·total **4** ≪ 14 tick(매 tick 전체 아님).
  - 0324 exit: a1 접근(enter) 후 멀어짐 → `zoneViewExited(s:a1)`==[a2](동적 가시 상실).
  - 0325 직렬화: `zoneViewWire` serializable **true**·**439B**>0(소켓 준비).
  - 0326 격리: `zoneViewSessions(z1)`==[s:a1,s:a2]·(z2)==[s:b1](누수 0).
  - 0327 이주 연속성: z1 A→B migrate·a1 이동 전후 → update **4**·total entity **1**·런타임 host **hostB**(뷰 끊김 0).
  - 0328 무굶김: a1·a2·a3 enter → `zoneViewAllKeyed` true·세션 3(모두 keyframe).
  - 0329 무손실: `zoneViewConserved` true(모든 frame 한 세션 귀속·고아 0).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #60 | 다운스트림 뷰 게이트웨이→실 클라 *전파* 미연결(sub-arc 마지막 홉) | ②/⑤ | 🟡 **유지·sub-arc 닫힘 후 잔여** — 0319~0330 이 *산출·포착·정확성·증분·직렬화·격리·이주 연속·무굶김·무손실·capstone(`downstreamCoherent`)* 까지 검증을 닫았으나, `gateway-msg.js:43~50` forward 는 `m.from.startsWith('zone')`+`bySession` 바인딩 *실 클라* 한정 → 브리지 합성 세션(`s:a1`·실 Client actor 0)은 전파 대상 부재. 다운스트림 절반의 *마지막 홉* | **STATE §3 OPEN GAPS**(무르익으면 §2) → 게이트: 게이트웨이가 포착 뷰를 실 클라로 forward + 실 클라 하니스(끄면 현 포착-only=회귀 0)·#57 실 프로세스와 같은 묶음 |
| #57 | zoneHosts=orch 인프로세스 논리 컨테이너(실 host.js OS 프로세스 아님) | ⑤ | 🔴 **유지·최상위 load-bearing** — 이 묶음(다운스트림 뷰)은 #57 과 직교(존 데이터 평면의 *내려가는* 절반)·미진전 | **STATE §2 NEXT**(불변) → 게이트: zone 런타임·host inbox 를 실 `host.js` 프로세스/소켓·`cluster-run.js` 실 spawn 통합(끄면 현 인프로세스=회귀 0) |
| #58 | `orchestrator.js` 코어 29.7KB — 30KB 근접 | 정리 | 🟡 **부분 완화** — 0323 정리 분할이 뷰 질의를 `orch-views.js` 로 빼 `orch-zonebridge.js` 30.6→28.1KB(>30KB 0). 단 코어 `orchestrator.js` 자체는 29.7KB(생성자 필드 잔존·미감소) | **STATE §3 OPEN GAPS** → 게이트: 코어 필드 init 을 mixin init 헬퍼로(기능 0·reg 0) |
| #53 | 정합 술어 by-construction(분기 부재) | 검증 | 🟡 **확장(다운스트림 판)** — `downstreamCoherent`(0330)·AOI 정확성(0320·`zoneVisibleIds`==산출 enter 가 *같은* `zone.visibleFor` 비교라 부분 순환)·무손실 회계(0329·세션별 합==frame 수는 분배 정의상 항진)도 같은 인프로세스 구조 정합 | **STATE §3 OPEN GAPS** → 게이트: #60 실 클라 전파/#57 실 프로세스가 진짜 분기(뷰 유실·소켓 지연) 도입 시 술어 load-bearing 화 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | 검증 | 🟡 **확장(+10 모드)** — 이 묶음 모드(hostzonedelta·hostzonemutual·zoneviewsplit·hostzoneexit·hostzoneviewwire·hostzoneisolate·hostzonemigrateview·hostzonekeyed·hostzoneconsv·downstreamcap)도 미승급(현 spine=`downstreamcap` 1개·나머지 git per-commit) | **STATE §3 OPEN GAPS** → 게이트: 안정 호를 묶음별 스모크 1~2 모드로 점진 승급(끄면 현 spine=회귀 0) |

> **🎯 단계 평결(SKILL §3.6)**: **다운스트림 AOI 뷰 검증 sub-arc 닫힘(0319~0330·capstone `downstreamCoherent`)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 2차. 이 sub-arc 는 SPINE §4 경로2 *월드 다운스트림*(0282 이래 no-op 싱크로 *드롭*하던 AOI 뷰)을 처음 세운 **새 능력**(존 박스 기능 8·과심화 아님)·0323 은 정리 분할(기능 0). **단 다운스트림은 *포착·검증* 까지 닫혔을 뿐 *전파*(게이트웨이→실 클라)는 미연결(#60)이고, 최상위 load-bearing 은 여전히 #57(실 host.js OS 프로세스)** — 둘 다 *실 프로세스/실 클라 하니스* 를 부르는 같은 다음 묶음. 다음 권고: **#57 실 host.js OS 프로세스/소켓 spawn(cluster-run.js 통합) + 진짜 비동기(#4)**, 그 위에 #60 다운스트림 전파. step-loop 이 원장을 읽어 STATE §2 로 승급.
