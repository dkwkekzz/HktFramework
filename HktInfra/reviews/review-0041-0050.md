# review-0041-0050 — 묶음 감사 (척추 정합성 · 재현 · 이슈→후속 step)

> 범위 **0041~0050** — *무엇을 했나*는 [progress/](progress/)(박스별 진행·버스 [4-bus.md](progress/4-bus.md)·서비스 [3-services.md](progress/3-services.md)) + [STATE.md](../STATE.md) §7 INDEX. 이 문서는 *정합한가*만(감사 전용). 호(arc): 버스 failover/replay 자기-크기조정(K 추정 폐기) + 소비자 lease lifecycle(시작·축출·재admission·적응) + 정리 2건(0043 가방 분할·0049 src/ 전환).
> **0049 src/ 전환 이후 알리바이는 `src/<box>.js:line`**(복사 전진 폐기 — `step-NNNN/` 스냅샷은 archive 0001~0048 동결·이후는 git 태그).

## §1 척추 정합성 (SPINE §5 5불변)

| # | 불변 | 판정 | 코드 알리바이 / 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 버스 ack/min-워터마크/lease sweep 는 전부 *ack 사건 구동* — 별도 tick·동기 I/O 0. `src/svc-inventory-bus.js:38-45`(sweep 가 `_onOutAck` 안에서 도는 순수 함수). grep: 시뮬 tick 내 `await`/DB 호출 0. |
| ② | 결정론 코어 | ✅ (🟡 메타) | `src/` 실 `Math.random` **0 hit**(시드 PRNG)·전 검증 시드[42,7,1234,99,2026] 재현. **단 `engine/panel-kit.js:29` Math.random 잔존**(브라우저 위젯 id·다이제스트 경로 밖) → #8 이월. |
| ③ | 권위 단일 소유 | ✅ | 이 묶음은 버스/랭킹 경로 — 가방 원장 pickup/give 쌍 거래 *무변경*(reg 25/25 비트 동일). lease 축출/재admission 은 *결과 버퍼 가지치기 정의역*이지 권위 이전 아님(`src/svc-inventory-bus.js:34` evicted delete = min 정의역 복귀, 소유권 무관). |
| ④ | 은닉·단일 연결 | ✅ (🟡 긴장) | 0046 producer 네임스페이스: 가방은 버스 너머라 둘째 게이트웨이를 *실제와 구별 못 함*(은닉 보존·`src/svc-inventory-core.js` 복합키 dedup). **단 둘째 게이트웨이는 버스 producer seam 으로 대표**(클라-대면 풀 토폴로지 아님·0046 §9) → 다중 게이트웨이 토폴로지 미착수(#11). |
| ⑤ | headless·원격 검증 | ✅ (🟡 메타) | `src/`·`engine/` UE 모듈(`CoreUObject`·`GameplayTags`·`UObject`) **0 hit**·`node run.js 0050` 멀티프로세스 14개 E2E 비트동일. **단 버스 failover replay 는 인프로세스 가설 모드만**(멀티프로세스 버스 failover 미검증) → #9 이월(확장). |

**메타 — 복사 전진/박스 유계**: 0043 이 가방(34KB>30KB)을 4분할(core 19.6·persist 11.8·bus 4.3·진입점 1.0, 바이트 동일 reg 0), 0049 가 step 마다 디렉토리 통복사를 폐기(src/ 제자리 + baseline 1벌 회전 + archive 동결) — 디렉토리 누적 압력 소멸. 현 박스 전부 <30KB이나 **`src/svc-inventory-core.js` 29.6KB 천장 근접**(다음 정리 후보).

## §2 검증 재현 (주장 아님 — 직접 실행)

- **`node run.js spine` = ALL OK** — **49 step 비트 사슬**(archive 0001~0048 reg + src 0050 all) 통과. "어느 step 해시도 불변"의 전 시리즈 증거.
- **`node run.js 0050`(busLeaseAdapt) = ALL OK** — 5시드 inv 다이제스트 `0x7a122947`/`0x9a31a1e5`/`0xbccd9849`/`0x99774ef5`/`0x4bc176e1`, 멀티프로세스 2회 == 인프로세스 == world **전부 동일**(결정론)·E2E 14프로세스 비트동일.
- **grep 감사**: `src/` Math.random 0(②)·`src+engine` UE 모듈 0(⑤)·`engine/panel-kit.js:29` Math.random **1 잔존**(#8).
- **각 step OFF=직전 비트 동일**(STATE §7): busOutAck/busSeenBound/busMinWm/busConsumerLease/busProducerNs/busSeenNs/busLeaseLife/busLeaseAdapt 전부 플래그=0 → 직전 step 25/25 비트 동일(회귀 0 규율 일관).

## §3 이슈 → 후속 step 연결 (전파 고리)

**(a) 묶음 내 자체 해소** — 한 step 의 §9 가 *다음* 게이트 step 으로 메워진 사슬(전파 고리가 도는 증거):

| 발견 | 이슈 | 해소 게이트 |
|---|---|---|
| 0040/0041 §9 | outBuffer/inBuffer/seenReqs 무계 성장 | ✅ 0041 busOutAck · 0042 busSeenBound (ack 워터마크 양방향 가지치기) |
| 0041 §9① | 단일 워터마크가 둘째 소비자(ranking) 늦은 복구 못 받침 | ✅ 0044 busMinWm (모든 소비자 frontier 의 min) |
| 0044 §9 | 영구 죽은 소비자가 min 고정 → outBuffer 무계 | ✅ 0045 busConsumerLease (침묵 기반 축출) |
| **리뷰 §2/§3**(0045 빈틈) | lease 시작-시점 죽음·축출 비가역 | ✅ 0048 busLeaseLife (지연 baseline + 재admission) |
| **리뷰**(ON×ON 잠복) | busProducerNs(0046)×busSeenBound(0042) 조합 무계 회귀 — per-step reg 0 으론 못 잡음 | ✅ 0047 busSeenNs (per-producer seen 워터마크 + seenns 조합 검증 1급화) |
| 0048 §9 | 고정 leaseSpan 임계(작으면 오축출·크면 늦은 drain) | ✅ 0050 busLeaseAdapt (관측 cadence 로 self-size) |

> **요지**: 이 묶음은 *리뷰가 잡은 두 결함*(0045 lease lifecycle 빈틈 §2/§3 → 0048, ON×ON 잠복 회귀 → 0047)이 후속 step 으로 흘러 메워진 모범 구간이다. per-step reg 0 만으론 ON×ON 조합 회귀를 못 잡는다는 0047 §9 교훈은 검증 방법론 자체의 자산.

**(b) 미해소 — 원장 이월**(목적지·게이트·arc 정합):

| # | 이슈 | 항 | 상태 | 목적지 → 게이트 |
|---|---|---|---|---|
| #11(신규) | per-producer ack 워터마크 + 다중 게이트웨이 풀 토폴로지 | ④/⑤ | 🟡 — 0046~0048 §9: dedup *네임스페이스*만 분리, ack/seen 의 producer 키잉·클라-대면 풀 와이어 미착수 | STATE §3 → producer 키잉 ack/seen + 게이트웨이 군 배치(단일 게이트웨이라 미발현=회귀 0) |
| #12(신규) | cadence EWMA 감쇠 · 시작 grace prior · bootstrap 1회 오축출 | ④ | 🟡 — 0050 §9: consumerMaxGap 단조(감쇠 0)·prior 없어 첫 G-침묵 1회 오축출(ev=1) | STATE §3 → EWMA 감쇠 + 시작 cadence prior(끄면 현 단조=회귀 0) |
| #4 | broker lockstep = 강제 동기(비동기 결정론) | ① | 🔴 유지 — 이 묶음 미착수 | STATE §2 → 논리/벡터 클럭(🔴 최우선·broker 대공사) |
| #9 | give-resend 견고성 + 멀티프로세스 패리티 | ③/⑤ | 🟡 유지(확장) — 버스 failover replay 도 인프로세스 가설 모드만 | STATE §3 → 멀티프로세스 failover 핸드셰이크 |
| #10 | give 요청 × result-ahead 결합(transfers≠base) | 서비스/버스 | 🟡 유지 — 이 묶음 미착수 | STATE §2/§3 → give-resend × 버스 요청 replay 통합 |
| #8 | `engine/panel-kit.js:29` Math.random | ②/정리 | 🟡 유지 — 코드 재확인 잔존 | STATE §3 → 시드 카운터 id |
| #1·#3·#6·#7 | 결정론 이원화·cross-zone AOI·동적경계·채팅 신뢰 | — | 🟡 유지 — 이 묶음(버스/lease) 미착수 | 원장 §2 유지 |
