# review 0301–0310 — host 프로세스 컨테이너 arc (실 host.js 물리 분리 씨앗·#9 잔여)

> 범위 0301~0310 · 인과 서사(무슨 서버·기능 리스트)는 [progress/5-coordination.md](progress/5-coordination.md) 오케스트레이터 기능 11. 이 문서는 *정당성 감사*만(척추 정합·재현·이슈).

---

## §1 척추 정합성 (SPINE §5 5불변)

이 묶음은 0291~0300(게이트웨이 직접 라우팅)이 남긴 잔여 — zone-host 핸들이 orch 인프로세스 *flat* `zoneRuntimes` Map·host=문자열 태그 — 를 메우려, host 를 **1급 프로세스 컨테이너**(`zoneHosts`)로 묶었다. 코디네이션 박스(`orchestrator.js`·신규 `orch-hostproc.js`·`orch-zonebridge.js`)·엣지(게이트웨이 디렉토리는 무변경) 심화. 새 박스 0(분할 산물 `orch-hostproc.js`).

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | host 컨테이너 유지(`_hostSet`)는 배치 집행 경로(tick 밖·`orch-hostproc.js:16`)·host 자기 루프 drain·tick 은 onTick 경계 배치(`orch-zonebridge.js:142` `_tickRuntimes`·시뮬 외 작업 0) |
| ② | 결정론 코어 | ✅ | `Math.random` src 0 hit(grep)·자료구조 귀속만·직렬화 round-trip(JSON)·시드 5종 동일 결과(reg·capstone) |
| ③ | 권위 단일 소유 | ✅**강화** | host 컨테이너 단일 귀속(`_hostSet` 어디 있든 떼고 한 곳에·`:16`)·`zoneHostSingleOwner`(`:31`)·**inbox stale 거부가 이주 후 이중 쓰기 차단**(`orch-zonebridge.js:147` `if(!c.zones.has(zoneId)){zoneHostStale++;continue}`·권위의 *프로세스* 판) |
| ④ | 은닉·단일 연결 | ✅ | host inbox·컨테이너는 orch 내부(클라 무관)·orch→`zone.js` 직접 import 0 hit(EntityZone 은 `makeActor` 팩토리 주입) |
| ⑤ | headless·원격 검증 | ✅(🟡 한계) | Node 순수·reg 0·UObject/UE src 0 hit. **단 `zoneHosts` 는 orch 인프로세스 *논리* 컨테이너**(실 host.js OS 프로세스/소켓 spawn 아님·#57 신규)·실 host.js 분리의 *씨앗*이되 `cluster-run.js` 미통합 |

**메타(구현 품질)**: ⒜ reg 0 규율 — 모든 step `zoneHostProc` OFF→직전 비트 동일(읽기 전용 step 0303·0307·0308·0309 는 ON/OFF 무관 0306 비트 동일). ⒝ 박스 유계 — **0305 정리 분할**(host-proc 메서드→`orch-hostproc.js`)로 `orch-zonebridge.js` 29.4→26.7KB(>30KB 트리거 선제 해소·#43 가드 동반). 단 `orchestrator.js` 가 생성자 필드(zoneHostProc·counters) 누적으로 **29.3KB**(30KB 근접·#57 watch). ⒞ host-proc 가 자기 박스(`orch-hostproc.js`)를 얻어 #9 데이터 평면 코드와 직교 분리.

## §2 검증 재현 (주장 아님 — 직접 실행)

- `node run.js spine` = **ALL OK** (src 누적 회귀 — 전 역사 불변을 현재 코드에 단언).
- `node src/verify.js hostproccap`(0310 capstone) = **5/5 OK** — hpcoh Y·consv Y·total 1·recv==drained+stale(r==d+s Y)·census 1·ledger 5/1/2/1(전 시드 42/7/1234/99/2026).
- `node src/verify.js zonehoststale`(0306) = **5/5 OK** — stale 1·recv 2 == drained 1 + stale 1·z1@hostB(이주 후)·a1 z1 보존·이주 중 떠난 frame 거부 실증.
- `node src/verify.js reg` = **ALL OK** — zoneHostProc OFF→baseline 비트 동일.
- grep 감사: `Math.random` src **0 hit** · `UObject|CoreUObject|GameplayTags|#include` src **0 hit** · orch→`zone.js` 직접 import **0 hit** · `wc -c src/*.js | awk '$1>30000'` = **0행**(최대 `orchestrator.js` 29329B).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #9 | 멀티프로세스 배선 — **host 프로세스 컨테이너 ✅(0301~0310·논리 층)** | ③/⑤ | 🟡 **진전·재정의** | 0291~0300 게이트웨이 직접 라우팅 위에, host=1급 프로세스 컨테이너(`zoneHosts`·자기 inbox 수신·자기 루프 tick·roster spawn/despawn·stale 거부)로 분리. **잔여=실 host.js *OS 프로세스/소켓* spawn**(아래 #57) → **STATE §2 NEXT** |
| #57 | **zoneHosts 가 orch 인프로세스 *논리* 컨테이너(실 OS 프로세스/소켓 아님)** | ⑤ | 🔴 **신규·load-bearing(#9 의 구체 잔여)** | host 프로세스 컨테이너(0301~0310)는 *씨앗*일 뿐 — `zoneHosts`/`zoneRuntimes` 는 orch 인프로세스 Map·자기 inbox 도 인프로세스 배열. 실 `host.js` OS 프로세스 spawn·소켓 수신·`cluster-run.js` 통합 미연결(멀티프로세스 entity give/failover/restart 핸드셰이크 미검증) | **STATE §2 NEXT**(최상위 load-bearing) → 게이트: zone 런타임을 `host.js` 실 프로세스로·host inbox 를 실 소켓 수신으로·`cluster-run.js` 실 spawn 통합(끄면 현 인프로세스 컨테이너=회귀 0) |
| #53 | 정합-0 가 by-construction(host-proc 판) | 검증 | 🟡 **확장** | `hostProcCoherent`(`orch-hostproc.js:66`)·`hostContainerCoherent`(`:55`)·bijection(`zoneHostSnapshot`)·stale 거부 모두 *같은 인프로세스*에서 `_hostSet` 단일 귀속·roster 상쇄·stale 은 `zoneHostStaleProbe`(0306) 만 합성 → 위반 경로 0 | **STATE §3 OPEN GAPS** → 게이트: #57 실 프로세스가 진짜 분기(소켓 지연·적용 실패) 도입 시 술어 load-bearing 화(끄면 구조적 0=회귀 0) |
| #41 | 테스트 seam 이 production wiring 에 위치 | ④/정리 | 🟡 **확장** | `zoneHostStaleProbe`(`src/topo-inject.js:79`)가 합성 stale frame+migrate 주입을 production 주입열에 gated 추가(미제공=무효·reg 0)·`zoneStaleProbe`(0296)에 이어 테스트 노브 누적 | **STATE §3 OPEN GAPS**(정리성) → 게이트: 전용 테스트 주입 경로로 이동(끄면 현 거동=회귀 0) |
| #16 | bespoke 모드 verify-kit 미승급 | ②/⑤/정리 | 🟡 **확장** | 0301~0310 bespoke 10모드(zonehostreg·zonehostrecv·zonehostdrift·zonehostroster·zonehoststale·zonehostcensus·zonehostcoh·zonehostbij·hostproccap) 미승급(현 spine 모드=`hostproccap` 만·0301~0309 는 git per-commit) | **STATE §3 OPEN GAPS**(정리성) → 게이트: 안정 호 묶음별 스모크 1~2 모드 점진 승급(끄면 현 spine=회귀 0) |
| #58 | `orchestrator.js` 코어 29.3KB — 30KB 근접(생성자 필드 재성장) | 정리 | 🟡 **신규(경미·watch)** | 0305 분할이 host-proc 메서드를 뺐으나 코어 클래스가 생성자 필드(zoneHostProc·zoneHosts·inbox·counters·zoneHostStale)로 재성장 → 다음 코디네이션 심화 전 정리 후보(#43 가드가 >30KB 시 경고) | **STATE §3 OPEN GAPS**(정리성) → 게이트: 코어 필드 init 을 mixin init 헬퍼로 묶거나 추가 분할(기능 0·reg 0) |

**유지(미해소·이월)**: #50·#46·#48·#44·#39·#55·#37·#38·#36·#4·#3·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#1(⛔범위밖).

> **🎯 단계 평결(SKILL §3.6)**: **host 프로세스 컨테이너(0301~0310·#9 잔여·load-bearing 의 논리 층)** — 0291~0300 verdict 가 지목한 게이트("실 host.js 물리 프로세스/소켓 분리·cluster-run.js 통합")를 *논리 컨테이너 층*까지 집행. 너비 1차 완료(⬜/🌱 0) 위 *승인된 심화* → **과심화 아님**(연속 지목된 최상위 게이트). **다음 권고: 실 host.js OS 프로세스/소켓 spawn(#57·cluster-run.js 실 spawn 통합) + 진짜 비동기(#4·논리 클럭).** step-loop 이 STATE §2 로 승급.
