# review 0491–0500 — #16 라운드 3차: 서비스 saga capstone 재작성 verify-kit 편입

> 범위: step-0491~0500 (검증 인프라 라운드·서버 기능 0). 인과 서사·진행 지도는 [progress/3-services.md](progress/3-services.md) "★ #16 서비스 계층 실행 검증 ✅". 이 문서는 *정당성 감사*(척추 5불변·재현·이슈)만.
>
> 판정 권위 = 실물 코드(`engine/verify-kit.js`·`src/svc-*.js`) + `node run.js`/`spine` 재현 + grep. 문서 PASS 를 베끼지 않는다.

이 묶음은 **박스 코드를 0줄도 수정하지 않는다** — 감사(2026-07) §2 ①-b(서비스 계층 실행 검증 0)를 집행: 거래소·우편·길드 정합 술어가 옛 *생성 step verify.js(git per-commit)*에만 살아 HEAD 재검증 불가였던 격차를, `run()` 을 서비스 opts 로 구동해 술어를 단언하는 9 capstone 으로 *재작성*(옛 코드 git history ~0248 소실)해 `engine/verify-kit.js` ORDER 로 편입.

## §1 척추 5불변 정합성

검증 하니스만 강화(박스 무변) — 승급 capstone 이 재단언하는 서비스 계층 불변을 매 spine 이 항구 검사하게 된 메타 강화. 위반 신설 0.

| 불변 | 판정 | 코드 알리바이 |
|---|---|---|
| ① 신성한 tick | ✅ | 거래소·우편·길드는 존 tick 밖 반응형 서비스(§2 판정 기준 통과). capstone 은 검증 함수·박스 tick 미변경(`verify-kit.js:1166~1394`). |
| ② 결정론 코어 | ✅ | 서비스 박스 `Math.random` src 실호출 **0**(grep). capstone 이 정합 술어·아이템 이동을 시드 [42,7,1234,99,2026] 재현 대조(`svcexchangecap` item0 seller→escrow→buyer 결정론). |
| ③ 권위 단일소유 | ✅ | **핵심 강화** — 승급 술어가 서비스 소유 단일성을 매 spine 단언: 거래소 `sagaLiveConsistent`(pending 3분할·`svc-exchange-core.js:154`)·escrow 교차(`giveOks==escrowXfers`)·우편 `sagaLiveConsistent`(mailC∧itemC∧escrowC∧sagaC·`svc-mail-core.js:106`)·길드 `rosterConsistent`(정확히 한 master·`svc-guild.js:9`)·`bankConsistent`(itemId 단일 길드 소유·`svc-guild.js:3`). |
| ④ 은닉·단일연결 | ✅ | capstone 은 `run()` 서비스 opts 만 주입(exchangeOps/mailOps/guildOps·invOps seam)·서비스 내부 미참조. 서비스간은 버스/give 경유(거래소↔가방 `_custody` give). `svcsvccombined`(`:1327`)가 3 서비스 공유 가방 격리(간섭 0)를 재단언. |
| ⑤ headless·원격검증 | ✅ | UE/UObject src **0**(grep). 서비스 capstone 은 in-proc `run()`·node 단언(헤드리스). 서비스 박스는 tick 밖이라 원격 검증 자명(더미=Node). |

**reg 0 알리바이(구조적)**: `engine/`·`src/verify.js` 는 `baseline/` 스냅샷 밖(`new-step.js:42` 제외)이라 검증 모드 추가가 `net-core`+박스 비트 무변 → reg 0. 매 step `node src/verify.js reg` = ALL OK.

**메타(하니스 품질)**: `promotedsvc`(`:1394`)가 서비스 9 capstone ORDER/MODES 등록을 매 spine 단언("no silent cap"). **비-vacuous 확인**: 각 시나리오가 실제 saga 를 구동 — 거래소 gives=2·escrowXfers=2·우편 gives=2·길드 vault=[gold1]·종합 minted=2(공허 아님·by-construction 트리비얼 아님).

## §2 검증 재현 (실행 출력 인용)

- `node run.js spine` = **ALL OK** (전 역사 불변 + 서비스 9 capstone 을 현재 src 에 단언·background).
- `node src/verify.js promotedsvc` → 9종 전부 ORDER∧MODES 등록(10 OK 행).
- `node src/verify.js svcsvccombined` → 5/5(거래소+우편+길드 한 run()·각 술어 동시 성립·공유 가방 격리).
- 대표 재현(편입 전 현재 src 5/5 확인 후 편입): `svcexchangecap` 5/5(gives2==acked2==oks2·pending0·item buyer)·`svcmailcap` 5/5(네 회계층 AND·escrowXfers2)·`svcguildcap` 5/5(master alice→bob·rosterConsistent)·`svcbankcap` 5/5(g1[gold1]·g2[gem0]·격리).
- grep 감사: 서비스 박스 `Math.random` 실호출 **0**·`UObject|GameplayTags` src **0**(async-core/net 힌트 2건은 주석·`:35`/`:20`)·`baseline/verify-kit.js` 부재(reg-0 구조 알리바이).
- 알리바이 라인: capstone 정의 `verify-kit.js:1166~1394`·서비스 술어 `svc-exchange-core.js:154`·`svc-mail-core.js:106,109`·`svc-guild.js:3,9`.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #16 | bespoke 검증 spine 미승급 | 전체/검증 | ✅ **대체로 해소** — 2차 grand capstone 9종(0481~0490) + **3차 서비스 saga capstone 9종(0491~0500)** ORDER 편입. 서비스 계층(거래소·우편·길드) 실행 검증이 이제 HEAD 매 spine 재검증. *잔여는 #76 로 분리*(경미). | 원장서 대체로 떨굼 — 잔여 완전 liveness 는 #76 |
| #76 | 완전 saga liveness 미발현(loss 주입 seam) | 검증 | 🟡 **신규(경미)** — 재작성 capstone 은 *정상·취소·만료·종합* 체제 커버. sagaLivenessConsistent(pending 3분할)는 정상 흐름 pending 0(0=0+0+0)로 성립 — 회신 손실 주입→abandon/permFailed *발현*은 미검증(옛 0131~0140/0172~0180 의 loss 주입 seam 필요). | STATE §3 OPEN GAPS → 게이트: exchange/mail 회신 경로 loss 주입 opt 추가 후 abandon/permFailed capstone(끄면 현 정상 흐름=회귀 0) |
| #75 | spine 런타임 성장 | 검증/도구 | 🟡 유지 — 서비스 capstone 은 in-proc run()(cluster child_process 아님)이라 성장 미미. 2차 cluster capstone(#75)와 별개. | STATE §3(기존) |
| #46 | 금고↔가방 escrow | 서비스 | 🟡 유지 — svcbankcap 은 금고 *내부* 원장 정합만(가방 미연동=가짜 escrow·2차 대기). | STATE §3(기존) |
| #74·#73·#68·#69 | 실 GW·완전-ON·경미 | ④⑤/검증 | 🟡 유지(이 라운드 무관) | STATE §3(기존) |

> **🎯 단계 평결 (0491-0500·SKILL §3.6)**: **#16 라운드 3차 — 서비스 saga capstone 재작성 편입(직전 0481-0490 verdict 의 "서비스 saga capstone 재작성 편입" 권고 + 감사 §2 ①-b 집행·검증 인프라 라운드)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *검증 하니스 강화*(서버 기능 0·박스 무수정→reg 0) → **과심화 아님**(코박기 아닌 하니스 항구화·"서비스 계층 실행 검증이 HEAD 에 있는가" 격차 해소). 옛 거래소0140·우편0170/0180·길드0190/0199 capstone 코드가 git 소실이라 *재작성*(run() 서비스 opts 구동·박스 노출 술어 단언). 편입 9종: 거래소 saga/교차/취소(0491·0492·0499)·우편 saga/교차/만료(0493·0494·0497)·길드 로스터/금고(0495·0496)·종합 격리(0498)·promotedsvc 가드(0500). **재현 증거**: `promotedsvc` 9/9·`spine` ALL OK·대표 capstone 5/5·비-vacuous(gives/escrowXfers/vault>0)·`Math.random`/UObject 서비스 박스 0·reg 구조 0. **#16 대체로 해소**·부수 **#76 신규(완전 liveness loss 주입 미검증·경미)**. **정직한 한계**: 정상·취소·만료·종합 체제만 — 회신 손실 발현(abandon/permFailed 3분할)은 loss seam 필요(#76). **다음 권고: #76 loss 주입 seam(완전 liveness 발현) 또는 #74 실 GW 프로세스 분리·#46 금고↔가방 escrow·#75 프로덕션 프로파일.** step-loop 이 STATE §2 로 승급.
