# review 0451–0460 — #4 실 run() net.step 배리어 실제 치환 (async-barrier)

> 범위: 0451~0460 = async-net(0441~0450·in-proc 등가)이 증명한 substrate 를 신규 박스 `src/async-barrier.js` 로 **실 `run()` 의 `net.step()` 중앙 lockstep 배리어에 꽂아 실제 대체**. run() 이 매 tick net.step() 대신 stepper 로 월드 입력을 정전 순서(m.id) holdback·손실 resync·지연 jitter 로 배달. 인과 서사·기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md)(비동기 substrate 기능 9)·[progress/README.md](progress/README.md). 이 문서는 *정당성 감사*만.

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick** ✅ — stepper 는 *배달 seam*(net.step 대체)·tick 안 동기 I/O·await·DB 0. 배달 로직은 net.step verbatim 인라인(`async-barrier.js:44 deliverMsg`)·onTick 은 그대로 순회(`:88`). 알리바이: `topo-run.js:102`(`if (ab) ab.step(); else net.step()`)·`:120`(flush).
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(주석만). 손실/지연 결정은 시드 PRNG(`__c.mulberry32`·`async-barrier.js:39`/`:47`). 핵심 명제 `worldDigest(run{asyncBarrier}) == worldDigest(run{})`(lockstep)를 `bare2ecap`/`barlossy`/`bardelay` 가 수치 증명. 배리어는 도착이 흐트러져도 *발신 순서(m.id) 재구성*으로 lockstep 재현. 알리바이: `async-barrier.js:57 cmp`(m.id 정렬)·`:64` world.sort.
- **③ 권위 단일 소유** ✅ — exactly-once(moveDup 0)·net.delivered dedup 이 중복 적용 차단·resync 가 유실 보전. 다중 존 이주=release+acquire(zone.js 핸드오프·배리어 무침습). 알리바이: `async-barrier.js:82 moveDeliv/moveDup`·`baraccount` 120==120·dup0.
- **④ 은닉·단일 연결** ✅ — 배리어는 net 계약(queue/actors/delivered)만 만짐·타 서버 내부/DB 직접 접근 0·클라는 게이트웨이 뷰만(`barview` seenSig). 알리바이: `async-barrier.js:10~13` 진입(common.js·async-core 만)·다른 src/ 박스 require 0.
- **⑤ headless·원격 검증** ✅ — 순수 JS·UE/UObject src 0 hit·`node run.js 0460 bare2ecap` 수치 증명. **메타(중요)**: 이 arc 는 이전 async-net/async-core 와 달리 **run() 이 async-barrier 를 실제 호출**한다(검증 전용 박스 아님) — 단 `opts.asyncBarrier` OFF(기본)면 `topo-run.js:88` 이 ab=null → net.step() 그대로 → **reg 구조적 0**(플래그 게이트). 한계(정직): 손실/지연 섭동은 *단일 존*(무이주)에서만 world==lockstep 검증 — 다중 존은 투명 통합만(§3 #72).

**메타(회귀 규율·박스 유계)**: reg = asyncBarrier OFF 경로(net.step) → baseline 비트 동일(`reg` ALL OK 전 step). topo-run.js 변경분(ab 생성 `:88`·gated step `:102`·flush `:120`·return `asyncBarrier` 필드 `:139`)은 OFF 면 전부 휴면(ab=null). 박스 >30KB 0개(async-barrier 8.4KB). 코드 델타는 0451~0457 async-barrier.js 증분+topo-run.js gated 배선·0455/0456/0458/0459/0460 는 verify.js 모드(0456 회계 계수만 박스 추가).

## §2 검증 재현 (직접 실행·수치 인용)

- `node run.js 0460 bare2ecap` → **5/5 OK**: ⒜ 단일 존·손실 20%+지연 30%+resync: world/뷰(seenSig) == lockstep·exactly-once(moveDup 0)·resyncs 24~32·delayed 32~43 ⒝ 다중 존(2)+핸드오프: world/log == lockstep.
- `node run.js 0460 reg` → **ALL OK**(asyncBarrier 미설정→net.step 비트 동일).
- `node run.js spine` → **ALL OK**(전 시리즈 누적 회귀·asyncBarrier OFF 경로 비-침습).
- grep 감사: `Math.random`/`UObject|CoreUObject|GameplayTags` src 0(주석 2건만)·async-barrier 는 `topo-run.js:88`/`:102` gated(OFF→net.step).
- 부분 재현(대표 모드): `barpass`(투명 seam world/log==lockstep)·`barhold`(정전 순서 holdback·held244)·`barlossy`(단일 존 손실 world==lockstep·resyncs18~26)·`barnoresync`(무-resync 발산·resync 복원=load-bearing)·`barmultizone`(2존 world/log==lockstep·handoffs17~20).
- 알리바이 라인: `async-barrier.js` 20(makeAsyncBarrier)·44(deliverMsg verbatim)·57(cmp m.id)·64(world.sort)·73(move 손실+resync)·80(지연 jitter)·82(회계) · `topo-run.js` 88(ab 생성 gated)·102(gated step)·120(flush).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #4 | broker lockstep = 강제 동기(진짜 비동기 미해제) | ① | 🟡 **대폭 진전(실 run() 치환 ✅·잔여=완전 async 전환)** | 0451~0460 이 `async-barrier.js` 로 실 run() net.step 중앙 배리어를 async substrate 로 *실제* 대체(월드입력 holdback/resync·손실+지연 하 world/뷰==lockstep·`bare2ecap` 5/5). **잔여**: 완전 async 전환(항상 ON·다중 존 이주 하 유계 resync·downstream 손실/재접속) → **목적지 STATE §2 NEXT**(다음 큰 load-bearing) → 게이트: 다중 존 이주 경계에 유계 resync 배선(끄면 현 gated OFF=회귀 0) |
| #72 | 다중 존 이주 하 손실/지연 섭동 미검증(유계 resync 필요) | ②/⑤ | 🔴 **신규(load-bearing·#4 실 치환의 다중 존 판)** | 0454~0460 손실/지연 섭동은 *단일 존*(무이주)서만 world==lockstep — 다중 존은 투명 통합만(`barmultizone`). 지연 move 가 *이주 경계*를 넘으면 옛 존에 배달돼 손실(move 는 이주 전 적용돼야) → **목적지 STATE §2 NEXT**(#4 완전 async 와 동반) → 게이트: 이주 전 유계 resync(resyncDelay < 이주 리드타임)·끄면 현 단일 존 검증=회귀 0 |
| #53 | 정합-0 이 by-construction(분기 시나리오 부재) | 검증 | 🟡 **확장(async-barrier 손실/지연 드라이버 단일 존)** | 배리어 손실/지연은 시드 PRNG 합성 드라이버(실 net transport 아님·단일 존)·단 `barnoresync` 가 무-resync 발산으로 *진짜 분기* 도입(by-construction 아님·resync load-bearing 확증) → **목적지 STATE §3 OPEN GAPS** → 게이트: 실 transport(net.transport loss)+다중 존 이주로 진짜 섭동 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/정리 | 🟡 **확장(+barpass~bare2ecap 10 모드 미승급)** | 이번 묶음 10 모드도 미승급·현 spine 모드=최신 `bare2ecap` 1개 → **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 안정 배리어 스모크 1~2 모드 점진 승급(끄면 현 spine=회귀 0) |
| #71 | async substrate 실 전송 by-construction | ⑤/검증 | 🟡 **부분 해소(0451~0460)** — async-barrier 는 실 run() net.step 을 실제 치환(더 이상 run() 밖 아님·단 gated OFF) — 실 transport 섭동은 아직 미배선(#53 잔여) | STATE §3 — 실 net.transport 손실을 배리어에 |
| #70 | 실 host.js child 경계 업스트림(#57 짝) | ⑤ | 🟡 유지(미접촉) | STATE §3 — 실 cluster 업스트림 RPC |
| #68·#69 | entity 컨테이너 동기·동치 transitive | ③/검증 | 🟡 유지(경미·미접촉) | STATE §3 |

> **🎯 단계 평결 (0451-0460·SKILL §3.6)**: **#4 실 run() net.step 배리어 실제 치환(0451~0460·직전 0441-0450 verdict 가 STATE §2 로 올린 load-bearing #4 실 치환)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *verdict 가 지목한 #4* → **과심화 아님**(코디네이션 substrate→실 run() 배선 기능 9·신규 박스 `async-barrier.js`). async-net 이 *검증 전용 등가* 였다면, 이번엔 실 `run()` 전송 경로에서 net.step 중앙 배리어를 async substrate 로 *실제 대체*(OFF→net.step reg 0). **재현 증거(시드 5종 5/5)**: `bare2ecap`(단일 존 손실+지연 world/뷰==lockstep·exactly-once·다중 존 투명)·`barnoresync`(무-resync 발산·resync 복원=load-bearing)·`reg`/`spine` ALL OK(asyncBarrier OFF→net.step 비트 동일)·`Math.random`/UObject src 0·async-barrier gated `topo-run.js:88`/`:102`·>30KB 박스 0(async-barrier 8.4KB). **의외 발견(0453)**: 실 존 순서 민감(enter rng·move 존재)→배리어는 발신 순서(m.id) 재구성으로 lockstep 재현·load-bearing=손실 복원(재정렬 아님). **#4 🟡대폭 진전(실 run() 치환 ✅·잔여=완전 async 전환)**·부수 **#72 신규(다중 존 이주 섭동 미검증·load-bearing)·#53 확장·#71 부분 해소**. **다음 권고: #4 완전 async 전환(다중 존 이주 유계 resync·항상 ON) 또는 실 host.js child 업스트림(#70), 그 다음 경미 정리(#68·#69·#72·#16).** step-loop 이 STATE §2 로 승급.
