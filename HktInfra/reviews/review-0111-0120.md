# review 0111–0120 — 거래소 수명주기·시세 피드 CQRS · 거래소↔가방 2-서비스 실물 거래

> 범위: 닫힌 step 0111~0120. *무엇을 했나*의 박스별 진행은 [progress/](progress/)(3-services 갱신). 이 문서는 *정합 감사*만 — 척추 5불변 + 재현 + 이슈→후속 연결. 권위: 실물 `../src/*.js`·`../engine/*.js` + `node run.js` 재현(주장 아님).

묶음 골격: **① 거래소 수명주기 발행 완비**(0111 취소·0115 만료 → sold/cancelled/expired 3종) · **② 시세 피드 CQRS arc**(0112 분리·MarketFeed 새 박스→0113 저널 replay late-join→0116 만료 반영) · **③ 매물 만료 TTL**(0114 시간 트리거 escrow 회수·보존식 4종 종결) · **④ 거래소↔가방 2-서비스 실물 거래**(0117 인출→0118 입금→0119 반환→0120 보존 — **#30 의 결합 절반 해소**).

## §1 척추 정합성 (SPINE §5 5불변)

| 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|
| ① 신성한 tick | ✅ | 거래소·시세 피드 전부 `onMsg`/내부 메서드만. `_custody` 는 거래소→가방 메시지 1발(`src/svc-exchange.js:52` `net.send(addr, inv, {item_req,give})`)·만료 sweep 은 주입 `exchSweep` 반응(`:67`)·MarketFeed 발신 0(`src/svc-market.js:14` onMsg 만). `grep "await |fs\.|readFileSync" src/svc-exchange.js src/svc-market.js` = **0 hit** — 박스 tick 경로 동기 I/O 0. |
| ② 결정론 코어 | ✅(🟡 #29 불변) | `grep Math.random src/` = **0 hit**. 거래량/시세 결정론: itemId 가방 mintTotal 단조·거래소 nextId 단조(`:77`)·만료 sweep `listings` 삽입 순(Map) 순회(`:70`·결정론)·MarketFeed 는 발행 사본의 순수 함수(`svc-market.js:25-29`). 🟡 변화 없음: 공지 epoch=promotedAt 단일클럭(#29)은 이번 묶음 미접촉. |
| ③ 권위 단일 소유 | ✅ **강화(2-서비스)** | **이 묶음의 머리**. escrow 를 *가방 원장의 한 아바타 'escrow'* 로 두어 아이템 쓰기 권위는 *가방 하나* — 거래소는 `_custody` give *요청자*만(`:79,106,118,91`). 인출 seller→escrow·입금 escrow→buyer·반환 escrow→seller 전부 가방 give = release+acquire 쌍. **2-서비스 보존 단언**: `escrowItemIds()`(거래소 open·`:127`) ≡ 가방 원장 escrow 소유 집합(exinvconsv 전 시드 true)·minted 불변(소유자만 이동)·매 아이템 정확히 한 소유자. 거래소 자기 보존도 4종 종결로 확장(`conserved() listed==open+sold+cancelled+expired`·`:123`). 가방 1-당사자·거래소 2-당사자가 *결합해도* 단일 소유 불침. |
| ④ 은닉·단일 연결 | 🟡 watch | 발행 무수정 구독 OK: svc.exchange.cancelled/expired→audit·sold/cancelled/expired→market(`src/topo-build.js` subs·발행자 무수정). **🟡 두 긴장**: ⒜ 거래소→가방은 *직접 주소*(`inv: inventoryAddr`·`topo-build.js:232`)·버스 아닌 명시 인터페이스(게이트웨이→가방 초기 패턴과 동형·수용 가능하나 버스화 미완) ⒝ 거래·sweep·선-적재는 topology 테스트 seam(`exchangeOps`·`invOps`·`topology.js:191,193`)이지 클라→게이트웨이 E2E 아님 → #30/④ 은닉 긴장 유지. |
| ⑤ headless·원격 검증 | ✅(🟡 #9) | `grep UObject\|CoreUObject\|GameplayTags src/` = **0 hit**·Node 전용. `node run.js spine` 120-step ALL OK. **🟡**: `grep "exchange\|market" src/host.js` = **0 hit** → 거래소↔가방 give 흐름·시세 피드 멀티프로세스 미배선(인프로세스 전용)·거래소 crash→reconstruct 는 post-run 직접 호출(pexsnap/mktpersist) → #9 확장. |

**메타(회귀 0 규율)**: 10 조각 전부 OFF 플래그 + early-return 가드 — `cancelPublish`/`expirePublish`(`svc-exchange.js:27,36`)·`exchangeTtl`(`:34`·ttl≤0 sweep no-op `:68`)·`invMode`(`_custody:53` early-return)·`marketFeed`(박스 0)·sold ev `item` 보강은 publish ON 일 때만(reg 시나리오 거래소 OFF). reg ALL OK = src=baseline 비트 동일. 박스 크기: svc-exchange 누적 **24.5KB**·svc-market 새 박스 **7.4KB** — 전부 ≤30KB(정리 step 불요). MarketFeed 는 0019 RankingService 의 거래소 판(같은 CQRS 골격·다른 입력=수명주기 발행).

## §2 검증 재현 (직접 실행·주장 아님)

- **`node run.js spine`** → **ALL OK** (120-step 누적 회귀 사슬 — 전 역사 불변이 *현재 src 코드*에서 하나도 안 깨짐). seed42 월드해시 `0x7a122947` 보존·멀티 2회·인프로세스·world 전부 OK.
- **`node src/verify.js reg`** → **ALL OK** (전 플래그 OFF=src=baseline 비트 동일·거래소↔가방 give·시세 피드·만료 전부 휴면).
- **`node src/verify.js exinvconsv`**(현재 step 0120 가설) → **ALL OK** — 거래소 open 매물(`escrowItemIds`)==가방 원장 escrow 소유 `["item4"]`·minted 5 불변·아이템 소유자 합 5(각 1소유자)·sold/can/exp 2/1/1·conserved 전 시드.
- **grep 5불변**: `Math.random` 0 · `UObject|CoreUObject|GameplayTags` 0 · `await/fs` (svc-exchange/svc-market) 0 · `host.js` 에 exchange/market 0(인프로세스 전용).
- **#16 알리바이**: `engine/verify-kit.js:702-703` ORDER = `…reliable·tail·inflight·degrade·inject·isolate·hide·repro`(~0024 에서 멈춤). 0111~0120 의 10 가설 모드(excancel·market·mktpersist·exexpire·exexppub·exmktexp·exinvlist·exinvbuy·exinvret·exinvconsv)는 각 step verify.js 셸에서 1회 단언 후 다음 step 이 교체 — 누적 회귀 미승급. spine 은 reg-0 만 보증.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #30 | 거래소↔가방 미결합·자기 박스 영속 | 서비스/데이터 | 🟡 **절반 해소** | **(a) 결합 ✅ 해소 0117~0120** — escrow 가 가방 원장에 실체화(give seller↔escrow↔buyer/seller·`svc-exchange.js:79,106,118`)·2-서비스 보존 단언(0120 escrowItemIds≡가방 escrow 소유). **(b) 잔여 🟡**: op 저널 여전히 거래소 박스 내(별 PersistStore·N-replica 0·#9 family). **STATE §3** → 게이트: 거래소 저널 PersistStore 박스화(끄면 현 단독 영속=회귀 0) |
| #31 | 거래소↔가방 give 낙관적·결과 미수신(2-서비스 원자성 부재) | 서비스/버스 | 🔴 **신규(load-bearing)** | 0117~0120 §9 — `_custody` 는 fire-and-forget(give 결과 item_result 미소비). escrow 가 (가방 손실로) 아이템 미보유 시 가방 give 실패해도 거래소 종결(sold/cancelled/expired) 성립 → 거래소 회계 ↔ 가방 권위 갈라짐(현 시나리오 escrow 항상 보유라 미발현). **STATE §2/§3** → 게이트: give 결과 수신 + 실패 보상(saga·롤백)·#10 give×result-ahead 와 같은 무대(끄면 낙관적 현 거동=회귀 0) |
| #32 | 시세 피드 reconstruct==라이브 가 전-수명주기-발행 전제 | 서비스 | 🟡 신규(경미) | 0116 §9 — reconstruct(거래소 저널 expire)는 만료를 셈하나 라이브는 *발행*된 expired 만 소비 → 만료 발행 OFF + ttl ON 이면 reconstruct≠라이브. CQRS read model 완전성=발행 완전성의 본질 계약. **STATE §3 OPEN GAPS** → 게이트: 발행 ON 전제 명문화 또는 시세 피드 자기 영속(끄면 발행-ON 정합=회귀 0) |
| #16 | bespoke 가설 모드 미승격 | ②/⑤/정리 | 🔴 **확장 +10(~50)** | 0111~0120 10 모드 추가 미승급. **STATE §3 OPEN GAPS** → 게이트: 안정 호(거래소↔가방·시세 피드) 스모크 1~2 모드 verify-kit 승급(끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장** | +거래소↔가방 give(0117~0119)·시세 피드(0112~0116) 전부 host.js 0·인프로세스 전용·crash→reconstruct post-run 직접. **STATE §3** → 게이트: 박스 host 배선 + 진짜 child.kill→replay(현 인프로세스 FIFO=회귀 0) |
| #33 | STATE §7 INDEX 30KB 천장 — 매 step 트림 강제 | 정리/도구 | 🟡 신규(경미) | 120행째 INDEX 가 step 수에 선형 → close-step 30KB 예산이 매 step §7 1줄 추가마다 옛 행 압축을 강제(이 묶음 진행 중 반복 발현). **STATE §3 OPEN GAPS**(도구성) → 게이트: INDEX 10행→묶음 1행 압축(전문은 reviews/)·또는 예산 상향(STATE 가독성 영향만=회귀 무관) |

기존 유지(변화 없음·코드 grep 재확인): #1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29 — 이번 묶음이 거래소·시세 피드 외 계층 무변경(프레즌스·버스·존·가방 코어 grep 무변경). #31 은 #10(give×result-ahead)·#9 의 거래소 발현·별 추적.
