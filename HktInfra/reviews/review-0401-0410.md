# review 0401–0410 — #62 코디네이터를 runMulti 복원력 코어로 승격(능력 합류)

> 범위 0401~0410(코디네이션·`src/cluster-coord.js`). 인과 서사·서버 기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md) 기능 20. 이 문서는 *정당성 감사*만(narrative 0).

## §1 척추 5불변 정합성 (실물 코드 알리바이)

직전 묶음(0391~0400)이 코디네이터의 placement 권위를 데이터 평면·orch 집행 뷰에 일원화했다. 이 묶음은 그 위에 *runMulti 의 복원력 능력*(펜싱·lease-timeout·restart·reprovision/mirror·warm-failover·시나리오·재구성 보고)을 zone cluster 상주 제어 평면에 이식 — 코디네이터를 runMulti superset 으로. 전부 새 박스 `cluster-coord.js`(run() 데이터 평면 미사용·require 0 순수 주입).

| # | 불변 | 판정 | 알리바이 (현재 코드) |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 복원력 메서드(fence/restart/reprovision/promote)·runScenario 훅은 *제어 평면*(시뮬 tick 밖). tick(`:75`) 의 미러 복제도 제어 평면. 시뮬 tick 내 동기 I/O 0. |
| ② | 결정론 코어 | ✅ | `src/` `Math.random` 0 hit. 시드 5종 동일(`coordmulticap` 5/5). 펜싱/silence 임계 결정론(`leaseTimeout` 고정). |
| ③ | 권위 단일 소유 | ✅**강화** | restart/promote 가 release+acquire 로 단일 소유 보존: restart(`:152`) pre-kill snapshot→loadstate(무손실), promoteStandby(`:178`) placement→standby+`_orchWriteBack`+미러 해제(승격된 standby 가 단일 권위). 펜싱(`:49`)이 추정 사망 host 발신 차단(split-brain 방지). |
| ④ | 은닉·단일 연결 | ✅**강함** | cluster-coord.js `require` 0 hit(순수 주입)·`zone.js` import 0 hit. cluster/driver/orch 공개 API(rpc·killHost·spawnOne·socketDead·running)만. |
| ⑤ | headless·원격 검증 | ✅ | 실 `host.js` child_process E2E — `coordmulticap` 가 `Cluster.spawn()`→실 프로세스 reprovision standby spawn·killHost·promote 구동. `UObject` src 0 hit. |

**메타(회귀 0·박스 유계)**: reg 5/5 비트 동일(복원력 메서드는 run() *종료 후* orch/cluster 객체에만 작용·OFF 플래그·구조적 reg 0). `src/` >30KB 박스 **0개**. cluster-coord.js 28.9KB — **0404 정리 step**(닫힌 arc 헤더 34줄→포인터 1줄·29.4→19.5KB·코드 무변경 reg 0)으로 박스 30KB 트리거를 *선제 집행*(CLAUDE.md §박스 분할 규율 모범 준수).

## §2 검증 재현 (직접 실행·인용)

- **`node run.js 0410`(coordmulticap·arc capstone)**: 5/5 OK — runScenario(6,{migrate z3 A→B@2·reprovision z1@hostA_s@3})+killHost(hostA)+promoteStandby(z1) 후 **runMultiCoherent Y**·a1 보존(따뜻한 standby 상태 손실 0)·migrations 1·reprovisions 1·promotions 1. 펜싱·silence·restart·reprovision/mirror·warm-failover·시나리오·clusterInfo 를 한 시나리오로 종합 입증.
- **`node src/verify.js reg`**: ALL OK(5/5 baseline 비트 동일).
- **`node run.js spine`**(전 시리즈 누적 회귀): **ALL OK** — 0401~0410 의 어떤 변경도 0001~0400 불변을 깨지 않음.
- **grep 감사**: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · cluster-coord `require` 0(순수 주입) · cluster-coord→`zone.js` import 0 · >30KB 박스 0(cluster-coord 28.9KB).

> **by-construction 주의(#53 family)**: warm-failover 의 상태 보존(a1 {8,8} 유지)은 *진짜 child_process kill + standby snapshot 비교*라 by-construction 아님(0405 reprovision 이 실 loadstate·0409 가 실 killHost 후 standby 조회). runMultiCoherent 의 미러-실재 검사도 실 cluster snapshot. 단 fence/silence 의 펜싱 *효과*(tick skip)는 carrier 가 없어 `fencedTicks` 계측으로만 관찰(전송 계측 부재·#62 잔여 family).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #62 | 코디네이터↔옛 cluster-run.js runMulti 코드 합류 | ⑤/검증 | 🟡**대폭 진전·재정의(능력 합류 ✅·잔여=코드 플립)** | 0401~0410 이 runMulti 의 복원력(펜싱·lease-timeout·restart·reprovision/mirror·warm-failover·시나리오·clusterInfo)을 코디네이터에 모두 이식 → 코디네이터가 runMulti *능력 superset*. **잔여**: 옛 `cluster-run.js` runMulti 가 *이 코디네이터를 호출*하도록 코드 delegate(플립)만 미수행 | **목적지 STATE §2 NEXT** → 게이트: runMulti 가 코디네이터 위임(끄면 현 병존=회귀 0) |
| #63 | 실 failover 상태 소실(영속 미연동) | ⑤/데이터 | 🟡**완화(0409)** | promoteStandby(`:178`)가 *사전 reprovision* 된 zone 의 따뜻한 standby 를 승격해 상태 손실 0(`coordpromote`/`coordmulticap` a1 보존). **잔여**: standby 없는 zone 의 failover 는 여전히 소실(영속 복구는 데이터 계층) | **목적지 STATE §3 OPEN GAPS** → 게이트: 영속서 존 상태 복구 또는 모든 권위 zone 자동 reprovision(끄면 현 빈 재가동=회귀 0) |
| #68 | orch zoneHost *entity 컨테이너* 미동기 | ③/정리 | 🟡유지(경미) | 변동 없음(이 묶음 무관·where-view 만 합류) | **목적지 STATE §3 OPEN GAPS** → 게이트: write-back 이 zoneHost 컨테이너도 동기 |
| #16 | bespoke 모드 spine 미승급 | 정리 | 🟡유지·확장 | 이 묶음 10 모드(coordfence~coordmulticap)도 미승급(현 spine 모드=최신 coordmulticap 1개) → cluster-coord spine 안전망 얇음 | **목적지 STATE §3** |
| #61·#4·#53 | 업스트림 실 클라·진짜 비동기·정합 술어 by-construction | ②/⑤/검증 | 🟡유지 | 변동 없음. 원장 유지 | — |

> **🎯 단계 평결(SKILL §3.6)**: progress 맵 ⬜/🌱 박스 **0**(너비 1차 완료 유지·0210 이래) 위 2차. 0401~0410 은 *직전 리뷰(0391~0400) verdict 가 STATE §2 로 올린 load-bearing #62* 를 집행 — **과심화 아님**(코디네이션 기능 20·승인된 다음 절). 능력 합류(병존)로 — runMulti full-topology 코드를 run()-경로에서 직접 합치는 대신, 코디네이터(새 박스·reg 0)에 runMulti 의 복원력 능력을 모두 이식하고 옛 runMulti 와 *병존*(원장 게이트 "끄면 현 병존=회귀 0" 그대로). **#62 능력 합류 ✅**(잔여=코드 플립=delegate)·부수로 **#63 완화**(따뜻한 standby 승격). 0404 정리 step 으로 박스 유계 모범 준수. **다음 권고**: 옛 cluster-run.js runMulti 가 코디네이터를 위임하도록 코드 플립(#62 마무리), 그 다음 업스트림 intent 실 클라(#61)·진짜 비동기(#4). step-loop 이 원장을 읽어 STATE §2 로 승급.
