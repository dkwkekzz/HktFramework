# review-0221-0230 — 5박스 3차 고도화 균형 라운드 (정당성 감사)

> 범위 0221~0230 · 인과 서사·서버별 기능 리스트는 [progress/](progress/)(1-edge·2-world·5-coordination·6-data 갱신). 이 문서는 *정당성 감사*만(척추 5불변·검증 재현·이슈→후속). 진행 마커 권위 = [../STATE.md](../STATE.md) §5.

## §1 척추 5불변 정합성 (SPINE §5)

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | touched 4박스(cache·worldlog·instance·loginqueue) *순수 반응형*(onTick 0·주석만: `cache.js:13`·`worldlog.js:13`·`instance.js:12`·`loginqueue.js:13`). 오케 `orchestrator.js:133 onTick` 은 0058 self-heal 제어 평면(lease/recover·존 tick 밖)이고 **이번 라운드 추가 place* 는 전부 onMsg**(`orchestrator.js:99 placeRebalance`·`:101 placeDrain`). reap·LRU·write-behind·fsync·계정검증·이탈 전부 op 수신 시에만 — tick 내 동기 I/O 0. |
| ② | 결정론 코어 | ✅ | `Math.random` 0 hit(5박스). 회수/이주 순서 전부 결정론: reap=active 삽입순(`instance.js:_reap`)·rebalance/drain=placement 삽입순·최소부하 tie-break=후보 배열순(`orchestrator.js:_leastLoaded`). LRU recency=`m.tick`(`cache.js _set(…,now)`·get touch)·fsync durableSeq=jseq(`worldlog.js _replayDurable`). 난수 0·전 모드 5시드 동일. |
| ③ | 권위 단일 소유 | ✅(모델)·🔴 실배선 #51 | instanceLeave=route release(`instance.js:48 routes.delete`)·reap 은 occupancy 0 만 회수(점유=권위 보유 보호·`instance.js:_reap`)·placeMigrate/Rebalance/Drain=placement 단일 키 release+acquire(총 배치 수 보존·공백/중복 0·`orchestrator.js:_rebalance`·`_drain`). **단 placement 은 advisory map** — 권위 *모델*은 올바르나 실 존(orch.pairs/promotion·zone.js)은 안 움직임(#51·이 라운드가 그 위에 자동화 2조각 더 쌓음). |
| ④ | 은닉·단일 연결 | ✅ | 신규 op 전부 제어/서비스/운영 경계(loginAuth·loginAbandon·placeRebalance·placeDrain·cacheCapacity·cacheLruTouch·worldBuffer/Flush·worldFsync·instanceLeave·instanceReap). 클라 와이어 계약 무변(reg 0 보증). loginAccounts 는 박스 생성 opts(`topo-build.js`)·클라 비노출. |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | UE 모듈(`UObject`·`CoreUObject`·`GameplayTags`) 0 hit(5박스). 전 모드 `node run.js`/`spine` headless 재현. **단 3차 10조각도 인프로세스 op-injection seam(`host.js` 멀티프로세스 0)** — #9 확장. |

**메타(회귀 규율·박스 유계)**: 3차 10조각 전부 OFF 플래그(미주입 휴면)로 **reg 0**(직전 step 비트 동일) — `instanceLeave`·`instanceReap`·`placeRebalance`·`placeDrain`·`cacheCapacity`(cap=∞)·`cacheLruTouch`(off)·`worldBuffer/Flush`·`worldFsync`(durableSeq 0)·`loginAuth`(빈 accounts)·`loginAbandon` 미주입 경로가 baseline 과 동일. 박스 크기 **건전**(cache 9.5·instance 11.4·loginqueue 11.0·worldlog 12.5·orch 22.3KB·전부 ≪30KB). **wiring**: 9조각은 기존 op-injection 채널 재사용(미접촉)·**0229 만 `topo-build.js` 에 `loginAccounts` 파라미터 +1줄 접촉**(31.5KB·#49 유지·미세). topo-run 미접촉(35.9KB).

## §2 검증 재현 (실행·인용)

- **`node run.js`**(현재 step 0230 = reg + loginabandon): `reg: 인프로세스 모드 → step-0027 와 비트 동일` + `loginabandon: … 균형 라운드 0221~0230 닫기` → **결과: ALL OK**. p2 이탈→큐 [p1,p3]·p3 당겨짐·abandoned 1·미줄 pX miss 1 (5/5).
- **`node run.js spine`**(src 누적 회귀 — 전 역사 불변을 *현재 코드*에 단언): **결과: ALL OK** (230-step 사슬·reg+전 가설 모드). "0221~0230 의 10조각이 0001~0220 의 어떤 불변도 안 깼다"의 증거.
- **grep 감사**: ② `Math.random` 0(5박스) · ⑤ UE 모듈 0(5박스) · ① touched 4박스 tick 내 `onTick/await/fs` 0(주석만·orch onTick=제어 평면) · place* 핸들러 onMsg 위치 확인(`orchestrator.js:99·101`).
- **#51 재확인(코드)**: `_rebalance`/`_drain` 은 `this.placement.set(…)` 만 호출·실 존(zone.js·pairs·promotion) 미호출 → placeRebalance/Drain 도 advisory(2차 placeMigrate 와 동일 paper map 위).
- **단일 src 한계(#16 재확인)**: `src/verify.js` 에 `kit.MODES['loginabandon']` 만 — 0221~0229 의 9 bespoke 모드는 git 태그(step-0221~0229)에만 보존·재실행 불가. ON-경로 의미는 각 close 시 1회 단언·spine 은 reg-0 만 보증.
- **#33 재발현(관측)**: 3차 라운드 중 `STATE.md` 가 30KB 예산을 *두 번* 초과(0229·0230 close)·매번 수동 트림으로 통과(0230 시 §7 INDEX 0101~0106 6행→1행 fold·현 29.8KB). INDEX 누적 천장이 *기능 step 마다* 압박 — #33 살아있음.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트 |
|---|---|---|---|---|
| #51 | **배치 SSOT advisory(실 존 미배선)** | ⑤/③ | 🔴 **확장**(load-bearing) | 0223 placeRebalance·0224 placeDrain 이 `placement` 맵 자동화를 더 쌓았으나 실 존 lifecycle(orch.pairs/promotion·zone.js) 미배선 → 정교한 자동 재배치/드레인이 여전히 *advisory*. 이 라운드로 advisory 표면만 커짐. **STATE §2 NEXT**(무르익음·다음 라운드 0순위) → 게이트: placeMigrate/Rebalance/Drain→실 존 핸드오프 트리거(OFF→현 paper map=회귀 0) |
| #50 | **instanceRoute orphan route**(despawn 시 routes 미정리) | ③/서비스 | 🟡 **완화**(미해소) | 0221 instanceLeave 가 player-side route 해제 primitive 제공·0222 instanceReap 은 occupancy 0 만 회수해 orphan-safe. **단 직접 instanceDespawn(0202)은 여전히 routes 잔류**(despawn-time 스윕 미구현). **STATE §3 OPEN GAPS** → 게이트: instanceDespawn 시 그 instance 로 라우팅된 routes 스윕(OFF→현 거동=회귀 0) |
| #49 | wiring 박스 >30KB(topo-build 31.5·topo-run 35.9KB) | 정리 | 🔴 **유지·미세 접촉** | 0229 가 `topo-build.js` 에 `loginAccounts` 파라미터 +1줄(0211~0220 의 "미접촉" 깨짐·단 미세). 9조각은 op-injection 채널 재사용. *다음 너비/멀티프로세스 배선* 전 분할 여전히 게이트. **STATE §2 NEXT** → 게이트: per-tick Ops 핸들러→`topo-inject.js` 추출 + close-step src size 체크(#43 동반·기능 0·reg 0) |
| #33 | STATE INDEX 천장(30KB) | 정리/도구 | 🔴 **재발현** | 3차 라운드 중 STATE 30KB 두 번 초과·수동 트림(0101~0106 fold)으로 통과. INDEX 가 *기능 step 마다* 압박 → 곧 또 초과. **STATE §3 OPEN GAPS**(도구성) → 게이트: 닫힌 0101~0200 개별 행을 reviews/ 묶음 행으로 일괄 fold(역사는 step 문서·git 보존·회귀 무관) |
| #16 | bespoke 모드 누적 회귀 미승급 | ②/⑤/정리 | 🔴 **확장**(+3차 10모드) | instanceleave·instancereap·placerebalance·placedrain·cachecapacity·cachetouch·worldwb·worldfsync·loginauth·loginabandon 전부 verify-kit 밖·spine 미포함. **STATE §3 OPEN GAPS** → 게이트: 안정 호 스모크 1~2모드 verify-kit 승급(OFF→현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장**(+3차 10조각 host.js 0) | 3차 심화 전부 인프로세스 op-injection seam(자동 재배치·드레인·write-behind·fsync·계정검증 멀티프로세스 미검증). **STATE §3** → 게이트: 멀티프로세스 배선 + 멱등/순서 보존(현 FIFO·인프로세스라 미발현=회귀 0) |

**단계 평결(§3.6)**: **3차 균형 라운드 완료** — 0221~0230 이 5박스를 *각 2조각씩* 심화(박스간 균형·한 박스 코박기 0). **그러나 너비(⬜/🌱 박스)는 0210 에 이미 0**이라 이 라운드는 순수 *심화*다 — 특히 오케스트레이터 2조각(0223·0224)은 load-bearing 갭 #51(배치 SSOT advisory)이 *열린 채로* 그 위에 정책 자동화를 더 쌓아 **advisory 표면을 키웠다**(정교하나 실 존과 단절). 박스간 균형은 건강하나, *다음 라운드는 "더 깊은 정책"보다 "advisory→실물 배선(#51)·정리/도구(#49·#33·#16)"로 방향을 틀 적기*. **신규 정당성 위반 0**(reg 0·spine OK·5불변 유지)·신규 이슈 0(전부 기존 #51/#50/#49/#33/#16/#9 의 확장/재발현). step-loop 이 STATE §2 로 #51·#49 를 승급.

> 미해소 이월(목적지·게이트 §2 원장): #51(🔴 확장·STATE §2 0순위)·#49(🔴 STATE §2)·#33(🔴 재발현)·#16·#9(확장)·#50(완화) + 기존 #46·#48·#44·#47·#45·#43·#41·#39·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#36·#37·#38 유지.
