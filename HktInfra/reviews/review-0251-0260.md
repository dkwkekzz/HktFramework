# review-0251-0260 — 정리(orch 분할) + 캐시 4차 고도화 arc

> 범위: 닫힌 step 0251~0260. 인과·기능 서사는 [progress/5-coordination.md](progress/5-coordination.md)(오케스트레이터 분리)·[progress/6-data.md](progress/6-data.md)(캐시 4차 arc). 이 문서는 *정당성 감사*만(척추 5불변·재현·이슈).
>
> 한 줄: **0251 이 #52(orchestrator.js 34KB>30KB)를 `orch-placement.js` 믹스인 분할로 닫고(27.5KB·reg 0), 0252~0260 이 캐시 박스를 Redis-like 정합/신뢰성 9 기능으로 4차 심화**. 너비 ⬜/🌱 0 유지·새 박스 0·신규 척추 위반 0.

## §1 척추 5불변 정합성

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 캐시·오케스트레이터 분리부 모두 `onTick` 없는 순수 반응형(`src/cache.js:22` 주석 "존 tick 밖·순수 반응형"·`grep -c onTick src/cache.js`=주석 1·메서드 0). 배치 런타임 메서드도 onMsg 경유(`orch-placement.js`). |
| ② | 결정론 코어 | ✅ | `grep Math.random src/cache.js src/orch-placement.js orchestrator.js`=0 hit. 캐시 연산 시드 무관·결정론(LRU victim=setAt 최소·tie-break iteration 순). |
| ③ | 권위 단일 소유 | ✅ **강화** | 0251 분할이 running release+acquire 단일 소유 로직(`orch-placement.js:_migrate`)을 verbatim 이동(prototype 동일·동작 불변). 캐시 0255 SETNX(`cache.js` cacheAdd)=최초-기록-승 단일 점유 primitive·0252 write-through/0257 delete=source SSOT 정합·0260 coherent()=구조 불변 단언(store∩negatives=∅ 등). |
| ④ | 은닉·단일 연결 | ✅ | 캐시 9 기능 전부 메시지 계약(cacheXxx 타입)만 노출·게이트웨이/서비스는 캐시 내부 무지. 0251 은 파일 분할(내부 구조)·외부 계약 무변경(net-core export 집합 동일·`net-core.js:24` __p('orch-placement') 추가 1줄). |
| ⑤ | headless·원격 검증 | ✅ | `grep UObject\|CoreUObject\|GameplayTags`=0 hit. 캐시 검증은 `CacheStore` 직접 구성+onMsg(Node·원격 재현)·`node run.js spine` ALL OK. |

**메타(구현 품질)**: 회귀 0 규율 — 0251 은 *플래그 없는 투명 분할*이라 reg 가 전체 동작 동일성을 단언(maxDiff 0·Object.assign 이 prototype 복원). 0252~0260 은 새 메시지 타입/토글이라 미수신 시 직전 비트 동일. **박스 크기**: orchestrator 34→27.5KB·orch-placement 9.1KB·cache 9.5→21.4KB — 모두 <30KB 예산 내(분할 효과 + 캐시 성장 여유).

## §2 검증 재현 (직접 실행·인용)

- **박스 크기**(`wc -c`): `orchestrator.js`=27538·`orch-placement.js`=9126·`cache.js`=21868 — #52 게이트(>30KB) 해소 확인, 캐시 4차 후에도 여유.
- **grep 감사**: `Math.random` 0 hit(②)·`UObject/CoreUObject/GameplayTags` 0 hit(⑤)·`cache.js` onTick=주석 1·메서드 0(①).
- **`node run.js 0260 reg`** → ALL OK(캐시 9 기능·분할 모두 baseline 비트 동일).
- **`node run.js 0260 orchsplit`** → 5/5 OK(분할 후 z1@A→C 이주 running==hostC·drift 0·단일 소유).
- **`node run.js 0260 cachecohere`** → 5/5 OK(14-op 혼합 매 단계 coherent()==true·최종 a=1·c 제거·x∈negatives·session:* 제거·source.a=1 정합).
- **`node run.js spine`**(전 역사 누적 회귀를 현재 코드에 단언) → **ALL OK**(background 재현). 단 spine ORDER 의 캐시 모드는 *현 step `cachecohere` 만*(0252~0259 의 8 모드는 bespoke·미승급 — #16).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #52 | orchestrator.js 34KB>30KB | 정리 | ✅ **해소 0251** | `orch-placement.js` 믹스인 분할(Object.assign·verbatim·reg 0)→27.5KB. 0241-0250 verdict 가 지정한 게이트 형태 그대로. 원장서 제거. |
| #49 | wiring 박스 >30KB(topo-build 31.5·topo-run 35.9·svc-exchange-core 30.7) | 정리 | 🔴 **유지·미진전(3라운드 연속)** | 0251 은 *클래스* orchestrator 를 믹스인 분할했으나 topo-build/topo-run 은 *단일 거대 함수*(buildTopology 280줄·run 240줄·공유 지역변수)라 같은 패턴 미적용. **목적지 STATE §2 NEXT**(너비/멀티프로세스 전 게이트) → 게이트: per-tick Ops/spec-add 를 helper 로 신중 추출(기능 0·reg 0)+close-step src 체크(#43 동반). |
| #16 | bespoke 모드 verify-kit 미승급 | ②/정리 | 🟡 **확장(+9)** | 캐시 4차 9 모드(cachewt·cachemget·cacheneg·cacheadd·cachesetex·cachedel·cachestats·cacheprefix·cachecohere) bespoke·cachecohere 만 현 step spine 모드 → 0252~0259 의 8 기능 양성 의미는 누적 spine 미단언(reg 0 는 보존). **목적지 STATE §3 OPEN GAPS** → 게이트: 안정 모드 묶음별 스모크 승급(끄면 현 spine=reg 0). |
| #54 | 캐시 4차 *집중 편중*(한 박스 9 기능·load-bearing 보류) | 단계/SKILL §3.6 | 🟡 **신규(평결)** | 너비 1차 완료라 2차 허용·과심화 반사 미발동(⬜/🌱 0)이나, 균형 라운드(5박스×2)와 달리 캐시 단독 9 기능·#51b/#49/#9 가 3라운드 연속 보류. **목적지 STATE §2 NEXT**(방향 권고) → 다음 묶음은 캐시 추가 심화보다 #49 분할·#51b 브리지·#9 멀티프로세스 우선. |
| #55 | write-through 가 주입 backing map 에만 기록(실 영속 아님) | 데이터 | 🟡 **신규(경미·#cache5 family)** | 0252 write-through 는 `cache.js` 의 `source`(주입 Map)에 동시 기록·실 PersistStore 박스 아님 → "정합의 절반"(영속 절반은 6-data 캐시 기능5 ⬜). **목적지 STATE §3 OPEN GAPS** → 게이트: 소스를 실 PersistStore 박스화(write-behind·끄면 현 주입 map=reg 0). |
| #51b | running(executed SSOT)↔실 EntityZone(zone.js) 미배선 | ⑤/③ | 🟡 **유지**(0251 무영향) | 0251 은 running 로직을 *이동만*(추상 레지스트리 그대로)·실 zone.js 브리지 미진전. **목적지 STATE §2 NEXT**(load-bearing·#49 후) → 게이트: running↔실 zone.js 핸드오프(OFF→현 추상=reg 0). |
| #53 | reconcile drift-0 by-construction | 검증 | 🟡 **유지**(0251 무영향) | 분할이 코드 위치만 옮김·구조적 drift-0 불변. **목적지 STATE §3** → #51b 실 배선이 진짜 분기 도입 시 load-bearing 화. |

> **그 외 유지**(0251~0260 무접촉): #46·#48·#44·#47·#45·#43·#50·#41·#39·#9·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#36·#37·#38. (#43 close-step src size 체크 미구현이 #49/#52 자동 미탐지를 또 허용 — #52 는 수동 `wc -c` 로만 보였다.)

> **단계 평결(SKILL §3.6)**: 1차 너비 완료 유지(⬜/🌱 0). 0251 정리(#52 ✅)는 0241-0250 verdict 집행·정당. 캐시 4차(0252~0260)는 2차 고도화로 허용·신규 척추 위반 0이나 *집중 편중*(#54) — **다음 묶음 권고: #49 wiring 분할(게이트) + #51b 실 zone.js 브리지(load-bearing) + #9 멀티프로세스**를 캐시 추가 심화보다 우선. review=기록, step-loop 이 STATE §2 로 승급.
