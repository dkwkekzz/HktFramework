# ⑤ 코디네이션 — "누가 어디에" 의 단일 진실

> 인덱스: [README.md](README.md) · 구조 권위: [../../SPINE.md](../../SPINE.md) §2 · 마커: [../../STATE.md](../../STATE.md) §5
>
> **계층 한 줄**: "플레이어 X 는 어느 게이트웨이·어느 존" 과 "어떤 존을 어느 서버가 맡나"를 한 곳이 안다. 어떤 단일 서버도 *영구 중심*이 아니다 — 죽으면 다른 데가 맡고, split-brain(둘이 동시에 주인) 0. 그리고 *소비자가 죽으면 스스로 되살린다*.

---

## 세션/프레즌스 서버 🟡 자라는 중 *(전용 박스 → 쓰기·발행·읽기 전 경로 failover-safe)*

**무슨 서버인가**: "누가 지금 어디에 있나"(소비자·세션의 위치/건강)를 한 곳에서 아는 SSOT 서버(`src/svc-presence.js`·관측 `svc-presence-monitor.js`). *비유 — 안내 데스크*: 모두가 "X 어디 있어?"를 여기 한 곳에 묻고, 직원이 쓰러지면 옆 직원이 같은 장부로 이어받되 쓰러진 직원의 옛 방송은 무시한다.

**필요한 기능들** (기능마다 다른 이론에 기댄다):

1. **위치 레지스트리(조회 한 곳)** ✅ — 왜: 귓속말·파티·핸드오프가 "X 어디"를 떠돌이로 찾으면 안 됨. / 어떻게: 단일 진실 원천(SSOT) 등록. / 했나: `step-0001` 레지스트리 씨앗.
2. **건강 프레즌스(누가 down/up)** ✅ — 왜: 위치를 능동적으로 알아야 치유 가능. / 어떻게: 소비자 건강을 버스로 알고 svc.presence 발행 + presmon 상태 기계. / 했나: `step-0055~0063`(아직 orch 곁붙이).
3. **전용 박스로 독립(디커플)** ✅ — 왜: orch 에 얹히면 이중 SSOT·강결합. / 어떻게: SSOT+발행을 PresenceService 로 떼고 보고 버스화(orch 가 주소 모름). / 했나: `step-0064~0065`.
4. **failover(SSOT 가 죽어도)** ✅ — 왜: 한 곳이 죽으면 끊김. / 어떻게: shadow 복제(같은 보고로 그림자)→승격 인계→하트비트 침묵으로 자가 사망 감지. / 했나: `step-0066~0068`(외부 트리거 0).
5. **읽기(pull) 질의 + 연속성** ✅ — 왜: 구독 못 한 쪽도 상태를 물으면 답해야. / 어떻게: pull 질의 + 승격 시 새 주소 공지→질의자 재타깃. / 했나: `step-0069~0070`.
6. **질의 인터페이스 실사용** ✅ — 왜: 인터페이스는 써야 산다. / 어떻게: 귓속말/파티 라우터(계층3)가 이 SSOT 를 조회처로. / 했나: `step-0071~0074`(읽기 failover 가 소비자 쪽까지 연속).
7. **디스커버리 메아리 펜싱** ✅ — 왜: 지연 공지가 죽은 박스로 역-재타깃하면 위험. / 어떻게: 승격 공지에 epoch(단조)→본 최고 이하 거부. / 했나: `step-0105~0106`(`svc-presence.js:70`·읽기-디스커버리 split-brain 0).
8. **진짜 플레이어 프레즌스** ⬜ — 왜: 지금은 *서비스 소비자* 건강만 앎. / 어떻게: "플레이어 X 가 어느 게이트웨이/존" SSOT. / 했나: 미착수.

**지금 어디 / 다음**: 등록 씨앗 → 전용 박스 → 쓰기·발행·읽기 전 경로 failover-safe SSOT 까지. 다음 = 진짜 플레이어 프레즌스. (펜싱 epoch 단일 클럭 가정 #29·펜싱 로직 중복 #28 잔여.)

## 오케스트레이터 서버 🟡 자라는 중 *(failover + self-healing 제어 루프)*

**무슨 서버인가**: 존 배치·죽은 노드 인계·죽은 소비자 되살리기를 *동적으로* 결정하는 서버(`src/orchestrator.js`). *비유 — 당직 관리자*: 주인이 항상 정확히 한 명이게 정하고, 누가 쓰러지면 대타 투입→확인→재시도→끝내 포기까지 자동.

**필요한 기능들**:

1. **zone failover + 펜싱(split-brain 0)** ✅ — 왜: 정적 배치 한계·주인 둘이면 split-brain. / 어떻게: lease·진짜 kill 감지·epoch 펜싱·재-provisioning. / 했나: `step-0009~0013`(broker lockstep 배리어=검증용 결정론 받침).
2. **소비자 self-healing 제어 루프** ✅ — 왜: 죽은 소비자가 사람 개입 없이 되살아나야. / 어떻게: 제어 루프(감지→recover→확인→재시도→상한 포기→발행). / 했나: `step-0056~0060`.
3. **역할 분리(순수 오케스트레이터)** 🟡 — 왜: 한 박스가 SSOT·치유·결정을 다 쥐면 비대. / 어떻게: 프레즌스 SSOT 떼냄(치유 HealService 는 아직 겸함). / 했나: `step-0064` 프레즌스 분리(치유 분리 ⬜).
4. **진짜 비동기(lockstep 배리어 해제)** ⬜ *가장 큰 빚* — 왜: 결정론이 아직 중앙 lockstep 에 의존. / 어떻게: 논리/벡터 클럭·인과 순서로 배리어 없이 결정론. / 했나: 미착수(STATE §3 🔴).
5. **존 배치 SSOT + 질의** ✅ 기본 — 왜: 정적 배치(2존 고정)는 한계·"누가 어디서 도나"를 한 곳이 알아야 라우팅·재배치가 선다. / 어떻게: 배치 맵 SSOT(zoneId→host·재배치 덮어씀) + 원격 request/reply 질의. / 했나: `step-0203` placeZone(배치 결정 권위) + `step-0204` placeQuery→placeReply(게이트웨이가 존 위치 조회·순수 읽기). 정적 배치 한계 제거 씨앗.
6. **부하 분산 배치 + 재배치 핸드오프 + 자동 트리거/드레인** ✅ — 왜: 배치를 부하로 결정·존을 host 사이로 이주·불균형/퇴역을 사람 손 없이 자동 해소. / 어떻게: ⒜ 후보 host 중 최소 부하 선택·동률 결정론 tie-break ⒝ 재배치는 release(기존)+acquire(신규) 쌍(권위 단일 소유 보존·공백/중복 0) ⒞ 불균형(최대−최소≥2) 자동 감지→최대→최소로 균형까지 수렴 ⒟ host 퇴역 시 그 host 의 모든 존을 나머지 최소부하로 연쇄 이주. / 했나: `step-0217` placeAuto + `step-0218` placeMigrate + `step-0223` placeRebalance + `step-0224` placeDrain. 미주입이면 휴면(reg 0).
7. **배치 결정→실 존 런타임 집행(executed SSOT)** ✅ — 왜: 6 의 배치 정책(place/migrate/rebalance/drain)이 `orchestrator.js:placement` *advisory paper map* 만 갱신하고 실 존은 안 움직였다(#51·"누가 어디서 *돈다*"가 아니라 "*돌아야 한다*"만). / 어떻게: 결정(placement)과 별개로 *실제 가동 중* 존 런타임을 추적하는 executed SSOT(`running`·zoneId→실 가동 host)를 두고, 모든 배치 op 가 paper 갱신마다 실 런타임 lifecycle(start/migrate/stop/re-acquire)을 구동(`placeExecute` 플래그·instance.js active/route 수명주기 동형). 결정==집행 표류 0(`placementDrift`). / 했나: `step-0241` running SSOT(start)→`0242` executed migrate(release+acquire)→`0243` rebalance→`0244` drain→`0245` reconcile capstone(drift 0)→`0246` stop→`0247` auto→`0248` host 장애 복구(placeHostDown·비자발 re-acquire)→`0249` 전 lifecycle capstone→`0250` placeQuery 가 실 가동 host 회신.
8. **추상 running→실 EntityZone(zone.js) 브리지** ✅ — 왜: 7 의 `running` 은 아직 zoneId→host *문자열* 추상 레지스트리였다(#51b·집행 SSOT 이되 실 `EntityZone`(`zone.js`) 인스턴스와 끊김). / 어떻게: 배치 집행이 *실 EntityZone 인스턴스*를 host 에 띄우고/이주하고/내리는 런타임 레지스트리(`zoneRuntimes`·`orch-zonebridge.js` 믹스인). 팩토리는 `makeActor`(`topo-actors.js:65`)가 zoneBridge ON 일 때 주입(직렬화 불가 함수→spec 아닌 액터 구성 시점·멀티프로세스-safe). 추상↔실물 정합 질의로 표류 0 단언. 기능마다 다른 이론: start=인스턴스화·migrate=같은 핸들 host 원자 교체(**상태 보존**)·hostdown=새 인스턴스(**상태 소실·비자발**·migrate 와 의미 분리)·drift/coherent=구조 불변. / 했나: `step-0272` 레지스트리(_bridgeStart·`orch-zonebridge.js:11`)→`0273` migrate(같은 핸들·zoneStarts 불변)→`0274` stop→`0275` hostDown 재가동(새 인스턴스)→`0276` zoneRuntimeDrift→`0277` rebalance 실 핸들 균형→`0278` drain 비움+bridgeCoherent→`0279` placeQuery runtimeHost 회신→`0280` fullyCoherent capstone(placement==running==zoneRuntimes 3층). zoneBridge OFF→전 step 비트 동일(reg 0). **데이터 평면(게이트웨이→실 존 enter/move·이주 시 entity 무손실)은 #56 으로 닫힘(아래 9)** — lifecycle/SSOT 층(이 항)에 이어 entity 트래픽까지 흐른다.
9. **브리지 존 데이터 평면(entity 트래픽·#56)** ✅ — 왜: 8 의 `zoneRuntimes` 는 *비활성 핸들*이었다(entity 0·`onTick` 미호출) → 0273 migrate "상태 보존"이 *구조적*(같은 핸들·zoneStarts 불변)이지 *행동적*(entity 실제 생존)이 아니었다(잃을 상태 0·리뷰 0271~0280 #56). / 어떻게: 게이트웨이→orch `zoneEnter`/`zoneMove`/`zoneLeave` 를 실 `EntityZone` 핸들로 라우팅(`_bridgeEnter`/`_bridgeMove`/`_bridgeLeave`·`orch-zonebridge.js:74,82,90`) + orch 가 자기 런타임 `onTick` 구동(`_tickRuntimes:99`·net 싱크가 view 흡수·`topo-actors.js:71`). lifecycle op 별 보존 의미를 *행동적으로* 단언: migrate=같은 핸들→무손실·hostdown=새 인스턴스→소실 계측(`zoneEntitiesLost:42`)·stop=계획적 폐기(`zoneEntitiesDiscarded:52`)·graceful(rebalance/drain)=같은 핸들→census 보존. entity 권위 단일 소유(`entitiesSingleOwner:111`)·executed 정합(`entityCoherent:117`·orphan 0)·보존 회계 항등식(`entityConserved:125`: total=enters−leaves−lost−discarded)·전 데이터 평면 capstone(`entityFlowCoherent:123`=fullyCoherent 3층+entityCoherent). / 했나: `step-0281` enter→`0282` move+런타임 tick→`0283` leave→`0284` migrate 무손실(행동적·totalEntities)→`0285` hostdown 소실→`0286` stop 폐기→`0287` 단일 소유→`0288` 정합(orphan 0)→`0289` graceful census 보존→`0290` capstone. 플래그 `zoneEntityFlow` OFF→0280 비트 동일(reg 0). **게이트웨이→실 존 *직접* 라우팅은 #9 으로 닫힘(아래 10·0291~0300)** — 실 host.js *물리* 프로세스 분리만 잔여.
10. **데이터 평면 게이트웨이 직접 라우팅 인계(#9)** 🟡 — 왜: 9 까지 entity 트래픽이 게이트웨이→orch→런타임이라 orch 가 라우팅 *결정*을 쥠(코디네이션이 데이터 평면 경유 — SPINE §4 는 게이트웨이가 직접 중계). / 어떻게: orch 는 *라우팅 결정을 게이트웨이에 넘기고 검증·집행만* 한다 — ⒜ 배치 집행마다 zone→host 를 게이트웨이에 push(`_pubZoneLoc`·서비스 디스커버리) ⒝ 게이트웨이가 host 태깅해 보낸 `zoneDeliver` 를 *실 런타임 host(running) 일치 시만* 적용(stale 거부·`orch-control.js` zoneDeliver 분기) ⒞ 장애 시 게이트웨이에 hostDown broadcast(죽은 host dir 일괄 무효화). 라우팅 권위는 게이트웨이로 이동·orch 는 존-host 보유자(=실 host.js 의 인프로세스 대역)로 검증·적용. / 했나: `step-0293` _pubZoneLoc→`0294`~`0295` zoneDeliver enter/move/leave 적용+stale 거부→`0297` hostDown broadcast→`0298` entityDirectCoherent→`0300` directFlowCoherent capstone(`orch-zonebridge.js`·`orchestrator.js`). 미설정이면 entity 가 0290 경로(→orch 직접·reg 0). *잔여: zone-host 핸들이 orch 인프로세스 `zoneRuntimes` Map — host 를 *프로세스 컨테이너*로 묶는 것은 아래 11(0301~0310 ✅)·실 host.js OS 프로세스/소켓 spawn 은 그 잔여.*
11. **데이터 평면 host 프로세스 컨테이너(실 host.js 물리 분리·#9 잔여)** 🟡 — 왜: 10 까지 게이트웨이 직접 라우팅은 섰으나 zone-host 핸들은 여전히 orch 인프로세스 *flat* `zoneRuntimes` Map·host=문자열 태그였다(실 host.js 프로세스 단위가 없음 — orch 가 모든 존을 직접 보유·tick). / 어떻게: host 를 *1급 프로세스 컨테이너*(`zoneHosts`: host→{zones,inbox})로 묶는다 — 실 host.js 프로세스(여러 존 소유·자기 소켓 수신·자기 루프 tick·spawn/despawn)의 씨앗. 기능마다 다른 이론: 귀속=단일 컨테이너 멱등(`_hostSet`·어디 있든 떼고 한 곳에)·수신=host 단위 inbox(per-runtime mbox 대체·소켓 1개=host 1개)·tick=host 자기 루프(자기 inbox drain 후 자기 존만 onTick)·roster=첫 존 spawn·마지막 존 despawn·**stale 거부=이주로 떠난 존 frame 거부**(실 프로세스 이중 쓰기 방지·c.zones 가 진짜 권위)·정합=single+drift0+roster 회계 닫힘. host-proc 메서드는 `orch-hostproc.js` 믹스인(0305 분할). / 했나: `step-0301` 컨테이너 레지스트리(`_hostSet`·`orch-hostproc.js`)→`0302` host 자기 inbox 수신+자기 루프 drain·tick(`orch-zonebridge.js` `_zoneDeliver`/`_tickRuntimes`)→`0303` 단일소유/drift 질의→`0304` roster register/deregister(reg−dereg==현 host)→`0305` 정리 분할(host-proc→`orch-hostproc.js`·기능 0)→`0306` inbox stale 거부(`zoneHostStale`·recv==drained+stale·probe)→`0307` host 프로세스 census→`0308` `hostContainerCoherent` primitive→`0309` 다중 churn bijection(`zoneHostSnapshot`)→`0310` capstone `hostProcCoherent`(=directFlowCoherent && hostContainerCoherent). 플래그 `zoneHostProc` OFF→0300 비트 동일(reg 0). *잔여: zoneHosts 는 orch 인프로세스 *논리* 컨테이너 — 실 host.js *OS 프로세스/소켓* spawn(cluster-run.js 통합)은 다음 arc.*
12. **host 프로세스 부하 균형·생애주기·장애(#9 잔여 심화)** 🟡 — 왜: 11 의 host 컨테이너는 *섰지만* 오케스트레이터가 그걸 *부하로 운영*하지 못했다(어느 host 가 만원인지·새 존을 어디로 보낼지·죽으면 누가 인수). / 어떻게: 기능마다 다른 척도/이론 — 존 수 불균형(`hostLoadSkew`)·**entity(≈동접) 가중 불균형**(`hostEntitySkew`·존 수는 같아도 만원 host 를 드러냄)·생애주기 이벤트 로그(`hostLifecycle`·spawn/despawn 순서·실 `cluster.spawnOne`/`killHost` 호출 지점 씨앗·`orch-hostproc.js:18,21`)·entity 가중 *자동 배치*(`placeAutoE`·최소 entity host 선택·`orch-control.js:24`)·entity 가중 *재배치*(`placeRebalanceE`·무거운→가벼운 존 이주·**gap 단조 감소 종료 보장**·무손실·`_rebalanceByEntities`·`orch-hostproc.js:57`)·다중/동시 host 장애 failover(생존 host 인수·죽은 host 비움·despawn 로그)·균형 술어(`hostBalanced`·`orch-hostproc.js:55`). / 했나: `step-0311` hostLoadSkew→`0312` hostLifecycle→`0313` 다중 존 host 장애+hostZones→`0314` hostEntitySkew→`0315` 다중 동시 host 장애+hostCount→`0316` placeAutoE→`0317` placeRebalanceE→`0318` hostBalanced capstone. 새 op 미수신/플래그 OFF→직전 step 비트 동일(reg 0). *한계: 부하 척도는 *수*(존/entity)만(tick 비용·대역 미반영)·정합 술어 by-construction(#53)·자동 트리거(임계 초과 자동 발화) 미연결(명시 op 의존)·zoneHosts 는 여전히 인프로세스 논리 컨테이너(실 OS 프로세스 #57).*
13. **적응형 recoverTimeout** ⬜ — 왜: 고정 timeout 은 부하 변동에 둔감. / 어떻게: 부하/지연 신호 기반 적응. / 했나: 미착수(2차 잔여).
14. **다운스트림 뷰 egress·신뢰 전파(orch=host·#9 후속·SPINE §4 경로2·0331~0350)** ✅(인프로세스) — 왜: 12 까지 host 컨테이너(zoneHosts)가 런타임 존을 *돌리고* AOI 뷰를 산출하지만 그 뷰는 버퍼에 *포착*만 됐다(②월드 기능 8⒜·게이트웨이→실 클라 전파 미연결 #60). orch(=host 보유자)가 그 뷰를 실제로 *내보내고* 손실에도 닿게 해야. / 어떻게(기능마다 다른 이론 — *기존 netcode 패턴의 다운스트림 판*): ⒜ egress 드레인(`_drainZoneEgress`·`orch-views.js`·런타임 존 버퍼의 새 frame 을 매 tick 게이트웨이로 송출·per-세션 단조 `dseq`·per-rt 커서) ⒝ ack 자기-크기조정(`zoneEgressBuf` 미-ack 보관→게이트웨이 `zoneViewAck`→워터마크 가지치기·버스 ack 0040 의 다운스트림 판) ⒞ gap-resync 재전송(`_resendEgress`·게이트웨이 gap 감지 `zoneResync`→버퍼서 dseq≥from 재전송·0008 ack/NAK 판) ⒟ 타임아웃 재전송(`_retransmitStale`·ack 없이 egressTimeout 경과 frame 재전송·세션 마지막 frame 손실 복구·zone heartbeat/bus 0058 판) ⒠ leave 정리(`_bridgeLeave` 에 egress 상태 제거·무계 성장 방지) ⒡ 정착 술어(`downstreamSettled` 모든 버퍼 0)·월드 전 정합 capstone(`downstreamWorldCoherent`=모든 존 downstreamCoherent && settled·`orch-views.js`). 손실 주입(`egressDrop` 테스트)·`egressTimeout` opt. / 했나: `step-0331` egress→`0335` dseq→`0336` ack→`0337` gap-resync→`0338` 타임아웃→`0339` leave 정리→`0341` 전파 capstone(downstreamDeliverCoherent)→`0350` 월드 grand capstone(downstreamWorldCoherent). 플래그 `zoneEgress` OFF→0330 비트 동일(reg 0). 실 클라 종단(`DownClient` 수렴 desync 0)은 ②월드 기능 8⒞. *잔여: egress·zoneHost 모두 인프로세스(실 OS 프로세스/소켓 spawn #57·아래 15).*
15. **실 cluster 호스트 드라이버 — orch zoneHost → 실 host.js OS 프로세스(#57·0351~0360)** ✅(드라이버 계약 + 실 spawn·잔여=실 데이터 평면) — 왜: 11~14 의 `zoneHosts`/egress/DownClient 가 전부 orch 인프로세스 *논리* 컨테이너였다(#57 최상위 load-bearing·"한 프로세스가 모든 일"의 잔재) → 논리 컨테이너를 *실 `host.js` 자식 OS 프로세스*로 물질화해야 SPINE §5 ⑤(실 OS 프로세스/소켓)가 선다. / 어떻게(기능마다 다른 이론): ⒜ *드라이버 계약* — orch 의 zoneHost 생애주기/데이터 평면 이벤트를 훅(on*)으로 노출(reconcile 패턴: 목표 매니페스트 `hostSpawnPlan`·tick 델타 `hostSpawnDelta`·`_hostSet` spawn/despawn→onSpawn/onDespawn·존 귀속→onAssign/onUnassign·frame→onFrame·egress→onEgress·`orch-hostproc.js`·`orch-bridge-init.js`) ⒝ *번역↔집행 분리(#4 비동기 경계)* — `ClusterHostDriver`(`cluster-hostdriver.js`)가 이벤트를 cluster 명령(spawnOne/killHost/rpc)으로 *동기* 번역(commands 큐)하고, `flush(cluster, specOf)` 가 그 큐를 실 `Cluster`(child_process)에 *async* 집행 ⒞ *실 인스턴스화* — `host.js` `zoneadd` cmd(가동 중 존 *증분* 추가·full-reset init 과 달리 기존 보존)로 한 실 host.js 프로세스가 여러 존 소유 ⒟ *정합 capstone* — `clusterHostsCoherent`(논리 컨테이너 ↔ 드라이버 명령 순계가 가동 host/존 수와 1:1). 전부 OFF 플래그(`clusterDriverRecord`/`clusterDriverReal`)·미부착→호출 0·reg 0. / 했나: `step-0351` hostSpawnPlan→`0352` hostSpawnDelta→`0353` clusterDriver 훅 seam→`0354` onAssign/onUnassign→`0355` onFrame→`0356` onEgress(계약 완비)→`0357` ClusterHostDriver 번역+flush→`0358` **실 child_process 존 인스턴스화 E2E**(실 host.js livePid 1·snapshot 에 zone z1)→`0359` zoneadd 다중 존(한 프로세스 z1·z2)→`0360` capstone `clusterHostsCoherent` + 실 Cluster 다중 host(A=[z1,z2]·B=[z3]). *0361~0370 에서 실 데이터 평면까지 집행(아래 16).*
16. **실 cluster 데이터 평면 집행 — 실 host.js 프로세스에서 도는 전 데이터 평면(#57·0361~0370)** ✅(실 프로세스 desync 0·잔여=runMulti 코어 통합) — 왜: 15 는 실 host.js 를 *띄우고 존을 인스턴스화*까지였다(spawn+zoneadd). entity 가 실제로 그 프로세스에서 *흐르고*(deliver/tick/이주/장애) 그 결과가 in-proc 권위와 일치해야 "실 OS 프로세스로 도는 인프라"가 선다. / 어떻게(기능마다 다른 이론·전부 실 child_process·소켓): ⒜ deliver(`flush` 가 frame 동봉 `{cmd:'deliver',items}`→실 `zone.onMsg`·`cluster-hostdriver.js:32`) ⒝ zonedel(`host.js` zonedel cmd·가동 중 존 제거·다른 존 보존·`host.js`) ⒞ tick+egress(`tickZone`→실 `zone.onTick` pending move 적용+view_delta 산출·`cluster-hostdriver.js:50`) ⒟ migrate 상태 보존(`migrateZone` snapshot→zoneadd→loadstate→zonedel·실 프로세스 경계 무손실·release+acquire·`:55`) ⒠ killHost+failover(`cluster.killHost` SIGKILL + `failoverZone` 생존 host 새 인스턴스·상태 소실 정직한 한계·`:65`) ⒡ reconcile(`reconcile` plan→spawn/zoneadd/killHost 상태 기반 수렴·`:70`) ⒢ 격리(`hostEntities` 교차 누수 0·`:81`) ⒣ 통합 E2E(`driveCluster` reconcile+deliver+tick 한 호출·runMulti analog·#62·`:88`) ⒤ 정합 술어(`clusterDesync` 양방향·`clusterCoherent`=desync 0·ghost 주입으로 by-construction 아님 입증·`:99,114`). 전부 OFF 플래그 `clusterDriverReal`·미주입→호출 0·reg 0. / 했나: `step-0361` deliver→`0362` zonedel→`0363` tick/egress→`0364` migrate 상태보존→`0365` killHost+failover→`0366` reconcile→`0367` 격리→`0368` driveCluster→`0369` clusterDesync→`0370` capstone `clusterCoherent`(2 host·3 zone·migrate desync 0). *잔여: cluster-run.js runMulti 코어에 orch 상주(broker 측 제어 평면)·연속 tick 루프(아래 17)·in-flight frame 재라우팅·업스트림 intent 실 클라(#61).*
17. **broker 측 제어 평면 상주 코디네이터(#62·runMulti 통합·0371~0380)** 🟡(능력 ✅·잔여=옛 runMulti 합류+양방향 동기) — 왜: 16 까지 실 host.js 데이터 평면이 desync 0 으로 돌았으나 그 구동을 *verify 하니스가 ad-hoc 으로*(driveCluster 0368 을 verify 가 직접 호출) 했다 — "한 프로세스가 broker 처럼 cluster 를 *상주 구동*"하는 제어 평면 코어가 없었다(#62). / 어떻게(기능마다 다른 이론): 새 박스 `cluster-coord.js` `ClusterCoordinator(orch,cluster,specOf,driver)`(require 0·순수 주입 의존) — ⒜ start(orch.hostSpawnPlan→driver.reconcile spawn+zoneadd 토폴로지 수렴) ⒝ tick(t)(pending frame 재생+전 존 1 tick·driveCluster per-tick 몸통) ⒞ run(ticks)(*연속 tick 루프*·runMulti 핵심 상주화) ⒟ 매-tick desync 가드(run 루프가 매 tick 끝 `clusterDesync`→`maxDesync`·중간 발산 검출·ghost 주입으로 진짜화) ⒠ 상주 migrate(driver.migrateZone·상태 보존·release+acquire) ⒡ 상주 failover(killHost+죽은 host 존 재가동·상태 소실 정직) ⒢ syncPlan(*비파괴* reconcile — snapshot 차분→누락 존만 zoneadd·기존 보존·idempotent·drift 자가 치유·driver.reconcile 의 파괴적 재-zoneadd 와 대조) ⒣ egress 집계(존별 `egressByZone`·누계 `egressTotal`) ⒤ report(운영 대시보드 단일 스냅샷) ⒥ coordCoherent capstone(`maxDesync==0 && 현 clusterDesync==0`·연속 루프 내내+지금 한 몸). / 했나: `step-0371` 골격+start→`0372` tick→`0373` run 연속 루프→`0374` 매-tick desync 가드→`0375` 상주 migrate→`0376` 상주 failover→`0377` syncPlan 비파괴 자가 치유→`0378` egress 집계→`0379` report→`0380` capstone `coordCoherent`(start→run(5)→drift→syncPlan 치유 뒤 정합). 새 박스·run() 미사용→reg 0(구조적). *잔여(세): ⒜ 이 코디네이터와 옛 `cluster-run.js` lockstep runMulti(14-프로세스)가 *두 별개 runner*·코드 합류 미수행(#62) ⒝ 코디네이터 migrate/failover 가 orch 권위 zoneHost 미갱신(단방향·실 migrate 후 clusterDesync stale 발산→capstone 이 migrate/failover 제외·#65·아래 18 해소) ⒞ reg 구조적 0 + bespoke 미승급(#16)으로 spine 안전망 얇음.*
18. **코디네이터 placement 권위 — 양방향 동기(#65·0381~0390)** 🟡(migrate/failover 정합 ✅·잔여=이중 권위 합류) — 왜: 17 의 코디네이터는 *데이터 평면*은 상주 구동했으나 lifecycle(migrate/failover)이 placement 권위를 안 들어, 실 migrate 후 `clusterDesync`(orch plan 기준 host 조회)가 stale 로 발산했다(#65·0380 capstone 이 migrate/failover 를 *제외*해야 했던 근본 원인). / 어떻게(권위 분리 이론): 코디네이터가 *placement 권위(where=어느 host 가 어느 존)*를 직접 소유하고 orch 는 *entity 권위(what=존 안 누가 어디)*를 유지 — 둘을 직교 분리해 lifecycle 갱신만 따라가면 정합. ⒜ placement SSOT(`cluster-coord.js:38`·start 가 hostSpawnPlan 에서 초기화 `:46`) ⒝ coordDesync(placement 로 host 조회·orch zoneEntityPos[host-무관]로 비교·`:54`) ⒞ migrate 가 placement 갱신(`:97`·핵심 fix→migrate 후 desync 0·옛 clusterDesync 1 대조) ⒟ run 가드·coordCoherent 가 coordDesync 채택(`:89,150`) ⒠ failover 가 placement 갱신+lostZones 기록(`:107,108`·상태 소실 명시 추적) ⒡ lost 의 *기대된 비자발 손실*(reverse 부재)을 coordDesync 에서 제외·forward ghost 검사 유지(`:62`) ⒢ placement-aware report(`:127`) ⒣ syncPlan 이 placement 로 차분(`:115`·migrate 후 옳은 host 복원) ⒤ placementCoherent bijection(placement⟷실 1:1·forward+reverse·`:138`) ⒥ capstone syncedCoherent(maxDesync0 && coordDesync0[lost 제외] && placementCoherent·migrate/failover *포함*·`:153`). / 했나: `step-0381` placement SSOT→`0382` coordDesync→`0383` migrate 갱신→`0384` coordCoherent 채택→`0385` failover+lost→`0386` lost 제외→`0387` report→`0388` syncPlan→`0389` placementCoherent→`0390` capstone syncedCoherent(`coordsyncedcap` 5/5·syncedCoherent Y·coordDesync 0 vs 옛 clusterDesync 1). 새 박스·run() 미사용→reg 0. *잔여: ⒜·⒝ 는 아래 19(0391~0400)에서 해소 — placement↔orch where-view 합류(#67)·tick placement-aware(#66). ⒞ cluster-run.js 합류 미수행(#62 유지).*
19. **tick placement-aware + orch 이중 권위 합류(#66·#67·0391~0400)** ✅(where-view 합류·잔여=entity 컨테이너 동기·#62 코드 합류) — 왜: 18 이 placement 권위로 *측정*(coordDesync/syncedCoherent)은 정합시켰으나 *데이터 평면 구동*은 여전히 stale orch plan 을 썼고(#66·`tick()`/deliver 가 `orch.hostSpawnPlan` 순회 → run 루프 *중* migrate 시 옛 host tick·0390 capstone 은 lifecycle 을 루프 *뒤* 실행해 미발현), placement 가 orch 의 집행 where-view(`orch.running`)와 *이중 권위*로 갈렸다(#67·write-back 아닌 decouple 로 #65 를 푼 대가). / 어떻게(기능마다 다른 이론): **#66 tick placement-aware** — tick 의 존 순회를 `this.placement` 로(`:94`)·deliver 재생 host 도 `this.placement[c.zoneId]` 로(`:89`)·`run(ticks, onTick)` mid-loop lifecycle 훅(`:105`)으로 루프 *중* migrate 를 발현시켜 placement-aware tick 이 즉시 추종(maxDesync 0). **#67 이중 권위 합류** — orch 집행 where-view 노출(`orchWhere()`·`:63`)·두 권위 합의 술어(`authoritiesAgree()`·`:65`·write-back 전 migrate 후 발산 노출)·migrate/failover 가 `_orchWriteBack(zone,host)`(`:67`)으로 orch.running/placement 동기(`:120`·`:131`·lost 존도 where 합의·entity 손실은 coordDesync 가 제외)·report 가 authoritiesAgree 노출(`:159`)·통합 술어 `unifiedCoherent()=syncedCoherent && authoritiesAgree`(`:182`·orch.running stale 주입 시 N·by-construction 아님). / 했나: `step-0391` tick placement 순회→`0392` deliver placement host→`0393` run(onTick) mid-loop migrate 발현(maxDesync 0·대조 clusterDesync 1)→`0394` orchWhere→`0395` authoritiesAgree 발산 노출→`0396` migrate write-back→`0397` failover write-back→`0398` report authoritiesAgree→`0399` unifiedCoherent→`0400` grand capstone(루프 *중* migrate+failover 후 unifiedCoherent·`coordunifiedcap` 5/5·unified Y·maxDesync 0·rpt coherent&&authoritiesAgree·옛 clusterDesync 1 대조). 새 박스·run() 미사용→reg 0(구조적·write-back 은 run() 종료 후 orch 객체에만 작용). *잔여: ⒜ write-back 은 where-view(orch.running/placement Map)만 동기 — orch 내부 *zoneHost entity 컨테이너*(0301~)는 미동기(#68·경미·where 권위는 합류됨) ⒝ cluster-run.js runMulti 코드 합류(#62 유지·아래 20 에서 능력 합류) ⒞ 업스트림 intent 실 클라(#61)·진짜 비동기(#4).*
20. **코디네이터를 runMulti 복원력 코어로 승격(#62 능력 합류·0401~0410)** 🟡(능력 superset ✅·잔여=코드 플립) — 왜: 17(0371~0380)이 코디네이터를 broker 측 제어 평면으로 *상주화*했으나, 옛 `cluster-run.js` runMulti(14-프로세스 lockstep·partition/kill/falsedeath/reprovision+mirror/lease-timeout/snapshot-restart/reconstruct 보고)가 *별개 runner* 로 병존했다(#62). 코디네이터엔 그 *복원력 능력*이 없어 runMulti 를 대체할 수 없었다. / 어떻게(기능마다 다른 이론·runMulti 의 각 복원력 메커니즘을 zone cluster 상주 제어 평면에 이식): ⒜ **epoch 펜싱**(`fence(host)`·`cluster-coord.js:49`·presumedDead+epoch·tick 이 추정 사망 host 의 존 건너뜀·stale 발신 차단·runMulti `:69,76` 판) ⒝ **silence lease-timeout**(`sweepSilence()`·`:51`·socketDead host 연속 침묵 leaseTimeout 초과 시 자동 fence·즉발 오판 방지·runMulti observeSilence `:72` 판) ⒞ **상태 보존 restart**(`restart(zone,newHost)`·`:152`·pre-kill snapshot→kill→spawn→loadstate·*계획적* 재시작 무손실·runMulti invRestart `:90` 판·failover 의 비자발 소실과 대조) ⒟ **따뜻한 standby reprovision**(`reprovisionStandby(zone,standbyHost)`·`:166`·snapshot→standby 사본+`mirrors` 등록·runMulti rep+mirrors `:202,219` 판) ⒠ **미러 입력 복제**(`tick()` 이 deliver frame 을 standby 에 재생+standby shadow tick·`:90`·standby lockstep 동기·runMulti 미러 deliver/tick `:170,198` 판) ⒡ **따뜻한 failover 승격**(`promoteStandby(zone)`·`:178`·primary 사망 시 동기된 standby 를 primary 로 승격·**상태 손실 0**·#63 완화·0376 빈 재가동과 대조) ⒢ **clusterInfo 재구성 보고**(`:242`·livePids/placement/epoch/presumedDead/복원력 계측·runMulti 반환 계약 `:227` 판) ⒣ **runScenario 통합 시나리오 루프**(`:119`·스크립트 열화 시나리오[migrate/restart/reprovision/kill/fence@at·sweepSilence]를 run(ticks,onTick) 위에·runMulti 의 시나리오 주입 `:176,202` 를 선언적으로) ⒤ **capstone runMultiCoherent**(`:236`·unifiedCoherent && 미러 standby 실재·종합 복원력 시나리오 후도 참). 모두 새 메서드·OFF 플래그(미호출→0400 동치)·새 박스 run() 미사용→reg 0(병존·끄면 회귀 0). / 했나: `step-0401` 펜싱→`0402` silence→`0403` restart→`0404` 정리(헤더 접기 29.4→19.5KB·기능 0)→`0405` reprovisionStandby→`0406` 미러 복제→`0407` clusterInfo→`0408` runScenario→`0409` promoteStandby(`coordpromote` 5/5·killHost→promote a1 보존)→`0410` capstone `runMultiCoherent`(`coordmulticap` 5/5·migrate+reprovision+kill+promote 후 Y·mig/reprov/promo 1). *잔여: 능력은 superset(코디네이터가 runMulti 복원력 전부 보유)이나 옛 cluster-run.js runMulti 가 *이 코디네이터를 호출*하도록 코드 합류(delegate·플립)는 미수행(#62 잔여·플립만)·코디네이터는 zone cluster 범위(runMulti full topology 의 gateway/services/partition 복원력은 미포함).*

21. **코디네이터를 runMulti zone-cluster 복원력의 단일 진입점으로 — 코드 합류(#62·0411~0420)** ✅(zone-cluster 범위·플립 완료) — 왜: 20(0401~0410)이 코디네이터에 runMulti 복원력 *능력 superset* 을 쌓았으나, 옛 `cluster-run.js` runMulti 와 *두 별개 runner* 로 병존했다(#62·"한 일을 두 곳이"). 코드 합류(옛 runMulti 가 코디네이터를 *호출*)가 미수행. / 어떻게(기능마다 다른 이론): verify 가 0401~0410 내내 손으로 짜던 배선·구동을 cluster-run.js 가 소유한 단일 진입점으로 흡수 — ⒜ **배선 패키지**(`coordSetup(opts,deps)`·`cluster-run.js:284`·run→orch/driver→Cluster spawn→makeClusterCoordinator 4단을 한 함수로) ⒝ **시나리오 번역기**(`coordScenarioFromOpts`·`:297`·옛 runMulti 스크립트 열화 스펙 `opts.coordSc`→코디네이터 `runScenario` 선언 시나리오·migrate/reprovision/kill/promote/fence/sweepSilence/restart 전부) ⒞ **단일 진입점**(`runMultiViaCoord(opts,deps,probe)`·`:314`·coordSetup+번역+runScenario+집계+shutdown 한 호출) ⒟ **반환 계약 parity**(결과 info 에 옛 runMulti clusterInfo 전송/토폴로지 필드 pids·parentPid·ipcMsgs/Bytes·wire 병합) ⒠ **동치 증명**(`coordAuthEquiv`·`:337`·실 cluster entity 위치==in-proc run() 권위·옛 runMulti[비트 동일 e2e]·runMultiViaCoord[coordDesync 0] 둘 다 귀착하는 공유 기준 ⇒ ≡) ⒡ **warm-failover 원자**(`runScenario` 가 promote 처리·`cluster-coord.js:127`·kill 과 같은 onTick→desync 윈도 0) ⒢ **OFF-게이트 위임**(`runMulti` 가 `opts.viaCoord` 이면 `runMultiViaCoord` 호출·`cluster-run.js:30`·미설정→옛 전 토폴로지 lockstep 경로 비트 동일=reg 0·e2e 보존). / 했나: `step-0411` coordSetup→`0412` coordScenarioFromOpts→`0413` runMultiViaCoord→`0414` clusterInfo parity→`0415` coordAuthEquiv 동치→`0416` warm-failover(promote)→`0417` fence/sweepSilence→`0418` restart→`0419` runMulti OFF-게이트 위임(`coorddelegate` 5/5)→`0420` grand capstone(`coordmergecap` 5/5·종합 warm-failover 후 runMultiCoherent Y·mig/reprov/promo 1·a1 보존·parity). 신규 함수 미부착/OFF-게이트·run() 미수정→reg 0. *한계(범위): 위임은 zone-cluster 시나리오 한정(코디네이터 범위)·옛 runMulti 의 전 토폴로지[gateway/services/partition·bus/inventory/chat] 복원력 흡수는 #62 범위 밖·deps.run/zoneSpecOf 주입 시 동작(공개 NET.runMulti deps 엔 부재). 동치는 transitive[공유 권위 귀착]·다른 substrate 직접 비트 대조 아님(#69).*

**지금 어디 / 다음**: zone failover + self-healing 루프 + 존 배치 SSOT/질의 + 부하 배치·재배치·드레인(0217~0224) + 배치 결정→실 존 런타임 executed SSOT(0241~0250) + **추상 running→실 EntityZone(zone.js) 브리지(0272~0280·orch 가 placement 집행으로 실 EntityZone lifecycle 구동·zoneRuntimes·`orch-zonebridge.js`·migrate 상태보존↔hostdown 소실 의미 분리·fullyCoherent 3층 정합)**까지, 그 위에 **브리지 존 데이터 평면(0281~0290·#56 ✅·entity enter/move/leave 가 실 EntityZone 핸들을 흐르고 런타임 onTick 구동·migrate 무손실↔hostdown 소실↔stop 폐기 행동적 단언·단일 소유·정합·보존 회계 capstone entityFlowCoherent/entityConserved)**까지 — 배치 정책이 *실 존 인스턴스*를 띄우고 그 안에 *실 entity 가 산다*(#51b·#56 ✅·코디네이션 SSOT↔실물↔데이터 평면 닫힘). 그 위에 **#9 멀티프로세스 배선(0291~0300·게이트웨이→실 존 직접 entity 라우팅·서비스 디스커버리 디렉토리·전송 seam·host mailbox·이주/장애 라우팅 정합·directFlowCoherent capstone)** 까지 — 라우팅 결정이 게이트웨이로 옮겨가고 orch 는 검증·집행만(데이터 평면이 코디네이션을 우회). 그 위에 **host 프로세스 컨테이너 arc(0301~0310·#9 잔여·실 host.js 물리 분리 씨앗)** 까지 — host 를 1급 프로세스 컨테이너(`zoneHosts`·`orch-hostproc.js`)로 묶어 *자기 inbox 로 수신·자기 루프로 tick·spawn/despawn roster·이주로 떠난 존 frame stale 거부(이중 쓰기 방지)*·정합/census/bijection/capstone hostProcCoherent. **정리: `0251` orch-placement·`0267` orch-control·`0305` orch-hostproc 믹스인 분할(<30KB·#52 ✅)**. 그 위에 **host 프로세스 부하 균형·생애주기·장애 sub-arc(0311~0318·#9 잔여 심화)** 까지 — 오케스트레이터가 host 컨테이너를 *부하로 운영*한다: 존 수·**entity 가중** 불균형 관측(`hostLoadSkew`·`hostEntitySkew`)·생애주기 이벤트 로그·entity 가중 자동 배치/재배치(`placeAutoE`/`placeRebalanceE`·만원 host 회피·gap 단조 감소 무손실)·다중/동시 host 장애 failover·균형 술어 capstone(`hostBalanced`). 그 위에 **다운스트림 뷰 egress·신뢰 전파(0331~0350·#9 후속·SPINE §4 경로2)** 까지 — orch(=host)가 런타임 존 산출 AOI 뷰를 게이트웨이로 송출(`_drainZoneEgress`·dseq)·ack 자기-크기조정·gap-resync/타임아웃 재전송·leave 정리·월드 전 정합 capstone(`downstreamWorldCoherent`)·종단 실 `DownClient` 수렴 desync 0(②월드 8⒞·#60 해소). 그 위에 **실 cluster 호스트 드라이버(0351~0360·#57·기능 15)** 까지 — orch zoneHost 이벤트(spawn/despawn·존 귀속·frame·egress)를 `ClusterHostDriver` 가 cluster 명령으로 번역하고 `flush` 가 *실 `Cluster`(child_process)* 에 집행 → 실 `host.js` OS 프로세스가 존을 인스턴스화(`hostspawnreal`·`hostmultizone`·`clusterhostcap` 5/5·실 host.js 2 프로세스 A=[z1,z2]·B=[z3]). **논리 zoneHost → 실 OS 프로세스 물질화 입증**. 그 위에 **실 cluster 데이터 평면 집행(0361~0370·#57·기능 16)** 까지 — 실 host.js 프로세스에서 entity 가 deliver/tick/이주/장애로 흐르고(`ClusterHostDriver` deliver/tickZone/migrateZone/failoverZone/reconcile/driveCluster) in-proc 권위와 desync 0(`clusterCoherent`·2 host·3 zone·migrate 상태 보존). **host→실 host.js OS 프로세스/소켓 데이터 평면 E2E 입증**. 그 위에 **broker 측 제어 평면 상주 코디네이터(0371~0380·#62·기능 17)** 까지 — verify ad-hoc 구동을 `ClusterCoordinator`(`cluster-coord.js`·start·tick·연속 run 루프·매-tick desync 가드·상주 migrate/failover·syncPlan 비파괴 자가 치유·egress 집계·report·coordCoherent capstone)로 상주화(`coordcap` 5/5 maxDesync 0·drift→syncPlan 치유·desync 0). **broker 측 제어 평면이 실 cluster 를 연속 tick 으로 상주 구동·지속 정합 입증**. 그 위에 **코디네이터 placement 권위 양방향 동기(0381~0390·#65·기능 18)** 까지 — 코디네이터가 placement 권위(where)를 직접 소유(orch=entity 권위/what 분리)하고 migrate/failover 마다 갱신·coordDesync/placementCoherent/syncedCoherent 로 *migrate/failover 를 포함한* 전체 lifecycle 뒤에도 정합(`coordsyncedcap` 5/5·옛 clusterDesync 1 대조). **#65 해소 — 이주/장애 후도 실 cluster==권위**. 그 위에 **tick placement-aware + orch 이중 권위 합류(0391~0400·#66·#67·기능 19)** 까지 — 데이터 평면(tick/deliver)이 placement 권위를 추종해 *run 루프 중* migrate 도 maxDesync 0(#66 발현·해소)·migrate/failover 가 orch 집행 where-view 에 write-back 해 두 where 권위가 단일로 합류(#67·`unifiedCoherent` 5/5·루프 중 migrate+failover 후도 Y·옛 clusterDesync 1 대조). **#66·#67 해소 — placement(where 권위)가 데이터 평면 구동과 orch 집행 뷰 둘 다를 한 몸으로 끈다**. 그 위에 **코디네이터를 runMulti 복원력 코어로 승격(0401~0410·#62 능력 합류·기능 20)** 까지 — runMulti 의 복원력 메커니즘(epoch 펜싱·silence lease-timeout·상태 보존 restart·따뜻한 standby reprovision+미러·warm-failover 승격·clusterInfo 보고·runScenario 시나리오 루프)을 zone cluster 상주 제어 평면에 모두 이식·capstone `runMultiCoherent`(`coordmulticap` 5/5). **#62 능력 합류 — 코디네이터가 runMulti 복원력 superset(특히 따뜻한 standby 승격으로 #63 failover 상태 소실 완화)**. 그 위에 **#62 코드 합류(0411~0420·기능 21)** 까지 — 코디네이터 능력 superset 을 cluster-run.js 단일 진입점(coordSetup/coordScenarioFromOpts/runMultiViaCoord)으로 묶어 옛 runMulti 가 `opts.viaCoord` 로 코디네이터를 *호출*(delegate·`coordmergecap` 5/5·OFF→옛 경로 비트 동일). **#62 ✅해소 — 두 runner 병존 종료(zone-cluster 범위)**. 다음(load-bearing) = **업스트림 intent 실 클라(#61·양방향 실 E2E)** + *진짜 비동기*(#4·논리 클럭) + orch zoneHost *entity 컨테이너* 동기(#68·경미) + 치유 HealService 분리.

---

> **📦 구조 분할(#49 arc·0267·0305·0323·기능 0)**: 오케스트레이터 제어 평면 핸들러(`onMsg`·`onTick`)를 `orch-control.js` 믹스인으로 분리(0251 `orch-placement.js`[배치 런타임]의 짝·`Object.assign(prototype)`·verbatim·reg 0·27.5→18.9KB). 이어 host-proc 층→`orch-hostproc.js`(0305) · 다운스트림 뷰 질의 6종→`orch-views.js`(0323·`orch-zonebridge.js` 30.6→28.1KB 유계화). 오케 믹스인 다분할(코어 + placement + control + zonebridge + hostproc + views + **bridge-init**)·전부 `Object.assign(Orchestrator.prototype)`·능력 무변경(reg 0). **#58 해소(0332)**: 생성자의 브리지/데이터평면/host컨테이너/egress 필드 대입 블록(0272~0331)을 전용 `orch-bridge-init.js` `_initBridgeFields(opts)` 로 이동 → 코어 `orchestrator.js` **29.7KB→22.8KB**(생성자 한 줄 호출·세 박스 모두 <30KB).

## 비동기 실행 substrate (#4) 🟡 자라는 중 *(논리 클럭·인과 정렬 in-proc + 실 Net·sim seam 브리지 in-proc 등가 + 실 run() net.step 배리어 실제 치환)*

**무슨 서버인가**: lockstep 배리어 없이도 결정론을 지키는 *논리 클럭·인과 정렬 기계*(`src/async-core.js` = 추상 substrate) + 그걸 **실 engine Net 메시지·동결 sim seam 에 잇는 브리지**(`src/async-net.js`·0441~). 오늘 결정론은 `net.step()` 중앙 lockstep 배리어가 떠받친다(모든 참여자 동기 tick·#4). 이 substrate 는 그걸 해제하되 결정론을 지키는 토대 — 메시지가 *물리적으로 다른 순서/손실*로 도착해도 인과(happens-before)를 복원해 *결정론 전순서*로 적용해 수렴한다. *비유 — 회의록 정리*: 여러 사람이 제각기 다른 순서로 말해도(또는 일부 발언이 누락돼 재전송돼도), 누가 무엇에 *이어서* 말했는지(인과)를 복원해 모두가 같은 회의록에 도달한다. **두 박스 모두 `run()` 경로가 호출하지 않는 검증 전용 substrate → run() 비트 불변(매 step reg 구조적 0)**.

**필요한 기능들**:
1. **논리 클럭(Lamport)** ✅ in-proc — 왜: 물리 시계 없이 인과를 측정해야(분산엔 전역 시계 없음). / 어떻게: Lamport(1978) 단조 카운터 — local/send 마다 +1·recv 는 max(local,msg)+1 → clock condition(a→b ⇒ C(a)<C(b)). / 했나: `step-0431` `makeLamportClock`(`async-core.js:17`·`recv:27`)·`step-0432` `lamportExchange`(N site 교환→이벤트 로그+happens-before 간선·`:36`)+`clockConditionViolations`(위반 0).
2. **결정론 전순서** ✅ in-proc — 왜: 인과(부분순서)만으론 *동시* 이벤트의 적용 순서가 안 정해진다(결정론 수렴 불가). / 어떻게: Lamport 전순서 키 (lc, siteIndex) — 동시 이벤트만 site 로 tie-break·clock condition 덕에 인과 존중·*내용의 함수*라 순열 불변. / 했나: `step-0433` `totalOrder`(`:90`)·`totalOrderSound`(strict+causal·`:94`) — 8 물리 순열 전부 같은 전순서.
3. **holdback 재정렬** ✅ in-proc — 왜: 실제론 이벤트가 교차-site 재정렬로 도착(링크별 지연 상이)·전체 집합 없이 안전 방출해야. / 어떻게: low-water-mark 안정성 — lwm=min_s(마지막 도착 lc)·lc≤lwm 만 방출(FIFO 가 더 작은 키 못 옴 보장). / 했나: `step-0434` `makeHoldback`(`:110`) — 인터리빙 불변·close 이전 23~37/40 점진 방출.
4. **인과 의존 배달** ✅ in-proc — 왜: holdback 은 사이트별 FIFO 가정에 기댐·더 강한 FIFO-free 보장 필요. / 어떻게: 각 이벤트 deps(=happens-before 선행) 명시 추적·선행 다 배달 전 보류(DAG→deadlock 0). / 했나: `step-0435` `causalDeliver`(`:142`)+`causalViolations` — 적대적 도착(역순/셔플) 6종 위반 0·stuck 0.
5. **async 수렴 desync 0** ✅ in-proc — 왜: 순서/재정렬/인과를 *상태 수렴*으로 마무리해야(#4 핵심 명제). / 어떻게: 배달열 순차 fold→상태 다이제스트(순서 민감·내용 함수)·두 site 상이 도착 순열→같은 다이제스트=desync 0. / 했나: `step-0436` `applyDigest`(`:172`)·`step-0437` `makeAsyncSite`(receive/tick 분리·`:183`·복제 불균등 속도 skew>0 비-lockstep·페이스 무관 수렴).
6. **손실 하 gap-resync** ✅ in-proc — 왜: 손실 시 holdback FIFO 불변이 깨짐(빠진 lc 건너뜀). / 어떻게: per-source 연속 시퀀스(sseq)·연속분만 holdback 에·hole 감지(gap)·재전송 채움. / 했나: `step-0438` `withSseq`·`makeResyncSite`(`:205`) — ~20% 손실→재전송→전부 배달·desync 0.
7. **인과 회계·exactly-once** ✅ in-proc — 왜: 수렴/복원이 *맞게* 동작하려면 장부가 닫혀야(손실 0·중복 0). / 어떻게: 배달열 vs 전체 집합 대조→emitted/applied/dups/missing/complete·순열+손실 교란에도 다이제스트 불변. / 했나: `step-0439` `accountDelivered`(`:231`)·`step-0440` grand capstone `asynce2ecap`(M 복제 순열+손실→clock0·전복제 desync0·인과존중·exactly-once).
8. **실 Net·sim seam 브리지 (in-proc 등가)** 🟡 in-proc 등가 ✅ (실 net.step 실제 치환 ⬜) — 왜: 1~7 의 substrate 는 *추상 이벤트*(`lamportExchange` 합성 로그)로만 증명됐다 → 실 전송 배리어 치환은 결국 *실 engine Net 메시지*(client→zone intent)가 *실 월드 상태*(동결 sim seam)로 수렴함을 뜻하므로, 그 브리지를 먼저 세워야(DownClient/UpClient 가 in-proc 먼저였듯). / 어떻게(기능마다 다른 이론): 신규 박스 `src/async-net.js` — ⒜ 실 Net-형 intent 스트림 + per-client Lamport 스탬프(`worldIntentStream`·`:21`) ⒝ 동결 `DummySimCore`(engine `ISimCore`)에 배달 순서대로 fold → 실 월드 digest(`simFold`·`:53`·순서 민감·totalOrder 정규화 순열 불변·raw 도착 갈림=substrate load-bearing) ⒞ 스트리밍 holdback 재정렬(`makeZoneMailbox`·`:66`) ⒟ **실 actor.onMsg 디스패치**(net.step 배달 절반 치환·`makeZoneActor`/`deliverToActor`·`:81`/`:91`) ⒠ 손실 sseq gap-resync(`makeZoneResync`·`:98`) ⒡ M 복제 수렴 desync 0(`convergeReplicas`·`:119`) ⒢ 배리어-free 독립 페이스(`driveAsyncReplicas`·`:137`·skew>0) ⒣ exactly-once 회계(`accountReplicas`·`:155`) ⒤ **실 engine Net 배리어 대조**(`runLockstepEngine`·`:175`·`new __c.Net`·register/step) — 배리어(net.step)로 배달하든 배리어-free substrate 로 배달하든 존 실 월드 == canonical(치환 결과 불변). substrate 원시는 async-core 재사용(복제 0). / 했나: `step-0441`~`0450` 각 1기능 + capstone `nete2ecap`(`capstoneReplicas`·`:199`·순열+손실+페이스→배리어==substrate==canonical·전복제 desync0·exactly-once·sound·skew26). **잔여**: 실 `net.step()` 을 *실제* 치환 → 아래 기능 9(0451~0460)가 집행.
9. **실 run() net.step 배리어 실제 치환** 🟡 단일 존 손실/지연 복원 ✅·다중 존 투명 ✅ (완전 async 전환 ⬜) — 왜: 8 은 *검증 전용*(async-net 은 run() 밖·`lamportExchange`/합성 드라이버) — 실 net.step 중앙 lockstep 배리어를 *실제* run() 전송 경로에서 대체해야 진짜 비동기. / 어떻게(기능마다 다른 이론): 신규 박스 `src/async-barrier.js` — run() 이 매 tick `net.step()` 대신 stepper 를 부른다(`topo-run.js:88`/`:102` gated·`opts.asyncBarrier` OFF→net.step() 그대로 = baseline 비트 동일·reg 구조적 0). ⒜ net.step 배달을 verbatim 인라인(`deliverMsg`·`async-barrier.js:44`·dedup/delay/stats/onMsg·같은 순서→world/log 불변) + 월드 입력 per-site Lamport 스탬프(`stamp`·`:41`) ⒝ **정전 순서(발신 순서 m.id) holdback 제자리 슬롯**(`step`·`:59`·world.sort by m.id·제자리 방출→비월드 위치 불변) — **의외 발견(0453)**: 실 존은 *순서 민감*(enter 가 zone.rng() 소비·move 는 entity 존재 요구)이라 임의 재정렬은 월드 파괴 → 배리어는 *발신 순서 재구성*으로 lockstep 재현(재정렬이 load-bearing 아님) ⒞ **move 손실+resync**(`:73`·move 만 확률 드롭·resyncDelay tick 뒤 재enqueue·재드롭 없음 체인 1·past-end 방지) — move 는 위치 가산(가환)이라 늦게 적용해도 최종 동일 → 복원만 하면 world==lockstep(load-bearing) ⒟ 교차-tick 지연 jitter(`:80`) ⒠ exactly-once 회계(`:82`·moveDeliv/moveDup). / 했나: `step-0451` seam(투명)→`0452` 인라인+스탬프→`0453` 정전 순서 holdback→`0454` move 손실+resync(단일 존 world==lockstep)→`0455` 무-resync 대조 발산(resync load-bearing 확증)→`0456` exactly-once(moveDup 0)→`0457` 지연 jitter→`0458` 다중 존(2존·핸드오프 투명·world/log==lockstep)→`0459` 다운스트림 뷰 수렴(desync 0)→`0460` capstone `bare2ecap`(손실+지연 world/뷰==lockstep·exactly-once·다중존 투명). 핵심 명제: `worldDigest(run{asyncBarrier}) == worldDigest(run{})`(lockstep). **잔여**: 다중 존 이주 하 유계 resync(→기능 10)·완전-ON.

10. **다중 존 이주 하 유계 resync (완전 async 전환·0461~0470)** 🟡 다중 존 loss+delay world/뷰==lockstep ✅ (완전-ON 경계 포함 async ⬜·lockstep 등가 불가) — 왜: 9(0451~0460)의 손실/지연 흡수는 *단일 존*(무이주)에서만 world==lockstep 이었다(가환·이주 없음). 다중 존에선 발산한다 — **의외 발견(0461)**: lockstep(무섭동) *자체가* 이주 후 stale 존에 도착한 move 를 폐기(`zone.js` move 핸들러 `if(this.ents.has(avatar))`)하는데, 지연/손실이 이주 타이밍을 바꾸면 이 move-drop 집합이 lockstep 과 달라져 최종 월드가 어긋난다(#72). / 어떻게(기능마다 다른 이론): `src/async-barrier.js` 에 **wrap-aware interior 유계 resync 가드** — barrier 가 소유 존을 peek(`ownerZone`·`:68`·net.actors 훑어 avatar 소유 authoritative 존)해, 엔티티가 자기 region *양 끝*(경계 `region.lo` + wrap 경계 `region.hi-1` 둘 다)에서 `horizon=max(resyncDelay,delayMax)+1`(`:67`) 이상 떨어진(=`interior`·`:70`) move 만 loss/delay 로 흡수한다. 경계 근처 move 는 즉시 in-order 배달(흡수 보류). deferSpan(loss=resyncDelay·delay=1..delayMax) < horizon 이므로 deferred move 는 엔티티가 이주 경계에 닿기 *전* 확실히 재배달(=**이주 전 유계 resync**) → 이주 타이밍 불변 → 다중 존 world/뷰==lockstep. redirect(현재 소유자로 재라우팅) 없이 유계로 exactly-once(lockstep 의 move-drop 집합 보존). / 했나: `step-0461` 발산 포착(negative control·grid24 loss+delay 무-가드→발산·#72 고정)→`0462` ownerZone+interior·loss 게이트(loss-only world==lockstep)→`0463` delay 게이트(delay-only)→`0464` 결합+exactly-once(moveDup0)→`0465` 유계 증명(deferSpan 회계·maxSpan3<horizon4)→`0466` 가드 load-bearing 대조(`mzGuard` 토글·ON 수렴/OFF 발산)→`0467` 이주 전 유계 resync 명제(pendingMoves·handoffsObs·`deferredAcrossHandoff`==0·이주 관측 시 미결 move 0)→`0468` exactly-once 완전 회계(moveDup0·lost0·pendingAtEnd0·moveDeliv360)→`0469` 다운스트림 뷰 수렴(전 클라 AOI==lockstep)→`0470` grand capstone `mze2ecap`(world·once·뷰·유계 4항 5/5·handoff 4~15). `asyncBarrier` OFF→net.step reg 0(barrier ON 경로만 변경). **정직한 한계**: 완전-ON(경계 포함 async)은 lockstep 등가가 불가능 — lockstep 자체가 이주 경계 move-drop 을 타이밍 의존적으로 하므로(0461 발견) interior 유계가 이 모델의 정합 최대치. 잔여=실 host.js child 경계 업스트림(#70).

**지금 어디 / 다음**: in-proc substrate(0431~0440·async-core) + 실 Net·sim seam 브리지 in-proc 등가(0441~0450·async-net) + 실 run() net.step 배리어 실제 치환(0451~0460·async-barrier·단일 존 손실+지연 world/뷰==lockstep) + **다중 존 이주 하 유계 resync(0461~0470·async-barrier wrap-aware interior 가드·다중 존 loss+delay 하 world/뷰==lockstep·exactly-once·deferSpan<horizon·이주 전 재배달·OFF→net.step reg 0)** ✅ 완성. #4 가 0001 이래 열린 채였던 lockstep 의존을 *실 run() 전송 경로*에서 async substrate 로 치환하고 이주가 있는 다중 존까지 결정론 보존. 다음 = 실 host.js child 경계 업스트림(#70)·완전-ON(경계 포함 async·lockstep 등가 불가·별 정합 기준 필요).

---

> **이 계층 다음 걸음**: ⒜ *진짜 비동기*(lockstep 배리어 제거) — **substrate ✅ in-proc(0431~40·`async-core`) + 실 Net·sim seam 브리지 등가 ✅(0441~50·`async-net`) + 실 run() net.step 배리어 실제 치환 ✅(0451~60·`async-barrier`) + 다중 존 이주 하 유계 resync ✅(0461~70·wrap-aware interior 가드·다중 존 loss+delay world/뷰==lockstep·OFF→reg 0)·잔여=완전-ON(경계 포함 async·lockstep 등가 불가)·실 host.js child 업스트림(#70)**. ⒝ 플레이어 프레즌스 실사용. ⒞ 치유(HealService) 분리.
