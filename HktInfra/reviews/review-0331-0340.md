# review 0331–0340 — 다운스트림 전파 sub-arc 1: egress→게이트웨이→클라 라우팅·신뢰성(#9 후속·#60 해소 진행)

> 범위 0331~0340. 인과 서사(무슨 서버·어떤 기능·왜/어떻게/했나)는 [progress/1-edge.md](progress/1-edge.md)(게이트웨이 다운스트림 전파·신뢰성)·[progress/2-world.md](progress/2-world.md)(존 egress)·[progress/5-coordination.md](progress/5-coordination.md)(orch egress 버퍼·재전송·정리 분할). 이 문서는 *정당성 감사*만 — 실물 코드(`src/*.js`)·`run.js`/inline 재현·grep.

## §1 척추 5불변 정합성 (SPINE §5)

- **① 신성한 tick** ✅ — 다운스트림 egress·재전송은 orch `onTick`(제어/전송 평면)·게이트웨이 라우팅은 메시지 처리. 시뮬 tick 안 동기 I/O·await 0. egress 드레인(`orch-views.js` `_drainZoneEgress`)·타임아웃 재전송(`_retransmitStale`)은 onTick 끝 전송만(`orch-control.js:100~102`).
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). dseq 는 세션별 단조 카운터(시드 함수·`zoneEgressSeq`)·5 시드 동일 egress 수(재현: egress 4 == gwRx 4 5종 동일). 손실 주입(`egressDrop`)도 결정론(고정 "sid#dseq" 키).
- **③ 권위 단일 소유** ✅ — 다운스트림은 *읽기전용 뷰* 전파라 업스트림 권위 무영향. 재전송 중복은 게이트웨이 인오더 게이팅이 드롭(`gateway.js` `_recvZoneView`·dseq<exp → return·belief 무오염). leave 정리(0339)는 egress 상태·바인딩 *제거*만(소유 이전 아님).
- **④ 은닉·단일 연결** ✅ — 클라는 게이트웨이만 안다: egress 는 orch→`gateway` 명시 송신(`zoneView` 봉투)·게이트웨이가 세션→클라(`downClients`) 해소해 forward(`gateway.js:_recvZoneView`). 클라는 존/host 무지. ack/resync 는 게이트웨이↔orch 명시 메시지. orch→`zone.js` 직접 import 0 hit.
- **⑤ headless·원격 검증** ✅ — Node 순수·`UObject|CoreUObject|GameplayTags` src 0 hit. 모든 신규(zoneEgress·egressDrop·egressTimeout) OFF→reg 0·spine ALL OK. zoneView frame 직렬화 가능(0325 와이어 경계 재사용·소켓 준비). **#60 진전**: 게이트웨이→클라 *전파* 가 0333(수신)·0334(라우팅)으로 연결(실 Client 수신은 0342+·다음 묶음).

**메타(구현 품질)**: reg 0 규율 유지(매 step OFF→직전 비트 동일). **#58 해소(0332)**: `orchestrator.js` 생성자의 브리지/데이터평면/host컨테이너/egress 필드 대입 블록(0272~0331)을 전용 `orch-bridge-init.js` `_initBridgeFields(opts)` 로 이동(`Object.assign(Orchestrator.prototype, OrchBridgeInit)`·`orchestrator.js:42,175`)→**orchestrator.js 30.5KB→22.8KB**·orch-bridge-init 13.1KB·>30KB 박스 0(최대 orch-zonebridge 28.4KB·gateway 28.2KB).

## §2 검증 재현 (주장 아님 — 실행 인용)

- `node run.js spine` = **ALL OK**(전 시리즈 src 누적 회귀·exit 0) · `node src/verify.js reg` = ALL OK.
- **grep 감사**: `Math.random` src 0 · `UObject|...` src 0 · orch→`zone.js` 직접 import 0 · >30KB 박스 0(`wc -c src/*.js` 최대 orch-zonebridge 28.4KB).
- **inline 재현**(현재 `src/` 박스 메서드 직접 구동·시드 [42,7,1234,99,2026]):
  - 0331/0333 egress→수신: `zoneEgressCount` **4** == `gatewayDownstreamCount` 4(존 산출 뷰가 게이트웨이까지·무손실 송출).
  - 0336 ack 가지치기: `zoneEgressPruned` **4** == egressed·`zoneEgressBufLen('s:a1')` **0**(전부 ack·버퍼 자기-크기조정 0).
  - 0337 손실 복구: `s:a1#2` 손실(`zoneEgressDropped` **1**)→`gatewayResyncsSent` **1**→`zoneResent` **3**→인오더 복구(downSeqNext==egressed).
  - 0334 라우팅: routed==rx·drop 0·세션→클라 바인딩 정확(s:a1→dc0·s:a2→dc1).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #58 | `orchestrator.js` 코어 29.7KB — 30KB 근접 | 정리 | ✅ **해소(0332)** — 생성자 0272~0331 필드 init 블록을 전용 `orch-bridge-init.js _initBridgeFields` 로 이동(투명 분할·reg 0). orchestrator.js 30.5KB→**22.8KB**·세 박스 모두 <30KB | (해소·원장서 떨굼·기록만) |
| #60 | 다운스트림 뷰 게이트웨이→실 클라 *전파* 미연결 | ②/⑤ | 🟡 **해소 진행(절반)** — 0331 egress(존 버퍼→게이트웨이 송출)·0333 게이트웨이 수신(세션 버퍼)·0334 세션→클라 라우팅(`downClients`)으로 *전파 경로* 연결. 단 라우팅 종단이 spectator addr(비-actor) — *실 Client actor* 수신은 0342+(다음 묶음·0346 capstone) | **STATE §3 OPEN GAPS**(다음 묶음서 완결) → 게이트: 실 DownClient 액터 수신·desync 0(0342~) |
| #57 | zoneHosts=orch 인프로세스 논리 컨테이너(실 host.js OS 프로세스 아님) | ⑤ | 🔴 **유지·최상위 load-bearing** — 이 묶음(다운스트림 전파)은 #57 과 직교·미진전 | **STATE §2 NEXT**(불변) → 게이트: zone 런타임·egress 를 실 `host.js` 프로세스/소켓·`cluster-run.js` 실 spawn 통합 |
| #53 | 정합 술어 by-construction(분기 부재) | 검증 | 🟡 **부분 진짜화** — 0337 손실(`egressDrop`)·0338 마지막 frame 손실이 *진짜 유실 분기* 도입 → gap-resync/타임아웃 재전송이 *실제 복구 경로*(by-construction 아님). 단 정착(downstreamSettled·0341)·격리(0340)는 여전히 구조 정합 | **STATE §3 OPEN GAPS** → 게이트: #57 실 소켓 지연·재정렬이 더 강한 분기 도입 시 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | 검증 | 🟡 **확장(+10 모드)** — egress·gwdown·gwroute·gwseq·gwack·gwloss·gwtimeout·gwleave·gwiso·bridgesplit 미승급(현 spine 모드=최신 step 1개·나머지 git per-commit) | **STATE §3 OPEN GAPS** → 게이트: 안정 호 묶음별 스모크 1~2 모드 점진 승급 |

> **🎯 단계 평결(SKILL §3.6)**: **다운스트림 *전파* sub-arc 개시(0331~0340)** — 0319~0330 이 *포착·검증*까지 닫은 다운스트림 뷰를 이제 *전파*(존 egress→게이트웨이 수신→세션→클라 라우팅)로 잇는다(SPINE §4 경로2 의 *마지막 홉* #60 을 절반 해소). 신뢰성(per-세션 dseq·ack 자기-크기조정·gap-resync 재전송·타임아웃 재전송·leave 정리·격리)은 *기존 netcode 패턴의 다운스트림 판*(0008 ack/NAK·0040 bus ack·0058 recoverRetry·0042 seenBound)이라 과심화 아님 — 미배선이던 절반을 세우는 **새 능력**. 0332 는 정리 분할(#58 해소·기능 0). progress 맵 ⬜/🌱 박스 0 유지. **다음(이 묶음 잔여)**: #60 의 *실 Client 수신*(0341~0350 묶음·DownClient 액터·desync 0 수렴)·그 위 최상위 load-bearing 은 여전히 #57(실 host.js OS 프로세스). step-loop 이 원장을 읽어 STATE §2 로 승급.
