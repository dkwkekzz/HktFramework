# review 0231–0240 — #16 spine 승급 라운드 (3차 bespoke 모드 누적 회귀화)

> 범위: step-0231~0240 (정리·도구 라운드 — 3차 균형 라운드 0221~0230 의 bespoke verify 모드 10개를 `engine/verify-kit.js` 누적 키트로 승급). 인과 서사·서버 기능 진행은 [progress/](progress/) — 이 문서는 정당성 감사만.
>
> 이 묶음은 *인프라 박스를 세우지 않는다* — 검증 하니스(verify-kit)만 강화한다. 따라서 감사 초점은 ⒜ 박스 코드 비-침습(reg 0 구조적) ⒝ 승급이 실제로 누적 단언을 더했나 ⒞ #16 의 *얼마가* 닫혔나(정직한 범위).

## §1 척추 5불변 정합성

이 라운드는 박스 `.js` 0줄 수정(아래 §2 git diff 알리바이) — 5불변은 *코드가 안 바뀌었으므로* 직전 묶음(0221~0230) 판정을 그대로 승계한다. 하니스 관점 메타 판정:

- **① 신성한 tick — ✅**: 승급 모드는 `run()` 결과만 단언(시뮬 외)·박스 onTick 무변. worldwb 모드는 write-behind 가 매 intent 디스크 안 때림을 *단언*(tick 보호를 회귀로 고정). 알리바이 `engine/verify-kit.js:803`(worldwb 함수).
- **② 결정론 코어 — ✅**: 승급 모드 전부 시드 [42,7,1234,99,2026] 5/5 동일 수치(§2)·`Math.random` 0 hit(src). 알리바이 `engine/verify-kit.js:703~`(승급 블록 시작 instanceleave).
- **③ 권위 단일 소유 — ✅(모델)·🔴 실배선 #51 유지**: instanceleave(release)·placerebalance/placedrain(release+acquire 쌍)·loginabandon(슬롯 release) 의 권위 보존을 *모델 수준*으로 누적 단언. 단 오케 배치는 여전히 advisory paper map(#51·placedrain/rebalance 가 `orchestrator.js` placement 맵만 갱신)·이 라운드는 그 단언을 회귀화했을 뿐 실배선 진전 0.
- **④ 은닉·단일 연결 — ✅**: 변동 없음(하니스 내부).
- **⑤ headless·원격 검증 — ✅ 강화**: 이 라운드의 *본질적 가치* — 0221~0230 의 양성 단언이 생성 step 한정(단일 src 모델상 다음 step 의 verify.js 로 교체돼 재실행 불가)에서 **매 `node run.js`/`spine` 항구 단언**으로. 알리바이 `engine/verify-kit.js:875`(MODES)·`:876`(ORDER 에 10모드)·`src/verify.js`(순수 셸로 정리·bespoke 0).

**메타(회귀 규율·박스 유계)**: 박스 `.js` diff 0(§2)이라 reg 0 은 *구조적 자명*(net-core+박스 = baseline 비트 동일·플래그 토글조차 불요). verify.js 가 처음으로 bespoke 0 인 순수 셸이 됨(승급으로 라이브 가설 흡수). verify-kit.js 178줄 증가(10 모드)이나 엔진 파일이라 30KB 박스 예산 무관.

## §2 검증 재현

- **`node run.js spine` → 결과: ALL OK** — reg(step-0027 비트 동일) + 승급 10모드 + classics 전부. "전 역사 불변 + 0221~0230 양성 의미가 현재 src 에서 안 깨짐"의 증거.
- **승급 10모드 5/5 OK**(`node run.js` 전체 인용): instanceleave(d1 occ 1·left 1·miss 1)·instancereap(active 1·reaped 3)·placerebalance(A1/B1/C1·moves 2)·placedrain(A 0·moves 2)·cachecapacity(size 2·evic 1)·cachetouch(k1 생존·touches 1)·worldwb(로그 2·버퍼 1)·worldfsync(durSeq 3·dur 3·full 5)·loginauth(authed 2·rejects 1)·loginabandon(큐 [p1,p3]·aband 1·miss 1). loginabandon `all` 에서 정확히 1회(중복 0).
- **git diff 알리바이**(`git diff --stat e09ef2c..HEAD`): `src/*.js` 박스(verify.js 제외) **0 변경** · `engine/verify-kit.js` +178 · `src/verify.js` −36(셸화). 박스 비-침습 입증.
- **grep 감사**: `Math.random` src 0 hit(②)·`UObject`/`CoreUObject`/`GameplayTags` src 0 hit(⑤).
- **#33 재현**: STATE.md 23.8KB(< 30KB) — step-loop 이 0107~0200 개별 INDEX 행을 reviews/ 묶음 행으로 fold(원장 #33 권고 그대로 집행)·30KB 천장 압박 해소.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #16 | bespoke 가설 모드 누적 회귀 미승급 | ②/⑤/정리 | 🟡 **부분 해소** — 3차 10모드(0221~0230) ✅ verify-kit 승급(이 라운드). **잔여**: 0033~0220 의 버스/lease/heal/프레즌스/라우팅/거래소/우편/길드/금고/1차/2차 bespoke 호 *수백 모드* 미승급(단일 src 모델상 각 step verify.js 교체로 재실행 불가·git 복구 필요) | STATE §3 OPEN GAPS — 게이트: 안정 호를 묶음별 스모크 1~2 모드로 verify-kit 점진 승급(끄면 현 spine=회귀 0) |
| #33 | STATE §7 INDEX 30KB 천장 | 정리/도구 | ✅ **해소**(이 라운드 step-loop) — 0107~0200 개별 행 → reviews/ 묶음 링크 행 fold(23.8KB)·원장 권고 집행. 0201~0240 1행/step 은 다음 천장까지 여유 | 해소 기록만(잔여 압박 시 0201~ 묶음 fold 동일 기법) |
| #49 | wiring 박스 30KB 초과 | 정리 | 🔴 **유지·미진전** — 이 라운드는 하니스만 건드려 topo-build(31.5)·topo-run(35.9)·svc-exchange-core(30.7KB) 그대로. *다음 너비/멀티프로세스 배선 전 분할 게이트* 여전 | STATE §2 NEXT(정리성·배선 전 게이트) — 게이트: topo-inject 추출 + close-step src size(#43) |
| #51 | 오케 배치 SSOT 실 존 미배선(paper map) | ⑤/③ | 🔴 **유지·미진전(load-bearing 0순위)** — 이 라운드가 placerebalance/placedrain 을 *회귀화*했으나 여전 advisory(실 존 핸드오프 0). 0221~0230 verdict 의 0순위가 또 미집행(step-loop 이 정리·도구 #16 을 먼저 택) | STATE §2 NEXT(0순위) — 게이트: place*→실 존 lifecycle 트리거 |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 유지 — 이 라운드 host.js 무관(하니스만) | STATE §3 — 게이트: 멀티프로세스 핸드셰이크 검증 |

> **신규 이슈 0**(박스 코드 무변). 그 외 원장 이슈(#1·#3·#4·#46·#48·#44·#47·#45·#43·#41·#39·#50·#37·#38·#36·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#7·#8·#6) 전부 코드 미접촉 → 상태 불변 유지.
