# review 0171–0180 — 우편 saga liveness arc 완성 + svc-mail 정리 2회

> 범위: step-0171~0180(닫힘). 인과 서사·서버별 기능 리스트는 [progress/3-services.md](progress/3-services.md) "우편(Mail) 서버" 기능 #11. 이 문서는 *정당성 감사*만 — 척추 5불변·검증 재현·이슈 전파.
>
> 한 줄: 거래소↔가방 saga liveness arc(0131~0140)의 *완전한 우편 판*(주기 재전송→상한→포기→재admission→2단 유계→발행 3종→liveness capstone) + 박스 예산 재초과(#34d)를 정리 2회(영속 부품 분할 0171·헤더 압축 0175)로 닫음.

## §1 척추 5불변 정합성

| # | 불변 | 판정 | 코드 알리바이 + 근거 |
|---|------|------|------|
| ① | 신성한 tick | ✅ | 우편은 존 tick 밖 순수 반응형 박스(`onTick` 없음). autoRetry 는 외부 주기 op(`mailSweep`)에 피기백(`src/svc-mail-txn.js:71` — ttl 체크 *앞*·TTL 회수와 직교)·readmit 은 외부 op(`:25`). 시뮬 tick 내 동기 I/O·재전송 0. |
| ② | 결정론 코어 | ✅ | 세계 쓰기 경로 무변경(재전송은 가방 give·`sagaDedup` 멱등으로 재실행 0). grep `Math.random` src/·engine/ → **신규 0**(기존 `engine/panel-kit.js:29` 위젯 id 만·#8·헤드리스 경로 밖). 테스트 손실 seam `ackDropAlways`(지속 손실)는 시드 무관 결정론(특정 gid Set). |
| ③ | 권위 단일 소유 | ✅강화 | 가방이 아이템 원장 권위·우편은 give *요청*만(`_custody`). 포기(0173)·영구 실패(0178)도 **abort 아님** — give 가 실제 성공했을 수 있어(가방 dedup→escrow 소유) `pending`(Set)에 미해결로 *남긴다*(`src/svc-mail-core.js:104,108`). 0180 capstone 이 `pending==pendingGive+abandonedGive+permFailed`(미해결 give 3분할·공백/중복 0·`:162`)로 단일성을 단언. release+acquire 쌍 거래(가방 give↔escrow) 불침. |
| ④ | 은닉·단일 연결 | ✅ | saga 발행 3종(saga_abandoned 0174·saga_readmitted 0177·saga_failed 0179)이 버스 경유 audit 구독(`src/topo-subs.js`·발행자 무수정·decouple). 클라 와이어 무변경(saga 는 서비스간 경계). |
| ⑤ | headless·원격 검증 | ✅(부분 #9) | grep `UObject`/`CoreUObject`/`GameplayTags` src·engine → **0**. 전 step Node·5시드 재현. **단** 0172~0180 saga liveness 전부 host.js 0(인프로세스) — autoRetry=`mailSweep` run-loop seam·readmit=`mailReadmit` op seam·지속 손실=`ackDropAlways` 인프로세스 모의·crash/reconstruct post-run(#9 확장). |

**메타(회귀 0 규율·박스 유계)**: ⒜ 전 step `reg` ALL OK — OFF 플래그(mailAutoRetry/mailMaxRetries/mailReadmitMax/mailAbandonPublish/mailReadmitPublish/mailFailPublish)·미호출 accessor(sagaLivenessConsistent) 끄면 직전 비트 동일. ⒝ **박스 예산 회복**: #34d(svc-mail-core 34.8KB) 를 *두 정리*로 닫음 — 0171 영속 부품(_snapState/_restore/_journal/crash/reconstruct)을 `svc-mail-persist.js` 추출(Object.assign 프로토타입·digest 비트 동일·거래소 0124·가방 svc-inventory-persist 0053 패턴), 0175 누적 per-step 역사 주석(17KB=파일 절반·33줄→**5줄**·각 step-NNNN.md 가 SSOT)을 압축 인덱스로 갈음(코드 0 변경). saga liveness(0172~0180)가 18.6→**25.3KB** 키웠으나 30KB 유계 유지. *0175 는 새 정리 기법(주석 압축·STATE §1~6 압축의 소스 판)·reg-0 안전.*

## §2 검증 재현 (직접 실행·인용)

- `node run.js spine`(src 누적 회귀·전 역사 불변을 현재 코드에 단언) → **`결과: ALL OK`**. "0001~0180 어느 불변도 현재 코드서 안 깨졌다"의 증거.
- `node src/verify.js reg`(전 시스템 cur==prev 비트) → **`결과: ALL OK`**.
- `node src/verify.js mailliveness`(0180 capstone) → 5시드 **네 체제 분할 4/4**: A 정상 `0=0+0+0`·B 재전송중 `1=1+0+0`·C abandon대기 `1=0+1+0`·D 영구종결 `1=0+0+1`·sagaLivenessConsistent 예(4/4). `결과: ALL OK`.
- 알리바이 grep: #42 해소(`svc-mail-txn.js:71` autoRetry sweep 피기백)·maxRetries cap(`svc-mail-core.js:104`)·readmitMax→permFailed(`:107`)·_readmit(`:125`)·capstone(`:162`)·#16 미승격(`engine/verify-kit.js` 우편 saga 모드 grep **0**).
- **알리바이(리뷰 무침습)**: `git status` → `src/`·`engine/`·`STATE.md`·`step-NNNN.md` diff **0**(reviews/ 만 변경). 회귀 사슬 불변.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|------|----|------|------|
| #34d | svc-mail-core 34.8KB>30KB(saga 재성장) | 정리 | ✅ **해소** | **0171** 영속 부품 분할(svc-mail-persist.js)+**0175** 헤더 압축 → core 25.3KB<30KB. 잔여 *도구 갭*(close-step src size 미체크)은 #43 으로 분리 |
| #42 | 우편 saga 재전송 수동(mailRetry 1회·주기 없음) | 서비스 | ✅ **해소** | **0172** mailAutoRetry(`svc-mail-txn.js:71`·mailSweep 피기백·거래소 0129 우편 판) → 주기 재전송. 잔여 *나이 무관 무차별*(#37 거래소 본체와 동형)은 우편도 상속 |
| #43 | close-step 이 src 박스 size 미체크(#34/#34d 재발) | 정리/도구 | 🔴 신규 | `engine/close-step.js:39` 가 STATE.md(30KB)·step.md(8KB)만 체크·src 박스 size 0 → #34/#34c/#34d 가 *수동 vigilance* 로만 잡힘. **목적지 STATE §3 OPEN GAPS**(도구성) → 게이트: close-step 에 src/*.js 박스 ≤30KB 체크 추가(#36 baseline-freshness 와 같은 close-step 강화 cluster·가독/안전만·회귀 무관) |
| #41 | 테스트 seam 이 production wiring(topo-build) | ④/정리 | 🟡 확장 | `ackDropAlways`(지속 손실 seam)도 `src/topo-build.js:241` 에 추가(mailAckDrop 0161-0170 에 이어 둘째)·gated(null=무효·reg 0). **목적지 STATE §3** → 게이트: topo-run inject 경로 이동(끄면 현 거동=회귀 0) |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 확장(+8) | 0172~0180 이 mailauto·mailmax·mailabandon·mailreadmit·mailreadmitpub·mailreadmitmax·mailfail·mailliveness + 정리 모드 mailsplit·mailhdr 추가(verify-kit grep 0). 단일 src 모델상 0172~0179 모드는 0180 으로 교체돼 재실행 불가. **목적지 STATE §3** → 게이트: 안정 호(우편 saga liveness) verify-kit 승격(스모크 1~2 모드·끄면 spine=회귀 0) |
| #33 | STATE §7 INDEX 30KB 천장 — 매 step 트림 강제 | 정리/도구 | 🟡 심화(최악) | 이번 묶음 **0172·0173·0174·0177·0178·0179·0180 거의 매 step** 천장 충돌(§5 6계층 행 1~6 전부 압축·§3 OPEN GAPS 5행·§2 build-infra·§6 압축). 누적 함정 정점. **목적지 STATE §3 OPEN GAPS**(도구성) → 게이트: INDEX 묶음당 1행 압축(전문 reviews/) 또는 예산 상향(가독성만·회귀 무관) |
| #9 | host.js 0(멀티프로세스 패리티) | ③/⑤ | 🟡 확장 | +0172~0180 우편 saga liveness 전부 인프로세스(autoRetry/readmit/지속손실/crash·reconstruct seam). **목적지 STATE §3** → 게이트: 멀티프로세스 saga 핸드셰이크+회신 손실(현 FIFO·인프로세스라 미발현=회귀 0) |

> **유지(이전 묶음·미해소)**: #37(saga 무차별 재전송·우편도 상속)·#39(MailFeed 무압축 전제)·#30b·#32·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#34c — 이번 묶음 미접촉. 전파는 README 원장.
