# review 0411–0420 — #62 코드 합류: 코디네이터를 runMulti zone-cluster 복원력의 단일 진입점으로

> 범위: step-0411~0420(코디네이션). 인과 서사·서버 기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md) 기능 21. 이 문서는 *정당성 감사*만(narrative 금지).

## §1 척추 정합성 (SPINE §5 5불변)

0401~0410 이 `cluster-coord.js`(ClusterCoordinator)에 쌓은 runMulti 복원력 *superset* 을, 이번 묶음이 `cluster-run.js` 소유 **단일 진입점**으로 묶어 옛 `runMulti` 가 코디네이터를 *호출*(delegate)하게 했다. 전부 신규 함수·OFF-게이트(`opts.viaCoord`)·run() 데이터 평면 미수정.

- **① 신성한 tick** ✅ — 코드 합류 함수(coordSetup/coordScenarioFromOpts/runMultiViaCoord/coordAuthEquiv)는 in-proc 권위를 *읽어* 실 cluster 를 수렴시키는 배선·번역일 뿐, 존 tick 안에 시뮬 외 작업 주입 0. 알리바이: `src/cluster-run.js:314` runMultiViaCoord(run()=권위 산출·읽기).
- **② 결정론 코어** ✅ — `Math.random` src 0 hit(grep). 시나리오는 선언적 `opts.coordSc`→`runScenario` 번역(순수 함수 `src/cluster-run.js:297`)·시드 PRNG 만.
- **③ 권위 단일 소유** ✅강화 — warm failover 의 kill→promote 를 *같은 onTick*(원자)으로 묶어 desync 윈도 0(`src/cluster-coord.js:127` runScenario promote·kill 동일 at). promoteStandby 는 placement→standby·미러 해제(release+acquire)로 이중 소유 0. `coordmergecap` 5/5 a1 보존·promotions 1.
- **④ 은닉·단일 연결** ✅강함 — `cluster-coord.js` require 0(순수 주입)·orch→zone.js 직접 import 0(grep). 코디네이터는 driver/cluster 주입으로만 실 host 접근.
- **⑤ headless·원격 검증** ✅ — 실 `host.js` child_process E2E(`coordmergecap` migrate+reprovision+kill+promote 실 프로세스). `UObject|CoreUObject|GameplayTags` src 0 hit.

**메타(회귀 규율·박스 유계)**: reg 0 — 신규 함수 미부착/OFF-게이트라 run() 비트 동일·`runMulti` 위임은 `viaCoord` 미설정 시 옛 전 토폴로지 lockstep 경로 그대로(`src/cluster-run.js:30`). 박스 >30KB 0개(최대 cluster-coord 29.3KB·cluster-run 26.9KB·gateway 27.6KB). cluster-coord 는 0416 promote 1줄+헤더만 추가(29.3KB·30KB 예산 내).

## §2 검증 재현 (직접 실행·수치 인용)

- `node run.js 0420 reg` → **ALL OK** (persist+rel+rst 등 6구성×5시드 logHash 비트 동일·"persistReplicas 미제공이면 0027 비트 동일").
- `node run.js 0420 coordmergecap` → **5/5 OK**: 종합 warm-failover(migrate z3→hostB@2·reprovision z1@hostA_s@3·kill hostA@4·promote z1@4) 후 coherent(runMultiCoherent) Y·mig 1·reprov 1·promo 1·a1 보존 Y·parity Y(전 시드).
- `node run.js spine` (백그라운드) → **ALL OK** (전 시리즈 누적 회귀·repro 멀티 2회 동일·인프로세스 동일·world 동일).
- grep 감사: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · `cluster-coord.js` require 0 · orch→zone.js import 0 · >30KB 박스 0.
- 단계별(자기 step 재현, 닫을 때 인용): coordsetup 5/5(placement==plan·maxDesync0)·coordscenfromopts 5/5(순수번역 Y)·runviacoord 5/5(standby a1 {8,8})·coordinfoparity 5/5(키 14종)·coordvsauth 5/5(match 2/2)·coorddelegpromote 5/5(promo1·z1@hostA_s)·coorddelegfence 5/5(epoch1·fencedTicks3)·coorddelegrestart 5/5(restarts1)·coorddelegate 5/5(coord shape·coherent Y).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #62 | 코디네이터↔cluster-run.js runMulti 코드 합류 | ⑤/검증 | ✅ **해소(0419~0420)** | `runMulti(opts.viaCoord)→runMultiViaCoord` 위임(`src/cluster-run.js:30`)·OFF→옛 경로 비트 동일(reg/e2e ALL OK). 원장서 제거(기록만). |
| #69 | 동치가 transitive(직접 비트 대조 아님·다른 substrate) | 검증 | 🟡 신규(경미·#53 family) | STATE §3 OPEN GAPS → coordAuthEquiv(`:337`)는 실 cluster==in-proc 권위를 단언(공유 기준 귀착 ⇒ ≡runMulti)·옛 runMulti(in-proc actor)와 runMultiViaCoord(실 host.js child)를 *직접* 비트 대조 않음(substrate 상이·불가). 게이트: 동일 substrate 비교 경로 도입 시 직접 대조(끄면 transitive=회귀 0) |
| #68 | orch zoneHost *entity 컨테이너* 미동기(where-view 만) | ③/정리 | 🟡 유지(경미) | STATE §3 — 이번 묶음 미접촉(#62 합류에 집중). where 권위는 합류됨·entity 정합은 coordDesync 보장 |
| #61 | 업스트림 intent 실 클라 경로 미연결 | ②/⑤ | 🟡 유지 | STATE §3(무르익으면 §2) — 다운스트림만 실 클라·업스트림 합성 주입 |
| #4 | broker lockstep = 강제 동기(진짜 비동기 미해제) | ① | 🟡 유지(load-bearing) | STATE §2 — 논리/벡터 클럭·인과 순서 |

> **범위 기록(이슈 아님)**: #62 는 *zone-cluster 복원력*의 단일 진입점화로 정의됨 — 옛 runMulti 의 *전 토폴로지*(gateway/services/partition·bus·inventory·chat) 복원력 흡수는 #62 범위 밖(코디네이터=zone-cluster 전용). 위임은 `deps.run`/`zoneSpecOf` 주입 시 동작(공개 `NET.runMulti` deps 엔 부재→zone-cluster 전용·verify 직접 주입). step-0420 정직한 한계에 기록.

> **🎯 단계 평결 (0411-0420·SKILL §3.6)**: **#62 코드 합류 — 코디네이터를 runMulti zone-cluster 복원력의 단일 진입점으로(0411~0420·직전 0401-0410 verdict 가 STATE §2 로 올린 load-bearing #62 마무리)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *verdict 가 지목한 #62 플립* → **과심화 아님**(코디네이션 기능 21). 0401~0410 이 능력 superset 을 쌓았고, 이번 묶음이 그것을 cluster-run.js 단일 진입점(coordSetup 0411·coordScenarioFromOpts 0412·runMultiViaCoord 0413·clusterInfo parity 0414·equivalence 0415·warm-failover/fence/restart 번역 0416~0418·**runMulti OFF-게이트 위임 0419**·grand capstone 0420)으로 묶어 옛 runMulti 가 코디네이터를 *호출*하게 합류. **재현 증거**: `coordmergecap` 5/5(runMultiCoherent Y·mig/reprov/promo 1·a1 보존·parity)·`coorddelegate` 5/5(coord shape·coherent Y)·`reg`/`e2e`/`spine` ALL OK(OFF 경로 보존)·`Math.random`/UObject src 0·cluster-coord require 0·orch→zone.js import 0·>30KB 박스 0(29.3KB). **#62 ✅해소**·#57 잔여(=#62)도 동반 닫힘. **다음 권고: 업스트림 intent 실 클라(#61·양방향 실 E2E)·진짜 비동기(#4·논리 클럭)·#68 entity 컨테이너 동기(경미 정리).** step-loop 이 STATE §2 로 승급.
