# 묶음 감사 review-0141-0150 — 정리(topology 분할) + 우편(Mail) 서비스 arc

> 범위: 닫힌 step 0141~0150. 인과 서사(무슨 서버·왜·어떻게)는 [progress/3-services.md](progress/3-services.md) 우편 서버 항목. 이 문서는 *그 인과가 척추에 정당한가*만 점적 판정한다(narrative 요약표 없음 — §0049 단일 src·알리바이는 현재 `../src/*.js`).
>
> 한 줄: 0141 = 정리(`topology.js` run 드라이버 → `topo-run.js`·기능 0·reg 0). 0142~0150 = 게임 서비스 계층 *우편* 전용 박스(SPINE §2 ⬜→🟡) — 거래소 arc(0107~0140)와 동형 골격(입금→수령→발행 3종→영속·압축→만료 TTL→회계 정합 capstone).

## §1. 척추 5불변 정합성

| 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|
| ① 신성한 tick | ✅ | 우편 박스는 존 tick 밖·`onTick` 없음. `grep -nE "await|fs\.|fetch\(" src/svc-mail.js` = **0 hit**. mailSweep 만료(`svc-mail.js:121`)는 *시간 트리거 주입*(`topo-run.js:195` mailOps seam·net.log 밖), tick 내 동기 작업 0. |
| ② 결정론 코어 | ✅ | `grep -rn Math.random src/` = **0 hit**. 우편은 난수 무관 — 입금/수령/만료 모두 정렬 순회(`svc-mail.js:123,182` recipient/id sort)·전 시드 digest 동일(0142~0146 exmail 결정론 열 "예"). 세계 상태 쓰기 경로 불변(우편은 서비스 상태). |
| ③ 권위 단일 소유 | ✅ **강화** | 우편함 쓰기 권위는 MailService 한 곳. **capstone 이 바로 이것을 단언**: `mailConsistent()`(`svc-mail.js:180`) = `sent==totalHeld+fetched+expired` — 우편 1통은 매 순간 정확히 한 상태(보유/수령/만료·공백·중복 0). 4체제 전부 true(§2). crash→reconstruct 후에도 불변(영속 replay 가 분할 보존). |
| ④ 은닉·단일 연결 | ✅ | 발행자 무수정 소비자 패턴: 우편 박스는 svc.mail.{sent,read,expired} 를 *발행만*(`svc-mail.js:97,132`), audit 는 *구독 행 추가만*으로 관측(`topo-subs.js` mail 3행). 우편 박스는 audit 무지. 발행=파생 스트림(비-침습·published==audit.seen·발행 ON/OFF projection 동일·§2). |
| ⑤ headless·원격 검증 | ✅ | `grep -nE "UObject|CoreUObject|GameplayTags" src/svc-mail.js src/topo-run.js` = **0 hit**. 순수 Node. `node run.js`/`node run.js spine` = ALL OK(§2·UE 0). |

**메타 — 회귀 0 규율**: ✅ 견고. 우편 매 step 새 항 OFF(또는 mail OFF) = 직전 비트 동일을 reg 가 매번 단언(0142~0150 reg ALL OK). 0141 정리는 verbatim 이동(기능 0)이라 mail OFF/ON 무관하게 baseline(0140) 비트 동일. mailFetch(0143)·mailSweep(0148)은 *새 op 타입*이라 미주입 시 분기 미진입(후방 호환).

**메타 — 박스 파일 유계 🔴 이슈**: 0141 정리가 `topology.js`(31.5KB) run 드라이버를 `topo-run.js` 로 분리해 topology.js 는 1.4KB(✅)로 슬림화했으나, **`topo-run.js` 가 31.2KB(31991B) > 30KB 예산** — bulk 가 *옮겨졌을 뿐 줄지 않았다*. 0141 문서는 "topo-run.js(~27KB)·박스 유계 복귀"라 적었으나 실측 31.2KB(문서-코드 불일치). `close-step.js` 는 STATE/step 문서 크기만 검사하고 src 박스 크기는 검사 안 해 미탐지. #34 의 (c) leg 로 이월(§3). 그 외 박스 ≤30KB(svc-mail 18.4KB).

## §2. 검증 재현 (직접 실행·인용)

- **`node run.js spine`** (전 역사 누적 회귀·src 150-step) → `결과: ALL OK`. 전 가설 모드(reg·wquorum·e2e·sacred·compact·recover-chat… + 현 exmail capstone)가 현재 코드에서 하나도 안 깨짐.
- **`node run.js 0150`**(현재 step·reg + capstone) → `결과: ALL OK`. reg(0027/0028 비트 동일·mail OFF)·exmail 4체제 전부 통과.
- **exmail capstone(0150)** 5시드 전부 `예(4/4)` — 분할: 수령만 `0/3/0`·만료만 `0/0/3`·혼합 `1/2/1`·crash 복구 정합 `예`:
  ```
  seed  | fetchOnly | expireOnly | mixed | crash복구 | 4체제 mailConsistent
    42  |   0/3/0   |   0/0/3    | 1/2/1 |    예     |   예(4/4)
  ```
  → sent==held+fetched+expired 가 *모든 체제*서 성립(우편 1통 정확히 한 상태·공백·중복 0).
- **grep 감사** (5불변 위반 탐지): `Math.random` src/ **0** · `await|fs\.|fetch` svc-mail **0** · `UObject|CoreUObject|GameplayTags` svc-mail/topo-run **0**. 위반 0.
- **박스 크기**: `wc -c` → svc-mail.js **18374**(✅<30720) · **topo-run.js 31991(🔴>30720)** · topology.js 1376(✅) · topo-build.js 26975(✅).

## §3. 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #34 | wiring 박스 30KB 초과 | 정리 | 🟡 **(c) 신규 leg** | (a) topo-build ✅0133·(b) topology ✅**0141**(run→topo-run·1.4KB). **(c) 잔여: topo-run.js 31.2KB** — 0141 이 bulk 를 옮겼을 뿐 미감소. **STATE §3 OPEN GAPS** → 게이트: per-tick 주입 핸들러(~41개)를 `topo-inject.js` 로 추출(0141 §9 자체 제안·정리 step·기능 0·reg 0). close-step 에 src 박스 freshness/size 체크 추가도 함께(#36 family). |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 **확장(+우편 9 모드)** | 우편 arc 의 ON-경로 의미(수령 무손실·발행 3종·영속 replay·압축·만료)는 각 step verify.js 에서 1회만 단언·다음 step 이 *덮어써* 사라짐(spine 엔 현 capstone 1개만). 거래소 saga(~60)에 우편 9 추가 → ~70 모드 미승격. **STATE §3 OPEN GAPS** → 게이트: 안정 호(우편·saga liveness) 스모크 1~2 모드 verify-kit 승격(끄면 현 spine=회귀 0). |
| #9 | inproc-only·멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장(+우편)** | 우편 박스도 host.js 0 — `mailOps`(`topo-run.js:195`)·`mailSweep` 는 run 루프 seam, crash/reconstruct(`svc-mail.js:140,147`)는 verify 가 post-run 직접 호출(인프로세스만). **STATE §3** → 게이트: 우편 클라 와이어 + 멀티프로세스 배선(현 FIFO·인프로세스라 미발현=회귀 0). |
| #33 | STATE §7 INDEX 30KB 천장 | 정리/도구 | 🟡 **유지(0141~0150 다시 10/10 발현)** | 이번 묶음 매 step 도 close-step 30KB 예산이 옛 INDEX 행 압축 강제(여러 step 은 다회 트림 — 실측). **STATE §3 OPEN GAPS** → 게이트: INDEX 묶음당 1행 압축 또는 예산 상향(가독성만·회귀 무관). |

**경미·watch (원장 미등재 — 여기 기록만)**: ⒜ `accountConsistent`(`:177`)와 `mailConsistent`(`:180`)가 *동일 코드*(이름만 다름) — 0150 §9 가 인정(거래소 0128/0140 명명 관례·아이템 우편 추가 시 2-서비스 보존으로 강화될 자리). 무발현(중복 단언일 뿐). ⒝ 우편은 *메시지/추상 본문* — 아이템 우편(만료 시 발신자 반환)은 거래소↔가방(0117~0120) 2-서비스 보존의 우편 판 필요(#9/서비스 백로그·arc 정합 대기).

**해소 기록**: 이번 묶음은 **#34(b)** 를 해소(0141·topology.js run()→topo-run·topology 1.4KB) — 단 bulk 가 topo-run 으로 이동해 (c) leg 신규(미감소). SPINE §2 게임 서비스 *우편* ⬜ 박스가 🟡 로 섰다(STATE §3 gap·진행 지도 7번째 서비스 박스).
