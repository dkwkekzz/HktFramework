# review 0101–0110 — 수신함 수명주기 완성 · 디스커버리 메아리 펜싱 · 거래소 서비스 arc

> 범위: 닫힌 step 0101~0110. *무엇을 했나*의 박스별 진행은 [progress/](progress/)(3-services·5-coordination·6-data 갱신). 이 문서는 *정합 감사*만 — 척추 5불변 + 재현 + 이슈→후속 연결. 권위: 실물 `../src/*.js`·`../engine/*.js` + `node run.js` 재현(주장 아님).

묶음 골격: **① 수신함 메모리·수명주기 완성**(0099 위 0101 읽음확인·0102 checkout 유계·0103 소비발행·0104 손실발행) · **② 디스커버리 채널 메아리 펜싱**(0105 presmon·0106 wrouter epoch) · **③ 거래소 게임 서비스 arc**(0107 분리·0108 발행·0109 영속·0110 압축 — 가방/파티와 같은 궤적).

## §1 척추 정합성 (SPINE §5 5불변)

| 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|
| ① 신성한 tick | ✅ | 거래소·수신함·프레즌스 전부 `onMsg`/내부 메서드만(onTick 0·발신 0 또는 버스 pub). 거래소 `crash()/reconstruct()` 는 박스 내부 동기(`src/svc-exchange.js:82,89`). `grep "await |fs\.|readFileSync" src/` = cluster 하니스 제어평면(`cluster-core.js`·`cluster-run.js`)·verify CLI 뿐 — 박스 tick 경로 0. |
| ② 결정론 코어 | ✅(🟡 단일클럭) | `grep Math.random src/` = **0 hit**. 거래소 listingId 단조(`nextId++`·`src/svc-exchange.js:53`)·reconstruct seq 순(`:90`). **🟡 긴장**: 공지 epoch = `promotedAt`(tick·`src/svc-presence.js:70`) — 단일 클럭 가정(0105/0106 §9). 게이트(announceEpoch=0)면 미발현이나 진짜 분산 동시 승격엔 논리 클럭 term 필요 → #29(#4 family). |
| ③ 권위 단일 소유 | ✅ **강화** | 거래소 escrow = 단일 쓰기 권위·이동은 쌍 거래: list=`listings.set`(acquire·`:54`)·buy/cancel=`listings.delete`(release·`:63,75`). 이중 판매/해결 가드: `if(!l){rejects++;return}`(`:62`)·`if(!l||l.seller!==p.seller)`(`:74`). 보존 `listed==open+sold+cancelled`(`conserved():97`)·pexsnap 전 시드 true. 가방 1-당사자 패턴을 2-당사자 교환으로 확장. |
| ④ 은닉·단일 연결 | ✅(🟡 watch) | 발행 무수정 구독: svc.mailbox.drained/overflowed·svc.exchange.sold → audit(`src/topo-build.js` subs). 디스커버리 메아리 펜싱이 죽은 박스 역-재타깃 차단(presmon `:33`·wrouter `:12` 동일 가드). **🟡**: ⒜ 거래소 거래·수신함 drain 은 topology 훅 주입(`exchangeOps`·`mboxDrain`)이지 클라→게이트웨이 E2E 아님(은닉 미완) ⒝ 펜싱 로직 presmon/wrouter 인라인 중복(0106 §9)→#28. |
| ⑤ headless·원격 검증 | ✅(🟡 #9) | `grep UObject\|CoreUObject\|GameplayTags src/` = **0 hit**·Node 전용. `node run.js spine` 110-step ALL OK. **🟡**: `grep exchange\|mailbox\|presence src/host.js` = **0 hit** → 거래소·수신함·프레즌스 박스 멀티프로세스 미배선(인프로세스 전용). 거래소 crash→reconstruct 는 *post-run 직접 호출*(pexper/pexsnap), 진짜 child.kill 아님 → #9 확장. |

**메타(회귀 0 규율)**: 이번 10 조각 전부 OFF 플래그 + early-return 가드 — `drainAck`/`checkoutBound`/`drainedPublish`/`lossPublish`(`src/svc-mailbox.js:43,47,50,52`)·`announceEpoch`(`svc-presence.js:33`·ev.epoch 가드)·`persist`/`publish`/`snapInterval`(`svc-exchange.js:17,28,31`)·`exchange`(박스 0). reg ALL OK 가 비트 동일 증명. 박스 크기: svc-exchange 새 박스 ~5KB(유계)·svc-mailbox 누적 ≤17KB·전부 ≤30KB 예산 내(정리 step 불요).

## §2 검증 재현 (직접 실행·주장 아님)

- **`node run.js spine`** → **ALL OK** (110-step 누적 회귀 사슬 — 전 역사 불변이 *현재 src 코드*에서 하나도 안 깨짐). seed42 월드해시 `0x7a122947` 보존·멀티 2회·인프로세스·world 전부 OK.
- **`node src/verify.js reg`** → **ALL OK** (전 플래그 OFF=src=baseline 비트 동일).
- **`node src/verify.js pexsnap`** → **ALL OK** (현재 step 0110 가설 — 스냅샷 upToSeq 6·tail 1 vs OFF tail 7·crash→reconstruct 양쪽 무손실).
- **grep 5불변**: `Math.random` 0 · `UObject|CoreUObject|GameplayTags` 0 · 박스 tick 내 `await/fs` 0(하니스/CLI만) · `host.js` 에 exchange/mailbox/presence 0(인프로세스 전용).
- **#16 알리바이**: `engine/verify-kit.js:702-703` ORDER = `reg…reliable·tail·inflight·degrade·inject·isolate·hide·repro`(~0024 에서 멈춤). 0101~0110 의 10 가설 모드(pread/pcobnd/pdrpub/pmloss/pannep/pwannep/pexch/pexpub/pexper/pexsnap)는 *각 step verify.js 셸에서 1회 단언 후 다음 step 이 교체* — 누적 회귀 미승급. spine 은 reg-0 만 보증.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #26 | inbox 드레인 읽음 영수증 | 서비스 | ✅ **해소 0101** | drainAck(2단계 읽음 checkout→ackDrain·재드레인 무손실·ack 시 안전 제거·`src/svc-mailbox.js:58,69`). 잔여(게이트웨이 경유 read E2E)는 #9/④ 은닉으로 흡수 |
| #16 | bespoke 가설 모드 미승격 | ②/⑤/정리 | 🔴 **확장 +10** | 0101~0110 10 모드 추가 미승급(총 ~40). **STATE §3 OPEN GAPS** → 게이트: 안정 호(수신함·거래소) 스모크 1~2 모드 verify-kit 승급(끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장** | +거래소(0107~0110)·수신함(0099~0104)·프레즌스 펜싱(0105~0106) 전부 `host.js` 0·post-run 직접 crash. **STATE §3** → 게이트: 박스 host 배선 + 진짜 child.kill→replay(현 인프로세스 FIFO=회귀 0) |
| #28 | 공지 펜싱 로직 presmon/wrouter 인라인 중복 | ④/정리 | 🟡 신규(경미) | 0106 §9 — 같은 epoch 가드가 `svc-presence-monitor.js:33`·`svc-whisper-handlers.js:12` 두 곳. **STATE §3 OPEN GAPS**(정리성) → 게이트: engine 공통 헬퍼 승급(셋째 소비자 시·끄면 현 인라인=회귀 0) |
| #29 | 공지 epoch=promotedAt 단일 클럭 | ② | 🟡 신규(#4 family) | 0105/0106 §9 — tick 단조 가정, 진짜 분산 동시 승격 시 충돌. **STATE §3** → 게이트: 논리 클럭/term 카운터(#4 비동기 결정론과 같은 무대·끄면 lockstep 현 거동=회귀 0) |
| #30 | 거래소↔가방 미결합·자기 박스 영속 | 서비스/데이터 | 🟡 신규 | 0107 §9 — 거래소 escrow 가 *자기 원장*만(가방 아이템 실제 이동 0)·저널이 거래소 박스 내(별 PersistStore 0). 진짜 존 넘는 거래·분산 영속 미완. **STATE §2/§3** → 게이트: 거래소↔가방 2-서비스 원자 거래 + PersistStore 박스화(끄면 현 단독 보존=회귀 0) |

기존 유지(변화 없음·코드 grep 재확인): #1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27 — 이번 묶음이 안 건드림(거래소·수신함·디스커버리 외 계층 무변경). #29 는 #4 의 디스커버리 발현·별 추적.
