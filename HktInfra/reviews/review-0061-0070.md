# review-0061-0070 — 프레즌스 박스 호: 대체 소비자 → 전용 박스 → failover-safe SSOT

> 범위: 0061~0070(닫힘) · 박스별 진행은 [progress/5-coordination.md](progress/5-coordination.md)·[progress/3-services.md](progress/3-services.md) · 이슈 원장 [README.md](README.md) §2.
> 호: 대체 소비자(0061 spawnReplace·0062 reconstruct) → presmon 관측(0063) → **전용 프레즌스 박스**(0064 분리 → 0065 보고 버스화 → 0066 shadow → 0067 failover 승격 → 0068 사망 자율 감지 → 0069 질의 → 0070 질의 failover). 코드 알리바이는 현재 `src/*.js`(누적)·델타는 git.

## §1 척추 정합성 (SPINE §5 5불변)

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 프레즌스/랭킹 박스 전부 *순수 메모리 반응형* — onTick 내 동기 I/O·`await`·DB 호출 grep 0 hit. presence onTick(`src/svc-presence.js:54`)은 하트비트 pub + 타임아웃 검사뿐. crash/promote/announce 는 틱 루프 deliver *직전* 주입(제어 평면·`src/topology.js`). |
| ② | 결정론 코어 | ✅ | `src/` 전체 `Math.random` **0 hit**. 프레즌스 박스는 난수 미사용(상태 기계·시드 무관). 자율 승격 시점 `promotedAt=34` 5시드 동일(presqcont). |
| ③ | 권위 단일 소유 | ✅ | *발행 권위* 항상 1: 발행 게이트 `this.active && this.bus`(`src/svc-presence.js:51`)·승격 = release(`crash()`:62 dead=true)+acquire(`promote()`:64 active=true). 0061 대체 소비자도 쌍 거래 — standby 가 permanent 신호에 *스스로* svc.item.out 재구독(`src/svc-ranking.js:50-52`·activated 가드). |
| ④ | 은닉·단일 연결 | ✅ | orch 가 보고를 버스 토픽으로(주소 무지·`src/orchestrator.js:53`·presenceAddr=null)·presmon 이 박스를 *공지로 동적 발견*(`src/svc-presence-monitor.js` svc.presence.active 재타깃). 질의는 명시 request/reply(SPINE §4 경로3). 박스 내부 직접 참조 0. |
| ⑤ | headless·원격 검증 | ✅ | UE 모듈(`UObject`·`CoreUObject`·`GameplayTags`·`Json`) grep **0 hit**. 전부 Node·`node run.js` 수치 증명. |

**메타(구현 품질)**: 회귀 0 규율 견고 — 이 호의 모든 플래그(spawnReplace·presenceBox·presenceReportBus·presenceShadow·presencePromote·presenceLease·presenceQuery·presenceAnnounce) OFF → 직전 step 비트 동일(각 step §3·reg). 박스 파일 전부 30KB 예산 내(최대 topo-build 25.6KB·presence 9.4KB·presmon 5.0KB). 0064 가 orch 三역할 중 *프레즌스 SSOT+발행*을 PresenceService 로 떼어 **#13 의 절반을 해소**(아래 §3).

## §2 검증 재현 (실행·인용)

- **`node run.js spine` → 결과: ALL OK**(70-step src 누적 회귀 — reg + 전 역사 가설 모드를 *현재 코드*에 단언·전 불변 무파손).
- **`node run.js`(현재 0070) → ALL OK**(4기둥 + E2E 14프로세스 비트동일).
- **`node src/verify.js presqcont`**(0070·*0066 shadow+0067 promote+0068 lease+0069 query 스택을 전이적으로 행사*) — 5시드 OK: 승격 공지→재타깃(`1/1→presence2`)·죽음 후 질의를 승격 박스가 답함(pri/std repliesSent `2/2`·recv==sent `4/4`)·`queried[ranking]=permanent` vs OFF stale `down`.
- **grep 감사**: `Math.random` src/ 0 · UE 모듈 0 · 프레즌스/랭킹 박스 동기 I/O 0 — 5불변 위반 0.

> **⚠ 재현 커버리지 갭(이슈 #16)**: spine(verify-kit ORDER)의 누적 가설 모드는 `reliable·tail·inflight`(~0024)에서 *멈췄다* — **0033+ 버스/lease/heal/프레즌스 호의 ON-경로 가설은 누적 회귀에 *승격되지 않았다***. spine 은 이들의 **reg-0(OFF=비트동일)만** 보증하고(reg 설정에 presence 0 hit), shadow 복제·failover 승격·자율 감지·질의 같은 *ON-경로 의미*는 **닫을 때 bespoke 모드로 1회** 단언된 뒤 다시 검증되지 않는다(다음 step 이 verify.js 를 덮어씀). 회귀 *예방*은 reg-0 가 받치나, 프레즌스 코드 리팩터가 ON-경로 의미를 깨도 spine 은 못 잡는다.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #13 | orch 三역할 → 전용 박스 분리 | ⑤/정리 | **🟡 절반 해소** | 프레즌스 SSOT+발행 ✅ **0064**(PresenceService 분리·orch⟂프레즌스)+0065(보고 버스화). **잔여**: 치유(recover/retry/giveup) 아직 orch 내 → HealService 분리는 열림(STATE §3·정리 step·기능 0) |
| #14 | 대체 소비자 spawn | ⑤ | **✅ 해소(형태 한정)** | **0061** spawnReplace(standby ranking2 가 svc.presence permanent 에 자기 활성화·`svc-ranking.js:50`)+**0062** reconstruct(다운타임 갭 저널 복원). 단 *사전 등록 standby 활성화*지 *런타임 동적 생성*은 아님(0061 §9) → 그 잔여는 #6 동적 토폴로지로 흡수 |
| #16 | bespoke 가설 모드 누적 회귀 미승격 | ②/⑤/정리 | 🔴 신규 | 0033+ 버스/lease/heal/프레즌스 ON-경로가 verify-kit 누적에 없음(§2 ⚠) → **STATE §3 OPEN GAPS**(정리성). 게이트: 안정 프레즌스/lease 가설을 verify-kit 으로 승격(또는 presence-stack 스모크 1 모드)·끄면 현 spine=회귀 0 |
| #17 | self-hb 메아리 + 재타깃 윈도 질의 손실 | ④/⑤ | 🟡 신규(경미) | 0068: 승격된 active 가 자기 하트비트 되받음(`hbRecv` 부풀음·무해·`topo-build.js:151`)·0070: 공지 전 윈도 질의는 죽은 primary 로 손실(0068/0070 §9) → **STATE §3**. 게이트: active 자기-hb 제외 + 재타깃 윈도 질의 재시도(0058 recoverRetry 의 질의 판·다중 standby/연쇄 failover 기반) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 유지(범위 확장) | **+0061~0070**: 프레즌스 박스 호 전체가 *인프로세스 전용* — `src/host.js` 에 presence/ranking2 박스 0(E2E 멀티프로세스 미참여) → presence failover/query 의 멀티프로세스 검증 0 (STATE §3·기존 게이트) |
| #15 | 적응형 lease 정밀화 | ④ | 🟡 유지 | 이 묶음 미착수(STATE §3·cadence EWMA/마진 jitter) |
| #4·#1·#3·#6·#7·#10·#11 | (이월) | — | 🟡 유지 | 이 묶음 미착수 — README §2 원장 그대로 |
| #8 | `engine/panel-kit.js:29` Math.random | ②/정리 | 🟡 유지 | 여전(시각 위젯 id·헤드리스 경로 밖·다이제스트 무관). STATE §3·시드 카운터 교체 |

> **양방향 재점검(직전 원장)**: #13·#14 가 이 묶음 게이트로 해소/절반해소(0064·0061). #12 는 0051-0060 에서 이미 ✅. 나머지 이월 이슈는 코드 변경 없어 유지.
