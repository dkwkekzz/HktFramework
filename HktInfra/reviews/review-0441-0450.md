# review 0441–0450 — #4 실 net.step 배리어 치환 (async substrate → 실 Net·sim seam in-proc 등가)

> 범위: 0441~0450 = async-core(0431~0440·추상 이벤트)가 증명한 substrate 를 신규 박스 `src/async-net.js`(run() 밖 검증 전용)로 **실 engine Net 메시지 형태**(client→zone intent)·**동결 sim seam(`DummySimCore`)**에 이어, 실 `net.step` 배리어 치환의 *in-proc 등가*를 증명. 인과 서사·기능 리스트는 [progress/5-coordination.md](progress/5-coordination.md)(비동기 substrate 항목·기능 8)·[progress/README.md](progress/README.md). 이 문서는 *정당성 감사*만.

## §1 척추 정합성 (SPINE §5 5불변)

- **① 신성한 tick** ✅ — async-net 은 *시뮬 tick 밖* substrate(net.step 동기 배리어 의존을 *치환할* 기계). run() tick 루프 무변경 → tick 안 동기 작업 0. 알리바이: `src/topo-run.js:92~116`(tick 루프·async-net 호출 0)·async-net 은 `src/net-core.js:29` export 1줄만. runLockstepEngine 이 세우는 `new __c.Net`(`async-net.js:176`)은 *verify 전용 자체 인스턴스*(run() 의 net 아님).
- **② 결정론 코어** ✅ — `Math.random` src 0 hit. 전 substrate 가 시드 PRNG(`mulberry32`)만·simFold 는 동결 `DummySimCore`(engine `ISimCore`·`engine/index.js:183`)로 실 월드 fold(순서 민감·내용 함수). totalOrder 정규화 fold 는 도착 순열 불변(`netsimfold` permInv Y)·raw 도착 fold 는 갈림(rawDiverge Y=substrate load-bearing). 알리바이: `async-net.js:53 simFold`·재사용 `async-core.js:90 totalOrder`.
- **③ 권위 단일 소유** ✅ — exactly-once(발신 intent 정확히 한 번 적용·dups 0·missing 0). 알리바이: `async-net.js:155 accountReplicas`(async-core.accountDelivered 재사용)·`netaccount` complete·dups0·missing0 5/5·존 actor 단일(`makeZoneActor`·`:81`).
- **④ 은닉·단일 연결** ✅ — async-net 은 실 Net 계약(`{from,to,payload}`)만 사용·register/send/step(`:182`/`:184`/`:186`)로 통신·타 박스 내부/DB 직접 접근 0(import 는 `common.js`(PRNG/FNV)+`engine/index.js`(DummySimCore·sim seam)만). 알리바이: `async-net.js:6~8` 진입·다른 src/ 박스 require 0.
- **⑤ headless·원격 검증** ✅ — 순수 JS·UE/UObject src 0 hit·`node run.js 0450 nete2ecap` 으로 수치 증명. 한계(정직): **in-proc 등가** — 실 `net.step` lockstep 배리어를 *아직 치환 안 함*(runLockstepEngine 은 verify 전용 자체 Net·run() 의 net.step 배선 미교체·async-core→async-net 처럼 등가 먼저·후속 arc). substrate 가 run() 밖에 살아 reg 구조적 0 — 실제 치환 시 회귀 게이트 재확인 필요(§3 #4·#71).

**메타(회귀 규율·박스 유계)**: reg 구조적 0 — async-net 은 run() 경로 미호출(grep `worldIntentStream|simFold|makeZone*|convergeReplicas|driveAsyncReplicas|runLockstepEngine|capstoneReplicas` in topo-run/build/actors/topology/gateway/zone = 0 hit)→run() 비트 불변(`reg` ALL OK 전 step). 박스 >30KB 0개(async-net 최종 17.4KB). 코드 델타는 0441~0449 async-net.js 증분(각 step 1 함수)+net-core.js export 1줄·0450 은 capstoneReplicas 1함수+verify.js capstone.

## §2 검증 재현 (직접 실행·수치 인용)

- `node run.js 0450 nete2ecap` → **5/5 OK**: 4 client·40 intent·M(3) 복제 순열+손실+독립 페이스(1·2·3) → 실 engine Net 배리어 == 배리어-free substrate == canonical·전 복제 desync 0·exactly-once(complete)·전순서 sound(strict+causal)·skew 26(비-lockstep)·손실 발생.
- `node run.js 0450 reg` → **ALL OK**(async-net 미호출→비트 동일).
- `node run.js spine` → **ALL OK**(전 시리즈 누적 회귀·async-net 비-침습).
- grep 감사: `Math.random` src 0 · `UObject|CoreUObject|GameplayTags` src 0 · async-net run() 경로 호출 0 hit · `new __c.Net`(`async-net.js:176`)은 verify 전용 자체 인스턴스.
- 부분 재현(대표 모드): `netsimfold`(totalOrder 순열 불변·raw 도착 갈림)·`netdispatch`(실 actor.digest==canonical·applied40)·`netlossy`(gaps≥10·resyncs≥4·수렴)·`netpace`(skew12~28·desync0)·`netbarrier`(delivered40·배리어==배리어-free==canonical).
- 알리바이 라인: `async-net.js` 21(worldIntentStream)·53(simFold)·66(makeZoneMailbox)·81/91(makeZoneActor/deliverToActor)·98(makeZoneResync)·119(convergeReplicas)·137(driveAsyncReplicas)·155(accountReplicas)·175(runLockstepEngine·`new __c.Net`:176)·199(capstoneReplicas).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #4 | broker lockstep = 강제 동기(진짜 비동기 미해제) | ① | 🟡 **대폭 진전(in-proc 등가 ✅·잔여=실 net.step *실제* 치환)** | 0441~0450 이 `async-net.js` 로 substrate 를 실 Net 메시지·실 sim seam 에 이어 *등가*(배리어==배리어-free substrate==canonical·`nete2ecap` 5/5) 증명. **잔여**: async-net 을 실 `net.step` 배리어 배선에 → **목적지 STATE §2 NEXT**(다음 큰 load-bearing) → 게이트: run() 의 net.step 동기 tick 을 async-net holdback/resync 배선으로(끄면 현 lockstep=회귀 0) |
| #71 | async substrate 가 실 전송/run() 미배선(검증 전용·by-construction) | ⑤/검증 | 🟡 **확장(async-net 도 run() 밖)** | 0441~0450 브리지는 실 Net *메시지 형태*·실 sim seam 까지 왔으나 여전히 run() 밖 검증 전용(runLockstepEngine 도 자체 Net) → **목적지 STATE §3 OPEN GAPS**(#4 실 치환과 동반) → 게이트: run() 의 net.step 이벤트를 async-net 에 흘려 진짜 분기 도입(끄면 합성=회귀 0) |
| #53 | 정합-0 이 by-construction(분기 시나리오 부재) | 검증 | 🟡 **확장(async-net 합성 드라이버)** | convergeReplicas/capstoneReplicas 는 verify 하니스가 짠 합성 순열+손실 드라이버(실 net 분기 아님·#53 의 비동기 브리지 판) → **목적지 STATE §3 OPEN GAPS**(#4 실 치환이 실 분기 도입) → 게이트: 실 net.step 배선 시 실 전송 열화가 분기 제공 |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/정리 | 🟡 **확장(+netintent~nete2ecap 10 모드 미승급)** | 이번 묶음 bespoke 10 모드도 미승급·현 spine 모드=최신 `nete2ecap` 1개(나머지 git per-commit) → **목적지 STATE §3 OPEN GAPS**(정리성) → 게이트: 안정 브리지 스모크 1~2 모드 점진 승급(끄면 현 spine=회귀 0) |
| #70 | UpClient/DownClient in-proc 액터(실 host.js OS 프로세스 아님) | ⑤ | 🟡 유지(이번 묶음 미접촉·#57 짝) | STATE §3 — 실 cluster 업스트림/다운스트림 RPC |
| #68 | orch zoneHost entity 컨테이너 미동기(where-view 만) | ③/정리 | 🟡 유지(경미·미접촉) | STATE §3 — #62 합류 묶음 |
| #69 | runMultiViaCoord 동치 transitive | 검증 | 🟡 유지(경미·미접촉) | STATE §3 — 동일 substrate 비교 경로 |

> **🎯 단계 평결 (0441-0450·SKILL §3.6)**: **#4 실 net.step 배리어 치환 브리지(0441~0450·직전 0431-0440 verdict 가 STATE §2 로 올린 load-bearing #4 의 실 Net·sim seam 판)**. progress 맵 ⬜/🌱 박스 0(너비 1차 완료 유지) 위 *verdict 가 지목한 #4*(0001 이래 최상위 미해소 load-bearing) → **과심화 아님**(코디네이션 substrate→실 Net 브리지 기능 8·신규 박스 `async-net.js`). async-core 가 *추상 이벤트* in-proc 이었듯, 실 net.step *실제* 치환에 앞서 그 등가(실 engine Net 배리어==배리어-free substrate==canonical)를 실 Net 메시지·실 sim seam 위에서 증명. **재현 증거(시드 5종 5/5)**: `nete2ecap`(순열+손실+페이스→배리어==substrate==canonical·전복제 desync0·exactly-once·sound·skew26)·`netbarrier`(delivered40·배리어==배리어-free==canonical)·`netsimfold`(totalOrder 순열 불변·raw 도착 갈림)·`reg`/`spine` ALL OK(async-net run() 미호출→비트 동일)·`Math.random`/UObject src 0·async-net run() 호출 0 hit·>30KB 박스 0(async-net 17.4KB). **#4 🟡대폭 진전(in-proc 등가 ✅·잔여=실 net.step *실제* 치환)**·부수 **#71·#53 확장(async-net 도 run() 밖 합성 by-construction)**. **다음 권고: #4 실 net.step 배리어 *실제* 치환(async-net→run() 배리어 배선·다음 큰 load-bearing) 또는 실 host.js child 업스트림(#70·#57 짝), 그 다음 경미 정리(#68·#69·#16 승급).** step-loop 이 STATE §2 로 승급.
