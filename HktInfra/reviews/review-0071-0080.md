# review-0071-0080 — 귓속말/파티 라우팅 + 전달 신뢰 호

> 범위: 닫힌 step **0071~0080**(프레즌스 질의 인터페이스의 첫 라우팅 소비자 → 전달 신뢰 호: 영수증→재시도→상한→통지→exactly-once). 박스별 진행 서사는 [progress/3-services.md](progress/3-services.md)·[progress/5-coordination.md](progress/5-coordination.md). 이 문서는 *정합 감사*만(narrative 요약표 없음 — progress·STATE INDEX 가 가짐).
>
> 권위: 판정은 PASS 주장이 아니라 실물 코드(`../src/*.js`) + `node run.js` 재현 + grep. 0049 단일 src/ 모델 — 알리바이는 *현재 누적 코드* 라인.

## §1 척추 정합성 — SPINE §5 5불변

| # | 불변 | 판정 | 코드 알리바이 |
|---|---|---|---|
| ① | 신성한 tick | ✅ (1 주석 편차) | wrouter/mbox/pservice 셋 다 존 tick 밖 비동기 서비스. **0077 이 라우터에 `onTick` 추가**(`src/svc-whisper.js:94`) — 단 이는 *제어 평면 벽시계 timeout*(전달 재시도)로 orch 의 recoverRetry onTick(0058)과 동형이지 존-시뮬 tick 아님. 게이팅 견고: `if (!this.deliverRetry || !this.inflight.size) return`(`:95`) → OFF 면 no-op(reg 0). **편차**: 박스 헤더 주석 `:16` 이 여전히 "onTick 없음·순수 반응형"이라 적힘(0077 후 stale·아래 #21) |
| ② | 결정론 코어 | ✅ | 세 박스 다 세계 상태 무변경. 전달/질의/ack/재시도 전부 정규 `net.send`(시드 로그). grep `Math.random` src/ **0 hit**. deliverTimeout·deliverMaxRetries 결정론 상수 |
| ③ | 권위 단일 소유 | ✅ | grep `minted|ledger|owner|mint(` in svc-whisper/mailbox/party = **0 hit** — 라우터/수신함/멤버십 다 전달·라우팅·멤버십 회계만(원장 권위 0). 전 step `minted ON==OFF`(비-침습). PartyService 는 *멤버십* 권위만(`src/svc-party.js:21`·set) |
| ④ | 은닉·단일 연결 | ✅ | 클라는 wrouter 에 귓속말/파티만 발신(대상 위치 무지). 라우터는 SSOT 를 질의 계약(presenceQuery/Reply)으로만·멤버십을 partyQuery 로만 소비(`src/svc-whisper.js:54·58`). 승격 박스 주소는 *공지로* 디스커버리(`:43` svc.presence.active·하드코딩 0). 박스 크기 유계(whisper 18.9KB·mbox 5.8KB·party 3.3KB <30KB) |
| ⑤ | headless·원격 검증 | ✅ | grep UObject/UE 모듈 src/ **0 hit**. `node run.js`/`spine` 수치 증명. 단 wrouter/mbox/pservice 는 멀티프로세스 host 미배선(#9 확장·아래) |

**회귀 0 규율**: 10 step 전부 새 플래그 OFF → baseline 비트 동일(reg ALL OK·월드해시 `0x7a122947`(seed42) 0070 이래 보존). 새 박스(wrouter·mbox·pservice)는 플래그 게이트로 토폴로지에서 빠짐(`src/topo-build.js` whisperAddr/mailboxOn/partyAddr 조건부 add) → 미설정이면 박스 0.

## §2 검증 재현 (실행·인용)

- **`node run.js spine`** (src 누적 회귀 — 전 역사 불변을 현재 코드에 단언): **ALL OK** (80-step 사슬). "0071~0080 이 더한 코드가 0001~0070 의 어느 불변도 안 깼다"의 증거.
- **`node run.js 0080`** (현재 step·wdedup): **ALL OK** — ON `received 1·inbox 1·duplicates 1`(exactly-once) vs OFF `2/2/0`(중복 적재)·둘 다 delivered 1. E2E 14프로세스 비트 동일.
- **grep 감사**: `Math.random` src/ 0 · UE 모듈 0 · svc-whisper/mailbox/party 내 동기 I/O·await 0 · 권위 쓰기(minted/ledger) 0.
- **멀티프로세스 host 범위**(grep `src/host.js`): wrouter/mbox/pservice/presence 박스 스폰 **0 hit** — whisper/party/partyTo 주입은 전부 inproc run 루프(`src/topology.js:174·176·178·180`)뿐. 0071~0080 호 전체가 inproc 제어 평면 전용(E2E 는 reg-0 비트 동일만 보증·ON-경로 미검증).

> 0072~0074 의 *bespoke 가설 모드*(whisperfo·party·whisperretry)는 현재 verify.js 가 0080(wdedup)으로 덮어써 직접 재현 불가 — 단일 src 모델의 본질(#16). spine 은 reg-0 만 든다.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #17(b) | 재타깃 윈도 질의 손실 | ④/⑤ | ✅ **0074 해소** | whisperRetry — 재타깃 시 보류 질의 재발신(`src/svc-whisper.js:61`·at-least-once 읽기). #17 의 self-hb 메아리(a)만 잔존 |
| #16 | bespoke 가설 누적 회귀 미승격 | ②/정리 | 🔴 **확장** | 0071~0080 이 10개 새 가설 모드(whisper·whisperfo·party·whisperretry·partysvc·wdeliver·wretry·wcap·wnotify·wdedup) 추가 — 전부 close 시 1회 단언·spine 은 reg-0 만. 전달 신뢰 호 *전체 ON-의미*가 누적 회귀 밖 → **STATE §3** · 게이트: 안정 호를 verify-kit 승격 or wrouter 스모크 1 모드(끄면 현 spine=reg 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장** | wrouter/mbox/pservice 호 전체가 `src/host.js` 미배선 → 전달 신뢰·파티 라우팅 멀티프로세스 미검증(#9 의 프레즌스 갭에 라우팅 호 추가) → **STATE §3** · 게이트: host 에 박스 배선 + 멀티프로세스 주입(현 inproc FIFO=회귀 0) |
| #18 | 전달 신뢰 호가 1:1 귓속말 한정 | 서비스 | 🟡 신규 | 0076~0080(영수증·재시도·상한·통지·dedup)은 whisper→mbox 단일 수신자만 배선 — 파티 1:N(0073) 부분 전달은 멤버별 영수증/dedup 집계 미적용 → **STATE §3** · 게이트: 멤버별 inflight/seen 키잉(0073 위·끄면 현 1:1 거동=회귀 0) |
| #19 | 파티 멤버십 영속·failover·증분 미착수 | 서비스/데이터 | 🟡 신규 | 0075 §9: PartyService 멤버십 인메모리(저널/failover 0·가방/프레즌스가 걸은 길 미적용)·`partyCreate` 덮어쓰기(증분 join/leave·변경 발행 0) → **STATE §2/§3** · 게이트: 멤버십 저널+재타깃(가방 0017/프레즌스 0066 패턴의 멤버십 판) |
| #20 | dedup seen 무계 + 전달 실패 발행 부재 | 서비스/④ | 🟡 신규 | 0080 §9: Mailbox `seen` 무한 성장(0042 seenBound/0047 per-producer 워터마크의 전달 판 미적용)·0079 §9: deliveryFailed point-to-point 만(svc 토픽 발행·실패율 관측 0) → **STATE §3** · 게이트: dedup 윈도/워터마크 + 실패 발행(끄면 현 거동=회귀 0) |
| #21 | svc-whisper 헤더 주석 stale | ②/정리 | 🟡 신규(경미) | `src/svc-whisper.js:16` "onTick 없음·순수 반응형"이 0077 onTick 추가 후 모순(코드는 정확·주석만 낡음) → **STATE §3 OPEN GAPS**(정리성) · 게이트: 주석 수정(정리 step·코드 0·reg 0) |

**유지(이 묶음 미착수·원장 잔존)**: #1 결정론 이원화 · #3 cross-zone AOI 긴장 · #4 broker lockstep · #6 동적 경계·N 존 · #7 채팅 홉 신뢰 · #8 panel-kit Math.random · #10 give×result-ahead · #11 per-producer ack 풀 토폴로지 · #13 HealService 분리(잔여) · #15 적응형 lease 정밀화 · #17(a) self-hb 메아리.

> **호 평가**: 0071~0080 은 SPINE 계층5(프레즌스 SSOT) 질의 인터페이스를 *실사용*으로 채운 호다 — 0069/0070 이 만든 pull 질의의 첫 진짜 소비자(귓속말 라우터)를 세우고, 그 위에 전달 신뢰를 *기존 신뢰성 패턴의 전달 판*으로 한 조각씩 쌓았다(0057 recoverAck→0076 · 0058 recoverRetry→0077 · 0059 recoverMaxRetries→0078 · 0060 presencePublish→0079 · 0026 id-reconciliation→0080). 패턴 재사용이 정연하고 회귀 0 규율·권위 0·신성한 tick 보존이 견고하다. 약점은 *검증 깊이*: 호 전체가 inproc·bespoke 모드라 누적 회귀(#16)와 멀티프로세스(#9)에서 ON-의미가 미보증 — 라우팅/전달 호가 길어질수록 이 빚이 커진다.
