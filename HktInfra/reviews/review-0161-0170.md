# review 0161–0170 — 아이템 우편↔가방 연동(3 레그 + 2-서비스 보존 + saga)

> 범위: 닫힌 step 0161~0170. 인과 서사·서버별 기능 리스트는 [progress/3-services.md](progress/3-services.md) 우편 서버 항(기능 9·10). 이 문서는 *정당성 감사*(척추 5불변 재현 + 이슈→후속)만.
> arc 한 줄: 0157~0160 아이템 우편이 *우편 박스 안 회계*(가짜 escrow)로만 추적되던 것(#40 load-bearing)을, 거래소↔가방(0117~0140)의 *완전한 동형*으로 — 가방 원장 간 3 레그 실물 이동(0161~0163)→2-서비스 보존(0164)→정리 분할(0165)→give saga 신뢰 전달(0166~0170)로 닫았다.

## §1 척추 5불변 정합성 (코드 알리바이 — 현재 `src/`)

| # | 불변 | 판정 | 알리바이 + 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 우편 박스는 존 tick 밖(오프라인 배송). `svc-mail-core.js`·`svc-mail-txn.js` 에 `onTick`·`await`·`fs`·`setTimeout` **0 hit**. give 는 반응형 `net.send`(`svc-mail-core.js:148`)·tick 무관. |
| ② | 결정론 코어 | ✅ | `Math.random` **0 hit**(svc-mail 3 파일). gid 는 단조 카운터(`svc-mail-core.js:_custody` gid++)·시드 무관 결정. 5시드 spine 동일. |
| ③ | 권위 단일 소유 | ✅ **강화** | 우편은 give 를 *요청*만(`svc-mail-core.js:147` `item_req{op:'give'}`)·가방이 원장 권위. 실제 release+acquire **쌍 거래**는 가방 `onMsg` 한 곳서 원자(`svc-inventory-txn.js:72-74` _unown+ledger.set+_own). 재전송(0168)은 가방 (replyTo,gid) dedup 으로 *재실행 0*(`exmlretry` ON xfers 4 vs OFF 5 — 이중 이동 hazard 를 dedup 이 막음 입증). |
| ④ | 은닉·단일 연결 | ✅ (watch) | 우편↔가방은 `item_req`/`item_result` 명시 인터페이스만 — 우편이 가방 내부 직접 안 읽음(`svc-mail-txn.js` item_result 핸들러는 회신 집계만). 2-서비스 보존(0164/0170)은 *verify 하니스*가 양 서비스 accessor 교차 비교(`escrowItemIds()` vs `inventory.byOwner.get('escrow')`) — 서비스 자신은 상대 모름(올바른 은닉). **watch**: ackDrop 테스트 seam 이 `topo-build.js`(production 토폴로지 빌더)에 삽입됨(#41). |
| ⑤ | headless·원격 검증 | ✅ | `UObject`·`CoreUObject`·`GameplayTags` **0 hit**. 전 step `node run.js` headless 통과·spine ALL OK. **단** 전부 인프로세스(host.js 0·#9). |

**메타(회귀 0 규율·박스 유계)**: 새 항 전부 OFF 플래그(mailInv·mailSaga·mailRetry op·미호출 accessor) → reg 0 입증(`run.js 0170 reg` ALL OK). **박스 예산 위반 🔴**: 0165 가 svc-mail.js(30.9KB)를 core/txn/entry 로 분할(25.6KB)했으나 saga(0166~0170)가 **`svc-mail-core.js` 를 34.8KB 로 재성장**(>30720) — close-step 이 src 박스 size 를 체크 안 해 미탐지(#34d·#34c/#36 family).

## §2 검증 재현 (직접 실행 인용)

- **`node run.js spine`** (src 누적 회귀 — 전 역사 불변을 *현재 코드*에 단언) → **결과: ALL OK**(exit 0). 0161~0170 의 어떤 항도 0001~0160 불변을 깨지 않음(아이템 우편↔가방·saga 추가에도 비트 회귀 0).
- **`node run.js 0170 exmllive`** (0170 capstone·현재 step) → **ALL OK**: 전 시드 normal·loss+retry 양 체제 `giveOks/escrowXfers 5/5`·두 서비스 합치 `예`·`sagaLiveConsistent 예(2/2)`. 우편 박스 내부 네 회계층(메시지·아이템·escrow·saga) 동시 닫힘 + 두 *독립* 서비스 회계 합치 재현.
- **`node run.js 0170 reg`** → ALL OK(persistReplicas 미제공=0027 분기 비트 동일).
- **grep 감사**(5불변): Math.random 0·await/fs/onTick 0·UObject 0(svc-mail 3 파일) — 전부 clean.
- **재현 한계(#16 직접 발현)**: 0161~0169 의 per-step 모드(exmlinv·exmlin2~4·mailsplit·exmlsaga·exmlpend·exmlretry·exmlsagac)는 단일 src/ 모델상 `verify.js` 가 0170 로 교체돼 *재실행 불가* — 그 ON-의미는 닫을 때 1회 단언 후 spine(reg-0)만 보증. 수치는 각 step 문서가 보존(예: exmlretry dedup ON 4/0/4 vs OFF 4/0/5).

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| **#40** | 아이템 우편 가방 미연동 | 서비스 | ✅ **해소**(0161~0170) | 거래소↔가방(0117~0140)의 완전한 우편 판: 3 레그(0161~0163·`svc-mail-core.js:_custody`)+2-서비스 보존(0164·escrowItemIds==가방 escrow)+give saga(0166~0170·회신/추적/재전송 멱등/정합/transfers capstone). README 원장서 제거·기록만. |
| **#34d** | `svc-mail-core.js` 34.8KB>30KB(saga 재성장) | 정리 | 🔴 신규(#34c/#36 family) | **STATE §3 OPEN GAPS**(정리성) → 게이트: saga 부분(saga/pending/_resendPending/sagaConsistent)을 `svc-mail-saga.js` 추출(거래소 0124 패턴·기능 0·reg 0) + close-step src 박스 size 체크(#36 와 같은 도구 게이트). |
| **#41** | ackDrop 테스트 seam 이 production wiring(topo-build)에 위치 | ④/정리 | 🟡 신규(경미) | **STATE §3 OPEN GAPS**(정리성) → 게이트: 다른 테스트 seam(invOps/mailOps)처럼 `topo-run.js` inject 경로로 이동(gated·null=무효라 끄면 현 거동=회귀 0). |
| **#42** | 우편 saga 재전송 수동(mailRetry op 1회·주기 없음) | 서비스 | 🟡 신규(#37 우편 판) | **STATE §3** → 게이트: mailSweep 피기백 autoRetry(거래소 0129 의 우편 판·끄면 현 수동=회귀 0). + per-give backoff(#37 본체와 합류 가능). |
| #16 | bespoke 가설 모드 verify-kit 미승격 | ②/⑤/정리 | 🔴 **확장**(+우편 saga 10 모드·exmlinv~exmllive) | §3 OPEN GAPS → 안정 호(우편 가방 연동·saga)를 verify-kit 스모크 1~2 모드 승격(끄면 spine=회귀 0). §2 재현 한계가 이 빚의 직접 비용. |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장**(+우편 saga host.js 0) | §3 → 0161~0170 가방 연동·saga 전부 인프로세스(`invOps`/`mailOps`/`mailRetry` run-loop seam·crash/reconstruct post-run·ackDrop 도 인프로세스 모의). 멀티프로세스 give 핸드셰이크 미검증. |
| #33 | STATE §7 INDEX 30KB 천장 | 정리/도구 | 🟡 **재발현 심화** | §3 OPEN GAPS(도구성) → 이번 묶음서 STATE 트림 ~5회 강제(§4 DURABLE·§5 service row·§3 ✅ note·§2 build-infra 압축). INDEX 묶음당 1행 압축 또는 예산 상향. |

**watch(이슈 미승격)**: 우편 saga 재전송 안전(0168)은 가방 `sagaDedup` 이 *외부에서 켜져야* 성립 — `mailSaga` 가 자동으로 가방 dedup 을 켜지 않는다(verify 가 `sagaDedup:true` 명시). 거래소도 동일 구조(exchSaga↔sagaDedup 별도)라 신규 이슈로 승격 않음 — wiring 결합으로 watch.

**유지(이월·목적지 불변)**: #39(MailFeed reconstruct 무압축·서비스)·#34c(topo-run 31.9KB·정리)·#37(saga 무차별 재전송·#42 와 합류 가능)·#38(announceUp 수동 seam)·#30b(거래소 저널 분산 영속)·#32(시세 발행 완전성)·#7(채팅 홉 신뢰)·#1·#3·#4·#6·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29.
