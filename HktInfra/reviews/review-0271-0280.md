# review 0271–0280 — 도구 #43 + #51b 실 zone.js 브리지 arc

> 범위: 0271(도구 갭 #43·close-step src size 가드) + 0272~0280(#51b 실 zone.js 브리지·전부 코디네이션·오케스트레이터). 인과 서사·서버별 기능 진척은 [progress/5-coordination.md](progress/5-coordination.md)(오케 기능 7→8) · 이 문서는 *정당성 감사*만.

## §1 척추 5불변 정합성

| # | 불변 | 판정 | 코드 알리바이 + 근거 |
|---|---|---|---|
| ① | 신성한 tick | ✅ | 전 브리지 메서드는 제어 평면(orch onMsg placeX 집행)·존 tick 밖. `src/orch-zonebridge.js` 전체가 동기 Map 연산(I/O·await 0)·`src/orch-control.js:34` placeQuery 도 순수 읽기 회신. EntityZone 팩토리(`zone.js`)는 인스턴스화만·onTick 미호출. |
| ② | 결정론 코어 | ✅ | 런타임 시드=`fnv1a(String(zid))`(`src/topo-actors.js:66`)·존별 결정론. `grep Math.random src/*.js`=**0 hit**. zoneBridge 전 경로 시드 PRNG. |
| ③ | 권위 단일 소유 | ✅강화 | `zoneRuntimes` 단일 키=한 존 한 host. migrate=같은 핸들 host 원자 교체(`orch-zonebridge.js:24` `rt.host=toHost`·중간 공백/중복 0). hostDown=죽은 host release 불가→생존 host 재배치(`:31` 새 인스턴스). drift/coherent/fullyCoherent 가 placement==running==zoneRuntimes 3층 단일성 단언(`:51,58,60`). |
| ④ | 은닉·단일 연결 | ✅ | orch 가 zone.js 를 *직접 import 하지 않음* — 팩토리를 `makeActor`(`topo-actors.js:65`)가 주입(코디네이션→월드 박스 직접 결합 회피). 존 spawn 은 오케스트레이터 정의 책임(SPINE §2). 게이트웨이는 placeQuery 회신(`orch-control.js:39` runtimeHost)으로만 위치 앎(실 런타임 주소 직접 모름·request/reply seam). |
| ⑤ | headless·원격 검증 | ✅ | 실 EntityZone 헤드리스 인스턴스(UE 0). `grep -E 'UObject|CoreUObject|GameplayTags' src/*.js`=**0 hit**. **단 인프로세스**(브리지=orch 메모리 내 핸들 레지스트리·실 host.js 소켓 0=#9). |

**메타(회귀 0·박스 유계)**: zoneBridge OFF 기본 → 전 브리지 메서드 가드(`orch-placement.js:13,19,34,41` `if (this.zoneBridge && this.zoneFactory)`)·placeQuery 필드 조건부(`orch-control.js:39` `if (this.zoneBridge)`) → OFF 비트 동일(reg ALL OK·maxDiff 0). 새 박스 `orch-zonebridge.js` 7.1KB·전 src/*.js ≤26KB(>30KB 0). 0271 이 그 가드를 *도구화*(close-step src size·#43).

## §2 검증 재현 (직접 실행·인용)

- `node src/verify.js zonecapstone` → **ALL OK**(5/5 seed). 7종 op(start·auto·migrate·rebalance·drain·hostdown·stop) 혼합 후 `fullyCoherent`=Y·`runtimeCount`==`runningCount`==`placedCount`==3(z4 stop·move 류 집합 보존).
- `node src/verify.js reg` → **ALL OK**(zoneBridge OFF·전 시나리오 비트 동일·maxDiff 0).
- `node run.js spine` → **ALL OK**(src 누적 회귀·전 역사 불변 현재 코드 단언).
- grep 감사: `Math.random` src 0·`UObject|CoreUObject|GameplayTags` src 0·`wc -c src/*.js|awk '$1>30000'`=**0행**(#43 가드가 close 마다 재확인·`engine/close-step.js:59~75`).
- **단일 src 모델 유의**: 0272~0279 의 bespoke 모드(zonebridge·zonemigrate·zonestop·zonehostdown·zonedrift·zonerebalance·zonedrain·zonequery)는 현 verify.js 에 부재(매 step 교체)·git per-commit 에만(#16). 현 spine 모드=`zonecapstone` 하나가 7종 op 전부를 한 번에 단언.

## §3 이슈 → 후속 step 연결

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #43 | close-step src 박스 size 미체크(#49 재발 도구 갭) | 정리/도구 | ✅ **해소(0271)** | `engine/close-step.js:59~75` src/*.js >30KB 비실패 경고. 인덱스서 제거(기록만). |
| #51b | running(executed SSOT)↔실 EntityZone(zone.js) 미배선 | ⑤/③ | ✅ **해소(0272~0280)** | orch-zonebridge.js 가 placement 집행으로 실 EntityZone lifecycle(start/migrate/stop/hostdown/rebalance/drain) 구동·읽기 경로(runtimeHost)·fullyCoherent 3층. 인덱스서 제거. **단 잔여는 #56(데이터 평면)·#9(프로세스)로 승계.** |
| #56 | 브리지 EntityZone 비활성(entity 트래픽·tick 0·migrate 상태보존 vacuous) | ③/⑤ | 🔴 **신규·load-bearing(2차/#9 무대)** | 브리지 존은 핸들만·entity 0·onTick 미호출 → migrate "상태 보존"은 *구조적*(같은 핸들·zoneStarts 불변)이지 *행동적*(entity 실제 생존)이 아니다. **목적지 STATE §2 NEXT**(#9 와 동반·게이트웨이→실 존 enter/move 라우팅 + 이주 시 entity 무손실 실증·OFF→현 비활성 핸들=회귀 0) |
| #53 | 정합 capstone by-construction(분기 부재) | 검증 | 🟡 **확장(#51b 판)** | 0276 drift·0278 coherent·0280 fullyCoherent 도 by-construction — `running`·`zoneRuntimes` 가 *같은 코드 경로*에서 항상 동시 갱신(`orch-placement.js:13~14` 등)→drift≠0 경로 0. **목적지 STATE §3** → 게이트: #9 실 프로세스가 *진짜 분기*(집행 OK·실 인스턴스화 실패) 도입 시 drift 탐지 load-bearing 화(끄면 구조적 0=회귀 0) |
| #16 | bespoke 모드 verify-kit 미승급 | ②/⑤/정리 | 🟡 **확장(+9 모드)** | zonebridge~zonecapstone 미승급·현 spine 모드=zonecapstone 만. **목적지 STATE §3**(정리성) → 게이트: 안정 호 묶음별 스모크 승급(끄면 현 spine=회귀 0) |
| #9 | 멀티프로세스 패리티 갭 | ③/⑤ | 🟡 **확장(+브리지)** | #51b 브리지는 orch 인프로세스 핸들 레지스트리·실 host.js 소켓 0. **이제 최상위 load-bearing**(브리지가 핸들까지 이었으므로 다음은 실 프로세스). **목적지 STATE §2 NEXT** → 게이트: 브리지 핸들→host.js 소켓 분리(끄면 인프로세스=회귀 0) |

> 그 외 유지(미접촉): #46·#48·#44·#50·#55·#1·#3·#4·#6·#7·#8·#10·#11·#13·#15·#17a·#25·#27·#28·#29·#30b·#32·#36·#37·#38·#39·#41.

**🎯 단계 평결(SKILL §3.6)**: 0271=도구 #43(net-core 무변경)·0272~0280=#51b load-bearing 브리지. 너비 1차 완료(⬜/🌱 0) 위 *승인된 심화*(prior 3라운드가 지정한 게이트)→**과심화 아님**. 새 박스 0·reg ALL OK·spine ALL OK. **다음 권고: #9 멀티프로세스 배선(브리지 핸들→실 host.js 소켓) + 브리지 존 데이터 평면(#56·entity 트래픽).** review=기록·step-loop 이 STATE §2 로 승급.
