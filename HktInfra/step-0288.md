# step-0288 — #56 브리지 존 데이터 평면 8: entity 정합(orphan 0)

> 직전 step-0287 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `entityCoherent()`: ⒜ 단일 소유(0287) + ⒝ entity 를 담은 실 런타임 존은 모두 executed `running` SSOT 에 있다(orphan 런타임 0 — running 밖인데 entity 보유 없음). entity 데이터 평면이 executed 배치 SSOT 와 어긋나지 않음을 단언. 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0287 비트 동일=회귀 0·읽기 전용). verify.js 새 모드 `zoneentcoherent`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zoneentcoherent` 5/5: z1·z2·z3 배치+enter(a1·a2→z1·a3→z2·a4→z3) → z1 migrate hostC + z2 stop → `coherent=Y`·`a3owner=null`(z2 폐기로 orphan 0)·`rtCnt=2`·`total=3`.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 질의 읽기·존 tick 밖. ②결정론: `Math.random` 0. ③권위 단일 소유: entityCoherent 가 단일 소유 + executed 정합을 한 술어로 묶음. ④은닉: 게이트웨이는 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 한계: entityCoherent 는 정상 op 에서 by-construction 참(stop/hostdown 이 런타임·entity 동시 정리) — 진짜 분기는 #9 실 프로세스. graceful(rebalance/drain) census 보존·전 계층 capstone 은 0289~0290.
