# step-0201 — 인스턴스(던전) 서버 분리: spawn 기본

> 직전 step-0200 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/instance.js`(InstanceServer) — 던전/매치 *일회성* 시뮬 인스턴스의 spawn SSOT. 계층: **월드(계층2)**. `instanceSpawn{instanceId,kind}` → active 맵 등록(같은 id 재-spawn = 멱등 no-op·권위 단일 소유). 존(영속 tick)과 *수명주기 분리*·onTick 없음(신성한 tick 밖).
- 배선: `net-core.js` 진입점 `__p('instance')` · `topo-actors.js`(import+`case 'instance'`) · `topo-build.js`(`instanceService` 플래그 OFF→박스 0) · `topo-run.js`(handle+`instanceOps` 주입 seam+return).
- OFF 플래그 **`instanceService`**(OFF→박스 0·instanceOps 주입 0 = baseline 0200 비트 동일=회귀 0) · verify.js 새 모드 **`instancespawn`**.
- 1차 너비 정책: 기본 통신만(spawn). despawn·클라 라우팅은 0202.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(instanceService 미제공 → 박스/주입 0 → baseline 0200 비트 동일·maxDiff 0).
- **가설 `instancespawn`**: 5/5 시드 OK — spawn 4회(d1·d2·d1 재-spawn·d3) → **active 3**(d1 멱등 no-op·중복 0)·d1/d2/d3 isActive 예·미spawn d9 아니오.
- **결정론**: 5 시드 동일 active/spawns(시드 로그 순서 결정).
- **spine ALL OK**: 전 역사 누적 회귀 사슬(현재 코드에서 0001~0200 불변 + reg 0 유지).

## 척추 5항 + 한계

- ① 신성한 tick ✅ — InstanceServer onTick 없음·spawn 은 onMsg 반응(존 tick 밖). ② 결정론 ✅ — `Math.random` 0·active=instanceOps 시드 순서. ③ 권위 단일 소유 ✅ — active 맵이 "어떤 인스턴스가 사나" 유일 SSOT·spawn 멱등(중복 0). ④ 은닉 ✅ — instanceOps 는 orch/gateway 발신 정규 net.send(클라 직접 0). ⑤ headless ✅ — Node·UE 0.
- 한계: spawn 만(despawn·라우팅·부하 분산 미착수 — 0202~). 멀티프로세스 미배선(host.js 0·#9 family·인프로세스 검증).
