# step-0343 — #9 후속: 다운스트림 상호 가시 수렴(증분 델타로 위치까지 desync 0)

> 직전 step-0342 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 권위 AOI 서명 질의 `zoneAuthSig(zoneId, avatar)` = avatar 가 지금 봐야 할 AOI 의 `id@x,y` 서명(host 권위·`DownClient.seenSig()` 와 같은 형식). 둘이 같으면 위치까지 desync 0(0342 의 id-only 보다 강함). 읽기 전용.
- 계층: 월드/엣지(②⑤·수렴 desync 0). 플래그 없음·읽기 전용 → baseline 비트 동일. verify.js 새 모드 `dcmutual`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 서명 질의 추가 → baseline(0342) 비트 동일.
- `dcmutual` 5/5 OK — a1@(5,5) 을 a2@(13,15) 쪽으로 8회 이동(반경 4 안 진입) 뒤: `dc0.seenSig` == `zoneAuthSig('z1','a1')`·`dc1.seenSig` == zoneAuthSig('z1','a2')(**위치까지 desync 0**)·둘 다 `[a1,a2]`(상호 가시·증분 델타 enter/update 로 수렴).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 클라 적용은 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 클라 읽기전용 뷰가 권위 재현으로 수렴. ④ 은닉: 클라는 게이트웨이만. ⑤ headless: Node 순수·reg 0.
- 한계: 인프로세스 DownClient·무손실 경로 수렴 — 손실 하 수렴(재전송 후 desync 0)·다중 클라 일관은 후속(0344+). 이상 없음.
