# step-0325 — #9 후속: host 산출 뷰의 직렬화 경계 (와이어 준비)

> 직전 step-0324 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 질의 `zoneViewWire(zoneId)` = 그 존 산출 view frame 들의 {frames, bytes, serializable}(JSON round-trip 동일 여부 + 와이어 바이트). 다운스트림 뷰가 실 소켓(host→게이트웨이→클라)을 탈 준비가 됐는지의 증거 — 함수/순환 참조가 섞이면 serializable=false. `_zoneDeliver`(0291·업스트림 직렬화 경계)의 다운스트림 짝.
- 계층: 코디네이션/월드(⑤②·#9 원격 검증). 읽기 전용 → 0324 비트 동일. verify.js 새 모드 `hostzoneviewwire`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0324) 비트 동일.
- `hostzoneviewwire` 5/5 OK — z1 에 a1·a2 enter·a1 이동: 산출 view_delta **4** frame 전부 JSON round-trip 동일(`serializable` Y)·와이어 바이트 **440**>0·frames == zoneViewFrames.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 5종 동일·바이트 동일. ③ 권위: 권위 존 산출. ④ 은닉: 게이트웨이 발신·직렬화 경계 통과. ⑤ headless: Node 순수·직렬화 가능 = 원격-검증 토대·reg 0.
- 한계: 직렬화 *가능성*만 확인(실 소켓 전송은 게이트웨이→클라 배선 후속). 이상 없음.
