# step-0355 — #57 실 host.js OS 프로세스 spawn 5: clusterDriver onFrame entity frame egress

> 직전 step-0354 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-bridge-init.js` — recorder 드라이버에 `onFrame(host,zoneId)` + 회계 `driverFrames`. host 컨테이너 inbox enqueue 마다 = 실 `cluster.rpc(host,{cmd:'deliver'})` 소켓 송신.
- `src/orch-zonebridge.js` — `_zoneDeliver` 의 host inbox enqueue 지점(0302)에서 `clusterDriver.onFrame(rt.host, zoneId)` 호출. 미부착→호출 0.
- 계층: 코디네이션/월드(⑤②). OFF 플래그 `clusterDriverRecord`(드라이버 null→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostframe`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미부착 → baseline(0354) 비트 동일.
- `hostframe` 5/5 OK — z1@hostA·a1 enter+6회 이동 뒤 `driverFrames`==`zoneHostFramesRecv`==`frames.length`==7(>0·모든 inbox 수신이 드라이버로)·모든 frame host==hostA.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: onFrame 은 _zoneDeliver(전송 seam·tick 경계) 안·시뮬 tick 밖. ② 결정론: 5종 동일·frame 수 7 일정. ③ 권위: 단일 존 단일 host(모든 frame hostA). ④ 은닉: host:zoneId 키만(직렬화 가능 frame). ⑤ headless: Node 순수·reg 0.
- 한계: onFrame 은 frame 의 *송신 이벤트*만 — 실 cluster.rpc deliver 집행은 cluster-run.js 후속(0357). 이상 없음.
