# step-0265 — 정리(#49 인접·선제): svc-guild 트랜잭션 핸들러 믹스인 분리

> 직전 step-0264 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-guild.js`(29.5KB·30KB 근접·성장 박스 선제 정리) → `GuildService` 의 *트랜잭션 핸들러*(`onMsg`: guildCreate/Join/Leave/Transfer/Deposit/Withdraw/Query)를 새 박스 파일 `src/svc-guild-txn.js` 믹스인으로 분리. 계층: 게임 서비스(길드). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변). 0124 svc-exchange-txn·0264 svc-exchange-persist 와 같은 패턴.
- 배선: svc-guild.js 가 `require('./svc-guild-txn.js')` 1줄 · 클래스 정의 직후 `Object.assign(GuildService.prototype, GuildTxn)`. 잔류(코어): 생성자·_normalize·_journalChange·_publishChange/_publishBank·accessors(membersOf·masterOf·bankOf·정합 단언)·crash·reconstruct. verify.js 새 모드 `gldsplit`.
- 결과 크기: svc-guild.js 29.5KB→24.4KB·svc-guild-txn.js 6.4KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `gldsplit`(가설): 5/5 — guildCreate+join+transfer(m1→a)+deposit×2+withdraw 후 master=a·members 4·bank=[i2]·로스터/금고 정합 true. 옮긴 onMsg 가 single-master·금고 단일 소유 보존(위임 무결).
- spine: ALL OK(전 역사 누적 회귀 + gldsplit 편입).

## 척추 5항 + 한계

①tick: 길드 트랜잭션은 존 tick 밖 제어 평면·무변경 ②결정론: 코드 이동만·시드 동일 ③권위: single-master·금고 itemId 단일 소유 보존 ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
