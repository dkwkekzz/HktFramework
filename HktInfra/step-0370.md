# step-0370 — #57 실 데이터 평면 10·grand capstone: 실 cluster 데이터 평면 E2E

> 직전 step-0369 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `clusterCoherent(orch, cluster)` = `clusterDesync==0` grand capstone 술어: 실 cluster 전체가 in-proc 권위와 한 몸(실 프로세스 경계 넘어 SPINE §5 수렴). #57 실 데이터 평면 sub-arc(0361~0370) 종합.
- 계층: 코디네이션/월드(⑤②). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `clusterdatacap`. **#57 실 데이터 평면 0361~0370 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0369) 비트 동일.
- `clusterdatacap` 5/5 OK — 2 host·3 zone(z1·z3@A·z2@B)·entity+move: ① `driveCluster` 뒤 `clusterCoherent` **Y**(전 entity 실 host==권위·desync 0) ② 실 `migrateZone` z1 A→B 뒤 a1 hostB 보존(== in-proc 권위) **Y** ③ hostA z1 release **Y**. host→실 host.js OS 프로세스/소켓 데이터 평면 E2E.
- `node run.js spine` = ALL OK (실 child_process·정리 shutdown).

## 척추 5항 + 한계

- ① tick: capstone 술어는 읽기·driveCluster tick 은 실 zone.onTick. ② 결정론: 5종 동일. ③ 권위: 실 프로세스 전 entity 가 in-proc 권위 단일 재현(desync 0)·migrate release+acquire. ④ 은닉·격리: 실 프로세스 경계가 존 격리 강제. ⑤ headless: 실 host.js 다중 프로세스 데이터 평면·snapshot — 원격 검증.
- 한계: #57 *실 데이터 평면*(deliver/tick/migrate/kill/failover/reconcile·desync 0) 닫힘. 잔여(다음 arc): cluster-run.js runMulti 코어에 orch 상주(broker 측 제어 평면)·연속 tick 루프·in-flight frame 재라우팅·업스트림 intent 실 클라(#61)·진짜 비동기(#4). 이상 없음.
