# step-0278 — #51b 실 zone.js 브리지 7: _drain 실 핸들 비움

> 직전 step-0277 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `bridgeCoherent()` 정합 불변 질의 추가(0280 capstone primitive): drift 0 **AND** runtimeCount==running 수 → 추상 집행 SSOT 와 실 EntityZone 레지스트리 완전 일치(한 존=한 host·양쪽). `_drain` 은 이미 매 move `_migrate`→`_bridgeMigrate`(0273)로 실 핸들을 graceful 이주하므로, host 드레인이 그 host 실 런타임을 비우고(runtimeOn 0) 정합 유지함을 단언.
- OFF 플래그 `zoneBridge`(기존·질의이므로 OFF→0277 비트 동일). verify.js 새 모드 `zonedrain`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonedrain`**: 5/5 seed — hostA(z1·z2) 드레인 후 `runtimeOn(hostA)`=0·z1·z2 생존 host 이주·runtimeCount=3·`bridgeCoherent`=Y·`zoneStarts`=3 불변(graceful 이주이지 재생성 아님·상태 보존·hostdown 과 구분).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 제어 평면·존 tick 밖. ②결정론 — 드레인 최소부하 tie-break 결정론. ③권위 단일 소유 — release+acquire 연쇄(공백/중복 0)·bridgeCoherent. ④은닉 — orch 집행. ⑤headless — 실 EntityZone. 한계: drain(자발·상태 보존) vs hostdown(비자발·소실) 의미 분리 — drain 은 zoneStarts 불변(같은 핸들), hostdown(0275)은 zoneRescued(새 인스턴스). 이상 없음.
