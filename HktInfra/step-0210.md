# step-0210 — 로그인 티켓 만료 (loginExpire·TTL) · 너비 1차 마지막 박스

> 직전 step-0209 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/loginqueue.js` — `loginExpire{ttl}` → `issuedAt+ttl ≤ now` 인 발급 티켓 회수(admitted 제거)·dequeue 가 issuedAt(tick) 기록·`expired`/`expires` 계측. 계층: **엣지(계층1)**. 들고만 있고 안 쓰는 티켓이 엣지 자원을 영영 묶지 않게(재접속/만료 토대·0001 스텁의 *만료* 실체화). 로그인 큐 박스 기본 통신 완비.
- 배선: `topo-run.js` loginOps 를 직접 `onMsg({...,tick})` 로(우편 mailOps 패턴·만료 기준 tick). loginExpire 미주입→미발화 = 0209 비트 동일(reg 0).
- OFF 플래그 **`loginQueue`** · verify.js 새 모드 **`loginexpire`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(loginQueue 미제공 → 0208 비트 동일).
- **가설 `loginexpire`**: 5/5 시드 OK — p1(issuedAt 4)·p2(issuedAt 6) 입장 → expire(ttl 2)@7 → **p1 만료(4+2≤7·ticket null)·p2 생존(6+2>7·tkt-2)**·admitted 1·expired 1.
- **결정론**: 5 시드 동일. **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(loginExpire onMsg 반응·존 tick 밖) ② 결정론 ✅(`Math.random` 0·issuedAt+ttl 비교) ③ 권위 단일 소유 ✅(admitted 가 엣지 입장 SSOT) ④ 은닉 ✅(loginOps gateway 발신) ⑤ headless ✅(Node·UE 0).
- **너비 1차 완료 — 빈 박스 5종(인스턴스·오케스트레이터 배치·캐시·월드 영속·로그인 큐) 전부 기본 통신 도달**(0201~0210). 단계 판정(1차 완료 여부·다음 방향)은 `infra-review`(progress 맵 ⬜→🟡). 각 박스의 심화(영속·failover·라우팅·부하 배치·write-behind·계정 검증)는 2차 고도화 백로그.
- 한계: 멀티프로세스 미배선(#9·전 신규 박스 host.js 0)·신규 5박스 spine 미승급(#16 family·bespoke 모드).
