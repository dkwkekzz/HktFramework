# step-0498 — svcsvccombined 3 서비스 종합 격리 편입 (#16 라운드 3차 8)

> 직전 step-0497 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 서비스 계층 *종합 격리* capstone 을 신규 편입: `run()` 을 exchange+mail+guild 세 서비스 동시 opts 로 구동하고 각 정합 술어가 *동시* 성립 + 상호 비간섭(서비스 격리·아이템 무손실)을 단언하는 새 mode `svcsvccombined` — 거래소 sagaLiveConsistent·우편 sagaLiveConsistent·길드 rosterConsistent·bankConsistent 전부 Y + 공유 가방 격리(minted 2·거래소 giveOks + 우편 giveOks == escrowXfers·item0 buyer·item1 bob 각 단일 소유). 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 8/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcsvccombined`: 5/5 OK — exch/mail/roster/bank/격리 전부 Y. 세 서비스가 한 run() 에서 공존·각 술어 성립·공유 가방(inventory) escrow 회계 합치(간섭 0).
- spine ALL OK — svcsvccombined 포함(서비스 계층 종합·누적 회귀).

## 척추 5항 + 한계

①tick: 세 서비스 모두 존 tick 밖·박스 무변. ②결정론: 다서비스 술어 AND·격리 대조·Math.random 0. ③권위: 공유 가방 아이템 각 단일 소유(거래소·우편 escrow 합치)·길드 single-master/금고 단일 소유 동시 재단언. ④은닉: 각 서비스 버스/give 경유·상호 내부 미참조. ⑤headless: node in-proc. — 이상 없음(서비스 격리=한 프로세스 내 논리 분리·실 프로세스 분리는 트랙 밖).
