# step-0418 — #62 코드 합류 8: restart — 상태 보존 계획 재시작 시나리오 번역·구동

> 직전 step-0417 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `coordScenarioFromOpts` 에 `restart`(상태 보존 계획 재시작·옛 runMulti `invRestart` 의 zone cluster 판) case 추가. coord `runScenario` 는 이미 처리(0403) — 번역만 잇는다.
- 계층: 코디네이션(#62 코드 합류 — 계획적(업그레이드·정비) 재시작을 선언 시나리오로·failover 와 대조해 entity 무손실).
- OFF 플래그: 미부착(verify 만 호출) → 회귀 0.
- verify.js 새 모드: `coorddelegrestart`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일).
- 가설 `coorddelegrestart`: 5/5 — migrate z3 off hostA + restart z1@hostA_r → coherent(runMultiCoherent) Y·restarts 1·a1==권위 1/1·placement[z1]=hostA_r(상태 보존).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉 ⑤headless — 이상 없음. restart 가 oldHost 를 kill 하므로 같은 host 의 다른 존(z3)은 migrate 로 먼저 비운다(scenario 순서 t2<t3) — 안 비우면 z3 소실(의도된 시나리오 제약·failover 와 동형).
