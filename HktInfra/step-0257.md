# step-0257 — 캐시 explicit delete (DEL)

> 직전 step-0256 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheDelete{key}` 핸들러 추가. 계층: 데이터(캐시). 플래그 없음(새 메시지 타입·미수신→0256 비트 동일=reg 0).
- 메커니즘: 키를 store·setAt·keyTtl·negatives 에서 제거 + writeThrough(0252) ON 이면 backing source(SSOT)에서도 제거. 무효화(0212·캐시 사본만 끊고 소스 유지→read-through 재적재)와 달리 *소스까지 영구 삭제*(계정 삭제·아이템 파괴). 없는 키 멱등(deletes/deleted 계측). Redis DEL 더미판.
- verify.js 새 모드 `cachedel`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(cacheDelete 미수신 → 비트 동일).
- `cachedel`(가설): 5/5 — WT ON: set k=v→delete→get = undefined(소스서도 제거). 대조 invalidate k2: get = v2(소스 유지·read-through 재적재)·deleted 1.
- spine: ALL OK(누적 회귀 + cachedel 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: 소스 SSOT 까지 일관 제거(영구 삭제) ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음.
