# step-0335 — #9 후속: 다운스트림 per-세션 시퀀스(egress dseq·순서/유실 추적)

> 직전 step-0334 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` `_drainZoneEgress` + `src/orch-bridge-init.js` — egress frame 마다 *세션별 단조* 다운스트림 시퀀스 `dseq` 부여(`zoneEgressSeq` Map·sessionId→next). zoneView 봉투에 `dseq` 동봉(클라가 순서/유실 감지·ack/재전송의 토대).
- `src/gateway.js` `_recvZoneView` — per-세션 `downSeqNext` 기대값으로 순서 추적: dseq==기대면 next 전진, 아니면 `downSeqGaps`++. 질의 `gatewayDownSeqNext`/`gatewayDownGaps`.
- 계층: 코디네이션/엣지(②⑤·SPINE §4 경로2 신뢰 전달 토대). 플래그 없음(egress 의 일부 — zoneEgress OFF→dseq 0). verify.js 새 모드 `gwseq`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneEgress OFF → egress 0·dseq 0 → baseline(0334) 비트 동일.
- `gwseq` 5/5 OK — a1·a2 enter+이동 뒤 `gatewayDownGaps` **0**(인오더 전송)·세션별 `gatewayDownSeqNext` = 그 세션 수신 frame 수(s:a1 2·s:a2 2 = dseq 0,1 연속).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: egress·추적 모두 메시지 처리(시뮬 외). ② 결정론: 5종 동일·dseq 결정론(시드 함수). ③ 권위: 시퀀스가 권위 무영향. ④ 은닉: dseq 는 orch→게이트웨이 전송 메타(클라 무관). ⑤ headless: Node 순수·직렬화·reg 0.
- 한계: 인오더 전송이라 gap 0 — 손실/재정렬 하의 gap 감지·재전송은 후속(0336+). dseq 는 아직 게이트웨이까지(클라 ack 미연결). 이상 없음.
