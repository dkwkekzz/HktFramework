# step-0303 — #9 잔여(실 host.js 물리 분리) 3: host 컨테이너 단일 소유 + 표류 질의

> 직전 step-0302 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — 읽기 전용 질의 2종. ① `zoneHostSingleOwner()`: 어떤 존도 두 host 컨테이너에 동시 귀속하지 않는다(존 host 단일 소유의 *컨테이너* 판·zone-host 핸들 0276 동형). ② `zoneHostDrift()`: host 컨테이너(host→{zones})와 집행 SSOT(running·zoneId→host)가 어긋난 존 수 — running 존이 자기 host 컨테이너에 없거나 다른 host·컨테이너엔 있는데 running 밖(orphan). placeExecute+zoneHostProc ON 이면 모든 배치 op 뒤 0.
- 계층: 코디네이션(⑤). 읽기 전용 → OFF/ON 무관 0302 비트 동일. verify.js 새 모드 `zonehostdrift`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0302) 비트 동일.
- `zonehostdrift` 5/5 OK — 혼합 lifecycle 후 zoneHostDrift 0(컨테이너==running)·zoneHostSingleOwner Y(존 두 host 없음) + directFlowCoherent Y·entityConserved Y·total 1·ledger 5/1/2/1.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 질의는 읽기 전용(tick 무관). ② 결정론: 자료구조 순회만·시드 5종 동일. ③ 권위: 단일 소유 불변을 1급 술어로 단언(컨테이너 차원). ④ 은닉: orch 내부 레지스트리 질의. ⑤ headless: Node 순수·reg 0.
- 한계: drift/single 은 정상 op 에선 by-construction 참(_hostSet 단일 귀속). 이주 *중* in-flight frame 을 host 가 거르는 stale 거부(0296 의 host 판)는 후속(probe 필요). 이상 없음.
