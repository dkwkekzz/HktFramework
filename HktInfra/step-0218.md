# step-0218 — 오케스트레이터 존 재배치 핸드오프(placeMigrate·권위 단일 소유)

> 직전 step-0217 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orchestrator.js` — `placeMigrate{zoneId,toHost}` → 이미 배치된 존을 release(기존 host)+acquire(toHost) *쌍*으로 옮긴다(Map 단일 키 원자 교체·존 권위 단일 소유 보존·공백/중복 0·0006 핸드오프의 배치 판). 미배치 존·같은 host 는 거부(no-op).
- 계층: 코디네이션(계층5). OFF 플래그: `placeMigrate` 미주입(미발화 → 0217 비트 동일·reg 0). verify.js 새 모드 `placemigrate`.
- 2차 고도화 오케스트레이터 #2 — 부하 배치(0217) 위에 재배치 핸드오프(존 권위가 host 사이를 매끄럽게 이주).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (placeMigrate 미주입 → 0217 비트 동일).
- placemigrate: 5/5 — z1 A→C 이동·부하 A 0/B 1/C 1·총 배치 2 보존(공백/중복 0)·migrations 1·migrateRejects 2(같은 host+미배치).
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 오케는 존 tick 밖·재배치는 제어 평면. ②결정론: 재배치 결정론(같은 ops→같은 배치). ③권위: release+acquire 쌍이 존 소유 host 를 어느 순간에도 정확히 1로 보존(총 배치 수 불변). ④은닉: 클라 접점 무변. ⑤headless: Node·원격 재현. — 이상 없음.
