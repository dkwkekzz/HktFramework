# step-0516 — guildbankpending saga 미해결 추적·회신 손실 감지 (#46 금고↔가방 escrow 6)

> 직전 step-0515 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild.js`+`topo-*` — 금고 saga 미해결 추적 + 회신 손실 감지(거래소 0125·우편 0167 의 금고 판). `_custody` 가 gid 를 `pending`(Set)·`pendingGive`(Map·재전송 소스)에 넣고 `_onGiveReply` 가 회신 도착 시 제거 → 무손실 0 drain. 테스트 seam `ackDrop`(1회)·`ackDropAlways`(매번)로 회신 손실 주입 시 그 gid pending 잔존(acked<gives·미해결 격차 가시). 계층: 3 게임 서비스. verify 새 모드 `guildbankpending`. `saga` OFF→pending 미채움→0515 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — saga OFF→pending 0·손실 seam 미제공→박스 비트 동일.
- 가설 `guildbankpending`: 5/5 OK — 무손실 pending 0 drain·손실(gid0) pending 1·pendingGive 1·acked 1<gives 2(미해결 가시).
- spine ALL OK — #46 arc 6모드(pending 추적).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 손실 감지 5시드 동일. ③권위: 미해결 give 는 pending 낙관적 잔존(가방 give 가 성공했을 수 있음). ④은닉: gid 매칭·give 경유. ⑤headless: node in-proc. — 이상 없음(재전송 pendingGive 보관·재전송/dedup 은 0517·손실 seam 은 테스트 주입[#41 family·production 무영향]).
