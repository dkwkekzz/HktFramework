# step-0269 — 정리(#49 인접·선제): svc-mailbox seen/epoch dedup 헬퍼 믹스인 분리

> 직전 step-0268 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mailbox.js`(24.7KB·30KB 근접·성장 박스 선제 정리) → `Mailbox` 의 *seen/epoch dedup 헬퍼*(`_pruneEpoch`·`epochKeyCount`·`_seenHas`·`_seenAdd`·`seenSize`·`_ack`: exactly-once dedup 워터마크 유계화 + epoch 펜싱 + ack 회신)를 새 박스 파일 `src/svc-mailbox-dedup.js` 믹스인으로 분리. 계층: 게임 서비스(채팅/소셜·귓속말 수신함). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변).
- 배선: svc-mailbox.js 가 `require('./svc-mailbox-dedup.js')` 1줄 · 클래스 정의 직후 `Object.assign(Mailbox.prototype, MailboxDedup)`. onMsg 가 _seenHas/_seenAdd/_ack 호출. 잔류(코어): 생성자·drain·ackDrain·onMsg·accessors. verify.js 새 모드 `mboxsplit`.
- 결과 크기: svc-mailbox.js 24.7KB→22.8KB·svc-mailbox-dedup.js 3.4KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `mboxsplit`(가설): 5/5 — whisperDeliver seq[1,2,1,3] → received 3(유일)·duplicates 1(seq1 중복)·seenSize 0(연속 흡수 유계)·acks 4(중복도 ack 재회신)·epochKeyCount 1. 옮긴 dedup 헬퍼가 exactly-once·유계화 무결.
- spine: ALL OK(전 역사 누적 회귀 + mboxsplit 편입).

## 척추 5항 + 한계

①tick: dedup 은 존 tick 밖 수신함 제어 평면·무변경 ②결정론: 코드 이동만·시드 동일 ③권위: 수신함 단일 소유·dedup 멱등 보존 ④은닉: 내부 구조 분할만·계약(whisperDeliver/whisperAck) 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
