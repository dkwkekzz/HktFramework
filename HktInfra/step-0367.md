# step-0367 — #57 실 데이터 평면 7: 실 다중 host 격리

> 직전 step-0366 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `hostEntities(cluster, host)` 읽기 헬퍼: snapshot → `{ zone: [entityId…] }`(존별 entity). 다중 host 데이터 평면 격리 검증의 관찰 primitive(각 host 가 자기 존 entity 만·교차 누수 0).
- 계층: 코디네이션/월드(⑤②·SPINE §2 존 격리). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostisoreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0366) 비트 동일.
- `hostisoreal` 5/5 OK — z1@hostA(a1)·z2@hostB(b1) → 실 deliver 뒤 `hostEntities`: hostA=={z1:[a1]}·hostB=={z2:[b1]}·교차 누수 0(a1∉hostB·b1∉hostA·실 프로세스 경계).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 헬퍼는 읽기 snapshot. ② 결정론: 5종 동일. ③ 권위: 각 entity 정확히 한 host 프로세스(이중 0). ④ 은닉·격리: 한 host 프로세스가 다른 host 의 존/entity 를 못 봄(실 프로세스 경계가 격리 강제). ⑤ headless: 실 host.js 2 프로세스·snapshot — 원격 검증.
- 한계: 이상 없음.
