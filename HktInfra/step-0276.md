# step-0276 — #51b 실 zone.js 브리지 5: zoneRuntimeDrift 정합 질의

> 직전 step-0275 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `zoneRuntimeDrift()` 질의 추가: `running`(zoneId→host 문자열 추상 SSOT)과 `zoneRuntimes`(실 EntityZone 핸들 host)가 어긋난 존 수(host 불일치 또는 한쪽에만 존재). placeExecute+zoneBridge ON 이면 전 배치 op 뒤 0 — 추상 집행 SSOT 와 실 런타임 레지스트리가 한 몸. `placementDrift`(0245·결정↔집행)의 **실물 판**. 읽기 전용(쓰기 무변경). 계층: 코디네이션.
- OFF 플래그 `zoneBridge`(기존·읽기 질의이므로 OFF→0275 비트 동일). verify.js 새 모드 `zonedrift`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonedrift`**: 5/5 seed — start(z1·z2·z3)·migrate(z1→hostC)·hostdown(hostB)·stop(z3) 혼합 후 `zoneRuntimeDrift`=0·runtimeCount=2==runningCount(추상 문자열 SSOT↔실 핸들 일치).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 읽기 질의·존 tick 밖. ②결정론 — 순수 읽기. ③권위 단일 소유 — drift 0 이 한 존=한 host(추상·실 양쪽) 보장. ④은닉 — orch 내부 질의. ⑤headless — 실 EntityZone. 한계: drift 는 host *문자열* 일치만 검사(핸들 내부 상태 비교 아님) — 충분(브리지 불변=배치 위치 일치). 이상 없음.
