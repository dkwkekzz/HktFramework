# step-0358 — #57 실 host.js OS 프로세스 spawn 8: 실 child_process 존 인스턴스화 E2E

> 직전 step-0357 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `flush(cluster, specOf)` 확장: `specOf(zone)`→존 spec 가 주어지면 init 을 host.js 호환 `{cmd:'init',specs:[spec]}` 로 보낸다 → 실 host.js 자식 프로세스가 `makeActor` 로 그 존을 인스턴스화. specOf 없으면 `{cmd:'init',zone}`(mock·0357 호환).
- `src/verify.js` — async 모드 `hostspawnreal`: orch 드라이버가 산출한 spawnOne/init 명령을 *실 Cluster(cluster-core·child_process)* 에 flush → 실 host.js 프로세스 spawn + 존 z1 인스턴스화. (deliver 는 실 frame items 필요·후속이라 이 proof 는 spawn+init 만.)
- 계층: 코디네이션(⑤·SPINE §5 ⑤ headless·원격 — 실 OS 프로세스/소켓). OFF 플래그 `clusterDriverReal`(미설정→드라이버 null·실 spawn 0 = baseline 비트 동일). verify.js 새 모드 `hostspawnreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0357) 비트 동일.
- `hostspawnreal` 5/5 OK — z1@hostA: 드라이버 commands==[spawnOne:hostA,init:z1]·실 Cluster flush 후 `livePids`==1(실 host.js 자식 프로세스 가동)·snapshot 에 zone `z1` 인스턴스 존재(kind 'zone'). orch 논리 zoneHost 컨테이너 → 실 OS 프로세스 존 가동.
- `node run.js spine` = ALL OK (실 child_process spawn 포함·정리 shutdown).

## 척추 5항 + 한계

- ① tick: flush 는 tick 밖 제어 평면(실 spawn/init). ② 결정론: 5종 동일·zoneSpec 시드=zoneId 해시. ③ 권위: 단일 존 단일 host 프로세스. ④ 은닉: 직렬화 spec(소켓 와이어). ⑤ headless: 실 host.js 자식 프로세스·소켓·snapshot — 원격 검증 통과.
- 한계: host.js init 은 *full-reset*(per-host 단일 init) — 다중 존 incremental 가동은 host.js zonehost-add cmd 후속(0359). deliver/egress 실 집행도 후속. 이상 없음.
