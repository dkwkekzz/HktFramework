# step-0294 — #9 멀티프로세스 배선 4: 게이트웨이→실 존 직접 enter 라우팅

> 직전 step-0293 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/gateway-msg.js` — onMsg 에 클라 intent `zoneEnter` 분기: 게이트웨이가 자기 `zoneDir` 로 존 host 를 해소해 `zoneDeliver`(host 태깅)로 직접 라우팅(orch 데이터 평면 우회·라우팅 *결정*이 게이트웨이에). 디렉토리 미스면 드롭. 계층: 엣지(#9).
- 박스 `src/orch-control.js` — onMsg `zoneDeliver` 분기: 게이트웨이가 결정한 host 가 실 런타임 host(running)와 일치할 때만 `_bridgeEnter` 적용, 불일치면 stale 거부(orch/host 는 검증·적용만).
- 박스 `src/topo-inject.js` — `gatewayDirectZone` ON 이면 entityOps 를 게이트웨이로 라우팅(클라→게이트웨이→직접). OFF→0281 경로(→orch).
- 박스 `src/gateway.js`(`gatewayZoneRoutes`/`gatewayZoneMisses`)·`src/orchestrator.js`(`gatewayDirectZone` 플래그·`zoneDirectApplied`/`zoneDirStale`)·`src/topo-build.js`(opt 배선) 계측·배선.
- OFF 플래그 `gatewayDirectZone`(OFF→entityOps→orch·zoneDeliver 미수신 = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `gwdirenter`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdirenter` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdirenter`: 5/5 게이트웨이 직접 라우팅한 enter(a1·a2→z1·a3→z2)가 실 런타임 도달 — z1cnt 2·z2cnt 1·total 3·routes 3==applied 3==enters 3·stale 0·miss 0·단일 소유·entityConserved.
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 직접 라우팅은 코디네이션/엣지 데이터 평면(존 tick 밖). ②결정론: 라우팅은 디렉토리(배치 op 함수)·순수. ③권위 단일 소유: entitiesSingleOwner 유지·host 불일치 frame 은 거부(이중 적용 0). ④은닉: 게이트웨이는 zoneDir(서비스 디스커버리)로만 host 해소·orch 내부 무지. ⑤headless·원격: zoneDeliver 는 직렬화 frame·게이트웨이가 결정 후 host 로 송신(실 host.js 소켓 라우팅과 동형).
- 한계: zone-host 핸들이 아직 orch 에 산다 — 게이트웨이가 host 를 *태깅*해 orch 로 보내면 orch 가 적용(실 host.js 프로세스로의 *물리* 소켓은 #9 후속). enter 만 직접(move/leave 는 0295). 이주 중 stale 거부 정합은 0296. 이상 없음.
