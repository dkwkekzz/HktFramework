# step-0431 — #4 진짜 비동기 1: Lamport 논리 클럭 원시

> 직전 step-0430 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 신규 박스 `src/async-core.js` — #4(진짜 비동기·lockstep 배리어 해제)를 위한 *논리 클럭·인과 정렬 substrate* 의 첫 조각. `makeLamportClock(site)`: 단조 카운터 + `local()`/`send()`(이벤트마다 +1·스탬프 반환) + `recv(msgT)`(=max(local,msgT)+1). Lamport clock condition(a→b ⇒ C(a)<C(b))의 토대.
- 계층: 코디네이션(#4 — net.step lockstep 배리어를 인과 순서로 대체할 substrate). `net-core.js` 진입점에 `__p('async-core')` 1줄 등록.
- OFF 플래그: 없음(필요 없음) — async-core 는 run() 경로가 *호출하지 않는* 검증 전용 박스 → run() 비트 불변 = **reg 구조적 0**. verify.js 새 모드 `lcstamp`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK (async-core run() 미호출·net-core 는 export 1줄만 추가 → 비트 동일).
- `lcstamp` 5/5 OK — K(시드 의존 5..10) 로컬 이벤트 스탬프 1..K 단조 증가·최종 클럭==K (seed 42 K5·7 K9·1234 K5·99 K5·2026 K6).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK(전 시리즈 누적 회귀).

## 척추 5항 + 한계

①신성한 tick ✅(async-core 는 시뮬 tick 밖 substrate) ②결정론 ✅(Math.random 0·시드 PRNG) ③권위 단일 ✅(무관·클럭 원시) ④은닉 ✅(무관) ⑤headless ✅(순수 JS·검증 전용). 한계: 아직 단일 site 로컬 스탬프만 — send/recv 인과 규칙의 *교차-site* 적용은 0432, 전순서·holdback·수렴은 0433~. substrate 가 net.step 배리어를 실제로 치환하는 것은 이 in-proc arc 다음(후속 arc).
