# step-0406 — #62 runMulti 합류 5·복원력: 미러 입력 복제로 standby 동기 유지

> 직전 step-0405 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `tick()` 이 ⒜ deliver frame 을 `mirrors` 의 standby host 에도 재생 ⒝ standby 존을 shadow `tickZone`(발신/egress 폐기)해 standby 가 primary 와 lockstep 동기 유지. 0405 는 standby 를 *띄우고 사본*까지였고, runMulti(`cluster-run.js:170` 미러 deliver·`:198` 미러 tick)가 권위 입력을 standby 로 미러해 *계속* 동기를 유지하던 것을 zone cluster 상주 제어 평면에. failover 시 따뜻한 standby 즉시 승격 가능. 계층: 코디네이션(#62).
- OFF 동치: mirrors 비면 두 미러 루프 도달 0 → 0405 동치. verify.js 새 모드 `coordmirror`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 21.2KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmirror 5/5**: run(5)+reprovisionStandby(z1,hostA_s) → tick(6),tick(7)(미러 deliver+shadow tick) → standby z1 a1 {8,8} == primary a1 {8,8}(lockstep 동기)·coordDesync 0.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 미러 복제는 제어 평면 ②결정론: 시드 5종 동일 ③권위: standby=shadow(발신 0·이중 쓰기 0)·primary 권위 유지·입력만 복제 ④은닉: cluster.rpc API 만 ⑤headless: 실 host.js standby child_process E2E. — **한계(후속)**: standby *승격*(failover→standby 를 primary 로)은 미연결 — 현재는 동기 유지만(승격 경로는 capstone/후속). clusterInfo(0407)·runScenario(0408)·restartedCoherent(0409)·capstone(0410).
