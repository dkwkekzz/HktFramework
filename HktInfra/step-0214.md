# step-0214 — 월드 영속 정합 capstone(worldCrash/worldRecover·스냅샷 arc 닫기)

> 직전 step-0213 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/worldlog.js` — `worldCrash`(투영 RAM 소실)·`worldRecover`(스냅샷+tail replay) 를 *메시지 구동* 프로토콜로(슈퍼바이저가 복구 명령). crash 후에도 스냅샷·tail 로그는 durable → 동일 digest 복원.
- 계층: 데이터(계층6). OFF 플래그: `worldCrash`/`worldRecover` 미주입(미발화 → 0213 비트 동일·reg 0). verify.js 새 모드 `worldcrash`.
- 2차 고도화 월드영속 #2 — 스냅샷 arc(0207 append~0208 replay~0213 압축~0214) 닫기. 스냅샷이 load-bearing(접힌 역사를 짊어짐)임을 증명.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (worldCrash/worldRecover 미주입 → 0213 비트 동일).
- worldcrash: 5/5 — worldCrash→worldRecover 후 digest(0x4c635da6) == 전체-replay digest(무손실)·스냅샷 제거 시 digest(0xb6050b9b) 달라짐(load-bearing)·crash/recover 1/1.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 월드영속은 존 tick 밖·onTick 0. ②결정론: 스냅샷+tail replay 결정론(같은 로그→같은 digest). ③권위: intent 로그가 월드 상태 유일 쓰기 경로·복구는 그 재현. ④은닉: crash/recover 는 제어 평면 메시지(클라 접점 무변). ⑤headless: 전 cycle 이 op 구동·Node·원격 재현. — 이상 없음.
