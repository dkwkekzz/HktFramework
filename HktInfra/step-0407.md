# step-0407 — #62 runMulti 합류 6: clusterInfo() 재구성 보고

> 직전 step-0406 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `clusterInfo()`: runMulti 가 반환하던 clusterInfo(`cluster-run.js:227`·livePids·hostIds·placement·epoch·presumedDead·…)의 코디네이터 판. report()(0379·운영 desync 건강)와 직교 — clusterInfo 는 *runMulti 반환 계약*(실 cluster 토폴로지 livePids/hostIds + 코디네이터 생애주기 placement/epoch/presumedDead/migrations/failovers/restarts/reprovisions/lost/mirrors). 코드 합류 시 runMulti 가 이 보고를 그대로 반환 가능. 계층: 코디네이션(#62).
- 읽기 전용·새 메서드. verify.js 새 모드 `coordinfo`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 22.1KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordinfo 5/5**: run(5)+fence(hostB)+restart(z1,hostA_r) → clusterInfo: epoch 1·presumedDead⊇[hostB]·placement 3존·restarts 1·ticks 5·livePids 2.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 보고는 제어 평면 ②결정론: 시드 5종 동일 ③권위: 단일 placement+epoch 권위 스냅샷 노출 ④은닉: cluster.livePids/hostIds 공개 API 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: clusterInfo 는 runMulti clusterInfo 의 *부분집합*(존 cluster 범위 — full topology 의 partition/ipcBytes/socketClosed 등 전송 계측은 미포함). runScenario 통합 루프(0408)·restartedCoherent(0409)·capstone runMultiCoherent(0410).
