# step-0439 — #4 진짜 비동기 9: 인과 회계·exactly-once

> 직전 step-0438 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `accountDelivered(delivered, allEvents)`: 배달열 vs 전체 집합 대조 → `{emitted, applied, unique, dups, missing, complete}`. 수렴(0436)·배리어-free(0437)·손실 복원(0438)이 *맞게* 동작하려면 장부가 닫혀야 함 — 발신한 모든 이벤트가 결국 *정확히 한 번* 적용(손실 0·중복 0), 순열/손실 교란에도 다이제스트 불변.
- 계층: 코디네이션(#4). substrate 의 신뢰성을 *회계 보존*으로 단언 — 닫힌 장부의 논리 클럭 판.
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `asyncaccount`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `asyncaccount` 5/5 OK — 4 site·52 이벤트, 순열+~20% 손실 5 시행 전부 complete(applied==emitted)·dups 합 **0**·missing 합 **0**·손실 실제 발생(resyncs>0)·5 시행 다이제스트 전부 정전 전순서 불변(digInv Y).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(다이제스트 불변·교란 무관) ③권위 단일 ✅(각 이벤트 정확히 한 번 적용=중복 쓰기 0) ④은닉 ✅ ⑤headless ✅. 한계: 회계는 *재전송이 결국 도착* 가정 위(영구 손실 상한은 후속·다운스트림 egress 동형). 0440 grand capstone 이 0431~0439 를 한 시나리오로 묶어 닫는다.
