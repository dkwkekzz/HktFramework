# step-0447 — #4 실 net.step 배리어 치환 7: 배리어-free 독립 페이스 복제

> 직전 step-0446 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `driveAsyncReplicas(events, M, seed, avatars, nsites)`: M 복제가 receive(도착 버퍼)와 tick(진행 1보)을 분리(async-core.makeAsyncSite 재사용)하고 서로 다른 페이스(1·2·3보/라운드)로 굴러 → 진행 skew>0(비-lockstep). net.step 배리어의 본질(모든 참여자를 한 동기 tick 으로 묶음)을 해제해도, holdback 안정성은 페이스 무관 → 최종 실 월드 digest 는 진행 입도에 무관(전 복제 == canonical).
- 계층: 코디네이션(#4). 중앙 배리어 없이 독립 페이스 복제가 desync 0 수렴.
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netpace`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netpace` 5/5 OK — 4 client·40 intent·M=4 복제·페이스 1·2·3: 진행 skew 12~28(**비-lockstep** 실동작)·전 복제 == canonical Y·**desync 0**.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(페이스 무관 수렴) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅(검증 전용). 이상 없음 — 배리어(동기 tick) 제거 후에도 페이스 skew 무관 수렴. async-net 은 run() 밖 substrate.
