# step-0219 — 로그인 큐 수용량 백프레셔(loginCapacity·동접 상한)

> 직전 step-0218 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/loginqueue.js` — `loginCapacity{cap}` → 동시 입장 상한 설정. 이후 `_dequeue` 는 `admitted < cap` 일 때만 입장, 초과면 입장 보류(player 를 큐 맨 앞에 잔류·`rejectedByCapacity`). 월드 동접 상한을 엣지에서 강제(폭주 시 줄은 늘되 월드는 cap 이상 안 받는다). 미설정이면 capacity=∞.
- 계층: 엣지(계층1·로그인). OFF 플래그: `loginCapacity` 미주입(capacity=∞ → 0218 비트 동일·reg 0). verify.js 새 모드 `logincapacity`.
- 2차 고도화 로그인 큐 #1 — enqueue/dequeue·TTL(0209~0210) 위에 수용량 백프레셔.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (loginCapacity 미주입 → capacity=∞ → 0218 비트 동일).
- logincapacity: 5/5 — p1·p2·p3 enqueue→cap2→dequeue×3: p1·p2 입장(admitted 2)·p3 보류(queueLength 1·position 0)·rejectedByCapacity 1.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 로그인 큐는 존 tick 밖·순수 반응형(onTick 0). ②결정론: FIFO·시드 의사난수만. ③권위: 대기열·admitted 가 엣지 입장 SSOT(백프레셔는 입장 보류). ④은닉: 클라 접점=게이트웨이 경유(내부 무변). ⑤headless: Node·원격 재현. — 이상 없음.
