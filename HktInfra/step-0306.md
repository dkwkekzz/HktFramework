# step-0306 — #9 잔여(실 host.js 물리 분리) 6: host inbox stale 거부

> 직전 step-0305 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_tickRuntimes` host inbox drain 시 frame 의 zoneId 가 그 host 컨테이너(`c.zones`)에 *없으면*(enqueue 후 drain 전 그 존이 다른 host 로 이주) 적용하지 않고 거부(`zoneHostStale++`·drained 미증가). **실 host.js 분리의 핵심 안전망**: 프로세스가 자기가 잃은 존의 frame 을 적용하면 이주 후 두 host 가 같은 존을 건드리는 이중 쓰기 — `c.zones` 가 host 의 현재 소유 = 진짜 권위. 불변: recv == drained + stale.
- `src/orchestrator.js` — 카운터 `zoneHostStale`. `src/topo-inject.js` — `zoneHostStaleProbe`(같은 tick 으로 존 frame + 그 존 migrate 를 순서대로 보내 stale 창을 합성). 계층: 코디네이션(⑤). zoneHostProc OFF→경로 미발화 = 0305 비트 동일. verify.js 새 모드 `zonehoststale`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneHostProc OFF → baseline(0305) 비트 동일(정상 tick 거부 0).
- `zonehoststale` 5/5 OK — 격리 시나리오(z1·z2@hostA·a1 enter z1·tick10 z1 move frame+z1 migrate A→B): zoneHostStale 1(hostA 가 떠난 z1 frame 거부)·recv 2 == drained 1 + stale 1·z1 이제 @hostB·total 1·a1 z1 보존(이주 무손실)·single·drift 0.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: drain 은 tick 경계 배치(시뮬 외 0). ② 결정론: 소유 검사·거부 카운트만·시드 5종 동일. ③ **권위**: 떠난 존 frame 거부 = 이주 후 이중 쓰기 차단(권위 단일 소유의 프로세스 판). ④ 은닉: orch 내부. ⑤ headless: Node 순수·reg 0.
- 한계: 거부된 frame 은 *드롭*(게이트웨이 재라우팅 재전송은 별도·move 라 무손실이지만 enter 라면 게이트웨이 dir 갱신 후 재시도 필요·후속). probe 는 합성 stale(같은 tick frame+migrate)·by-construction. 이상 없음.
