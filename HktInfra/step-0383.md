# step-0383 — #65 양방향 동기 3: migrate 가 placement 갱신 (핵심 fix)

> 직전 step-0382 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `migrate(zone,from,to)` 가 실 cluster 이주 후 `this.placement[zone]=to` 로 *where 권위*를 갱신(한 줄). 이게 #65 핵심 fix: coordDesync 가 새 host(to)를 조회 → entity 발견 → **migrate 후도 desync 0**. 대조로 driver.clusterDesync 는 orch plan(stale·여전히 from)으로 from 조회 → 발산. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordmigsync`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmigsync 5/5**: run(3)+migrate z1 A→B → placedHost('z1')==hostB·**coordDesync 0**(placement 갱신→정확) vs **driver.clusterDesync 1**(orch plan stale·옛 #65 버그 재현 대조).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: placement 갱신은 tick 밖 ②결정론: 동일 ③권위: where 권위(placement)가 lifecycle 따라 갱신·entity 권위(orch)는 host-무관 → migrate 후 단일 소유 유지 ④은닉: cluster API 만 ⑤headless: 실 host 이주. — 이상 없음(coordCoherent 가 coordDesync 채택은 0384·failover 의 lost 처리는 0385~).
