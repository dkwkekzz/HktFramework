# step-0157 — 아이템 첨부 우편(mailItem — mailSend item·itemSent/itemHeld 회계)

> 직전 step-0156 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailSend` 가 선택 필드 `item`(아이템 id)을 받아 우편 1통이 아이템 1개를 함께 보유(거래소 escrow 의 우편 판). 카운터 `itemSent`(아이템 실은 입금)·accessor `itemHeld()`(보유 중 아이템). 저널/reconstruct/crash/digest 가 item 을 동봉(첨부 시만 digest 추가). 계층:게임 서비스.
- OFF 플래그 `mailItem`(기본 false) → item=null·itemSent 0·digest 무변경 = 0156 비트 동일(회귀 0). verify.js 새 모드 `exmlitem`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlitem`(h1 3통: 2통 아이템 첨부·1통 메시지만): sent=3·itemSent=2·itemHeld=2·mailConsistent(`예`)·crash→reconstruct 아이템 보존(`예`) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖(우편 박스) ②결정론 난수 0 ③권위 우편함 단일 소유(아이템도 우편에 묶임) ④은닉 우편 박스 내부 ⑤headless `node run.js` 통과 — 이상 없음. 아이템은 *우편 박스 내 회계*로만 추적 — 발신자 가방서 인출(give)·만료 시 가방 반환 등 가방(inventory) 연동은 백로그(거래소↔가방 0117~0120 류 2-서비스 보존이 다음 자연 확장). 수령 이동(0158)·만료 회수(0159)·아이템 회계 capstone(0160) 후속.
