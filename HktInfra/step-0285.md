# step-0285 — #56 브리지 존 데이터 평면 5: hostdown 소실(정직한 한계·loss accounting)

> 직전 step-0284 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` `_bridgeHostDown` — 새 인스턴스 교체 *전* 죽은 인스턴스의 `ents.size` 를 `zoneEntitiesLost` 에 더한다(host 장애는 graceful 이주 불가 → entity 소실·정직한 계측). migrate(같은 핸들·무손실)와 결정적 대조. `src/orchestrator.js` — `zoneEntitiesLost` 계측. 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0284 비트 동일=회귀 0). verify.js 새 모드 `zonehostdownent`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zonehostdownent` 5/5: z1@hostA(a1·a2·a3)·z2@hostB(b1·b2) → hostA 장애 → `z1cnt=0`(새 빈 인스턴스)·`z2cnt=2`(무사)·`zoneEntitiesLost=3`·`zoneRescued=1`·`total=2`·`runtimeOn(hostA)=0`.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: hostdown 복구는 orch 제어 평면·존 tick 밖. ②결정론: 소실 계측 결정론·`Math.random` 0. ③권위 단일 소유: 죽은 host release 불가→생존 host 재가동(공백 없이 단일 소유 회복). ④은닉: 게이트웨이는 placeHostDown 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 정직한 한계: host 장애 시 그 인스턴스의 entity 는 *소실*된다(zoneEntitiesLost 로 가시화). 잃은 상태의 복구(영속서 재구성·intent replay)는 데이터 계층 후속(#56 범위 밖·범위는 전파 무손실 vs 소실 구분까지). migrate(무손실)와 hostdown(소실)의 의미 차이가 이제 데이터 평면 수치로 확정.
