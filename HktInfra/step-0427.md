# step-0427 — #61 업스트림 intent 실 클라 7: UpClient leave (zoneLeave 발신)

> 직전 step-0426 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/client.js` — `UpClient` 에 `leaveAt` 추가: onTick 이 leaveAt(절대 tick)에 `zoneLeave` 발신·이후 발신 0(`left`). 실 클라 접속 생애주기 완결(enter→move→leave).
- 계층: 엣지/월드(#61).
- OFF 플래그: upClients=null(기본)→스폰 0→회귀 0.
- verify.js 새 모드: `upleave`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK.
- 가설 `upleave`: 5/5 — uc0 sent 4(enter+2move+leave)→leaveAt(8) 후 권위 존서 a1 제거(zoneEntityPos null).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유(leave→제거) ④은닉 ⑤headless — 이상 없음.
