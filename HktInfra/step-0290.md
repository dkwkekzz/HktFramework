# step-0290 — #56 브리지 존 데이터 평면 10·capstone: 전 데이터 평면 정합 + 보존 회계

> 직전 step-0289 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `entityFlowCoherent()` = `fullyCoherent()`(placement==running==zoneRuntimes 3층·0280) && `entityCoherent()`(단일 소유+orphan 0·0288). `entityConserved()` 보존 항등식: `totalEntities() == zoneEnters − zoneLeaves − zoneEntitiesLost − zoneEntitiesDiscarded`(graceful op 은 무손실이라 항 밖). #56 arc(0281~0290) 닫는 capstone. 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0289 비트 동일=회귀 0·읽기 전용). verify.js 새 모드 `zoneflowcap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zoneflowcap` 5/5: 혼합 lifecycle(enter5·move·leave1·hostdownC(소실2)·stop z2(폐기1)·migrate·rebalance·drain) 후 `flow=Y`·`consv=Y`·`total=1`(==5−1−2−1)·ledger `5/1/2/1`·`migs=2`. 전 op 발화.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 전 op orch 제어 평면·존 tick 밖. ②결정론: `Math.random` 0·시드 무관 동일 결과. ③권위 단일 소유: entityFlowCoherent 가 host 3층 단일성 + entity 단일 소유를 한 술어로 묶음. ④은닉: 게이트웨이는 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 한계: 데이터 평면 정합·보존이 정상 op 에선 by-construction 참(graceful=같은 핸들·destructive=동시 정리) — 진짜 분기(부분 실패·경합)는 #9 실 프로세스에서 load-bearing. 게이트웨이→실 존 *직접* 라우팅(현재는 orch 경유)도 #9 후속. #56(브리지 존 entity 트래픽·migrate 무손실 vacuous)은 이 arc 로 해소.
