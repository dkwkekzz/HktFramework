# step-0494 — svcmailxfer 우편↔가방 2-서비스 교차+liveness 편입 (#16 라운드 3차 4)

> 직전 step-0493 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 우편↔가방 2-서비스 교차 회계 + saga liveness capstone(옛 0164/0180 판)을 재작성 편입: 새 mode `svcmailxfer` — 우편 `giveOks` == 가방 `escrowXfers`(두 서비스 escrow 회계 합치) + `sagaLivenessConsistent`(pending 3분할: pendingGive+abandonedGive+permFailed) + 아이템 무손실 단일 소유. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 4/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcmailxfer`: 5/5 OK — giveOks 2 == escrowXfers 2(두 서비스 합치)·pending 0(정상 drain)·sagaLivenessConsistent(3분할 닫힘)·item0 bob 소유.
- spine ALL OK — 우편 saga 2종(svcmailcap+svcmailxfer) 서비스 계층 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 교차+liveness 대조·Math.random 0. ③권위: 두 서비스 escrow 회계 합치가 아이템 단일 소유·pending 3분할 무공백 재단언. ④은닉: give 경유. ⑤headless: node in-proc. — 이상 없음(정상 흐름서 pending 0·3분할 술어는 loss 체제서도 성립하도록 설계됨).
