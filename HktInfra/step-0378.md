# step-0378 — #62 runMulti 통합 8: 다운스트림 egress 집계

> 직전 step-0377 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — tick 이 산출한 view_delta frame 을 존별(`egressByZone`)·누계(`egressTotal`)로 회계. broker 측 제어 평면이 매 tick 흘려보낸 다운스트림 뷰의 운영 계측(어느 존이 얼마나 송출하나). 새 필드 2개. 계층: 코디네이션(#62).
- verify.js 새 모드 `coordegress`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordegress 5/5**: run(5)→egressTotal 2·z1 1·z2 1·z3 0(빈 존 미송출)·egressTotal==run() 반환==z1+z2+z3·clusterCoherent Y.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 회계는 tick 산출 부산물(시뮬 외 작업 무) ②결정론: 같은 입력→같은 egress 분포 ③권위: 송출은 권위 존 산출 ④은닉: cluster API 만 ⑤headless: 실 host zone view_delta. — 이상 없음(이동 entity 존만 송출·정적 수렴 후 추가 송출 0).
