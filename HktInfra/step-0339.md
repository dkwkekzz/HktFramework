# step-0339 — #9 후속: 다운스트림 leave 정리(stale 바인딩/버퍼 회수)

> 직전 step-0338 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway.js` `_downCleanup(sid)` — leave 시 그 세션의 다운스트림 상태(`downClients`·`downSeqNext`·`downResyncPending`·`zoneViewIn`) 일괄 제거. `src/gateway-msg.js` zoneLeave 핸들러에서 호출. 질의 `gatewayCleanedCount`·계측 `downCleaned`.
- `src/orch-zonebridge.js` `_bridgeLeave` — 그 세션의 egress 상태(`zoneEgressBuf`·`zoneEgressSeq`·`zoneEgressAcked`) 제거(게이트웨이 정리의 orch 짝·무계 성장 방지). 0334 한계(stale 바인딩 누적) 해소.
- 계층: 엣지/코디네이션(④⑤). 플래그 없음 — egress OFF 면 정리 맵 빈 채라 delete no-op·맵은 egress 경로 외 미참조 → baseline 비트 동일. verify.js 새 모드 `gwleave`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — egress OFF → 정리 대상 맵 빈 채·미참조 → baseline(0338) 비트 동일.
- `gwleave` 5/5 OK — a1 leave 후: `gatewayCleanedCount` **1**·`downClientOf('s:a1')` **null**·orch `zoneEgressBufLen('s:a1')` **0**·a2 보존(`downClientOf('s:a2')`=dc1·영향 격리).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 정리는 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 정리가 권위 무영향. ④ 은닉: 게이트웨이/orch 각자 자기 상태 정리(명시 메시지 경유). ⑤ headless: Node 순수·reg 0.
- 한계: disconnect(전체 종료) 경로의 다운스트림 정리는 zoneLeave 경유만(전용 disconnect 훅은 후속). 이상 없음.
