# step-0342 — #9 후속: 실 다운스트림 클라 수렴(host AOI == 클라 뷰 desync 0)

> 직전 step-0341 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/client.js` — 수신 전용 `DownClient` 액터(view/view_delta 를 받아 자기 `seen` Map 으로 적용·발신 0·은닉). 0331~0341 전파 종단(spectator addr·0334 한계)을 *실 클라*로 교체.
- `src/topo-actors.js`(case 'downclient')·`src/topo-build.js`(`downClients` opt→dc0.. 스폰)·`src/topo-run.js`(`r.downclients` 수집) 배선.
- 계층: 엣지/월드(④⑤·SPINE §4 경로2 종단 = 실 클라 수렴). 플래그 없음 — `downClients` 0(기본)이면 스폰 0 → baseline 비트 동일. verify.js 새 모드 `dcconv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — downClients 0 → DownClient 스폰 0 → baseline(0341) 비트 동일.
- `dcconv` 5/5 OK — a1@dc0·a2@dc1 입장·이동 뒤: `dc0.seenIds` **[a1]** == `zoneVisibleIds('z1','a1')` [a1]·`dc1.seenIds` **[a2]** == zoneVisibleIds('z1','a2') [a2](host 권위 AOI == 클라 뷰·**desync 0 수렴**).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 클라 수신은 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 클라는 읽기전용 뷰만(권위 무영향). ④ 은닉: 클라는 게이트웨이만 안다(존/host 무지). ⑤ headless: Node 순수·reg 0.
- 한계: DownClient 는 인프로세스 액터(실 OS 프로세스/소켓 spawn 은 후속). 정적 AOI 수렴 — 증분/이동/손실 하 수렴은 후속(0343+). 이상 없음.
