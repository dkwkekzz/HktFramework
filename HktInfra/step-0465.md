# step-0465 — 유계 resync 증명(deferSpan < horizon)

> 직전 step-0464 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/async-barrier.js` — 유계 resync 회계 노출. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`. verify.js 새 모드 `mzbound`.
- `deferSpan` = 재배달 tick − defer tick (loss=resyncDelay 상수·delay=1..delayMax). `maxSpan`(관측 최대)·`deferN`(총 defer 수)·`horizon`(=max(resyncDelay,delayMax)+1) 을 stats 에 노출.
- 명제: `maxSpan < horizon` → interior(엔티티가 region 양 끝에서 horizon 이상)인 move 만 defer 하므로, 재배달까지 엔티티가 이주 경계에 닿을 수 없다 = **이주 전 유계 resync**. barrier ON 경로만 변경(회계) → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → net.step·baseline 비트 동일).
- `mzbound` 5/5 OK — deferN 44~103·maxSpan **3** < horizon **4**(span<horizon Y 전 시드)·world==lockstep **Y**.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: 유계 span 이 lockstep 재현 보장 ③권위 단일 보존 ④은닉 존 읽기 peek ⑤headless 이상 없음.
- 발견: maxSpan=3(=delayMax)·horizon=4 로 항상 strict 미만 — 유계성이 구성적(by-construction)으로 성립(resyncDelay=2·delayMax=3 둘 다 horizon 미만).
