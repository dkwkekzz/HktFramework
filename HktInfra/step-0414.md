# step-0414 — #62 코드 합류 4: runMultiViaCoord info 의 옛 runMulti clusterInfo parity

> 직전 step-0413 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `runMultiViaCoord` 결과 `info` 에 옛 runMulti `clusterInfo`(cluster-run.js:227~)의 전송/토폴로지 필드를 병합: `pids·parentPid·port·ipcMsgs/ipcBytes/ipcMsgsIn/ipcBytesIn·allSerializable·wire`. 코디네이터 생애주기 info(livePids/placement/epoch/…) 위에 실 cluster 계측을 얹어 runMulti 반환 계약과 parity.
- 계층: 코디네이션(#62 코드 합류 — 합류 후 runMulti 가 그대로 반환할 수 있는 계약 완비).
- OFF 플래그: 미부착(verify 만 호출·결과 객체에만 작용) → 회귀 0.
- verify.js 새 모드: `coordinfoparity`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일).
- 가설 `coordinfoparity`: 5/5 — info 가 runMulti 키 집합 14종 완비·pids 3(2 host + standby)·ipcMsgs 66(>0)·parentPid==process.pid·allSerializable Y.
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 이상 없음(결과 집계만·시뮬 비-침습).
