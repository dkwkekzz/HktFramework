# step-0461 — 다중 존 loss+delay 발산 포착(negative control·#72 고정)

> 직전 step-0460 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 코드(박스) 무변경 — `src/verify.js` 셸에 새 모드 `mzdiverge` 만 추가(negative control). 계층: 코디네이션(#4 완전 async 전환 arc 0461~ 시작).
- 0451~0460 은 loss+delay 를 *단일 존*에서만 world==lockstep 로 흡수했다(가환·무이주). 이 모드는 그 barrier(무-가드)를 **다중 존(grid24·zones2)** 에서 loss+delay 로 돌려 **발산(world != lockstep)** 을 수치로 고정한다.
- 원인: lockstep(OFF)은 *이주 후 stale 존에 도착한 move 를 폐기*(`zone.js` move 핸들러 `if(this.ents.has(avatar))`)한다. 지연/손실이 이주 타이밍을 바꾸면 이 move-drop 집합이 lockstep 과 달라져 최종 월드가 어긋난다 = #72 격차.
- OFF 플래그: `asyncBarrier`(미설정→net.step()·baseline 비트 동일). barrier 코드 무변경 → reg 구조적 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (src box == baseline 비트 동일 — 코드 무변경).
- `mzdiverge` 5/5 OK — 다중 존 loss+delay: handoffs 4~15(실 이주)·resync 73~97·delay 94~126·world==lockstep **N(발산)**. negative control 이므로 *발산해야* 통과(가드 부재 확증).
- `spine` = ALL OK (전 역사 불변 현재 코드에 단언).

## 척추 5항 + 한계

- ①tick 이상 없음(월드 입력 배달만) ②결정론: 발산은 *가드 부재*의 결정론적 결과(같은 시드 재현) ③권위 단일: 이주 release+acquire 불변 ④은닉 이상 없음 ⑤headless 이상 없음.
- 한계(의도된 것): 이 step 은 해소가 아니라 *발산 고정*이다 — 0462~ 가 wrap-aware interior 유계 resync 가드로 해소한다. 발견: 발산 원인은 재정렬이 아니라 lockstep 의 이주 경계 move-drop 집합 변동.
