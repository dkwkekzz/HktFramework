# step-0181 — 길드(Guild) 서비스 분리(guildService·GuildService)

> 직전 step-0180 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/svc-guild.js`(GuildService) — 길드 로스터+마스터십 SSOT. `guildCreate{guildId,master,members}`→로스터 쓰기(`_normalize`: master 항상 멤버에 포함·중복 제거)·`guildQuery`→`guildRoster` 회신(request/reply·SPINE §4 경로3). 존 tick 밖 *순수 반응형*(onTick 없음·권위=로스터/마스터). single-master 불변(매 길드 정확히 한 master·master∈members). 계층: **3 게임 서비스**(SPINE §6 길드 ⬜→착수). 파티 0075 의 *영속 조직* 판.
- 배선: `net-core.js`(`__p('svc-guild')` 진입점)·`topo-actors.js`(import+`case 'guild'`)·`topo-build.js`(`guildService` 플래그·`if (guildService) add('guild')`)·`topo-run.js`(`guild` 노출·`guildOps` 주입 seam·return).
- OFF 플래그 `guildService`(기본 false → 박스 0 = 0180 비트 동일=회귀 0) · verify.js 새 모드 `guild`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildService 미설정 → 박스 0 → net.log+상태 0180 비트 동일.
- **가설 `guild`**: 5/5 시드 OK — 길드 2개 결성(g1 master x/멤버 3·g2 master c3/멤버 2)·single-master 불변 예·질의 2/회신 2·회신==SSOT·결정론(전 시드 동일 형상).
- **spine**: ALL OK(전 시리즈 누적 회귀·아래 닫기서 확인).

## 척추 5항 + 한계

①신성한 tick: 길드는 존 tick 밖 비동기 서비스(onTick 없음·로스터는 오래 사는 상태). ②결정론: 시드 의사난수만·전 시드 동일 형상. ③권위 단일 소유: single-master 불변(매 길드 정확히 한 master·master∈members). ④은닉: 소비자는 질의(request/reply)로만 로스터 소비·저장 방식 무지. ⑤headless·원격: Node·`node run.js` 수치 증명. — 이상 없음.
