# step-0374 — #62 runMulti 통합 4: 매-tick desync 가드

> 직전 step-0373 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — run 루프가 매 tick 끝에 `driver.clusterDesync(orch,cluster)` 를 측정해 `maxDesync`(루프 최악)에 누적. 정합이 *끝*뿐 아니라 *매 tick 내내* 유지됨을 단언(중간 발산 검출). 새 필드 `maxDesync`. 계층: 코디네이션(#62).
- verify.js 새 모드 `coorddesync`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coorddesync 5/5**: 정상 run(5)→maxDesync 0(5 tick 내내 수렴)·ghost entity('ghostX') 주입 시 clusterDesync 1(가드 지표가 발산 검출·0369 동형).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 가드 측정은 tick 밖 제어 평면 ②결정론: 정상 경로 maxDesync 0 ③권위: ghost(권위 모름) 주입이 desync>0 으로 잡힘=단일 소유 가드 건전 ④은닉: driver/cluster API 만 ⑤headless: 실 host snapshot 측정. — 이상 없음.
