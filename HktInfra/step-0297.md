# step-0297 — #9 멀티프로세스 배선 7: host 장애 dir 무효화 + 직접 라우팅 복구

> 직전 step-0296 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orch-control.js` — `placeHostDown` 처리 뒤 게이트웨이에 `hostDown` broadcast(장애 검출 신호). `_bridgeHostDown` 의 survivor zoneLoc 재push 가 *먼저* 도착하므로 구조 존은 새 host 로 갱신·미구조 존만 삭제 잔류. 계층: 코디네이션(#9).
- 박스 `src/gateway-msg.js` — onMsg 의 orch 블록에 `hostDown` 분기: 죽은 host 의 모든 dir 엔트리 일괄 삭제(게이트웨이가 죽은 host 로 직접 라우팅 안 함).
- 박스 `src/gateway.js`(`gatewayHostInvalidated`)·`src/orchestrator.js`(`hostDownBroadcasts`) 계측.
- OFF 플래그 `gatewayZoneDir`(OFF→broadcast 0 = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `gwdirdown`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdirdown` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdirdown`: 5/5 hostA 장애→z1 survivor(hostC) 재가동(a1 소실·새 인스턴스)→직접 enter a3 가 갱신 dir 로 hostC 도달 — z1{a3}·z2{a2}·total 2·runtimeHost hostC·runtimeOn(hostA) 0·dir==running·zoneEntitiesLost 1·gatewayHostInvalidated 1·entityConserved(2==3−1).
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 장애 검출/dir 무효화는 코디네이션·엣지(존 tick 밖). ②결정론: 장애·재가동·무효화 전부 op 함수·순수. ③권위 단일 소유: 재가동 존은 survivor 단일 host·죽은 host dir 삭제로 이중 라우팅 0. ④은닉: 게이트웨이는 hostDown/zoneLoc 명시 신호로만 갱신. ⑤headless·원격: hostDown broadcast 는 실 클러스터 장애 검출(gossip/health) 동형.
- 한계: entity 소실(a1)은 정직한 한계 그대로(영속 복구는 데이터 계층 후속·0285 와 동일). hostDown broadcast 는 net.send(1-tick) — survivor 재push 와 같은 tick 발신·순서로 정합. zone-host 는 여전히 orch 보유(실 host.js 후속). 이상 없음.
