# step-0474 — 실 host.js child 경계 업스트림 4: 경계 넘어 수렴 desync 0

> 직전 step-0473 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — `upstreamAuthSig(cluster, host, zone)` 추가. 계층: 월드(#70·SPINE §5 수렴). 드라이버 미부착→run() reg 0. verify.js 새 모드 `upcconverge`.
- 실 host.js 존의 권위 AOI 서명(snapshot·`id@x,y` 정렬·UpClient.seenSig 형식) 노출. 실 UpClient 가 발신(enter+move)→경계 넘어 실 존 적용→egress 뷰 수신 뒤 `seenSig()==authSig`(desync 0) 단언 — 발신→권위 반영→뷰 수렴이 *실 프로세스 경계*를 넘어 성립(#61 in-proc upconverge 의 실 OS 프로세스 판).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향).
- `upcconverge` 5/5 OK — UpClient.seenSig == 실 존 authSig 전 시드(예: seed42 a1@1,6·seed2026 a1@14,0)·desync 0.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 실 존 tick 만 상태 변경 ②결정론: 발신 intent→결정론 존 반영 ③권위 단일: 실 host.js 존 유일 소유 ④은닉: 발신·수신 모두 게이트웨이-형 경유 ⑤headless: 실 child_process·경계 넘어 수렴.
- 한계: 이상 없음(단일 클라·단일 존). 다중 tick 구동 통합은 0475(driveUpstream).
