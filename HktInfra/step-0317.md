# step-0317 — #9 잔여: entity 가중 부하 재배치 placeRebalanceE

> 직전 step-0316 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-control.js`·`src/orch-hostproc.js`·`src/orchestrator.js` — 새 op `placeRebalanceE {hosts[]}` = `_rebalanceByEntities`: entity 부하 가장 무거운 host 의 한 존을 가장 가벼운 host 로 이주(release+acquire), entity gap(max−min) < 2 까지 반복. placeRebalance(0223·*존 수*)의 entity 가중 판. **진동 방지**: gap 을 strict 하게 줄이는 존만 골라 이주 → gap 단조 감소 = 종료 보장. placement+running 함께 갱신(drift 0). 계측 `rebalanceEMoves`.
- 계층: 코디네이션(⑤). placeRebalanceE 미수신이면 미발화 → 0316 비트 동일(새 op·플래그 불요). verify.js 새 모드 `hostrebalanceentity`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — placeRebalanceE 미주입 → baseline(0316) 비트 동일.
- `hostrebalanceentity` 5/5 OK — z1·z2@A(각 entity 3 → A 부하 6)·z3@C(0)·gap 6: `placeRebalanceE([A,C])` 로 z1(3명)→C → A={z2}(3)·C={z1,z3}(3)·entity skew **6→0**·rebalanceEMoves 1·총 entity **6 보존**(같은 핸들 이주·무손실)·entityConserved Y·hostContainerCoherent Y·running 3.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 재배치 결정(시뮬 외·비동기 코디네이션). ② 결정론: 시드 5종 동일·gap 단조 종료. ③ 권위: migrate=release+acquire 쌍·단일 소유·무손실. ④ 은닉: orch 내부 부하 질의. ⑤ headless: Node 순수·reg 0.
- 한계: 단일 pass 휴리스틱(전역 최적 아님)·자동 트리거(임계 초과 시 자동 발화)는 placeRebalanceE 명시 호출에 의존(자동화 후속). 이상 없음.
