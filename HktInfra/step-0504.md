# step-0504 — mailsagaabandonpub 포기 발행 E2E 관측 (#16 라운드 4차 4)

> 직전 step-0503 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagaabandonpub`: 포기 발행 `mailAbandonPublish`+audit 구독. 포기 시 `svc.mail.saga_abandoned` 1회 발행 → 버스 → audit 수신. `abandonPublished==giveAbandoned`(1:1) AND `audit.seen('svc.mail.saga_abandoned')==giveAbandoned`(버스 경유 실제 도달). 운영 가시화가 은닉 통신(발행→버스→감사)으로 닿음을 E2E 단언. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 4/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagaabandonpub`: 5/5 OK — giveAbandoned 1·abandonPublished 1·auditSeen 1·sagaConsistent+liveness Y. 발행 수 == 포기 수 == 감사 수신 수(누락·중복 0).
- spine ALL OK — 손실 체제 4모드 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 발행/수신 5시드 동일. ③권위: 포기 발행은 관측일 뿐 상태 변경 아님. ④은닉: 우편→버스→audit 토픽 pub/sub(직접 결합 0·audit 는 발행자 무수정 신규 소비자). ⑤headless: node in-proc. — 이상 없음(발행→버스→감사 도달 = SPINE §4 서버간 이벤트 버스 경로 준수).
