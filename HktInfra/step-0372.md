# step-0372 — #62 runMulti 통합 2: ClusterCoordinator.tick (제어 평면 데이터 평면 1 tick)

> 직전 step-0371 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `tick(t)` 추가: ① pending entity frame 재생(driver.commands 의 deliver frame→실 host zone.onMsg·첫 tick 에 enter/move 적용·이후 빈 큐) ② 전 존 1 tick(pending move 적용 + AOI view_delta egress 산출). driveCluster(0368)의 per-tick 몸통을 코디네이터 상주 메서드로 — 연속 루프(0373)의 단위. 계층: 코디네이션(#62).
- verify.js 새 모드 `coordtick`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordtick 5/5**: start()+tick(1)→views 2(z1·z2 각 1 view_delta)·clusterCoherent Y(전 entity 실 host==in-proc 권위·desync 0)·coord.ticks==1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 제어 평면 데이터 평면 tick 은 존 시뮬 tick 의 실 host.js 집행(시뮬 외 작업 미혼입) ②결정론: 같은 frame 재생·plan.order 정렬→desync 0 ③권위: in-proc orch 가 SSOT·실 host 가 재현 ④은닉: driver/cluster API 만 ⑤headless: 실 host.js child_process zone.onTick. — 이상 없음.
