# step-0442 — #4 실 net.step 배리어 치환 2: 실 sim seam fold — 수렴 다이제스트

> 직전 step-0441 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `simFold(orderedEvents, seed, avatars)`: 배달 순서대로 **동결 sim seam DummySimCore**(engine·`ISimCore`)에 intent 를 순차 tick 으로 fold → 실 월드 `serialize()`·digest. async-core 의 *추상* applyDigest 를 *실 월드 상태* fold 로 승격. 순서 민감(DummySimCore.counter 가 매 tick 위치 누적 해시 → 적용 순서 다르면 다른 serialize).
- 계층: 코디네이션(#4). 실 net.step 배리어 치환 = 결국 실 월드 상태 수렴 → sim seam 을 substrate 에 연결.
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netsimfold`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netsimfold` 5/5 OK — 4 client·40 intent·K=6 순열: totalOrder(0433) 정규화 fold **도착 순열 불변**(수렴 다이제스트) Y·결정론 Y·**raw 도착 fold 갈림** Y(substrate 가 load-bearing — 정규화 없으면 desync). 즉 배리어 대신 substrate 로 수렴.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(동결 sim·시드) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅(순수 JS·검증 전용). 한계(정직): fold 는 아직 *배달열 소비*만 — holdback 재정렬·실 actor.onMsg 디스패치·손실 resync 는 후속(0443~). raw 갈림은 seed 5종 전부 관측(순서 민감 확증). async-net 은 run() 밖 substrate.
