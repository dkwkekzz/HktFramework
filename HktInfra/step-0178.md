# step-0178 — 아이템 우편 saga 재admission 횟수 상한(mailReadmitMax)

> 직전 step-0177 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`: `_resendPending` 포기 분기에 readmitMax 검사 — gid 가 `readmitMax` 회 재admission 된 뒤 또 포기되면 *영구 실패*(permFailed++)로 abandonedGive 에 안 넣어 재admission 차단(무한 abandon↔readmit 루프 방지). `_readmit` 가 readmitCount 누적. 계층: 게임 서비스(우편)·서비스간 saga.
- 필드: `readmitMax`/`readmitCount`/`permFailed`. `svc-mail-persist.js` crash(): readmitCount/permFailed 소실. `src/topo-build.js`: `mailReadmitMax` 기본 + 박스 opts 배선(audit 구독 불요).
- OFF 게이트 `mailReadmitMax=0`(무제한 = 0177 비트 동일). verify.js 새 모드 `mailreadmitmax`. 거래소 0137·0059 recoverMaxRetries 의 우편 판.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: readmitMax 0 = 0177 비트 동일(전 시스템 5시드) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailreadmitmax`**: 5시드 전부 — 반복 abandon↔readmit 서 무제한 readmitted/permFailed=2/0·상한1 1/1(1회 재admission 후 영구 실패·차단)·양 체제 sagaConsistent 예.
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖 — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경(재전송 멱등). ③권위 단일 소유: 영구 실패도 pending 잔존(미해결 보존)·원장 권위 불변. ④은닉·단일 연결: 클라 와이어 무변경(saga 서비스간). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (다음: saga 영구 실패 발행 mailFailPublish·거래소 0138 의 우편 판 — saga liveness 발행 종결 마디.)
