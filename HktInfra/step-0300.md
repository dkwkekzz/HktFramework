# step-0300 — #9 멀티프로세스 배선 10·capstone: 전 데이터 평면 게이트웨이 직접 라우팅

> 직전 step-0299 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orch-zonebridge.js` — `directFlowCoherent()` capstone 질의: `entityFlowCoherent()`(placement==running==zoneRuntimes 3층 + entity 단일 소유/orphan 0·0290) **AND** `entityDirectCoherent()`(직접 라우팅 정합 + stale 누수 0·0298). 참이면 "배치 3층 정합 + entity 정확히 한 존 + 게이트웨이 직접 라우팅 전부 옳게 도달". 계층: 코디네이션(#9). 읽기 전용.
- OFF 경로 무관(질의만·기존 플래그 OFF→이전 비트 동일 = 회귀 0). verify.js 새 모드 `gwdircap`. **#9 멀티프로세스 배선 arc 0291~0300 닫기.**

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdircap` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdircap`: 5/5 0290 와 같은 destructive+graceful 혼합 lifecycle(place×3·hostdown·stop·migrate·rebalance·drain + enter5·move·leave)을 **게이트웨이 직접 라우팅만으로**(seam+mailbox+직접 라우팅 전부 ON) — directFlowCoherent·entityConserved(total 1==5−1−2−1)·dir==running(bijection)·ledger 5/1/2/1·gatewayZoneRoutes 7==zoneDirectApplied 7·zoneDirStale 0·gatewayZoneMisses 0.
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 전 라우팅·디렉토리·seam·mailbox 는 코디네이션·엣지(존 tick 밖). ②결정론: 전 데이터 평면 op·직렬화·라우팅·보존 회계 순수(시드 함수). ③권위 단일 소유: destructive+graceful 혼합 churn 아래 entitiesSingleOwner·zone 당 단일 host·dir 단일 엔트리·stale 거부. ④은닉: 게이트웨이는 zoneDir(서비스 디스커버리)로만 해소→직접 라우팅·orch 내부 무지·dir==running bijection. ⑤headless·원격: 전 데이터 평면이 직렬화 frame·게이트웨이 직접 라우팅·소켓 수신 버퍼·서비스 디스커버리 — 실 host.js 프로세스 분리의 완전한 씨앗.
- 한계(#9 잔여): zone-host 핸들이 여전히 orch 인프로세스 Map(zoneRuntimes) 에 산다 — 게이트웨이가 host 를 *태깅*해 orch 로 보내면 orch 가 적용하는 형태(실 host.js *물리* 프로세스/소켓 분리는 다음 arc·cluster-run.js 통합). entity 소실(hostdown)은 정직한 한계(영속 복구는 데이터 계층). #9 는 *직접 라우팅 계약·서비스 디스커버리·전송 seam* 을 세웠고, 물리 프로세스 분리만 남는다.
