# step-0245 — 배치 SSOT 실배선(#51) 5: placement↔running reconcile capstone

> 직전 step-0244 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placementDrift()` 읽기 질의: 결정(`placement`)과 집행(`running`)이 어긋난 존 수(host 불일치/한쪽만 존재). placeExecute ON 이면 모든 배치 op(place/migrate/rebalance/drain) 뒤 drift 0(advisory paper 표류 없음)을 단언 — advisory→executed arc 닫기. 쓰기 무변경(질의 1개). 계층:코디네이션.
- OFF 플래그 `placeExecute`(OFF→running 빈 채·질의 호출 0=0244 비트 동일=회귀 0).
- verify.js 새 모드 `placereconcile`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placereconcile`**: 5시드 모두 drift 0·running 4/placed 4·드레인한 hostB running 0·A2/B0/C2(합 4) — 혼합 op(place+migrate+rebalance+drain) 후 결정==집행.
- **결정론/권위/수렴**: `run.js all` ALL OK(15모드+classics).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면 ②결정론: 시드 PRNG ③권위 단일 소유: 매 op executed release+acquire·drift 0 으로 결정==집행 확증 ④은닉: orch 내부 ⑤headless: node 검증. — 이상 없음. 한계: running 은 아직 orch 내 *추상 레지스트리*(실 EntityZone host 이주 아님)·executed placeStop(존 런타임 종료)·placeAuto(부하 기반 실 가동)는 후속(0246~).
