# STATE — 살아있는 현재

> "지금 어디까지 왔고 다음은 무엇인가"의 **단일 진실 원천(SSOT)**.
> 큰 목표·규칙은 [CLAUDE.md](CLAUDE.md) · 척추(인프라 큰 그림)는 [SPINE.md](SPINE.md) · 각 step 상세 기록은 `step-NNNN.md`.
>
> **구조 규칙(에이전트 효율)**: 이 파일은 *고정 크기 대시보드*다. §1~6은 step을 닫을 때마다 **덮어쓴다(rewrite)** — 누적하지 않는다. 오직 §7 INDEX만 1행 append. 발견/한계의 *전문*은 STATE가 아니라 `step-NNNN.md`에 산다. 마커: ✅해소 🟡부분 🔴열림(최우선) ⬜백로그 🔧노브.

---

## 1. NOW

- **닫힌 step**: [step-0005](step-0005.md) — 클라 예측 뷰 + 사후 조정(GGPO 일반화). step-0004 의 권위-측 논리-tick 정렬 *위에서*, 뷰 경로(존→클라)에 지연을 더하고 클라가 *잠정 예측*(공유 결정론 코어로 현재 권위 상태 로컬 재현 → 뷰 RTT 은닉)·*사후 조정*(확정 레이어가 권위 비트 일치 검증 → desync 0 수렴)함을 증명. `engine/` 무수정(routeFilter 훅 재사용).
- **한 줄 상태**: 클라가 *거울*이 됐다 — `ProxySimulator` 가 공유 결정론 코어(같은 `makeSim`·`seed`·`INPUT_DELAY`)로 자기 입력 스케줄을 스스로 관찰·재구성해 두 레이어를 굴린다: **확정**(참 `INPUT_DELAY`, 독립 결정론 복제 — 뷰는 검증·워터마크일 뿐, 뷰 50% 손실에도 desync 0)·**예측**(`predictDelay`, 화면 — 뷰 RTT 은닉). 결정론 전파 클라판 입증(`pred`): 정확 예측 클라가 자기 입력만으로 권위 뷰 매 tick 비트 재현(confDesync 0·오예측 0). 뷰 지연 곡선(`curve` ⒜): 은닉 RTT·롤백 깊이는 ↑(2→14), confDesync·오예측은 0 평탄(수렴은 *비용*이지 정확도 손실 아님). 정확 vs eager(`curve` ⒝): 화면 오예측 0→70(러버밴딩), 그러나 확정 수렴은 *모델 무관* 0(안전망). 회귀 0: 예측 ON 도 세계 사슬 불변(예측 = 순수 클라 측 읽기 모델 재유도, 세계 쓰기 무관). 권위=1·**클라 자기권위 0**(읽기 모델만, 권위 미주장). **단 (a) 단일 클라 정확 예측은 *절대 안 틀린다*** — 진짜 오예측/롤백은 *원격 입력* = 다중 클라 필요(eager 는 합성) **(b) 뷰 경로에만 격리** **(c) 재시뮬 = tick 0 부터(v1 clone 부재)**.
- **다음**: step-0006 (§2).

---

## 2. NEXT — step-0006 가설 (후보, 권위는 이 절)

**다중 클라/엔티티 intent 인터리빙 + *진짜* 클라 예측 오예측/롤백 — 동결 `ISimCore` v1·현실 전송·클라 예측 위에서, 2 클라가 각자 아바타를 움직이고 *상대 입력*을 예측하다 빗나가면 롤백+재시뮬로 desync 0 에 수렴함을 증명한다.**

step-0005 는 *단일 클라*라 자기 입력 완전지(知) 아래 정확 예측이 절대 안 틀렸다(오예측 0) — 화면이 *틀려서* 롤백하는 GGPO 본연은 클라가 모르는 정보 = **원격 플레이어 입력**에서 온다(0005 §8.1). 이제 클라 2 + 아바타 2 로 ⒜ intent 인터리빙(도착 순서 경합)의 결정론을 자극하고 ⒝ 각 클라가 *상대 입력을 예측*하다 빗나가면 *진짜* 오예측 → 롤백+재시뮬 → 권위 재현으로 desync 0 수렴(GGPO 본연). 다중 엔티티는 v1 계약이 이미 지원(spawn(av1)·spawn(av2), 0003 conf 가 이미 구동) — v2 불요.

**검증할 것**: ① **회귀 0** — 클라 1·예측 노브=0 이면 0005 골든 비트 동일(전송 골든 유지). 둘째 클라 추가가 첫째 사슬에 비-침습. ② **결정론 전파** — 2 아바타 권위·추종자·양 클라 확정 레이어가 같은 해시로 수렴(intent 인터리빙 결정론). ③ **권위 보존·수렴** — 매 tick 아바타별 소유자=1·양 클라 확정 desync(조정 후) 0. ④ **가설** — 원격 입력 예측 정확도·오예측/롤백 빈도·수렴 tick 수를 뷰 지연·상대 활동도 따라 수치로(진짜 러버밴딩 곡선).

> 후속 한 조각: **반응적 복원(재요청/스냅샷 재동기)**(0004 §8.2, 영구 손실·failover 선결) · **공간 분할 + 권위 핸드오프**(존 2개·경계 띠·엔티티 이주) · **AOI(범위 좁힘, 씨앗 `HktGridRelevancyComponent`)** · **UE-모듈-free 코어 분리**(C++ 교체 선결, §3 🔴).

**병행 백로그(블로킹 아님)**: ⬜ 반응적 복원(재요청/NAK·스냅샷, 0004 §8.2) · ⬜ 추종자 승격(failover, 0002 §8.3) · ⬜ jitter-buffer 적응(INPUT_DELAY 고정 해소, 0004 §8.3) · ⬜ ARQ/NAK(맹목 k-송신 대체, 0004 §8.6) · ⬜ 입력 스케줄 ack(upstream 지터 시 클라 `at` 도출, 0005 §8.3) · ⬜ 잠정-권위 전파(예측을 이웃에 발행, 0005 §8.5) · ⬜ N-추종자 팬아웃(0002 §8.5) · ⬜ ISimCore v2(clone/deserialize·이벤트·PropertyId, 0003 §8.1·0005 §8.4) · ⬜ 존 배정 로직(zone1 고정 해소) · ⬜ 복제 탭을 게이트웨이→버스로 이전(0002 §8.2) · ⬜ 서버간 인증/버스(0001 §8.3) · ⬜ 재접속·티켓 만료(0001 §8.5) · ✅ 시각 관찰 셸(0004 수립, 0005 재사용: `engine/panel-kit.js` 키트 + `step-NNNN.html` — dual-mode·헤드리스 ASCII).

**빌드 인프라 — `engine/` 추출 완료(0004), 0005 무수정 재사용**: 공통 하니스(시드 PRNG·FNV·전송 모델 `Net`·엣지/코디 스텁·동결 `ISimCore` 2구현 = `engine/index.js`, 시각 관찰 키트 = `engine/panel-kit.js`)를 `engine/` 한 곳으로 모음 — reg 0 으로 비-침습 증명. 코어는 **dual-mode**(Node `require` + 브라우저 `<script>` 전역). 0005 는 `engine/` 무수정 — net-core 가 `ProxySimulator`(클라 예측/조정)와 뷰 경로 routeFilter 만 더했다(전송 substrate 는 0004 훅 재사용). step-0006 의 net-core 도 `engine/` 위에 *다중 클라/예측 프로토콜만* 더한다. 출발 템플릿은 `step-0005/net-core.js`·`step-0005/panel.js`.

---

## 3. OPEN GAPS — 열린 격차 (계층은 [SPINE.md](SPINE.md) §6, 매 step 하나씩 메움)

| 마커 | 격차 | 계층 | 상태 |
|---|---|---|---|
| ✅ | **골격 토폴로지** | 전체 | step-0001 해소 — 4박스 + 세션 수명주기 계약 + 은닉(누설 0) + 비트 결정론(골든 해시 5종). |
| ✅ | **존 결정론 복제** | 월드 | step-0002 해소 — 추종자 존이 입력열만으로 매 tick 권위와 같은 해시(전 60 tick 사슬 일치·골든 도달), 상태 전송 0바이트. 회귀 0(off=골든, on=비-침습). |
| ✅ | **Sim 인터페이스 동결** | 월드 | step-0003 해소 — 존 시뮬이 동결 `ISimCore` v1 계약(spawn/despawn/tick/liveIds/serialize+결정론) 뒤로. 더미(객체맵)·array(SoA) 2구현이 표현 무관 비트 동일 골든, 교체=`run.makeSim` 단일 seam(인프라 구체 시뮬 참조 0건). 단 인터페이스는 *최소*(단일 엔티티) — 다중 엔티티/이벤트면 v2(0003 §8.1). |
| ✅ | **현실 전송 위 결정론 복제 (복제 경로)** | 월드/전송 | step-0004 해소 — `engine/Net` 전송 모델(지연·손실·재정렬+중복송신 신뢰성) + `ZoneHost` 논리-tick 스케줄링(`at` 도장·`applyTick=at+INPUT_DELAY`). 타이밍↔내용 분리 증명(`sep`: 스케줄 사슬 타이밍 불변·desync 0 vs naive 갈림). 전송 ON 권위·추종자 desync 0, 열화 곡선(지연 절벽·손실 복원). 단 *복제 팬아웃에만* — 뷰 경로/클라 예측·반응적 복원은 후속(0004 §8.1·8.2). |
| ✅ | **클라 예측 뷰·사후 조정 (단일 클라)** | 월드 | step-0005 해소 — `ProxySimulator`(클라 측, 공유 결정론 코어)가 확정 레이어(권위 비트 재현·검증, `pred` confDesync 0)·예측 레이어(뷰 RTT 은닉)로 뷰 경로 지연 아래 desync 0 수렴. 뷰 지연 곡선(`curve`: 은닉 RTT/롤백 깊이 ↑, 수렴 0 평탄)·정확 vs eager(확정은 모델 무관 수렴). 단 *단일 클라*라 진짜 오예측/롤백 = 원격 입력(다중 클라) 필요 — eager 는 합성(0005 §8.1). 씨앗: `HktProxySimulatorComponent`. |
| 🔴 | **다중 클라 미검증 — 진짜 오예측/롤백·intent 인터리빙 (최우선)** | 월드 | 0005 는 *단일 클라*라 정확 예측이 절대 안 틀림. 2 클라가 *상대 입력*을 예측하다 빗나가는 진짜 GGPO 오예측·롤백, intent 도착 순서 경합의 결정론 미검증 — **step-0006**(0005 §8.1·0001 §8.6). |
| 🟡 | **반응적 손실 복원 없음** | 월드/전송 | 0004 는 선제 redundancy 만 — 예산 초과 영구 손실엔 재요청/NAK·스냅샷 재동기 없음(추종자 복원 없이 갈림). 0005 의 확정 레이어는 뷰 손실에 강건하나(자기 재유도) *입력* 영구 손실은 미해결. failover·핸드오프의 선결(0004 §8.2). |
| 🔴 | **HktCore UE-모듈 의존 (UE-free 검증 미충족)** | 월드 | `HktCore.Build.cs` 가 `Core`·`CoreUObject`·`GameplayTags`·`Json`·`JsonUtilities` 링크 — 'UObject 0' 이나 UE 소스/UBT 없이 빌드 불가. C++ 시뮬을 UE 없이 검증하려면(불변, §4) UE-모듈-free 코어 분리 또는 헤드리스 shim 필요. 더미(Node)/array 스탠드인 단계엔 비차단, **C++ 승격의 선결**(0003 §8.2). |
| 🟡 | **추종자 승격(failover) 없음** | 월드 | 0002 동기화만 증명 — 권위 사망 시 추종자→권위 전환(뷰 발행·소유자 이전)은 미구현(0002 §8.3). 핸드오프 step 과 함께. |
| ⬜ | **로그인 큐·티켓 실체화** | 엣지 | 스텁→계정 검증·대기열·티켓 만료(0001 §8.5). 씨앗: `HktLoginComponent`. |
| ⬜ | **AOI(범위 좁힘)** | 월드 | 뷰가 전체 상태(0001) — 클라가 자기 근방만 수신하도록. 씨앗: `HktGridRelevancyComponent`. |
| ⬜ | **다중 클라 인터리빙 → 진짜 예측/조정** | 월드 | 0005 단일 클라 예측을 다중 클라로 — 원격 입력 예측 실패·롤백(GGPO 본연)·intent 경합 결정론. 씨앗: `HktProxySimulatorComponent`. |
| ⬜ | **공간 분할 + 권위 핸드오프** | 월드 | 존 2개·경계 띠 상호 구독·엔티티 이주(쌍 거래, 소유자=1). 존 배정도 zone1 고정(0001 §8.4). |
| ⬜ | **다중 클라 인터리빙** | 월드 | 클라 1뿐(0001 §8.6) — intent 도착 순서 경합의 결정론 미자극. |
| ⬜ | **서버간 인증 없음** | 버스 | 존이 게이트웨이 발신을 암묵 신뢰(0001 §8.3) — 분산 시 서버간 인증/버스 필요. |
| ⬜ | **가방 서비스 분리** | 서비스 | 아이템 원장을 존 tick 밖으로 — 트랜잭션·dupe 방지·존 넘는 거래. 씨앗: `HktPlayerInventoryComponent`. |
| ⬜ | **이벤트 버스** | 버스 | 서버간 pub/sub — N×N 직접 결합 제거. 씨앗: `FHktEvent`(EventTag). |
| ⬜ | **채팅·길드·거래소·우편·랭킹** | 서비스 | 가방 분리 패턴의 반복 적용(팬아웃·비-시뮬 상태). |
| ⬜ | **세션/프레즌스 + 오케스트레이터** | 코디네이션 | "누가 어디에" SSOT · 존 배치·부하 분산·인스턴스 spawn. |
| ⬜ | **캐시 + write-behind 영속** | 데이터 | 동기 I/O 를 모든 tick 에서 제거. 세계가 세션보다 오래 산다. 씨앗: `Hkt*DatabaseComponent`. |
| ⬜ | **크래시 복구·재접속·late-join** | 전체 | 영속에서 뷰/권위 재구성 — 소실 권위의 고리 닫기. |

> **상시 렌즈 — 척추** ([SPINE.md](SPINE.md) §5 필독): 매 step은 verify 4기둥 + 척추 체크 4항(① 신성한 tick ② 결정론 코어 ③ 권위 단일 소유 ④ 은닉·단일 연결)으로 판정. 분리 판정 기준: *그 일이 존 시뮬 tick 과 같은 박자로 돌아야 하는가?*

---

## 4. DURABLE CONSTRAINTS — 모든 step이 지킬 정전(canonical) 사실

> 여러 step이 반복 참조하는 불변. 새 step은 이걸 어기면 척추에서 벗어난 것 — 재설계.

- **회귀 0**: 새로 더한 항을 끄면(플래그=0) 직전 step과 **비트 단위 동일**(verify `reg`, maxDiff=0).
- **신성한 tick**: 존 시뮬 tick 안에 시뮬 외 작업(동기 I/O·인증·트랜잭션·팬아웃) 금지 — 전부 비동기 서비스/버스 경유.
- **결정론 코어**: 세계 상태의 쓰기 경로는 intent→HktCore 하나. 같은 시드·로그 → 같은 해시. **`Math.random` 금지, 시드 의사난수만.**
- **권위 단일 소유**: 모든 상태 조각의 쓰기 권위는 정확히 한 곳(매 tick 소유자=1). 이동은 release+acquire **쌍 거래**/트랜잭션.
- **은닉·단일 연결**: 클라는 게이트웨이만 안다. 서버간은 버스/명시 인터페이스만 — 타 서버 내부·DB 직접 접근 금지.
- **수렴(desync 0)**: 클라 예측 뷰는 권위 재현으로 수렴. 겹친 뷰의 참여자는 조정 후 일치.
- **복제 = 재현, 상태 전송 아님**: `(seed+params+intent 로그)` 가 기본. 전체 스냅샷은 late-join·복구의 최후 수단.
- **Sim = 인터페이스 우선, C++ 최후 교체**: 존 시뮬은 *동결된 Sim 인터페이스* 뒤에 산다 — 오늘은 더미 구현(헤드리스), 인프라 전체를 원격 E2E 로 세운 뒤 인터페이스 무변경으로 얇은 C++ 호스트로 *교체만*. C++ 선-구축으로 원격 검증 루프를 끊지 않는다 ([TOOLS.md](TOOLS.md) §4·§6). 더미는 throwaway 가 아니라 인터페이스의 *첫 구현*.
- **UE-free 검증(게임 시뮬 포함, 협상 불가)**: 모든 서버 동작 검증은 *결정론 시뮬 코어까지* Unreal Engine 없이 헤드리스로 가능해야 한다. 더미=Node 라 자명히 충족 — C++ 승격 경로도 시뮬 *검증*에 UE 모듈을 링크하지 않는다. **'UObject 0' 으로는 부족**: 현 `HktCore` 는 `Core`·`CoreUObject`·`GameplayTags`·`Json` 의존(§3 격차) → UE-모듈-free 코어 분리 또는 얇은 헤드리스 shim 으로 충족. 'UE 없이 빌드·검증되는가'가 시뮬 도구의 통과 조건 ([TOOLS.md](TOOLS.md) §1·§4).
- **수치 = verify 출력**: 모든 문서 수치는 시드 [42, 7, 1234, 99, 2026] 평균으로 재현.
- **HktGameplay 원칙 상속**: HktCore 순수성(UObject 0) · 서버 권위(클라 읽기전용) · ISP 3-Layer · `SetPropertyDirty` 경유. 인프라는 *확장*하되 *깨지 않는다*.
- **한 step = 한 조각**: 더 떠올라도 다음으로 전가. 새 코어는 직전 코어를 잇고 박스/계약 하나만 더한다.
- **도구 = tick 판정 + 원격-검증** ([TOOLS.md](TOOLS.md)): tick 동기는 C++(HktCore), 그 외는 원격에서 빌드·헤드리스 검증되는 도구(Go·Node·컨테이너). 시뮬만 무거운 빌드로 격리(HktCore "UObject 0" 순수성 = 얇은 헤드리스 호스트로 CI 빌드 가능 = 빌드 자산).
- **데이터 3분할** ([TOOLS.md](TOOLS.md) §3): ① 월드 영속 = intent 로그(event sourcing) + 스냅샷 ② 트랜잭션 진실 원천(가방·계정) = PostgreSQL→분산 SQL ③ Redis = 휘발/캐시(진실 원천 아님). 월드 상태를 DB 행으로 저장하지 않는다.

---

## 5. 6계층 진행 현황 ([SPINE.md](SPINE.md) §6)

| # | 계층 | 박스 | 상태 |
|---|------|------|------|
| 1 | 엣지 | 로그인/인증 · 게이트웨이 | 🟡 **0001 스텁 수립** — 일회성 티켓·단일 연결·은닉(박리) 계약 성립. 대기열·만료·재접속 후속 |
| 2 | 월드 | 존 · 인스턴스 (분할·AOI·조정·핸드오프) | 🟡 **0001 존 VM + 0002 복제 + 0003 Sim 동결 + 0004 현실전송 위 복제 + 0005 클라 예측/조정** — 시뮬이 동결 `ISimCore` v1 뒤로(더미·array·단일 seam), 논리-tick 스케줄링이 전송 지터 아래 사슬 정렬(타이밍↔내용 분리), `ProxySimulator` 가 뷰 지연 아래 클라 거울로 desync 0 수렴(단일 클라). **다음: 다중 클라·진짜 오예측/롤백(0006)** · 분할·AOI·핸드오프·승격은 후속 |
| 3 | 게임 서비스 | 가방 · 채팅 · 길드 · 거래소 · 우편 · 랭킹 | ⬜ — 전부 존 프로세스 안(`HktPlayerInventoryComponent` 등) |
| 4 | 버스 | 이벤트 버스 | 🟡 **0004 전송 substrate(`engine/Net`)** — 지연·손실·재정렬+신뢰성 전송 모델 수립(복제 팬아웃). pub/sub 버스·서버간 인증은 미착수(0001 §8.3) |
| 5 | 코디네이션 | 세션/프레즌스 · 오케스트레이터 | 🟡 **0001 레지스트리 스텁** — 티켓 일회성·세션 SSOT·수명주기(closed). 존 배정은 고정(0001 §8.4) |
| 6 | 데이터 | 캐시 · DB · write-behind | ⬜ — 영속 0(세계가 프로세스와 함께 죽는다). 씨앗: `Hkt*DatabaseComponent` |

---

## 6. 빠른 참조

- 네트워크 인프라 큰 그림·계층 책임·씨앗 매핑·척추 체크 4항: [SPINE.md](SPINE.md)
- **도구·기술 스택(각 박스를 무엇으로 짓는가)·데이터 3분할·교차 관심사**: [TOOLS.md](TOOLS.md)
- 시뮬 도메인(무엇을 네트워킹하는가): [../HktGameplay/CLAUDE.md](../HktGameplay/CLAUDE.md)
- **의외의 발견 / 정직한 한계 전문**: 각 `step-NNNN.md` (STATE는 중복 보관하지 않음 — 위 §3·§4가 현재 load-bearing 요약).

---

## 7. INDEX — 시리즈 검증 현황 (유일하게 append, 1행/step)

| step | 더한 한 조각 | 결과 (회귀 0 전제) |
|---|---|---|
| [0001](step-0001.md) | 최소 골격 토폴로지 (4박스 + 세션 계약) | **통과** — E2E 수명주기(뷰 21·intent 20/20·closed)·은닉 0/47·비트 결정론(자기 재현, 골든 5종)·권위 위반 0·무자격 3경로 전부 존 도달 전 차단. **정직**: 결정론≠복제(→0002)·전송 행복경로만 |
| [0002](step-0002.md) | 존 결정론 복제 (추종자 존 + 게이트웨이 입력-미러 탭) | **통과** — 권위·추종자 매 tick 사슬 일치(0/60 desync·골든 도달)·상태 전송 0바이트(입력 22 msgs)·권위 보존(권위=1·추종자 자기권위 0)·회귀 0(off=골든, on=비-침습). **정직**: 전송 행복경로만(→0003)·승격 없음·N=1 바이트이득 7.7% |
| [0003](step-0003.md) | Sim 인터페이스 동결 (ISimCore v1 + 더미·array 2구현 + 단일 교체 seam) | **통과** — 회귀 0(추출 비-침습, 골든 비트동일)·계약 동결(`conf`: 더미·array seed-only 결정론·수명주기·표현 무관 trace 비트동일)·단일 seam(`swap`: 인프라 7클래스 구체 시뮬 참조 0건, makeSim 만 바꿔 교체→골든·E2E 불변)·0002 전 불변 유지(사슬 0/60·권위 0·desync 0·은닉 0). **정직**: 인터페이스 최소(단일 엔티티→v2)·array 는 C++ 스탠드인·전송 행복경로만(→0004) |
| [0004](step-0004.md) | 현실 전송(지연·손실·재정렬) + 논리-tick 입력 스케줄링 (+ `engine/` 추출) | **통과** — 회귀 0(전송끔+ID=1 → 0001/0003 골든 비트동일, 추출 비-침습)·분리(`sep`: 스케줄 사슬 타이밍 5종 불변·desync 0 vs naive 5종 갈림·desync ~262)·전파/권위(`prop`·`auth`: 전송 ON 권위·추종자 desync 0·권위=1·추종자 자기권위 0)·열화 곡선(`curve`: 지연 절벽 예산 7tick·손실 복원 redundancy 1→3 desync 597→0). **정직**: 전송 복제경로만(뷰/예측 →0005)·복원 선제 redundancy 만(반응적 없음)·단일 엔티티·INPUT_DELAY 고정 |
| [0005](step-0005.md) | 클라 예측 뷰 + 사후 조정 (GGPO 일반화 — `ProxySimulator`, 뷰 경로 지연) | **통과** — 회귀 0(예측 ON 도 세계 사슬 불변 = 순수 클라 측, ID=1 0001/0003·ID=8 0004 전송 골든)·결정론 전파 클라판(`pred`: 정확 예측이 자기 입력만으로 권위 뷰 매 tick 비트 재현, confDesync 0·오예측 0)·수렴/권위(`conv`·`auth`: 확정 desync 0·권위=1·클라 자기권위 0·은닉 RTT 뷰 지연 따라 ↑)·곡선(`curve`: ⒜ 은닉 RTT/롤백 깊이 2→14 ↑·수렴 0 평탄 ⒝ eager 오예측 0→70·확정 수렴 모델 무관 0). **정직**: 단일 클라라 진짜 오예측 = 원격 입력(다중 클라 →0006)·eager 합성·뷰 경로만 격리·재시뮬 tick 0 부터(v1 clone 부재) |
