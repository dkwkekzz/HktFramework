# STATE — 살아있는 현재

> "지금 어디까지 왔고 다음은 무엇인가"의 **단일 진실 원천(SSOT)**.
> 큰 목표·규칙은 [CLAUDE.md](CLAUDE.md) · 척추(인프라 큰 그림)는 [SPINE.md](SPINE.md) · 각 step 상세 기록은 `step-NNNN.md`.
>
> **구조 규칙(에이전트 효율 — 토큰이 step 수에 비례해 터지지 않게 *강제*)**: 이 파일은 *고정 크기 대시보드*다. §1~6은 step을 닫을 때마다 **덮어쓴다(rewrite)** — 누적하지 않는다. 오직 §7 INDEX만 **literal 1줄** append(`step | 조각 | 통과+핵심수치 1개` — *문단 금지*). 발견/한계의 *전문*은 STATE가 아니라 `step-NNNN.md`에 산다. **세 가지 누적 함정 금지**: ① §5 에서 큰 그림 진척을 step별로 *재나열*하지 말 것 — 현재 마커 + 핵심 step 목록만 ② §3 에서 ✅해소된 격차를 전문 보존하지 말 것 — *한 줄로 떨어뜨린다* ③ §4 는 *여러 step 이 반복 참조하는 불변*만. 닫힌 step 의 한-문단 INDEX 요약 history 는 [STATE-INDEX-ARCHIVE.md](STATE-INDEX-ARCHIVE.md)(읽기용·필독 아님). 마커: ✅해소 🟡부분 🔴열림(최우선) ⬜백로그 🔧노브.

---

## 1. NOW

- **닫힌 step**: [step-0013](step-0013.md) — 진짜 프로세스 kill 아래 failover(분단 윈도 → 실 `child.kill` · 타임아웃/소켓 추측 감지 · 재-provisioning · 거짓 사망 epoch 펜싱). 0012 의 분단은 *논리 윈도*(broker 가 침묵 시점을 *앎*)였고 분단 호스트는 *살아 있었다*(펜싱 대상). 이 step 은 그 분단을 *진짜 kill* 로 내린다(액터 무수정 — *cluster/host 의 와이어 생애주기*만): 인프로세스(`run`)=engine Net 그대로(0012 비트 동일) · 멀티프로세스(`runMulti`)= broker 가 **(a) 실 `child.kill('SIGKILL')`**(프로세스 소멸·소켓 RST)을 결정론 tick 에 주입하고 *소켓 close 신호*로 침묵을 **감지**(윈도 아님) → deliver/tick 스킵=deathTick 비트 동일(죽은 프로세스는 *안 돌아옴*) **(b) 소켓 살아 있는 침묵은 침묵 tick 을 세어 leaseTimeout 초과 시 presumedDead 로 *추측*(타임아웃)** **(c) 거짓 사망 복귀를 stale *epoch* 펜싱**(발신 전량 드롭→split-brain 0) **(d) kill→승격으로 N=1 이면 *새 프로세스 spawn*·스냅샷 상태 동기(`loadstate`)·권위 입력 미러로 핫 standby 복원(N≥2)**. lockstep 배리어·토픽 버스·failover *무변경*. `engine/` 무수정.
- **한 줄 상태**: 코디네이션이 *진짜 프로세스 소멸*(child.kill SIGKILL) 아래에서도 권위 단일 소유·desync 0 을 지키고, 죽은 자리를 *새 프로세스로 재충원*하며, *거짓 사망 복귀*를 epoch 으로 거부한다 — `node verify.js` 한 줄이 원격에서 broker+자식 프로세스 기동·실 kill·재충원·검증. 핵심: ① **진짜 kill = 분단 = 사망 비트 동일** — 실제 SIGKILL(소켓 RST·pid 소멸)인데 인프로세스 deathTick·0012 분단과 *logDigest·fullDigest 비트 동일*(repro 가 0012·0011·0010·0009 와 정확히 일치). 권위 보존은 *원인 무관* 침묵의 함수 — 단 *감지 메커니즘*만 다름(사망=플래그·분단=윈도·kill=*소켓 close 신호*) ② **벽시계 비결정을 논리 tick 으로 가둠** — kill 의 RST 도착은 벽시계 비결정이나, `killHost` 가 kill 직후 socketDead 세우고 close 를 await → *어느 tick 에서 침묵 시작*은 `kill.at` 의 순수 함수(벽시계가 다이제스트에 안 닿음) ③ **감지의 쉬움이 사망 종류를 가름** — close 는 *신호*(진짜 kill), 침묵 길이는 *추측*(거짓 사망, 타임아웃)이고 추측은 *틀릴 수 있어* epoch 펜싱 필요 ④ **epoch 펜싱이 거짓 사망을 진짜 사망과 비트 동일로** — 복귀 호스트가 *살아 출력*(epochFenced 55~64=실제 시도)해도 전량 드롭→net.log 0 기여→소유자 1 ⑤ **재-provisioning divergence 0 은 멱등 상태까지 동기해야 성립** — ents 만 복사하면 핸드오프 재전송 재-acquire·다른 sibling 으로 1~3 샘 → ents·sessions·`acquired`·같은 sibling 동기해야 0. 검증: 회귀 0(`reg`, 인프로세스 → 0012 비트 동일 20/20)·E2E 동치(`e2e`, 멀티프로세스=인프로세스 비트 동일·desync 0)·진짜 kill(`kill`, 실 child.kill→소켓 감지→deathTick 비트 동일·죽은 pid 소멸·소유자 1·desync 0)·재-provisioning(`reprovision`, 새 standby spawn·미러~120·divergence 0·N≥2·full=인프로세스·비-침습)·거짓 사망(`falsedeath`, 타임아웃 추측@42·epoch 1·epochFenced→비트 동일·소유자 1)·분리(`isolate`, 9 구분 pid·죽은 pid 소멸)·은닉(`hide`, 누설 0)·재현(`repro`, 멀티 2회=인프로세스=0012~0009). **의도적 경량화 유지**. **단 (a) kill 타이밍=논리 tick 주입(결정론 — 진짜 비동기 벽시계 타임아웃 곡선 아님)** **(b) 재-provisioning=단일 standby·broker 중앙 미러(자가 디스커버리·다중 standby·점진 동기 아님)** **(c) epoch=broker 단일 카운터(분산 합의 토큰·스토리지 강제 아님)** **(d) 거짓 사망=침묵→복귀 1회(플래핑·부분 복귀·재합류 아님)** **(e) lockstep 배리어=중앙 코디네이터(진짜 비동기 아님)·broker 단일 허브·여전히 Node·C++ 시뮬 미교체·존↔존 ghost 미착수**.
- **다음**: step-0014 (§2).

---

## 2. NEXT — step-0014 가설 (후보, 권위는 이 절)

**비동기 실행 아래 결정론 — lockstep 배리어를 풀고, *진짜 비동기*(벽시계 타임아웃 감지·노드별 자유 진행) 아래에서 논리 클럭/인과 순서로 결정론·권위 보존을 지키는 것. step-0013 까지 broker 는 매 tick *중앙 배리어*로 전 프로세스를 묶었다(매 tick 응답 대기) — 그 덕에 진짜 kill 의 벽시계 비결정을 *논리 tick* 으로 가뒀지만, 진짜 비동기·미지의 분단(벽시계 타임아웃 추측)·노드별 자유 진행은 안 탔다. 다음은 그 배리어를 (부분) 해제하고 논리 클럭(Lamport/벡터)·인과 순서로 발신 순서를 재조립해, 동기 배리어 없이도 같은 입력→같은 상태·소유자=1 을 증명하는 것([STATE.md](STATE.md) §3·0012 §9-3·0013 §9-1).**

step-0013 이 분단 윈도를 *진짜 child.kill* 로 내리고 소켓/타임아웃 추측 감지·재-provisioning·거짓 사망 epoch 펜싱을 세웠다(E2E 비트 동일·desync 0·소유자 1·죽은 pid 소멸). 그러나 결정론은 *lockstep 배리어*(중앙 코디네이터)가 떠받쳤다 — broker 가 매 tick 전 프로세스 응답을 기다려 발신 전역순서를 보존했다. 다음 자연한 한 조각 후보는 그 배리어를 풀고 *진짜 비동기*로 가는 것: 노드가 자유 진행하고, 벽시계 타임아웃으로 사망을 *추측*하며(0013 의 추측을 *벽시계*로), 논리 클럭/인과 순서가 발신을 재조립해 결정론을 지킨다. **단 이는 큰 조각이라** — C++ 시뮬 교체(§3 🔴)·물리적 다중 브로커(분산 epoch)·가방 서비스 분리도 강한 후보. 권위는 이 절이되, 다음 step 시작 시 한 조각으로 좁힐 것.

**검증할 것(후보)**: ① **회귀 0** — 배리어 모드(동기)면 0013 비트 동일. ② **비동기 진행** — 노드별 자유 진행 아래 논리 클럭/인과 순서로 발신 재조립 → 같은 입력 → 같은 상태(결정론 보존). ③ **벽시계 타임아웃 감지** — 0013 의 *논리 tick* 추측을 *벽시계* 추측으로(bounded gap 의 비결정 곡선) → 소유자 1·desync 0. ④ **원격 한 줄 기동·은닉·재현** 유지(비동기라 재현이 어려운 점 — 가설의 핵심 긴장, 논리 클럭이 풀어야).

> 후속 한 조각: **물리적 다중-브로커 버스 분산**(broker 허브를 여러 브로커로·분산 epoch/합의 토큰 — 0012 §9-1·0013 §9-3) · **C++ 시뮬 코어 headless 교체**(§3 🔴) · **가방 서비스 분리**(서비스 계층 첫 박스 — 토픽 버스 서비스로) · **존 N개·동적 경계**(0006 §8.2) · **존↔존 ghost 증분화·복원**(0007 §8.3) · **소켓 층 지연·재정렬·비대칭 분단**(0012 §9-4) · **거짓 사망 플래핑·부분 복귀·재합류**(0013 §9-4).

**병행 백로그(블로킹 아님)**: ⬜ 비동기 실행 아래 결정론(논리 클럭, 0012 §9-3·0013 §9-1) · ⬜ 물리적 다중-브로커 버스 분산·분산 epoch(0012 §9-1·0013 §9-3) · ⬜ 거짓 사망 플래핑·부분 복귀·stale 호스트 재합류(0013 §9-4) · ⬜ 다중 standby·자가 디스커버리·점진 상태 동기(0013 §9-2) · ⬜ 소켓 층 지연·재정렬·비대칭 분단(0012 §9-4) · ⬜ 다중 클라 결정론 복제·예측(0001~0004 무거운 모델은 C++ 승격에서 부활) · ⬜ 존 N개·동적 경계(0006 §8.2) · ⬜ 존↔존 ghost 증분화·복원(0007 §8.3·0008 §8.6) · ⬜ cross-zone 지연 완화(0007 §8.2) · ⬜ 제어 평면 손실 복원(0008 §8.2·0009 §8.2) · ⬜ 가변 지연 재정렬 아래 seq 복원(0008 §8.4) · ⬜ 서버간 인증(0001 §8.3) · ⬜ 재접속·티켓 만료(0001 §8.5) · ⬜ 가방 서비스 분리(존 tick 밖) · ✅ 시각화·검증 testbed 단일화(0010 후: `run.js`+`report.html`+`live.js` 구현, 손그림 html 8장 폐기 — 데이터층은 `engine/panel-kit.js` 재사용, 설계 [TESTBED.md](TESTBED.md)) · ✅ 레코더 per-tick 엔티티 위치·AOI 시각화(`onTick` 소비 → `report.html` 공간 격자 맵) + verify `scenario` 브리지(`run.js scenario` — 공유 번역기·trace 4기둥 단언) 완료 · 🟡 시나리오 `inject`(클라 intent 주입)만 남음 — net-core write-seam 필요(동결 0013 엔 없음, 다음 복사 전진에서 1줄 심음 → testbed 무수정 활성).

**빌드 인프라 — `engine/` 추출 완료(0004), 0005~0013 무수정 재사용**: 공통 하니스(시드 PRNG·FNV·전송 모델 `Net`·엣지/코디 스텁·동결 `ISimCore` 2구현 = `engine/index.js`, 시각 관찰 키트 = `engine/panel-kit.js`)를 `engine/` 한 곳에. 코어는 **dual-mode**(Node `require` + 브라우저 `<script>` 전역). 0013 은 `engine/` 무수정·액터 무수정 — net-core 는 0012 액터·토폴로지 빌더 그대로, `cluster.js`(broker=버스 허브·토픽 pub/sub·열화 + **진짜 kill 생애주기**: child.kill·소켓 close 감지·타임아웃 추측·epoch 펜싱·재-provisioning spawn+미러)·`host.js`(자식=spawn·IPC 0·reqId 멱등·fence + **loadstate** 상태 동기)의 *kill 생애주기만* 더함. broker 는 engine Net 을 *전송 substrate* 로 재사용한다(액터 무수정). step-0014 의 net-core 는 lockstep 배리어를 풀고 논리 클럭/인과 순서를 더한다. 출발 템플릿은 `step-0013/net-core.js`·`step-0013/cluster.js`·`step-0013/host.js`.

**TESTBED 도구 인계(0010 후 구현·동결 step 무수정)**: `run.js`(검증 단일 진입점 — `node run.js`·`spine`·`<NNNN>`·`report`·**`scenario`**·`live`) + 자기완결 `report.html`(녹화 레코더, **공간 위치+AOI 맵 포함**) + `live.js`(SSE 라이브 모니터, 존 kill→failover 인터랙티브)가 섰다([TESTBED.md](TESTBED.md) §12 구현 현황). 테스트베드 마무리는 **거의 완료**: ⒜ `onTick(t,state)` 훅(0011 심음, 미제공=no-op→reg 0) ✅ · ⒝ verify **`scenario <file>`** 브리지(`run.js scenario` — `report` 와 `loadScenario` 번역기 공유, trace 4기둥[권위=1·desync 0·kill→승격·멀티프로세스 비트동일]을 프로그램적 단언·exit 0/1. verify.js 동결이라 run.js 에 둠 = §5-4 "에이전트도 trace 직접 단언") ✅ · ⒞ 레코더가 `onTick` 위치를 소비해 *엔티티 공간 위치·AOI 반경·존 경계*를 `report.html` 격자 맵으로 렌더 ✅. **남은 단 하나 = scenario `inject`**(클라 intent 주입): net-core 의 클라 *write-seam* 이 필요한데 동결 0013 엔 없고(클라 move=시드 RNG 함수), run.js 단독 구현은 "둘째 구현 0"(§8) 위배 → **다음 net-core 복사 전진에서 reg-safe 1줄 심으면**(onTick 선례) testbed 무수정 활성. 그 전까진 경고 후 무시(kill/transport/opts 만). "레코더 이상→export→회귀 케이스" 고리는 `inject` 없이도 kill/transport 시나리오로 이미 성립.

---

## 3. OPEN GAPS — 열린 격차 (계층은 [SPINE.md](SPINE.md) §6, 매 step 하나씩 메움)

| 마커 | 격차 | 계층 | 상태 |
|---|---|---|---|
| 🔴 | **C++ 시뮬 코어 headless 빌드 (최우선)** | 월드 | 결정론 시뮬 코어가 UE 모듈(`Core`·`CoreUObject`·`GameplayTags`·`Json` 등)에 링크되면 'UObject 0' 이라도 UE 소스/UBT 없이 빌드 불가 → 원격 검증 불가. UE-모듈-free 코어 분리 또는 얇은 헤드리스 shim 필요(§4 불변). C++ 승격의 선결(0003 §8.2). |
| 🔴 | **비동기 실행 아래 결정론 (lockstep 배리어 해제·step-0014 후보)** | 코디네이션 | 0013 까지 결정론은 *중앙 lockstep 배리어*(broker 가 매 tick 전 프로세스 응답 대기)가 떠받친다. 진짜 비동기·노드 자유 진행·벽시계 타임아웃 곡선은 미착수 — 논리 클럭(Lamport/벡터)·인과 순서로 배리어 없이 결정론·소유자 1 보존이 후속(0012 §9-3·0013 §9-1). |
| ⬜ | **로그인 큐·티켓 실체화** | 엣지 | 스텁→계정 검증·대기열·티켓 만료(0001 §8.5). |
| ⬜ | **다중 클라 결정론 복제·예측** | 월드 | 0002~0004 의 결정론 복제·예측은 *C++ 시뮬 코어 승격*에서 부활(더미는 경량 라우터). 다중 클라 intent 인터리빙·예측/롤백(0001 §8.6). |
| ⬜ | **서버간 인증 없음** | 버스 | 존이 게이트웨이 발신을 암묵 신뢰(0001 §8.3) — 분산 시 서버간 인증 필요. |
| ⬜ | **가방 서비스 분리** | 서비스 | 아이템 원장을 존 tick 밖으로 — 트랜잭션·dupe 방지·존 넘는 거래. |
| ⬜ | **이벤트 버스(서버간 pub/sub)** | 버스 | N×N 직접 결합 제거 — 0012 토픽 버스가 *전송*은 깔았으나 서비스 층 미적용. |
| ⬜ | **채팅·길드·거래소·우편·랭킹** | 서비스 | 가방 분리 패턴의 반복(팬아웃·비-시뮬 상태). |
| ⬜ | **세션/프레즌스 + 오케스트레이터** | 코디네이션 | "누가 어디에" SSOT·존 배치·부하 분산·인스턴스 spawn. |
| ⬜ | **캐시 + write-behind 영속** | 데이터 | 동기 I/O 를 모든 tick 에서 제거 — 세계가 세션보다 오래 산다. |
| ⬜ | **크래시 복구·재접속·late-join** | 전체 | 영속에서 뷰/권위 재구성 — 소실 권위의 고리 닫기. |

> **✅ 해소된 격차 (0001~0013)** — 골격 토폴로지(0001)·존 결정론 복제(0002)·Sim 인터페이스 동결(0003)·현실 전송 위 복제(0004)·AOI 브로드캐스트(0005)·공간 분할·권위 핸드오프(0006)·증분 AOI(0007)·전송 열화 아래 반응적 복원(0008)·추종자 승격 failover(0009)·프로세스 경계 E2E(0010)·실 TCP 소켓 와이어(0011)·버스 분산·열화 내성(0012)·진짜 프로세스 kill 아래 failover(0013, epoch 펜싱·재-provisioning). 상세 = 각 `step-NNNN.md` · §7 INDEX · [STATE-INDEX-ARCHIVE.md](STATE-INDEX-ARCHIVE.md).

> **상시 렌즈 — 척추** ([SPINE.md](SPINE.md) §5 필독): 매 step은 verify 4기둥 + 척추 체크 5항(① 신성한 tick ② 결정론 코어 ③ 권위 단일 소유 ④ 은닉·단일 연결 ⑤ headless·원격 검증)으로 판정. 분리 판정 기준: *그 일이 존 시뮬 tick 과 같은 박자로 돌아야 하는가?*

---

## 4. DURABLE CONSTRAINTS — 모든 step이 지킬 정전(canonical) 사실

> 여러 step이 반복 참조하는 불변. 새 step은 이걸 어기면 척추에서 벗어난 것 — 재설계.

- **회귀 0**: 새로 더한 항을 끄면(플래그=0) 직전 step과 **비트 단위 동일**(verify `reg`, maxDiff=0).
- **신성한 tick**: 존 시뮬 tick 안에 시뮬 외 작업(동기 I/O·인증·트랜잭션·팬아웃) 금지 — 전부 비동기 서비스/버스 경유.
- **결정론 코어**: 세계 상태의 쓰기 경로는 intent→결정론 시뮬 코어 하나. 같은 시드·로그 → 같은 해시. **`Math.random` 금지, 시드 의사난수만.**
- **권위 단일 소유**: 모든 상태 조각의 쓰기 권위는 정확히 한 곳(매 tick 소유자=1). 이동은 release+acquire **쌍 거래**/트랜잭션.
- **은닉·단일 연결**: 클라는 게이트웨이만 안다. 서버간은 버스/명시 인터페이스만 — 타 서버 내부·DB 직접 접근 금지.
- **수렴(desync 0)**: 클라 예측 뷰는 권위 재현으로 수렴. 겹친 뷰의 참여자는 조정 후 일치.
- **복제 = 재현, 상태 전송 아님**: `(seed+params+intent 로그)` 가 기본. 전체 스냅샷은 late-join·복구의 최후 수단.
- **Sim = 인터페이스 우선, C++ 최후 교체**: 존 시뮬은 *동결된 Sim 인터페이스* 뒤에 산다 — 오늘은 더미 구현(헤드리스), 인프라 전체를 원격 E2E 로 세운 뒤 인터페이스 무변경으로 얇은 C++ 호스트로 *교체만*. C++ 선-구축으로 원격 검증 루프를 끊지 않는다 ([TOOLS.md](TOOLS.md) §4·§6). 더미는 throwaway 가 아니라 인터페이스의 *첫 구현*.
- **headless·원격 검증(게임 시뮬 포함, 협상 불가)**: 모든 서버 동작 검증은 *결정론 시뮬 코어까지* Unreal Engine·GUI 없이 headless 로, 원격(이 환경/CI)에서 가능해야 한다. 더미=Node 라 자명히 충족 — C++ 승격 경로도 시뮬 *검증*에 UE 모듈을 링크하지 않는다. **'UObject 0' 으로는 부족**: 결정론 시뮬 코어가 UE 모듈(`Core`·`CoreUObject`·`GameplayTags`·`Json` 등)에 링크되면 headless 빌드 불가(§3 격차) → UE-모듈-free 코어 분리 또는 얇은 헤드리스 shim 으로 충족. 'UE 없이, 원격에서 빌드·통신·검증되는가'가 모든 박스(시뮬 포함)의 통과 조건 ([TOOLS.md](TOOLS.md) §1·§4).
- **수치 = verify 출력**: 모든 문서 수치는 시드 [42, 7, 1234, 99, 2026] 평균으로 재현.
- **코어 netcode 불변**: 결정론 시뮬 코어 순수성(UObject 0 — headless 빌드 가능) · 서버 권위(클라 읽기전용) · ISP 3-Layer · 시뮬 상태 직접쓰기 금지(intent 경유). 인프라는 *확장*하되 *깨지 않는다*.
- **한 step = 한 조각**: 더 떠올라도 다음으로 전가. 새 코어는 직전 코어를 잇고 박스/계약 하나만 더한다.
- **도구 = tick 판정 + 원격-검증** ([TOOLS.md](TOOLS.md)): tick 동기는 C++(결정론 시뮬 코어), 그 외는 원격에서 빌드·헤드리스 검증되는 도구(Go·Node·컨테이너). 시뮬만 무거운 빌드로 격리(시뮬 코어 "UObject 0" 순수성 = 얇은 헤드리스 호스트로 CI 빌드 가능 = 빌드 자산).
- **데이터 3분할** ([TOOLS.md](TOOLS.md) §3): ① 월드 영속 = intent 로그(event sourcing) + 스냅샷 ② 트랜잭션 진실 원천(가방·계정) = PostgreSQL→분산 SQL ③ Redis = 휘발/캐시(진실 원천 아님). 월드 상태를 DB 행으로 저장하지 않는다.

---

## 5. 6계층 진행 현황 ([SPINE.md](SPINE.md) §6)

| # | 계층 | 박스 | 상태 (현재 마커 + 핵심 step) |
|---|------|------|------|
| 1 | 엣지 | 로그인/인증 · 게이트웨이 | 🟡 0001 스텁(일회 티켓·단일 연결·은닉) + 0010 별 OS 프로세스. 대기열·만료·재접속 후속 |
| 2 | 월드 | 존 · 인스턴스 (분할·AOI·조정·핸드오프) | 🟡 0001 존 VM +0002~0004 결정론 복제(현실 전송)·동결 Sim +0005 AOI +0006 분할·핸드오프(소유자=1) +0007 증분 AOI +0008 반응적 복원 +0009 failover +0010 별 프로세스 +0013 죽은 추종자 재충원(재-provisioning·divergence 0·N≥2). 0002~0004 비트-결정론 복제는 C++ 승격에서 부활. 존 N개·동적 경계 후속 |
| 3 | 게임 서비스 | 가방 · 채팅 · 길드 · 거래소 · 우편 · 랭킹 | ⬜ 미착수(프로세스 분리가 실체화 → 별 프로세스 서비스로 분리 가능) |
| 4 | 버스 | 이벤트 버스 | 🟡 0004 전송 substrate(`engine/Net`) +0008 핸드오프/델타 라우트 +0010 IPC +0011 실 TCP 소켓 와이어 +0012 토픽 pub/sub 버스·소켓 층 열화 내성(주소 무지·발행자 무수정). 다중 브로커 분산·지연/재정렬·서버간 인증 후속 |
| 5 | 코디네이션 | 세션/프레즌스 · 오케스트레이터 | 🟡 0001 레지스트리 +0009 Orchestrator(lease·failover) +0010 broker(lockstep 배리어) +0011 broker=TCP 서버 +0012 broker=버스 허브·분단 감지·펜싱 +0013 진짜 kill(소켓 close 감지)·타임아웃 추측(거짓 사망)·epoch 펜싱·재-provisioning(split-brain 0·소유자 1). broker 물리 분산·분산 epoch·진짜 비동기(배리어 해제) 후속 |
| 6 | 데이터 | 캐시 · DB · write-behind | ⬜ 영속 0(세계가 프로세스와 함께 죽는다) |

---

## 6. 빠른 참조

- 네트워크 인프라 큰 그림·계층 책임·씨앗 매핑·척추 체크 5항: [SPINE.md](SPINE.md)
- **도구·기술 스택(각 박스를 무엇으로 짓는가)·데이터 3분할·교차 관심사**: [TOOLS.md](TOOLS.md)
- **의외의 발견 / 정직한 한계 전문**: 각 `step-NNNN.md` (STATE는 중복 보관하지 않음 — 위 §3·§4가 현재 load-bearing 요약).

---

## 7. INDEX — 시리즈 검증 현황 (유일하게 append, 1행/step)

| step | 더한 한 조각 | 결과 (회귀 0 전제) |
|---|---|---|
| [0001](step-0001.md) | 최소 골격 토폴로지 (4박스+세션 계약) | 통과 · E2E 수명주기·은닉 0/47·비트 결정론(골든 5종) |
| [0002](step-0002.md) | 존 결정론 복제 (추종자 존+입력 미러 탭) | 통과 · 0/60 desync·상태 전송 0바이트·회귀 0 |
| [0003](step-0003.md) | Sim 인터페이스 동결 (ISimCore v1·2구현·단일 seam) | 통과 · 표현 무관 비트 동일·구체 시뮬 참조 0건 |
| [0004](step-0004.md) | 현실 전송(지연·손실·재정렬)+논리-tick 스케줄링 (+engine/ 추출) | 통과 · 타이밍↔내용 분리·redundancy 1→3 desync 597→0 |
| [0005](step-0005.md) | 멀티 클라+AOI 브로드캐스트 (EntityZone, 시뮬 0) | 통과 · seen==트루스·절감 51~68%·재현 결정론 |
| [0006](step-0006.md) | 공간 분할+존 간 권위 핸드오프 (EntityZone ×2) | 통과 · 소유자+in-flight=1·이중쓰기/공백 0·지연 1tick |
| [0007](step-0007.md) | 증분 AOI (enter/exit/update+누적 재구성) | 통과 · 증분≡전체 288/288·대역폭 절감 19~33% |
| [0008](step-0008.md) | 전송 열화 아래 핸드오프+반응적 복원 (ack/재전송·seq/NAK/keyframe) | 통과 · 손실 0~30% 권위 위반 0·최종 desync 0 수렴 |
| [0009](step-0009.md) | 추종자 승격 failover (shadow 복제·lease 감지·승격) | 통과 · 사망→소유자 1 회복·bounded gap→0·이중쓰기 0 |
| [0010](step-0010.md) | 프로세스 경계 현실화 (실 프로세스/IPC·broker lockstep) | 통과 · 멀티프로세스=인프로세스 비트 동일·9 구분 pid·공유 메모리 0 |
| [0011](step-0011.md) | 실 TCP 소켓 전송 현실화 (IPC 파이프→TCP·프레이밍) | 통과 · 실 소켓=인프로세스 비트 동일·결정론=*순서*의 함수 |
| [0012](step-0012.md) | 버스 분산+실 네트워크 열화 내성 (토픽 pub/sub·드롭+resend·분단·펜싱) | 통과 · 링크 분단=deathTick 비트 동일·소유자 1·split-brain 0·멱등 effectively-once |
| [0013](step-0013.md) | 진짜 프로세스 kill 아래 failover (실 child.kill·소켓 close/타임아웃 추측 감지·재-provisioning·거짓 사망 epoch 펜싱) | 통과 · 진짜 kill=인프로세스 deathTick 비트 동일·재충원 divergence 0·N≥2·거짓 사망 epoch 펜싱 split-brain 0·소유자 1 |
