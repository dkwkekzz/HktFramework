# STATE — 살아있는 현재

> "지금 어디까지 왔고 다음은 무엇인가"의 **단일 진실 원천(SSOT)**.
> 큰 목표·규칙은 [CLAUDE.md](CLAUDE.md) · 척추(인프라 큰 그림)는 [SPINE.md](SPINE.md) · 각 step 상세 기록은 `step-NNNN.md`.
>
> **구조 규칙(에이전트 효율)**: 이 파일은 *고정 크기 대시보드*다. §1~6은 step을 닫을 때마다 **덮어쓴다(rewrite)** — 누적하지 않는다. 오직 §7 INDEX만 1행 append. 발견/한계의 *전문*은 STATE가 아니라 `step-NNNN.md`에 산다. 마커: ✅해소 🟡부분 🔴열림(최우선) ⬜백로그 🔧노브.

---

## 1. NOW

- **닫힌 step**: [step-0009](step-0009.md) — 추종자 승격(failover). 0008 반응적 복원은 *전송 열화*는 메우나 *영구 단절*(존 사망)은 못 메운다(어떤 재전송도 죽은 존의 권위를 못 살린다). 0002 가 세운 추종자 존(입력열만으로 권위와 같은 상태로 수렴)을 *사망 감지 시 권위로 승격*시킴: 추종자가 **shadow 복제**(입력 replay 평행 세계·상태 전송 0)로 권위와 lockstep 유지 → **Orchestrator** 가 **lease 하트비트** 결손으로 사망 감지 → **승격**(shadow→authority + 강제 keyframe 재동기 + sibling relink + 게이트웨이 reroute + 반대편 추종자 retire=펜싱). failover=false 면 0008 비트 동일. 0008 의 ①~④·복원·은닉은 그대로, *복제·승격층*만 추가. `engine/` 무수정.
- **한 줄 상태**: 더미 존이 *사망*(영구 단절)을 견딘다 — 추종자 입력 replay 복제(상태 전송 0·divergence 0) + orch lease 감지 + 승격(강제 keyframe 재동기). 핵심: ① failover 는 0008 prevention 과 *정반대* — 사망은 사망 전 감지 불가라 *bounded gap*(감지 창)은 불가피, "공백 0"이 아니라 "*bounded gap → 완전 회복*" ② 상태 보존이 *공짜* — 추종자가 사망 너머로도 lockstep 복제(상태 전송 0바이트, 0002 그대로)라 승격이 곧 연속 ③ 사망 복원과 손실 복원이 *같은 keyframe 장치* 공유(0008 needsKeyframe 재사용) ④ orch 단일 승격 = 펜싱(이중쓰기 0). 검증: 회귀 0(`reg`, fo=off → 0008 비트 동일 rec on/off·zones 1·2)·복제 충실도(`replica`, 추종자 매 tick 비트 동일 ents·무사망 0008 불변)·권위 연속(`failover`, 존 사망 → OFF 영구 소실 1~4 vs ON 소실 0·bounded gap 3 tick→소유자=1·이중쓰기 0·전 생존·복제 충실도 0·승격 1)·뷰 복원(`viewrec`, 승격 후 강제 keyframe → 최종 desync 0)·감지 창(`window`, leaseTimeout 2~6 → gap 창 비례·항상 최종 0)·은닉(`hide`, lease/promote/orch 누설 0)·재현(`repro`, 승격 타이밍도 시드 결정론). **의도적 경량화 유지**. **단 (a) bounded gap 은 불가피(failover=회복≠예방)** **(b) 제어 평면(lease/promote) 신뢰 가정** **(c) 거짓 사망·split-brain 펜싱 미검증(사망=진짜 가정)** **(d) 추종자 N=1·재-provisioning 없음(2번째 사망 불가)** **(e) 프로세스 경계·존↔존 ghost·비트-결정론 복제 여전히 미착수(0008 상속)**.
- **다음**: step-0010 (§2).

---

## 2. NEXT — step-0010 가설 (후보, 권위는 이 절)

**프로세스 경계 현실화·원격 멀티프로세스 E2E — 재정렬 목표("모든 서버 headless·서로 완전 통신·원격 검증")의 *본진*. 지금 전 서버(존·추종자·orch·게이트웨이·로그인·레지스트리)는 단일 Node 프로세스 내 액터(`engine/Net` 인메모리 큐)다. host 태그 경계를 *실 전송*(소켓/IPC)으로 갈아끼워 진짜 멀티프로세스로 분리하고, 원격(이 환경/CI)에서 한 줄로 띄워 E2E 로 검증한다.**

0001~0009 가 netcode 프로토콜·토폴로지(골격·결정론 복제·동결 Sim·현실 전송·AOI·분할/핸드오프·증분·반응적 복원·failover)를 *단일 프로세스 헤드리스 하니스*로 증명했다. 이제 [TOOLS.md](TOOLS.md) §4·§6 의 "프로토콜 우선 증명 → host 경계 현실화" 순서에 따라 *프로세스 경계*를 현실화할 때다. `engine/Net` 의 인메모리 큐를 *직렬화 가능 메시지 경계*(이미 전 메시지가 JSON 직렬화 가능 — §4 transport seam)는 그대로 두고, host 태그를 실제 프로세스/소켓 분리로 교체. 핵심: **인터페이스 무변경**(net-core 계약 그대로) + **원격 검증 루프 보존**(`node verify.js` 가 멀티프로세스를 띄워 같은 4기둥 검증).

**검증할 것**: ① **회귀 0** — 인프로세스 모드면 0009 비트 동일(전송 seam 교체가 비-침습). ② **프로세스 분리** — 각 서버가 별 프로세스/소켓으로 띄워져 직렬화 메시지로만 통신(공유 메모리 0). ③ **E2E 동치** — 멀티프로세스 실행이 인프로세스와 같은 상태 해시(전송 seam 이 의미 보존). ④ **원격 한 줄 기동** — 이 환경/CI 에서 `node verify.js` 가 전 프로세스를 띄워 ALL OK(로컬 엔진·특정 머신 비종속). ⑤ **은닉·재현** 유지.

> 후속 한 조각: **제어 평면 손실 아래 failover**(0009 §8.2·8.3: lease/promote 손실·거짓 사망·split-brain 펜싱·추종자 재-provisioning) · **존 N개·동적 경계**(0006 §8.2) · **존↔존 ghost 증분화·복원**(0007 §8.3·0008 §8.6) · **가방 서비스 분리**(서비스 계층 첫 박스) · **이벤트 버스**(존↔서비스 pub/sub) · **UE-모듈-free 코어 분리**(C++ 교체 선결, §3 🔴).

**병행 백로그(블로킹 아님)**: ⬜ 다중 클라 결정론 복제·예측(0001~0004 무거운 모델은 C++ 승격에서 부활) · ⬜ 존 N개·동적 경계(2 존·전 존 브로드캐스트 해소, 0006 §8.2) · ⬜ 존↔존 ghost 증분화·복원(0007 §8.3·0008 §8.6) · ⬜ cross-zone 지연 완화(클라 예측/보간, 0007 §8.2) · ⬜ 제어 평면 손실 복원(0008 §8.2·0009 §8.2) · ⬜ 거짓 사망·split-brain 펜싱·추종자 재-provisioning(0009 §8.3·8.4) · ⬜ 가변 지연 재정렬 아래 seq 복원(0008 §8.4) · ⬜ 서버간 인증/버스(0001 §8.3) · ⬜ 재접속·티켓 만료(0001 §8.5) · ⬜ 가방 서비스 분리(존 tick 밖) · ✅ 시각 관찰 셸(0004 수립, 0005~0009 재사용: `engine/panel-kit.js` 키트 + `step-NNNN.html` — dual-mode·헤드리스 ASCII).

**빌드 인프라 — `engine/` 추출 완료(0004), 0005~0009 무수정 재사용**: 공통 하니스(시드 PRNG·FNV·전송 모델 `Net`·엣지/코디 스텁·동결 `ISimCore` 2구현 = `engine/index.js`, 시각 관찰 키트 = `engine/panel-kit.js`)를 `engine/` 한 곳에. 코어는 **dual-mode**(Node `require` + 브라우저 `<script>` 전역). 0009 은 `engine/` 무수정 — net-core 가 추종자 shadow 복제(입력 replay 평행 세계)·Orchestrator(lease 감지·승격)를 더하고, 게이트웨이에 입력 미러/reroute 를 입혔다(결정론 VM 미사용 — 더미 경량화). step-0010 의 net-core 는 프로세스 경계(실 전송 seam)를 현실화한다. 출발 템플릿은 `step-0009/net-core.js`·`step-0009/panel.js`.

---

## 3. OPEN GAPS — 열린 격차 (계층은 [SPINE.md](SPINE.md) §6, 매 step 하나씩 메움)

| 마커 | 격차 | 계층 | 상태 |
|---|---|---|---|
| ✅ | **골격 토폴로지** | 전체 | step-0001 해소 — 4박스 + 세션 수명주기 계약 + 은닉(누설 0) + 비트 결정론(골든 해시 5종). |
| ✅ | **존 결정론 복제** | 월드 | step-0002 해소 — 추종자 존이 입력열만으로 매 tick 권위와 같은 해시(전 60 tick 사슬 일치·골든 도달), 상태 전송 0바이트. 회귀 0(off=골든, on=비-침습). |
| ✅ | **Sim 인터페이스 동결** | 월드 | step-0003 해소 — 존 시뮬이 동결 `ISimCore` v1 계약(spawn/despawn/tick/liveIds/serialize+결정론) 뒤로. 더미(객체맵)·array(SoA) 2구현이 표현 무관 비트 동일 골든, 교체=`run.makeSim` 단일 seam(인프라 구체 시뮬 참조 0건). 단 인터페이스는 *최소*(단일 엔티티) — 다중 엔티티/이벤트면 v2(0003 §8.1). |
| ✅ | **현실 전송 위 결정론 복제 (복제 경로)** | 월드/전송 | step-0004 해소 — `engine/Net` 전송 모델(지연·손실·재정렬+중복송신 신뢰성) + `ZoneHost` 논리-tick 스케줄링(`at` 도장·`applyTick=at+INPUT_DELAY`). 타이밍↔내용 분리 증명(`sep`: 스케줄 사슬 타이밍 불변·desync 0 vs naive 갈림). 전송 ON 권위·추종자 desync 0, 열화 곡선(지연 절벽·손실 복원). 단 *복제 팬아웃에만* — 뷰 경로/클라 예측·반응적 복원은 후속(0004 §8.1·8.2). |
| ✅ | **AOI 이벤트 브로드캐스트 (전체 스냅샷 → 증분)** | 월드 | step-0005~0006 전체 스냅샷 + step-0007 **증분(enter/exit/update)** 해소 — `EntityZone` 가 *시뮬 없이* 위치 맵 + 반경 R AOI 로 주변 클라 갱신, 0007 은 변화량 비례(정지 시 0·증분≡전체 매 tick·세션 이주 reset). 단 *정적 반경·존↔존 ghost 는 여전히 전체*(0007 §8.3). |
| ✅ | **공간 분할 + 권위 핸드오프** | 월드 | step-0006 해소 — 존 2개([0,8)/[8,16))·경계 띠 ghost 상호 구독·엔터티가 경계 넘을 때 권위 release+acquire 쌍 거래 이주(소유자+in-flight=1·이중쓰기/공백 0·지연 1tick)·경계 AOI 연속(seen==전 존 트루스 6/6). 단 *존 2개·1D·정적 경계·전송 행복*(0006 §8.2·8.3). |
| ✅ | **전송 열화 아래 핸드오프·증분 = 공백/desync 위험** | 월드/전송 | step-0008 해소 — 핸드오프 ack/재전송(권위-of-record outbox → 토큰 유실에도 공백 0·이중쓰기 0) + 증분 seq/NAK/keyframe(+heartbeat) 재동기(델타 유실 → 최종 desync 0). 손실 0~30% 권위 위반 0 불변·desync 0 수렴(`authrec`·`deltarec`·`curve`). 단 *영구 단절(손실 1.0)=failover 의 일*·꼬리 유실 heartbeat 대역·제어 평면 신뢰 가정(0008 §8). |
| ✅ | **추종자 승격(failover)** | 월드 | step-0009 해소 — 추종자 shadow 복제(입력 replay·상태 전송 0·divergence 0) + Orchestrator lease 감지 + 승격(강제 keyframe 재동기). 존 사망 → OFF 영구 소실 vs ON bounded gap(감지 창)→소유자=1 회복·이중쓰기 0(펜싱)·전 생존·desync 0. 단 *bounded gap 불가피(회복≠예방)·제어 평면 신뢰·거짓 사망/split-brain 미검증·추종자 N=1*(0009 §8). |
| 🔴 | **프로세스 경계·원격 멀티프로세스 E2E 미실현 (최우선·step-0010)** | 전체 | 재정렬 목표(모든 서버 headless·서로 완전 통신·원격 검증)의 *본진*. 전 "서버"가 단일 Node 프로세스 내 액터이고 `engine/Net` 인메모리 큐로만 통신. 척추 ⑤ 는 `node verify.js`(헤드리스·원격 ALL OK)로 *충족*하나 host 태그 경계가 아직 *실 프로세스/소켓 분리*는 아니다. 프로토콜·토폴로지를 헤드리스로 증명(0001~0009)했으니 이제 host 경계를 실 전송(소켓/IPC)으로 현실화·원격 멀티프로세스 E2E 로 검증 — **step-0010**([TOOLS.md](TOOLS.md) §4·§6). |
| 🔴 | **C++ 시뮬 코어 headless 빌드 (원격 검증 미충족 위험)** | 월드 | 최후에 교체될 결정론 시뮬 코어가 UE 모듈(`Core`·`CoreUObject`·`GameplayTags`·`Json` 등)에 링크되면 'UObject 0' 이라도 UE 소스/UBT 없이 빌드 불가 → headless·원격 검증 불가. UE-모듈-free 코어 분리 또는 얇은 헤드리스 shim 필요(불변, §4). 더미(Node)/array 스탠드인 단계엔 비차단, **C++ 승격의 선결**(0003 §8.2). |
| ⬜ | **로그인 큐·티켓 실체화** | 엣지 | 스텁→계정 검증·대기열·티켓 만료(0001 §8.5). |
| ✅ | **증분 AOI(관심영역 구독)** | 월드 | step-0007 해소 — view_delta(enter/exit/update)로 대역폭이 변화량 비례(정지 시 0)·증분≡전체 매 tick(288/288)·세션 이주 reset(stale 0)·절감 19~33%. 단 *cross-zone 지연 완화는 안 옴*(증분은 대역폭만, 같은 파이프라인)·*손실 시 desync*(0007 §8.1·8.2). |
| ⬜ | **다중 클라 결정론 복제·예측** | 월드 | 0002~0004 의 결정론 복제·예측은 *동결 인터페이스 뒤 C++ 시뮬 코어 승격*에서 부활 — 더미(0005)는 경량 라우터라 비트-결정론 미사용. 다중 클라 intent 인터리빙·예측/롤백(0001 §8.6). |
| ⬜ | **서버간 인증 없음** | 버스 | 존이 게이트웨이 발신을 암묵 신뢰(0001 §8.3) — 분산 시 서버간 인증/버스 필요. |
| ⬜ | **가방 서비스 분리** | 서비스 | 아이템 원장을 존 tick 밖으로 — 트랜잭션·dupe 방지·존 넘는 거래. |
| ⬜ | **이벤트 버스** | 버스 | 서버간 pub/sub — N×N 직접 결합 제거. |
| ⬜ | **채팅·길드·거래소·우편·랭킹** | 서비스 | 가방 분리 패턴의 반복 적용(팬아웃·비-시뮬 상태). |
| ⬜ | **세션/프레즌스 + 오케스트레이터** | 코디네이션 | "누가 어디에" SSOT · 존 배치·부하 분산·인스턴스 spawn. |
| ⬜ | **캐시 + write-behind 영속** | 데이터 | 동기 I/O 를 모든 tick 에서 제거. 세계가 세션보다 오래 산다. |
| ⬜ | **크래시 복구·재접속·late-join** | 전체 | 영속에서 뷰/권위 재구성 — 소실 권위의 고리 닫기. |

> **상시 렌즈 — 척추** ([SPINE.md](SPINE.md) §5 필독): 매 step은 verify 4기둥 + 척추 체크 5항(① 신성한 tick ② 결정론 코어 ③ 권위 단일 소유 ④ 은닉·단일 연결 ⑤ headless·원격 검증)으로 판정. 분리 판정 기준: *그 일이 존 시뮬 tick 과 같은 박자로 돌아야 하는가?*

---

## 4. DURABLE CONSTRAINTS — 모든 step이 지킬 정전(canonical) 사실

> 여러 step이 반복 참조하는 불변. 새 step은 이걸 어기면 척추에서 벗어난 것 — 재설계.

- **회귀 0**: 새로 더한 항을 끄면(플래그=0) 직전 step과 **비트 단위 동일**(verify `reg`, maxDiff=0).
- **신성한 tick**: 존 시뮬 tick 안에 시뮬 외 작업(동기 I/O·인증·트랜잭션·팬아웃) 금지 — 전부 비동기 서비스/버스 경유.
- **결정론 코어**: 세계 상태의 쓰기 경로는 intent→결정론 시뮬 코어 하나. 같은 시드·로그 → 같은 해시. **`Math.random` 금지, 시드 의사난수만.**
- **권위 단일 소유**: 모든 상태 조각의 쓰기 권위는 정확히 한 곳(매 tick 소유자=1). 이동은 release+acquire **쌍 거래**/트랜잭션.
- **은닉·단일 연결**: 클라는 게이트웨이만 안다. 서버간은 버스/명시 인터페이스만 — 타 서버 내부·DB 직접 접근 금지.
- **수렴(desync 0)**: 클라 예측 뷰는 권위 재현으로 수렴. 겹친 뷰의 참여자는 조정 후 일치.
- **복제 = 재현, 상태 전송 아님**: `(seed+params+intent 로그)` 가 기본. 전체 스냅샷은 late-join·복구의 최후 수단.
- **Sim = 인터페이스 우선, C++ 최후 교체**: 존 시뮬은 *동결된 Sim 인터페이스* 뒤에 산다 — 오늘은 더미 구현(헤드리스), 인프라 전체를 원격 E2E 로 세운 뒤 인터페이스 무변경으로 얇은 C++ 호스트로 *교체만*. C++ 선-구축으로 원격 검증 루프를 끊지 않는다 ([TOOLS.md](TOOLS.md) §4·§6). 더미는 throwaway 가 아니라 인터페이스의 *첫 구현*.
- **headless·원격 검증(게임 시뮬 포함, 협상 불가)**: 모든 서버 동작 검증은 *결정론 시뮬 코어까지* Unreal Engine·GUI 없이 headless 로, 원격(이 환경/CI)에서 가능해야 한다. 더미=Node 라 자명히 충족 — C++ 승격 경로도 시뮬 *검증*에 UE 모듈을 링크하지 않는다. **'UObject 0' 으로는 부족**: 결정론 시뮬 코어가 UE 모듈(`Core`·`CoreUObject`·`GameplayTags`·`Json` 등)에 링크되면 headless 빌드 불가(§3 격차) → UE-모듈-free 코어 분리 또는 얇은 헤드리스 shim 으로 충족. 'UE 없이, 원격에서 빌드·통신·검증되는가'가 모든 박스(시뮬 포함)의 통과 조건 ([TOOLS.md](TOOLS.md) §1·§4).
- **수치 = verify 출력**: 모든 문서 수치는 시드 [42, 7, 1234, 99, 2026] 평균으로 재현.
- **코어 netcode 불변**: 결정론 시뮬 코어 순수성(UObject 0 — headless 빌드 가능) · 서버 권위(클라 읽기전용) · ISP 3-Layer · 시뮬 상태 직접쓰기 금지(intent 경유). 인프라는 *확장*하되 *깨지 않는다*.
- **한 step = 한 조각**: 더 떠올라도 다음으로 전가. 새 코어는 직전 코어를 잇고 박스/계약 하나만 더한다.
- **도구 = tick 판정 + 원격-검증** ([TOOLS.md](TOOLS.md)): tick 동기는 C++(결정론 시뮬 코어), 그 외는 원격에서 빌드·헤드리스 검증되는 도구(Go·Node·컨테이너). 시뮬만 무거운 빌드로 격리(시뮬 코어 "UObject 0" 순수성 = 얇은 헤드리스 호스트로 CI 빌드 가능 = 빌드 자산).
- **데이터 3분할** ([TOOLS.md](TOOLS.md) §3): ① 월드 영속 = intent 로그(event sourcing) + 스냅샷 ② 트랜잭션 진실 원천(가방·계정) = PostgreSQL→분산 SQL ③ Redis = 휘발/캐시(진실 원천 아님). 월드 상태를 DB 행으로 저장하지 않는다.

---

## 5. 6계층 진행 현황 ([SPINE.md](SPINE.md) §6)

| # | 계층 | 박스 | 상태 |
|---|------|------|------|
| 1 | 엣지 | 로그인/인증 · 게이트웨이 | 🟡 **0001 스텁 수립** — 일회성 티켓·단일 연결·은닉(박리) 계약 성립. 대기열·만료·재접속 후속 |
| 2 | 월드 | 존 · 인스턴스 (분할·AOI·조정·핸드오프) | 🟡 **0001 존 VM + 0002~0004 결정론 복제(현실 전송)·동결 Sim + 0005 AOI + 0006 공간 분할·권위 핸드오프 + 0007 증분 AOI + 0008 반응적 복원 + 0009 추종자 승격(failover)** — 0002~0004 의 비트-결정론 복제·예측은 *C++ 시뮬 코어 승급*에서 부활(동결 인터페이스 뒤), 0005~0009 더미는 경량 `EntityZone`(위치 맵 + AOI)로 라우팅·관심영역·수명주기·은닉·분할/핸드오프(소유자=1)/증분(변화량 비례)·손실 복원(핸드오프 ack·재전송=권위-of-record / 증분 seq·NAK·keyframe)·**사망 복원(추종자 shadow 복제=입력 replay·상태 전송 0 → orch lease 감지 → 승격=강제 keyframe·bounded gap→소유자=1 회복)**을 세움. **다음: 프로세스 경계 현실화(0010)** · 존 N개·동적 경계는 후속 |
| 3 | 게임 서비스 | 가방 · 채팅 · 길드 · 거래소 · 우편 · 랭킹 | ⬜ — 전부 존 프로세스 안(분리 전) |
| 4 | 버스 | 이벤트 버스 | 🟡 **0004 전송 substrate(`engine/Net`) + 0008 핸드오프·델타 라우트로 확장** — 지연·손실·재정렬+신뢰성 전송 모델(복제 팬아웃 + 핸드오프·증분 라우트, `routeFilter`). pub/sub 버스·서버간 인증은 미착수(0001 §8.3) |
| 5 | 코디네이션 | 세션/프레즌스 · 오케스트레이터 | 🟡 **0001 레지스트리 스텁 + 0009 Orchestrator(failover)** — 티켓 일회성·세션 SSOT·수명주기(closed) + lease 하트비트 감시·사망 감지·단일 승격(펜싱). 존 배정 고정·부하 분산·재-provisioning 은 후속(0001 §8.4·0009 §8.4) |
| 6 | 데이터 | 캐시 · DB · write-behind | ⬜ — 영속 0(세계가 프로세스와 함께 죽는다) |

---

## 6. 빠른 참조

- 네트워크 인프라 큰 그림·계층 책임·씨앗 매핑·척추 체크 5항: [SPINE.md](SPINE.md)
- **도구·기술 스택(각 박스를 무엇으로 짓는가)·데이터 3분할·교차 관심사**: [TOOLS.md](TOOLS.md)
- **의외의 발견 / 정직한 한계 전문**: 각 `step-NNNN.md` (STATE는 중복 보관하지 않음 — 위 §3·§4가 현재 load-bearing 요약).

---

## 7. INDEX — 시리즈 검증 현황 (유일하게 append, 1행/step)

| step | 더한 한 조각 | 결과 (회귀 0 전제) |
|---|---|---|
| [0001](step-0001.md) | 최소 골격 토폴로지 (4박스 + 세션 계약) | **통과** — E2E 수명주기(뷰 21·intent 20/20·closed)·은닉 0/47·비트 결정론(자기 재현, 골든 5종)·권위 위반 0·무자격 3경로 전부 존 도달 전 차단. **정직**: 결정론≠복제(→0002)·전송 행복경로만 |
| [0002](step-0002.md) | 존 결정론 복제 (추종자 존 + 게이트웨이 입력-미러 탭) | **통과** — 권위·추종자 매 tick 사슬 일치(0/60 desync·골든 도달)·상태 전송 0바이트(입력 22 msgs)·권위 보존(권위=1·추종자 자기권위 0)·회귀 0(off=골든, on=비-침습). **정직**: 전송 행복경로만(→0003)·승격 없음·N=1 바이트이득 7.7% |
| [0003](step-0003.md) | Sim 인터페이스 동결 (ISimCore v1 + 더미·array 2구현 + 단일 교체 seam) | **통과** — 회귀 0(추출 비-침습, 골든 비트동일)·계약 동결(`conf`: 더미·array seed-only 결정론·수명주기·표현 무관 trace 비트동일)·단일 seam(`swap`: 인프라 7클래스 구체 시뮬 참조 0건, makeSim 만 바꿔 교체→골든·E2E 불변)·0002 전 불변 유지(사슬 0/60·권위 0·desync 0·은닉 0). **정직**: 인터페이스 최소(단일 엔티티→v2)·array 는 C++ 스탠드인·전송 행복경로만(→0004) |
| [0004](step-0004.md) | 현실 전송(지연·손실·재정렬) + 논리-tick 입력 스케줄링 (+ `engine/` 추출) | **통과** — 회귀 0(전송끔+ID=1 → 0001/0003 골든 비트동일, 추출 비-침습)·분리(`sep`: 스케줄 사슬 타이밍 5종 불변·desync 0 vs naive 5종 갈림·desync ~262)·전파/권위(`prop`·`auth`: 전송 ON 권위·추종자 desync 0·권위=1·추종자 자기권위 0)·열화 곡선(`curve`: 지연 절벽 예산 7tick·손실 복원 redundancy 1→3 desync 597→0). **정직**: 전송 복제경로만(뷰/예측 →0005)·복원 선제 redundancy 만(반응적 없음)·단일 엔티티·INPUT_DELAY 고정 |
| [0005](step-0005.md) | 멀티 클라 + AOI 이벤트 브로드캐스트 (간단한 더미 서버 — `EntityZone`, 시뮬 0) | **통과** — AOI 정확(`aoi`: 클라 seen == 반경 R 그라운드 트루스 4/4·절감 51~68%)·반경 곡선(`radius`: 절감 81%→0% as R 1→16)·수명주기(`life`: 떠난 엔터티가 주변 AOI 에서 사라짐·존 제거)·은닉(`hide`: 내부 토폴로지 누설 0)·재현(`repro`: 같은 시드 → 같은 위치+AOI, `Math.random` 0). **정직(의도적 경량화)**: 비트-결정론 복제 없음(재현성만 — 복제/예측은 C++ 승급에서 부활)·AOI 매 tick 전체 스냅샷(증분 없음)·단일 존·정적 반경·전송 행복 |
| [0006](step-0006.md) | 공간 분할 + 존 간 권위 핸드오프 (`EntityZone` ×2 — 분할·경계 ghost·토큰 보존 핸드오프) | **통과** — 회귀 0(`reg`: zones=1 → 0005 net.log·상태 비트 동일 5/5)·권위 보존(`handoff`: 횡단 17~20회·매 tick 소유자+in-flight=1·이중쓰기/공백 0·지연 1tick)·경계 AOI 연속(`aoi`: seen==전 존 합산 트루스 6/6·절감 67~71%)·수명주기(`life`: 떠난 엔터티 주변 AOI 소멸·전 존 제거)·은닉(`hide`: zone2/handoff/ghost/sessionId 누설 0)·재현(`repro`). **정직**: AOI 정확성=*수렴 후* 성질(cross-zone 지연~3tick, 매 tick 아님)·존 2개·1D·정적 경계·핸드오프/전송 행복(열화 시 토큰 유실=권위 공백 미검증) |
| [0007](step-0007.md) | 증분 AOI (전체 스냅샷 → enter/exit/update + 클라 누적 재구성 + 세션 이주 reset) | **통과** — 회귀 0(`reg`: inc=off → 0006 net.log·상태 비트 동일 zones 1·2)·증분 정확(`delta`: 증분 재구성==전체 스냅샷 288/288 셀·불일치 0)·대역폭(`bw`: 절감 19~33%·정지 꼬리 8tick=0)·경계 AOI 연속(`aoi`: seen==전 존 트루스 6/6)·권위 보존(`handoff`: 0006 불변 유지·소유자+in-flight=1)·수명주기(`life`: exit 증분)·은닉(`hide`: view_delta 누설 0)·재현(`repro`). **정직**: 증분 누적이라 *손실 시 desync*(자가치유 상실→반응적 복원 선결)·cross-zone 지연 완화 안 옴(증분=대역폭만)·존↔존 ghost 여전히 전체·절감률=시나리오 의존(동시 이동 worst case) |
| [0008](step-0008.md) | 전송 열화 아래 핸드오프 + 반응적 복원 (핸드오프 ack/재전송=권위-of-record · 증분 seq/NAK/keyframe/heartbeat · 전송 모델을 핸드오프·델타 라우트에) | **통과** — 회귀 0(`reg`: rec=off → 0007 net.log·상태 비트 동일 zones 1·2)·권위 보존(`handoff`: 무손실 소유자+in-flight=1·ack==handoff·증분≡전체)·권위 복원(`authrec`: 토큰 유실 0.2 → OFF 소실 1~3(=유실 토큰 수) vs ON 공백 0·이중쓰기 0·소실 0·재전송 1~8)·증분 복원(`deltarec`: 델타 유실 → OFF 복원 장치 0 vs ON 최종 desync 0·NAK 19~35·keyframe 18~34)·열화 곡선(`curve`: 손실 0~30% 권위 위반 0 불변·델타 desync 0 수렴·오버헤드 성장)·은닉(`hide`: NAK/seq 누설 0)·재현(`repro`: 손실 패턴도 시드 결정론·digest==0007=완전 수렴). 대조는 *구조적·시드-robust*(OFF 소실=클라 아바타 집합 기준·OFF 복원장치 부재·월드 타이밍 가드·유실 게이트, 1~400 스윕 무결, §8-7). **정직**: 꼬리 유실 heartbeat 대역("정지 0"과 긴장)·제어 평면(ack/NAK) 신뢰 가정·상수 지연(재정렬 격리)·손실 1.0=failover 의 일·존↔존 ghost·비트-결정론 복제 미착수 |
| [0009](step-0009.md) | 추종자 승격(failover) (추종자 shadow 복제=입력 replay 평행 세계 · Orchestrator lease 감지 · 승격=shadow→authority + 강제 keyframe + relink/reroute/retire) | **통과** — 회귀 0(`reg`: fo=off → 0008 net.log·상태 비트 동일 rec on/off·zones 1·2, 20/20)·복제 충실도(`replica`: 추종자 매 tick 비트 동일 ents divergence 0·상태 전송 0·무사망 0008 불변)·권위 연속(`failover`: 존 사망 tick40 → OFF 영구 소실 1~4 vs ON 소실 0·bounded gap 3tick→최종 0·이중쓰기 0·복제충실도 0·승격 1)·뷰 복원(`viewrec`: 승격 강제 keyframe 1~4 → 최종 desync 0)·감지 창(`window`: leaseTimeout 2~6 → gap 창 2.0~6.0 비례·항상 최종 0·소실 0)·은닉(`hide`: lease/promote/orch 누설 0)·재현(`repro`: 승격 타이밍도 시드 결정론). 대조는 *구조적·시드-robust*(ON 불변 1~200 스윕 무결·OFF=승격 장치 부재 구조 대조·소유 0 시드 vacuous 보고, §8-8). **정직**: bounded gap 불가피(failover=회복≠예방)·제어 평면(lease/promote) 신뢰 가정·거짓 사망/split-brain 펜싱 미검증(사망=진짜)·추종자 N=1 재-provisioning 없음·프로세스 경계/존↔존 ghost/비트-결정론 복제 미착수 |
