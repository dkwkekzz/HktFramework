# step-0457 — #4 실 net.step 배리어 실제 치환 7: 교차-tick 지연 jitter

> 직전 step-0456 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-barrier.js` — cfg `{delay, delayMax}`: 배리어가 *move* 를 확률적으로 1..delayMax tick 늦게 배달(손실 아님·항상 재enqueue·재지연 없음·past-end 방지) — 배달 타이밍을 tick 배리어에서 *분리*(배리어-free 진행 판). move 는 가환이라 늦게 적용해도 최종 월드 동일 → world==lockstep. `delayed` 계수.
- 계층: 코디네이션(#4). 배달 타이밍이 lockstep tick 에서 풀려도(jitter) 월드 수렴.
- OFF 플래그: `asyncBarrier`(미설정→net.step 그대로 = **reg 구조적 0**). verify.js 새 모드 `bardelay`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `bardelay` 5/5 OK — 4 client·단일 존·40% move 지연(1~4 tick): `run({asyncBarrier:{delay}})` worldDigest == lockstep Y·delayed 39~58(배달 타이밍 tick 분리)·moveDup 0.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(지연 jitter 무관·world==lockstep) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅. 이상 없음 — 배달 타이밍이 tick 배리어에서 풀려도(교차-tick 지연) move 가환성 덕에 월드 수렴. asyncBarrier OFF→reg 구조적 0.
