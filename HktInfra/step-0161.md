# step-0161 — 아이템 우편 발신 인출 leg1(mailInv — mailSend → 발신자 가방→우편 custody give)

> 직전 step-0160 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `inv`/`invMode` opts + `_custody(itemId, from, to, cause)` 헬퍼(거래소 `_custody` 0117 의 우편 판): invMode ON 이면 `mailSend` 가 아이템을 발신자 가방→`'mailcustody'`(의제 소유자·in-transit 보관)로 `item_req`/`give`. 가방이 권위·우편은 요청만(은닉). `gives` 계측. 계층:게임 서비스(우편↔가방 2-서비스).
- 배선: `topo-build.js` — `mailInv` 플래그·`inv:(mailInv&&inventory)?inventoryAddr:null`·`invMode:mailInv` 를 mail opts 로.
- OFF 플래그 `mailInv`(기본 false) → give 0·가방 무변경 = 0160 비트 동일(회귀 0). verify.js 새 모드 `exmlinv1`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlinv1`(발신자 x 가 item0·item1 pickup 후 둘을 아이템 우편 발신): gives=2·item0/item1 owner=`mailcustody`·발신자 잔여 없음·itemHeld=2 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖(우편·가방 박스) ②결정론 난수 0 ③권위 — 아이템 권위는 가방 단독(우편은 give 요청만·escrow 쌍 거래로 소유자=1 보존) ④은닉 우편→가방은 `item_req` 명시 인터페이스(직접 원장 접근 0) ⑤headless `node run.js` 통과 — 이상 없음. give 는 *fire-and-forget*(결과 회신·실패 보상 없음) — 발신자가 미소유면 가방이 거부(failedOps)하나 우편은 모름(낙관적 적재). saga 회신(0165)·실패 보상(0166)이 이 고리를 닫는다. 수령 입금(0162)·만료 반환(0163)·2-서비스 보존(0164) 후속.
