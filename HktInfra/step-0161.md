# step-0161 — 아이템 우편↔가방 leg1: 발신 시 발신자 가방 인출(mailInv·escrow custody)

> 직전 step-0160 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `_custody(itemId, from, to, cause)` 헬퍼(거래소 0117 `_custody` 의 우편 판) + `mailSend` 가 아이템 실은 통마다 `_custody(item, from, 'escrow')` 호출 → 가방에 `item_req{op:'give'}`. 아이템이 발신자 가방을 *실제로 떠나* escrow custody 로 이동(추상 escrow 0157~0160 → 실 2-서비스). 계측 `gives`. 계층:게임 서비스.
- `src/topo-build.js` — mail 박스에 `inv`(가방 주소)·`invMode`(mailInv) 배선. OFF 플래그 `mailInv`(기본 false) → give 0·가방 무변경 = 0160 비트 동일(회귀 0). verify.js 새 모드 `exmlinv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlinv`(발신자 x 가 item0·item1 mint 후 아이템 우편 2통 발신): ON gives/escrowXfers 2/2·item0·item1 소유자 escrow/escrow(가방 원장서 실제 이동)·OFF 소유자 x/x(추상 escrow)·itemHeld ON/OFF 2/2(우편 박스 회계 불변) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖) ②결정론 난수 0 ③권위 가방이 원장 권위·우편은 give 요청만(쌍 거래 release+acquire 는 가방 onMsg 원자) ④은닉 우편→가방 명시 인터페이스(item_req) ⑤headless `node run.js` 통과 — 이상 없음. give 회신(item_result)은 *아직 미수신*(fire-and-forget) — 회신 비동기 수신·미해결 추적은 saga(0165~·거래소 0121~ 류) 후속. 수령 입금 leg2(0162)·만료 반환 leg3(0163)·2-서비스 보존 capstone(0164) 후속.
