# step-0248 — 배치 SSOT 실배선(#51) 8: host 장애 복구(placeHostDown·비자발 re-acquire)

> 직전 step-0247 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placeHostDown{host, hosts[]}` op + `_hostDown`: 비자발적으로 죽은 host 의 모든 존을 생존 host 중 최소부하로 *재가동(re-acquire)*. 드레인(_drain·graceful migrate)과 달리 죽은 host 는 release 불가 → running 단일 키 재배치(공백 없이 한 존 정확히 한 host 회복). 결정(placement)도 함께 갱신해 drift 0. 생존 host 없으면 보류. 계수 `hostDowns`/`hostRescued`. 계층:코디네이션.
- OFF: `placeHostDown` 미수신이면 미발화=0247 비트 동일=회귀 0(새 op).
- verify.js 새 모드 `placehostdown`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placehostdown`**: 5시드 모두 A run 0·B 2·C 1·rescued 2·drift 0 — z1·z2@A·z3@B 에서 A 장애 후 z1→C·z2→B 재가동·3존 전부 생존·결정==집행.
- **결정론/권위/수렴**: `run.js all` ALL OK(18모드+classics).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면 ②결정론: 시드 PRNG·매 존 최소부하 재계산 결정론 ③권위 단일 소유: 복구 후 한 존 정확히 한 host(공백/중복 0) ④은닉: orch 내부 ⑤headless: node 검증. — 한계: running 은 orch 내 추상 레지스트리(실 EntityZone host 이주는 아님)·#51 의 paper→executed 격차를 *코디네이션 SSOT 층*에서 닫음(실 zone.js 핸드오프 트리거는 후속·#51 잔여). #51 거의 닫힘 → 다음 묶음(0241~0250) 방향은 `infra-review` 평결.
