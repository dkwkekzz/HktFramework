# step-0492 — svcexchangexfer 거래소↔가방 2-서비스 교차 회계 편입 (#16 라운드 3차 2)

> 직전 step-0491 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 거래소↔가방 2-서비스 교차 회계 capstone(옛 0130 판)을 재작성 편입: `run()` 을 exchange+saga opts 로 구동하고 **두 서비스 escrow 회계가 합치**하는지 단언하는 새 mode `svcexchangexfer` — 거래소 `giveOks` == 가방 `escrowXfers`(escrow 관여 transfer 수) + 아이템 무손실 단일 소유(minted1·item0 buyer 소유·dupe 0). 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 2/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcexchangexfer`: 5/5 OK — giveOks 2 == escrowXfers 2(두 서비스 교차 정합)·minted 1·item0 seller→escrow→buyer 무손실 단일 소유.
- spine ALL OK — svcexchangecap+svcexchangexfer 둘 다 서비스 계층 누적 회귀 포함.

## 척추 5항 + 한계

①tick: 거래소 존 tick 밖·박스 무변. ②결정론: 교차 회계 대조·Math.random 0. ③권위: giveOks==escrowXfers 가 아이템 escrow 이동의 두 서비스 단일 소유 합치를 재단언(이중 소유·손실 0). ④은닉: 거래소↔가방 give 경유. ⑤headless: node in-proc. — 이상 없음.
