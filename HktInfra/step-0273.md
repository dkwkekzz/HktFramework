# step-0273 — #51b 실 zone.js 브리지 2: _migrate 실 런타임 host 이주

> 직전 step-0272 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `_bridgeMigrate(zoneId,toHost)` 추가: 같은 EntityZone 핸들의 host 를 release(기존)+acquire(toHost) 원자 교체(zoneRuntimes 단일 키·**재생성 아님·상태/entity 보존**). 미가동·같은 host 멱등 no-op. 계층: 코디네이션.
- `orchestrator.js` — `zoneMigrations` 계측 필드 추가.
- `orch-placement.js` `_migrate` 에 가드 1줄: `zoneBridge` ON 이면 `_bridgeMigrate` 도 호출(실 핸들 이주). OFF→0272 비트 동일.
- OFF 플래그 `zoneBridge`(기존). verify.js 새 모드 `zonemigrate`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(zoneBridge OFF → maxDiff 0·비트 동일).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonemigrate`**: 5/5 seed — z1 A→C→A 두 번 이주에도 `zoneStarts`=2 **불변**(z1·z2 각 1회만 인스턴스화 = 재생성 아님·같은 핸들 재사용)·`zoneMigrations`=2·최종 z1 host=hostA·z2=hostB·runtimeCount=2(단일 소유).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 제어 평면(_migrate 집행)·존 tick 밖. ②결정론 — 핸들 이주는 host 필드 교체뿐(상태 불변). ③권위 단일 소유 — zoneRuntimes 단일 키 host 원자 교체(중간 공백/중복 0). ④은닉 — orch 집행 책임. ⑤headless — 실 EntityZone. 한계: entity 상태 *생존*의 직접 증명(이주 후 entity 보존)은 0274+(bridged 존에 entity 주입). 0273 은 zoneStarts 불변으로 *재생성 아님*(=상태 보존)을 구조적으로 증명. 이상 없음.
