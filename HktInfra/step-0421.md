# step-0421 — #61 업스트림 intent 실 클라 1: UpClient 골격 (zoneEnter 발신)

> 직전 step-0420 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/client.js` — `UpClient` 클래스 추가(발신 액터): DownClient(수신 전용)의 *발신* 짝. `onTick(S)` 가 joinAt 에 `zoneEnter` 를 게이트웨이로 발신(gatewayDirectZone 경로·합성 entityOps 대체). net.register 가 this.net/this.addr 주입·net.step() 이 매 tick onTick 구동.
- 배선: `topo-actors.js`(`case 'upclient': new UpClient(spec.opts)`)·`topo-build.js`(`upClients` opt→uc0.. 스폰)·`topo-run.js`(upclients 수집·반환).
- 계층: 엣지/월드(#61 — 업스트림 intent 경로를 실 클라 액터 발신으로).
- OFF 플래그: `upClients`=null(기본)→스폰 0→`this.order` 불변→회귀 0.
- verify.js 새 모드: `upclient`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (upClients 미설정→upclient 액터 0→run() 비트 동일).
- 가설 `upclient`: 5/5 — uc0 가 joinAt(3) 에 zoneEnter 발신(sent 1)→a1 이 실 존 z1 에 enter(orch.zoneEntityPos {5,5}).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉(클라는 게이트웨이만 발신) ⑤headless — 이상 없음. 골격 단계라 move/수신/leave 는 이후 step(0422~).
