# step-0336 — #9 후속: 다운스트림 egress 버퍼 자기-크기조정(게이트웨이 ack 가지치기)

> 직전 step-0335 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` `_drainZoneEgress` + `src/orch-bridge-init.js` — egress frame 을 세션별 *미-ack 버퍼* `zoneEgressBuf`(sessionId→[{dseq,frame}])에 보관(재전송 소스). `_onZoneViewAck(sid,dseq)`: ack 워터마크 단조 전진 + dseq≤워터마크 가지치기(자기-크기조정). 질의 `zoneEgressBufLen`/`zoneEgressAckedOf`·계측 `zoneEgressPruned`/`zoneEgressBufPeak`.
- `src/orch-control.js` onMsg — `zoneViewAck` 분기 → `_onZoneViewAck`. `src/gateway.js` `_recvZoneView` — dseq 수신 시 orch 에 `zoneViewAck{sessionId,dseq}` 발신.
- 계층: 코디네이션/엣지(②⑤·버스 ack 0040 의 다운스트림 판). 플래그 없음(egress 의 일부·zoneEgress OFF→egress/ack 0). verify.js 새 모드 `gwack`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneEgress OFF → egress/ack 0·zoneViewAck 분기 dead → baseline(0335) 비트 동일.
- `gwack` 5/5 OK — a1·a2 정상 인오더 흐름: `zoneEgressPruned` **4** == egressed 4·세션 버퍼 **0**(전부 ack·가지침)·ack 워터마크 == 마지막 dseq(s:a1·s:a2 각 1 = frame수−1).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: egress·ack 모두 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: ack/가지치기가 권위 무영향. ④ 은닉: ack 는 게이트웨이↔orch 전송 메타(명시 메시지). ⑤ headless: Node 순수·직렬화·reg 0.
- 한계: 정상 인오더라 버퍼 항상 즉시 가지쳐 peak 작음 — 손실 구간 자동 성장·재전송 발화는 후속(0337+). 이상 없음.
