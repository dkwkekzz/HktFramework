# step-0349 — #9 후속: 다운스트림 운영 대시보드(전파 평면 한눈 요약)

> 직전 step-0348 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway.js` `downstreamReport()` — 게이트웨이 다운스트림 평면 한눈 요약 `{rx, routed, dropped, gaps, resyncs, cleaned, sessions, isolated}`(0331~0348 지표 단일 집계·운영 관측). 읽기 전용.
- 계층: 엣지(④⑤·운영 가시성). 플래그 없음·읽기 전용 → baseline 비트 동일. verify.js 새 모드 `dcreport`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 집계 질의 → baseline(0348) 비트 동일.
- `dcreport` 5/5 OK — a1·a2 이동·`s:a1#2` 손실 후 report: `rx` **7**·`routed` **5**(rx>routed — 재전송 중복·gap frame 도 rx 집계, 인오더만 routed)·`dropped` **0**·`gaps` **1**·`resyncs` **1**·`isolated` Y·`sessions` 2·둘 다 `convergedTo` Y(desync 0).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 집계는 읽기. ② 결정론: 5종 동일. ③ 권위: 무영향. ④ 은닉: 게이트웨이 내부 회계 요약. ⑤ headless: Node 순수·reg 0.
- 한계: report 는 게이트웨이 측 다운스트림 지표(orch egress·정착은 orch downstreamSettled 별도). 이상 없음.
