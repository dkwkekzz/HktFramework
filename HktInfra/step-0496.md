# step-0496 — svcbankcap 길드 금고 원장 정합 편입 (#16 라운드 3차 6)

> 직전 step-0495 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 길드 금고 원장 정합 capstone(옛 0199 `bankConsistent` 판)을 재작성 편입: `run()` 을 guildService+guildBank opts 로 구동(2 길드 create→deposit/withdraw)하고 `bankConsistent`(itemId 단일 길드 소유·교차 중복 0·금고 내 중복 0) + 금고 회계(예치−인출==잔여·길드간 격리)를 단언하는 새 mode `svcbankcap`. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 6/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcbankcap`: 5/5 OK — g1 vault=[gold1](예치 gold0/gold1−인출 gold0=잔여 gold1)·g2 vault=[gem0](격리)·`bankConsistent` Y(itemId 단일 길드 소유·중복 0).
- spine ALL OK — svcbankcap 포함(길드 로스터+금고 서비스 계층 누적 회귀).

## 척추 5항 + 한계

①tick: 금고 존 tick 밖·박스 무변. ②결정론: 금고 원장·회계 대조·Math.random 0. ③권위: itemId 단일 길드 소유(교차 중복 0=이중 소유 0)·예치−인출==잔여로 원장 권위 단일 소유 재단언. ④은닉: 멤버→길드 버스 경유. ⑤headless: node in-proc. — 이상 없음(단 금고↔가방 escrow 연동은 #46·이 capstone 은 금고 *내부* 원장 정합).
