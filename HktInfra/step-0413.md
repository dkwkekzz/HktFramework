# step-0413 — #62 코드 합류 3: runMultiViaCoord — 코디네이터 단일 진입점 한 호출 구동

> 직전 step-0412 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-run.js` — `runMultiViaCoord(opts, deps, probe)` 추가: 0411 `coordSetup` + 0412 `coordScenarioFromOpts` + `coord.runScenario` 를 묶은 *단일 진입점*. 옛 runMulti(전 토폴로지 lockstep)의 zone-cluster 복원력 판을 한 호출로 구동·집계(`coherent`=runMultiCoherent·`desync`=coordDesync·`maxDesync`·`info`=clusterInfo·`probe`=shutdown 전 커스텀 측정). 호출자 손배선/shutdown 제거.
- 계층: 코디네이션(#62 코드 합류 — 코디네이터 구동을 cluster-run.js 가 소유한 한 함수로).
- OFF 플래그: 미부착(verify 만 호출·runMulti 옛 경로 불변) → 회귀 0.
- verify.js 새 모드: `runviacoord`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일).
- 가설 `runviacoord`: 5/5 — migrate+reprovision 한 호출 → coherent(runMultiCoherent) Y·coordDesync 0·migrations 1·reprovisions 1·probe 따뜻한 standby a1 {8,8} 실재.
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉 ⑤headless — 이상 없음(코디네이터 구동 래퍼·실 child_process E2E·시뮬 비-침습).
