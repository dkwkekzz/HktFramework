# step-0277 — #51b 실 zone.js 브리지 6: _rebalance 실 핸들 균형

> 직전 step-0276 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `zoneRuntimeHosts()` 질의 추가: 실 EntityZone 을 하나라도 돌리는 host 집합(running 문자열 `runningHosts` 의 실물 짝·분산/비움 검증). `_rebalance` 는 이미 매 move 마다 `_migrate`→`_bridgeMigrate`(0273)로 실 핸들을 함께 이주하므로, 자동 부하 재배치가 실 핸들을 고르게 분산함을 단언(transitive·새 집행 코드 없음·질의 1개 delta).
- OFF 플래그 `zoneBridge`(기존·질의이므로 OFF→0276 비트 동일). verify.js 새 모드 `zonerebalance`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonerebalance`**: 5/5 seed — 3존 hostA 몰림 후 placeRebalance→실 런타임 runtimeOn A/B/C 각 1·`zoneRuntimeHosts`=3·`zoneRuntimeDrift`=0·`zoneStarts`=3 불변(이주이지 재생성 아님·상태 보존).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 제어 평면·존 tick 밖. ②결정론 — 균형 tie-break 결정론. ③권위 단일 소유 — 매 move release+acquire(중간 공백/중복 0)·drift 0. ④은닉 — orch 집행. ⑤headless — 실 EntityZone. 한계: _rebalance 의 브리지는 _migrate 경유 transitive(0273) — 이 step 은 그 실 핸들 분산을 *검증*(zoneRuntimeHosts 질의)으로 굳힘. 이상 없음.
