# step-0184 — 길드 영속·failover(guildPersist·변경 저널 replay)

> 직전 step-0183 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — `_journalChange`(로스터 변경 명령 create/join/leave 를 durable 저널 append)·`crash()`(projection 소실·저널 보존)·`reconstruct()`(저널 seq replay 로 로스터+마스터십 재구성·master 보호 동일 적용). projection 휘발/저널 durable. 파티 0085 event sourcing 의 길드 판. 계층: **3 게임 서비스** + **6 데이터**(영속).
- 배선: `topo-build.js`(`guildPersist` 플래그·guild opts persist).
- OFF 플래그 `guildPersist`(OFF → 저널 0·reconstruct 빈 로스터 = 0183 비트 동일=회귀 0) · verify.js 새 모드 `guildpersist`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js` ALL OK — guildPersist OFF → 0183 비트 동일.
- **가설 `guildpersist`**: 5/5 시드 OK — ON crash→reconstruct digest == 죽기 전(pre==post `f0030286`)·single-master 보존. OFF reconstruct 빈 로스터(소실·0).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 저널은 존 tick 밖 비동기. ②결정론: 전 시드 동일 digest·replay 재현. ③권위 단일 소유: reconstruct 가 master 보호 동일 적용→single-master 보존. ④은닉: 자기 영속 저널만으로 복원·외부 무지. ⑤headless·원격: Node·run.js 증명. — 한계: 저널 무계 성장(0185 스냅샷 압축이 해소).
