# step-0231 — instanceleave 모드 spine 승급 (#16 — 3차 라운드 누적 회귀화 시작)

> 직전 step-0230 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0221 의 bespoke `instanceleave`(인스턴스 플레이어 이탈) 검증 모드를 누적 키트로 승급(MODES+ORDER 등록·OPS/BASE 함수 내 지역화). 계층: 검증 하니스(박스 코드 무수정). OFF 플래그 없음 — 박스 `.js` 0줄 수정이므로 reg 0 자명(verify-kit·verify 는 baseline 비대상). 정리·도구 step(#16): 생성 step(0221) 한정이던 양성 단언을 매 spine 항구화.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `instanceleave`: 5/5 OK — d1 occ 1·p1 route 해제·left 1·미배정 pX miss 1.
- spine ALL OK — instanceleave 가 이제 누적 회귀에 포함(전 역사 불변 + 0221 인스턴스 이탈을 현재 src 에 단언).

## 척추 5항 + 한계

①tick: 검증 모드 추가일 뿐 박스 onTick 무변(시뮬 외). ②결정론: run 결과만 단언·Math.random 0. ③권위: instanceLeave=release 단언(소유자=1 보존 확인). ④은닉: 영향 없음. ⑤headless: node 단언, 변동 없음. — 이상 없음.
