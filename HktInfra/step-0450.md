# step-0450 — #4 실 net.step 배리어 치환 10·grand capstone: in-proc 등가 E2E

> 직전 step-0449 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `capstoneReplicas(events, M, seed, avatars, nsites)`(복합 드라이버) + verify 전용 grand capstone `nete2ecap`: 0441~0449 의 모든 조각(실 Net-형 스트림·sim fold·holdback·디스패치·resync·복제 수렴·배리어-free·exactly-once·배리어 등가)을 한 시나리오로 단언 — M 복제 존이 *서로 다른 순열+손실*을 *독립 페이스*(1·2·3보)로 받아 재구성·동결 sim fold. 코드 박스는 capstoneReplicas 1함수만 추가(async-net 동결).
- 계층: 코디네이션(#4). **#4 실 net.step 배리어 치환 in-proc 등가 sub-arc(0441~0450) 닫기** — 실 Net 메시지·실 sim 위에서 배리어와 배리어-free substrate 의 실 월드 등가를 완성.
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `nete2ecap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `nete2ecap` 5/5 OK — 4 client·40 intent·M=3 복제: ⒜ 실 engine Net **배리어 == 배리어-free substrate == canonical** Y ⒝ 전 복제 **desync 0** ⒞ **exactly-once**(complete) ⒟ 전순서 **sound**(엄격+인과 존중) ⒠ 배리어-free **skew 26**(비-lockstep) ⒡ 손실 실발생.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(순열+손실+페이스 교란 무관·전 복제 수렴) ③권위 단일 ✅(exactly-once) ④은닉 ✅(실 Net 계약) ⑤headless ✅(순수 JS·검증 전용). 한계(정직): 이 arc 는 실 Net 메시지·실 sim 위의 *in-proc 등가* — 실 `net.step()` 을 아직 *치환하지 않음*(async-core→async-net 처럼 등가 먼저, run() 의 배리어 실제 교체는 후속 arc). async-net 은 run() 밖에 살아 reg 구조적 0 — 실제 치환 시 회귀 게이트 재확인 필요.
