# step-0328 — #9 후속: 다운스트림 세션 무굶김 (모두 keyframe 수신)

> 직전 step-0327 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 술어 `zoneViewAllKeyed(zoneId)` = 그 존의 *모든 활성 세션*(rt.zone.sessions)이 적어도 한 번 reset keyframe(초기 전체 뷰)을 받았는가. 한 세션도 굶기지 않는다(no-starvation·접속한 플레이어는 누구나 자기 세계를 받는다 = 다운스트림 무손실의 토대).
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0327 비트 동일. verify.js 새 모드 `hostzonekeyed`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0327) 비트 동일.
- `hostzonekeyed` 5/5 OK — z1 에 a1·a2·a3 enter: `zoneViewSessions` **3**·세 세션 각 reset **≥1**(r1·r2·r3 = 1·1·1)·`zoneViewAllKeyed` **true**(아무도 안 굶음).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 5종 동일. ③ 권위: 권위 존이 각 세션에 keyframe. ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·reg 0.
- 한계: 초기 keyframe 보장만(전송 신뢰성/재전송은 게이트웨이→클라 배선 후속). 이상 없음.
