# step-0505 — mailsagareadmit 포기 give 재admission 복구 기제 (#16 라운드 4차 5)

> 직전 step-0504 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagareadmit`: 포기(abandonedGive) 후 운영이 `mailReadmit` op → `abandonedGive→pendingGive` 역이행·`readmitted`=1·retryCount 리셋(재전송 재개 대기). `pending` 불변(재admission 은 미해결 상태를 안 바꿈)·sagaLivenessConsistent. 포기(0503)의 역이행 — 손실 해소 후 재개의 발판. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 5/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagareadmit`: 5/5 OK — readmitted 1·abandonedGive 0·pendingGive 1·pending 1·sagaConsistent+liveness Y. 포기 give 가 재전송 큐로 복귀(재admission).
- spine ALL OK — 손실 체제 5모드 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 재admission 5시드 동일. ③권위: pending 불변 — 재admission 은 재전송 자격만 복원(아이템 상태 무변경·이중적용 0). ④은닉: mailReadmit op 경유. ⑤headless: node in-proc. — 정직한 한계: 정적 손실 seam 은 mid-run 해소 불가라 *재admission 후 실제 drain*(loss cleared)까지는 이 하니스로 구성 못 함 — 여기선 재admission *기제*(abandonedGive→pendingGive 역이행)만 단언. drain 자체는 0501 transient(drop-once 자가 치유)가 커버.
