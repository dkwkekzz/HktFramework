# step-0467 — 이주 전 유계 resync 명제(deferredAcrossHandoff==0)

> 직전 step-0466 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/async-barrier.js` — 이주 경계 걸침 회계. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`. verify.js 새 모드 `mzhandoff`.
- `pendingMoves`(미결=defer 됐으나 미재배달 move id→avatar): defer 시 등록·재배달 시 해제. `handoffsObs`(관측된 이주 수)·`deferredAcrossHandoff`(이주 관측 시 그 avatar 미결 move 존재 횟수).
- 명제: **이주(handoff) 관측 시 그 avatar 의 미결 deferred move 는 0** → defer 는 항상 이주 전 재배달됨 = 이주 전 유계 resync 를 *인과적으로*(handoff 메시지 관측) 확증. barrier ON 회계만 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → net.step·baseline 비트 동일).
- `mzhandoff` 5/5 OK — deferN 44~103·handoffsObs 4~15(실 이주 관측)·deferredAcrossHandoff **0** 전 시드·world==lockstep **Y**.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: 유계 resync 가 이주 경계에 미결 없음 보장 ③권위 단일: 이주 release+acquire 무손실 ④은닉: barrier 는 handoff 메시지·존 읽기만 ⑤headless 이상 없음.
- 발견: 0465(span<horizon)의 *정적* 유계성이 실제로 *동적* 이주 관측에서도 성립(deferredAcrossHandoff 항상 0) — 유계 명제가 구성적+관측적 둘 다 확증.
