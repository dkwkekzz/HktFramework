# step-0315 — #9 잔여: 다중 동시 host 프로세스 장애 (hostCount)

> 직전 step-0314 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 질의 `hostCount()` = 지금 존을 하나라도 돌리는 host 컨테이너 수("몇 대의 host.js 프로세스가 떠 있나"·운영 대시보드 기본 카운터). runningHosts(0249·집행 SSOT host 집합)의 컨테이너 판. 다중 동시 장애 후 생존 host 수로 수렴.
- 계층: 코디네이션(⑤). 읽기 전용 → 0314 비트 동일. verify.js 새 모드 `hostdoublefail`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0314) 비트 동일.
- `hostdoublefail` 5/5 OK — z1·z2@A·z3·z4@B(2 host 각 2존)·`placeHostDown(hostA, [A,B,C])`→z1·z2→C·`placeHostDown(hostB, [B,C])`(죽은 A 제외)→z3·z4→C: `hostCount()` **3→1**(2대 죽고 1대 남음)·hostZones(C) **4**개·생애주기 despawn A·B(2건)·hostContainerCoherent Y·bijection(zoneHostSnapshot=={C:[z1..z4]})·running 4.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 질의(시뮬 외 0). ② 결정론: 시드 5종 동일·재가동 최소부하 결정론. ③ 권위: 연속 장애에도 단일 소유 회복(공백 0). ④ 은닉: orch 내부 컨테이너. ⑤ headless: Node 순수·reg 0.
- 한계: 죽은 host 를 survivor 후보에서 빼는 책임은 op 호출자(hosts 인자=현 가동 클러스터)·자동 가동목록 추적은 후속. 이상 없음.
