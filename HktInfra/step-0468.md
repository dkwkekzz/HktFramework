# step-0468 — exactly-once 완전 회계(moveDup0·lost0·pendingAtEnd0)

> 직전 step-0467 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/async-barrier.js` — `pendingAtEnd`(종료 시 미결 deferred move 수) stats 노출. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`. verify.js 새 모드 `mzexactly`.
- exactly-once 완전 회계: ⒜ moveDup==0(중복 배달 0) ⒝ lost==0(resync ON → 유실 0) ⒞ pendingAtEnd==0(모든 deferred move 결국 배달) ⒟ moveDeliv>0. 유계 resync 가 redirect 없이 *정확히 한 번* 배달함을 단언(0464 moveDup0 를 유실·미결까지 확장).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → net.step·baseline 비트 동일).
- `mzexactly` 5/5 OK — moveDeliv **360**(전 시드 일정)·moveDup **0**·lost **0**·pendingAtEnd **0**·world==lockstep **Y**.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: moveDeliv 전 시드 일정(360)=결정론 배달 ③권위 단일: exactly-once 로 유실·중복·미결 0 ④은닉 존 읽기 peek ⑤headless 이상 없음.
- 발견: moveDeliv 가 전 시드 동일(360) — 손실/지연 섭동에도 *배달된 고유 move 총수*가 lockstep 과 정확히 일치(exactly-once 의 강한 형태).
