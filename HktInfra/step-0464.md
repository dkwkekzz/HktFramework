# step-0464 — 다중 존 결합 loss+delay 유계 resync + exactly-once

> 직전 step-0463 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 코드(박스) 무변경 — 0462(loss)·0463(delay) 의 interior 유계 resync 가드를 *동시* 적용하는 verify 모드 `mzresync` 추가. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`.
- 결합 섭동(loss 0.2 + delay 0.3)을 다중 존(grid24·zones2)에서 돌려 ⒜ world==lockstep(유계 resync 로 이주 타이밍 불변) ⒝ **exactly-once**(moveDup==0 — deferred move 가 정확히 한 번 배달·redirect 없이 유계 resync 로 유실·중복 0) 를 한 시나리오로 단언.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (코드 무변경 → src box == baseline 비트 동일).
- `mzresync` 5/5 OK — 다중 존 결합 loss+delay: handoffs 4~15·resyncs 18~46·delayed 26~57·moveDup **0**·world==lockstep **Y** 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: 결합 섭동 하 lockstep 재현 ③권위 단일: exactly-once 로 유실·중복 0 ④은닉: 존 읽기 peek ⑤headless 이상 없음.
- 한계: 이상 없음. moveDup0 은 유계 resync 가 *중복 배달 없이* 손실을 복원함을 확인(0468 이 완전 회계로 재확증).
