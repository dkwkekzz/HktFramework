# step-0352 — #57 실 host.js OS 프로세스 spawn 2: hostSpawnDelta reconcile 델타

> 직전 step-0351 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — `hostSpawnDelta(prev)` 읽기 전용. 직전 spawn 돼 있던 host 집합(prev) 대비 현재 목표(zoneHosts)의 차이 `{spawn, kill, keep}`(각 정렬). hostSpawnPlan(0351·목표 상태)에 수렴하기 위해 드라이버가 *이번 reconcile tick 에 집행할 동작*: spawn=cluster.spawnOne·kill=cluster.killHost·keep=유지. 실 cluster 가 외부 상태(이미 뜬 프로세스)와 목표를 맞추는 표준 reconcile 패턴.
- 계층: 코디네이션(⑤). OFF 플래그 없음 — 읽기 전용 질의 → baseline 비트 동일. verify.js 새 모드 `hostdelta`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 질의 추가 → baseline(0351) 비트 동일.
- `hostdelta` 5/5 OK — z1@hostA·z2@hostB 뒤 hostB 드레인(→최소부하 hostC): 최종 컨테이너 {hostA:[z1], hostC:[z2]}·prev=[hostA,hostB] 대비 `spawn`=[hostC]·`kill`=[hostB]·`keep`=[hostA].
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 델타는 읽기 질의(쓰기·발신 0). ② 결정론: 5종 동일. ③ 권위: 델타가 집행 SSOT(zoneHosts) 기준 — kill 된 host 는 더는 존 미소유. ④ 은닉: host id 목록만(직렬화 가능). ⑤ headless: Node 순수·reg 0.
- 한계: 아직 델타 *산출*만 — 드라이버 훅 집행은 후속(0353). 이상 없음.
