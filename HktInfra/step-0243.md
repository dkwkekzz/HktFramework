# step-0243 — 배치 SSOT 실배선(#51) 3: executed placeRebalance(자동 재배치 실 이주)

> 직전 step-0242 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `_rebalance` 의 매 paper `placement.set` 옆에 `placeExecute` ON 이면 `_migrate`(0242) 호출 — 자동 부하 재배치가 실 존 런타임도 release+acquire 로 함께 이주(running 이 *실제* 균형 수렴·0223 자동 트리거의 집행 판). 계층:코디네이션.
- OFF 플래그 `placeExecute`(OFF→_rebalance 는 paper 만=0242 비트 동일=회귀 0).
- verify.js 새 모드 `placerebalexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5조 비트 동일(baseline).
- **가설 `placerebalexec`**: 5시드 모두 running A/B/C 각 1·rtMig 2·runningCount 3 — z1·z2·z3@hostA(3/0/0)가 *실제* 1/1/1 로 균형·결정(placement)==집행(running) 전 존.
- **결정론/권위/수렴**: `run.js all` ALL OK.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 코디네이션 제어 평면 ②결정론: 시드 PRNG·host/zone 순서 결정론 ③권위 단일 소유: 매 _migrate 원자 교체 ④은닉: orch 내부 ⑤headless: node 검증. — 이상 없음(drain executed 는 0244·reconcile capstone 은 0245 후속).
