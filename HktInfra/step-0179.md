# step-0179 — 아이템 우편 saga 영구 실패 발행(mailFailPublish)

> 직전 step-0178 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-core.js`(`_resendPending` 영구 실패 분기): permFailed 시 `svc.mail.saga_failed{gid,itemId,cause}` 1회 발행(failPublished++·saga liveness 발행 종결 마디·거래소 0138 의 우편 판). 계층: 게임 서비스(우편)·버스.
- 필드: `failPublish`/`failPublished`. `svc-mail-persist.js` crash(): failPublished 소실. `src/topo-build.js`: `mailFailPublish` 기본 + 박스 opts + buildSubs 설정. `src/topo-subs.js`: audit 가 `svc.mail.saga_failed` 구독(ON 일 때만).
- OFF 플래그 `mailFailPublish`(OFF→발행 0·구독 미추가 = 0178 비트 동일). verify.js 새 모드 `mailfail`. 우편 saga 수명주기 발행 3종(포기 0174·재개 0177·종결 0179) 완비.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: failPublish OFF = 0178 비트 동일(전 시스템 5시드) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경.
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailfail`**: 5시드 전부 — ON permFailed 1·published/audit 1/1(발행==영구실패==관측 1:1)·OFF published 0.
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편 존 tick 밖·발행 파생 스트림 — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경. ③권위 단일 소유: 우편함 권위 불변(발행 비-침습). ④은닉·단일 연결: audit 가 발행자 무수정으로 구독(버스 경유). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (다음: saga liveness 회계 정합 capstone·거래소 0140 의 우편 판 — pending 분할 불변 단언.)
