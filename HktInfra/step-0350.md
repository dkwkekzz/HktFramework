# step-0350 — #9 후속 grand capstone: 월드 다운스트림 E2E 전 정합

> 직전 step-0349 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 월드 다운스트림 전 정합 술어 `downstreamWorldCoherent()` = 모든 런타임 존 `downstreamCoherent`(host 산출 AOI 뷰 포착 건강·0330) && `downstreamSettled`(산출 frame 전부 전파·ack·복구·0341). orch 측 "포착 + 전파 정착" 단일 술어.
- grand capstone(verify) = `downstreamWorldCoherent` && 모든 실 DownClient `convergedTo(zoneAuthSig)`(desync 0·수렴 0346) && 게이트웨이 `gatewayDeliveryIsolated`(0340). host→게이트웨이→실 클라 E2E 완결.
- 계층: 코디네이션/월드/엣지(②④⑤·SPINE §4 경로2 월드 다운스트림). 읽기 전용 → baseline 비트 동일. verify.js 새 모드 `worldcap`. **월드 다운스트림 데이터 평면 0319~0350 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 술어 추가 → baseline(0349) 비트 동일.
- `worldcap` 5/5 OK — z1@A(a1@dc0·a2@dc1 late-join)·z2@C(b1@dc2)·a1 이동·z1 A→B migrate·`s:a1#2` 손실 뒤: `downstreamWorldCoherent` **Y**·dc0·dc1·dc2 모두 `convergedTo` **Y**(host 권위 desync 0)·`isolated` **Y**.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 술어는 읽기·뷰 산출 onTick. ② 결정론: 5종 동일. ③ 권위: 클라 읽기전용 뷰가 권위 재현으로 수렴(다중 존·손실·migrate·late-join 에도). ④ 은닉: 각 클라 게이트웨이 단일 연결로 자기 세션 뷰만. ⑤ headless: Node 순수·직렬화·reg 0.
- 한계: 인프로세스 DownClient·zoneHost(실 OS 프로세스/소켓 spawn 은 후속·cluster-run.js). 이상 없음.
