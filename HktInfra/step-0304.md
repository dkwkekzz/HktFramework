# step-0304 — #9 잔여(실 host.js 물리 분리) 4: host roster register/deregister 계측

> 직전 step-0303 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_hostSet` 이 host 컨테이너를 *처음 만들 때*(첫 존) `hostRegisters++`(실 host.js 프로세스 **spawn** 씨앗 — 그 host 가 첫 존을 호스팅), *마지막 존 잃어 지울 때* `hostDeregisters++`(**despawn** 씨앗). 질의 `hostRegistered(host)`(컨테이너 존재 = 프로세스 가동). 불변: registers−deregisters == 현 host 수.
- `src/orchestrator.js` — 카운터 `hostRegisters`·`hostDeregisters`. 계층: 코디네이션(⑤). zoneHostProc OFF→_hostSet no-op = 0303 비트 동일. verify.js 새 모드 `zonehostroster`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneHostProc OFF → baseline(0303) 비트 동일.
- `zonehostroster` 5/5 OK — 혼합 lifecycle(hostdown C·stop z2·migrate z1·rebalance·drain) 후: hostRegisters 5 − hostDeregisters 3 = net 2 == 현 host 수 == running host 수 + roster(hostRegistered)==running hosts + directFlowCoherent·total 1·ledger 5/1/2/1.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: roster 계측은 배치 집행 경로(tick 밖). ② 결정론: 카운터 증감만·시드 5종 동일(reg5/dereg3 안정). ③ 권위: host 가 첫/마지막 존에 spawn/despawn — 프로세스 수명과 존 소유가 한 몸. ④ 은닉: orch 내부 roster. ⑤ headless: Node 순수·reg 0.
- 한계: roster 는 아직 *논리* 가동 표식 — 실 host.js OS 프로세스 spawn/kill(cluster-run.js) 연결은 후속. 이상 없음.
