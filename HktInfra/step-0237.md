# step-0237 — worldwb 모드 spine 승급 (#16 — 3차 라운드 누적 회귀화 7/10)

> 직전 step-0236 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0227 의 bespoke `worldwb`(월드 영속 write-behind 버퍼) 검증 모드를 누적 키트로 승급(OPS/BASE 함수 내 지역화). 계층: 검증 하니스(박스 코드 무수정). OFF 플래그 없음 — 박스 `.js` 0줄 → reg 0 자명. #16 정리·도구 step 7/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `worldwb`: 5/5 OK — flush 후 로그 2·버퍼 0, 추가 pickup 은 버퍼(1)만(미flush)·replay 에 gold 미반영.
- spine ALL OK — worldwb 누적 회귀 편입(0227 쓰기 지연·배치를 현재 src 에 단언).

## 척추 5항 + 한계

①tick: 검증 모드 추가(시뮬 외)·write-behind 가 매 intent 디스크 안 때림(tick 보호 단언). ②결정론: replay 결정론·Math.random 0. ③권위: 월드 로그=intent SSOT(쓰기 경로 하나). ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음.
