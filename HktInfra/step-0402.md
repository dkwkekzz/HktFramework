# step-0402 — #62 runMulti 합류 2·복원력: silence lease-timeout 자동 펜싱

> 직전 step-0401 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — silence 기반 자동 펜싱: 필드 `leaseTimeout`(임계 3)·`_silent`(host→연속 침묵 sweep) + `sweepSilence()`(placement host 중 `cluster.socketDead`(전송 층 사망 감지)인 host 의 연속 침묵을 세어 leaseTimeout 초과 시 자동 `fence`·살아있으면 counter 리셋). fence(0401)는 수동 — runMulti `observeSilence`(`cluster-run.js:72`)의 임계 기반 추측 펜싱을 zone cluster 상주 제어 평면에. 계층: 코디네이션(#62).
- OFF 동치: socketDead host 0 이면 counter 0·무동작 → 0401 동치. verify.js 새 모드 `coordsilence`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 28.4KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordsilence 5/5**: run(5)+killHost(hostB) → sweepSilence ×2 presumedDead 없음(임계 3 미만) → 3번째 sweep 에 자동 fence(presumedDead={hostB}·epoch 1).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: sweep 은 제어 평면 ②결정론: 시드 5종 동일·임계 결정론 ③권위: 임계 기반 추측 사망→펜싱(즉발 오판 방지) ④은닉: cluster.socketDead 신호만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: socketDead 는 killHost 가 *즉시* 세우므로 임계는 *연속 sweep* 수일 뿐 — partition/falsedeath 같은 침묵-without-kill 은 zone cluster 에 미구현(runMulti 의 분단 윈도는 full topology 판). snapshot-restart(0403)·reprovision+mirror(0404~0405).
