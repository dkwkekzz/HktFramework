# step-0493 — svcmailcap 아이템 우편↔가방 saga 정합 편입 (#16 라운드 3차 3)

> 직전 step-0492 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 아이템 첨부 우편↔가방 saga 정합 capstone(옛 0170 `sagaLiveConsistent` 판)을 재작성 편입: `run()` 을 mail+mailItem+mailInv+mailSaga+inventory opts 로 구동하고 우편 박스 *네 회계층 동시 닫힘*을 단언하는 새 mode `svcmailcap` — mailConsistent(메시지 통수)·itemConsistent(아이템 상태 분할)·escrowConsistent(escrow 집합)·sagaConsistent(saga 회계) AND. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 3/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcmailcap`: 5/5 OK — mailSend(첨부 escrow 인출)→mailFetch(입금)·gives2==acked2==oks2·pending0·sagaLiveConsistent(네 회계층 AND)·item0 alice→escrow→bob(escrowXfers 2).
- spine ALL OK — svcmailcap 포함(거래소 2종 + 우편 saga·서비스 계층 누적 회귀).

## 척추 5항 + 한계

①tick: 우편 존 tick 밖 반응형·박스 무변. ②결정론: 네 술어 AND 대조·Math.random 0. ③권위: escrow custody 쌍 거래·아이템 단일 소유(bob)·saga 회계 닫힘 재단언. ④은닉: 우편↔가방 give 경유. ⑤headless: node in-proc. — **한계**: 정상 흐름 정합(sagaLiveConsistent)에 집중 — loss 체제(sagaLivenessConsistent 3분할)는 후속.
