# step-0316 — #9 잔여: entity 가중 부하 인지 자동 배치 placeAutoE

> 직전 step-0315 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-control.js`·`src/orch-hostproc.js`·`src/orchestrator.js` — 새 op `placeAutoE {zoneId, hosts[]}` = 후보 host 중 *entity 최소 부하*(`_leastLoadedByEntities`·`hostEntityLoad`) 선택 배치. placeAuto(0217·*존 수*)의 entity 가중 판 — 존 수는 같아도 만원인 host 를 피한다(동접 가중). 계측 `autoEPlacements`.
- 계층: 코디네이션(⑤). placeAutoE 미수신이면 미발화 → 0315 비트 동일(새 op·플래그 불요). verify.js 새 모드 `hostautoentity`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — placeAutoE 미주입 → baseline(0315) 비트 동일.
- `hostautoentity` 5/5 OK — z1@A(엔티티 5)·z2@B(엔티티 0)·존 수 균형(A·B 각 1): `placeAuto`(존 수)는 z3→**hostA**(만원에 또 얹음·동률 tie-break), `placeAutoE`(entity)는 z3→**hostB**(한가)·autoEPlacements 1·hostContainerCoherent Y·running 3.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 배치 결정(시뮬 외·비동기 코디네이션). ② 결정론: 시드 5종 동일·동률 후보 순서 tie-break. ③ 권위: _start 로 단일 소유 가동. ④ 은닉: orch 내부 부하 질의. ⑤ headless: Node 순수·reg 0.
- 한계: entity 부하도 *수*만(tick 비용·대역 미반영). 가중 트리거 자동 재배치(과부하 host 자동 분산)는 후속. 이상 없음.
