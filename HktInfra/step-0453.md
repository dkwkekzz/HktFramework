# step-0453 — #4 실 net.step 배리어 실제 치환 3: 월드 입력 정전 순서 holdback(제자리 슬롯)

> 직전 step-0452 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-barrier.js` — stepper 가 매 tick 의 월드 입력을 holdback 버퍼로 모아 **정전(canonical) 순서 = 전역 발신 순서 `m.id`** 로 재구성해 *제자리 슬롯*에 방출(비월드 메시지 위치 불변 → 상호작용 보존). 실 존은 *순서 민감*(enter 가 zone.rng() 소비·move 는 entity 존재 요구)이라 임의 재정렬은 월드 파괴 — substrate 의 일은 도착이 흐트러져도 발신 순서를 재구성해 lockstep 과 같은 순서로 배달하는 것. 정상 run() 은 큐가 이미 발신 순서라 이 정렬은 *항등*(투명) → world/log 불변. load-bearing 은 손실 복원(0455~).
- 계층: 코디네이션(#4). net.step 중앙 배리어 대신 배리어가 월드 입력을 정전 순서로 재구성 배달.
- OFF 플래그: `asyncBarrier`(미설정→ab=null→net.step() 그대로 = **baseline 비트 동일·reg 구조적 0**). verify.js 새 모드 `barhold`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK(asyncBarrier 미설정→net.step 경로 비트 동일).
- `barhold` 5/5 OK — 4 client·30 move·48 tick: `run({asyncBarrier})` worldDigest == lockstep Y·logDigest == Y(투명·정전 순서 재구성 항등)·held **244**(월드 입력 holdback 통과).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(정전 순서 재구성·world/log 불변) ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅. 의외의 발견(정직): 실 존은 *순서 민감*(enter rng 소비·move 존재 의존) — 임의 (lc,site) 재정렬은 월드 파괴. 따라서 배리어는 *발신 순서(m.id) 재구성*으로 lockstep 을 재현(load-bearing=손실 복원, 재정렬 아님). asyncBarrier OFF→reg 구조적 0.
