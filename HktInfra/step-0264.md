# step-0264 — 정리(#49 wiring): svc-exchange-core 영속/failover 메서드 믹스인 분리

> 직전 step-0263 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-exchange-core.js`(30.7KB>30KB 트리거) → `ExchangeService` 의 *영속/스냅샷/failover 메서드*(`_bump`·`_snapState`·`_restore`·`_journal`·`crash`·`reconstruct`)를 새 박스 파일 `src/svc-exchange-persist.js` 믹스인으로 분리. 계층: 게임 서비스(거래소). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변). 0251 orch-placement 믹스인의 거래소 판.
- 배선: svc-exchange-core.js 가 `require('./svc-exchange-persist.js')` 1줄 · 클래스 정의 직후 `Object.assign(ExchangeService.prototype, ExchangePersist)`(this 바인딩·메서드 해소 동일). 0124 의 svc-exchange-txn 증강과 같은 패턴. verify.js 새 모드 `xchsplit`.
- 결과 크기: svc-exchange-core.js 30.7KB→26.6KB(<30KB)·svc-exchange-persist.js 5.7KB. **#49 의 마지막 >30KB 박스 해소 — topo-run·topo-build·svc-exchange-core 셋 다 <30KB.**

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `xchsplit`(가설): 5/5 — exchList×2+exchBuy 후 crash→reconstruct 가 durable 저널서 projection 복원(pre L/S 2/1 == post 2/1·open 1)·보존 불변(conserved true) 유지. 옮긴 영속/failover 경로 무결.
- spine: ALL OK(전 역사 누적 회귀 + xchsplit 편입).

## 척추 5항 + 한계

①tick: 영속/failover 는 존 tick 밖 거래소 제어 평면·무변경 ②결정론: 코드 이동만·저널 replay 비트 동일 ③권위: escrow 단일 소유·보존 불변 유지 ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
