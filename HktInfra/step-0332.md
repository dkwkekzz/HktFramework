# step-0332 — 정리: 브리지 필드 init 분리(orchestrator.js >30KB 유계화)

> 직전 step-0331 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` 생성자 — 0272~0331 의 브리지/데이터평면/host컨테이너/egress 필드 대입 블록(~43줄·코멘트 밀도 高)을 **전용 새 파일 `src/orch-bridge-init.js`** 의 `OrchBridgeInit._initBridgeFields(opts)` 로 이동하고 생성자는 `this._initBridgeFields(opts)` 한 줄로 호출(require + `Object.assign(Orchestrator.prototype, OrchBridgeInit)`). 같은 순서·동일 대입·생성자 같은 지점 호출 → 동작 비트 불변(reg 0).
- 계층: 정리(기능 0·코디네이션 박스 유계화). 플래그 없음(투명 분할·orch-placement 0251·orch-control 0267·orch-hostproc 0305·orch-views 0323 계보). verify.js 새 모드 `bridgesplit`(스모크).
- **0331 egress 가 orchestrator.js 를 30.5KB>30KB 로 넘긴 close-step 가드를 해소** — 다음 기능 step 전 정리(비대화 트리거). 전용 파일로 떼 세 박스(orchestrator·orch-zonebridge·orch-bridge-init) 모두 <30KB.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- orchestrator.js **30.5KB → 22.8KB**·새 `orch-bridge-init.js` 10.8KB·orch-zonebridge.js 28.0KB(불변). 박스 >30KB 0개.
- `reg` ALL OK — OFF 경로 baseline(0331) 비트 동일(필드 이동만·동작 무변경).
- `bridgesplit` 5/5 OK — 분리 후에도 브리지 데이터 평면 온전: egress **4** == zoneViewFrames 4 && `downstreamCoherent(z1)` Y(추출된 필드가 정상 배선).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick ② 결정론 ③ 권위 ④ 은닉 ⑤ headless — 모두 무변경(정의 위치만 이동·동작 비트 불변). 
- 한계: 정리 step(새 기능 0). 이상 없음.
