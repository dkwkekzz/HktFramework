# step-0346 — #9 후속 capstone: 실 다운스트림 클라 수렴 전 정합

> 직전 step-0345 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/client.js` `DownClient.convergedTo(authSig)` — 이 클라 뷰가 권위 AOI 서명으로 수렴했는가(desync 0 의 수신자 측 선언).
- capstone(verify) = 다중 클라·상호 가시·migrate·손실 뒤: 모든 클라 `convergedTo(zoneAuthSig)`(host 권위 desync 0) && 교차 관찰자 일치(공유 entity 위치 dc0==dc1==host·0345) && `downstreamSettled`(전부 도달·복구·0341).
- 계층: 월드/엣지/코디네이션(②④⑤). 플래그 없음·읽기 → baseline 비트 동일. verify.js 새 모드 `dccap`. **수렴 sub-arc 0342~0346 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 헬퍼 추가 → baseline(0345) 비트 동일.
- `dccap` 5/5 OK — a1@dc0·a2@dc1 상호 가시·z1 A→B migrate·`s:a1#3` 손실 뒤: `conv0`·`conv1` **Y**(둘 다 host 권위로 수렴)·`agree` **Y**(공유 entity 교차 일치)·`settled` **Y**(전부 도달·복구).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 전부 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 클라 읽기전용 뷰가 권위 재현으로 수렴(migrate·손실에도). ④ 은닉: 각 클라 게이트웨이만. ⑤ headless: Node 순수·reg 0.
- 한계: 인프로세스 DownClient(실 OS 프로세스/소켓 spawn 은 후속). 이상 없음.
