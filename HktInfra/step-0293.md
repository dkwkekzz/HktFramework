# step-0293 — #9 멀티프로세스 배선 3: 게이트웨이 존 디렉토리(서비스 디스커버리)

> 직전 step-0292 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/gateway.js` — `zoneDir`(zoneId→host) 필드 + `zoneDirOf`/`zoneDirSize` 질의. 게이트웨이가 "어느 존이 어느 host 런타임에서 도나"를 캐시(라우팅 테이블·서비스 디스커버리의 씨앗·#9 직접 라우팅 전제). 계층: 엣지(#9).
- 박스 `src/gateway-msg.js` — onMsg 의 `m.from==='orch'` 블록에 `zoneLoc` 분기: host 면 set·null 이면 delete(은닉 — 게이트웨이는 명시 메시지로만 학습).
- 박스 `src/orch-zonebridge.js` — `_pubZoneLoc(zoneId, host)` 헬퍼 + 배치 집행 4곳(`_bridgeStart`/`_bridgeMigrate`/`_bridgeHostDown`/`_bridgeStop[null]`)에서 게이트웨이로 push.
- 박스 `src/orchestrator.js` — `gatewayZoneDir` 플래그 + `zoneLocPushed` 계측. 박스 `src/topo-build.js` — opt 배선.
- OFF 플래그 `gatewayZoneDir`(OFF→push 0 = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `gwzonedir`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwzonedir` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwzonedir`: 5/5 혼합 배치 lifecycle(place×3·hostdown·stop·migrate·rebalance·drain) 후 게이트웨이 zoneDir == orch.running — dirN 2 == runN 2·push 7(>0·실제 디스커버리 발생).
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: zoneLoc push 는 코디네이션 제어 평면(존 tick 밖)·시뮬 무관. ②결정론: push 는 배치 op(시드 함수)에 종속·순수. ③권위 단일 소유: 디렉토리는 zone 당 단일 host(running 의 거울). ④은닉: 게이트웨이는 orch 내부를 모른 채 명시 zoneLoc 메시지로만 학습(SPINE §5 ④ 준수·서버간 명시 인터페이스). ⑤headless·원격: 디렉토리는 직렬화 가능 메시지로 갱신(원격 서비스 디스커버리 동형).
- 한계: push 는 net.send(orch→gateway·1-tick 지연) — 짧은 stale 창 존재(이주 직후 게이트웨이가 옛 host 를 잠깐 봄). 직접 라우팅 시 stale 거부/갱신은 후속(0296). 아직 게이트웨이가 디렉토리를 *읽기만* 하고 라우팅엔 안 씀(0294 직접 enter). 이상 없음.
