# step-0309 — #9 잔여(실 host.js 물리 분리) 9: 다중 동시 이주 host 컨테이너 bijection

> 직전 step-0308 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 질의 `zoneHostSnapshot()`: 현재 host→[존…] 배치 전체를 한 장으로(`zoneDirSnapshot` 0299·게이트웨이 디렉토리의 *host 프로세스 컨테이너* 판). 다중 동시 이주(rebalance/drain) 후 이 스냅샷이 running 을 host 별로 묶은 것과 정확한 bijection(키·값 둘 다)인지 검증.
- 계층: 코디네이션(⑤). 읽기 전용 → 0308 비트 동일. verify.js 새 모드 `zonehostbij`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0308) 비트 동일.
- `zonehostbij` 5/5 OK — z1~z4 모두 hostA 몰림·각 enter 1·rebalance[A,B,C]·drain hostA(다중 동시 이주) 후: host 스냅샷 == running 의 host 별 묶음(bijection Y)·hostContainerCoherent Y·census total 4 보존(graceful 무손실)·totalEntities 4·hostA 비움(존 0).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 읽기 전용 스냅샷(tick 무관). ② 결정론: 다중 이주도 시드 5종 동일 bijection. ③ 권위: 동시 이주 churn 후에도 존이 정확히 한 host 프로세스에(bijection). ④ 은닉: orch 내부. ⑤ headless: Node 순수·reg 0.
- 한계: bijection 은 by-construction(_hostSet 단일 귀속)·graceful 이주는 같은 핸들(무손실). 전 데이터 평면 + host 층 capstone 은 0310. 이상 없음.
