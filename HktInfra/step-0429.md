# step-0429 — #61 업스트림 intent 실 클라 9: 업스트림 회계 (발신 intent == 권위 반영)

> 직전 step-0428 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/client.js` — `UpClient` 에 `intentLog`(발신 매니페스트)·`intentDelta()`(누적 변위) 추가. _emit 이 발신 op 를 기록 → 업스트림 회계: enter 위치 + 발신 변위 == 권위 최종(발신 손실 0)·intentLog 길이 == sent.
- 계층: 엣지/월드(#61).
- OFF 플래그: upClients=null(기본)→스폰 0→회귀 0.
- verify.js 새 모드: `upaccount`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK.
- 가설 `upaccount`: 5/5 — uc0 발신 4(enter+3move)·intentLog 4==sent·최종 권위 {9,9}==enter(5,5)+intentDelta(4,4)(발신 intent 전부 반영).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉 ⑤headless — 이상 없음. 회계는 클램프 0 시나리오(그리드 내)에서 enter+Δ==권위 — 클램프 발생 시 ≤(경계 포화·별도 검증 불요).
