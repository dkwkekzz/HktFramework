# step-0229 — 로그인 계정 검증(loginAuth·엣지 큐 이전 차단)

> 직전 step-0228 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/loginqueue.js` — `loginAuth{player}`: validAccounts(opts.accounts)에 있는 유효 계정만 enqueue(검증 통과), 미인증은 거부(`authRejects`·줄 이전 차단). 0001 LoginServer 계정 검증을 엣지 큐에 실체화 — 불량 접속이 대기열·월드에 안 닿는다. (topo-build `loginAccounts` 파라미터 추가.)
- 계층: 1(엣지·로그인) · OFF 플래그: validAccounts 빈 채/loginAuth 미수신(미발화 → 0228 비트 동일=회귀 0) · verify.js 새 모드 `loginauth`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `loginauth`: 5/5 OK — validAccounts[p1,p2] → p1·p2 enqueue(큐 2·authed 2)·pX 미인증 거부(authRejects 1·positionOf −1)
- spine ALL OK

## 척추 5항 + 한계

①tick: 로그인 큐는 존 tick 밖 엣지. ②결정론: Set 조회·시드 PRNG. ③권위: 대기열/티켓 SSOT 는 큐 단일. ④은닉: 명시 메시지(loginAuth)만. ⑤headless: Node·원격 재현. — 한계: validAccounts 는 더미(opts 주입) — 실제 계정 DB 조회는 데이터 계층 연동 후속.
