# step-0203 — 오케스트레이터 존 배치 SSOT (placeZone)

> 직전 step-0202 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placeZone{zoneId,host}` 추가: 배치 맵 SSOT(zoneId→host·재배치 덮어씀)·`placementOf`/`placedCount` 질의. 계층: **코디네이션(계층5)**. "누가 어디서 도나"의 배치 결정 권위 — 정적 배치(0006 의 2존 고정) 한계 제거 씨앗. 기존 orch(존 failover)에 *추가-휴면* 필드/분기.
- 배선: `topo-run.js` `placementOps` 주입 seam(orch 부재 시 주입 0). OFF(=placementOps 미주입)→placeZone 미발화 = 0202 비트 동일(reg 0).
- verify.js 새 모드 **`zoneplace`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(placementOps 미주입 → orch 추가 분기 휴면 → 0202 비트 동일).
- **가설 `zoneplace`**: 5/5 시드 OK — place 3(zone1@A·zone2@B·zone1@C 재배치) → **placedCount 2·zone1=hostC·zone2=hostB**·placements 3.
- **결정론**: 5 시드 동일. **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(orch onTick 은 failover 제어 평면·placement 은 onMsg 반응·존 tick 밖) ② 결정론 ✅(`Math.random` 0·배치=placementOps 시드 순서) ③ 권위 단일 소유 ✅(배치 맵이 "누가 어디서 도나" 유일 SSOT) ④ 은닉 ✅(placementOps gateway 발신) ⑤ headless ✅(Node·UE 0).
- 한계: 배치 *기록*만(place). 질의 인터페이스(request/reply over net)·부하 기반 배치 결정·인스턴스 spawn 연동은 후속(0204 질의·이후 2차). 멀티프로세스 미배선(#9).
