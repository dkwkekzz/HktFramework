# step-0274 — #51b 실 zone.js 브리지 3: _stop 실 런타임 종료

> 직전 step-0273 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `_bridgeStop(zoneId)` 추가: placeStop 집행 시 실 EntityZone 런타임을 `zoneRuntimes` 에서 제거(핸들 폐기·GC 대상). 없는 존 멱등 no-op. 계층: 코디네이션(instance.js _despawn 의 존 판).
- `orchestrator.js` — `zoneStops` 계측 필드.
- `orch-placement.js` `_stop` 에 가드 1줄: `zoneBridge` ON 이면 `_bridgeStop` 도 호출. OFF→0273 비트 동일.
- OFF 플래그 `zoneBridge`(기존). verify.js 새 모드 `zonestop`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonestop`**: 5/5 seed — placeStop(z1) 후 runtimeCount=1·z1 핸들 gone(null)·z2 live·zoneStops=1·zoneStarts=2 불변·없는 z3 stop 멱등(zoneStops 무증).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 제어 평면·존 tick 밖. ②결정론 — 제거는 Map.delete(상태 영향 없음). ③권위 단일 소유 — 퇴역 존은 단일 키 제거(공백/중복 없음·다른 존 무영향). ④은닉 — orch 집행 책임. ⑤headless — 실 EntityZone. 한계: start/migrate/stop lifecycle 의 실 런타임 집행 완료 — 남은 것은 entity 무손실 직접 증명(0275)·host 장애 복구(_hostDown) 실 판·rebalance/drain 실 판. 이상 없음.
