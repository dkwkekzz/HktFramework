# step-0295 — #9 멀티프로세스 배선 5: 게이트웨이→실 존 직접 move/leave 라우팅

> 직전 step-0294 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/gateway-msg.js` — onMsg 에 클라 intent `zoneMove`/`zoneLeave` 직접 라우팅 분기 추가(0294 enter 와 동형: zoneDir 로 host 해소→zoneDeliver host 태깅). 디렉토리 미스면 드롭. 계층: 엣지(#9).
- 박스 `src/orch-control.js` — `zoneDeliver` 분기에 `op==='move'`(`_bridgeMove`)·`op==='leave'`(`_bridgeLeave`) 적용 추가(host 일치 검증 후·stale 거부 공유).
- OFF 플래그 `gatewayDirectZone` 재사용(OFF→entityOps→orch·zoneDeliver 미수신 = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `gwdirmove`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdirmove` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdirmove`: 5/5 enter(a1·a2)+move(a1)+leave(a2) 모두 게이트웨이 직접 라우팅으로 실 런타임 적용 — z1 a1 잔존·total 1·routes 4==applied 4·moves 1·leaves 1·stale 0·miss 0·entityConserved(1==2−1)·단일 소유.
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: move 위치 적용은 런타임 존 onTick(존 데이터 평면)·라우팅은 엣지·시뮬 무관. ②결정론: 직접 라우팅은 디렉토리(배치 op 함수)·순수. ③권위 단일 소유: entitiesSingleOwner 유지·host 불일치 거부. ④은닉: 게이트웨이는 zoneDir 로만 해소·orch 내부 무지. ⑤headless·원격: zoneDeliver 직렬화 frame·게이트웨이 결정 후 host 송신.
- 한계: zone-host 핸들이 여전히 orch 보유(실 host.js 물리 소켓은 #9 후속). 이주 중 stale 거부 정합(게이트웨이 디렉토리가 늦으면 frame 거부)은 0296 에서 갱신·재해소로 닫는다. 이상 없음.
