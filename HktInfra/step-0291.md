# step-0291 — #9 멀티프로세스 배선 1: 존 런타임 전송 seam(_zoneDeliver)

> 직전 step-0290 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orch-zonebridge.js` — `_zoneDeliver(rt, frame)` 전송 seam 추가. 브리지 enter/move/leave 가 실 EntityZone 핸들에 직접 `rt.zone.onMsg(...)` 하던 것을 이 단일 경로로 통일. 계층: 코디네이션/월드(#9).
- 박스 `src/orchestrator.js` — `zoneHostHandle` 플래그 + `zoneFramesDelivered`/`zoneFrameBytes` 계측. ON 이면 frame 을 `JSON.stringify`→`JSON.parse` round-trip(소켓 와이어의 씨앗·host.js deliver cmd 동형)시켜 적용 → 데이터 평면이 *직렬화 가능한 메시지 경계*를 통과(원격 host.js 프로세스 분리의 전제).
- 박스 `src/topo-build.js` — `zoneHostHandle` opt 를 orch spec 으로 배선.
- OFF 플래그 `zoneHostHandle`(OFF→직접 onMsg = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `zonehandle`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `zonehandle` OFF 경로(zoneHostHandle 미설정)는 baseline 5묶음 모두 비트 동일(0x881d9b53 등·ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `zonehandle`: 5/5 혼합 lifecycle 의 enter/move/leave 가 JSON 직렬화 seam round-trip 후에도 flow Y·consv Y·total 1·단일 소유 — frames 7(==enters5+moves1+leaves1·소실 0)·bytes 546(>0·실제 와이어 증거).
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 전송 seam 은 코디네이션 데이터 평면(존 tick 밖)·시뮬 tick 무관. ②결정론: frame 직렬화는 순수(시드 무관·같은 입력 같은 바이트). ③권위 단일 소유: entitiesSingleOwner 유지. ④은닉: seam 은 직렬화 가능한 frame 만 통과(함수/내부 핸들 누설 0 — 오히려 은닉 *강화*). ⑤headless·원격: frame 이 JSON 와이어를 타므로 원격 host.js 분리 가능성의 *첫 조각*(bytes>0 이 증거).
- 한계: 아직 frame 은 *같은 프로세스 안*에서 round-trip 한다(실 소켓 아님) — orch 가 여전히 핸들을 직접 보유. 실 host.js 소켓 분리·게이트웨이 직접 라우팅은 후속(#9 0292~). 이상 없음.
