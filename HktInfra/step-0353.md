# step-0353 — #57 실 host.js OS 프로세스 spawn 3: clusterDriver 훅 seam

> 직전 step-0352 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-bridge-init.js` — `clusterDriver` 필드(기본 null) + `clusterDriverRecord` ON 이면 인프로세스 recorder 드라이버({spawns,despawns,onSpawn,onDespawn}) 부착 + 회계 `driverSpawns/driverDespawns`.
- `src/orch-hostproc.js` — `_hostSet` 의 첫-존 spawn·마지막-존 despawn 지점에서 `clusterDriver.onSpawn(host)/onDespawn(host)` 호출(cluster-run.js 가 실 cluster.spawnOne/killHost 로 집행할 이음새). 미부착(null)→호출 0.
- `src/topo-build.js` — `clusterDriverRecord` opt 배선(orch 로 전달).
- 계층: 코디네이션(⑤). OFF 플래그 `clusterDriverRecord`(미설정→드라이버 null·`&&` 단락·호출 0 = baseline 비트 동일). verify.js 새 모드 `hostdrive`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미부착 → `_hostSet` 훅 단락 → baseline(0352) 비트 동일.
- `hostdrive` 5/5 OK — clusterDriverRecord ON: z1@hostA·z2@hostB·hostB drain→hostC 뒤 recorder `spawns`==[hostA,hostB,hostC]·`despawns`==[hostB]·`driverSpawns`==`hostRegisters`==3·`driverDespawns`==`hostDeregisters`==1·생애주기 spawn 순서 정합.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 훅은 배치 집행(_hostSet) 안에서만·시뮬 tick 밖. ② 결정론: 5종 동일·호출 순서=배치 op 순서. ③ 권위: spawn/despawn 이 단일 소유 zoneHosts 회계와 1:1. ④ 은닉: 드라이버는 host id 만 받음(존 내부 무관). ⑤ headless: Node 순수·reg 0.
- 한계: recorder 는 인프로세스 검증용 — 실 cluster.spawnOne/killHost 배선은 cluster-run.js 후속(0357). 이상 없음.
