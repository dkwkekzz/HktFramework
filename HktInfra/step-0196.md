# step-0196 — 길드 금고 아이템 수 배지 읽기 모델 (guildBankFeed·GuildFeed bankCount)

> 직전 step-0195 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guildfeed.js` — 0186 의 멤버 수 배지를 금고로 확장: GuildFeed 가 `svc.guild.bank.changed` 도 구독해 guildId 별 `bankCount` 배지 유지(deposit +1·withdraw −1). vault SSOT 와 독립한 파생 읽기 모델(CQRS·발신 0·권위 0). onMsg 를 토픽 분기로 재구성(svc.guild.changed 경로 비트 동일).
- `src/topo-build.js`·`src/topo-subs.js` — `guildBankFeed` 플래그 → GuildFeed `svc.guild.bank.changed` 구독.
- OFF: `guildBankFeed` OFF·발행 부재면 미구독·배지 0 = 0195 비트 동일(reg). verify.js 새 모드 `guildbankfeed`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — 미구독 시 bankCounts 빈 Map → 0195 비트 동일.
- **가설 `guildbankfeed`**: 5/5 시드 OK — g1 배지 2==vault 2·g2 배지 1==vault 1·OFF 배지 0.
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 배지 투영 존 tick 밖. ②결정론: 5/5 동일 배지. ③권위 단일 소유: 배지는 vault 권위와 독립한 사본. ④은닉: 발행 스트림 파생·vault 직접 접근 0. ⑤headless·원격: run.js 증명. — 이상 없음. 한계: 배지 휘발(0197 영속). 후속: bank 배지 영속·late-join(0197).
