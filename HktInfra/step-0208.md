# step-0208 — 월드 영속 replay 재구성 (crash→로그 replay 무손실)

> 직전 step-0207 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/worldlog.js` — `replay()`(durable 로그 전수 재적용 → 투영 재구성)·`_apply`(move→위치·pickup→소지 결정론 reducer)·`crash()`(투영 소실·로그는 durable)·`stateDigest()`/`stateOf`. 계층: **데이터(계층6)**. crash 후 *로그만으로* 동일 상태 복원 — 월드 상태는 DB 행이 아니라 로그로 산다(event sourcing 의 무손실 복구·복제=재현).
- 배선 변경 없음(0207 `worldOps` seam·replay 는 verify post-run 호출·가방/길드 crash·reconstruct 패턴). OFF 플래그 **`worldLog`**(OFF→박스 0=0206 비트 동일).
- verify.js 새 모드 **`worldreplay`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(worldLog 미제공 → 0206 비트 동일).
- **가설 `worldreplay`**: 5/5 시드 OK — append 4 → replay digest(pre) == crash 후 replay digest(post) **0x7de275ff·무손실 예**·h1 pos 6+sword·h2 pos 8.
- **결정론**: 5 시드 동일 digest(로그 순서가 상태 결정). **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(replay 는 제어 평면·존 tick 밖) ② 결정론 ✅(`Math.random` 0·_apply reducer 결정론·로그+시드→동일 상태) ③ 권위 단일 소유 ✅(intent 로그가 세계의 유일 진실·투영은 파생) ④ 은닉 ✅(worldOps zone 발신) ⑤ headless ✅(Node·UE 0).
- 한계: 월드 영속 박스 *기본 통신 완비*(append+replay). 주기 스냅샷 압축·실 존 상태 연동·다중 클라 결정론 복제(#1·#9)는 2차. 멀티프로세스 미배선(#9).
