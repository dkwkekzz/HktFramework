# step-0331 — #9 후속: 다운스트림 egress(host 산출 뷰를 게이트웨이로 송출)

> 직전 step-0330 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — `_drainZoneEgress()`: 각 런타임 존이 onTick 으로 산출해 버퍼링 싱크(`rt.zone.net.buf`)에 쌓은 *새* view/view_delta frame 을 게이트웨이로 송출(`zoneView` 봉투·zoneId·sessionId 태깅). per-rt egress 커서 `rt.egN` 으로 한 frame 정확히 1회·버퍼 미삭제(0319~0330 질의 보존). 질의 `zoneEgressCount()`.
- `src/orch-control.js` onTick — `_tickRuntimes` 뒤 `if (this.zoneEgress) this._drainZoneEgress()`. `src/orchestrator.js`·`src/topo-build.js` — 플래그 `zoneEgress` 배선(`this.zoneViewEgressed` 계측).
- 계층: 코디네이션/월드(⑤②·SPINE §4 경로2 월드 다운스트림 존→게이트웨이 배선). OFF 플래그 `zoneEgress`(OFF→송출 0 = baseline 비트 동일). verify.js 새 모드 `egress`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEgress` OFF 경로 → baseline(0330) 비트 동일.
- `egress` 5/5 OK — z1@A 에 a1·a2 enter + a1 이동 뒤 `zoneEgressCount` = **4** = `zoneViewFrames` 4(산출된 모든 뷰가 빠짐없이 게이트웨이로 송출·버퍼 잔류 0·무손실 송출).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick·egress 도 onTick 송출(시뮬 외 작업 아님·전송). ② 결정론: 5종 동일 송출 수. ③ 권위: 다운스트림 송출이 업스트림 단일 소유·보존을 안 깸. ④ 은닉: orch→게이트웨이 명시 송출(게이트웨이 내부 무지). ⑤ headless: Node 순수·frame 직렬화 가능·reg 0.
- 한계: 이 step 은 *송출*까지 — 게이트웨이가 `zoneView` 를 받아 세션→클라로 라우팅하는 것은 후속(0332+). 게이트웨이 orch-branch 가 `zoneView` 를 아직 무시(드롭). 실 클라 수신·ack·재전송도 후속. 이상 없음.
