# step-0204 — 오케스트레이터 존 배치 질의 (placeQuery→placeReply)

> 직전 step-0203 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — `placeQuery{zoneId}` → `placeReply{zoneId,host}` 회신(request/reply over net·순수 읽기·`_lastPlaceReply` 보관). 계층: **코디네이션(계층5)**. 배치 SSOT(0203)를 게이트웨이가 *원격에서* 읽는 경로(SPINE §4 경로3·프레즌스 0069/우편 0156 질의의 배치 판). 미배치 존은 host=null.
- 배선: 0203 의 `placementOps` seam 재사용(placeQuery 도 같은 경로). placeQuery 미주입→미발화 = 0203 비트 동일(reg 0).
- verify.js 새 모드 **`zonequery`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(placeQuery 미주입 → 0203 비트 동일).
- **가설 `zonequery`**: 5/5 시드 OK — place 2 + query(zone1·미배치 zone9) → **queriesRx 2·repliesSent 2**·lastReply zone9=null(미배치)·zone1 회신 hostA.
- **결정론**: 5 시드 동일. **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(placeQuery onMsg 반응·존 tick 밖) ② 결정론 ✅(`Math.random` 0) ③ 권위 단일 소유 ✅(질의는 순수 읽기·배치 무변경) ④ 은닉 ✅(게이트웨이가 주소 대신 질의로 위치 획득·request/reply) ⑤ headless ✅(Node·UE 0).
- 한계: 오케스트레이터 배치 박스 *기본 통신 완비*(place+query). 부하 기반 배치 결정·인스턴스 spawn 연동·재배치 핸드오프는 2차. 멀티프로세스 미배선(#9).
