# step-0382 — #65 양방향 동기 2: coordDesync (placement 기준 정합)

> 직전 step-0381 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `coordDesync()` 추가: 코디네이터 placement(zone→host SSOT)로 각 존의 host 를 조회해 실 host entity vs orch entity 권위(`zoneEntityPos`·host-무관) 양방향 불일치 수. driver.clusterDesync 는 orch.hostSpawnPlan(stale 가능)으로 host 조회 → migrate 후 발산; coordDesync 는 placement 권위로 조회 → lifecycle 갱신만 따라가면 정확. 계층: 코디네이션(#65).
- verify.js 새 모드 `coorddesync2`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coorddesync2 5/5**: run(3) 후 coordDesync 0 && == driver.clusterDesync 0(placement==orch plan 일 때 동치 입증).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 측정은 tick 밖 ②결정론: 같은 입력 동일 ③권위: placement(where)로 host 조회·zoneEntityPos(what)로 비교 — 두 권위 분리가 정합의 토대 ④은닉: cluster API 만 ⑤headless: 실 host snapshot. — 이상 없음(migrate 후 정확성은 0383~).
