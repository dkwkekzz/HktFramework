# step-0491 — svcexchangecap 서비스 saga capstone 재작성 편입 (#16 라운드 3차 1)

> 직전 step-0490 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — #16 라운드 3차(서비스 계층 실행 검증 편입·감사 §2 ①-b) 개시: 거래소↔가방 saga 정합 capstone(옛 0140 `sagaLiveConsistent` 판)을 **재작성**해 누적 회귀 ORDER 로 편입. 옛 capstone 코드는 git history 소실(단일 src 전환·history ~0248 까지만)이라 복구 불가 → `run()` 을 서비스 opts(`exchange`+`exchInventory`+`exchSaga`+`inventory`+`bus`)로 구동하고 노출된 정합 술어를 단언하는 새 mode `svcexchangecap` 작성(MODES+ORDER·자기완결·`run`/`check`/`pad` 재사용). 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 3차 1/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `svcexchangecap`: 5/5 OK — seller pickup→exchList(escrow 인출)→exchBuy(escrow 입금)·가방 give 2건→saga 회신 drain(gives2==acked2==oks2·pending0)·`sagaLiveConsistent`(pending 3분할+회계 닫힘)·item0 seller→escrow→buyer 실물 이동.
- spine ALL OK — svcexchangecap 포함(grand capstone 9종 + 서비스 계층 첫 capstone).

## 척추 5항 + 한계

①tick: 거래소는 존 tick 밖 반응형 서비스(신성한 tick 보존)·검증 모드 추가·박스 무변. ②결정론: 정합 술어·item 이동 대조·Math.random 0. ③권위: escrow 쌍 거래(release+acquire)·아이템 단일 소유(buyer)·saga 회계 닫힘 재단언. ④은닉: 거래소↔가방 버스/give 경유. ⑤headless: node in-proc run·단언. — **한계**: 옛 0140 은 재시도 상한·포기·재admission·영구실패 liveness 전체였으나, 이 재작성은 *정상 흐름* 정합(sagaLiveConsistent)에 집중 — loss 체제(sagaLivenessConsistent 3분할)는 후속 step 으로 전가.
