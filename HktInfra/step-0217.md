# step-0217 — 오케스트레이터 부하 배치(placeAuto·부하 분산)

> 직전 step-0216 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orchestrator.js` — `placeAuto{zoneId,hosts}` → 후보 host 중 *최소 부하*(`hostLoad`=배치된 존 수 최소) host 를 골라 존 자동 배치. 동률은 후보 배열 순서로 결정론 tie-break(`_leastLoaded`). 정적 배치(0203 수동 placeZone) 한계 제거·부하 분산.
- 계층: 코디네이션(계층5). OFF 플래그: `placeAuto` 미주입(미발화 → 0216 비트 동일·reg 0). verify.js 새 모드 `placeload`.
- 2차 고도화 오케스트레이터 #1 — placeZone/placeQuery(0203~0204) 위에 부하 기반 자동 배치.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (placeAuto 미주입 → 0216 비트 동일).
- placeload: 5/5 — z1~z4 placeAuto → A·B·C·A 라운드로빈 균형·부하 hostA 2/hostB 1/hostC 1·autoPlacements 4.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 오케는 존 tick 밖·배치는 제어 평면(onTick 무관). ②결정론: 최소 부하 선택+순서 tie-break 결정론. ③권위: 배치 SSOT(placement 맵)가 "누가 어디서 도나" 단일 소유. ④은닉: 클라 접점 무변. ⑤headless: Node·원격 재현. — 이상 없음.
