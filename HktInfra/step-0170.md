# step-0170 — 아이템 우편 give↔가방 transfers capstone(sagaLiveConsistent + giveOks==escrowXfers)

> 직전 step-0169 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail-core.js` — `sagaLiveConsistent()` 단언용 읽기 accessor: 우편 박스 *내부* 네 회계층 동시 닫힘 — mailConsistent(메시지 0150) AND itemConsistent(아이템 0160) AND escrowConsistent(escrow 집합 0164) AND sagaConsistent(saga 0169). 거래소 0140 의 우편 판. + verify 가 우편 giveOks == 가방 escrowXfers(두 *독립* 서비스 회계 합치·거래소 0130 의 우편 판). 계층:게임 서비스.
- 미호출 accessor → 0169 비트 동일(회귀 0·0140 capstone 패턴). verify.js 새 모드 `exmllive`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmllive`(3상태+saga·정상/회신손실+재전송 양 체제): normal giveOks/xfers 5/5·loss+retry 5/5(재실행 0)·두 서비스 합치 예·양 체제 sagaLiveConsistent 예(2/2) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — 두 서비스 회계 합치를 단언(우편 giveOks==가방 escrowXfers·은닉 경계 너머 무손실) ④은닉 우편이 가방 내부 직접 안 읽음(verify 가 양쪽 accessor 교차) ⑤headless `node run.js` 통과 — 이상 없음. **아이템 우편↔가방 saga arc(0161~0170) 닫힘** — 거래소↔가방(0117~0140)의 우편 판 완성. 우편 박스가 메시지·미읽음 배지·아이템·가방 연동(3레그+saga) 네 축으로 섰다. 주기 재전송(autoRetry·exchSweep 피기백 류)·발행 게이트 통합·길드 후속.
