# step-0170 — 아이템 우편 saga liveness capstone(mailLiveConsistent — 아이템 우편↔가방 arc 닫기)

> 직전 step-0169 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailLiveConsistent()` 단언용 읽기 accessor: `mailGiveConsistent()`(0167·give 회계) ∧ `itemConsistent()`(0160·아이템 회계)가 함께 성립. 거래소 0140 sagaLiveConsistent 의 우편 판. 0161~0169 의 세 leg·2-서비스 보존·saga 회신·보상·재전송·교차 정합이 *동시에* 닫힘을 단언. 계층:게임 서비스(우편↔가방).
- 미호출 accessor → 0169 비트 동일(회귀 0). verify.js 새 모드 `exmllive`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmllive`(4체제: 정상·보상·혼합·crash 복구): 3체제 mailLiveConsistent 예·2-서비스 보존(우편≡가방)·교차 정합(ackedOk==mailXfers)·crash 복구 후 아이템 회계+보존 전부 `예` 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — *바로 이것을 단언*(give·아이템 회계 동시 닫힘·아이템은 한 곳에만·소유자=1) ④은닉 의제 소유자 custody 분리·item_req 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. **아이템 우편 arc(0161~0170) 완성 — 리뷰 #40 닫힘**: 우편이 메시지·미읽음 배지·아이템(가방 연동)까지 선 완전한 서비스. 잔여(백로그): 재전송 상한/backoff(#37 family)·멀티프로세스 배선(#9·회신 손실 실발현)·leg2/3 실패 보상(현 leg1 만)·자기 영속 reconstruct 무압축 전제(#39).
