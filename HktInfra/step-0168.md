# step-0168 — 아이템 우편 saga 회신 재전송 + idempotent dedup(mailRetry·가방 sagaDedup)

> 직전 step-0167 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail-core.js` — `_resendPending()`: pendingGive 의 미해결 give 를 *같은 gid* 로 재발신(retries++·gives 무증가). `ackDrop` 을 drop-once(transient 손실)로 변경 → 재전송이 통과. `src/svc-mail-txn.js` — `mailRetry` op → `_resendPending()`. 가방 `sagaDedup`(0126·기존) 이 (replyTo,gid) 로 *재실행 없이 재회신*. 거래소 0126 의 우편 판. 계층:게임 서비스.
- mailRetry op 부재면 0167 비트 동일(회귀 0). verify.js 새 모드 `exmlretry`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlretry`(gid1 회신 1회 손실 → mailRetry): dedup ON acked/pend/xfers 4/0/4(재실행 0·itemConsistent 보존)·dedup OFF 4/0/5(재실행→escrowXfers++ 아이템 오배치·안전 위반) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖) ②결정론 난수 0 ③권위 — *바로 이것을 보호*(재실행 0=이중 이동 0·가방 권위 보존) ④은닉 (replyTo,gid) 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. mailRetry 는 명시 op 1회 — 타임아웃 기반 주기 재전송(autoRetry·exchSweep 피기백 류 0129)은 후속. dedup 맵 유계화(saga_done)·정합 capstone(sagaConsistent)·transfers capstone(0170) 후속.
