# step-0168 — 아이템 우편 미해결 give 재전송 멱등(mailRetry + 가방 sagaDedup)

> 직전 step-0167 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `pending` Set→Map(gid→give 파라미터·재전송 소스) + `_resendPending()`: pending give 를 *같은 gid* 로 재발신·`retries` 계측. 가방 `sagaDedup`((replyTo,gid)→원결과)이 재실행 없이 원래 결과 재회신(거래소 0126 exchRetry+sagaDedup 의 우편 판·재실행 0). 계층:게임 서비스(우편↔가방 saga).
- 배선: `topo-run.js` — `mailRetryAt` 주입(at tick 에 `mail._resendPending()`). 가방 sagaDedup 은 기존 플래그(0126) 재사용.
- OFF: `mailRetryAt` 미주입·sagaDedup OFF → 재전송 0 = 0167 비트 동일(회귀 0). verify.js 새 모드 `exmlretry`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlretry`(발신 직후 pending 재전송): gives=1·retries=1·acked=1·inv transfers=1(이중 이동 0·dedup)·item0 owner=mailcustody 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0·gid 단조 ③권위 가방 단독(dedup 재회신은 재실행 0·소유자=1 불변) ④은닉 replyTo/item_req 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. 재전송은 *무차별·무상한*(매 트리거 전체 pending 재발신) — per-give backoff·재전송 상한(거래소 0131 sagaMaxRetries 의 우편 판)은 미구현(거래소 #37 의 우편 family·멀티프로세스 무대서 무르익음). 회신 손실 자체는 인프로세스 FIFO 라 미발현(#9) — 재전송은 *멱등성*만 입증.
