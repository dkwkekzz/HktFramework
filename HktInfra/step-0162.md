# step-0162 — 아이템 우편 수령 입금 leg2(mailFetch → 우편 custody→수령자 가방 give)

> 직전 step-0161 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailFetch` 가 수령 통의 아이템마다 `_custody(item, 'mailcustody', rcpt, 'mailfetch')`(거래소 0118 buy 입금 leg 의 우편 판). 발신 인출(0161)의 짝 — 발신자서 빠진 실물이 수령자 가방에 입금. 계층:게임 서비스(우편↔가방).
- OFF: `invMode` OFF·아이템 미첨부 → give 0 = 0161 비트 동일(회귀 0). verify.js 새 모드 `exmlinv2`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlinv2`(x→h1·h2 발신·h1 만 수령): item0 owner=`h1`(수령 입금)·item1 owner=`mailcustody`(미수령 잔류)·gives=3(발신2+수령1)·itemFetched=1 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0 ③권위 아이템 권위 가방 단독(custody→수령자 쌍 거래·소유자=1) ④은닉 `item_req` 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. 수령 give 도 *fire-and-forget*(결과 회신·실패 보상 0161 과 동일 한계) — saga 회신(0165)이 닫는다. 만료 반환(0163)·2-서비스 보존(0164) 후속.
