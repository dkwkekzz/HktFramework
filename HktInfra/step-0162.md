# step-0162 — 아이템 우편↔가방 leg2: 수령 시 escrow→수신자 가방 입금(mailInv)

> 직전 step-0161 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailFetch` 가 아이템 실은 통마다 `_custody(item, 'escrow', rcpt)` 호출 → 가방에 `item_req{op:'give'}`(escrow→수신자). 아이템이 escrow 를 떠나 수신자 가방으로 입금(거래소 0118 buy leg2 의 우편 판). leg1(0161 발신 인출)의 짝 — 발신자→escrow→수신자 2-홉 custody 가 닫힌다. 계층:게임 서비스.
- 기존 OFF 플래그 `mailInv` 재사용 → give 0·가방 무변경 = 0161 비트 동일(회귀 0). verify.js 새 모드 `exmlin2`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlin2`(x→item0·item1 발신 후 h1 수령): ON gives/escrowXfers 4/4(발신2+수령2)·item0·item1 소유자 h1/h1(수령 후 가방 실 입금)·OFF 소유자 x/x(추상 escrow)·itemFetched 2 전 시드 OK.

## 척추 5항 + 한계

- ①tick 우편 박스(존 tick 밖) ②결정론 난수 0 ③권위 가방 원장 권위·쌍 거래 원자 ④은닉 item_req 명시 인터페이스 ⑤headless `node run.js` 통과 — 이상 없음. give 회신(item_result)은 여전히 fire-and-forget(saga 0165~ 후속). 만료 반환 leg3(0163)·2-서비스 보존 capstone(0164·escrow 잔액==itemHeld) 후속.
