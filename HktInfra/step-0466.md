# step-0466 — 유계 resync 가드 load-bearing 대조(ON 수렴 vs OFF 발산)

> 직전 step-0465 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/async-barrier.js` — `cfg.mzGuard` 토글(기본 ON) 추가. 계층: 코디네이션(#4 완전 async 전환). OFF 플래그 `asyncBarrier`. verify.js 새 모드 `mzguardctl`.
- `mzGuard:false` 면 `interior()` 가 무조건 true 반환(가드 우회 → 전 move 흡수 = 0461 무-가드 등가). 같은 다중 존 결합 섭동 시나리오를 가드 ON/OFF 로 대조.
- 명제: interior 유계 resync 가드는 *load-bearing* — 있으면 world==lockstep(수렴), 없으면 발산(0455 무-resync 대조의 다중 존 판).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (asyncBarrier OFF → net.step·baseline 비트 동일).
- `mzguardctl` 5/5 OK — 가드 ON 수렴 **Y** && 가드 OFF 발산 **Y** 전 시드(둘 다 성립해야 통과).
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: 대조가 가드의 결정론적 load-bearing 확증 ③권위 단일 보존 ④은닉 존 읽기 peek ⑤headless 이상 없음.
- 한계: 이상 없음. 발산은 *가드 부재*의 결정론적 결과(0461 발산과 동일 원인).
