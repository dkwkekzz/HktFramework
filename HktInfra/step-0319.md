# step-0319 — #9 후속: host 프로세스 AOI 뷰 산출 포착 (downstream 데이터 평면 씨앗)

> 직전 step-0318 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/topo-actors.js` — 런타임 EntityZone 의 net 싱크를 no-op(`{send(){}}`·뷰 *드롭*)에서 *버퍼링 싱크*(`{buf:[], send}`)로. host 프로세스가 onTick 으로 산출한 AOI 뷰(view/view_delta)를 보관 = SPINE §4 경로2 *월드 다운스트림*(host→세션 뷰) 전파의 씨앗(0282 이래 "#9 후속" 으로 미뤄둔 데이터 평면의 *내려가는* 절반).
- `src/orch-zonebridge.js` — 읽기 전용 질의 `zoneViewsFor(zoneId)`·`zoneViewFrames()` = 런타임 존이 산출해 버퍼에 쌓은 view frame 수. 버퍼는 orch 가 *읽을 뿐* 전역 net 을 안 건드림 → 미읽으면 무관.
- 계층: 코디네이션/월드(⑤②). 플래그 불요(버퍼 미읽으면 동작 불변·zoneBridge OFF 면 팩토리 미발화) → 0318 비트 동일. verify.js 새 모드 `hostzoneviews`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneBridge OFF 면 런타임 팩토리 미발화 → baseline(0318) 비트 동일.
- `hostzoneviews` 5/5 OK — z1@A 에 a1·a2 enter+이동: 런타임 onTick 이 세션별 view_delta(reset keyframe + 증분) 산출 → `zoneViewFrames()` **4**(2 세션 × 2 frame)·`zoneViewsFor(z1)` 4·미가동 존(zX) 0·총 entity 2.
- `node run.js` = ALL OK · `spine` = ALL OK(버퍼링 싱크는 prior 모드가 버퍼를 안 읽으므로 비트 동일).

## 척추 5항 + 한계

- ① tick: 뷰 산출은 런타임 onTick 안(시뮬 산출물)·포착은 읽기. ② 결정론: 시드 5종 동일. ③ 권위: 권위 존이 자기 세션 뷰만 산출. ④ 은닉: 버퍼는 orch 내부(아직 클라 미전파). ⑤ headless: Node 순수·reg 0.
- 한계: 포착만·아직 게이트웨이→클라 *전파* 미연결(후속). 버퍼는 무한 누적(전파/소비 step 에서 drain). 이상 없음.
