# step-0235 — cachecapacity 모드 spine 승급 (#16 — 3차 라운드 누적 회귀화 5/10)

> 직전 step-0234 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0225 의 bespoke `cachecapacity`(캐시 용량 LRU 회수) 검증 모드를 누적 키트로 승급(OPS/BASE 함수 내 지역화). 계층: 검증 하니스(박스 코드 무수정). OFF 플래그 없음 — 박스 `.js` 0줄 → reg 0 자명. #16 정리·도구 step 5/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `cachecapacity`: 5/5 OK — cap 2 초과 시 가장 오래된 k1 회수·size 2·capEvicted 1.
- spine ALL OK — cachecapacity 누적 회귀 편입(0225 개수 유계를 현재 src 에 단언).

## 척추 5항 + 한계

①tick: 검증 모드 추가(시뮬 외). ②결정론: setAt 최소 키 결정론 회수·Math.random 0. ③권위: 캐시는 읽기 모델(권위 무관). ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음.
