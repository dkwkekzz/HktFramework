# step-0292 — #9 멀티프로세스 배선 2: 존 host mailbox(수신 버퍼 큐)

> 직전 step-0291 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orch-zonebridge.js` — `_zoneDeliver` 에 mailbox 분기: `zoneHostMailbox` ON 시 frame 을 즉시 적용 대신 핸들 `rt.mbox` 큐에 enqueue. `_tickRuntimes` 가 onTick 전 mbox 를 FIFO 일괄 drain(소켓 수신 버퍼 + host.js per-tick deliver 배치의 씨앗·비동기 수신→tick 경계 일괄 처리). `_bridgeStart` 가 rt 에 `mbox: []` 부착. 계층: 코디네이션/월드(#9).
- 박스 `src/orchestrator.js` — `zoneHostMailbox` 플래그 + `zoneFrameQueueMax`(최대 큐 깊이)·`zoneFramesDrained`(drain 누적) 계측.
- 박스 `src/topo-build.js` — `zoneHostMailbox` opt 를 orch spec 으로 배선.
- OFF 플래그 `zoneHostMailbox`(OFF→0291 즉시 적용 = baseline 비트 동일 = 회귀 0). verify.js 새 모드 `zonembox`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `zonembox` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `zonembox`: 5/5 enter/move/leave 가 비동기 큐를 거쳐 onTick drain 돼도 flow Y·consv Y·total 1 — deliv 7·drain 7(잔류 0·무손실)·queueMax 1(실제 큐 경유 증거).
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: drain 은 런타임 존 onTick 경계(존 데이터 평면)·시뮬 tick 무관. ②결정론: 큐는 FIFO·시드 무관(같은 입력 같은 결과). ③권위 단일 소유: entitiesSingleOwner 유지. ④은닉: 큐는 직렬화된 frame 만 보유(내부 핸들 누설 0). ⑤headless·원격: tick 경계 일괄 수신은 host.js deliver cmd 와 동형(원격 분리 한 걸음 더).
- 한계: 큐는 같은 프로세스 안 Map 핸들(실 소켓 아님·#9 후속). 동일 tick 에 도착하는 op 가 적어 queueMax 1 — 폭주 수신 압력은 미관측(다중 클라 트래픽 후속). 이상 없음.
