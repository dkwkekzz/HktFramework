# step-0307 — #9 잔여(실 host.js 물리 분리) 7: host 프로세스 entity census

> 직전 step-0306 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — 읽기 전용 질의 `zoneHostCensus()`: 전 host 컨테이너의 `{total, hosts:{host:{zones, entities}}}` 분포(entity 수 = 그 host 소유 존들의 zoneRuntimes ents 합). `entityCensus`(0289·존별)의 *host 프로세스별* 판 — "어느 host 프로세스가 몇 존·몇 entity 를 지나"(부하·재배치 판단의 실 단위). graceful op(migrate/rebalance/drain)는 total 불변·host 간 분포만 재편.
- 계층: 코디네이션(⑤). 읽기 전용 → 0306 비트 동일. verify.js 새 모드 `zonehostcensus`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0306) 비트 동일.
- `zonehostcensus` 5/5 OK — 혼합 lifecycle 후: census.total 1 == totalEntities·전 host 존 합 == running.size·census host 키 == running hosts + directFlowCoherent Y·total 1·ledger 5/1/2/1.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 읽기 전용 census(tick 무관). ② 결정론: 자료구조 순회 합·시드 5종 동일. ③ 권위: host 프로세스별 분포가 전역 census·running 과 정합(중복/누락 0). ④ 은닉: orch 내부 질의. ⑤ headless: Node 순수·reg 0. 이상 없음.
