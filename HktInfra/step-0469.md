# step-0469 — 다중 존 loss+delay 하 다운스트림 뷰 수렴(desync 0)

> 직전 step-0468 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 코드(박스) 무변경 — verify 모드 `mzdownstream` 추가. 계층: 코디네이션/월드(#4 완전 async 전환·SPINE §4 경로2 다운스트림 뷰). OFF 플래그 `asyncBarrier`.
- 다중 존(grid24) 결합 loss+delay 하에서 *모든 클라의 AOI 뷰*(seenSig)가 lockstep(무섭동) 뷰와 정확히 일치 = 다운스트림 **desync 0**. 유계 resync 가 월드 상태뿐 아니라 클라가 *관찰하는 뷰*까지 수렴시킴을 단언(0459 단일 존 수렴의 다중 존+이주 판).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (코드 무변경 → src box == baseline 비트 동일).
- `mzdownstream` 5/5 OK — 전 클라 뷰 **6/6 수렴**(seenSig(on)==seenSig(off))·resyncs 18~46·delayed 26~57·handoffs 4~15.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 배달만 ②결정론: 뷰는 월드의 결정론 함수 ③권위 단일: 이주 뒤 뷰 재현 desync 0 ④은닉: 클라는 게이트웨이 뷰만 소비 ⑤headless 이상 없음.
- 한계: 이상 없음. 뷰 수렴은 world==lockstep(0464)에 함의되지만, 이 step 은 *클라 관찰 차원*을 명시 분리 단언(다운스트림 SPINE §4 경로2).
