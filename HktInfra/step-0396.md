# step-0396 — #67 orch 이중 권위 합류 3: migrate 가 orch where-view 에 write-back

> 직전 step-0395 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `_orchWriteBack(zone, host)` 헬퍼 + `migrate()` 가 placement 갱신 후 이를 호출해 `orch.running`/`orch.placement`(orch 의 제2 where 권위)도 같은 host 로 동기. migrate 후 `authoritiesAgree()` Y(0395 의 N 을 합류·이중 권위 → 단일 where 권위). orch 내부 zoneHost 컨테이너(entity 권위)는 건드리지 않는다(where-view 만). 계층: 코디네이션(#67).
- OFF 동치: placement 기반 술어(coordDesync/syncedCoherent/maxDesync) 불변 = 0395 동치. write-back 은 run() 종료 후 orch 객체에만 작용 → reg 0. verify.js 새 모드 `coordmigwb`. 새 박스·run() 미사용. cluster-coord.js 22.9KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordmigwb 5/5**: run(5)+migrate z1 A→B → authoritiesAgree **Y**·orchWhere[z1]==hostB·coordDesync 0·syncedCoherent Y.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: write-back 은 시뮬 tick 밖(제어 평면) ②결정론: 시드 5종 동일 ③권위: 두 where 권위를 단일로 합류(placement→orch.running 동기·release+acquire 보존) ④은닉: orch 공개 자료구조(running/placement Map)만·zone.js import 0 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: failover write-back 은 0397(lost 존 처리 포함)·lost-robust authoritiesAgree+report 는 0398.
