# step-0471 — 실 host.js child 경계 업스트림 1: enter intent 배달

> 직전 step-0470 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — 업스트림 seam 추가. 계층: 코디네이션/엣지(#70·#57 짝·SPINE §4 경로1). 드라이버 미부착(clusterDriverReal OFF)→run() 호출 0. verify.js 새 모드 `upclenter`.
- `intentToZoneMsg(op, from)`: 게이트웨이-형 intent(`zoneEnter`)를 존 msg(`{type:'enter', sessionId:'s:'+avatar, avatar}`)로 번역 — 게이트웨이가 하던 번역을 업스트림 경계 seam 이 재현(step-0471=enter 만·move/leave 는 0472/0476).
- `deliverIntent(cluster, host, op)`: intent 1발을 번역해 실 host.js `deliver`(zone.onMsg) 로 소켓 배달.
- 실 UpClient(`NET.UpClient`·부모 프로세스)의 zoneEnter intent 가 *실 프로세스 경계*를 넘어 실 host.js 자식의 존에 entity 를 생성(#61 in-proc UpClient 의 실 OS 프로세스 짝).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향 → baseline 비트 동일).
- `upclenter` 5/5 OK — 실 UpClient zoneEnter 발신 1 → 실 host.js 존 entity 1·a1 present **Y** 전 시드(경계 넘어 enter 적용).
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 실 존 tick 은 별 프로세스·intent 배달만 ②결정론: 시드 존 rng·enter 위치 재현 ③권위 단일: 실 host.js 존이 유일 소유 ④은닉: UpClient→게이트웨이-형 intent→seam→실 존(클라는 게이트웨이만) ⑤headless: 실 child_process host.js·UE 0.
- 한계: 이번 step 은 enter 만·단방향(뷰 반환은 0473). intent→존 msg 번역이 게이트웨이 대신 seam 에 산다(실 게이트웨이 경유 아님·번역 계약 동일).
