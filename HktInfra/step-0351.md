# step-0351 — #57 실 host.js OS 프로세스 spawn 1: hostSpawnPlan 매니페스트

> 직전 step-0350 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — `hostSpawnPlan()` 읽기 전용 매니페스트. zoneHostSnapshot(0309·host→[존…])을 *실 cluster 드라이버가 집행할 spawn 계약*으로 감싼다: `{ hosts:{host:{zones,count}}, order(정렬·결정론 spawn 순서), hostCount, zones(총계) }`. cluster.spawnOne(host)+init(zone specs)의 입력 — host.js 가 자기 makeActor 로 존 재구성(멀티프로세스-safe·존 spec 내부 미적재).
- 계층: 코디네이션(⑤·SPINE §2 오케스트레이터). OFF 플래그 없음 — 읽기 전용 질의(쓰기 0) → baseline 비트 동일. verify.js 새 모드 `hostplan`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 질의 추가 → baseline(0350) 비트 동일.
- `hostplan` 5/5 OK — z1@hostA·z2@hostB·z3@hostA 배치 뒤 `hostCount`==2·`order`==[hostA,hostB]·A=[z1,z3](count 2)·B=[z2](count 1)·`zones`==`runningCount`==3·zoneHostSnapshot bijection.
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 매니페스트는 읽기 질의(쓰기·발신 0). ② 결정론: 5종 동일·order 정렬로 결정론 spawn 순서. ③ 권위: running(집행 SSOT)과 zones 총계 정합. ④ 은닉: 직렬화 가능한 매니페스트(존 spec 내부 미적재). ⑤ headless: Node 순수·reg 0.
- 한계: 아직 매니페스트 *기술*만 — 실 cluster.spawnOne 집행은 후속(0353~). 이상 없음.
