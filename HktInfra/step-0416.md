# step-0416 — #62 코드 합류 6: warm failover — kill+promote 시나리오 번역·구동

> 직전 step-0415 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-coord.js` — `runScenario` 가 `promote`(따뜻한 standby 승격)를 처리: `kill` 과 같은 `at` 이면 동일 onTick 에서 kill→promote 원자 실행(desync 측정은 onTick *전*이라 윈도 0·maxDesync 0 보존).
- 박스 `src/cluster-run.js` — `coordScenarioFromOpts` 에 `kill`·`promote` case 추가(옛 runMulti `wire.kill`+승격 판).
- 계층: 코디네이션(#62 코드 합류 — warm failover 를 선언 시나리오로 번역·구동).
- OFF 플래그: promote 미호출이면 0410 동치·coordScenarioFromOpts 는 미부착(verify 만) → 회귀 0.
- verify.js 새 모드: `coorddelegpromote`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (run() 비트 동일·promote 휴면).
- 가설 `coorddelegpromote`: 5/5 — migrate z3 off hostA·reprovision z1@hostA_s·kill hostA·promote z1(동일 tick) → coherent(runMultiCoherent) Y·promotions 1·a1==권위 1/1·placement[z1]=hostA_s(상태 손실 0).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유(kill→promote 원자·이중 쓰기 0) ④은닉 ⑤headless — 이상 없음. kill 과 promote 를 같은 at 에 둬 desync 윈도 0(따로 두면 kill~promote 사이 maxDesync>0 로 syncedCoherent 깨짐 — 의도된 제약).
