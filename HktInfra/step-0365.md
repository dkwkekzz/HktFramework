# step-0365 — #57 실 데이터 평면 5: 실 host.js killHost + failover 재가동

> 직전 step-0364 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `failoverZone(cluster, zone, toHost, specOf)`: 죽은 host 의 존을 생존 host 에 *새 인스턴스* 재가동(상태 소실·비자발). orch `_bridgeHostDown`(0285·hostdown=새 인스턴스·소실)의 *실 child_process* 판 — 죽은 host 는 snapshot 불가라 상태 이전 불가(migrate 와 대조·정직한 한계). 실 `cluster.killHost`(child_process SIGKILL·소켓 RST·0013 기존) 활용.
- 계층: 코디네이션/월드(⑤②). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostkillreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0364) 비트 동일.
- `hostkillreal` 5/5 OK — hostA(z1+a1)·hostB(z2) → `cluster.killHost('hostA')`: `livePids` 2→1(실 프로세스 종료)·`failoverZone` z1→hostB(새 빈 존)·hostB snapshot zones==[z1,z2]·z1 의 a1 소실(죽은 host 상태 복구 불가·정직한 한계).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: kill/failover 는 제어 평면. ② 결정론: 5종 동일. ③ 권위: 죽은 host 의 존은 생존 host 단일 재가동(이중 0). ④ 은닉: 직렬화 spec. ⑤ headless: 실 child_process SIGKILL·소켓 RST 감지·생존 host 인수 — 원격 검증.
- 한계: failover 는 상태 소실(영속 스토어 연동 시 복구 가능 — 데이터 계층 후속·migrate 0364 는 graceful 이라 보존). 이상 없음.
