# step-0381 — #65 양방향 동기 1: 코디네이터 placement SSOT

> 직전 step-0380 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — 코디네이터에 `placement`(zone→실 host SSOT) 필드 + start() 가 orch.hostSpawnPlan 에서 초기화 + `placedHost(zone)` 질의. #62 가 남긴 잔여(#65): 코디네이터 lifecycle 이 실 cluster 만 바꾸고 orch 권위 placement 미갱신 → 실 migrate 후 clusterDesync stale 발산. 해법 = 코디네이터가 *placement 권위*(where)를 직접 소유(orch=entity 권위/what 분리). 이 step=SSOT 토대. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordplace`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordplace 5/5**: start 후 placement z1@hostA·z2@hostB·z3@hostA(==orch plan)·placedHost('z1')==placement·placedHost('nope')==null.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: placement 초기화는 tick 밖 ②결정론: plan.order 정렬 ③권위: placement 권위(where)를 코디네이터가 소유·orch=entity 권위(what) 분리 ④은닉: cluster API 만 ⑤headless: 실 host 동일. — 이상 없음(lifecycle 갱신은 0383~).
