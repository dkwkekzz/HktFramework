# step-0400 — #66+#67 grand capstone: 루프 중 migrate+failover 후 unifiedCoherent

> 직전 step-0399 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 코드 0 — 0399 `unifiedCoherent()`(syncedCoherent && authoritiesAgree)를 *가장 어려운* 시나리오에 적용: run 루프 *중* t=2 migrate z1 A→B + 루프 *중* t=3 failover hostA→hostB. tick placement-aware(#66·0391~0393)가 루프 도중 lifecycle 을 추종해 maxDesync 0, orch write-back(#67·0396~0397)이 두 where 권위를 합류해 authoritiesAgree → unifiedCoherent Y. **#66 tick placement-aware + #67 orch 이중 권위 합류 sub-arc(0391~0400) 닫기**. 계층: 코디네이션(#66+#67).
- 박스 무변경(verify.js 새 모드 `coordunifiedcap` 만) → reg 0(자명). cluster-coord.js 23.9KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordunifiedcap 5/5**: run(5, 루프 중 t=2 migrate·t=3 failover) → unifiedCoherent **Y**·maxDesync 0·report coherent&&authoritiesAgree Y — 대조 driver.clusterDesync **1**(옛 orch plan stale).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: lifecycle 훅은 시뮬 tick 밖(제어 평면) ②결정론: 시드 5종 동일 ③권위: 루프 중 migrate/failover 후도 placement(where) 단일 권위·orch 합류·release+acquire ④은닉: driver/cluster/orch 공개 API 만(zone.js import 0) ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: ⒜ failover 는 여전히 상태 소실(#63·lostZones 로 용인·영속 복구 후속) ⒝ orch 내부 zoneHost 컨테이너(0301~·entity 권위)는 미동기 — write-back 은 where-view(running/placement)만(컨테이너 합류는 후속) ⒞ 옛 cluster-run.js runMulti 코드 합류(#62 잔여)·업스트림 intent 실 클라(#61)·진짜 비동기(#4) 후속.
