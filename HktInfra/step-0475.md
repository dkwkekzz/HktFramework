# step-0475 — 실 host.js child 경계 업스트림 5: driveUpstream 다중 tick

> 직전 step-0474 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — `driveUpstream(cluster, upclients, ticks, zoneOf)` 추가. 계층: 코디네이션/엣지(#70). 드라이버 미부착→run() reg 0. verify.js 새 모드 `upcdrive`.
- 0471~0474 를 매-tick 루프로 통합: ⒜ upclient.onTick 발신 intent 를 capturing net 으로 포착·경계 배달 ⒝ 그 존을 실 host.js tick(move 적용+뷰 산출) ⒞ egress 뷰 되먹임. `zoneOf(zone)→host` 라우팅. 실 UpClient 가 다중 tick plan 을 경계 넘어 완결·최종 수렴.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향).
- `upcdrive` 5/5 OK — plan[[2,1],[3,0],[-1,2]] 구동·applied 4 == uc.sent(enter+3 move)·seenSig==authSig 전 시드(예: seed42 a1@3,8)·desync 0.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick: 실 존 tick 만 상태 변경·구동은 배달/되먹임 ②결정론: plan→결정론 반영 ③권위 단일: 실 host.js 존 ④은닉: 게이트웨이-형 발신/수신 ⑤headless: 실 child_process 다중 tick E2E.
- 한계: 이상 없음(단일 클라·단일 존). leave 생애주기(0476)·다중 클라(0477)·손실(0478)로 확장.
