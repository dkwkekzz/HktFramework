# step-0279 — #51b 실 zone.js 브리지 8: placeQuery 실 런타임 host 회신

> 직전 step-0278 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-control.js` `placeQuery` 핸들러 — 회신(placeReply)에 `zoneBridge` ON 일 때 `runtimeHost`(실 EntityZone 핸들의 host·`zoneRuntimeHostOf`) 필드 추가. 게이트웨이가 *추상 running 문자열*이 아니라 **실물 런타임 위치**로 라우팅(0250 running 회신의 브리지 판·읽기 경로 완성). OFF→필드 미추가 = 0250 reply 바이트 동일. 계층: 코디네이션(읽기 경로·SPINE §4 경로3).
- OFF 플래그 `zoneBridge`(기존·조건부 필드이므로 OFF→0278 비트 동일). verify.js 새 모드 `zonequery`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·placeQuery reply 바이트 동일·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonequery`**: 5/5 seed — z1 이주(hostA→hostC) 후 placeQuery 회신 `runtimeHost`=hostC==실 핸들 host==`running`==`host`(결정) 4값 일치(게이트웨이 실물 위치 라우팅).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 읽기 회신·존 tick 밖. ②결정론 — 순수 읽기. ③권위 단일 소유 — 회신은 단일 host 가리킴. ④은닉 — 게이트웨이는 orch 질의로만 위치 앎(실 런타임 주소 직접 모름·request/reply seam). ⑤headless — 실 EntityZone. 한계: 읽기 경로는 host 문자열까지(실 핸들 *연결*은 #9 멀티프로세스에서 실 소켓). 이상 없음.
