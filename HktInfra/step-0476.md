# step-0476 — 실 host.js child 경계 업스트림 6: leave 생애주기

> 직전 step-0475 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cluster-hostdriver.js` — `intentToZoneMsg` 에 `zoneLeave`→존 leave(`{type:'leave', sessionId:'s:'+avatar, avatar}`) 번역 추가. 계층: 엣지/월드(#70). 드라이버 미부착→run() reg 0. verify.js 새 모드 `upcleave`.
- 실 UpClient.leaveAt 이 발신한 종료 intent 가 경계 넘어 실 host.js 존에서 entity 를 제거 — 접속 생애주기 enter→move→leave 완결의 실 프로세스 판. `driveUpstream` 이 leave 도 자동 배달.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (드라이버 미부착·run() 무영향).
- `upcleave` 5/5 OK — leaveAt 3·applied 3(enter+move+leave)·leave 후 실 존 size 0·a1 제거 **Y** 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 이상 없음 ②결정론: 생애주기 재현 ③권위 단일: leave 로 소유 해제(공백 0) ④은닉: 게이트웨이-형 leave ⑤headless: 실 child_process.
- 한계: 이상 없음. 다중 클라(0477)·손실 하 멱등(0478)로 확장.
