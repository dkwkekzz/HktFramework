# step-0434 — #4 진짜 비동기 4: holdback 재정렬 버퍼

> 직전 step-0433 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `makeHoldback(nsites)`: 교차-site 재정렬 도착(사이트별 링크 FIFO·site 끼리 임의 인터리빙)에서 *low-water-mark 안정성*으로 전순서를 점진 방출. lwm = min_s(마지막 도착 lc) → lc ≤ lwm 인 버퍼 이벤트는 더 작은 키가 못 오므로(FIFO) 안전 방출·미도착 site 1개라도 있으면 보류. `close()` = 잔여 전순서 flush.
- 계층: 코디네이션(#4). 0433 전순서를 *스트리밍 안전 방출*로 — 전체 집합 없이도 도착하는 족족 안정분만 내보낸다.
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `lcreorder`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `lcreorder` 5/5 OK — 4 site·40 이벤트, 6 인터리빙 전부 방출열 sig == 정전 전순서(totalOrder)·`close` 이전 방출 23~37/40(진짜 holdback·끝에서 한꺼번에 정렬한 게 아님).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(방출열=내용 함수·인터리빙 불변) ③권위 ✅ ④은닉 ✅ ⑤headless ✅. 한계: 안정성은 *사이트별 링크 FIFO* 가정에 의존(현실적·TCP/순서 링크). 손실(이벤트 누락)·인과 의존(deps) 하 배달은 0435(인과 배달)·0438(손실 resync). 동시 이벤트 tie-break 은 site(인과 무관).
