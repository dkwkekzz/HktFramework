# step-0502 — mailsagaunacked 지속 손실 하 미해결 무손실 회계 (#16 라운드 4차 2)

> 직전 step-0501 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagaunacked`: 우편 saga 를 지속 손실 seam `mailAckDropAlways`(상한 없음)로 구동 — 재전송이 영영 통과 못 해도 `gives==ackedGives+pending`(새는 give 0)·`pending==pendingGive`(전량 재전송 중)·retries≥2·sagaConsistent+sagaLivenessConsistent. 미해결 give 가 *가시하고 유계*임을 단언. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 2/10 — 0501 transient(자가 치유)의 대조: 손실이 안 낫는 국면에서도 회계는 새지 않는다.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagaunacked`: 5/5 OK — gives 1·acked 0·pending 1·pendingGive 1·retries 2·sagaConsistent+liveness Y. 미해결 give 가 pendingGive 로 전량 분류(누락·중복 0).
- spine ALL OK — 손실 체제 2모드 누적 회귀 편입.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 지속 손실 5시드 동일. ③권위: 미해결 give 는 pending 에 낙관적 잔존(재실행 아님·아이템 이중적용 0). ④은닉: give 경유. ⑤headless: node in-proc. — 이상 없음(pending 잔존은 정상 — 미해결의 정직한 가시화·포기 상한은 0503에서).
