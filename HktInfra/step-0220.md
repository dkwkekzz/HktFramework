# step-0220 — 로그인 큐 재접속 세션 재개(loginReconnect·균형 라운드 닫기)

> 직전 step-0219 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/loginqueue.js` — `loginReconnect{player}` → 아직 admitted(유효 티켓) player 면 기존 티켓 그대로 재개(새 티켓 미발급·`ticketSeq` 불변·재큐 없음·멱등 resume). 티켓 만료/미발급이면 재개 불가(`reconnectMisses`·재큐 필요). 끊겼다 금방 돌아온 세션이 줄을 다시 안 선다.
- 계층: 엣지(계층1·로그인). OFF 플래그: `loginReconnect` 미주입(미발화 → 0219 비트 동일·reg 0). verify.js 새 모드 `loginreconnect`.
- 2차 고도화 로그인 큐 #2 — 대기열·TTL·백프레셔(0209~0219) 위에 재접속 연속성. **5박스 2차 균형 라운드(0211~0220) 닫기.**

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK (loginReconnect 미주입 → 0219 비트 동일).
- loginreconnect: 5/5 — p1 입장(tkt-1)→reconnect 재개(같은 tkt-1·ticketSeq 1 불변=새 티켓 0·resumes 1)·p2 미입장→재개 실패(reconnectMisses 1)·reconnects 2.
- spine: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 로그인 큐는 존 tick 밖·순수 반응형(onTick 0). ②결정론: 재개 멱등·시드 의사난수만. ③권위: admitted 가 엣지 세션 SSOT(재개는 기존 티켓 반환·새 SSOT 항 0). ④은닉: 클라 접점=게이트웨이 경유. ⑤headless: Node·원격 재현. — 이상 없음.
