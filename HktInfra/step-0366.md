# step-0366 — #57 실 데이터 평면 6: reconcile (목표 plan → 실 cluster 수렴)

> 직전 step-0365 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `reconcile(plan, cluster, specOf)`: orch `hostSpawnPlan` 목표에 실 cluster 를 수렴 — 목표 밖 host `killHost`·목표 host 미가동 시 `spawnOne`·각 존 `zoneadd`. per-event flush(0357·이벤트 기반)와 직교한 *상태 기반* 집행(외부 상태=이미 뜬 프로세스를 목표로 맞추는 표준 cluster 패턴). 반환=집행 동작 수.
- 계층: 코디네이션(⑤). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostreconcilereal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0365) 비트 동일.
- `hostreconcilereal` 5/5 OK — orch z1·z3@hostA·z2@hostB(`hostSpawnPlan`=hostA=z1/z3 hostB=z2) → 빈 실 cluster `reconcile`: 실 host.js hostA snapshot=[z1,z3]·hostB=[z2]·`livePids`==2(목표 상태 == 실 집행).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: reconcile 은 제어 평면. ② 결정론: 5종 동일·plan.order 정렬로 결정론 spawn 순서. ③ 권위: 목표==집행(plan↔실 cluster bijection). ④ 은닉: plan 직렬화·specOf 재구성. ⑤ headless: 실 host.js 다중 프로세스 수렴·snapshot 검증 — 원격.
- 한계: reconcile 은 현재 *전량 zoneadd*(이미 가동 존도 재호출·idempotent) — 증분 diff(hostSpawnDelta 활용)는 최적화 후속. 이상 없음.
