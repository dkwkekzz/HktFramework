# step-0394 — #67 orch 이중 권위 합류 1: orchWhere() 제2 where 권위 노출

> 직전 step-0393 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `orchWhere()` 새 메서드: orch 의 *집행 where-view*(zone→`orch.runningHostOf`) 스냅샷. 코디네이터 `placement`(0381~·코디네이터 where 권위)와 별개로 orch 가 들고 있는 *제2 where 권위*를 노출 — #67(이중 권위) 가시화. migrate/failover 전(정상 경로)엔 두 권위 일치, 후엔 orch 가 stale(0395 노출→0396~ write-back 합류). 계층: 코디네이션(#67).
- 읽기 전용·새 메서드. verify.js 새 모드 `coordorchwhere`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 21.6KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordorchwhere 5/5**: run(5)(migrate 없음) → orchWhere == placement Y(정상 경로 두 where 권위 일치)·coordDesync 0.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 질의는 시뮬 tick 밖 ②결정론: 시드 5종 동일 ③권위: 두 where 권위(코디네이터 placement·orch running) 노출·정상 경로 일치 ④은닉: orch 공개 API(runningHostOf)만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: 아직 *노출*만 — migrate 후 두 권위 발산은 0395 가 단언, write-back 합류는 0396~0397.
