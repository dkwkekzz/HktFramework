# step-0389 — #65 양방향 동기 9: placementCoherent bijection 불변

> 직전 step-0388 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `placementCoherent()` 추가: 코디네이터 placement(where 권위) ⟷ 실 cluster host 의 존 배치가 정확히 1:1 — ⒜ forward(placement[z]=h 인 존이 실 host h 에 존재) ⒝ reverse(실 host h 의 모든 존이 placement[z]==h). 양방향 동기의 단일 술어(placement 가 진짜 실 cluster 를 반영). 계층: 코디네이션(#65).
- verify.js 새 모드 `coordplacecoh`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordplacecoh 5/5**: run(3)+migrate z1 A→B → placementCoherent Y(placement z1@B ⟷ 실 일치)·placement 미갱신 채 실 hostB 의 z1 zonedel 주입 시 placementCoherent N(forward 위반 검출).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 측정 tick 밖 ②결정론: 정렬 비교 ③권위: placement⟷실 1:1 로 단일 소유 입증 ④은닉: cluster API 만 ⑤headless: 실 host snapshot. — 이상 없음(grand capstone 0390).
