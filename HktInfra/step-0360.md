# step-0360 — #57 실 host.js OS 프로세스 spawn 10·capstone: clusterHostsCoherent + 실 다중 host E2E

> 직전 step-0359 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-hostproc.js` — `clusterHostsCoherent()` 읽기 전용 capstone 술어: ⒜ hostContainerCoherent(존 단일 소유·컨테이너==집행·roster 회계·0308) ⒝ 드라이버 부착 시 `driverSpawns−driverDespawns`==`zoneHosts.size`(실 spawnOne/killHost 가 가동 프로세스 집합과 1:1) ⒞ `driverAssigns−driverUnassigns`==`runningCount`(실 host 가동 존과 1:1). 미부착이면 ⒜ 만.
- 계층: 코디네이션(⑤). OFF 플래그 없음 — 읽기 전용 술어 → baseline 비트 동일. verify.js 새 모드 `clusterhostcap`. **#57 드라이버 계약 sub-arc(0351~0360) 닫기**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 술어 추가 → baseline(0359) 비트 동일.
- `clusterhostcap` 5/5 OK — ① in-proc(recorder): place z1·z2@A + migrate z2→B + a1 entity 흐름 + egress 뒤 `clusterHostsCoherent` **Y**(spawn/despawn·assign/unassign 순계 == 가동 host/존 수) ② 실 Cluster: z1·z2@hostA·z3@hostB → 실 host.js **2 프로세스**(livePids 2)·hostA snapshot=[z1,z2]·hostB=[z3].
- `node run.js spine` = ALL OK (실 child_process 다중 host·정리 shutdown).

## 척추 5항 + 한계

- ① tick: 술어는 읽기·flush 는 tick 밖 제어 평면. ② 결정론: 5종 동일. ③ 권위: 드라이버 명령 순계 == 가동 host/존(단일 소유). ④ 은닉: 직렬화 spec/명령. ⑤ headless: 실 host.js 다중 자식 프로세스·소켓·snapshot — 원격 검증 통과.
- 한계: #57 *드라이버 계약 + 실 spawn/zoneadd* 닫힘. 잔여(다음 arc): deliver/egress 실 소켓 데이터 평면 집행·migrate/killHost 실 프로세스 전환·cluster-run.js runMulti 통합. 이상 없음.
