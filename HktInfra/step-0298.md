# step-0298 — #9 멀티프로세스 배선 8: 직접 라우팅 데이터 평면 단일 소유 + 정합

> 직전 step-0297 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orch-zonebridge.js` — `entityDirectCoherent()` 읽기 질의 추가: `entityCoherent()`(단일 소유 + entity 보유 런타임 모두 running·orphan 0) **AND** `zoneDirStale === 0`(낡은 host frame 이 하나도 적용 안 됨·전부 거부) → 직접 라우팅 데이터 평면 정합 + 오라우팅 누수 0 의 단일 술어. 계층: 코디네이션(#9). 읽기 전용·동작 무변경.
- OFF 경로 무관(질의만 추가·기존 플래그 OFF→이전 비트 동일 = 회귀 0). verify.js 새 모드 `gwdirsingle`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — `gwdirsingle` OFF 경로는 baseline 5묶음 모두 비트 동일(ALL OK).
- 결정론 전파·권위/수렴 — 전 모드 ALL OK(spine).
- 가설 `gwdirsingle`: 5/5 직접 라우팅 혼합 lifecycle(place×3·migrate·direct enter×5·move·leave) 후 entitiesSingleOwner·entityDirectCoherent·entityConserved(total 4==5−1)·dir==running(bijection)·gatewayZoneRoutes 7==zoneDirectApplied 7·zoneDirStale 0·gatewayZoneMisses 0.
- spine ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 질의·라우팅은 코디네이션(존 tick 밖). ②결정론: 전 op 함수·순수. ③권위 단일 소유: entitiesSingleOwner 직접 라우팅 churn(migrate 포함) 아래 유지·stale 누수 0(이중 적용 0). ④은닉: 게이트웨이 zoneDir 로만 해소·dir==running bijection. ⑤headless·원격: 전 데이터 평면이 직렬화 frame·게이트웨이 직접 라우팅으로 결정론 수렴.
- 한계: 이 모드는 graceful churn(migrate) 중심 — destructive(hostdown/stop) 혼합 + 보존 회계 종합은 capstone(0300). zone-host 는 여전히 orch 보유. 이상 없음.
