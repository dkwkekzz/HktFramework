# step-0438 — #4 진짜 비동기 8: 손실 하 gap-resync 수렴

> 직전 step-0437 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `withSseq(events)`(per-source 연속 시퀀스 부여)·`makeResyncSite(nsites)`: 수신측이 *연속분(sseq)만* 안쪽 holdback 에 넘긴다 → sseq 가 expected 를 앞지르면 hole=손실 감지(gap)·앞선 이벤트 보류·재전송(`resync`)으로 hole 채우면 frontier 전진. 손실/재정렬 아래서도 holdback 의 FIFO 불변을 *복원*(다운스트림 egress gap-resync 의 논리 클럭 판).
- 계층: 코디네이션(#4). 0434~0437(무손실 가정) 위에 *손실 복원*을 더해 substrate 의 신뢰성 마무리.
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `asynclossy`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `asynclossy` 5/5 OK — 4 site·50 이벤트, ~20% 손실(5~13개)→갭 검출 gaps 27~35·재전송 resyncs==손실 수→홀 채움→전부 배달(50/50)·digest == 정전 전순서 → **손실 하 desync 0**.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(재전송 순서 무관·내용 함수) ③권위 ✅ ④은닉 ✅ ⑤headless ✅. 한계: 재전송은 *모든 손실분이 결국 도착* 가정(영구 손실 미모델 — 실 NAK/타임아웃 상한은 다운스트림 egress 와 동형, 후속 배선 arc). gaps 는 hole-앞선-도착 횟수(검출 신호)·정밀 hole 목록은 `missing()`.
