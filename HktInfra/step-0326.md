# step-0326 — #9 후속: 다중 존 독립 다운스트림 (존 간 뷰 격리)

> 직전 step-0325 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 질의 `zoneViewSessions(zoneId)` = 그 존이 뷰를 산출한 sessionId 집합(정렬). 다중 존이 동시에 돌 때 각 존이 *자기 세션에만* 뷰를 내보내는지(존 간 누수 0·공간 분할 격리) 검증의 단위.
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0325 비트 동일. verify.js 새 모드 `hostzoneisolate`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0325) 비트 동일.
- `hostzoneisolate` 5/5 OK — z1@A(a1·a2)·z2@B(b1) 동시 가동: `zoneViewSessions(z1)` == {s:a1,s:a2}·`zoneViewSessions(z2)` == {s:b1}(존 간 누수 0)·z1 entered 에 b1 없음·z2 entered 에 a1/a2 없음(격리).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 각 존 onTick 독립·검증 읽기. ② 결정론: 5종 동일. ③ 권위: 각 존 권위가 자기 세션 뷰만. ④ 은닉: 게이트웨이 발신·존 간 무누수. ⑤ headless: Node 순수·reg 0.
- 한계: 존 경계 ghost/핸드오프 후 교차 가시(boundary AOI)는 별개(존 분할 기존 메커니즘). 이상 없음.
