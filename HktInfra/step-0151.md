# step-0151 — 우편 미읽음 배지 읽기 모델(mailFeed — svc.mail.sent 구독→수신자별 unread)

> 직전 step-0150 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/svc-mailfeed.js` — **MailFeed**: 우편 박스가 발행하는 `svc.mail.sent`(0144)를 *소비만* 해 수신자별 `{unread, sent}` 투영. `unreadOf`·`sentOf`·`totalUnread`·`crash`·`digest`. 거래소 MarketFeed(0112)의 우편 판 — 우편함 권위 0·발신 0(순수 반응형·존 tick 밖). 계층:게임 서비스.
- 배선: `net-core.js`(번들 require) · `topo-actors.js`(`kind:'mailfeed'`→MailFeed) · `topo-build.js`(`mailFeed && mail`→박스 add·buildSubs 전달) · `topo-subs.js`(`mailFeed && mail && mailSentPublish`→`svc.mail.sent`→mailfeed 구독) · `topo-run.js`(run 결과 `mailfeed` 노출).
- OFF 플래그 `mailFeed`(기본 false) → 박스·구독 0 = 0150 비트 동일(회귀 0). verify.js 새 모드 `exmailfeed`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · 권위 보존(feed 는 권위 0·소비만) · spine ALL OK.
- 가설 `exmailfeed`(5통: h1×3·h2×2): unreadOf(h1)=3·unreadOf(h2)=2·totalUnread=5·mail.sent=5 — feed==우편 권위(`예`) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 반응형(onTick 0) ②결정론 난수 0 ③권위 우편함 권위 불변(feed 는 파생 뷰·쓰기 0) ④은닉 버스 구독만(발행자 주소 무지) ⑤headless `node run.js` 통과(UE 0) — 이상 없음. 0151 은 svc.mail.sent 만 소비(unread 단조 증가) — 읽음/만료로 깎는 반영은 0152~0153, 그 전엔 unread==sent.
