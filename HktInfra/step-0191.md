# step-0191 — 길드 금고(Guild Bank) deposit (guildBank·guildDeposit·bank arc 0191~0200 출발)

> 직전 step-0190 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — 길드 *공유 아이템 원장*(금고/vault: guildId→[itemId]). `guildDeposit{guildId,member,itemId}` → 멤버가 아이템을 길드 금고에 예치. 멤버만(비멤버·미존재 길드 graceful no-op·은닉)·집합 의미론(중복 멱등 no-op·중복 0). 권위 단일 소유(척추 ③·itemId 는 금고 1곳). 로스터/마스터십과 직교. 거래소 escrow 0117·우편 custody 0157 의 *조직 공유* 판.
- `src/topo-build.js` — `guildBank` 플래그 → guild opts.bank.
- OFF: `bank`(guildBank) OFF·guildDeposit 미주입이면 금고 0 = 0190 비트 동일(reg). guildOps 가 이미 임의 op 라우팅(topo-run 무변경). verify.js 새 모드 `guildbank`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node run.js`/`reg` ALL OK — bank OFF·미주입 → 0190 비트 동일.
- **가설 `guildbank`**: 5/5 시드 OK — g1 vault `["sword","shield"]`·deposits 5(중복·비멤버·미존재 길드 포함)·g2(미존재) 금고 0·중복 0(멱등).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 금고 예치 존 tick 밖(순수 반응형). ②결정론: 5/5 동일 vault. ③권위 단일 소유: itemId 는 길드 금고 1곳·중복 0. ④은닉: 비멤버/미존재 길드 graceful no-op(로스터 선결). ⑤headless·원격: Node·run.js 증명. — 이상 없음. 후속: withdraw(0192)·발행·영속·배지·정합 capstone·가방 escrow 연동.
