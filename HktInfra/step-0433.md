# step-0433 — #4 진짜 비동기 3: 결정론 전순서·인과 존중

> 직전 step-0432 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `totalOrder(events)`(키 (lc, siteIndex)·tie-break site)·`orderSig`·`totalOrderSound`. 인과(부분순서)는 동시 이벤트의 적용 순서를 안 정한다 → 결정론 수렴엔 *전순서* 필요. Lamport 전순서는 *내용의 함수*라 어떤 물리 도착 순열이든 같은 전순서를 낸다. clock condition 덕에 인과 존중(동시 이벤트만 tie-break·인과 무관).
- 계층: 코디네이션(#4). 0432 인과 간선 위에 전순서를 얹어 0434 holdback·0436 수렴이 쓸 *유일·결정론* 적용 순서 확립.
- OFF 플래그: 없음 — run() 경로 미호출 → **reg 구조적 0**. verify.js 새 모드 `lcorder`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `lcorder` 5/5 OK — 4 site·40 이벤트, 8 물리 순열(Fisher-Yates 셔플) 전부 같은 `orderSig`(동일 전순서)·엄격(동률 키 0)·인과 존중(모든 간선 a→b 위치 보존).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(전순서=내용 함수·순열 불변) ③권위 ✅ ④은닉 ✅ ⑤headless ✅(순수 JS). 한계: 전순서는 *전체 이벤트 집합이 손에 있을 때* 계산 — 스트리밍/부분 도착 하 *언제 적용해도 안전한가*(holdback 안정성)는 0434. 의외: siteIdx 가 lamportExchange 의 numeric site 와 클럭 라벨('s0') 둘 다 받게 보정.
