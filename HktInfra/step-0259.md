# step-0259 — 캐시 namespace 무효화 (SCAN+DEL)

> 직전 step-0258 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/cache.js` — `cacheDeletePrefix{prefix}` 핸들러 추가. 계층: 데이터(캐시). 플래그 없음(새 메시지 타입·미수신→0258 비트 동일=reg 0).
- 메커니즘: prefix 로 시작하는 *모든* 키를 store(+writeThrough 면 source)서 제거(store·source 키 합집합 victims·setAt·keyTtl·negatives 정리). 한 유저 세션 전체("session:") 또는 길드 해체("guild:42:") 한 방에 무효화. 단일 delete(0257)의 패턴판. Redis SCAN+DEL 더미판.
- verify.js 새 모드 `cacheprefix`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(cacheDeletePrefix 미수신 → 비트 동일).
- `cacheprefix`(가설): 5/5 — set session:a,session:b,item:c → deletePrefix "session:" → session 2개 제거(false)·item:c 생존(true)·prefixDeleted 2.
- spine: ALL OK(누적 회귀 + cacheprefix 편입).

## 척추 5항 + 한계

①tick: 캐시 박스 존 tick 밖·순수 반응형 ②결정론: 시드 무관·해시 무변경 ③권위: namespace 일관 제거(WT 면 소스까지) ④은닉: 메시지 계약만 ⑤headless: Node 직접 구성 검증. 이상 없음.
