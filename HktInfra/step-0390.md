# step-0390 — #65 양방향 동기 10·grand capstone: syncedCoherent (migrate/failover 포함 정합)

> 직전 step-0389 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `syncedCoherent()` grand capstone 술어: `maxDesync==0`(연속 루프) && `coordDesync==0`(현 entity 정합·lost 제외) && `placementCoherent`(placement⟷실 1:1). 0380 coordCoherent 가 *migrate/failover 를 제외*해야 했던 것과 대조 — 양방향 동기(0381~0389)로 *migrate/failover 를 포함한* 전체 lifecycle 뒤에도 참. #65 양방향 동기 sub-arc(0381~0390) 종합. 계층: 코디네이션(#65).
- verify.js 새 모드 `coordsyncedcap`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 19.9KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordsyncedcap 5/5**: run(5)+migrate z1 A→B+failover hostA→hostB → **syncedCoherent Y**·placementCoherent Y·coordDesync 0 — 대조로 **driver.clusterDesync 1**(옛 #65 버그·orch plan stale·0380 이 migrate/failover 제외한 이유).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 술어는 tick 밖 ②결정론: 시드 5종 동일 ③권위: placement(where)+orch(entity)+lost 추적으로 migrate/failover 후 단일 소유 유지 ④은닉: driver/cluster API 만(zone.js import 0·require 0) ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: ⒜ failover 는 여전히 상태 소실(#63·lostZones 로 coherence 정의에 용인만·영속 복구 후속) ⒝ orch 의 *내부* zoneHost/zoneRuntimes 는 미갱신(코디네이터 placement 가 별도 권위) — orch 자체 placement API 와의 합류는 후속 ⒞ cluster-run.js 옛 lockstep runMulti 합류(#62 잔여)·업스트림 intent 실 클라(#61)·진짜 비동기(#4) 후속.
