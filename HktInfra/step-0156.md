# step-0156 — 미읽음 배지 질의 인터페이스(mailUnreadQuery→mailUnreadReply)

> 직전 step-0155 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mailfeed.js` — onMsg 가 `{type:'mailUnreadQuery', rcpt}` 요청에 `{type:'mailUnreadReply', rcpt, unread}` 회신(`net.send(addr, m.from, …)`). `queriesRx`·`repliesSent`·`_lastReply` 계측. request/reply over net(SPINE §4 경로3·프레즌스 0069 presenceQuery 의 우편 판). 순수 읽기(배지 무변경). 계층:게임 서비스.
- 배선: `topo-run.js` — `mailFeedQuery` 주입(at tick 에 게이트웨이→mailfeed 로 질의 전달·클라/운영 배지 조회 모델).
- OFF: 질의 미수신(`mailFeedQuery` 미제공) = 0155 비트 동일(회귀 0). verify.js 새 모드 `exmlfq`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlfq`(h1 3통·h2 2통 입금 후 두 배지 질의): queriesRx=2·repliesSent=2(1:1)·회신 unread==질의 시점 배지(`예`)·배지 3/2 전 시드 OK.

## 척추 5항 + 한계

- ①tick 반응형(질의 응답·존 tick 무관) ②결정론 난수 0 ③권위 배지 무변경(순수 읽기) ④은닉 request/reply over net(직접 접근 0) ⑤headless `node run.js` 통과 — 이상 없음. 배지 질의로 MailFeed arc 의 읽기 경로 완비(push 발행 + pull 질의). 다음: 아이템 첨부 우편(0157~).
