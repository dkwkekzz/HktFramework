# step-0347 — #9 후속: 게이트웨이 수신 버퍼 유계화(다운스트림 메모리 상한)

> 직전 step-0346 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/gateway.js` `_recvZoneView` — 세션별 수신 버퍼 `zoneViewIn` 에 유계 창 `downRecvWindow`(K) 적용: 전달 후 frame 은 클라(DownClient)가 보유하므로 게이트웨이는 *최근 K* 만 보관(`a.length > K → shift`) → per-세션 메모리 O(K) 상한. 계측 `downRecvPeak`. K=0 무계(이전).
- `src/topo-actors.js`·`src/topo-build.js`·`src/topo-boxes.js` — `downRecvWindow` opt 배선(makeActor 가 게이트웨이에 속성 주입).
- 계층: 엣지(④⑤·버스 seenBound 0042·수신함 유계 0099 의 다운스트림 판). 플래그 `downRecvWindow` 0(기본)→무계 = baseline 비트 동일. verify.js 새 모드 `dcwindow`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — downRecvWindow 0 → 무계(shift 0) → baseline(0346) 비트 동일.
- `dcwindow` 5/5 OK — a1@dc0 6 이동(rx **7**) + K=2: `downRecvPeak` **2**·`gatewayViewsFor('s:a1')` **2**(유계·무계면 7)·클라 `convergedTo` **Y**(버퍼 유계가 전달·수렴 무영향·desync 0).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 버퍼 관리는 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 무영향. ④ 은닉: 게이트웨이 내부 회계. ⑤ headless: Node 순수·reg 0.
- 한계: 수신 버퍼는 게이트웨이 회계용(전달은 즉시·클라가 상태 보유) — 창은 관측/디버깅 보존분. 이상 없음.
