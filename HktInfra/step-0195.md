# step-0195 — 길드 금고 저널 스냅샷 압축 (guildSnapshot 의 금고 확장)

> 직전 step-0194 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — 0185 의 스냅샷 압축을 금고로 확장: 스냅샷에 로스터뿐 아니라 *vault 도* 담고(bank: [[guildId,[itemId...]]...]) 이하 저널 가지치기 → 저널은 tail 만(유계). `reconstruct()` 가 스냅샷 guilds+bank 에서 출발해 tail(seq>upToSeq)만 replay → 전체 저널 replay 와 비트 동일(무손실).
- OFF: `guildSnapshot` 0(snapInterval 0)이면 압축 0·저널 무계 = 0194 비트 동일(reg). verify.js 새 모드 `guildbanksnapshot`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — snapInterval 0·금고 미사용 시 스냅샷 미발화 → 0194 비트 동일.
- **가설 `guildbanksnapshot`**: 5/5 시드 OK — snapInterval 4 → snaps 2·tail 1 < full 9·reconstruct vault `[i2,i4,i5,i6]` == 무압축 reconstruct(무손실).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 압축/복원 존 tick 밖. ②결정론: 5/5 동일 tail·vault. ③권위 단일 소유: 스냅샷+tail 가 itemId 금고 1곳 유지. ④은닉: 자기 저널/스냅샷만. ⑤headless·원격: run.js 증명. — 이상 없음. 후속: bank 배지 읽기 모델(0196·GuildFeed 금고 아이템 수).
