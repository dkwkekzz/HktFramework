# step-0435 — #4 진짜 비동기 5: 인과 의존 배달

> 직전 step-0434 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `causalDeliver(events, edges, arrival)`: 각 이벤트의 직접 인과 선행(`deps` = happens-before 간선 a→e 의 a)을 명시 추적해, 선행이 모두 배달되기 전엔 보류·충족 시 연쇄 방출. + `depsFromEdges`·`causalViolations`. holdback(0434)의 *사이트별 FIFO 가정*보다 강하다 — FIFO 없이 *어떤* 적대적 도착 순열이든 원인→결과 보존(DAG 라 deadlock 0).
- 계층: 코디네이션(#4). 전순서(0433)·FIFO holdback(0434) 위에 *FIFO-free 인과 보장*을 더해 손실/재정렬 substrate(0438) 토대.
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `lccausal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `lccausal` 5/5 OK — 4 site·44 이벤트, 적대적 도착 6종(발신 역순·전순서 역전·셔플 4) 전부 인과 위반 합 **0**·미배달(stuck) **0**(전부 결국 배달).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(deps=DAG·시드 PRNG) ③권위 ✅ ④은닉 ✅ ⑤headless ✅. 한계: 인과 배달은 *인과 순서*만 보장(동시 이벤트 적용 순서는 도착 의존) — 결정론 *수렴*(두 site 같은 최종 상태)엔 전순서(0433)와 결합 필요 → 0436(async 수렴). 손실(deps 영영 미도착) 하 resync 는 0438.
