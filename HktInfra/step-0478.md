# step-0478 — 실 host.js child 경계 업스트림 8: 소켓 손실 하 멱등 수렴

> 직전 step-0477 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 코드(박스) 무변경 — 손실 wire(`{drop:0.3, dropSeed}`)로 실 소켓 유실을 주입하는 verify 모드 `upclossy` 추가. 계층: 엣지/월드(#70·SPINE §5 ②③). 손실 wire 미주입→run() 무영향·reg 0.
- rpc 가 결정론 재전송(`resends`)하고 host.js 가 reqId 멱등(replyCache)으로 중복 cmd 를 재실행 없이 dedup(`idempotentHits`) → 재전송된 intent 가 경계 넘어 *정확히 한 번* 적용(exactly-once) → 여전히 seenSig==authSig 수렴. 손실이 진짜 발생(dupCmds>0)했는데 위치가 안 어긋남이 exactly-once 증거.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` = ALL OK (손실 wire 미주입·run() 무영향).
- `upclossy` 5/5 OK — resends 6~21·dupCmds 3~9(실 재전송)·host idempotentHits 2~9(dedup 발동)·seenSig==authSig 수렴 전 시드.
- `spine` = ALL OK.

## 척추 5항 + 한계

- ①tick 이상 없음 ②결정론: 손실 하 재전송+멱등→exactly-once 재현 ③권위 단일: 중복 cmd 재적용 0(멱등)→위치 정확 ④은닉: 게이트웨이-형 경유 ⑤headless: 실 child_process·소켓 손실.
- 한계: 이상 없음. 손실은 소켓 wire drop(cmd/res 양방향)·intent 자체 유실은 rpc 재전송이 복원(신뢰 왕복).
