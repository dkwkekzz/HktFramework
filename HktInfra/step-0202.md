# step-0202 — 인스턴스(던전) 서버: despawn (수명주기 SSOT 완성)

> 직전 step-0201 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/instance.js` — `instanceDespawn{instanceId}` 추가: active 에서 제거(없는 id 멱등 graceful no-op)·`retired` 누적(실제 종료만). 계층: **월드(계층2)**. 0201 spawn 의 짝 → 던전/매치 *일회성 수명*(떴다 사라짐) SSOT 완성. 존(영속)과 수명주기 분리 명시.
- 배선 변경 없음(0201 의 `instanceOps` seam 재사용 — instanceDespawn op 도 같은 경로). OFF 플래그 **`instanceService`**(OFF→박스 0=0200 비트 동일).
- verify.js 새 모드 **`instancedespawn`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(instanceService 미제공 → 0200 비트 동일).
- **가설 `instancedespawn`**: 5/5 시드 OK — spawn 3 + despawn 2(d2 종료 + 없는 d9 멱등 no-op) → **active 2·retired 1**(실제 종료만)·d1/d3 isActive·d2 아니오.
- **결정론**: 5 시드 동일 active/retired. **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(onTick 없음·despawn onMsg 반응) ② 결정론 ✅(`Math.random` 0) ③ 권위 단일 소유 ✅(active SSOT·despawn 멱등) ④ 은닉 ✅(instanceOps orch 발신) ⑤ headless ✅(Node·UE 0).
- 한계: 인스턴스 박스 *기본 통신 완비*(spawn/despawn). 클라 라우팅·오케스트레이터 연동 spawn(수요 기반)은 후속(0203~ 오케스트레이터 배치). 멀티프로세스 미배선(#9).
