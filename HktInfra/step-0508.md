# step-0508 — mailsagafailpub 영구 실패 발행 E2E·수명주기 발행 삼종 완비 (#16 라운드 4차 8)

> 직전 step-0507 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagafailpub`: 영구 실패 발행 `mailFailPublish`+audit. 영구 실패 시 `svc.mail.saga_failed` 1회 발행 → 버스 → audit 수신. `failPublished==permFailed`(1:1) AND `audit.seen('svc.mail.saga_failed')==permFailed`. 포기(0504)·재admission(0506) 발행의 종결 마디 — saga liveness 수명주기 발행 삼종(abandon/readmit/fail)이 은닉 통신으로 완비. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 8/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagafailpub`: 5/5 OK — permFailed 1·failPublished 1·auditSeen 1·sagaConsistent+liveness Y. 종결 발행 == 영구 실패 수 == 감사 수신.
- spine ALL OK — 손실 체제 8모드 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 발행/수신 5시드 동일. ③권위: 종결 발행은 관측일 뿐 상태 변경 아님. ④은닉: 우편→버스→audit 토픽 pub/sub(발행 삼종 abandon/readmit/fail 전부 감사 신규 소비). ⑤headless: node in-proc. — 이상 없음(saga 수명주기 발행이 포기→재개→종결로 완비 — 운영이 미해결 give 의 전 생애를 버스 관측만으로 추적).
