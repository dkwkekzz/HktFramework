# step-0423 — #61 업스트림 intent 실 클라 3: UpClient 양방향 (자기 AOI 뷰 수신)

> 직전 step-0422 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/client.js` — `UpClient` 에 수신 추가(DownClient 동형): `onMsg(view/view_delta→seen)`·`seenIds`/`seenSig`/`convergedTo`. enter 가 uc0 발신이라 게이트웨이가 세션→uc0 바인딩 → 자기 AOI 뷰가 uc0 으로 전달됨. 발신+수신 = 실 양방향 클라.
- 계층: 엣지/월드(#61).
- OFF 플래그: upClients=null(기본)→스폰 0→회귀 0.
- verify.js 새 모드: `uprecv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK.
- 가설 `uprecv`: 5/5 — uc0 발신(sent 3)+수신(deltas 3)·seenIds [a1]·seenSig a1@9,8(권위 위치 반영).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 ④은닉(클라는 게이트웨이만) ⑤headless — 이상 없음. 수렴(desync 0) 정식 단언은 0424.
