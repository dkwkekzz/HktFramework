# step-0158 — 아이템 우편 수령(itemFetched — 보유→수령 아이템 이동)

> 직전 step-0157 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailFetch` 가 보유→수령 이동 시 아이템 실은 통수만큼 `itemFetched++`(아이템도 read 로 이동). reconstruct fetch 분기·digest R 행에 item 동봉. 회계 itemHeld→itemFetched 전이. 계층:게임 서비스.
- OFF: `mailItem` OFF·아이템 미첨부 → itemFetched 0 = 0157 비트 동일(회귀 0). verify.js 새 모드 `exmlitf`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlitf`(h1 3통[2아이템]+h2 1아이템·h1 만 수령): itemSent=3·itemHeld=1·itemFetched=2·itemSent==itemHeld+itemFetched+itemExpired(`예`)·crash 복구 정합(`예`) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0 ③권위 우편함 단일 소유 ④은닉 우편 박스 내부 ⑤headless `node run.js` 통과 — 이상 없음. 아이템 수령은 *우편 박스 내 회계 전이*(itemHeld→itemFetched) — 수령자 가방 입금(give)은 백로그(가방 연동). 만료 회수(0159)·아이템 회계 capstone(0160) 후속.
