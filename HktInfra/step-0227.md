# step-0227 — 월드 영속 write-behind 버퍼(worldBuffer/worldFlush·쓰기 지연 배치)

> 직전 step-0226 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/worldlog.js` — `worldBuffer{intent}`: intent 를 durable 로그가 아닌 *버퍼*에 모은다(쓰기 지연·매 intent 디스크 안 때림). `worldFlush`: 버퍼를 durable 로그에 일괄 적층(배치·순서 보존). flush 전 버퍼는 비-durable(crash 윈도 = write-behind 의 본질).
- 계층: 6(데이터·월드영속) · OFF 플래그: worldBuffer/worldFlush 미수신(버퍼 빈 채 → 0226 비트 동일=회귀 0) · verify.js 새 모드 `worldwb`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `worldwb`: 5/5 OK — 2 intent 버퍼→flush→로그 2·버퍼 0·flushed 2 / 추가 pickup 버퍼(1)만→로그 2 불변·replay 에 gold 미반영(비-durable)
- spine ALL OK

## 척추 5항 + 한계

①tick: 쓰기 지연·배치로 매 intent 의 동기 디스크 I/O 를 tick 밖으로 — 신성한 tick 강화. ②결정론: flush 순서 보존(seq 단조)·시드 PRNG. ③권위: 로그는 월드 상태 유일 쓰기 경로(intent). ④은닉: 명시 메시지만. ⑤headless: Node·원격 재현. — 한계(설계 의도): 미flush 버퍼는 비-durable(crash 시 소실) — flush 주기가 durability 윈도. fsync durable barrier 는 0228.
