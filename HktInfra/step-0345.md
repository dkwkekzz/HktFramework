# step-0345 — #9 후속: 다중 클라 교차 관찰자 일치(겹친 뷰 desync 0)

> 직전 step-0344 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/client.js` `DownClient.seenPos(id)` — 그 클라가 보는 한 entity 의 위치 서명(`'x,y'`|null). 두 클라가 겹친 AOI 의 *공유* entity 를 같은 위치로 보는지 비교용. 척추 "겹친 뷰의 참여자는 일치" 의 다운스트림 판.
- 계층: 월드/엣지(②⑤·수렴 desync 0). 플래그 없음·읽기 헬퍼 → baseline 비트 동일. verify.js 새 모드 `dcagree`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 헬퍼 추가 → baseline(0344) 비트 동일.
- `dcagree` 5/5 OK — a1·a2 상호 가시 시 dc0·dc1 모두 a1=(13,13)·a2=(13,15) 로 본다: 공유 entity 위치 `dc0.seenPos == dc1.seenPos == zoneEntityPos`(서로 일치 && host 권위 일치·**교차 관찰자 desync 0**).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 클라 적용은 메시지 처리(시뮬 외). ② 결정론: 5종 동일. ③ 권위: 두 클라 뷰가 같은 권위로 수렴. ④ 은닉: 각 클라 게이트웨이만. ⑤ headless: Node 순수·reg 0.
- 한계: 인프로세스 DownClient·정적 일치. 다중 클라+손실+lifecycle 합성 capstone 은 후속(0346). 이상 없음.
