# step-0458 — #4 실 net.step 배리어 실제 치환 8: 다중 존+핸드오프 통합

> 직전 step-0457 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/verify.js` — 배리어를 *다중 존*(2 존·공간 분할·핸드오프/이주) 토폴로지에 구동 `barmultizone`: holdback 이 월드 입력을 발신 순서(m.id)로 재구성해 제자리 슬롯 방출하므로 이주(핸드오프)가 있어도 투명 → world/log==lockstep. 코드 박스 무변경(0453 holdback 경로 구동) → reg 구조적 0.
- 계층: 코디네이션(#4). 배리어가 단일 존을 넘어 다중 존/이주 토폴로지에 통합됨을 확증.
- OFF 플래그: `asyncBarrier`(미설정→net.step 그대로 = **reg 구조적 0**). verify.js 새 모드 `barmultizone`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `barmultizone` 5/5 OK — 6 client·2 존·70 tick: `run({asyncBarrier})` worldDigest == lockstep Y·logDigest == Y·handoffs 17~20(이주 실발생)·held 366(월드 입력 holdback 통과).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅ ③권위 단일 ✅(이주=release+acquire 쌍 거래·소유자 1) ④은닉 ✅ ⑤headless ✅. 한계(정직): 다중 존 *투명* 통합(holdback 재구성)은 완결 — 단 손실/지연 *섭동*의 다중 존 판은 이주 전 resync 지연 유계 필요(단일 존 0454~0457 에서 검증·이주 하 유계 resync 는 후속). asyncBarrier OFF→reg 구조적 0.
