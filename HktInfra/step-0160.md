# step-0160 — 아이템 우편 회계 정합 capstone(itemConsistent — itemSent==itemHeld+itemFetched+itemExpired)

> 직전 step-0159 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `itemConsistent()` 단언용 읽기 accessor: 아이템 1개는 매 순간 정확히 한 상태(보유 itemHeld·수령 itemFetched·만료 itemExpired)에 분할(공백·중복 0)·`itemSent == 셋의 합`. 0157~0159 전이의 창발 불변. 0150 mailConsistent(메시지 통수 판)의 아이템 판. 계층:게임 서비스.
- 미호출 accessor → 0159 비트 동일(회귀 0·0150·0140 capstone 패턴). verify.js 새 모드 `exmlitc`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlitc`(4체제: 수령만·만료만·혼합·crash 복구): 4체제 itemConsistent 예(4/4)·분할 수령만 0/2/0·만료만 0/0/2·혼합 1/1/1·crash→reconstruct 정합+digest 보존 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — *바로 이것을 단언*(아이템 1개=보유/수령/만료 정확히 한 상태) ④은닉 우편 박스 내부 읽기 ⑤headless `node run.js` 통과 — 이상 없음. 아이템 우편 arc(0157~0160) 닫힘 — 우편 박스가 메시지(0142~0150)·미읽음 배지(0151~0156)·아이템(0157~0160) 세 축으로 섰다. 만료 아이템 발신자 반환·수령 시 가방 입금 등 가방(inventory) 연동 2-서비스 보존(거래소↔가방 0117~0120 류)이 다음 자연 확장(백로그).
