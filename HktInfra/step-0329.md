# step-0329 — #9 후속: 다운스트림 뷰 무손실 회계 (고아 frame 0)

> 직전 step-0328 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-views.js` — 읽기 전용 술어 `zoneViewConserved(zoneId)` = 산출된 모든 view frame 이 *정확히 한 세션*에 귀속되는가: ⒜ 모든 view/view_delta frame 이 비어있지 않은 sessionId(주소 없는 배달 불가 frame 0) ⒝ 세션별 total 합 == 전체 frame 수(분배 무손실). host 산출 뷰가 빠짐없이 배달 주소를 갖는다.
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0328 비트 동일. verify.js 새 모드 `hostzoneconsv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0328) 비트 동일.
- `hostzoneconsv` 5/5 OK — z1 에 a1·a2·a3 enter + 각자 이동: `zoneViewFrames` **8**·세션별 total 합 **8**(== frames)·모든 frame 에 sessionId·`zoneViewConserved` **true**(고아 frame 0).
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출 onTick 안·검증 읽기. ② 결정론: 5종 동일. ③ 권위: 각 frame 단일 세션 귀속(무손실 배달 가능성). ④ 은닉: 게이트웨이 발신. ⑤ headless: Node 순수·reg 0.
- 한계: 배달 *가능성*(주소 보유) 회계만(실 전송 무손실은 게이트웨이→클라 재전송/ack 후속). 이상 없음.
