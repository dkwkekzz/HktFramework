# step-0289 — #56 브리지 존 데이터 평면 9: graceful(rebalance/drain) census 보존

> 직전 step-0288 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `entityCensus()` {total, zones:{zoneId:count}}(전 런타임 entity 분포 스냅샷). graceful 재배치(rebalance/drain)는 `_migrate`(같은 핸들·0273)를 거치므로 entity total 무손실 보존(분포만 재편) — hostdown/stop(파괴)과 대조. 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0288 비트 동일=회귀 0·읽기 전용). verify.js 새 모드 `zonegraceful`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zonegraceful` 5/5: z1·z2·z3@hostA + enter(total 4) → rebalance(분산) + drain(hostB) → `total=4` 보존·`single=Y`·`coherent=Y`·`rebMoves=2`·`drainMoves=1`(graceful op 실발생).
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: rebalance/drain 은 orch 제어 평면·존 tick 밖. ②결정론: `Math.random` 0. ③권위 단일 소유: graceful 이주는 같은 핸들 release+acquire·중간 공백/중복 0·total 보존. ④은닉: 게이트웨이는 placeRebalance/placeDrain 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 한계: graceful(rebalance/drain·무손실) vs destructive(hostdown/stop·손실)의 entity census 대비가 데이터 평면으로 확정. 전 데이터 평면 capstone(혼합 lifecycle 후 census·단일 소유·정합 동시)은 0290.
