# step-0272 — #51b 실 zone.js 브리지 1: orch 존 런타임 레지스트리

> 직전 step-0271 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/orch-zonebridge.js`(믹스인) — `_bridgeStart(zoneId,host)` + 존 런타임 질의(`zoneRuntimeOf`·`zoneRuntimeHostOf`·`runtimeCount`). 계층: 코디네이션(존 spawn = orch 정의 책임·SPINE §2).
- `orch-placement.js` `_start` 에 가드 1줄: `zoneBridge` ON 이면 `_bridgeStart` 도 호출 → placeZone 집행이 *실 EntityZone 인스턴스*를 host 에 띄움(zoneRuntimes 레지스트리). 0241~0250 의 `running`(zoneId→host 문자열) 추상을 실 zone.js 런타임에 연결.
- `topo-actors.js` makeActor: zoneBridge ON 일 때 orch 에 `zoneFactory`(zoneId→`new EntityZone(fnv1a(zid), {grid,radius})`) 주입(직렬화 불가 함수이므로 spec 아닌 액터 구성 시점·멀티프로세스-safe). `topo-build.js`: `zoneBridge` 불린 + `zoneRtGrid/Radius` 를 orch spec 으로 전달.
- OFF 플래그 `zoneBridge`(기본 false → `_start` 가드 통과 = 0271 비트 동일). verify.js 새 모드 `zonebridge`.

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(zoneBridge OFF → 전 시나리오 비트 동일·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**(전 누적 모드·E2E 14프로세스 비트동일).
- **가설 `zonebridge`**: 5/5 seed — placeZone(z1→hostA·z2→hostB) 집행 후 runtimeCount=2·zoneStarts=2·z1 host=hostA·z2 host=hostB·실 EntityZone 핸들(ents Map+isAuthority) Y·멱등 재-place(z1 같은 host) 신규 인스턴스 0.
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 브리지는 제어 평면(_start 집행)·존 tick 밖. ②결정론 — 런타임 시드=zoneId 해시(Math.random 0). ③권위 단일 소유 — 한 존=한 런타임 핸들(zoneRuntimes 단일 키). ④은닉 — orch 가 존을 spawn 하는 것은 정의 책임(위반 아님)·팩토리 주입으로 zone.js 직접 import 회피. ⑤headless — 실 EntityZone 헤드리스 인스턴스. 한계: 0272 는 *레지스트리*만 — _migrate/_stop 의 실 런타임 이주·존 entity 상태 전송은 후속(0273~). 이상 없음.
