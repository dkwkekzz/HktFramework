# step-0164 — 아이템 우편 2-서비스 보존(mailCustodyItems — 보유 우편 아이템 ≡ 가방 mailcustody 소유)

> 직전 step-0163 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailCustodyItems()` 단언용 읽기 accessor: 보유(held) 우편이 든 아이템 id 집합. 가방의 `'mailcustody'` 소유 집합과 일치해야 함(거래소 0120 `escrowItemIds`≡가방 escrow 의 우편 판). 세 leg(인출·입금·반환)가 in-transit 아이템을 보유 우편과 정확히 일치시킴(공백·중복 0). 계층:게임 서비스(우편↔가방).
- 미호출 accessor → 0163 비트 동일(회귀 0). verify.js 새 모드 `exmlinv4`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlinv4`(혼합: item0/1 수령→h1·item3 만료→x 반환·item2 보유): mailCustodyItems()=`[item2]` == 가방 mailcustody 소유 `[item2]`(합치 `예`)·item0→h1·item3→x 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — *바로 이것을 단언*(아이템은 발신자/custody/수령자 중 한 곳에만·보유 우편≡custody·소유자=1 보존) ④은닉 가방 byOwner 는 가방 권위·우편은 자기 보유만 단언 ⑤headless `node run.js` 통과 — 이상 없음. 보존은 *give 가 모두 성공*함을 전제(0161~0163 fire-and-forget) — give 실패 시 우편 보유≠custody 가능(0166 보상이 닫음). saga 회신(0165)이 give 결과를 우편에 돌려준다.
