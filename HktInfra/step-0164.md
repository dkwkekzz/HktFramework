# step-0164 — 아이템 우편↔가방 2-서비스 보존 capstone(escrowItemIds·escrowConsistent)

> 직전 step-0163 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — 우편이 escrow custody 중이라 믿는 itemId 집합 `escrowIds`(발신 시 add·수령/만료 시 delete·invMode 일 때만·`_custody` 에서 방향으로 판정). 단언용 read accessor `escrowItemIds()`(가방 'escrow' 소유 집합과 교차 정합)·`escrowConsistent()`(우편 내부 itemHeld==escrowIds.size). reconstruct 가 저널 replay 로 escrowIds 재계산(custody 재발행 없이). 계층:게임 서비스.
- 미호출 accessor·invMode OFF 시 escrowIds 미변경 → 0163 비트 동일(회귀 0·거래소 0120·0140 capstone 패턴). verify.js 새 모드 `exmlin4`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlin4`(3상태 동시: item0 수령→h1·item1 만료반환→x·item2 보유→escrow): 우편 escrowIds==가방 escrow=={item2}(집합 일치 예)·escrowConsistent 예·minted 3·분포 h1/x/escrow·crash→reconstruct 정합+digest 보존 전 시드 OK.

## 척추 5항 + 한계

- ①tick 읽기 accessor(존 tick 무관) ②결정론 단언만(난수 0) ③권위 — *바로 이것을 단언*(아이템 1개=수령자/발신자/escrow 정확히 한 곳·두 서비스 일치) ④은닉 우편이 가방 내부를 직접 안 읽음(verify 가 양쪽 accessor 교차 비교) ⑤headless `node run.js` 통과 — 이상 없음. 아이템 우편↔가방 arc(0161~0164) 닫힘 — 거래소↔가방(0117~0120)의 우편 판. give 는 fire-and-forget(무손실 가정) — 회신 비동기 수신·실패 보상은 saga(0165~·거래소 0121~ 류) 후속.
