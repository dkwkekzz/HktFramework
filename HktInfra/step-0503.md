# step-0503 — mailsagaabandon 재시도 상한 → 포기 국면 (#16 라운드 4차 3)

> 직전 step-0502 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 새 mode `mailsagaabandon`: 지속 손실 + 재시도 상한 `mailMaxRetries`=2. gid 당 N회 재전송 후 포기 → `giveAbandoned`=1·`pendingGive`=0(재전송 중단)·`abandonedGive`=1(재admission 대기)·`pending` 잔존. 미해결의 두 번째 분할 이행(pendingGive→abandonedGive) 발현·`pending==abandonedGive`·sagaLivenessConsistent. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. arc 3/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정.
- 가설 `mailsagaabandon`: 5/5 OK — giveAbandoned 1·pendingGive 0·abandonedGive 1·pending 1·sagaConsistent+liveness Y. 상한 도달 give 가 재전송 중단되고 재admission 대기로 이행(pending 무공백 잔존).
- spine ALL OK — 손실 체제 3모드 누적 회귀.

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 상한 도달 5시드 동일. ③권위: 포기는 *재전송 중단*일 뿐 abort 아님 — give 가 실제 성공했을 수 있어 pending 낙관적 잔존(아이템 이중적용 0). ④은닉: give 경유. ⑤headless: node in-proc. — 이상 없음(포기 give 는 abandonedGive 로 간직해 손실 해소 후 재개 가능·재admission 은 0505에서).
