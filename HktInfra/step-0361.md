# step-0361 — #57 실 데이터 평면 1: 실 host.js deliver E2E

> 직전 step-0360 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_zoneDeliver` 의 `clusterDriver.onFrame(rt.host, zoneId, f)` 호출에 frame `f` 동봉(실 deliver 페이로드).
- `src/cluster-hostdriver.js` — `onFrame(h,z,frame)` 이 `{op:'deliver',host,zoneId,frame}` 적재 + `flush` deliver 가 frame 동봉 시 실 host.js `{cmd:'deliver',items:[{gi:0,m:{to:zoneId,from,payload}}]}` 집행(실 프로세스 `zone.onMsg` 적용). frame 미동봉(0357 mock)→zoneId 만(하위 호환).
- 계층: 코디네이션/월드(⑤②·SPINE §4 경로1 데이터 평면). OFF 플래그 `clusterDriverReal`(미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostdeliverreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0360) 비트 동일.
- `hostdeliverreal` 5/5 OK — z1@hostA·a1 enter 뒤 실 host.js spawn+zoneadd+deliver: snapshot 의 a1 위치 `{x:5,y:5}` == in-proc orch `zoneEntityPos('z1','a1')` `{x:5,y:5}`(enter 만 rng 소비·tick 무관 → 실 소켓 데이터 평면 desync 0).
- `node run.js spine` = ALL OK (실 child_process·정리 shutdown).

## 척추 5항 + 한계

- ① tick: onFrame 은 전송 seam·flush 는 tick 밖. ② 결정론: 5종 동일·존 시드=zoneId 해시 → 실/in-proc enter 위치 동일. ③ 권위: 실 프로세스 zone 이 권위 재현(desync 0). ④ 은닉: deliver items 직렬화 메시지(m.to=존 addr). ⑤ headless: 실 host.js zone.onMsg·snapshot — 원격 검증.
- 한계: deliver-only(이동 없음) — move 는 onTick 적용이라 실 tick 집행이 후속(0363). 이상 없음.
