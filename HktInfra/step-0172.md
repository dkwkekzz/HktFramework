# step-0172 — 아이템 우편 saga 자동 주기 재전송(mailAutoRetry)

> 직전 step-0171 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/svc-mail-txn.js`(mailSweep 핸들러)에 `if (this.autoRetry) this._resendPending();` 1줄 — ttl 체크 *앞*(autoRetry 와 TTL 회수 직교·같은 주기 신호 sweep 재사용). 계층: 게임 서비스(우편)·서비스간 saga.
- `src/svc-mail-core.js`: `this.autoRetry = opts.autoRetry || false;` 필드. `src/topo-build.js`: `mailAutoRetry=false` 기본 + 배선(`autoRetry: mailAutoRetry`).
- OFF 플래그 `mailAutoRetry`(OFF→sweep 은 TTL 회수만 = 0171 비트 동일). verify.js 새 모드 `mailauto`.
- 메커니즘: 회신 손실로 pending 에 남은 give 를 *명시 mailRetry op(0168) 없이* 주기 mailSweep 신호만으로 자동 재발신(거래소 0129 exchSweep autoRetry 의 우편 판). 가방 dedup(0168)이 재실행 0 보장.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: autoRetry OFF = 0171 비트 동일(전 시스템 5시드 cur==prev) — `결과: ALL OK`.
- **결정론 전파**: repro 멀티+인프로세스 동일 무변경(run.js ALL OK).
- **권위/수렴**: 권위 단일 소유·desync 0 무변경.
- **가설 `mailauto`**: 5시드 전부 — OFF pending/retries=1/0(명시 RETRY 없어 잔존)·ON 0/1(주기 sweep 만으로 drain)·ON 닫힌 고리(gives==acked)·양 체제 sagaConsistent 예.
- **spine**: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 우편은 존 tick 밖 별 박스·sweep 은 외부 주기 op — 무변경. ②결정론 코어: 세계 쓰기 경로 무변경(재전송은 가방 give·dedup 으로 멱등). ③권위 단일 소유: 우편함/가방 원장 권위 불변(재전송은 재회신만 유도·재실행 0). ④은닉·단일 연결: 클라 와이어 무변경(saga 는 서비스간 경계). ⑤headless·원격 검증: Node·5시드 재현.
- 이상 없음. (다음: saga 재시도 상한 mailMaxRetries·거래소 0131 의 우편 판.)
