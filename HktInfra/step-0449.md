# step-0449 — #4 실 net.step 배리어 치환 9: lockstep 배리어 등가(실 engine Net 대조)

> 직전 step-0448 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `runLockstepEngine(events, seed, avatars, nsites)`: 이 arc 에서 처음으로 **실 engine Net**(중앙 lockstep 배리어 `net.step()`)을 실체로 세운다 — client 발신 actor 들 + 존 수신 actor 를 `net.register`·`net.step()` T tick 구동(중앙 큐 하나 = 배리어). 존은 도착 intent 를 버퍼링→전순서 적용(substrate)→실 월드. 명제: **배리어(net.step)로 배달하든 배리어-free substrate 로 배달하든 존 실 월드 == canonical** → 배리어를 substrate 로 치환해도 결과 불변.
- 계층: 코디네이션(#4). 실 net.step 배리어와 배리어-free substrate 의 실 월드 등가 확립.
- OFF 플래그: 없음 — async-net run() 미호출(자체 Net 인스턴스는 verify 전용) → **reg 구조적 0**. verify.js 새 모드 `netbarrier`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netbarrier` 5/5 OK — 4 client·40 intent: 실 net.step 배리어 delivered == 40(무손실)·**배리어+substrate == canonical** Y·**배리어-free async(M=4·손실) == canonical** Y. naive갈림 N = 배리어의 중앙 FIFO 가 이미 일관 순서를 제공(그 *보장*을 substrate 가 배리어 없이 재현) — raw 순열 갈림은 0442/0446 에서 확증됨.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(배리어·배리어-free 둘 다 canonical) ③권위 단일 ✅ ④은닉 ✅(실 Net register/send/step) ⑤headless ✅(검증 전용). 한계(정직): 등가는 *실 월드 상태(canonical)* 수준 — 이 verify 는 자체 Net 인스턴스를 세울 뿐 run() 의 실 net.step 을 아직 *치환*하지 않는다(in-proc 등가 먼저·실제 치환은 후속 arc). async-net 은 run() 밖 substrate.
