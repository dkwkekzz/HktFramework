# step-0511 — guildbankdeposit 금고 예치 escrow 실 이동 (#46 금고↔가방 escrow 1)

> 직전 step-0510 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`src/svc-guild.js`+`svc-guild-txn.js`+`topo-boxes.js`+`topo-build.js` — 금고↔가방 escrow 실연동 arc(0511~0520·#46) 시작. 0191~0200 금고 vault 는 itemId *문자열*만 보유(가짜 escrow)였다. 새 OFF 플래그 `guildBankInv` + `_custody` 헬퍼: guildDeposit 이 `_custody(멤버→'escrow')` give 를 가방에 보내 아이템이 *실제로* 멤버 가방서 빠져 escrow 소유로 이동(거래소 list leg 0117 의 금고 판). inv/invMode·gives·escrowIds 추가. 계층: 3 게임 서비스. verify 새 모드 `guildbankdeposit`. `guildBankInv` OFF→_custody no-op→give 0→0510 비트 동일.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — guildBankInv OFF→가방 give 0·박스 비트 동일(net-core+박스 = baseline).
- 가설 `guildbankdeposit`: 5/5 OK — gives 1·inv.ownerOf(item0)==escrow(실 이동)·vault ["item0"]·escrowXfers 1(가방 escrow 회계 발현).
- spine ALL OK — #46 arc 첫 모드 편입.

## 척추 5항 + 한계

①tick: 금고는 존 tick 밖 순수 반응형·박스 무변. ②결정론: give 5시드 동일. ③권위: 아이템이 멤버 가방→escrow 단일 이동(가방이 원장 권위·중복 0)·vault 는 길드 소유 기록. ④은닉: 금고는 `item_req` give 요청만(가방 내부 모름). ⑤headless: node in-proc. — 이상 없음(예치 인출 leg 만·입금 leg 는 인출 0512).
