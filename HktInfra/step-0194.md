# step-0194 — 길드 금고 영속·failover (guildPersist 의 금고 확장·저널 replay)

> 직전 step-0193 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-guild.js` — 0184 의 변경 저널을 금고로 확장: 예치/인출 성사를 durable 저널에 append(kind deposit/withdraw). `crash()` 가 vault 도 비우고(휘발 projection), `reconstruct()` 가 저널을 seq 순 replay 해 vault 재구성(로스터와 같은 저널·같은 루프) → 죽기 전과 비트 동일. 금고 미사용 시 vault 빈 Map → 비우기·복원 모두 빈 Map.
- OFF: `guildPersist` OFF 면 저널 0·crash 후 빈 금고(소실) = 0193 비트 동일(reg). verify.js 새 모드 `guildbankpersist`. 계층: **3 게임 서비스**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: ALL OK — persist OFF·금고 미사용 시 vault 빈 Map → 0193 비트 동일.
- **가설 `guildbankpersist`**: 5/5 시드 OK — crash→reconstruct vault `[potion,shield]`==pre(예치 3·인출 1)·OFF post 빈 금고(소실).
- **spine**: ALL OK.

## 척추 5항 + 한계

①신성한 tick: 영속/복원 존 tick 밖. ②결정론: 5/5 동일 vault. ③권위 단일 소유: replay 가 itemId 금고 1곳 유지. ④은닉: 자기 저널만으로 복원. ⑤headless·원격: run.js 증명. — 이상 없음. 한계: 저널 무계(0195 스냅샷 압축으로 vault 포함). 후속: bank 저널 스냅샷 압축(0195).
