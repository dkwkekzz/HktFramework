# step-0380 — #62 runMulti 통합 10·grand capstone: coordCoherent (broker 측 제어 평면 E2E)

> 직전 step-0379 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `coordCoherent()` grand capstone 술어: `maxDesync==0`(연속 루프 내내) **&&** 현 `clusterDesync==0`(지금). broker 측 제어 평면(start→연속 run→drift→syncPlan 자가 치유)이 SPINE §5 수렴을 실 프로세스 경계 넘어 *지속적으로* 만족함을 단일 술어로. #62 runMulti 통합 sub-arc(0371~0380) 종합. 계층: 코디네이션(#62).
- verify.js 새 모드 `coordcap`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordcap 5/5**: start→run(5)[maxDesync 0]→z3 drift→syncPlan 치유(z3 복원 Y)→coordCoherent Y·report.coherent Y. host→실 host.js child_process 데이터 평면을 *상주 코디네이터*가 구동·정합.
- **spine ALL OK**. cluster-coord.js 10.9KB(<30KB).

## 척추 5항 + 한계

①tick: 코디네이터 루프가 실 host zone.onTick 만 구동 ②결정론: maxDesync 0·시드 5종 동일 ③권위: in-proc orch SSOT·실 cluster 재현·단일 소유 ④은닉: driver/cluster API 만(zone.js 직접 import 0) ⑤headless: 실 host.js child_process E2E·원격. — **한계(후속)**: ⒜ migrate/failover 는 orch-side placement 미갱신이라 capstone 시나리오엔 미포함(0375/0376 이 직접 검증) — orch 권위 placement 와 실 cluster lifecycle 양방향 동기는 후속 ⒝ runMulti 자체(cluster-run.js)와의 코드 합류는 미연결(코디네이터가 runMulti 핵심을 상주화했으나 옛 lockstep runMulti 와 별 경로) ⒞ 업스트림 intent 실 클라(#61)·진짜 비동기(#4) 후속.
