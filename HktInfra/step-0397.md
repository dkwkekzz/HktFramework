# step-0397 — #67 orch 이중 권위 합류 4: failover 도 orch where-view 에 write-back

> 직전 step-0396 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `failover(deadHost, toHost)` 가 죽은 host 의 존을 toHost 로 재가동하며 placement 갱신 + `_orchWriteBack(z, toHost)` 로 orch.running/placement 도 동기. migrate(0396)+failover(0397) 둘 다 거친 lifecycle 뒤에도 `authoritiesAgree()` Y. lost 존도 *where* 는 toHost 로 합의(entity 손실은 coordDesync 가 0386 으로 별도 제외). 계층: 코디네이션(#67).
- OFF 동치: placement 기반 술어 불변 = 0396 동치. write-back 은 run() 종료 후 orch 객체에만 작용 → reg 0. verify.js 새 모드 `coordfailwb`. 새 박스·run() 미사용. cluster-coord.js 23.1KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordfailwb 5/5**: run(5)+migrate z1 A→B+failover hostA→hostB → authoritiesAgree **Y**·orchWhere[z3]==hostB·syncedCoherent Y·lost 1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: write-back 은 시뮬 tick 밖 ②결정론: 시드 5종 동일 ③권위: failover 후도 두 where 권위 단일 합류·lost 존 where 합의 ④은닉: orch 공개 Map 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: authoritiesAgree 가 lost 존을 명시 처리하진 않음(현 시나리오는 write-back 후 우연히 일치) — lost-robust authoritiesAgree + report 노출은 0398·복합 lifecycle 정합은 0399·capstone 0400.
