# step-0501 — mailsagatransient 일시 회신 손실 자가 치유 (#16 라운드 4차 1)

> 직전 step-0500 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 완전 saga liveness *손실 체제* arc(0501~0510·STATE §2 ⒜) 시작. 새 mode `mailsagatransient` + 공용 하니스 `runMailLoss`: 우편 아이템 saga 를 내장 손실 seam `mailAckDrop`(1회 드롭)로 구동해 *실제 회신 손실*을 주입, autoRetry sweep 재전송이 ack 를 재유도 → pending 0 drain·`gives==ackedGives`·retries≥1(손실 발현). 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. 0491~0500 서비스 capstone 이 *행복 경로*(0,0,0 자명 참)만 봤던 sagaLivenessConsistent 를 손실 하에서 비자명 검증하는 arc 의 1/10.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `mailsagatransient`: 5/5 OK — gives 1·acked 1·pending 0·retries 1·sagaConsistent+sagaLivenessConsistent Y. drop-once 손실이 재전송으로 자가 치유(미해결 0).
- spine ALL OK — 서비스 saga 누적 회귀에 손실 체제 첫 모드 편입(ORDER +1).

## 척추 5항 + 한계

①tick: 우편 존 tick 밖·박스 무변. ②결정론: 손실+재전송 대조·Math.random 0·5시드 동일. ③권위: 아이템 alice→escrow 단일 소유·재전송은 *재회신* 유도(재실행 아님·가방 dedup). ④은닉: give 경유·item_req. ⑤headless: node in-proc. — 이상 없음(transient 손실은 재전송으로 스스로 낫는다·pending 잔존은 지속 손실 국면 0502~에서 발현).
