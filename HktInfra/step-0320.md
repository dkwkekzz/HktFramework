# step-0320 — #9 후속: host 산출 뷰의 AOI 정확성

> 직전 step-0319 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — 읽기 전용 질의 `zoneViewBuf(zoneId)`(산출 뷰 원본 {to,payload} 배열)·`zoneVisibleIds(zoneId, avatar)`(반경 R AOI 가시 entity 집합·zone.visibleFor). host 가 산출한 view 의 enter 가 진짜 AOI(가까운 것만·먼 것 제외)와 일치하는지 검증하는 창.
- 계층: 코디네이션/월드(⑤②). 읽기 전용 → 0319 비트 동일. verify.js 새 모드 `hostzoneaoi`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 읽기 전용 추가 → baseline(0319) 비트 동일.
- `hostzoneaoi` 5/5 OK — z1 결정론 위치(존 시드 고정·run 시드 무관): a1(5,5)·a2(13,15)·Chebyshev 거리 10 > 반경 4 → 서로 AOI 밖: 각 세션 reset view_delta enter == `zoneVisibleIds`(a1→[a1]·a2→[a2]·자기만)·모든 frame `to`=='gateway'.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 뷰 산출은 onTick 안·검증은 읽기. ② 결정론: 존 시드 고정 → 위치·뷰 5종 동일. ③ 권위: 권위 존이 자기 세션 AOI 뷰 산출. ④ 은닉: 게이트웨이 주소로 발신(클라 직접 아님). ⑤ headless: Node 순수·reg 0.
- 한계: 정지 상태 AOI 만 검증(이동 중 증분 델타 정확성은 후속)·아직 게이트웨이→클라 전파 미연결. 이상 없음.
