# step-0482 — bare2ecap grand capstone 누적 회귀 승격 (#16 라운드 2차 2)

> 직전 step-0481 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0460 의 bespoke `bare2ecap`(run() net.step 중앙 배리어를 async-barrier stepper 로 *실제* 치환한 grand capstone)을 누적 회귀로 승격(MODES+ORDER 등록·`run`/`worldDigest`/`logDigest`/`check`/`pad` 지역 helper 재사용). 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. `src/verify.js` 순수 셸. #16 라운드 2차 2/N — 옛 grand capstone HEAD 재검증 격차 해소 이어감.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일).
- 가설 `bare2ecap`: 5/5 OK — 단일 존 world/뷰==lockstep·exactly-once(moveDup0)·섭동 실재(resync r2~21·delay d9~27)·다중 존 world/log 투명(handoffs>0).
- spine ALL OK — bare2ecap + mze2ecap 둘 다 누적 회귀 포함(0451~0460 실 배리어 치환 + 0461~0470 다중 존 유계 resync 를 현재 src 에 항구 단언).

## 척추 5항 + 한계

①tick: 검증 모드 승급·박스 onTick 무변. ②결정론: run 결과 단언·Math.random 0. ③권위: world==lockstep·exactly-once 로 소유 보존 재단언. ④은닉: 영향 없음. ⑤headless: node 단언. — 이상 없음.
