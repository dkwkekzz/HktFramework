# step-0403 — #62 runMulti 합류 3·복원력: 상태 보존 restart

> 직전 step-0402 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `restart(zone, newHost)` 상주 복원력 메서드 + `restarts` 계측: 죽기 *전* `snapshot` 으로 상태를 굳히고 `killHost`(프로세스 사망·RAM 소실) 후 `newHost` 에 `spawnOne`+`zoneadd`+`loadstate` 로 상태 복원·placement/orch where-view 갱신. *계획적* 재시작(업그레이드·정비)으로 entity 무손실 — `failover`(0376·비자발·상태 소실)와 대조. runMulti `invRestart`(`cluster-run.js:90`)의 zone cluster 판. socketDead 가 host 단위 영속이라 newHost(새 id)로 재가동(runMulti `inventory_r` 와 동형). 계층: 코디네이션(#62).
- OFF 동치: restart 미호출이면 0402 동치. verify.js 새 모드 `coordrestart`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 29.4KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordrestart 5/5**: run(5)→restart(z1, hostA_r) → a1 위치 보존(pre {8,8} == post {8,8})·placement[z1]=hostA_r·restarts 1·coordDesync 0.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: restart 는 제어 평면 lifecycle ②결정론: 시드 5종 동일 a1 {8,8} ③권위: snapshot→kill→loadstate 로 단일 소유 보존(pre-kill 스냅샷=무손실·release+acquire) ④은닉: cluster.rpc/killHost/spawnOne API 만 ⑤headless: 실 host.js child_process kill+respawn E2E. — **한계(후속)**: restart 는 *계획적*(pre-kill snapshot 가능)만 — 비자발 crash 후 상태 복구는 월드 영속(#63·intent 로그/snapshot) 묶음. reprovision+mirror(0404~0405)·clusterInfo(0406)·runScenario(0407)·capstone(0410).
