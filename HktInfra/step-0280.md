# step-0280 — #51b 실 zone.js 브리지 9·capstone: 전 계층 정합

> 직전 step-0279 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `orch-zonebridge.js` — `fullyCoherent()` 전 계층 정합 질의 추가(#51b capstone primitive): placement(결정)==running(집행)==zoneRuntimes(실물) **세 층 완전 일치** — ⒜ `placementDrift`=0(결정==집행·0245) ⒝ `bridgeCoherent`(집행==실물·0278) ⒞ `placedCount`==`runtimeCount`(결정 수==실 런타임 수). 참이면 추상 SSOT 와 실 zone.js 런타임이 한 몸. 읽기 전용. 계층: 코디네이션.
- OFF 플래그 `zoneBridge`(기존·질의이므로 OFF→0279 비트 동일). verify.js 새 모드 `zonecapstone`. **#51b 실 zone.js 브리지 arc(0272~0280) 닫기.**

## 검증 (수치 = `node run.js` 출력)

- **reg 0**: `verify reg` ALL OK(OFF·maxDiff 0).
- **결정론/권위/수렴**: `run.js all` = **ALL OK**.
- **가설 `zonecapstone`**: 5/5 seed — 7종 op(start·auto·migrate·rebalance·drain·hostdown·stop) 혼합 후 `fullyCoherent`=Y·`runtimeCount`==`runningCount`==`placedCount`==3(z4 stop·move 류 집합 보존·공백/중복 0).
- spine ALL OK.

## 척추 5항 + 한계

①신성한 tick — 읽기 질의·전 브리지 op 가 제어 평면(존 tick 밖). ②결정론 — 순수 읽기·전 op 결정론 tie-break. ③권위 단일 소유 — 한 존=한 host(결정·집행·실물 3층 일치)·공백/중복 0. ④은닉 — orch 집행 책임·게이트웨이는 질의로만 위치 앎. ⑤headless — 실 EntityZone 헤드리스 인스턴스. 한계: 브리지는 orch 인프로세스 핸들 레지스트리까지 — 실 *프로세스* 분리(host.js 소켓)는 #9 멀티프로세스, entity 트래픽의 실 zone.js 흐름도 그 위에서. #51b 는 "추상 running→실 EntityZone 런타임 lifecycle 구동" 을 완성. 이상 없음.
