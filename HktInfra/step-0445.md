# step-0445 — #4 실 net.step 배리어 치환 5: 손실 하 per-client sseq gap-resync

> 직전 step-0444 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-net.js` — `makeZoneResync(nsites)`: 실 intent 에 per-client 연속 시퀀스(sseq)를 붙여, 수신 존이 *연속분만* 안쪽 holdback 에 넘긴다 → sseq 가 expected 를 앞지르면 hole=손실 감지·재전송(resync)으로 채우면 frontier 전진(async-core.makeResyncSite 재사용). 실 전송의 손실(net.step transport loss)을 substrate 가 복원. 손실+재정렬 아래서도 방출열 == totalOrder → simFold 수렴.
- 계층: 코디네이션(#4). 배리어 없이도 손실 복원으로 존 상태 수렴 유지.
- OFF 플래그: 없음 — async-net run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `netlossy`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netlossy` 5/5 OK — 4 client·40 intent·~20% 손실·K=6: 방출 simFold == canonical(**수렴**) Y·gaps ≥ 10(**손실 감지** 실동작)·resyncs ≥ 4(**재전송** 실동작).
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅ ③권위 단일 ✅ ④은닉 ✅ ⑤headless ✅(검증 전용). 이상 없음 — 손실+재정렬 교란에도 sseq gap-resync 로 정전 수렴. 한계: 재전송 소스는 검증 하니스(실 재전송 프로토콜 배선은 후속). async-net 은 run() 밖 substrate.
