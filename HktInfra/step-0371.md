# step-0371 — #62 runMulti 통합 1: ClusterCoordinator 골격 + start()

> 직전 step-0370 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/cluster-coord.js` — `ClusterCoordinator(orch, cluster, specOf, driver)`: orch(in-proc 권위)+실 cluster+ClusterHostDriver 를 묶는 *broker 측 제어 평면 상주* 객체. `start()` = orch.hostSpawnPlan 목표로 driver.reconcile(미가동 host spawnOne+각 존 zoneadd+목표 밖 killHost) 호출해 실 cluster 토폴로지 수렴. 계층: 코디네이션(#62 cluster-run.js runMulti 통합 — verify ad-hoc 구동을 상주 코디네이터로 이전).
- `src/net-core.js` — 진입점에 `__p('cluster-coord')` 등록(dual-mode export). run() 데이터 평면 미사용 → 호출 0 = baseline 비트 동일.
- verify.js 새 모드 `coordstart`(+ 공유 시나리오 빌더 `coordScenario`).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 새 박스·run() 미사용 → `reg` 5/5 net.log+상태 0x… baseline 비트 동일.
- **coordstart 5/5**: 2 host·3 zone(z1@A·z2@B·z3@A) — coord.start()→acted 5(host 2 spawn + 존 3 zoneadd)·topoOk Y(각 host 가 자기 plan roster 소유)·livePids==hostCount(2) Y.
- **spine ALL OK** (전 시리즈 누적 회귀·E2E 비트동일).

## 척추 5항 + 한계

①tick: 토폴로지 수렴은 tick 밖 제어 평면 ②결정론: plan.order 정렬 spawn·시드 무관 ③권위: orch 가 목표 SSOT·reconcile 가 실 cluster 를 맞춤 ④은닉: 코디네이터는 driver/cluster API 만 ⑤headless: 실 host.js child_process spawn·원격. — 이상 없음(start 는 토폴로지만, 데이터 평면 tick 은 0372~).
