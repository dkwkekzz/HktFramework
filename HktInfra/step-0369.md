# step-0369 — #57 실 데이터 평면 9: clusterDesync 정합 술어

> 직전 step-0368 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-hostdriver.js` — `clusterDesync(orch, cluster)`: 실 host.js entity 위치가 in-proc orch 권위와 어긋난 수(양방향 — 실에 있는데 권위 위치 불일치/부재 + 권위에 있는데 실에 부재). desync 0 = 실 프로세스 경계 넘어 모든 entity 가 권위 재현(SPINE §5 수렴). 실 cluster 데이터 평면의 단일 건강 지표.
- 계층: 코디네이션/월드(⑤②). OFF 플래그 `clusterDriverReal`(드라이버 미주입→호출 0 = baseline 비트 동일). verify.js 새 모드 `hostdesyncreal`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — 드라이버 미주입 → baseline(0368) 비트 동일.
- `hostdesyncreal` 5/5 OK — z1@A(a1+3move)·z2@B(b1) `driveCluster` 뒤 `clusterDesync`==0(전 entity 실 host 위치==권위). 민감도: 실 host 에만 권위 밖 `ghost` entity 주입+tick → `clusterDesync`==1(술어가 불일치를 실제로 검출·by-construction 아님).
- `node run.js spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: 술어는 읽기 snapshot. ② 결정론: 5종 동일. ③ 권위: desync 0 = 실 프로세스 entity 가 in-proc 권위 단일 재현. ④ 은닉: snapshot 직렬화. ⑤ headless: 실 host.js 위치 vs 권위 비교 — 원격 검증.
- 한계: 술어는 *위치* 비교(세션/뷰 등 다른 상태는 별 술어) — 단 ghost 주입 검출로 by-construction(#53) 아님 입증. 이상 없음.
