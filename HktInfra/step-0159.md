# step-0159 — 아이템 우편 만료 회수(itemExpired — 만료 우편의 아이템 회수)

> 직전 step-0158 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/svc-mail.js` — `mailSweep` 만료 시 아이템 실은 통수만큼 `itemExpired++`(itemHeld→itemExpired 전이). reconstruct expire 분기도 box 의 mm.item 으로 itemExpired 집계(저널 무변경). 계층:게임 서비스.
- OFF: `mailItem` OFF·아이템 미첨부 → itemExpired 0 = 0158 비트 동일(회귀 0). verify.js 새 모드 `exmlitx`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 · 결정론 5시드 동일 · spine ALL OK.
- 가설 `exmlitx`(h1 수령·h2 만료·h3 생존 아이템 각 1): itemSent=3·held/fetch/exp=1/1/1·itemSent==분할합(`예`)·crash 복구 정합(`예`) 전 시드 OK.

## 척추 5항 + 한계

- ①tick 존 tick 밖 ②결정론 난수 0·정렬 순회 ③권위 우편함 단일 소유 ④은닉 우편 박스 내부 ⑤headless `node run.js` 통과 — 이상 없음. 만료된 아이템은 *우편 박스 내 회수 회계*까지 — 발신자 가방 반환(give)은 백로그(가방 연동). 아이템 회계 capstone(0160)이 itemSent==itemHeld+itemFetched+itemExpired 를 모든 체제로 닫는다.
