# step-0386 — #65 양방향 동기 6: coordDesync 가 lost 의 기대된 부재 제외

> 직전 step-0385 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `coordDesync` 가 `lostZones` 의 *reverse* 부재(권위에 있는데 실에 부재)를 제외(forward=ghost 검사는 유지). failover 는 상태 소실(#63)이라 lost 존은 실 cluster 가 비고 orch 권위엔 entity 가 남아 reverse desync 가 뜬다 — 이는 *비자발 합의된 손실*이지 불일치가 아니다(crash=양쪽 손실). 계층: 코디네이션(#65).
- verify.js 새 모드 `coordfocoh`. 새 박스·run() 미사용 → reg 0.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordfocoh 5/5**: run(3)+failover hostA→hostB → coordDesync 0(lost z1·z3 reverse 제외)·lost 존(z1)에 ghost('ghostX') 주입 시 coordDesync 1(forward 검사 유지·유령 검출).
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 측정 tick 밖 ②결정론: 동일 ③권위: lost 존 reverse 제외는 *비자발 손실 합의*·ghost forward 검출로 단일 소유 가드 유지 ④은닉: cluster API 만 ⑤headless: 실 host. — **한계**: lost 제외는 failover 손실을 *coherence 정의에서 용인*할 뿐 복구는 아님(#63·영속 연동 후속).
