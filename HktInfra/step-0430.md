# step-0430 — #61 업스트림 intent 실 클라 10·capstone: 양방향 실 클라 E2E

> 직전 step-0429 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 검증 전용 grand capstone(코드 박스 무변경·reg 구조적 0) — 실 클라(`UpClient`)들이 intent 발신→게이트웨이→실 존(브리지 데이터 평면)→egress→자기 뷰 수신으로 *양방향 E2E*(desync 0)·생애주기(enter/move/leave)·보존(enters−leaves==present)·발신 회계를 한 시나리오로 단언. **#61 업스트림 실 클라 sub-arc(0421~0430) 닫기** — 다운스트림(0342~0350 실 DownClient)에 이어 업스트림도 실 클라(합성 entityOps 종료).
- 계층: 엣지/월드(#61).
- OFF 플래그: 검증 전용·upClients=null→회귀 0.
- verify.js 새 모드: `upe2ecap`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK.
- 가설 `upe2ecap`: 5/5 — uc0(a1@z1 체류) 수렴 Y·a1 체류·uc1(b1@z2 leave@9) 후 b1 제거·보존 Y(enters2−leaves1==present1)·발신 회계 Y(intentLog==sent·leave 기록).
- spine: ALL OK.

## 척추 5항 + 한계

- ①신성한 tick ②결정론 ③권위 단일 소유 ④은닉(클라는 게이트웨이만) ⑤headless·원격 검증 — 이상 없음.
- 한계(범위): UpClient E2E 는 *in-proc 브리지 데이터 평면*(orch zoneRuntimes·egress)에서 desync 0. 실 host.js child_process 경계 넘는 업스트림(#57 실 cluster)은 후속(다운스트림 0361~0370 의 업스트림 짝). 동치는 권위 귀착(#69 류).
