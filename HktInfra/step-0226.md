# step-0226 — 캐시 recency touch(cacheLruTouch·진짜 LRU)

> 직전 step-0225 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

박스 `src/cache.js` — `cacheLruTouch{on}`: lruTouch ON 이면 get *hit* 시 recency(setAt)를 now 로 갱신 → 자주 읽는 핫 키가 0225 용량 회수에서 살아남는다(진짜 LRU = set+get 둘 다 recency). 0225 는 set-시각만이라 FIFO 에 가까웠다.
- 계층: 6(데이터·캐시) · OFF 플래그: lruTouch=false(cacheLruTouch 미수신 → get 이 recency 미갱신 → 0225 비트 동일=회귀 0) · verify.js 새 모드 `cachetouch`

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 (인프로세스 → 0027 비트 동일·maxDiff 0)
- 결정론 전파·권위/수렴: 4기둥 ALL OK (5시드)
- 가설 `cachetouch`: 5/5 OK — touch ON·cap 2·k1·k2 set·get k1(touches 1)·k3 set → k2 회수·k1 생존(0225 면 k1 회수됐을 것)·capEvicted 1
- spine ALL OK

## 척추 5항 + 한계

①tick: 캐시는 존 tick 밖 순수 반응형. ②결정론: setAt=m.tick·시드 PRNG. ③권위: 캐시 사본(권위는 소스). ④은닉: 명시 메시지(cacheLruTouch)만. ⑤headless: Node·원격 재현. — 이상 없음(캐시 3차 라운드 ✅: 개수 유계 0225 + 진짜 LRU 0226).
