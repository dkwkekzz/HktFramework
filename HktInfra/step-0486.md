# step-0486 — upce2ecap grand capstone 누적 회귀 승격 (#16 라운드 2차 6·cluster 첫)

> 직전 step-0485 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

`engine/verify-kit.js` — 0480 의 bespoke `upce2ecap`(실 UpClient 발신 intent 가 실 host.js child 프로세스 경계 넘어 존→egress 뷰 되먹임 E2E)을 누적 회귀로 승격. **첫 cluster child_process capstone 승급** — `makeVerifyKit` ctx 에 cluster deps(`Cluster`/`makeClusterHostDriver`/`makeClusterCoordinator`/`runMultiViaCoord`/`coordAuthEquiv`) 주입 배선을 추가(engine→src 직접 import 없이 `src/verify.js` 셸이 넘김). upce2ecap 지역 helper(GRID/zoneSpecOf/withCluster/tickUp) 함수 내부 지역화. 계층: 검증 하니스(박스 무수정). OFF 플래그 없음 — reg 0 자명. #16 라운드 2차 6/N.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg 0 — 박스 미수정(net-core+박스 = baseline 비트 동일·cluster deps 는 ctx 주입일 뿐 net-core 무관).
- 가설 `upce2ecap`: 5/5 OK — 실 child_process 경계 넘어 uc0 수렴(seenSig==authSig)·회계(enterPos+Σmove==실 존)·exactly-once(손실 resends6~18·dup0~10 하 수렴)·uc1 leave 제거.
- spine ALL OK — upce2ecap 포함(자기완결 5종 + 첫 cluster child_process capstone).

## 척추 5항 + 한계

①tick: 검증 모드 승급·박스 무변. ②결정론: 수렴 대조·Math.random 0. ③권위: exactly-once·회계가 소유 단일 보존 재단언. ④은닉: seam 번역(게이트웨이 계약)·클라 단일 연결. ⑤headless: 실 child_process host.js spawn·node 단언(원격 경계 검증). — 이상 없음(업스트림 seam 은 실 GW 프로세스 아님·#74 는 트랙 후속·검증 항구화와 무관).
