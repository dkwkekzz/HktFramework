# step-0302 — #9 잔여(실 host.js 물리 분리) 2: host 자기 inbox 수신 + 자기 루프 drain·tick

> 직전 step-0301 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `_zoneDeliver(rt, frame, zoneId)`: zoneHostProc ON 이면 frame 을 그 존의 **host 컨테이너 inbox**(zoneId 태깅)에 enqueue — per-runtime mbox(0292)를 *host 단일 수신 버퍼*로 대체(소켓 1개 = host 프로세스 1개). `_tickRuntimes`: host 단위 루프로 각 host 가 *자기* inbox 를 drain(zoneId 로 소유 존 dispatch·FIFO) 후 *자기* 소유 존만 onTick(실 host.js 프로세스 루프 씨앗·flat 전역 순회 대체).
- `src/orchestrator.js` — `zoneHosts` 컨테이너에 `inbox` + 카운터 `zoneHostFramesRecv`·`zoneHostDrained`. 계층: 코디네이션(⑤). OFF→0301 비트 동일.
- `_bridgeEnter/_bridgeMove/_bridgeLeave` 가 `_zoneDeliver` 에 zoneId 전달. verify.js 새 모드 `zonehostrecv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — zoneHostProc OFF → baseline(0301) 비트 동일(per-runtime mbox 경로 보존).
- `zonehostrecv` 5/5 OK — 혼합 lifecycle 의 전 entity frame 7개(enter5+move1+leave1)가 host inbox 경유: recv 7 == drain 7(잔류 0·무손실) + directFlowCoherent Y·entityConserved Y·total 1·ledger 5/1/2/1 + host 컨테이너==running.
- `node run.js` = ALL OK · `spine` = ALL OK.

## 척추 5항 + 한계

- ① tick: host 자기 루프가 자기 inbox drain 후 자기 존만 onTick — 시뮬 외 작업 0(전달은 tick 경계 배치). ② 결정론: 직렬화 round-trip·FIFO drain(per-zone 순서 보존)·시드 5종 동일. ③ 권위: zoneId 로 정확히 소유 존에 dispatch. ④ 은닉: host inbox 는 orch 내부(클라 무관). ⑤ headless: Node 순수·reg 0.
- 한계: cross-zone onTick 순서가 flat→host 단위로 바뀌나, 런타임 존 onTick send 는 net 싱크가 흡수(클라 직접 전파 0)이고 검증 술어는 entity 수/ledger(순서 무관)뿐 → capstone 수치 동일. inbox 가 *낡은 host* frame 을 거르는 stale 거부는 후속(0303~). 이상 없음.
