# step-0207 — 월드 영속 박스: intent 로그 append 기본

> 직전 step-0206 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 새 박스 `src/worldlog.js`(WorldLog) — 월드(존) 상태의 *intent 로그* event sourcing. 계층: **데이터(계층6·데이터 3분할 ①)**. `worldAppend{intent}` → append-only 로그(seq 단조)·`length`/`at`. 세계 상태의 *유일 쓰기 경로*(intent·SPINE §4 경로1)를 durable 로그로 — 월드 상태는 DB 행이 아니라 로그로 산다(서비스 PersistStore·캐시와 직교). 존 tick 밖.
- 배선: `net-core.js` `__p('worldlog')` · `topo-actors.js` · `topo-build.js`(`worldLog` 플래그) · `topo-run.js`(handle+`worldOps` seam+return).
- OFF 플래그 **`worldLog`**(OFF→박스 0·worldOps 주입 0 = 0206 비트 동일=회귀 0) · verify.js 새 모드 **`worldappend`**.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: `node src/verify.js reg` ALL OK(worldLog 미제공 → 박스 0 → 0206 비트 동일).
- **가설 `worldappend`**: 5/5 시드 OK — intent 4(move·move·pickup·move) append → **로그 길이 4·seq 단조 1~4·at(2)=move:h2**·appends 4.
- **결정론**: 5 시드 동일(로그 순서가 상태 결정). **spine ALL OK**.

## 척추 5항 + 한계

- ① 신성한 tick ✅(worldAppend onMsg 반응·존 tick 밖·동기 디스크 I/O 0) ② 결정론 ✅(`Math.random` 0·로그 순서=intent 순서) ③ 권위 단일 소유 ✅(intent 가 세계의 유일 쓰기 경로·로그가 그 진실) ④ 은닉 ✅(worldOps zone 발신) ⑤ headless ✅(Node·UE 0).
- 한계: append 만(로그 적층). replay 재구성·주기 스냅샷·복구는 후속(0208 replay·이후 2차). 멀티프로세스 미배선(#9).
