# step-0241 — 배치 SSOT 실배선(#51) 1: 존 런타임 레지스트리(running·executed SSOT)

> 직전 step-0240 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orchestrator.js` — 존 런타임 레지스트리 `running`(zoneId→실 가동 host·executed SSOT) + `_start`(존 런타임 가동·instance.js `_spawn` 동형) + 질의 `runningHostOf`/`runningOn`/`runningCount`. `placeZone` 핸들러가 `placeExecute` ON 이면 paper `placement`(결정) 갱신에 더해 `_start`(집행)를 호출 — advisory paper map → executed lifecycle 의 첫 조각(#51). 계층:코디네이션.
- `src/topo-build.js` — orch 생성 opts 에 `placeExecute` 배선(+2줄).
- OFF 플래그 `placeExecute`(기본 false·OFF→placeZone 은 paper 만 갱신=0240 비트 동일=회귀 0).
- verify.js 새 모드 `placeexec`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: zones1/zones2+rec+fo/svc+bus/chatp/persist 5조 비트 동일(0x3835712d·0x59d70205…=baseline). placeExecute OFF 경로 무변경.
- **가설 `placeexec`**: 5시드 모두 placed 2·running 2·z1 run hostA·hostA run 1·starts 2 — placement(결정)==running(집행)·drift 0·재배치 멱등.
- **결정론/권위/수렴**: `run.js all` ALL OK(e2e·sacred·repro 멀티프로세스 비트 동일 유지).
- **spine**: ALL OK(누적 회귀 사슬 — 0231~0240 승급 10모드 포함 전부 통과).

## 척추 5항 + 한계

①신성한 tick: running 은 코디네이션 제어 평면 상태(존 시뮬 tick 밖) ②결정론: 시드 PRNG·Math.random 0 ③권위 단일 소유: running 은 zoneId 단일 키 = 한 존 정확히 한 host(executed 판) ④은닉: 배치/런타임은 orch 내부·클라 무관 ⑤headless: node 검증. — 이상 없음(executed SSOT 는 아직 placeZone start 만·migrate/rebalance/drain 실배선은 0242~ 후속).
