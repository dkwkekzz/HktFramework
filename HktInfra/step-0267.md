# step-0267 — 정리(#49 인접·선제): orchestrator 제어 평면 핸들러 믹스인 분리

> 직전 step-0266 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 박스 `src/orchestrator.js`(27.5KB·30KB 근접·성장 박스 선제 정리·0251 orch-placement 분할의 짝) → `Orchestrator` 의 *제어 평면 핸들러*(`onMsg`·`onTick`)를 새 박스 파일 `src/orch-control.js` 믹스인으로 분리. 계층: 코디네이션(오케스트레이터). **OFF 플래그 없음 — 투명 분할**(정의 위치만 이동·동작 비트 불변).
- 배선: orchestrator.js 가 `require('./orch-control.js')` 1줄 · 클래스 정의 직후 `Object.assign(Orchestrator.prototype, OrchControl)`(OrchPlacement 와 나란히). onMsg 의 placeX 분기는 _start/_migrate(placement 믹스인)로 그대로 해소. 잔류(코어): 생성자·monitor·_track·_presence·_survivorOf. verify.js 새 모드 `orchctlsplit`.
- 결과 크기: orchestrator.js 27.5KB→18.9KB·orch-control.js 9.6KB. (0251 orch-placement 9.1KB 와 함께 오케 3분할: 코어 18.9 + placement 9.1 + control 9.6.)

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- reg: ALL OK(src vs baseline 비트 동일·maxDiff 0 — 투명 분할 증명).
- `orchctlsplit`(가설): 5/5 — failover 토폴로지에 placeZone(z1@hostA·z2@hostB) 주입 → onMsg 가 배치 SSOT 갱신(placementOf z1=hostA·z2=hostB·placedCount 2)·failover onTick 정상. 옮긴 제어 평면 핸들러 무결.
- spine: ALL OK(전 역사 누적 회귀 + orchctlsplit 편입).

## 척추 5항 + 한계

①tick: onMsg/onTick 은 존 tick 밖 배치 제어 평면·무변경 ②결정론: 코드 이동만·시드 동일 ③권위: 배치 단일 소유 보존(placedCount 정합) ④은닉: 내부 구조 분할만·계약 무변경 ⑤headless: Node·무변경. 이상 없음(순수 구조 분할).
