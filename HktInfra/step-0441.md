# step-0441 — #4 실 net.step 배리어 치환 1: 실 Net-형 intent 스트림 + Lamport 스탬프

> 직전 step-0440 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- 신규 박스 `src/async-net.js` — async-core(0431~0440·추상 이벤트)가 증명한 논리 클럭·인과 정렬 substrate 를 **실 engine Net 메시지 형태**(`{from:'client'+s, to:'zone1', payload:{type:'intent',avatar,dx,dy}}`)에 잇는 arc 의 첫 조각. `worldIntentStream(seed,{clients,avatars,msgs})` — 다중 client(발신 site)가 각자 Lamport 클럭으로 실 Net-형 intent 발신 → 이벤트 로그 + program-order 간선. `streamSig` 재현 대조. 원시(makeLamportClock 등)는 async-core 재사용(복제 0).
- 계층: 코디네이션(#4). async-core substrate → 실 전송 메시지 브리지 시작.
- OFF 플래그: 없음 — async-net 은 run() 미호출 → **reg 구조적 0**(net-core 진입점에 require 1줄만 추가). verify.js 새 모드 `netintent`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `netintent` 5/5 OK — 4 client·40 intent: per-client lc 단조 Y·clock condition(program 간선 a→b⇒lc(a)<lc(b)) 위반 **0**·재현(같은 seed 동일 streamSig) Y.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(시드 PRNG·Math.random 0) ③권위 단일 ✅ ④은닉 ✅(실 Net 계약 from/to/payload) ⑤headless ✅(순수 JS·검증 전용). 한계(정직): 이 조각은 *스트림 생성·스탬프*만 — 재정렬/holdback/실 actor 디스패치/sim fold 는 후속(0442~). async-net 은 run() 밖 substrate → 통합 시 회귀 게이트 재확인 필요.
