# step-0408 — #62 runMulti 합류 7: runScenario 통합 시나리오 루프

> 직전 step-0407 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/cluster-coord.js` — `runScenario(ticks, scenario)`: `run(ticks, onTick)`(0393) 위에 스크립트된 열화 시나리오 구동을 단일 진입점으로. scenario 의 각 이벤트(`migrate`/`restart`/`reprovision`/`kill`/`fence` @at·`sweepSilence`)를 지정 tick 의 onTick 훅으로 번역. runMulti 의 핵심인 *스크립트 시나리오 주입*(`cluster-run.js:176` kill·`:202` rep)을 verify ad-hoc onTick 대신 *선언적 시나리오*로(runMulti 호환). 계층: 코디네이션(#62).
- OFF 동치: 빈 시나리오면 run(ticks)=0407 동치. verify.js 새 모드 `coordscenario`. 새 박스·run() 미사용 → reg 0. cluster-coord.js 22.9KB(<30KB).

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- **reg 0**: 5/5 baseline 비트 동일.
- **coordscenario 5/5**: runScenario(6, {migrate z1 A→B @2, reprovision z2@hostB_s @3}) → unifiedCoherent Y·maxDesync 0·migrations 1·reprovisions 1.
- **spine ALL OK**.

## 척추 5항 + 한계

①tick: 시나리오 훅은 제어 평면 ②결정론: 시드 5종 동일 ③권위: 루프 중 migrate+reprovision 후도 단일 where 권위(unifiedCoherent Y) ④은닉: cluster/driver API 만 ⑤headless: 실 host.js child_process E2E. — **한계(후속)**: restart 이벤트는 co-located 존이 없는 host 에만 안전(restart 가 host 전체 kill·co-located 존 stranding). 따뜻한 standby *승격*(failover→promote)은 0409·capstone 0410.
