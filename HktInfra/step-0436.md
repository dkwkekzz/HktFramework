# step-0436 — #4 진짜 비동기 6: async 수렴 desync 0

> 직전 step-0435 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/async-core.js` — `applyDigest(ordered)`: 배달열을 순차 fold(state_n = h(state_{n-1} | key(e)), key=(site,lc,kind)=내용)해 상태 다이제스트를 낸다 — 순서 민감(틀린 순서면 갈림)·내용 함수(같은 전순서면 같음). 순서(0433)·재정렬(0434)·인과(0435)를 *상태 수렴*으로 마무리.
- 계층: 코디네이션(#4). 두 site 가 *서로 다른 물리 도착 순열*을 각자 holdback 으로 재구성·적용 → 같은 다이제스트 = **desync 0**(중앙 lockstep 없이 수렴). #4 의 핵심 명제(같은 이벤트 multiset → 같은 수렴).
- OFF 플래그: 없음 — run() 미호출 → **reg 구조적 0**. verify.js 새 모드 `asyncconv`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` → ALL OK.
- `asyncconv` 5/5 OK — 4 site·42 이벤트, siteA·siteB 도착 순열 실제로 상이(distinct Y)인데 digestA == digestB == 정전 전순서 다이제스트 → **desync 0**.
- `node run.js` 4기둥 ALL OK · `spine` ALL OK.

## 척추 5항 + 한계

①신성한 tick ✅ ②결정론 ✅(다이제스트=내용 함수·도착 무관) ③권위 ✅ ④은닉 ✅ ⑤headless ✅. 한계: 수렴은 *두 site 가 같은 이벤트 multiset 을 다 받음* 가정(손실 0). 손실 하 gap-resync 수렴은 0438. site 별 진행 속도(배리어-free)가 결과에 무관함은 0437.
