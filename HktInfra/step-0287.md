# step-0287 — #56 브리지 존 데이터 평면 7: entity 단일 소유 불변

> 직전 step-0286 · 권위 [STATE.md](STATE.md) · 척추 [SPINE.md](SPINE.md) · 코드 `src/`(제자리) · reg=src vs baseline 비트 대조

## 한 일 (delta)

- `src/orch-zonebridge.js` — `entityOwnerZone(avatar)`(첫 매칭 zoneId)·`entityOwnerCount(avatar)`(거주 존 수)·`entitiesSingleOwner()`(어떤 avatar 도 두 존 동시 거주 없음). entity 차원의 권위 단일 소유(존 핸들 host 단일 소유 0276 과 동형·*중복* 측). 계층: 코디네이션↔월드.
- OFF 플래그 `zoneEntityFlow`(OFF→0286 비트 동일=회귀 0·읽기 전용 질의). verify.js 새 모드 `zoneowner`.

## 검증 (수치 = `node run.js` / `node run.js spine` 출력)

- `reg` ALL OK — `zoneEntityFlow` OFF→baseline 비트 동일(maxDiff 0).
- 가설 `zoneowner` 5/5: a1·a2→z1·a3→z2·a4→z3 enter + z1→hostC migrate(같은 핸들·존 집합 불변) → `single=Y`·`a1zone=z1`·`a3cnt=1`·`a4cnt=1`·`total=4`. migrate 가 존 집합을 안 바꾸므로 단일 소유 보존.
- `node run.js spine` ALL OK.

## 척추 5항 + 한계

- ①신성한 tick: 질의는 읽기·존 tick 밖. ②결정론: 결정론 적재·`Math.random` 0. ③권위 단일 소유: 이 step 이 데이터 평면 단일 소유를 *수치로 단언*(entity 차원). ④은닉: 게이트웨이는 메시지로만. ⑤headless: 실 EntityZone 핸들·UE 0.
- 한계: 정상 op(enter 한 존·migrate 같은 핸들)에선 단일 소유가 by-construction 참 — 진짜 분기(중복 적재)는 #9 실 프로세스 경합에서 load-bearing 화. entity 정합(running 존에만 거주)·graceful census·capstone 은 0288~ 후속.
