# RENDER-STATE — 렌더러 트랙 현재 위치 (살아있는 대시보드)

> 렌더러 트랙의 SSOT — *지금 어디까지·다음 렌즈는 무엇인가*. 척추(원칙)=[RENDER.md](RENDER.md), 규약=[CLAUDE.md](CLAUDE.md) "두 트랙".
> 시뮬 트랙 [STATE.md](STATE.md) 와 **직교** — step 번호 없음, 회귀·verify 4기둥 무관. 렌더러는 `STATE.md` 를 만지지 않는다.
> **고정 크기 대시보드**: §1~5 는 렌즈마다 덮어쓴다(rewrite). §6 INDEX 만 1줄/렌즈 append.

---

## 1. NOW — 켜진 렌즈 (`engine/hws-3d.js` — `progV` voxel 세계 · `progT` 레거시 하이트필드)

**L-V1 전환 완료**: 시뮬이 voxel(W×H×D)이라 우측 뷰가 **진짜 3D voxel 세계**로 바뀌었다 — 형태(높이)는 더 이상 렌더러의 발명이 아니라 **sim 의 사실**(R 점유 = 기하). 좌측은 레거시 2.5D 하이트필드(z=0 바닥 슬라이스)로 대비 보존.

**우측 = voxel 세계 (`progV` — L-V1)**:
- ✅ **R 점유 인스턴스드 큐브**: R(또는 의미있는 E)이 있는 칸만 단위 큐브 1개로 — 빈칸=void(생략). 위치 = sim `(x,y,z)`(sim-z=월드 위, 중력 방향 z=0 바닥). CPU 가 매 프레임 점유 칸을 스캔해 인스턴스(셀+필드) 빌드 → `drawElementsInstanced`. *형태 발명 0*(VOXEL V-A).
- ✅ **E 밝기(VOXEL V-B)**: 고도 표현 폐기. 흐르는 E(고활성 A)=발광 큐브(클수록 밝게)·고인 E(저활성)=파란 물 voxel(깊이로 청록↔남빛).
- ✅ **승계 렌즈(차원만 큐브로)**: 상 3분기(고체/액체/빛·lerp 0)·조성 `G` 절차적 해시 색(나무/결정 ↔ 돌)·밀도(R)·생명 발광 점·별 FSM 이산 재질(kindling/burning/ash) — 별은 **부력 z**(step-0035)로 천장까지 떠오른 제 높이에 그려진다.
- ✅ **L-V2 물 볼륨**: 물 큐브를 *반투명*으로(불투명 먼저 → 물 블렌드, 뒤 큐브 비침). 깊이(고인 E)로 Beer-Lambert 흡광(`transmit=exp(-depth·absorb)`·얕음 청록·깊음 남)·표면 프레넬(`uEye` 시선 비스듬할수록 반사↑·하늘색)·깊이 알파(얕음 투명·깊음 짙음). 쌓인 물 voxel 이 블렌드로 누적돼 *볼륨감*. 불투명 voxel 은 `vWet=0` → 불변.
- ✅ **L-V3 `∇R` 거칠기**: 고체 큐브 면 노멀을 R 6-이웃 라플라시안(`|∇²R|` 고주파)으로 변조 — 들쭉날쭉한 R=거친 암석·매끈한 R=매끈. CPU 가 인스턴스에 `rough` 실어 보냄(8번째 float)·FS 가 서브셀 해시(`hash33(floor(vWorld·6))`)로 facet 흔듦. 높이 불변(셰이딩 노멀만·도함수 읽기)·물/빛 큐브 `rough=0` 불변.

**좌측 = 에너지 변위 (`progT` — 레거시 2.5D)**: z=0 바닥 슬라이스 하이트필드(h=E+R) + 표준 오버레이(링·빔·경계·호버). "옛 착시 ↔ 새 정직한 voxel" 교육 대비.

**RETIRED (`progW` 2.5D 세계해석 — 더 이상 컴파일 안 됨)**: 물 막 흡광·프레넬·`∇R` 거칠기·`∇E` flowmap·`G` 해시 — 수식은 코드에 *참조 보존*, voxel 재구현은 L-V2+.

---

## 2. NEXT — 다음 렌즈 (한 커밋 = 한 조각)

**L-V1·L-V2·L-V3 닫힘** → 다음 한 조각 후보(택1):
- **`∇E` 바람** — 흐르는 E 큐브 발광에 `∇E` 하류 방향 이류 띠(세기에 더해 방향). CPU 가 셀별 ∇E 방향을 인스턴스에 실어 보내고 FS 가 `uTime` 이류(코어 불변·도함수 읽기). 2.5D `progW` 에 있던 렌즈를 큐브로(RETIRED 수식 참조).
- **`∇²E` 안개·응결** — 빈 칸 미량 E 를 대기 산란·볼류메트릭(raymarch — 더 큰 조각).

직교 백로그: **greedy meshing**(노출면만 병합 — 인스턴스 수 절감) · **voxel 픽킹**(레이 vs 점유 큐브 DCA — 현재 클릭은 레거시 하이트필드 기준) · **3D 오버레이**(링·빔의 voxel 등가) · **물 정렬**(현재 물-물 블렌드는 인스턴스 순서 — 겹친 물 약한 순서 의존, 카메라 거리 정렬은 백로그).

---

## 3. OPEN GAPS — 설계(RENDER.md) vs 코드 격차

| 마커 | 격차 | 설계 근거 |
|---|---|---|
| 🟡 | **물-물 정렬** — 반투명 물 큐브가 인스턴스 순서로 블렌드(뒤→앞 카메라 정렬 아님). 겹친 물 약한 순서 의존 — 단일 층·얕은 물은 무관, 깊은 물기둥서만 미세 | — |
| 🟡 | **voxel 픽킹** — 클릭이 아직 레거시 하이트필드 레이마칭 기준(voxel 점유 레이 DCA 아님). voxel 뷰 클릭은 x,y 컬럼만 정확 | DESIGN-3D 픽킹 |
| 🟡 | **3D 오버레이** — 링·빔·경계·호버가 하이트필드 구성물이라 voxel 뷰선 생략. 3D 마커는 후속 렌즈 | — |
| ⬜ | **승계 보류 렌즈** — `∇E` 바람·`∇²E` 안개는 2.5D `progW` 에 있었으나 voxel 재구현 대기(RETIRED 수식 보존) | RENDER §5 |
| ✅ | ~~L-V3 ∇R 거칠기~~ → 고체 면 노멀을 R 라플라시안(`|∇²R|`·CPU→인스턴스 rough)으로 변조(들쭉날쭉=거친 암석)·FS 서브셀 해시 facet·높이 불변·물/빛 불변 | RENDER §5 |
| ✅ | ~~L-V2 물 볼륨~~ → 물 큐브 반투명(불투명 먼저→물 블렌드·뒤 비침)·Beer-Lambert 흡광·프레넬(`uEye`)·깊이 알파; 쌓임 누적=볼륨감 | VOXEL.md L-V2 |
| ✅ | ~~L-V1 voxel 렌즈~~ → R 점유 인스턴스드 큐브(높이=sim-z·발명 0) + E 밝기(흐름=발광·고임=물); 상/조성(G 해시)/밀도/별 FSM(부력 z)/생명 점 차원 일반화 | VOXEL.md L-V1 |

---

## 4. DURABLE — 렌더러 트랙 불변

- **형태 author 0** — 필드가 만든 것만 비춘다. *어느 양이 z·색·빛이 되는가*는 읽기(허용), *양의 분포 재성형*은 author(금지). 도함수·필터(∇R·∇E)는 읽기. **voxel(L-V1~) 에선 더 강해짐**: 위치=sim `(x,y,z)` 그대로 — 렌더러가 높이를 *발명조차 안 한다*(R 점유 = 기하, VOXEL V-A).
- **z = 물질(R) only** — 에너지(E)는 흐르든 고이든 z 를 안 만든다(응축상 R 만 공간 점유). 2.5D 에선 `hOf(R)`, **voxel 에선 sim-z 가 곧 월드 위**(중력 방향 z=0 바닥). E 는 색 밝기로만(VOXEL V-B). RENDER §2·§5.
- **voxel 렌즈(L-V1~)**: `progV` 인스턴스드 단위 큐브 — CPU 가 매 프레임 *점유 칸*(R>0.08 또는 의미있는 E)을 스캔해 인스턴스(셀+필드+거칠기 8 float) 빌드 → `drawElementsInstanced`. 분류 문턱은 CPU 방출 == VS 색칠(불일치 0). **두 패스(L-V2)**: 불투명(고체·빛, `vaoV`) 깊이 쓰기 on 먼저 → 물(반투명, `vaoVW`) 깊이 쓰기 off·블렌드 on 나중(뒤 큐브 비침). `vWet` varying 이 프레넬·알파를 게이트(불투명은 불변). **이웃 도함수(L-V3~)**: ∇R 같은 이웃 필요 양은 *CPU 빌드*가 6-이웃에서 계산해 인스턴스에 실어 보낸다(셰이더는 셀별 인스턴스라 이웃을 못 봄 — 텍스처 샘플 대신 사전계산). 좌측 레거시 하이트필드(`progT`, z=0 슬라이스)는 교육 대비로 보존. 점/별은 `uVoxel` 로 높이 분기(sim-z ↔ 하이트필드). 성능: 점유 스캔 O(W·H·D)·greedy meshing 백로그.
- **소유 파일**: `engine/hws-3d.js`(+ 프레젠테이션 한정 `hws-ui.css`/`hws-ui.js`) · `RENDER.md` · `RENDER-STATE.md`. **불가침**(시뮬 소유): `hws-laws.js`·`hws-kernel.js`·`hws-sim.js`·`golden-sim.json`·`step-NNNN/*`·`STATE.md`·`SPINE.md`.
- **검증 3종**: ① `node engine/validate/verify-sim-engine.js` 골든 해시 불변(=시뮬 안 건드린 알리바이) ② `node engine/validate/smoke-dom-3d.js` 3D 스모크 ③ **눈 검증**(화면이 권위) + 척추 한 항(형태 author 0).
- **필드 있으면 읽고 없으면 no-op** — 시뮬 step 진행과 독립. 필드에 형태가 실리면 렌즈가 코드 0으로 받는다.

---

## 5. ROADMAP — 렌즈 순서 (위에서부터 하나씩)

| 순서 | 렌즈 | 상태 |
|---|---|---|
| 1 | **z = 물질(R) only** — 고인 E도 z에서 뺀다 | ✅ DONE |
| 2 | **물 = R 위 반투명 막** — E→깊이·투과·색, 바닥 R 비침 | ✅ DONE |
| 3 | **고체 거칠기 = `∇R` 노멀 디테일** | ✅ DONE |
| 4 | **`G` → 절차적 색**(해시 — 혈통 무한 분화, 팔레트 캡 제거) | ✅ DONE |
| 5 | **`∇E` → 파생 바람** flowmap | ✅ DONE |
| 6 | **FSM → 이산 재질**(kindling/burning/ash) | ✅ DONE |
| — | **2.5D 6렌즈 완주** → voxel 전환(VOXEL.md) | — |
| **L-V1** | **voxel 세계** — R 점유 인스턴스드 큐브 + E 밝기, 기존 렌즈 차원 일반화 | ✅ DONE |
| **L-V2** | **물 볼륨** — 반투명 큐브·깊이 흡광·프레넬·바닥 투과(뒤 비침) | ✅ DONE |
| **L-V3** | **`∇R` 거칠기** — 고체 면 노멀 R 라플라시안 변조(거친 암석) | ✅ DONE |
| L-V+ | `∇E` 바람·`∇²E` 안개 voxel 재구현 · greedy meshing · voxel 픽킹 · 물 정렬 | ⬜ NEXT/백로그 |

---

## 6. INDEX — 1줄/렌즈 (append-only)

| 렌즈 | 더한 조각 | 검증 |
|---|---|---|
| — | (RENDER.md 척추 정립 + RENDER-STATE 신설) | 첫 렌즈 대기 |
| z = 물질(R) only | `matH = hOf(R)` — 저활성E(물) z 기여 제거, 법선도 R 만. 물은 z 서 빠지되 재질(파랑·투과)로만 읽음 | eq PASS · golden PASS · 3D 스모크 PASS |
| 물 = R 위 반투명 막 | 액체 셀 = 바닥 물질색 `transmit=exp(-depth·absorb)` 흡광 블렌드(얕음=바닥 비침·청록/깊음=짙은 남) + 프레넬(`uEye`)·시선 글린트, 고체 `vWet=0` 불변 | golden PASS · 3D 스모크 PASS · 눈 검증(브라우저) |
| 고체 거칠기 = ∇R 노멀 | R 라플라시안 `|∇²R|` 진폭 + 셀 해시 방향으로 고체 노멀 미세 변조(들쭉날쭉 R=거친 암석). 높이 불변·셰이딩만, 액체/공허 rough=0 | golden PASS · 3D 스모크 PASS · 눈 검증(브라우저) |
| G → 절차적 색 | geneCol/storeCol/geneColP 고정 4색 → 황금비 색상환 해시(`hue=fract(g·φ⁻¹)` HSV→RGB) 3곳 일관, 혈통 무한 분화·무유전 호박색 불변 | golden PASS · 3D 스모크 PASS · 눈 검증(브라우저) |
| ∇E → 파생 바람 | 중앙차분 ∇E 하류 방향 flowmap 위상 + `uTime` 이류 띠로 발광 변조(세기에 더해 방향). 무흐름/약한 ∇E 는 flowVis=1, 코어 불변 | golden PASS · 3D 스모크 PASS · 눈 검증(브라우저) |
| FSM → 이산 재질 | 별 `sim.stars[].state`(0/1/2) → 이산 재질 점(VS_STAR/FS_STAR): kindling 어두운 응결핵·burning 백열 블룸·ash 회색 잔불, 색·크기·높이 문턱 분기(lerp 0). kIgnite=0 no-op | golden PASS · 3D 스모크 PASS · 눈 검증(브라우저) |
| **L-V1 voxel 세계** | `progV` 인스턴스드 단위 큐브 — R 점유 칸만(빈칸 void 생략), 위치=sim `(x,y,z)`(sim-z=월드 위·발명 0), E 밝기(흐름=발광·고임=파란 물). 상/조성(G 해시)/밀도/별 FSM(부력 z)/생명 점 차원 일반화. 좌측 레거시 하이트필드 보존. progW 2.5D 세계해석 RETIRED | golden 불변(IDENTICAL·sun@ 포함 전 시나리오 OK) · 3D 스모크 PASS · 눈 검증(브라우저 대기 — 헤드리스) |
| **L-V2 물 볼륨** | 물 큐브 반투명 분리 패스(불투명 깊이 먼저 → 물 블렌드·깊이쓰기 off, 뒤 큐브 비침). Beer-Lambert 흡광(`transmit=exp(-depth·absorb)`)·프레넬(`uEye` 시선·하늘 반사)·깊이 알파(얕음 투명·깊음 짙음)·쌓임 누적=볼륨감. `vWet=0` 불투명 불변 | golden 불변(IDENTICAL) · 3D 스모크 PASS · 눈 검증(브라우저 대기 — 헤드리스) |
| **L-V3 ∇R 거칠기** | 고체 큐브 면 노멀을 R 6-이웃 라플라시안(`|∇²R|`·CPU 사전계산→인스턴스 8번째 float)으로 변조 — FS 가 서브셀 해시(`hash33(floor(vWorld·6))`)로 facet 흔듦(들쭉날쭉 R=거친 암석·매끈=매끈). 높이 불변(셰이딩만)·물/빛 rough=0 불변 | golden 불변(IDENTICAL) · 3D 스모크 PASS · 눈 검증(브라우저 대기 — 헤드리스) |
