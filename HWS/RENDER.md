# RENDER — 필드를 세계로 번역하는 설계

> 스냅샷(E·R·A·G)을 *읽기만* 해 세계로 그린다. 형태를 author 하지 않는다.
> 큰 목표 [CLAUDE.md](CLAUDE.md) · 존재론 척추 [SPINE.md](SPINE.md) · 렌더 엔진 배관 [engine/DESIGN-3D.md](engine/DESIGN-3D.md).
>
> ⚠️ **3D 전환 진행 중 ([VOXEL.md](VOXEL.md))**: 시뮬 voxel 전환(V1~V5+)·**렌더 L-V1(voxel 세계) 닫힘**. 이제 우측 뷰는 *R 점유 = 기하*인 진짜 voxel 세계다 — 공간 점유는 시뮬의 사실이고(높이 발명 0), 렌더러에 남는 권한은 *색·재질·빛*뿐(E 크기 = 밝기, 클수록 밝게). **따라서 아래 §2 제1원칙("sim은 2D — z는 렌더러가 발명")·§5 물 렌즈("R 위 막")는 *레거시 2.5D 좌측 뷰에만* 적용된다** — voxel 우측 뷰의 척추는 [VOXEL.md](VOXEL.md) §2·§4, 현황은 [RENDER-STATE.md](RENDER-STATE.md). §2/§5 의 전면 개정은 별도 문서 패스(L-V 사다리 완주 시).

---

## 1. 목적 — author 없이 스스로 굴러가는 세계를 그린다

세계엔 *타입*이 없다. 셰이더도 하나뿐이다. 돌·물·나무·빛은 별도 자료형이나 별도 렌더 규칙이 아니라 **같은 필드(E·R·A·G)를 다른 렌즈로 읽은 결과**다. 시뮬이 새 개체·새 물질을 내면 렌더러는 **코드 0**으로 받는다 — 정체를 좌표에서 *연역*하지, 새 타입마다 그리는 법을 *지정*하지 않는다.

렌더러가 정하는 건 단 하나: **어느 추상량이 무엇으로 보이는가**(높이·색·빛). 그 양이 *어디 있는가*(분포)는 전부 시뮬의 몫이다.

---

## 2. 제1원칙 — 에너지와 물질은 다른 층, z축은 물질만의 것

이 sim은 2D 필드다. **공간의 z축(물리적 높이)은 렌더러가 발명한다** — 그러니 "무엇이 z가 되는가"를 정직하게 골라야 한다.

| | 에너지 (E의 흐름) | 물질 (R, 응축상) |
|---|---|---|
| 본성 | 장(field) — 퍼짐 | 질량 — 공간 점유 |
| 형태·높이 | **없다** | **있다** |
| 보이는 법 | 빛·열·흐름(발광·색) | 지형·표면·덩어리(z·법선) |

**확정: 물리적 높이(z) = 물질(R)에서만 온다.** 에너지(E)는 — 흐르든 고이든 — z를 만들지 않는다. 응축상 R만 공간을 점유하므로 R이 쌓인 만큼만 솟는다.

> **고인 E도 에너지다.** "물"은 별도 물질 필드가 아니라 *E가 고인 자리*다. 그러니 물도 z를 들어올리지 않는다 — R 지형 위에 *얹힌다*(§5).

---

## 3. 입력 계약 — `FHktWorldView` 채널

64×64. 매 tick 통째 업로드해도 무료.

| 채널 | 양 | 용도 |
|---|---|---|
| R | `E` | 흐름량(고이면 물, 흐르면 빛) |
| G | `R` | 저장체 = **물질**(지형 높이의 유일한 원천) |
| B | `A` | 활성도 = 에너지/물질 분류 다이얼 |
| A | `G` 태그 · FSM | 조성(종·혈통) 색 · 이산 상전이 |
| 인스턴스 | 에이전트 `m`(+G) | 발광 생명 점군 |
| GPU 파생 | `∇R` · `∇E` · `∇²E` | 거칠기 · 바람 · 안개(코어 불변) |
| ⏳ Q | `q`(에너지 질 — 자유에너지/엔트로피, 시뮬 선행) | **흑체 색온도**(§8) — 시뮬이 `q` 를 실으면 읽고, 미발현이면 no-op |

---

## 4. 분류 다이얼 — 활성도 `A`로 에너지/물질을 가른다 (연속, 타입 아님)

`A`(= E throughput `|dE/dt|` EMA)는 "흐름이 사나"를 잰다 = *세계가 살아있는 정도*. 이 한 축이 모든 갈림을 만든다 — 분기는 authored 타입이 아니라 *연속 좌표 위의 문턱*이다.

| 양 | `A` | 층 | 세계 원형 | z 기여 |
|---|---|---|---|---|
| 흐르는 `E` | 큼 | 에너지 | 빛·불·흐름·열 | **0** (→발광) |
| 고인 `E` | ≈0 | (R 위 막) | 물·웅덩이 | **0** (R 위에 얹힘) |
| `R` | — | 물질 | 바위·나무·퇴적 | **= R** (솟음) |

조성·정체는 *양*이 아니라 *속성*의 함수다:
- **상(phase)**: R 우세 = 고체 / 고인 E = 액체 / 빈칸 = 공허. 결정화·증발 문턱에서 **이산 분기**(lerp 금지).
- **조성(G)**: 종·혈통 색. 돌 vs 나무를 가르는 건 메시가 아니라 *유전 G의 유무*. **G→색은 절차적**(해시)이어야 한다 — 혈통이 무한히 갈려도 author 0으로 색이 분화한다(고정 팔레트 금지).

---

## 5. 지형 · 물 · 빛 — 세 렌즈

모두 같은 필드를 *다르게 읽는다*. 새 매질이 필요하면 새 에셋이 아니라 *같은 장을 읽는 새 도함수*를 더한다.

### 지형 (고체 R)
- **높이** `z = hOf(R)`. R 누적이 곧 고도 — 퇴적·결정화가 땅을 빚는다. 법선은 이웃 `R` 기울기.
- **거칠기**: R의 고주파 성분(`∇R`/라플라시안 거칠기)을 노멀 디테일로 변조. 매끈한 R 변화 = 매끈, 들쭉날쭉한 R = 거친 암석 표면. *광택 없음(무광) + 어두운 색*은 거침이 아니라 재질 단서다 — 진짜 거침은 이 노멀 디테일에서 나온다.
- **조성**: 유전 `G` 있으면 나무·결정(색조 분기·약한 내부 발광), 없으면 돌(무광·어두움).

### 물 (고인 E)
물은 **z에 솟지 않는다.** 자기 셀의 물질 높이(R) 위에 얹힌 반투명 막이고, E는 *깊이·투과·색*으로만 나타난다.

- **표면 높이** = 그 셀의 R(바닥 물질). E가 표면을 추가로 들어올리지 않는다 → 물은 아래 R 지형을 따라 드리운다(평탄화가 아니라 *물질 바닥을 따른 결과*).
- **깊이(E 양)** → 흡광 색: 얕으면 청록, 깊으면 짙은 남.
- **투과**: 바닥 R이 비친다(고체는 불투명).
- **표면**: 매끈 → 프레넬 반사·광택.

> **물이 다를 수밖에 없는 이유**: 물질이 아니라 *물질의 골을 채운 다른 층*이기 때문이다. 고체 = z를 *이루는* 불투명·무광, 액체 = z 위에 *얹힌* 반투명·광택.

### 빛 (흐르는 E)
- 흐르는 에너지(고활성 E·A)는 z에서 빠져 **발광**으로 간다. 별 연소·확산 전선이 *솟지 않고 빛난다*.
- FSM 상태로 **이산 분기**(lerp 금지): `kindling`(어두운 응결핵) → `burning`(고강도 emissive + 포인트라이트 + 상승 입자) → `ash`(식어 가라앉음).
- 전역 발광 총합은 `A` 보존으로 일정 — "빛은 사라지지 않고 돌 뿐"(정직한 세계).
- **색온도(흑체)는 *세기*가 아니라 *질*에서 온다 → §8**(시뮬 Q 선행). 지금은 밝기=`A`(flux 양)만 — *얼마나* 밝나는 읽되 *어떤 색의 열*인가(붉은 불씨↔백열↔청색)는 못 읽는다(필드에 질이 없어서). 이 색온도 렌즈는 시뮬이 `q` 를 실어야 author 0 으로 켜진다.

### 파생 (코어 불변)
- **바람·해류**: `∇E` → flowmap UV / 벡터필드.
- **안개·응결**: `∇²E` → 볼류메트릭 밀도.

---

## 6. author 금지선 — 무엇이 읽기이고 무엇이 author인가

| 허용 (정직한 읽기) | 금지 (form author) |
|---|---|
| *어느 양*이 z·색·빛이 되는가 선택 | 양의 *분포*를 재성형 |
| 도함수·필터(∇R 거칠기, ∇E 바람) | 필드에 없는 실루엣 곡률을 손으로 박기 |
| z = R, 물=R 위 막 (물질 바닥을 따름) | 물을 "평평한 시트"로 평탄화(E 분포 변형) |
| 색온도 = *시뮬이 실은* `q`(에너지 질) 를 읽음 | E 농도로 색온도를 *지어냄* — 질 없는 필드에 질을 발명 = **연성 author**(§8) |

**실루엣(형태)은 시뮬의 일이다.** 막·곡률·가지·줄무늬는 flux가 필드에 빚어야 하고, 그러면 같은 렌즈가 **코드 0**으로 받아 그린다. 그게 렌즈가 옳다는 증거다. 그때까지 렌더러는 *높이(=물질)·색·재질·빛*만 정직하게 비춘다.

**질(質)도 형태와 같다 — 시뮬의 일이다.** 에너지의 *색온도*(흑체)는 흐름의 하류다: 시뮬이 에너지에 질(`q`)을 실어야 렌더러가 흑체 곡선 하나로 만물(불씨·흐름·별)을 코드 0 으로 칠한다. 그때까지 빛은 *세기*(`A`)로만 읽고 색온도는 비워 둔다 — 지어내지 않는다(§8).

---

## 7. UE 모듈 매핑

스냅샷 소비 = `HktPresentation`(`FHktWorldView`) · 필드 볼륨 = `HktVoxelCore`/`HktVoxelTerrain` · 발광·바람 입자 = `HktVFX`/`HktVoxelVFX` · 개체 외형 = `HktVoxelSkin`.

UE는 절대 필드를 만지지 않는다 — 화면은 전부 스냅샷의 함수(서버 권위·결정론 불변).

---

## 8. 에너지의 질 — 다음 큰 렌즈 (시뮬 Q 선행)

> 필드 렌즈 사다리(L-V1~L-V5)는 닫혔다 — 렌더러는 시뮬이 *지금* 내보내는 양을 다 읽었다. 더 풍부한 *에너지* 시각화는 이제 시뮬이 **새 정보**를 실을 때 켜진다. 그 첫째가 **에너지의 질(質)**이다. 존재론 근거 = [SPINE.md](SPINE.md) 여섯째 축(자유에너지·둘째 법칙).

**왜 색온도인가.** 지금 빛은 *세기*(`A`=flux 양)만 읽는다 — *얼마나* 밝나는 알지만 *어떤 열*인가(붉은 불씨 ↔ 주황 불꽃 ↔ 백열 ↔ 청색 별)는 못 읽는다. 그건 **에너지의 질**(단위질량당 농축도 = 온도/자유에너지)이고, 흑체 복사색이 그 보편적 시각 서명이다. 질은 *양*과 독립이다(같은 양의 에너지라도 농축된 고질 ↔ 흩어진 저질). 그래서 렌더러가 `A` 만으론 절대 못 만든다.

**왜 시뮬이 선행해야 하나 (정직성).** E 농도로 색온도를 *근사*할 수는 있다 — 하지만 그건 *질 없는 필드에 질을 발명*하는 연성 author 다(§6·`화면은 스냅샷의 함수`를 깬다). 색온도가 *원인 있는* 정직한 세계가 되려면 질이 **시뮬의 사실**이어야 한다. 시뮬이 에너지에 질 `q` 를 실으면, 렌더러는 그걸 흑체 곡선 하나로 **만물에 일관되게**(불씨·흐름·별·식은 웅덩이) 코드 0 으로 칠한다.

**렌더 연결 (시뮬 Q 발현 시 켤 렌즈 = L-Q).**
- **채널**: `q`(에너지 질, [0,1] 또는 자유에너지 분율) 한 채널 추가 = `FHktWorldView` 한 양 증가(§3). 64×64 라 무료.
- **읽기 함수**: `색 = blackbody(q)` (붉음→주황→백색→청색), **세기 = `A`**(현행 유지). 질×세기 = 색온도 있는 발광. *분포 재성형 0* — `q` 를 읽어 색에 매핑만.
- **이산 분기 유지**: 별 FSM(kindling/burning/ash)은 그대로 — `q` 는 그 *위*의 연속 색온도(라벨이 띄엄, 색온도가 연속).
- **장부 시각화**: 고질(별 근처)→저질(주변)→열적(식어 가라앉음)의 *질 구배*가 흑체 그라데이션으로 보인다 = "빛은 사라지지 않고 *식으며* 돈다"(정직성의 심화).

**둘째 렌즈 후보 — 장거리 운반 → 조명 (L-I, 시뮬 transport 선행).** 시뮬에 복사/이류가 들어오면 "이 칸이 먼 별서 flux 받음"이 필드의 사실이 되어, 렌더러가 god-ray·원경 조명을 *지어내지 않고* 읽어 그린다(현재 별빛은 제 기둥 분수라 원경 조명은 author 라 못 켠다). 이건 시뮬 트랙 [STATE.md](STATE.md) 의 희소 스택 블로커 해결과 *같은 항*이다.

> **요약**: 렌더 척추는 이제 *준비*돼 있다 — 시뮬이 `q`(질)·transport(운반)를 실으면 색온도(L-Q)·조명(L-I) 렌즈가 author 0 으로 켜진다. 그때까지 빛은 세기만 정직하게 비추고 색온도는 비워 둔다. 진행 현황·blocked 표시는 [RENDER-STATE.md](RENDER-STATE.md) §3·§5.
