// O0 세계관 공리 — 세계에 어떤 존재와 현상이 허용되는지 정의하고 위반을 거부한다.
//
// 계층 순서로는 O0 이 맨 앞이지만(공리 → 존재론 → 상태), 구현 순서는 O1 → O2 → O0 이다
// (WORKFLOW §9). 공리가 무엇을 막는지 말하려면 먼저 "막을 것"(존재론 원소·상태 자리)이
// 있어야 하기 때문이다. 그래서 O0 는 O1·O2 의 어휘로 적히고, 대신 그 둘이 이미 걸어 둔
// 관문들이 어느 공리를 강제하고 있었는지를 되짚어 값으로 남긴다.

export * from './axiom.ts';
export * from './definition.ts';
