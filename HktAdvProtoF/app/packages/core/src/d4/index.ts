// D4 의존 충족도 평가 — 모양이 값을 만나 압력이 된다.
//
// D0~D3 은 전부 **모양**이었다: 무엇에 기댈 수 있고(D0), 그것이 어떤 그래프이고(D1),
// 종이 무엇을 물려주고(D2), 개체가 어떻게 갈라지는가(D3). 그 모양들은 세계의 값을 한 번도
// 보지 않았다. D4 가 처음으로 세계를 읽는다.
//
//   D4-a  개체가 지고 온 값과 세계의 자리를 O2 트리 하나로 모은다 — 지금의 세계.
//   D4-b  조건과 실제 값의 거리를 결핍(0~1)으로 읽는다 — 재고가 하나 모자란 것과 텅 빈 것은 다르다.
//   D4-c  Pressure = Strength × Deficit × Urgency × FailureRisk 로 압력을 내고 5단계를 판정한다.
//
// 그리고 원문이 건 두 조건이 여기서 성립한다:
//   **식량이 충분하면 압력이 0 이다** — 곱이므로 결핍 0 은 목적을 만들지 않는다.
//   **식량이 줄면 압력이 점진적으로 오른다** — 결핍이 커지고, 결핍이 이어질수록 위험이 커진다.
//
// 이 압력이 P 계층의 입력이다. 목적은 지어내는 것이 아니라 **비어 있는 자리에서 자란다.**
export * from './violation.ts';
export * from './snapshot.ts';
export * from './deficit.ts';
export * from './pressure.ts';
