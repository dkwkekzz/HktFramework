// P3 가능성 그래프 지연 확장 — 모든 가능성을 미리 만들지 않는다.
//
// P0~P2 는 결핍 앞에 놓이는 길을 세우고(P0), 방향으로 묶고(P1), 누가 서 있는지로 좁혔다(P2).
// 남은 문제는 크기다 — 좁혔어도 여전히 "할 수 있는 것 전부" 이고, 세계가 커지면 그 전부를
// 매 틱 펼치는 것은 불가능하다. P3 은 **지금 관련된 부분만** 펼친다.
//
// 무엇이 관련되는가는 셋에서 온다: 지금 보이는 것·기억·관계(P3-b). 그리고 그전에
// 원자 사이의 **먼저**가 있어야 한다(P3-a) — 순서를 모르면 무엇을 펼쳐야 할지도 모른다.
export * from './violation.ts';
export * from './prerequisite.ts';
