// R0 세계 상태 저장소 — 정식화된 세계의 실제 상태를 원장 하나에 담고 시간을 가로질러 조회한다.
//
// 여기서 계층이 바뀐다. P 계층까지는 전부 아직 일어나지 않은 것(가능성·목적·계획)이었다.
// R0 부터는 세계에 실제로 적히는 것을 다루고, 그래서 처음으로 **지우면 안 되는 것**이 생긴다.
//
// 이 계층의 한 문장: **R0 은 세계를 바꾸지 않는다 — 담고, 지우지 않고, 시간을 가로질러 읽는다.**
// 조립 관문은 O2 가, 스냅샷의 모양은 D4 가, 시간은 V1 이 이미 세웠다. R0 이 더하는 것은 주인과 열이다.
export * from './violation.ts';
export * from './ledger.ts';
export * from './query.ts';
