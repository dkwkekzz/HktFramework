// D1 의존 그래프 스키마 — 확정된 열한 종을 그래프로 세운다.
//
// D0 는 "무엇에 기댈 수 있는가" 의 목록을 확정했다. 목록은 아직 그래프가 아니다 — 열한 종을
// 점으로 세우고 그 점들을 이어야 "이것이 끊기면 저것이 무너진다" 가 계산 가능해진다.
//
// D1 이 하는 일은 그 점과 선의 모양을 못박는 것이고, 지켜야 할 것은 하나로 요약된다:
// **D0 가 못박은 종의 성격을 그래프가 어겨서는 안 된다.**
//
//   줄지 않는 것(depletes=false)은 소모할 수 없다 — 규칙을 다 써 버릴 수는 없다.
//   그 대상이어야 하는 것(targeting='named')은 무엇으로든 대체될 수 없다.
//   종이 읽지 않는 자리를 조건으로 걸 수 없다 — 자원 노드가 남의 신뢰로 채워지지 않는다.
//
// 그리고 그래프 자체에도 규칙이 있다: 뿌리에서 닿지 않는 노드는 이 주체의 의존이 아니고,
// 한 주체의 그래프 안에서 의존이 맴돌면 D4 의 압력이 끝나지 않는다.
export * from './violation.ts';
export * from './node.ts';
export * from './edge.ts';
