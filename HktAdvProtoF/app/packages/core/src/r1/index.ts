// R1 사건으로만 바뀌는 세계 — 상태를 바꾸는 길을 하나로 좁힌다.
//
// R0 은 세계에 주인을 주었지만 그 원장의 근거는 아직 사람이 적은 문자열 한 줄이었다.
// R1 이후로는 genesis 를 뺀 모든 칸이 사건 id 를 가리키고, 사건 없이 담긴 칸은 감사에서 걸린다.
//
// 이 계층의 한 문장: **R1 은 새 문법을 만들지 않는다 — 이미 있는 문법(P0 걸림·O1 사건·R0 원장)을
// 하나의 통로로 잇는다.**
export * from './violation.ts';
export * from './event.ts';
