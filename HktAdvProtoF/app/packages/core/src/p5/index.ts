// P5 전략과 행동 계획 — 고른 목적을 실제 행동 단위까지 분해한다.
//
// P4 까지 오면 주체는 무엇을 좇을지 안다. 그런데 그 목적은 아직 한 걸음으로 적혀 있다 —
// "마을과 주고받는다". 실제로는 그전에 가져올 것이 있고, 그전에 볼 것이 있다.
//
// 이 계층의 한 문장: **계획은 새로 만드는 것이 아니라 이미 걸린 "먼저" 를 잇는 것이다.**
// P3-a 가 원자 사이의 먼저를 P0 걸림에서 계산했고, P4-a 가 그것을 세계와 맞대어 한 칸짜리
// 먼저로 냈다. P5 는 그 한 칸을 더 물을 것이 없을 때까지 되풀이할 뿐이다.
export * from './violation.ts';
export * from './chain.ts';
