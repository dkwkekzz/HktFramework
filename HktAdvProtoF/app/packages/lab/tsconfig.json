{
  "// ": "에디터용 — 실제 검사는 루트 tsconfig 가 전체를 한 번에 수행한다 (npm run typecheck).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "lib": ["ES2023", "DOM"]
  },
  "include": ["src/**/*.ts", "suites/**/*.ts", "test/**/*.ts", "scripts/**/*.ts", "verify/**/*.ts", "vite.config.ts"]
}
