# 진행 기록 — 단계 3 (M3 충돌하는 주체)

> STATE.md 구현 현황 표에서 분리한 **완료 작업 상세** (CLAUDE.md·WORKFLOW §8 규칙).
> 이 단계는 닫혔다 — 리뷰 대기 (`/advproto-review`, WORKFLOW §10 — 사용자가 요청할 때만).
> 현재 상태·TODO 는 [STATE.md](../STATE.md) 를 본다. 완료 판정의 원본은 언제나 증거 파일
> (`app/packages/contracts/evidence/`)이다 — 이 문서는 사람이 읽는 확인 장면의 기록일 뿐이다.
>
> 착수 순서는 `R0 → R1 → R2 → R3 → R4 → D5 → R5 → R6` 이고 표도 그 순서로 선다.

## 모듈별 확인 장면

| 모듈 | 상태 | 확인 장면 |
|---|---|---|
| R0-a 원장과 커밋 관문 | DONE | `node --test` — 세계에 처음으로 **주인**이 생긴다. D4 의 스냅샷은 필요할 때마다 새로 조립되는 한 컷이었고 지나간 값은 아무 데도 남지 않았다 — R0 은 그것을 담는다. 새로 짓는 것은 최소다: 조립 관문은 O2 `assembleWorld`, 무엇이 달라졌는지는 O2 `worldDiff`, 시간은 V1 틱, 해시는 V1 `stateHash`. **원장은 시간이 아니라 변화를 센다** — 한 자리도 다르지 않은 세계는 칸을 늘리지 않는다(`empty-commit`). 칸마다 앞 칸의 해시를 품으므로 **지나간 칸의 값 하나를 손대면 그 뒤가 전부 어긋나고**(`broken-chain`), 손댄 원장 위에는 새 칸을 쌓지도 못한다. 근거의 사건 자리(`CommitCause.eventIds`)는 **비운 채로 연다** — 채우는 것은 R1 이고, 지금은 사람이 읽는 이름만 요구한다(`causeless-commit`). O2 관문이 막은 값이 하나라도 섞이면 커밋 전체가 물린다 — 세계는 반쪽으로 담기지 않는다. 되돌아가는 틱·같은 틱·두 번째 genesis 도 각자의 사유로 걸리고, 물린 커밋은 던지지 않고 원장을 그대로 둔다. 같은 재료면 분해했다 다시 세워도 같은 지문이다(O2 왕복 성질을 실제로 쓴다) |
| R0-b 시간을 가로지르는 조회 | DONE | `node --test` — 담기만 하고 물을 수 없으면 원장이 아니다. **묻는 틱과 답하는 틱은 다르다** — 원장은 변화를 세므로 틱 405 에 칸이 없고, 그러면 "없다" 가 아니라 **틱 403 의 세계가 아직 서 있다**가 답이며 답이 어느 틱에서 왔는지가 함께 온다. 자리의 역사는 **값이 실제로 바뀐 칸만** 남는다(재고는 세 줄인데 굶주림은 처음 선 한 줄뿐이다) — 재료를 새로 만들지 않고 각 칸이 이미 품은 `changes`(O2 worldDiff)를 자리로 거를 뿐이다. 두 틱 사이의 차이도 O2 가 세고 R0 은 어느 칸과 어느 칸인지만 고른다. 그리고 **던지지 않는다**: 세계가 서기 전을 물으면 "값이 없는 것이 아니라 물을 자리가 없다" 로 답이 갈리고, 빈 원장·세계에 없는 자리·마지막 칸보다 뒤도 각자의 사유로 선다 |
| **R0 세계 상태 저장소** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R0.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r0` — **세계에 주인이 생긴다.** D4 가 만든 열 틱을 그대로 담으면 **여섯만 남는다** — 창고가 바닥난 뒤의 넷(틱 421·427·433·442)은 시간은 흘렀지만 세계가 한 자리도 다르지 않기 때문이다. **원장은 시간이 아니라 변화를 센다.** 남은 여섯 칸에는 늘 46 자리가 서 있는데 첫 칸에서 그 전부가 새로 생기고 그 뒤로 달라지는 것은 사냥꾼 넷의 재고뿐이다 — 담는 것은 세계 전체이되 세는 것은 차이다. 가운데 조회표에서 **묻는 틱과 답하는 틱이 갈린다**: 틱 430 에는 칸이 없지만 세계는 있다(틱 415 의 세계가 아직 서 있고, 답이 어느 칸에서 왔는지가 값과 함께 온다). 자리의 역사는 값이 바뀐 칸만 남아 재고는 여섯 줄인데 선 곳은 한 줄뿐이다. 아래에서 **틱 406 의 재고 하나를 99 로 고치면 그 칸만 걸리는 것이 아니다** — 다시 센 해시가 다음 칸의 기대값이 되므로 뒤 칸 셋과 원장의 지문까지 여덟 자리가 어긋난다. R0 이 새로 지은 것은 최소다(조립 관문=O2 · 차이=O2 · 시간=V1 · 해시=V1); 더한 것은 **주인과 열**뿐이고, 근거의 사건 자리는 **비운 채로 R1 에 넘긴다**. 설 수 없는 커밋 일곱은 각자의 사유로 물리고 원장은 그대로 남는다 (Chromium 확인: 7섹션 전부 채움·빈칸 0, 표 10개·58행, 붉은 행 0, 4xx·콘솔 오류 없음) |
| R0-c 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 17항 · 실패 8항 · 경계 17항) 전부 통과, Lab `/r0` 화면 7요소 채움, 증거 R0.json 생성. 이때 사슬 검사를 한 칸 고쳤다: **다시 센 해시를 다음 칸의 기대값으로 넘긴다** — 적힌 해시를 그대로 넘기면 손댄 칸 하나만 걸리고 그 뒤가 멀쩡해 보이기 때문이다(그것이 소급 수정의 수법이다). 이제 틱 406 을 손대면 그 칸·뒤 칸 셋·원장의 지문까지 여덟 자리가 어긋난다. R1 을 PLANNED 로 선등록해 "착수 가능" 목록이 다시 계산된다(#663 규칙) |
| R1-a 사건의 모양과 걸림 | DONE | `node --test` — P0-c 가 요청까지 만들어 두고 남긴 한 줄("세계에 적히는 것은 R1·R2 의 몫이다")을 갚는다. 사건을 새로 설계하지 않는다 — O1 `Event` 가 이미 연 자리(틱·일으킨 자·바뀐 상태·까닭)를 **요청과 세계에서** 채울 뿐이고, 서지 못하면 사유가 남는다. 못박은 것 둘: **세계는 요청한 만큼만 바뀐다**(요청서 밖의 자리를 슬쩍 끼우면 `unrequested-effect` — 이것이 없으면 P0-c 관문이 통째로 무의미해진다) · **사건은 자기가 선 세계를 기억한다**(`from` 을 손으로 적게 하지 않고 세계에서 읽는다 — 적게 하면 틀리게 적을 수 있다). 획득이 남의 신뢰를 바꾸려 하면 P0-c `off-atom-change` 가 그대로 옮겨 오고, 공짜 사건·바꾸는 자리와 치르는 자리를 바꿔 적은 것·세계에 없는 자리·값이 지금과 전부 같은 사건이 각자의 사유로 걸린다. **자연 발생 사건(일으킨 자 null)은 유예했다** — 근거를 대려면 규칙이 실체화(W2)돼야 하고, 지금 열면 아무 자리나 바꾸는 통로가 하나 더 생긴다 |
| R1-b 세계에 적용 | DONE | `node --test` — R0 이 비워 둔 근거 자리가 **처음으로 찬다**: 사건을 얹으면 그 칸의 `cause.eventIds` 가 사건 id 를 가리키고, 바뀌는 자리는 사건이 적은 셋뿐이며 나머지 세계(상단 11 의 몸·몰이꾼이 선 곳)는 그대로다. 새 저장소를 짓지 않는다 — 적용은 R0 커밋 그대로라서 R0 이 지키던 것이 전부 그대로 지켜진다(**재고를 0 아래로 내리는 사건은 세계가 거부한다** — O2 관문이 그대로 말한다). R1-b 가 더한 것은 셋이다: **낡은 전제 위에 쓰지 않는다**(사건이 만들어진 뒤 세계가 움직였으면 `stale-effect` — 그 사건은 이미 다른 세계의 것이다) · **되돌릴 수 없는 것은 되돌리지 못한다**(P0-b `reversible: false` 인 제거가 깎은 몸은 예전 값으로 못 돌아간다. 획득이 옮긴 재고는 돌아간다 — 모든 손실이 봉인되는 것은 아니다) · **사건 없이 담긴 칸을 찾아낸다**(`witnessViolations` — 이것이 "사건 없는 변경 금지" 를 주장이 아니라 검사로 만든다. genesis 는 예외다) |
| **R1 사건으로만 바뀌는 세계** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R1.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r1` — **세계를 바꾸는 길이 하나가 된다.** R0 의 원장은 근거가 사람이 적은 문자열 한 줄이었다 — 세계는 재고가 왜 줄었는지 몰랐다. 같은 겨울을 **몰이꾼 04 가 낸 사건 다섯**(알아보고·가져오고·주고받고·먹고·친다)으로 다시 세우면 타임라인(공용 렌더러 ④의 둘째 소비자)이 왼쪽부터 서고, **genesis 를 뺀 다섯 칸이 각각 사건 id 하나를 댄다**(감사: 사건 없이 담긴 칸 0). 가운데에서 **세계는 요청한 만큼만 바뀐다** — 04 의 재고가 10 → 8 로 움직이는 동안 사제의 재고는 10 그대로다(04 의 요청서에 그 자리가 없다). 아래에서 **어떤 자리는 돌아오지 않는다**: 제거가 깎은 상단 11 의 몸(0.8 → 0.2)은 되돌리는 사건으로 복구되지 않고(`irreversible-undo`), 대가로 깎인 04 의 몸은 다시 채울 수 있다 — 봉인되는 것은 그 원자가 **한 일**이다. 설 수 없는 여덟 중 **다섯은 사건이 서기 전에, 셋은 세계에 얹는 자리에서** 걸리고, 바닥난 창고에서 또 먹는 사건은 **서기는 하되 세계가 거부한다**(O2 범위 — R1 이 다시 판정하지 않는다). R1 이 새로 만든 문법은 없다: 요청은 P0-c, 사건의 모양은 O1, 담기는 규칙은 R0 이 이미 정했고 R1 은 그것을 하나의 통로로 이었다 (Chromium 확인: 7섹션 전부 채움·빈칸 0, 타임라인 1개·걸음 5·화살표 4, 표 9개·38행, 붉은 행 0, 4xx·콘솔 오류 없음) |
| R1-c 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 17항 · 실패 6항 · 경계 11항) 전부 통과, Lab `/r1` 화면 7요소 채움, 증거 R1.json 생성. R2 를 PLANNED 로 선등록해 "착수 가능" 목록이 다시 계산된다(#663 규칙) |
| R2-a 흔적의 통로 | DONE | `node --test` — 통로를 **원자가 아니라 자리**에 건다(같은 자국을 여럿이 남길 수 있다는 것이 곧 관찰의 뜻이다). P0-b 열여섯이 움직일 수 있는 자리 30 이 전부 답을 갖고, O1 이 연 통로 6종이 전부 쓰인다. **새지 않는 자리 일곱은 선언된 예외**다 — 정보 영역 다섯(앎·확신·출처·거짓 표시·비밀)과 몸 안 둘(허기·대사). 여기서 하나라도 새게 하면 R3 의 선택 감지와 R4 의 오인이 통째로 무의미해진다(남의 앎을 직접 읽을 수 있으면 오해할 것이 없다). 정보 영역이라도 **퍼지는 중인 말은 들린다**(`rumorSpread` — R4 소문이 설 자리). 애매함의 재료도 여기서 나온다: 체력은 열둘이 움직여 가장 애매하고 파손은 제거 하나뿐이라 부순 자를 가리킨다. 통로 없는 자리·세계에 없는 자리·이유 없는 침묵·낡은 예외·쓰이지 않는 통로·6종 밖의 통로가 각각의 사유로 걸리고, **원자가 새 자리를 열면 표면이 곧바로 그 자리의 답을 요구한다** |
| R2-b 세기·수명·자리 | DONE | `node --test` — 사건 하나가 흔적으로 갈린다. 넷 다 앞 계층에서 읽어 온다: 통로는 R2-a 표, 수명은 P0-b `reversible`, 자리는 세계의 `physical.region`, 세기의 잴 수 있음/없음은 P4-a `MEASURABLE_SPAN` 그대로다. **세계가 폭을 적어 둔 자리는 그 폭으로 재고, 상한이 열린 자리(재고·빚)는 그 자리 자신이 눈금이 된다** — 열에서 여덟은 옅은 자국이고 열에서 0 은 최대치이며 없던 것이 서는 것도 최대치다. **앎만 움직인 사건은 흔적을 내지 않고, 새는 것은 그것을 얻느라 닳은 몸뿐이다** — 그 자국의 애매함은 0.73(체력을 움직이는 원자가 열둘)이라 누군가 무언가 했다는 것 말고는 말해 주지 않는다. **되돌릴 수 없는 원자가 바꾼 자리의 흔적은 사라지지 않고**(수명 null) 대가로 깎인 제 몸의 자국은 삭는다 — R1-b 가 봉인한 것과 정확히 같은 자리다. **흔적은 지닌 자가 선 곳에서 나므로 같은 사건이 두 곳에 남는다**(협곡의 04 와 마을의 11). 일으킨 자 없는 사건·원인 없는 흔적·표면의 구멍·자리 없는 흔적이 각각의 사유로 걸린다 |
| R2-c 현상장과 감사 | DONE | `node --test` — 흔적은 나는 순간에만 있는 것이 아니라 **남아 있다가 삭는다**. 현상장은 R1 `EventLog` 와 같은 모양이라 지우지 않는다 — 삭는 것은 **묻는 틱에 따라 답이 갈려서**다(R0-b 가 "묻는 틱과 답하는 틱은 다르다" 로 세운 태도 그대로). 감사가 둘을 가른다: **위반**(로그에 없는 원인·봉인된 자리의 누출·움직이지 않은 자리의 흔적·**새는 자리를 움직였는데 흔적이 하나도 없는 사건**)과 **사실**(흔적 없이 지나간 사건 — 앎만 움직인 사건은 세계를 바꾸고도 아무것도 남기지 않으며 그것이 정보전이 성립하는 조건이다). 마지막 위반이 "사건은 흔적을 남긴다" 를 주장이 아니라 검사로 만든다 — R1-b `witnessViolations` 의 짝이다. 봉인을 걷고 통로까지 내주면 **남의 확신이 그대로 읽히는 세계**가 되는 것을 대조군으로 보인다 |
| **R2 사건이 남기는 흔적** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R2.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r2` — **세계가 바뀌는 것과 그것이 알려지는 것이 갈린다.** R1 의 다섯 사건은 세계를 움직였지만 아무도 보지 못했다 — 같은 다섯을 흔적으로 읽으면 통로 넷으로 **열다섯**이 난다. 위에서 자리 30 이 전부 통로를 대는 표가 펼쳐지고 **통로는 원자가 아니라 자리가 정한다**(같은 자국을 여럿이 남길 수 있다는 것이 곧 관찰의 뜻이고, 그것이 애매함이 된다). 가운데에서 **앎은 새지 않는다** — 04 가 마비독을 알아낸 일에서 밖으로 나온 것은 그것을 알아내느라 닳은 몸뿐이고, 그 자국은 열여섯 중 열둘이 남길 수 있어(애매함 0.73) 누군가 무언가 했다는 것 말고는 말해 주지 않는다. 봉인을 걷고 통로를 내주면 흔적이 둘에서 셋이 되는데 **늘어난 하나가 남의 확신**이다 — 그런 세계에서는 오해할 것이 없으므로 R3 도 R4 도 설 자리가 없다. 아래에서 **어떤 흔적은 사라지지 않는다**: 되돌릴 수 없는 원자(제거)가 **바꾼** 자리의 자국은 수명이 없고, 그 일을 하느라 닳은 제 몸의 자국은 이틀이면 삭는다(R1-b 가 봉인한 것과 정확히 같은 자리다). 그래서 **원장은 여섯 칸을 다 갖고 있는데 한참 뒤 세계에 서 있는 흔적은 둘뿐이고, 그 둘조차 누가 냈는지 말해 주지 않는다.** 그리고 **아무 흔적도 남기지 않는 사건이 실제로 있다** — 앎만 움직인 사건은 세계를 바꾸고도 조용하며, 감사는 그것을 위반이 아니라 사실로 센다. 설 수 없는 흔적 아홉은 **넷은 흔적이 나기 전에, 다섯은 현상장 감사에서** 각자의 사유로 거부된다. R2 가 새로 정한 것은 통로 표와 애매함 눈금뿐이고 나머지는 전부 앞 계층에서 읽어 온다 (Chromium 확인: 7섹션 전부 채움·빈칸 0, 통과 배지 "자리 30(봉인 7) · 흔적 15 · 사라지지 않는 흔적 2 · 시나리오 3/3", 표 12개·82행, 타임라인 1개·걸음 5, 게이지 5행, 붉은 행 0, 페이지 요청 4xx 0 — 콘솔의 404 는 dev 서버 favicon.ico 하나뿐으로 페이지 리소스가 아니다) |
| R2-d 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 24항 · 실패 6항 · 경계 15항) 전부 통과, Lab `/r2` 화면 7요소 채움, 증거 R2.json 생성. R3 을 PLANNED 로 선등록해 "착수 가능" 목록이 다시 계산된다(#663 규칙). 이때 착수 순서를 한 칸 배웠다: **계약을 VERIFIED 로 올리는 것은 증거가 난 뒤여야 한다** — 증거 없는 VERIFIED 주장은 `/lab/v0` 판정을 붉게 만들고, 그 붉은 페이지가 V3 7요소 검사를 떨어뜨려 증거 생성기가 돌지 못하는 교착이 된다. 계약을 IN_PROGRESS 로 둔 채 증거를 낸 뒤 올리면 한 바퀴에 선다 |
| R3-a 거리와 차폐 | DONE | `node --test` — S0-b 가 판정까지 만들어 두고 남긴 한 줄("어디서 났는지를 거리로 바꾸는 일은 위치를 아는 R3 의 몫이다")을 갚는다. **적히지 않은 거리는 없는 거리다** — 같은 자리는 0, 세계가 적어 둔 장소 간 거리는 어느 쪽에 적혀 있어도 읽고, 없으면 닿지 않는다(D3 "적히지 않은 사이는 없는 사이" 와 같은 태도). **차폐는 자리를 건널 때만 든다** — 같은 자리에 선 자는 이미 가림막 안쪽이라, 협곡 안에서 오간 것은 협곡 안의 넷에게는 보이고 밖에서는 죽는다. 통로별 감쇠 계수는 R3 이 지어내지 않고 **S0-b `CHANNEL_SPECS` 의 문장을 값으로 옮긴다**(빛 1 — "차폐에 가장 약하다" · 소리 0.5 — "벽을 돌아온다" · 흔적·냄새·의념·보고 0). 통로 하나를 빠뜨린 표·1 을 넘는 감쇠·6종 밖의 통로·두 번 적힌 통로가 각각 걸리고, 값마다 근거가 S0-b 를 가리키지 않으면 걸린다 |
| R3-b 감지와 Percept | DONE | `node --test` — 판정을 새로 만들지 않는다: S0-b `perceives` 가 통로·문턱·거리 셋을 이미 묻고 사유(`no-channel`·`too-faint`·`too-far`)까지 낸다. R3-b 는 그 함수에 **세계를 먹인다**. 그래서 같은 다섯 흔적 앞에서 **사냥꾼은 빛과 흔적을, 벌레는 냄새만 읽고 둘이 읽는 것은 하나도 겹치지 않으며**(같은 자리에 서 있는데도), 상단은 보고 통로뿐이라 아무것도 모른다. 마을에서 보면 협곡의 가림막이 빛 0.29 를 0.174 로 깎아 문턱을 못 넘고, 흔적은 가림막을 지나도 도달 거리 5m 에서 걸린다. 그리고 이 계층의 한 줄: **지각에는 진실이 실리지 않는다.** 감지한 자가 얻는 것은 통로·세기·자리·거리·애매함 아홉 자리까지이고 어느 자리가 움직였는지·누가 냈는지·무슨 원자였는지는 없다 — **흔적을 통째로 스프레드하면 `truth-leak` 로 걸린다**(주장으로 두면 언젠가 세계가 조용히 전지해진다). 세계에 없는 흔적·원래보다 센 감지·통로나 자리를 바꿔 적은 지각도 각각 걸린다 |
| R3-c 지각장과 감사 | DONE | `node --test` — 세계에는 여럿이 산다. R2-c 현상장과 같은 모양으로 담고(지우지 않는다), 감사가 **위반**과 **사실**을 가른다. 셋이 같은 흔적을 둘러보면 대조표가 서고 못 읽은 칸에는 **왜 못 읽었는지**가 선다(통로 없음·너무 옅다·너무 멀다). **아무도 듣지 못한 소리가 있다** — 위반이 아니라 사실이다(세계는 아무도 안 볼 때도 바뀌고, 그 자국은 볼 사람이 오기를 기다리다 삭는다. 이것이 막히면 아무도 몰래 무언가 할 수 없다). 이미 삭은 흔적은 애초에 보이지 않고 아직 나지 않은 흔적도 마찬가지다 — **R2-c `standingAt` 이 판정한 그대로이고 R3 이 다시 정하지 않는다.** 감지 프로필 없는 주체의 지각·그 틱에 서 있지 않은 흔적의 지각·진실이 실린 지각이 각각 걸린다 |
| **R3 감각과 위치에 따른 감지** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R3.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r3` — **놓인 것과 읽히는 것이 갈린다.** R2 의 흔적은 세계에 놓였을 뿐 아직 아무의 것도 아니었다. 넷이 겨울을 둘러보면 **같은 협곡에 선 둘이 읽는 것이 하나도 겹치지 않는다** — 사냥꾼은 자국을 보고 장막벌레는 냄새를 맡는다(종이 다르면 열린 통로가 다르다 — S1). **몸 없는 상단과 어머니신은 끝내 모른다**: 보고 통로뿐인데 이 겨울의 보고는 문턱에 못 미치고, 신의 의념 잔향은 아예 나지 않았다 — 조직이 늘 늦게 아는 자리가 이것이다. 가운데 대조표에서 흔적마다 누가 보고 누가 못 보는지가 서고 **못 읽은 칸에는 왜 못 읽었는지가 선다**(통로 없음·너무 옅다·너무 멀다). 그 아래에서 **선 곳 하나가 읽는 것을 0 으로 만든다** — 같은 사냥꾼의 눈인데 마을에 서면 협곡의 가림막이 빛 0.18·0.11 을 0.109·0.067 로 깎아 문턱을 못 넘고, 자국은 깎이지 않지만 도달 거리 5m 에서 걸린다. 거리를 아예 적지 않으면 협곡은 그에게 **없는 곳**이다. 그리고 **아무도 보지 못한 흔적 셋이 남는데 감사는 그것을 위반으로 세지 않는다** — 세계는 아무도 안 볼 때도 바뀐다. 마지막으로 **지각에는 진실이 실리지 않는다**: 읽은 자가 얻는 것은 아홉 자리(통로·세기·자리·거리·틱·애매함·id·주체·흔적 id)까지이고 어느 자리가 움직였는지·누가 냈는지·무슨 원자였는지는 없다 — 흔적을 통째로 스프레드하면 `truth-leak` 으로 걸린다. R3 이 새로 정한 것은 거리와 차폐를 세계에서 읽는 규칙 하나뿐이고 감쇠 계수조차 S0-b 의 문장을 옮긴 것이다. 설 수 없는 지각 아홉은 **둘은 감지 자리에서, 일곱은 지각 검사에서** 거부된다 (Chromium 확인: 7섹션 전부 채움·빈칸 0, 통과 배지 "흔적 6 · 읽은 주체 2/4 · 지각 3 · 아무도 못 본 흔적 3 · 시나리오 3/3", 표 11개·43행, diff 뷰 1개(갈림 표시), 게이지 5행, 시나리오 항목 37, 붉은 행 0, 페이지 요청 4xx 0 — 콘솔의 404 는 dev 서버 favicon.ico 하나뿐이다) |
| R3-d 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 22항 · 실패 5항 · 경계 12항) 전부 통과, Lab `/r3` 화면 7요소 채움, 증거 R3.json 생성. R4 를 PLANNED 로 선등록해 "착수 가능" 목록이 다시 계산된다(#663 규칙). R2-d 에서 배운 순서를 그대로 따랐다 — 증거를 먼저 내고 계약을 VERIFIED 로 올린다 |
| R4-a 짐작의 후보 | DONE | `node --test` — R3 이 남긴 한 줄("읽은 것에서 무엇이 일어났는지 짐작하는 것은 R4 다")의 첫 칸을 갚는다. 후보를 R4 가 세지 않는다 — **통로 → 자리(R2-a `LEAK_CHANNELS`) → 원자(R2-a `atomsMoving`)** 두 걸음을 읽어 올 뿐이고, 통로 표를 손대면 후보가 그 자리에서 따라 움직인다(소리에서 `physical.broken` 을 지우면 후보가 0 이 된다). 재료로 통로를 쓰는 이유는 하나다: **자리가 어느 통로로 새는지는 세계의 규칙이지 세계의 장부가 아니다**(불이 뜨겁다는 것을 아는 것과 같다). 그래서 **소리를 들은 자는 무엇이 있었는지 안다** — 소리가 나는 자리는 하나뿐이고 그 자리를 움직이는 원자는 제거 하나뿐이다. 반대로 자국과 냄새는 열둘을 가리켜 누군가 무언가 했다는 것 말고는 말해 주지 않는다. 그리고 검사 하나가 선다: **후보에서 잰 애매함은 지각이 실어 온 애매함보다 작을 수 없다** — 지각의 애매함은 실제로 움직인 자리 하나에서, 후보의 애매함은 통로가 여는 자리 전부에서 세므로 통로 쪽이 언제나 넓다. 뒤집히면 짐작이 실제 자리를 몰래 본 것이다(`guess-narrower-than-trace`). 후보 없는 통로·6종 밖의 통로·후보가 실제를 못 덮는 자리가 각각의 사유로 걸린다 |
| R4-b 믿음과 확신 | DONE | `node --test` — 원문 §6.1 이 보인 장면("같은 현상을 사냥꾼과 종교인이 다르게 읽는다")을 값으로 세운다. **R4 가 새로 정하는 것은 여기 한 줄뿐이다: 자기가 낼 수 있는 것으로 읽는다.** 재료조차 만들지 않는다 — P2 `PossibilityGrammar.allowed` 가 이미 "유형이 깔고 능력이 얹고 금기가 덜어 낸" 목록을 갖고 있고 R4 는 후보를 그것과 겹칠 뿐이다. 그래서 **죽이지 않는 자에게 그 자국은 죽임이 아니고, 그 일이 실제로 일어났다면 그는 틀린다** — 그것이 이 계층이 있는 이유다(틀린 믿음이 없으면 R4 는 한 틱 늦은 전지일 뿐이다). 다만 **후보가 하나뿐이면 문법을 묻지 않는다**: 좁힘은 여럿 중에서 고르는 일이라 고를 것이 없으면 편향이 낄 자리도 없고, 부서지는 소리를 들은 사제는 제가 죽이지 않아도 무언가 부서졌다는 것을 안다(세계의 규칙은 누구나 안다). 겹침이 하나도 없어도 믿음은 사라지지 않는다 — **내가 낼 수 있는 무엇도 아니라는 것도 하나의 읽기다.** 확신은 P4-c 와 같은 태도다: 요소 셋(좁힘 3·세기 2·반복 1)에서 **재계산되고**(손으로 고치면 `confidence-drift`) **좁힘이 상한이다** — 열둘 중 하나인 자국은 아무리 진하게 여러 번 봐도 확신할 수 없고, 여러 번 봐서 굳는 것은 "무언가 있었다" 쪽이다. 근거 없는 믿음·남의 눈을 빌린 믿음·후보 밖의 짚음·손으로 늘린 후보·범위 밖 확신·과한 확신·주인 없는 믿음·진실이 실린 믿음이 각각의 사유로 걸린다 |
| R4-c 믿음 그래프와 감사 | DONE | `node --test` — 세계에는 여럿이 살고 한 사람도 여러 자국을 읽으며 **같은 자국을 다시 읽기도 한다**. R3-c 지각장·R2-c 현상장과 같은 모양으로 담고(지우지 않는다), 감사가 위반과 사실을 가른다. 여기서는 사실 쪽이 이 계층의 전부다: **빗나간 믿음은 위반이 아니다**(그것을 막으면 R4 는 한 틱 늦은 전지가 되고 원문 §6.1 이 갈라 놓은 둘이 도로 붙는다) · **아무도 아무것도 믿지 않는 흔적**(R3-c 가 "아무도 보지 못한 흔적" 을 사실로 센 것의 다음 칸) · **삭은 자국 위에 남는 믿음**(자국은 사라졌는데 믿음은 남는다 — 사라진 창고가 기억에 남는 것과 같은 자리이고 R5 거짓 믿음의 씨앗이다). 실제와의 대조(`compareToTruth`)는 **감사만 본다** — 주체가 부를 수 있으면 그 순간 세계가 전지해진다. 대조는 셋으로 갈린다: 정확히 짚음·틀리진 않았으나 좁히지 못함·빗나감. 같은 자국을 다시 읽으면 믿음이 늘지 않고 굳는다(id 가 믿는 자와 흔적에서 나오므로 두 번째 믿음은 같은 믿음이다). 감사가 막는 것은 후보가 실제를 아예 덮지 못하는 자리(`candidate-miss`)와 세계에 서 있지 않은 자의 믿음뿐이다 |
| **R4 믿는 세계** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R4.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r4` — **읽은 것과 무엇으로 읽는가가 갈린다.** R3 은 넷을 세워 "누가 읽는가" 를 갈랐지만 거기서 갈린 것은 **종**이었다 — 눈이 다르면 다른 세계를 사는 것은 당연하다. 여기서는 협곡에 **같은 종·같은 눈·같은 자리**의 셋을 세운다(몰이꾼·사제·상단). 다른 것은 손뿐이다. 위에서 통로별 후보표가 펼쳐지고 **후보를 R4 가 세지 않는다**(통로 → 자리 → 원자 두 걸음을 R2-a 에서 읽어 온다) — **소리를 들은 자는 무엇이 있었는지 알고**(그 자리를 움직이는 원자가 제거 하나뿐이다) 자국·냄새를 읽은 자는 열둘 중 하나라는 것만 안다. 셋은 겨울을 세 번 둘러보고 **똑같이 셋씩 읽는데**(눈이 같으니 당연하다) 그 다음이 갈린다: 자국의 후보 열둘에서 **사제는 죽임을, 상단은 빼앗기를 덜어 낸다**(P2 금기가 정한 그대로다). 그리고 **그 자국을 낸 것은 제거다** — 좁게 짚은 둘이 더 확신하는데 그중 **사제가 빗나간다**(0.333 을 확신하며 틀리고, 몰이꾼은 0.267 로 덜 확신하며 틀리지 않는다). 낼 손이 없는 일은 떠오르지도 않는다. 그리고 **그 틀림은 감사가 위반으로 세지 않는다** — 막으면 R4 는 짐작이 아니라 한 틱 늦은 전지(全知)가 되고 원문 §6.1 이 갈라 놓은 둘이 도로 붙는다. 아래 게이지에서 **두 번 읽으면 확신이 오르되 좁힘이 허락한 데서 멈추고**(빛 0.29 를 두 번 본 몰이꾼은 상한 0.333 에 닿아 멈추고 사제·상단은 0.379 까지 간다), 세 번째 둘러봄의 틱에는 **그 빛이 이미 삭았는데 믿음 여섯이 그대로 남아 있다**(사라진 창고가 기억에 남는 자리 — R5 의 씨앗이다). 서 있는 흔적 여섯 중 다섯에는 아무 믿음도 서지 않는다. R4 가 새로 정한 것은 한 줄뿐이다 — **자기가 낼 수 있는 것으로 읽는다**(다만 후보가 하나뿐이면 묻지 않는다: 고를 것이 없으면 편향도 없다). 설 수 없는 믿음 열하나는 **둘은 믿음을 세우는 자리에서, 아홉은 검사에서** 거부된다 (Chromium 확인: 통과 배지 "믿음 9 · 빗나감 1 · 삭은 자국의 믿음 6 · 아무도 믿지 않는 흔적 5 · 시나리오 3/3", 7섹션 전부 채움·빈칸 0, 표 13개·54행, 게이지 6행, diff 뷰 3개, 시나리오 항목 43, 붉은 행 0 — 빗나간 믿음은 어긋남이 아니므로 노란 행으로 선다, 페이지 요청 4xx 0 — 콘솔의 404 는 dev 서버 `/favicon.ico` 하나뿐으로 페이지 리소스가 아니다) |
| R4-d 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 20항 · 실패 6항 · 경계 19항) 전부 통과, Lab `/r4` 화면 7요소 채움, 증거 R4.json 생성. R5 를 PLANNED 로 선등록해 "착수 가능" 목록이 다시 계산된다(#663 규칙). R2-d 에서 배운 순서를 그대로 따랐다 — 증거를 먼저 내고 계약을 VERIFIED 로 올린다 |
| D5-a 요구의 자리 | DONE | `node --test` — 다툼을 찾으려면 먼저 무엇을 놓고 다투는지를 한 모양으로 적어야 한다. **여기서 새로 정하는 것은 없다** — D1 노드가 이미 자리와 대역을 갖고 있고 대체 가능성도 D1 간선이 적어 둔 값이다. D5-a 가 하는 일은 여러 그래프의 노드를 **한 평면에 늘어놓는 것**뿐이고, 그래야 겹침이 보인다. 못박은 것 하나: **시간은 요구가 아니다** — 주기 조건은 세계의 어느 자리도 잡지 않으므로(장막이 걷히는 주기는 누구에게나 같은 속도로 오고 한 사람이 많이 쓴다고 남의 몫이 줄지 않는다) 겹치지도 다투지도 않는다. 요구는 **두 이름을 함께 진다** — 자리(어느 영역·누구의·어느 경로)와 대상(무엇을 가리키는가). 겹침이 두 축에서 나기 때문이다: 같은 자리를 둘이 보는 것과, 자리는 각자 것인데 세계에 하나뿐인 대상을 둘이 보는 것은 다른 다툼이다. **대체 가능성의 방향을 뒤집지 않는다** — "말린 고기를 다른 것으로 바꿀 수 있는가" 는 그 노드에 **기대는** 간선이 답하고, 뿌리에는 그런 간선이 없어 0 이다(제 몸은 대신할 수 없다). 그리고 **남의 자리를 요구할 수도 있다**(짝의 몸은 내 것이 아니다) — 그것이 주체 간 다툼이 설 자리다. 없는 노드의 요구·남의 그래프를 제 것이라 적은 요구·시간을 요구로 세운 것·범위 밖 대체 가능성이 각각의 사유로 걸린다 |
| D5-b 겹침과 다툼 | DONE | `node --test` — 이 계층에서 가장 중요한 문장은 겹침을 찾는 규칙이 아니라 그 다음 줄이다: **겹친다고 다툼은 아니다.** 여럿이 같은 짝의 몸에 기대는 것은 겹침이지만 다툼이 아니고(짝이 성하면 다 만족한다), 그것을 다툼으로 세면 세계는 모든 겹침이 싸움인 곳이 되어 다툼이 아무 뜻도 없어진다. 그래서 겹침(`Contest`)과 다툼(`DependencyConflict`)을 두 단계로 나눈다. **겹침은 두 축에서 난다** — 자리 축(문자 그대로 한 값을 둘이 본다)과 대상 축(자리는 각자 것인데 가리키는 것이 하나다). 대상 축 겹침이 어느 자리 축 겹침 안에 통째로 들어가면 남기지 않는다(같은 다툼을 두 번 세지 않는다). **다툼이 되는 조건은 둘이다**: `opposed`(같은 자리에 동시에 만족될 수 없는 대역 — **한 몸이 두 곳에 있을 수 없고**, 사냥꾼이 원하는 것과 그 몸 주인이 원하는 것이 반대인 자리도 여기다 = ModulePlan D5 예시) · `scarcity`(같은 대상에 걸린 최소 필요 합이 세계에 있는 것보다 크다 — **이 조건만 세계를 본다**). **셋째 조건(배타적 점유)은 유예했다** — 한 짝이 넷의 등을 동시에 맡을 수 없다는 것을 판정하려면 세계에 수용량을 적을 자리가 있어야 하는데 O2 에 없고, 여기서 지어내면 D5 가 정하는 것이 늘어난다(W 계층이 갚는다). 그리고 **D5 는 이기는 자를 정하지 않는다** — 급함조차 D4 가 잰 값을 읽어 올 뿐이고(손으로 고치면 `severity-drift`), 이기는 자를 적으면 `winner-declared` 로 걸린다. 창고를 채우면 같은 그래프에서 다툼 하나가 사라진다 |
| D5-c 충돌장과 감사 | DONE | `node --test` — R2-c 현상장·R3-c 지각장·R4-c 믿음 그래프와 같은 모양으로 담고(지우지 않는다), 감사가 위반과 사실을 가른다. **사실 쪽이 이 계층의 절반이다** — 다툼이 되지 못한 겹침(`Peace`)이 사유와 함께 남아야 "왜 여기서는 안 싸우는가" 를 물을 수 있고, 아무 다툼에도 끼지 않은 주체도 위반이 아니다. 위반 쪽에 하나가 새로 선다: **조건을 갖췄는데 충돌장에 없는 겹침**(`missing-contest`) — 같은 재료로 다시 판정해 대조하므로 "다툼을 빠뜨리지 않는다" 가 주장이 아니라 검사가 된다(R1-b `witnessViolations` 의 짝). 그리고 **주체↔경합 대상 이분 그래프**가 선다(공용 렌더러 ②). 이분인 이유는 다툼의 모양 자체가 그렇기 때문이다 — 한쪽 열은 주체, 다른 열은 그들이 함께 보는 것이고 **선은 언제나 주체에서 대상으로만 간다**. 주체끼리는 잇지 않는다: **누가 누구와 싸우는지는 아직 아무도 모른다**(서로를 봐야 알고 그것은 R3·R4 다). 선의 굵기는 그 요구의 압력이고 그 값도 D4 에서 온다. 창고를 채우면 **같은 그래프인데 다툼이 하나 줄고 다툼 아닌 겹침이 하나 는다** — 겹침 자체는 그대로다 |
| **D5 의존 충돌 탐지** | **VERIFIED** ([증거](../app/packages/contracts/evidence/D5.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/d5` — **혼자 있는 세계에는 다툴 것이 없다.** D4 까지의 겨울에는 사냥꾼 넷이 있었지만 넷은 서로를 몰랐다 — 각자의 그래프와 각자의 압력이 있었을 뿐이다(원문 §18 이 다툼을 다주체 뒤에 두는 이유다). 여기서 같은 겨울에 나머지 넷(장막벌레·채집 결사·나라·어미신)을 마저 세우면 **요구 쉰하나 중 열둘이 겹치고, 그중 다섯만 다툼이 된다.** 위에서 겹침마다 판정이 사유와 함께 서는데 **다툼이 아닌 줄이 더 많다** — 사냥꾼과 결사는 같은 협곡을, 여섯이 같은 통행법을, 벌레와 신은 같은 둥지를 보는데 셋 다 다툼이 아니다(대역이 함께 서거나, 수용량을 적을 자리가 세계에 없다). **겹친다고 다툼은 아니다** — 이것을 다툼으로 세면 세계는 모든 겹침이 싸움인 곳이 되고 다툼이 아무 뜻도 없어진다. 가운데 이분 그래프에서 **선은 언제나 주체에서 대상으로만 가고 주체끼리는 이어지지 않는다** — 누가 누구와 싸우는지는 아직 아무도 모르기 때문이다(서로를 봐야 알고 그것은 R3·R4 다). 다툼 다섯 중 **넷이 한 주체 안의 것**이다: 사냥꾼은 사냥터(협곡)와 겨울 움막(마을)을 함께 요구하는데 그 둘은 같은 자리의 다른 값이다 — **한 몸이 두 곳에 있을 수 없고, D2 가 명시적으로 D5 에 넘긴 자리다.** 그리고 아래에서 **창고가 비면 넷이 같은 고기를 놓고 갈린다** — 재고 10·6·4 에서는 같은 것을 원해도 다툼이 아니었고(넷의 요구 합 12 보다 세계에 있는 것이 많다) **재고 2 에서 다툼이 선다.** 단계 3 의 대표 장면("두 인간이 음식 하나를 원한다")이 여기서 값으로 섰다. 혼자 선 세계로 되돌리면 **남과의 다툼만 사라지고 제 안의 다툼은 남는다.** D5 가 새로 정한 것은 다툼의 조건 둘뿐이고(양립 불가·모자람, **모자람만 세계를 본다**), 셋째(배타적 점유)는 세계에 수용량을 적을 자리가 없어 W 계층으로 유예했다. **D5 는 이기는 자를 정하지 않는다** — 급함조차 D4 에서 읽어 오고, 이기는 자를 적으면 걸린다. 설 수 없는 다툼 아홉은 각자의 사유로 거부된다 (Chromium 확인: 통과 배지 "겹침 12 · 다툼 5 · 다툼 아닌 겹침 7 · 아무 다툼에도 없는 주체 4 · 시나리오 3/3", 7섹션 전부 채움·빈칸 0, 이분 그래프 SVG 2개 — 바닥난 겨울 노드 9·선 13 / 가득한 겨울 노드 8·선 9(대상 하나가 없다), 표 11개·55행, 게이지 5행, diff 뷰 3개, 시나리오 항목 45, 붉은 행 0 — 다툼 행은 어긋남이 아니므로 노란 행으로 선다, 페이지 요청 4xx 0 — 콘솔의 404 는 dev 서버 `/favicon.ico` 하나뿐이다) |
| D5-d 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 27항 · 실패 6항 · 경계 12항) 전부 통과, Lab `/d5` 화면 7요소 채움, 증거 D5.json 생성. **공용 렌더러 `graph.ts` 를 D5 소스 명부에 넣었다** — 열린 이슈 "공용 렌더러 해시 공백" 을 절반 갚는다(이제 그 파일을 고치면 D5 증거가 낡는다. `gauge.ts` 는 남았다). R5 는 이미 선등록돼 있어 "착수 가능" 이 그대로 R5 를 계산한다 |
| R5-a 기억과 지목 | DONE | `node --test` — R4 가 비워 둔 두 자리 중 첫 칸을 갚는다. 새로 정하는 것은 두 줄뿐이다: **다시 볼 수 없게 된 믿음이 기억이다**(목록조차 R5 가 만들지 않는다 — R4-c `staleBeliefs` 가 이미 세어 두었고 여기서는 이름이 바뀌는 자리를 판정할 뿐이다) · **겪은 자만 누구인지 안다**(제 자리가 바뀐 자는 사건의 결과가 제 장부에 남았으므로 안다 — 그것이 원한이 설 수 있는 유일한 뿌리다). **겪음이 주는 것은 지목뿐이다** — 무엇이 있었는지는 겪은 자에게도 여전히 짐작이고(후보는 R2 `atomsMoving`, 좁힘은 R4 문법) 확신은 좁힘을 넘지 못한다. **기억은 바래지 않는다** — 굳는 순간의 확신을 그대로 지고 손으로 고치면 `memory-drift` |
| R5-b 지닌 사이 | DONE | `node --test` — 세계에는 이미 사이가 적혀 있지만(O2 `relational` 일곱) 그것은 **세계의 장부**이고 사건(R1)만 그것을 바꾼다. R5 가 내는 것은 **그가 지닌 사이**이고, 둘이 갈릴 수 있다는 것이 이 계층의 전부다. 여기서 아무 숫자도 지어내지 않는다 — 축은 O2 가(여섯. 소속은 참거짓이라 밀 값이 아니다), 방향은 P0-b 걸림이, 크기는 R4 확신이, 폭은 O2 `numericRange` 가 정한다. 표를 그대로 읽으면 셋이 드러나는데 셋 다 R5 가 정한 것이 아니다: **원한은 쌓이기만 한다**(열여섯 중 그것을 치르는 원자가 하나도 없다) · **배신은 신뢰에 방향이 없다**(그 자리를 쓰면서 동시에 치른다) · **제거는 사이를 하나도 건드리지 않는다**. 그래서 맞은 자의 원한은 "맞았다" 가 아니라 **"빼앗김이었을 수도 있다"** 에서 온다 — 흐리게 알수록 흐리게 원망한다. 이 계층의 문장 하나: **지목 없는 기억은 아무도 밀지 못한다** |
| R5-c 말과 소문 | DONE | `node --test` — R4 는 벽을 하나 세웠다(`foreign-belief` — 근거는 제 지각뿐이다). **R5 는 그 벽을 허물지 않고 통로를 하나 낸다**: 말하면 `report` 통로의 흔적이 나고 듣는 자는 **제 귀로** 그것을 읽는다. 통로가 열리자 앞 계층이 정해 둔 것들이 저절로 따라온다 — 누가 들을 수 있는가는 R3 감지가, 무엇이 빠지는가는 R4 좁힘이, 한 입마다 옅어지는 것은 R4 의 상한이 `min` 을 지나며 정한다. **얼마나 힘줘 말하는가와 얼마나 좁게 말하는가는 다르다**(R4 가 세기와 좁힘을 따로 잰 그 자리다). R5 가 새로 정하는 것은 한 줄이다 — **말에는 지목이 실리고, 지목은 좁혀지지 않는다.** 기억장은 R2-c·R3-c·R4-c·D5-c 와 같은 모양이고 여기서도 사실 쪽이 절반이다: 지목 없는 기억(대부분이다) · 틀린 지목 · 아무도 듣지 못한 말 · 아무도 말하지 않은 기억. **거짓말은 아직 없다** — 기만은 E1 의 자리다 |
| **R5 기억과 관계** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R5.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r5` — **무슨 일이 있었는지는 모르는데 누구 탓인지는 안다.** R4 까지의 세계에서는 아무도 아무를 원망할 수 없었다 — 믿음에는 **누가 냈는지가 없기** 때문이다(`truth-copied` 가 막는다). 위에서 같은 사건 앞에 다섯을 세우면 **제 자리가 움직인 하나만 상대를 짚는다**: 04 가 낸 제거는 상단 11 의 몸을 0.8 → 0.2 로 깎았고, 11 은 그 자국을 보지 않았어도 제 장부가 움직인 것을 안다. **그런데 무엇으로 당했는지는 모른다** — 체력을 움직이는 원자가 열둘이고 제 손이 하나를 덜어 열하나가 남아 확신은 0.333 에서 멈춘다(겪었다고 확신 1 이 되지는 않는다). 손으로 낸 04 조차 겪은 것이 아니다. 가운데에서 **본 자들의 믿음 아홉 중 여섯이 굳어 기억이 되고 셋은 남는다** — 사라지지 않는 자국은 백 틱이 지나도 가서 보면 되기 때문이고, 굳은 여섯 어디에도 지목은 없다. 그리고 **11 이 마을에서 입을 열면 셋이 듣는다**(귀 없는 장막벌레는 못 듣는데, 그 사유는 R5 의 것이 아니라 R3 감지의 것이다). **같은 말을 듣고 사제만 죽임을 덜어 내는데 셋 다 04 를 짚는다** — 내용은 각자의 손이 좁히고 지목은 고를 것이 없는 한 마디라 좁혀지지 않는다. 그래서 그 사건 하나가 **네 이야기 두 종**이 되고(원문 §20), 아래에서 **04 를 원망하는 자가 하나에서 넷으로 는다 — 세계의 장부에는 그들 사이에 아무것도 적혀 있지 않은데.** 그리고 **한 입을 거친 말은 세기 0.333 으로 떨어져 아무에게도 닿지 않는다** — R5 가 정한 것이 아니라 R4 의 좁힘 상한이 `min` 을 지나며 옅어지고 R3 의 문턱 0.5 가 그것을 자르기 때문이다. R5 가 새로 정한 것은 셋뿐이고(다시 볼 수 없게 된 믿음이 기억이다 · 겪은 자만 누구인지 안다 · 말에는 지목이 실린다) 나머지는 전부 앞 계층에서 읽어 온다 — 축은 O2, 미는 방향은 P0-b(**원한은 쌓이기만 한다** — 열여섯 중 그것을 치르는 원자가 하나도 없다), 크기는 R4 확신. 설 수 없는 열다섯은 각자의 사유·경로로 거부되고, **틀린 지목은 위반이 아니다**(막으면 R5 는 한 입 늦은 전지가 된다) (Chromium 확인: 통과 배지 "기억 10(겪음 1 · 봄 6 · 들음 3) · 지목 4 · 이야기 4갈래 중 2종 · 못 들은 말 1 · 시나리오 3/3", 7섹션 전부 채움·빈칸 0, 표 15개·96행, 관계망 SVG 1개 — 노드 5·선 17, 게이지 4행, 타임라인 걸음 5, diff 뷰 1개, 시나리오 항목 53, 붉은 행 0 — 좁혀진 이야기·갈린 값은 어긋남이 아니므로 노란 행 9 로 선다, 페이지 요청 4xx 0 — 콘솔의 404 는 dev 서버 `/favicon.ico` 하나뿐이다) |
| R5-d 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 25항 · 실패 7항 · 경계 18항) 전부 통과, Lab `/r5` 화면 7요소 채움, 증거 R5.json 생성. **R6 를 PLANNED 로 선등록**해 "착수 가능" 목록이 다시 계산된다(#663 규칙 — 선등록 전에는 `verify` 가 "착수 가능 계산 실패 ✘ (빈 목록)" 로 exit 1 이었다). R2-d 에서 배운 순서를 그대로 따랐다 — 증거를 먼저 내고 계약을 VERIFIED 로 올린다 |
| R6-a 의도의 모양과 겨눔 | DONE | `node --test` — P5 가 남긴 한 줄("계획은 아직 요청이 아니다")을 갚는다. 형식을 새로 만들지 않는다: P0-c `ActionProposal` 이 §19 `WorldChangeRequest` 를 이미 갖고 있고 `fitAction` 이 판정도 한다. 하는 일은 셋뿐이다 — 막히지 않은 첫 걸음을 고르고(순서는 P5, 낼 수 있는가는 P4-a 가 이미 적어 두었다), 요청서로 옮기고, 상대가 필요한 원자인지 묻는다(**여섯뿐이고 그 목록도 P0-b `touches: 'between'` 이 갈라 두었다**). **바꿀 자리·치를 자리는 호출자가 준다** — 고를 근거가 없고 골라도 달라지는 것이 없기 때문이다(P0-b 는 자리의 패턴만 적었지 어느 물건인지도 사이 자리가 누구의 장부에 서는지도 적지 않았다). R6 가 더하는 것은 **누구를 겨누는가** 하나다 |
| R6-b 사이가 상대를 고른다 | DONE | `node --test` — R6 가 실제로 비어 있던 자리를 채운다. P1 은 자리와 자원을, P2 는 "낼 수 있는가" 를, P4 는 "무엇을 좇는가" 를 물었을 뿐 **누구에게** 는 어디에도 없었다(P4-b 조차 사이를 평균으로 읽었다 — P4 가 남긴 부채). 정하는 것은 둘이고 **둘 다 결과를 갖는다**: **겨눌 수 있는 것은 아는 상대뿐이다**(R5 지목이 짚은 자 + 세계가 사이를 적어 둔 자 — 둘 다 아니면 그는 없는 사람이다) · **누구를 겨누는가는 사이가 고른다**(등지는 손은 원한, 내미는 손은 신뢰 — 축은 P4-b 가 쓴 동의 축 그대로이고 값은 R5 가 잰 것 그대로다). 못박은 것 둘: 고를 상대가 없으면 걸음이 서지 못하고, **그 축의 값이 실제로 선 상대만 후보다** — 원한 0 인 자를 빼앗을 상대로 고르면 세계는 아는 사람이면 누구든 손대는 곳이 된다. 그래서 **밖에서 본 자는 빼앗을 상대가 없고 소문을 들으면 그 순간 겨눌 수 있게 된다** |
| R6-c 의도장과 고리 닫기 | DONE | `node --test` — R2-c·R3-c·R4-c·D5-c·R5-c 와 같은 모양으로 담고 감사가 위반과 사실을 가른다. 앞의 다섯에 없던 것이 하나 있다: **고리가 닫힌다.** 그리고 그 고리는 주장이 아니라 **검사**다 — `closeLoop` 이 의도를 R1 에 실제로 먹여 사건을 세우고(`mintEvent`) 세계에 얹고(`applyEvent`) 그 사건에서 R2 흔적이 나는지를 값으로 낸다. **R6 는 여기서 아무것도 새로 판정하지 않는다**(요청은 P0-c, 사건은 R1, 새는 자리는 R2-a) — 하는 일은 잇는 것뿐이고 이어지지 않으면 사유가 남는다(재고를 0 아래로 내리면 O2 관문이 막고 흔적은 나지 않는다). 사실 쪽도 절반이다: **아무 의도도 내지 못한 주체**(세계는 아무도 손대지 않는 틱에도 굴러간다) · **사건이 되고도 조용한 의도**(R2-c 가 이미 사실로 센 자리). 막는 것은 한 틱에 둘을 내는 것과 뿌리 없는 의도뿐이다 |
| **R6 행동 의도 생성** | **VERIFIED** ([증거](../app/packages/contracts/evidence/R6.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/r6` — **원한이 손이 되고, 그래서 고리가 닫힌다.** R5 는 넷을 04 앞에 세웠지만 **원망은 아직 아무것도 하지 않았다.** 위에서 같은 다섯을 놓고 소문 전후를 나란히 보면 **말을 듣기 전 목격자 셋에게는 아는 상대가 하나도 없다** — 밖에서 자국만 본 자에게는 지목이 없고(R4 가 `actorId` 를 싣지 않는다) 세계도 그들 사이를 적어 두지 않았다. 들은 뒤에는 셋 다 04 를 안다: **말 한 마디가 세 손을 움직였다.** 가운데에서 **원망과 손이 갈린다** — 넷이 04 를 원망하는데 빼앗는 손이 서는 것은 **둘뿐**이다. 상단 둘(겪은 11 과 목격자 상단)은 P2 가 "다음 겨울에 문이 닫히기 때문" 이라고 그 손을 닫아 두었다. **원망한다고 아무 손이나 나가지 않는다.** 그 아래에서 **축이 갈리면 상대가 갈린다**: 04 는 원한이 선 상대가 없어 등지는 손으로는 아무도 겨누지 못하는데 내미는 손으로는 마을을 겨눈다(세계가 그 신뢰 0.8 을 적어 두었다). 나머지 넷은 정확히 반대다 — **원망할 사람은 있는데 손 내밀 사람이 없다.** 그리고 마지막에 **고리가 닫힌다**: 선 의도 둘이 R1 에서 사건이 되고 R2 가 흔적 여섯을 세 통로(자국·냄새·보고)로 내며 원장에 칸이 둘 는다. 흔적은 전부 제 원인 사건을 대므로 **다시 읽힐 수 있다** — 결핍 → 목적 → 계획 → **의도** → 사건 → 흔적 → 지각 → 믿음 → 기억·사이 → **다시 의도**. 그 고리는 주장이 아니라 검사다(`closeLoop` 이 실제로 먹여 본다). R6 가 새로 정한 것은 둘뿐이고(겨눌 수 있는 것은 아는 상대뿐이다 · 누구를 겨누는가는 사이가 고른다) 나머지는 전부 앞 계층에서 온다 — 상대가 필요한 원자 여섯은 P0-b `touches`, 축은 P0-b `consent`, 값은 R5, 낼 수 있는 손은 P2, 요청 관문은 P0-c, 사건은 R1, 흔적은 R2. 설 수 없는 아홉은 각자의 사유·자리로 거부된다 (Chromium 확인: 통과 배지 "의도 2(겨눔 2) · 사건 2 · 흔적 6 · 소문이 움직인 손 3 · 아무것도 못 낸 주체 4 · 시나리오 3/3", 7섹션 전부 채움·빈칸 0, 표 11개·41행, 게이지 6행, 타임라인 걸음 4, diff 뷰 1개, 시나리오 항목 43, 붉은 행 0 — 못 선 손·갈린 축은 어긋남이 아니므로 노란 행 7 로 선다, 페이지 요청 4xx 0 — 콘솔의 404 는 dev 서버 `/favicon.ico` 하나뿐이다) |
| R6-d 눈 검증 | DONE | `node --test` + `npm run verify` — 시나리오 3종(정상 25항 · 실패 6항 · 경계 14항) 전부 통과, Lab `/r6` 화면 7요소 채움, 증거 R6.json 생성. **E0 를 PLANNED 로 선등록**해 "착수 가능" 목록이 다시 계산된다(#663 규칙). 장면을 세우다 둘을 배웠다: **고리는 R5 장면의 세계 위에서 돌려야 한다**(R1 원장에는 목격자 셋이 서 있지 않아 R2-b 가 흔적 낼 자리를 못 찾는다) · **한 틱에 두 칸을 담을 수 없다**(R0 이 같은 틱을 막으므로 넷이 차례로 손을 낸다) |

## 단계 게이트 (닫힘)

**단계 3 (M3 충돌하는 주체) 닫힘** — R0·R1·R2·R3·R4·D5·R5·R6 여덟이 전부 VERIFIED 이고
레지스트리가 `… → D4 → D5 → P0 → R0 → P1 → R1 → P2 → P3 → P4 → P5 → R2 → R3 → R4 → R5 → R6`
으로 등록하며, 착수 가능 목록은 이제 **E0** 를 계산한다. `npm run verify` 31/31 exit 0.
대표 장면("두 인간이 음식 하나를 원한다")은 아직 서지 않았다 — R0 이 세운 것은 그 장면이
적힐 **자리**(세계의 열), R1 이 세운 것은 그 자리를 바꾸는 **유일한 통로**(사건), R2 가 세운 것은
그 통로가 **밖에서 보이는 표면**(흔적)이며, R3 이 세운 것은 **그 표면을 누가 읽는가**이고,
R4 가 세운 것은 **그것을 무엇으로 읽는가**이고, R5 가 세운 것은 **그 읽음이 누구를 향하는가**이며,
R6 가 세운 것은 **그 향함이 어떻게 손이 되는가**다 — 그리고 그 손이 다시 사건과 흔적을 내면서
**고리가 닫힌다.**
둘이 다투려면 서로를 봐야 하는데, R3 까지 오면 그 "봄" 이 주체마다 갈리고(같은 협곡에 선
사냥꾼과 장막벌레가 다른 것을 읽고 마을의 조직은 아무것도 모른다), R4 까지 오면 **같은 것을 본
둘이 다른 세계에 산다**(같은 자국을 사제는 죽임이 아닌 것으로 읽고, 그래서 틀린다). 남은 것은
다투는 자리 자체(D5·E0)와 그 오해가 행동이 되는 통로(R6)다.
**R5 까지 오면 그 오해가 남을 향한다** — 겪은 자 하나가 짚은 이름이 말을 타고 셋에게 건너가고,
세계의 장부에는 아무것도 적히지 않은 채 넷이 한 사람을 원망하게 된다. **그리고 R6 에서 그
원망이 손이 된다** — 넷 중 둘의 손이 04 를 겨눈 사건이 되고, 그 사건이 흔적 여섯을 내며 다시
읽힐 수 있게 된다. 여기서 단계 3 이 한 바퀴를 돈다: 결핍 → 목적 → 계획 → 의도 → 사건 → 흔적
→ 지각 → 믿음 → 기억·사이 → **다시 의도**. 남은 것은 **둘이 서로를 알아보고 다투는 자리**를
상황으로 묶는 일(E0)과 결과를 확정하는 일(E3)이다.
**대표 장면("두 인간이 음식 하나를 원한다")이 D5 로 섰다** — `/lab/d5` 에서 창고가 비어 가는 걸음을
따라가면 재고 10·6·4 에서는 다툼이 아니던 것이 **재고 2 에서 다툼으로 선다**(넷의 요구 합 12 이
세계에 있는 것을 넘어서는 칸). 다만 **둘이 서로를 알아보고 다투는 장면**은 아직이다 — D5 는
"이 대상 앞에 이들이 함께 서 있다" 까지이고, 누가 누구와 싸우는지는 서로를 봐야 알며(R3·R4),
상황으로 묶는 것은 E0, 결과를 확정하는 것은 E3 다.
~~R0 착수 전 조건 (단계 2 리뷰 §3-1, #673)~~ — **해소(2026-08-03).** P3 Chromium 눈검증을
실측으로 수행해 단계 2 표의 P3 행에 기록했다. R0 은 그 게이트가 빈 뒤에 착수했다.
