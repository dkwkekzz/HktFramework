# 진행 기록 — 단계 1 (M1 의존하는 주체)

> STATE.md 구현 현황 표에서 분리한 **완료 작업 상세** (CLAUDE.md·WORKFLOW §8 규칙).
> 이 단계는 닫혔다 — 리뷰: [reviews/stage1-review.md](../reviews/stage1-review.md).
> 현재 상태·TODO 는 [STATE.md](../STATE.md) 를 본다. 완료 판정의 원본은 언제나 증거 파일
> (`app/packages/contracts/evidence/`)이다 — 이 문서는 사람이 읽는 확인 장면의 기록일 뿐이다.

## 모듈별 확인 장면

| 모듈 | 상태 | 확인 장면 |
|---|---|---|
| S0-a 주체 경계와 그래프 자리 | DONE | `node --test` — 사람은 몸, 조직은 구성원, 국가는 영역+구성원, 신은 앵커로 세계에 걸린다. 결함 있는 경계는 경계로 세지 않으므로 요구 경계도 함께 빈다. 손으로 지은 그래프 ID 는 무엇이 와야 하는지와 함께 거부된다 |
| S0-b 감지 프로필 | DONE | `node --test` — 같은 장막의 빛을 사냥꾼은 300m 안에서 보고 국가는 통로가 없어 못 본다. 문턱 0(전지한 감각)과 몸 없는 주체의 눈이 각각의 사유로 거부된다 |
| S0-c 의존·유지 자리 | DONE | `node --test` — 허기 의존은 자기 경계 안에서 서고, 행상의 신뢰를 원하는 것은 경계 밖이어도 선다. 자리 전체를 범위로 잡은 의존은 "결코 벗어나지 않는 조건" 으로 거부된다 |
| S0-d 주체 골격 | DONE | `node --test` — 개체 ID·그래프 4종이 종+이름표에서 갈라져 나오고, 공리를 어긴 능력을 붙이거나 종이 열지 않은 자리로 무너진다고 적으면 거부된다 |
| S0-e 5질문 검사기 | DONE | `node --test` — 주체 5종 × 질문 5개 격자가 다 차고, 신원·경계·종이 무너진 주체는 다섯 전부가 막힌다 |
| **S0 공통 주체 모델** | **VERIFIED** ([증거](../app/packages/contracts/evidence/S0.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/s0` — 주체 종류별 최소 경계표와 통로 6종 성격표가 펼쳐지고, 붉은 장막에 선 다섯(사냥꾼·장막벌레 군집·채집 길드·협곡을 낀 나라·붉은 장막의 어미)이 각자 다른 경계·감각으로 선다. 둥지에서 장막이 걷힐 때 그 빛을 보는 것은 사냥꾼 하나뿐이고 나머지 넷은 각자의 이유로 못 본다. 그런데도 5종 × 5질문 격자 스물다섯 칸이 전부 차고, 결함 주체 13종은 각자의 사유·자리와 함께 어느 질문이 막혔는지까지 나온다 (Chromium 확인: 빈 섹션 0, 콘솔 오류 없음) |
| S1-a 신체 원형 | DONE | `node --test` — 사람은 몸으로 조직은 몸 없이 서고, 눈이 빛과 흔적을·입과 사지가 아무것도 열지 않는다. 몸 없는 종이 생물 자리를 열거나 몸이 있는데 깎일 자리가 없으면 각각의 사유로 걸린다 |
| S1-b 종의 감각 | DONE | `node --test` — 종의 통로가 개체의 감각이 되고, 배수가 거리와 문턱을 반대로 흔든다. 눈 없이 빛을 보거나 귀로 빛을 보거나 보고에 기관을 적으면 각각의 사유로 걸린다 |
| S1-c 생애 | DONE | `node --test` — 단계 이름이 O2 `growthStage` 선택지 넷에서만 나오고, 같은 허기가 유체 20틱·성체 30틱·노체 40틱이 된다. 늙지 않는 몸과 늙는 나라가 각각의 사유로 걸린다 |
| S1-d 기본 의존 | DONE | `node --test` — 종의 템플릿에서 찍어 낸 의존이 누구의 자리인지·몇 틱 뒤인지를 얻어 S0 검사를 그대로 지난다. 종이 열지 않은 자리는 원천에서 막힌다 |
| S1-e 종 원형 조립 | DONE | `node --test` — 원형이 여전히 O1 Rule·O0 종 정의이고, 씨앗 하나에서 감각·의존·능력이 함께 나온다. 평생 열리지 않는 능력과 인용 없이 열리는 능력이 각각 걸린다 |
| **S1 종 원형** | **VERIFIED** ([증거](../app/packages/contracts/evidence/S1.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/s1` — 기관 6종이 여는 통로표가 펼쳐지고, 붉은 장막의 종 다섯이 각자의 몸·감각·생애·의존으로 선다 (사냥꾼과 장막벌레만 몸이 있고 늙는다). 가운데 대조표에서 **같은 사냥꾼 종이 세 개의 개체가 된다** — 유체는 20틱 만에 굶어 무너지고 빛을 210m 까지만 보며 능력 하나만 열고, 노체는 40틱을 버티는 대신 180m 로 좁아진다. 결함 종 14종은 각자의 사유·자리·문장으로 거부된다 (Chromium 확인: 4xx 없음, 콘솔 오류 없음 — 브라우저 기본 favicon 요청만 404) |
| S2-a 해석 | DONE | `node --test` — 같은 붉은 빛을 사냥 문화는 "지나간 자국", 제의 문화는 "어미의 숨" 으로 읽고 그 읽기가 O1 Claim 이 된다. 종이 열지 않은 통로를 읽는 문화는 거부된다 |
| S2-b 가치 템플릿 | DONE | `node --test` — 문화의 유지 템플릿에서 찍어 낸 값이 S0 관문을 그대로 지나고, 경계 밖(협곡의 재고)도 원할 수 있다. 종이 무너지는 자리(허기)를 문화가 다시 밀면 걸린다 |
| S2-c 역할 | DONE | `node --test` — 사제는 장막 부름을 얻고 독 감별을 잃는다. 종이 이미 여는 능력을 또 여는 입문 의례, 아무도 열지 않은 것을 금하는 금기, 공리를 어긴 능력이 각각의 사유로 걸린다 |
| S2-d 문화 원형 조립 | DONE | `node --test` — 종 씨앗 위에 문화가 겹쳐 개체 씨앗이 되고, 감각·의존은 그대로인 채 읽기·원함·능력만 갈린다. 금기가 능력을 전부 막으면 `total-taboo` 로 막힌다 |
| **S2 문화·역할 원형** | **VERIFIED** ([증거](../app/packages/contracts/evidence/S2.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/s2` — 개체의 다섯 자리가 각각 종에서 오는지 문화에서 오는지가 표로 갈리고, 붉은 장막 세계의 문화 셋(자국을 쫓는 자들·어미를 섬기는 자들·고개를 넘는 상단)이 자리 여섯과 함께 선다. 가운데 대조표에서 **같은 사냥꾼 종의 셋이 같은 붉은 빛 앞에서 갈린다** — 하나는 "장막벌레가 방금 지나갔다" 로 읽고 쫓고, 하나는 "어미가 숨을 내쉬었다" 로 읽고 물러나고, 하나는 "고개가 막혔다" 로 읽고 값을 센다. 눈도 무너질 조건도 셋이 똑같은데(감각은 종의 것) 원하는 자리와 할 수 있는 것은 전부 다르다 — 사제만 장막을 부르고 몰이꾼은 전언 새김을 잃는다. 결함 문화 15종은 각자의 사유·자리와 함께 어느 문화·어느 역할에서 걸렸는지까지 나온다 (Chromium 확인: 빈 섹션 0, 4xx 없음, 콘솔 오류 없음 — 브라우저 기본 favicon 요청만 404) |
| S3-a 과거 사건 | DONE | `node --test` — 이력이 O1 Event 가 되고 남긴 값이 O1 State 가 된다. 흔적 없는 과거·아직 오지 않은 일·시간을 거스른 이력·같은 자리의 다툼이 각각의 사유로 걸린다 |
| S3-b 성격 | DONE | `node --test` — 겁이 많으면 같은 허기가 더 급해지고 같은 빛을 덜 확신한다. 없는 자리를 흔들거나 배수가 양끝을 넘거나 두 성격이 한 자리를 다투면 걸린다 |
| S3-c 개체 조립 | DONE | `node --test` — 다섯 층이 한 개체가 되고 값마다 유래(종·문화·자리·이력·성격·개체)가 붙는다. 실재하는 능력이어도 유래를 못 대면 `orphan-value` 로 막힌다 |
| **S3 개별 주체 생성** | **VERIFIED** ([증거](../app/packages/contracts/evidence/S3.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/s3` — 다섯 층이 각각 무엇을 주고 무엇을 막는지가 표로 서고, 붉은 장막 세계의 개체 넷(몰이꾼 셋 + 사제 하나)이 각자의 이력·성격과 함께 선다. 가운데 대조표에서 **같은 종·같은 문화·같은 자리의 몰이꾼 셋이 갈린다** — 눈도 무너질 자리도 할 수 있는 것도 셋이 똑같은데, 허기의 급함이 1 / 0.56 / 0.8 이고 같은 붉은 빛에 대한 확신이 0.42 / 0.7 / 0.7 이다. 그 갈림을 만든 것은 지고 온 것(빚 40 · 원한 0.6 · 없음)과 타고난 기울기(겁 · 욕심 · 없음)뿐이고, **값 38개가 하나도 빠짐없이 유래를 댄다**. 결함 개체 13종은 각자의 사유·자리로 거부되고, 마지막 하나는 세계에 실재하고 공리도 지난 능력이 유래가 없다는 이유만으로 막힌다 (Chromium 확인: 빈 섹션 0, 4xx 없음, 콘솔 오류 없음 — 브라우저 기본 favicon 요청만 404) |
| D0-a 대상 11종 확정 | DONE | `node --test` — 원문 D1 의 노드 종류 9개가 확정 11종으로 남김없이 해소되고, `state` 하나가 환경·신체로 갈린다. 해소를 지우거나 없는 종으로 보내거나 근거를 빼면 각각의 사유로 걸린다 |
| D0-b 종별 세계 걸림 | DONE | `node --test` — 열한 종이 O1 12타입·O2 9영역에 걸리고 9영역이 하나도 남김없이 읽힌다. 같은 고개 통행법 하나가 제도·규칙·의례 셋에 받아들여지고, 자원이라 적고 장소를 가리키면 "공간으로 걸 수 있다" 까지 함께 나온다 |
| **D0 의존 대상 타입** | **VERIFIED** ([증거](../app/packages/contracts/evidence/D0.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/d0` — 원문 두 목록(D0 11 · D1 9)이 11종으로 좁혀지는 대조표가 펼쳐지고, 열한 종이 각각 무엇에 기대고 대상이 세계의 무엇이며 어디서 읽고 쓰면 주는지·갈아탈 수 있는지를 한 표로 편다. 9영역이 하나도 남김없이 어느 종엔가 읽히고, 시간만 읽을 자리가 없어 V1 틱을 읽는다. 가운데 대조표에서 **몰이꾼 04 의 굶주림 하나 앞에 열 가지가 놓이고 각자 다른 종으로 선다** — 말린 고기(자원)·협곡(공간)·바닥의 온기(환경)·제 몸의 허기(신체)·어미(주체)·행상의 신뢰(관계)·마비독을 아는 것(정보), 그리고 **고개 통행법 하나가 제도·규칙·의례 셋으로 갈린다**(종을 정하는 것은 대상이 아니라 기대는 방식이다). 지나간 사건 하나는 어느 종도 받지 않는다. 결함 걸림 9종 + 어긋난 선언 9종은 각자의 사유·경로와 함께, 무엇으로 적어야 하는지까지 나온다 (Chromium 확인: 빈 섹션 0, 붉은 행 0, 4xx 없음 — 브라우저 기본 favicon 요청만 404) |
| D1-a 의존 노드 | DONE | `node --test` — 노드의 조건이 그 종이 읽는 자리(D0 `readDomains`)여야 서고, 자원 노드가 남의 신뢰를 조건으로 걸면 "다른 종의 의존이다" 로 거부된다. 시간 종만 틱 조건을 갖고, 대상 검사는 D0 `fitTarget` 을 그대로 지난다 |
| D1-b 의존 간선 | DONE | `node --test` — 관계 7종이 종마다 갈리고(허락은 제도·규칙, 알림은 정보, 되풀이는 의례·관계·시간), 줄지 않는 것을 소모하거나 그 대상이어야 하는 종을 대체 가능하다 적으면 거부된다. 끊겨도 세계에 아무것도 남지 않는 간선도 막힌다 |
| D1-c 그래프 조립 | DONE | `node --test` — 뿌리에서 닿지 않는 노드·맴도는 의존·다른 주체의 노드가 각각의 사유로 걸리고, 노드·간선을 뒤집어 적어도 같은 그래프 해시가 나온다 |
| D1-d 공용 그래프 뷰 | DONE | `node --test` — 뿌리에서의 깊이가 열, 선언 순서가 행. 100번 그려도 같은 배치이고, 닿지 않는 노드는 숨지 않고 맨 오른쪽에 선다 (WORKFLOW §6-2 새 렌더러 별도 카드) |
| **D1 의존 그래프 스키마** | **VERIFIED** ([증거](../app/packages/contracts/evidence/D1.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/d1` — **화면 가운데에 그래프가 그려진다**(공용 렌더러 ② 첫 사용). 몰이꾼 04 의 굶주림 하나에서 일곱 노드가 여섯 관계로 뻗는다: 주린 몸 ─consumes→ 겨울 식량 ─requires→ 사냥터 ─authorized_by→ 고개 통행권 / ─sustained_by→ 장막 주기, 그리고 겨울 식량 ─informed_by→ 마비독 감별 · ─produced_by→ 행상의 신뢰. 뿌리는 굵은 테두리 하나뿐이고 **한 사람의 굶주림이 일곱 종을 건드린다**. 관계 7종의 성격표와 노드·간선 표(강도·급함·대체·끊김 시한·그때 세계에 남는 것)가 아래에 펴지고, 결함 그래프 12종은 각자의 사유·경로로 거부된다 — 맴도는 의존은 그림으로 한 번 더 보인다. 순서를 뒤집어도 같은 해시 (Chromium 확인: 빈 섹션 0, 붉은 행 0, SVG 노드 7·간선 6 실측, 4xx 없음 — 브라우저 기본 favicon 요청만 404) |
| D2-a 뿌리 만들기 | DONE | `node --test` — 뿌리의 자리·범위·주인이 종이 말한 무너짐(S1 NeedTemplate)에서 그대로 나오고, 설계도에는 그것을 고쳐 적을 자리가 아예 없다. 뿌리 없는 무너짐·무너지지 않는 자리의 뿌리·같은 자리의 두 뿌리가 각각의 사유로 걸린다 |
| D2-b 채움 갈래 | DONE | `node --test` — 종의 빈칸(자기·몸·바깥)이 개체의 ID 로 채워지고 시한이 단계의 대사로 나뉜다(대사 2 면 30틱이 15틱). 없는 것을 채우거나 아무것도 채우지 않거나 뿌리의 시한을 두 번 적으면 각각의 사유로 걸린다 |
| D2-c 생존·번식 무단절 | DONE | `node --test` — 채울 것 없는 무너짐이 "15틱 뒤에 무너지는데 채울 길이 없다" 로 지목되고, 늙는 종이 대를 밝히지 않으면 한 세대로 끝난다고 걸린다. 늙지 않는 종이 낳는다고 적어도 걸린다. 그래프 자체의 사유는 D1 관문에서 그대로 안고 온다 |
| **D2 종 기본 의존 그래프 생성** | **VERIFIED** ([증거](../app/packages/contracts/evidence/D2.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/d2` — **종 다섯이 각자의 기본 의존 그래프를 물려주는 그림 다섯이 펼쳐진다**(손으로 적은 그래프가 아니라 종에서 찍어 낸 것이다). 사냥꾼은 굶는 것·다치는 것·대가 끊기는 것이 세 뿌리로 갈려 아홉 노드로 뻗고, 장막벌레는 군집 하나가 지금의 생존이자 다음 세대다(생존·대). 몸 없는 셋은 대를 잇지 않는다 — 그들은 낳는 것이 아니라 세워지고 흩어진다. 가운데 대조표에서 **같은 종의 두 개체가 같은 모양 해시를 받고 그래프 해시만 갈린다**(이름도 사슬도 수치도 같고 자리의 주인만 다르다). 그 아래에서 **같은 설계도가 단계마다 다른 시한을 받는다** — 주린 몸이 유체 20 · 성체 30 · 노체 40틱, 대 이을 몸이 267 · 400 · 533틱, 사슬 안쪽의 사냥터도 4 · 6 · 8틱으로 함께 흔들린다. 무단절 판정표 일곱 줄이 전부 "끊기지 않는다" 로 서고, 결함 설계도 15종은 각자의 사유·경로로 거부된다 — 채울 것 없는 무너짐은 그림에서 붉은 파선으로 한 번 더 보인다 (Chromium 확인: 빈 섹션 0, 붉은 행 0, SVG 6개의 노드·간선 실측(9·7 / 4·4 / 3·2 / 3·2 / 3·2 / 결함 4·2), 4xx 없음 — 브라우저 기본 favicon 요청만 404) |
| D3-a 개인화 | DONE | `node --test` — 뿌리 간선의 급함·시한이 종의 템플릿이 아니라 개체의 실제 Need 에서 다시 읽힌다(겁 많은 개체는 0.8 → 1). 노드와 사슬은 하나도 변하지 않고, 대 잇는 뿌리는 개체의 무너짐이 아니므로 건너뛴다. 남의 그래프·표본 그래프는 `foreign-base` 로 막힌다 |
| D3-b 변형 문법 | DONE | `node --test` — 개체가 할 수 있는 일은 더함·약화·끊음 셋뿐이고 노드를 지우는 편집은 문법에 없다. 다섯 유래(능력·문화·자리·이력·성격)를 개체가 실제로 가져야 하며, 없는 기댐·줄지 않는 약화·아무것도 안 바꾸는 변형이 각각 걸린다 |
| D3-c 전환 검사 | DONE | `node --test` — 덜어 낸 무게와 세운 무게가 장부로 남는다. 줄이면서 세우지 않으면 `free-conversion`, 더 가벼우면 `light-conversion`, 능력이 유래인데 새 의존이 그 능력의 대가 자리(psychic.energy)에 안 걸리면 `costless-conversion`. 변형 뒤 뿌리가 비면 `severed-need` |
| D3-d 그래프 diff 뷰 | DONE | `node --test` — 공용 그래프 뷰가 더함=녹·끊김=적·흔들림=노랑을 받는다. 끊긴 간선은 개인 그래프에 없으므로 기본에서 데려와 파선으로 그린다 (WORKFLOW §6-2 새 렌더러 별도 카드). D1 `diffGraphs` 에 `changedEdges` 를 더해 "수치만 달라진 간선" 부채를 갚는다 |
| **D3 개인·문화·능력에 의한 의존 변형** | **VERIFIED** ([증거](../app/packages/contracts/evidence/D3.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/d3` — **같은 종·같은 기본 그래프를 받은 넷이 네 개의 다른 그래프로 갈라진다.** 그림마다 갈림이 색으로 실린다(더해진 노드=녹, 흔들린 기댐=노랑). 같은 허기가 04 는 1, 11 은 0.56, 23·31 은 0.8 로 급하고 — 그것을 흔든 것은 성격이며 시한 30틱은 넷이 같다(성격은 급함을 흔들 뿐 몸이 버티는 시간은 흔들지 못한다). 갈래는 유래별로 붙는다: 빚진 04 에게만 마을의 참을성이, 몰이꾼 셋에게만 등을 맡길 짝이, 어미를 섬기는 31 에게만 제사가 붙는다. 가운데 전환 장부에서 **사제의 식량 기댐이 0.95 → 0.45 로 줄고 의념의 샘 0.6 이 새로 서며, 그 의념은 붉은 장막이 치르는 대가의 자리(psychic.energy)에 정확히 걸린다** — 그런데도 사제는 여전히 굶는다(뿌리는 종의 것이라 개체가 지우지 못한다). 결함 변형 10종은 각자의 사유로 거부되고, 공짜로 벗어나려는 그래프는 "노랑만 있고 녹이 없는" 그림으로 한 번 더 보인다 (Chromium 확인: 빈 섹션 0, 붉은 행 0, SVG 5개의 노드·갈림 실측(11/+2, 10/+1, 10/+1, 11/+2, 결함 9/+0), 4xx·콘솔 오류 없음) |
| D4-a 세계 스냅샷 | DONE | `node --test` — 개체가 지고 온 값(S3 residue)과 세계의 자리가 O2 조립 관문을 지나 트리 하나로 모인다. 스키마를 어긴 값은 세계에 들어가지 않고, 같은 자리의 두 값은 O0 `state-exclusion` 으로 막힌다. 시간만 흘러도 값은 그대로다 — 값을 바꾸는 것은 사건(R1)의 몫이다 |
| D4-b 결핍 읽기 | DONE | `node --test` — 조건과 실제 값의 거리가 0~1 로 읽힌다(재고 1 → 0.5, 재고 0 → 1). 딱 그 값인 자리는 절반이 없고, 아무도 안 적은 자리는 완전히 비어 있으며, 시계 조건은 창을 놓친 직후가 가장 크다 |
| D4-c 압력과 5단계 | DONE | `node --test` — `Strength × Deficit × Urgency × FailureRisk` 의 네 자리가 보고에 그대로 남는다. 시한이 짧을수록 처음부터 위험하고(체력 1틱 → 0.5, 허기 30틱 → 0.032), 결핍이 이어질수록 오른다. 결핍 0 이면 압력 0 이고, 그때만 충족이다 |
| D4-d 게이지·추이 렌더러 | DONE | `node --test` — 공용 렌더러 ③ (WORKFLOW §6-2 새 렌더러 별도 카드). 가로 막대 게이지와 세로 막대 추이가 5단계 색(초록→노랑→주황→빨강)으로 그려지고, 색과 이름은 부르는 쪽이 준다 |
| **D4 의존 충족도 평가** | **VERIFIED** ([증거](../app/packages/contracts/evidence/D4.json)) | `npm run dev -w @hkt/lab` → `http://localhost:5173/#/d4` — **세계가 처음으로 값을 갖는다.** 창고에 열 개가 있고 몰이꾼은 협곡에 있고 사제의 의념은 이백이다. 압력 게이지가 뿌리마다 서고, 창고가 넉넉한데도 판정은 성한 몸을 가리킨다 — 협곡에 있으니 겨울 움막(아랫마을)이 완전히 비었기 때문이다(한 몸은 한 곳에만 있다 · D5 가 볼 다툼). 가운데 추이 막대 열 개가 **0.00 → 0.95 로 한 번도 내려가지 않고 오르며 충족·충족·충족·충족·불안정·결핍·위기·위기·붕괴·붕괴** 로 색이 갈린다. 그리고 **같은 세계·같은 시각인데 넷의 굶주림이 다르다** — 0.31 / 0.17 / 0.25 / 0.12. 사제가 가장 덜 급한 까닭은 의념이 아직 차 있어서다(D3 의 전환이 여기서 값이 된다). 그래프 전체가 5단계로 칠해지고, 잴 수 없는 읽기 7종은 각자의 사유로 거부된다 (Chromium 확인: 빈 섹션 0, 붉은 행 0, 게이지 3+3+4줄·추이 10칸 실측, 4xx 없음 — 브라우저 기본 favicon 요청만 404) |
