; HktGameplay 플러그인 — 콘텐츠 redirector
;
; 모듈 리팩터(Design-HktTerrain-Module-Refactor.md PR-C)로 인한 클래스/구조체 경로 변경 fixup.
; 본 파일은 UE 가 플러그인 Config 디렉터리에서 자동 머지한다 (Engine.ini 레이어).

[CoreRedirects]
; UHktVoxelTerrainStyleSet → UHktTerrainStyleSet (HktVoxelTerrain → HktTerrain 모듈 이관 + 리네임)
+ClassRedirects=(OldName="/Script/HktVoxelTerrain.HktVoxelTerrainStyleSet",NewName="/Script/HktTerrain.HktTerrainStyleSet")
