# STATE — 살아있는 현재

> "지금 어디까지 왔고 다음은 무엇인가"의 **단일 진실 원천(SSOT)**.
> 큰 목표는 [CLAUDE.md](CLAUDE.md) · 작업 절차는 [hwr-task 스킬](../.claude/skills/hwr-task/SKILL.md) · 각 규칙의 상세는 `rules/rule_NNNN/rule_NNNN.md`.
>
> **구조 규칙**: 이 파일은 *고정 크기 대시보드*다. §1~3 은 규칙을 닫을 때마다 **덮어쓴다(rewrite)** — 누적 금지. 오직 §4 INDEX 만 **literal 1줄/규칙** append. 규칙의 발견·한계 전문은 STATE 가 아니라 `rule_NNNN.md`.
> 마커: ✅완료 🟡진행중 🔴예정(최우선) ⬜백로그

---

## 1. NOW — 지금

- **세계 로직(엔진)**: [`engine.js`](engine.js) `stepWorld(world, rules, params)` 가 보편 역학을 소유 — *연속*(관성 적분·`a=F/m`·토러스 랩·tick) + *이산*(공유 결합 병합 `reconcileMerges`, 이온 결합 전하 이동 `reconcileTransfers`). 규칙은 **힘을 누적하거나 사건을 표시**(`apply`)할 뿐. 뷰어·verify 가 같은 엔진을 import. 뷰어는 **모든 규칙 자동 탐색·항상 적용**, **3D 렌더**, 원소 개수 변동 자동 반영, 로드 대상은 **시나리오**.
- **닫힌 규칙**:
  - ✅ [rule_0001](rules/rule_0001/rule_0001.md) **관성** — 관성=변화 저항, 질량=그 크기(`a=F/m`)·힘 없으면 등속. 외부 힘 입구(`world.impulses`→`Δv=J/m`). `Σmv` 보존. verify 9개.
  - ✅ [rule_0002](rules/rule_0002/rule_0002.md) **결합** — 접촉+접근+부드러움(`vStick` 문턱) 시 결합. **전기음성도 `en`** 차이로 갈림: ΔEN 작음→**공유**(비탄성 병합, 하나의 질점), ΔEN 큼→**이온**(en 높은 쪽이 전자 빼앗음→+/− 이온, 병합 안 함). 보존 `Σm`·`Σmv`·`Σq`, KE→`bondEnergy`. `en` 없으면 전부 공유(하위 호환). verify 18개.
  - ✅ [rule_0003](rules/rule_0003/rule_0003.md) **전자기력** — *힘 규칙은 4대 기본 상호작용만 정의*(중력·전자기력·강력·약력). 이 규칙은 **전자기력** 하나이고 **전기·반발·마찰이 모두 여기서 창발**(Born–Landé): 쿨롱(전기, 부호별 밂/당김) + 전자구름 겹침 반발(단단함) + 접촉 소산(마찰, 접선 미끄럼까지). 전하 `q` 가 원천 — 중성은 EM 안 보임(공유 병합 무방해). 운동량 보존(마찰은 *상대*운동만 소산). verify 12개.
- **원칙**: 특별한 국소 힘을 author 하지 않는다 — force 규칙은 4대 기본 중 하나. 반발·마찰·전기는 전자기력에서 *창발*. 중력·강력·약력은 후속 force 규칙.
- **창발 확인**: H₂(en 같음)→공유 병합, NaCl(en 차이 큼)→이온 결합 후 전자기력에 묶인 이온쌍(평형 6.0). 마찰: 접선 속도를 준 이온쌍이 EM 소산으로 상대운동 소멸(3.0→0.0). **전기음성도가 결합 운명을 가르고 전자기력이 붙잡고(반발)·미끄럼을 멈춘다(마찰).**
- **다음**: rule_0004 — 껍질/원자가(고정 `en`·이온 1회 이동을 *창발*로). 설계부터(§2).

## 2. NEXT — 다음 한 조각 (창발 주기율표를 향한 로드맵)

> 한 번에 하나의 온전한 규칙. 각 규칙은 *보편 양/법칙*만 더하고, 검·물·주기율표는 그 위에서 *창발*한다(author 안 함). 주기율표는 입력이 아니라 *측정 출력*.

- 🔴 **rule_0004 껍질/원자가 (예정·최우선)** — 지금 `en`·전하는 *seed/단순*(이온 1회 이동, 다중 결합 안 됨). 결합가를 **불연속 껍질**로: 채우면 안정(비활성), 거의 찬·빈 것은 반응성. **다중 결합가·주기성**이 여기서 창발. en 이 *왜* 그 값인지도 껍질 구조에서.
- ⬜ **rule_0005 이온 격자/결정** — 다수 +/− 이온이 교대 배열로 안정한 결정 창발(현재 단일 NaCl 쌍 확인). 전기력 위에서 측정.
- ⬜ **rule_0006+ 조성 정체성·가역 분해·극성** — 조성→분자형, 결합 깨짐(역과정)→세부 균형, 공유 분자의 극성(내부 구조). 그 위에서 아이템·캐릭터 창발.
- ⬜ (전가) 결합의 **회전/내부 형상**(질점→강체).

## 3. OPEN — 열린 격차

| 마커 | 격차 | 상태 |
|---|---|---|
| 🔴 | 결합가가 *단순* | `en` 고정·이온 1회 이동·다중 결합 없음. rule_0004(껍질/원자가)가 주기성·다중 결합을 창발. |
| ⬜ | 이온 결정이 단일 쌍까지만 | 다수 이온의 교대 격자. rule_0005 에서 측정. |
| ⬜ | 주기율표가 아직 없음 | 비슷한 원소의 주기적 재발. rule_0004 껍질 채움에서 *창발*로 측정. |
| ⬜ | 합성 원소가 질점·결합 비가역·무극성 | 회전·분해(역과정)·분자 극성 없음. 후속 전가. |
| ⬜ | 창발 — 아이템·캐릭터가 규칙의 결과로 | 분자(H₂·NaCl)까지 창발. 규칙이 더 쌓인 뒤 상위 측정 |

## 4. INDEX — 규칙 인덱스 (1줄/규칙, append-only)

- **rule_0001** 관성 — 변화 저항=질량(`a=F/m`, 무거울수록 덜 변함)·힘 없으면 등속·저항/적분은 엔진(`engine.js`)·rule_0001 은 외부 힘 입구(`world.impulses`→`Δv=J/m`)·`Σmv` 보존·`ΔΣp=ΣJ` 닫힘·verify 9개 단언. [설계](rules/rule_0001/rule_0001.md)
- **rule_0002** 결합 — 접촉+접근+부드러움(`vStick` 문턱) 시 결합·**전기음성도 `en` 차이로 공유(병합)/이온(전자 이동) 갈림**(ΔEN<ionicThreshold→비탄성 병합 하나의 질점, ΔEN≥→en 높은 쪽이 전자 빼앗아 +/− 이온·병합 안 함)·규칙은 사건 표시(`pendingMerges`/`pendingTransfers`)·엔진이 실행·보존 `Σm`·`Σmv`·`Σq`(전자 이동만)·KE→`bondEnergy`·문턱으로 "닿는 게 다 뭉치진 않음"·`en` 없으면 전부 공유(하위 호환)·verify 18개 단언. [설계](rules/rule_0002/rule_0002.md)
- **rule_0003** 전자기력 — 힘 규칙은 4대 기본 상호작용만(여긴 전자기력 하나)·전기·반발·마찰이 모두 EM 에서 창발(쿨롱 Madelung + 전자구름 겹침 반발 Born + 접촉 소산 마찰)·반대 이온이 평형 거리 `r*=(kBorn/kCoulomb)^(1/(bornExp−2))` 에 안착→NaCl 이온쌍 창발·접선 미끄럼도 같은 EM 으로 감쇠(마찰 창발)·중성(`q=0`)은 EM 안 보여 공유 병합 무방해·운동량 보존(마찰은 상대운동만 소산)·순수 힘 규칙(`apply` 누적)·verify 12개 단언. [설계](rules/rule_0003/rule_0003.md)
