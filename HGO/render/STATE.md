# STATE — 살아있는 현재 (render 트랙)

> "지금 어느 렌즈까지 왔고 다음 렌즈는 무엇인가"의 **단일 진실 원천(SSOT)**.
> 렌즈 척추·입력 계약·author 금지선은 [RENDER.md](RENDER.md) · 시뮬 존재론은 [../SPINE.md](../SPINE.md) · 시뮬 현재는 [../atom/STATE.md](../atom/STATE.md).
>
> **구조 규칙(에이전트 효율)**: 고정 크기 대시보드. §1~5 는 렌즈 닫을 때마다 **덮어쓴다(rewrite)** — 누적 금지. 오직 §6 INDEX 만 **literal 1줄/렌즈** append. 마커: ✅해소 🟡부분 🔴열림 ⬜백로그 ⛔blocked(시뮬 선행).

---

## 1. NOW

- **닫힌 렌즈**: **렌즈-011 (L-element)** — *원소가 바뀌면 색이 바뀐다 — 핵 변환(붕괴·융합)이 화면에 보인다.* 지금까지 구의 색은 들뜸 x(밝기, L-glow)만 실었고 양성자 수 Z 는 *크기*(질량 Z+N)에만 썼다 — 그래서 Phase D 핵 장면(step-0031~38)은 β붕괴가 Z+N 보존·정지 무대라 화면에서 *정지 동일 점*으로만 보였다(바뀌는 건 Z 뿐인데 색 채널이 없었음). 이 렌즈가 Z(원소 정체성)를 *색조*로 읽는다: `measureZRange(atoms)`(측정 Z 범위)·`elementHue(Z,lo,hi)`(범위 정규화 연속 사상 — 저 Z 파랑 0.66→고 Z 빨강 0)·`hsvToRgb` → `drawAtom` 이 구 색조를 Z 로(밝기는 여전히 들뜸 x — **직교 채널**). **author 0**: 종류별 색 박기(`if Z==8 파랑`)가 아니라 측정 범위 정규화(λ→가시광 창과 동형). **단일 원소(범위 0)는 무채색**(시뮬에 없는 색 변이 author 0). ⇒ 탄소→질소→산소 사슬(0035)이 색 이동으로, 골짜기 수렴(0037·38)이 여러 색→한 색으로 보인다. (직전 **렌즈-010 L-Ebond**·**009 L-order**·**008 L-glow**·**007 L-trail**·**006 L-bond**·**005 L-recoil**: 눈 검증 ⏳.)
- **한 줄 상태**: 핵 장면(step-0031~38)에서 붕괴·융합이 *색 이동*으로 보인다 — 색조는 시뮬 측정값 Z 를 범위 정규화로 읽을 뿐(author 0). 알리바이 성립(**atom/ diff 0** — diff 는 `render/`(engine·validate) + `HGO/viewer.html`(범례 1줄)만).
- **다음**: §2 — atoms 채널의 Z 를 읽어 핵 변환 *결과*(원소 바뀜)를 색으로 표시 완료. 남은 렌즈: 변환 *순간* 섬광(L-nuc — 시뮬 이벤트 신호 대기·blocked)·온도색(L-T blocked)·도플러색·연결 성분 묶음색. 근사 author 금지.

---

## 2. NEXT

> 렌더는 시뮬과 직교 — 큰 호는 [RENDER.md](RENDER.md) §4 렌즈 로스터가 SSOT. 여기선 *다음 한 렌즈*만.

### 계약 감사 결과 (RENDER §2 채널 × 렌즈 — 부채 측정)

atoms.{rx,ry}→L-3d ✅ · {Z+N}→크기 ✅ · **{Z}→L-element ✅** · {x}→L-glow ✅ · **{e}(전하 Z−e)→미독 🔴(L-ion 즉시 가능)** · **{N}(동위원소)→크기로 부분만·구분 채널 미독 🔴(L-isotope 즉시 가능)** · {vx,vy}(온도)→L-T ⛔. photons.{lambda}→L-λ·{E}→밝기·{from,to}→L-line·{px,py}→L-recoil·{rx0,ry0}→L-trail 전부 ✅. bonds[i,j]→L-bond·[2]Eabs→L-Ebond·[3]order→L-order 전부 ✅. 이벤트(decay/fuse 순간)→L-nuc ⛔. **즉시 가능 부채 2: L-ion(전하)·L-isotope(N).** (이 감사가 Z 누락을 잡았어야 했다 — 절차로 편입.)

### 다음 렌즈 후보 (▶ 가능한 것만)

- **L-ion — ⬜ 후보(즉시 가능, ▶ 다음 권장)**: 전하 `Z−e`(이온화) → 구 테두리/명도(측정 범위 정규화). e·Z 늘 실림(미독 채널). 이온이 중성과 구분돼 보임(step-0001 이온·0013~14 이온화).
- **L-isotope — ⬜ 후보(즉시 가능)**: 중성자 수 `N`(동위원소) → 구 채도/명도. 지금은 크기(Z+N)로 부분만, 같은 Z 다른 N 구분 채널 없음(미독).
- **L-element — ✅ 렌즈-011 닫힘**: 원자 양성자 수 `Z`(원소) → 구 색조(측정 Z 범위 정규화·연속 사상). 핵 변환(decay·fuse, step-0031~38)이 Z 를 바꿔도 안 보이던 것을 색 이동으로 표시 — atoms 채널의 미사용 필드 Z 를 읽음. author 0(범위 정규화·단일 원소 무채색).
- **L-Ebond — ✅ 렌즈-010 닫힘**: 결합 E `sim.bonds[k][2]`=Eabs → 결합선 밝기 등급(maxE 정규화). bonds 채널 마지막 미사용 필드를 읽음 → atom-0034 까지 따라잡음. 후속 여지: 결합 길이(두 원자 측정 거리) → 굵기 — 즉시 가능(위치 읽기).
- **L-order — ✅ 렌즈-009 닫힘**: 결합 차수 `sim.bonds[k][3]` → 평행선 수(단일·이중·삼중). 게이트(step-0018 bondOrder) 충족돼 깸.
- **L-bond — ✅ 렌즈-006 닫힘**: 결합쌍 `sim.bonds`([i,j]) → 결합선(분자 윤곽). 후속 정제 여지: *분자(연결 성분)별 묶음 색·윤곽 채움*(연결 성분을 측정해 같은 분자를 한 덩이로) — 즉시 가능(연결 성분은 bonds 간선의 측정·읽기).
- **⛔ L-nuc (blocked)**: 붕괴·융합 *순간* 플래시/입자 줄기는 시뮬이 *이벤트 신호*(변환 타임스탬프·방출 입자)를 내보낸 뒤. 현재 scene 불리언 `decayActive`/`fuseActive`·per-atom `nuc`/`lep`(보존 장부, 빛 아님)만 → glow author 금지. 변환 *결과*(원소 바뀜)는 이제 **L-element(렌즈-011)가 Z→색으로 표시** — *순간*(섬광)만 신호 대기.
- **L-glow — ✅ 렌즈-008 닫힘**: 들뜸 준위 x → 등급 광원 밝기. 미사용 계약 채널(§2 "들뜸 x=밝기")을 불리언→등급으로 정제. 후속 여지: *방출 직전 충전 펄스*(준위 상승 애니메이션 — 시뮬이 준위 전이 타임스탬프를 내보낸 뒤).
- **L-trail — ✅ 렌즈-007 닫힘**: 출생→현재 실변위 자취. 후속 정제 여지: *도플러 색이동*(접근/후퇴 광자 λ 이동 — 시뮬이 광원 속도를 광자 λ 에 실은 뒤) · 시간 적분 잔광(누적 프레임 페이드).
- **L-recoil — ✅ 렌즈-005 닫힘**: 운동량(px,py) → 빛 줄기 글리프(정규화 방향 지시자). L-trail(007)이 실거리 자취로 보완.
- **L-line (정제) — ✅ 렌즈-004 닫힘**: 전이선별 빈도 집계 완료. 후속: 선폭(도플러/자연폭)·로그 세기축 — 시뮬이 그 양을 내보낸 뒤.
- **⛔ L-T (blocked)**: 흑체 온도색은 시뮬이 *열/온도*를 의미 있게 굴린 뒤(현재 자유 운동만 — 온도가 정적). 속도→색은 atom 뷰어가 이미 함; 렌더는 *방출색*에 집중.

> **게이트 규율**: 렌즈-009(L-order)·010(L-Ebond)이 bonds 채널의 미사용 필드(차수 bond[3]·E bond[2])를 모두 읽어 **render 가 atom-0034 까지 따라잡음** — 현재 렌더 가능한 모든 방출 채널 소진. *즉시 가능 잔여 칸*: 연결 성분 묶음색·결합 길이(이미 실린 위치/간선의 읽기). 남은 *시뮬 선행* 렌즈(L-T 온도, L-nuc 변환 이벤트, 도플러색)는 그 신호를 실을 때까지 비워 둔다 — 근사 author 금지(RENDER §3). 핵 변환(0031~34)의 *결과*는 위치·질량 채널로 이미 표시, *순간*만 대기.

---

## 3. OPEN GAPS — 열린 격차

| 마커 | 격차 | 상태 |
|---|---|---|
| ✅ | **빛이 안 보인다(시뮬은 방출하나 화면 0)** | 렌즈-001 해소 — λ→스펙트럼 색. 전문은 RENDER §4. |
| ✅ | **세계가 평면뿐(입체감 0)** | 렌즈-002 해소 — 평면 z=0 세계를 원근 3D 무대로(음영 구·궤도 카메라·바닥 격자). z author 0. 눈 검증 PASS. |
| ✅ | **카메라가 고정(자동 선회뿐, 시점 조절 불가)** | 렌즈-003 해소 — 마우스 인터랙티브 카메라(드래그 회전·휠 줌·우/Shift드래그 팬). `camState`+`attachControls`. |
| ✅ | **스펙트럼 띠가 유무만(세기 없음)** | 렌즈-004 해소 — `measureLines` 가 전이선별 빈도 집계, 밝기=빈도/maxCount(측정 정규화). 강한 전이=밝고 약한 전이=흐리게. |
| ✅ | **광자가 공간을 안 난다(방향·전파 없음)** | 렌즈-005 해소 — atom step-0003 recoil·0004 propagate 가 운동량(px,py)을 실음. 빛 줄기=운동량 방향, 길이∝\|p\|(측정). px=py=0 이면 줄기 0(author 0). |
| ✅ | **빛 줄기가 방향만(실제 난 거리 없음)** | 렌즈-007 해소 — 출생 `(rx0,ry0)`→현 위치 `(rx,ry)` 실변위(step-0004·0007)를 읽어 전파 트레일로. 길이=측정 실거리(정규화 글리프 아님). 변위 0이면 트레일 0(author 0). |
| ✅ | **들뜬 원자가 다 똑같이 밝다(준위 안 보임)** | 렌즈-008 해소 — 들뜸 x 를 불리언→*등급* 읽기(x=양자수 0..3, maxX 정규화). 더 들뜬 원자가 더 밝은 헤일로. x=0이면 글로우 0(author 0). 미사용 계약 채널 §2 "들뜸 x=밝기" 충족. |
| ✅ | **원자가 결합해도 화면엔 따로 논다(분자 안 보임)** | 렌즈-006 해소 — atom step-0010 bond·0012 valence 가 결합 목록 `sim.bonds`([i,j])을 실음. 결합한 두 원자를 청백 선으로 이음(분자 윤곽=연결 성분 읽기). 결합 0이면 선 0(author 0). |
| ✅ | **이중·삼중 결합이 단일과 똑같아 보인다(차수 안 보임)** | 렌즈-009 해소 — atom step-0018 bondOrder 가 `sim.bonds[k][3]=order`(공유 전자쌍 수)를 실음. 차수를 읽어 결합선을 그만큼 평행 복제(단일 1줄·이중 2줄·삼중 3줄, O=O·N≡N). 차수 없으면 단일(author 0). |
| ✅ | **결합 세기가 안 보인다(강·약 결합 똑같은 톤)** | 렌즈-010 해소 — atom step-0015 bondLocalE 가 `sim.bonds[k][2]=Eabs`(결합별 E)를 실음. E 를 읽어 결합선 밝기 등급화(maxE 정규화 — 강한 결합 밝게). E 없으면 밝기 0(author 0). bonds 채널 완독 → atom-0034 따라잡음. |
| ✅ | **핵 변환으로 원소가 바뀌어도 화면엔 똑같은 점(원소 안 보임)** | 렌즈-011 해소 — atom step-0031~38 decay·fuse 가 Z(원소)를 바꿈. Z 를 색조로 읽어(측정 범위 정규화·연속 사상) 원소가 바뀌면 색이 바뀐다(C→N→O 색 이동·골짜기 수렴=여러 색→한 색). 단일 원소면 무채색(author 0). |
| 🔴 | **이온이 중성과 똑같아 보인다(전하 Z−e 안 보임)** | 계약 감사서 드러난 미독 채널 — e·Z 는 atoms 에 늘 실리나 전하/이온화를 읽는 렌즈 없음. L-ion(즉시 가능): Z−e → 구 테두리/명도(측정 범위 정규화). ▶ 다음 권장. |
| 🔴 | **동위원소가 안 구분된다(중성자 N 부분만)** | 계약 감사서 드러난 미독 채널 — N 은 크기(Z+N)로 *부분*만, 같은 Z 다른 N 구분 채널 없음. L-isotope(즉시 가능): N → 채도/명도. |
| ⛔ | **핵 변환 *순간*이 안 보인다(붕괴·융합 플래시)** | L-nuc blocked — 시뮬이 변환 *이벤트 신호*(타임스탬프·방출 입자)를 안 냄(scene 불리언·nuc/lep 장부만). 변환 *결과*(원소 바뀜)는 이제 L-element(렌즈-011)가 Z→색으로 표시. *순간*(섬광)만 신호 대기. |
| ⬜ | **장면이 step-0002 하나뿐(발광)** | 빛 있는 장면이 하나. 결합 장면(0010~0012)은 노출되나 광자는 적음(0011 화학발광만). 시뮬이 새 발광/결합 장면을 더하면 자동 노출(viewer 가 SCENES 읽음). |

---

## 4. DURABLE CONSTRAINTS — 모든 렌즈가 지킬 정전 사실

- **시뮬 알리바이**: 렌더 diff 는 `render/`(+ 스킬)에만. `atom/` 비변경 → 골든 해시 비트 불변. 커밋 전 `git status` 확인.
- **author 0**: 색·이펙트는 시뮬 양의 함수일 뿐(RENDER §3). 종류별 색 박기·없는 분포 합성 금지.
- **정규화는 측정**: 화면 창(λ→nm·속도→hue)은 데이터에서 *잰* 범위로. 손으로 박은 임계 금지.
- **읽기 전용 로드**: atom 엔진은 `require`/`<script src=../atom/...>` 로 *읽기만*. 시뮬 객체(atoms·photons)에 렌더 필드 쓰지 않음(섬광 등은 렌더 전용 병렬 배열).
- **한 커밋 = 한 렌즈**: 더 떠올라도 다음 렌즈로 전가.

---

## 5. 소유 / 불가침 (SPINE §7)

- **소유**: `render/` 폴더 (`RENDER.md` · `STATE.md` · `engine/spectral.js` · `engine/render.js` · `engine/validate/smoke.js`). **render 는 viewer.html 을 두지 않는다** — 단일 뷰어는 트랙 밖 공유 셸 `HGO/viewer.html`.
- **불가침**: `atom/`(시뮬 코어·`STATE.md`·`steps/`·골든) · `../SPINE.md` · `../CLAUDE.md` — 읽기만(atom/ 진짜 diff 0). **공유 셸** `HGO/viewer.html` 은 render 그리기 위임 배선만 허용(RENDER §6). 스킬은 `.claude/skills/hgo-render-step/`.

---

## 6. INDEX — 렌즈 검증 현황 (유일하게 append, **literal 1줄/렌즈**)

> 규칙: 한 행 = `렌즈 | 읽은 양→번역 | 통과 + 핵심 수치 1개`. *문단 금지* — 전문은 이 문서 아님(렌더는 step 문서 없음, 렌즈는 가볍다).

렌즈-001 (L-λ) | 광자 lambda → 가시광 스펙트럼 색 + 측정 스펙트럼 띠 (render.js 그리기, 공용 뷰어 HGO/viewer.html 위임) | 스모크 PASS(광자 51·선 3·λ[1.333,20.571]·순서 보존) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-002 (L-3d) | 평면 세계(z=0) → 원근 3D 무대(음영 구·발광 빌보드·바닥 격자·궤도 카메라) — 위치=sim(rx,ry,0) 그대로, z author 0 | 스모크 PASS(중심→(280,280)·평면 depth>0·Δdepth 원근) · atom/ diff 0 · 눈 검증 PASS(사용자 브라우저 — "3d로 보이는건 성공")
렌즈-003 (L-cam) | 인터랙티브 카메라 — 드래그=궤도·휠=줌·우/Shift드래그=팬(`camState`+`attachControls`, 공용 뷰어 1줄 위임). 자동 선회(tick) 폐기. 프레젠테이션 한 항(시뮬 무관) | 스모크 PASS(궤도 Δpx=126·회전해도 타깃 중앙 고정·줌 스케일 변경) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-004 (L-line) | 광자 from→to 별 빈도 집계 → 분광 띠 세기(밝기=빈도/maxCount, 측정 정규화). 유무→세기 정제, 새 시뮬 양 0(`measureLines`) | 스모크 PASS(선 3·빈도합 51/51 보존·maxCount=24 측정·λ오름차순) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-005 (L-recoil) | 광자 운동량(px,py) → 빛 줄기/이방성(길이∝\|p\|/maxP 측정, 머리→자취 그라디언트). 게이트 해제(step-0003·0004). px=py=0 → 줄기 0(author 0). `measureMaxMomentum`·`photonStreak` | 스모크 PASS(방향 38/38·maxP 0.737·줄기 Δpx 20·축=운동량 정렬·무방향→null) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-006 (L-bond) | 결합 목록 sim.bonds([i,j] 원자쌍) → 결합한 두 원자를 잇는 분자 결합선(연결 성분 읽기). 게이트 해제(step-0010 bond·0012 valence). 결합 0 → 선 0(author 0). `bondSegment`·`drawBonds`, viewer 범례 1줄 | 스모크 PASS(15 결합·유효 인덱스·선분 Δpx 243·무효 원자→null) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-007 (L-trail) | 광자 출생(rx0,ry0)→현 위치(rx,ry) 실변위 → 전파 트레일(머리 밝음→출생 투명, 색=L-λ). L-recoil 정규화 글리프의 실거리 정제, 새 시뮬 양 0(step-0004·0007 변위 읽기). 변위 0 → 트레일 0(author 0). `photonTrail`, viewer 범례 1줄 | 스모크 PASS(38/38 변위·트레일 Δpx 39·축=출생→현재 정렬·무변위→null) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-008 (L-glow) | 원자 들뜸 준위 x(양자수 0..3) → 등급 광원 밝기(maxX 정규화 온백 헤일로, 반경·세기 ∝ x/maxX). 불리언→등급 정제, 미사용 계약 채널 §2 "들뜸 x=밝기" 충족. x=0 → 글로우 0(author 0). `measureMaxExcitation`·`excitationGlow`, viewer 범례 1줄 | 스모크 PASS(maxX 2·g(2)1.0>g(1)0.5 등급·바닥→0·단조·상한1) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-009 (L-order) | 결합 차수 sim.bonds[k][3](공유 전자쌍 수 1·2·3) → 결합선 평행 복제(단일 1줄·이중 2줄·삼중 3줄, O=O·N≡N). 게이트 해제(step-0018 bondOrder). 차수 없으면 단일(author 0). `bondOrder`·`bondMultiline`, viewer 범례 1줄 | 스모크 PASS(maxOrder 3·분포{1:4,2:8,3:3} 합=결합수·평행선 k줄·평행·분리·무차수→단일) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-010 (L-Ebond) | 결합 E sim.bonds[k][2]=Eabs → 결합선 밝기 등급(maxE 정규화 — 강한 결합 밝게, magnitude 채널). 게이트 해제(step-0015 bondLocalE). bonds 채널 완독 → atom-0034 따라잡음. E 없으면 밝기 0(author 0). `measureMaxBondEnergy`·`bondEnergy`·`bondGlow`, viewer 범례 1줄 | 스모크 PASS(maxE 1.121·강(1.12)1.0>약(0.01)0.01 등급·단조·상한1·E없음→0) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
렌즈-011 (L-element) | 원자 양성자 수 Z(원소 정체성) → 구 색조(측정 Z 범위 정규화·연속 사상 저 Z 파랑→고 Z 빨강, hue=Z·밝기=들뜸 x 직교). 핵 변환(decay·fuse step-0031~38)이 Z 를 바꿔도 안 보이던 것을 색 이동으로. 단일 원소(범위 0) 무채색·종류별 색 박기 0(author 0). `measureZRange`·`elementHue`·`hsvToRgb`, viewer 범례 1줄 | 스모크 PASS(step-0035 Z[6,9]4종·다른원소→다른색조 0.66→0.00·Z단조·유효RGB·단일원소→중립) · atom/ diff 0 · 눈 검증 ⏳(사용자 브라우저)
