# review-0041–0050 — 묶음 감사 (핵 율 두 방향 · 상대론 완비 · 핵합성 사다리)

> 범위: step-0041~0050(10개·전부 닫힘·십진 경계 41–50). 서사(왜·이론·진척)는 Phase D 서사 — *미작성(D 보류)*. 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 본 감사는 *정합성*(척추·verify·이슈)만.
> 호(arc): 융합 Q값도 결합에너지서(0041) → 짝수 A 두 안정점·별 점화 순환·Bateman 시계열(0042~44 측정) → **붕괴율 Sargent Q⁵(0045)·융합율 Gamow(0046)** → **상대론 완비: 좌표속도 상한(0047)+KE=(γ−1)mc²(0049)** → fuse+decay 합성 시계열(0048) → **E_G(Z₁Z₂)² 전하 사다리(0050)**.

---

## §1 척추 정합성 (SPINE §5 4항)

이 묶음은 *핵 율의 두 방향*(붕괴·융합)과 *상대론*을 같은 척추(질량공식 B + 키네매틱 γ)에 싣는다. 비판적 판정:

- **① 단일 척추 — ✅(강화)**. 자료형을 더하지 않는다: 0041 `fuseMassFormula`가 0040 의 정지질량 편입을 *융합*에 확장(`hgo-laws.js:805-806` released=ΔB_fus·`:814` 저장고 미인출)·relCap(0047)/relKE(0049)는 저장 v 를 *고유속도 u=γ·v_coord* 로 *재해석*할 뿐 별도 필드 0(`hgo-laws.js:953-955`·`hgo-kernel.js:238`). 0050 전하 게이트는 egPair=fuseEG·(Z₁Z₂)² — Z 다발의 함수(`hgo-laws.js:794-795`). **알리바이**: `hgo-kernel.js:238`(KE 게이트 분기), `hgo-laws.js:795`.
- **② 창발 환원 — ✅(정점)**. author 분기 0(grep `module.exports` 만). 율의 *함수형*마저 author 아님: 붕괴 keff=k·(Q/Qref)⁵(`hgo-laws.js:892-895`·Q=ΔB 결합에너지서)·융합 P=exp(−√(egPair/E))(`:796`). c=최고속이 γ 함수형서 창발(`:954` 점근·`hgo-kernel.js:238` KE→∞)·고Z 융합 억제가 (Z₁Z₂)² 서(`:795`). **알리바이**: `hgo-laws.js:894`(Q⁵), `hgo-laws.js:796`(Gamow 지수).
- **③ 국소 상호작용 — 🟡(유지·바스만 잔존)**. 붕괴 *그 원자 혼자*(`hgo-laws.js:855`)·융합 *그 두 원자*(`:781-789` 쌍 루프·전역 조율자 0). 단 `sim.escaped` 단일 바스 잔존 — fuse 가 흡수 KE+방출 Q 적재(`:808-809`)·decayRecoilPair −Δp 적재(`:919`). **#1 그대로**. 측정 step(0042~44·48)의 "고정 시드 재시뮬"은 scene 진단일 뿐 법칙 아님(국소성 무관). **알리바이**: `hgo-laws.js:808`.
- **④ 닫힌 장부 — ✅**. M=A−B 편입(붕괴+융합)에도 E 머신 — `hgo-kernel.js:238` KE 가법항이 정지질량 −ΔB 와 상쇄. 율 게이트는 *언제* 변환할지만 바꿈·energetics 불변(0045·0046·0050 발열량 ΔB 게이트 무관). 잔차: 순 핵·측정 1e-15~1e-13·드리프트(0047·49) 0.00·라이브 융합(0046 3.55e-11·0050 8.19e-12·0048 4.55e-13) 임계 1e-9 내.

**회귀 0 규율(메타)**: 6개 게이트(`fuseMassFormula·decaySargent·fuseGamow·relCap·relKE·fuseEGcharge`) 전부 노브=0 → early-skip/원시 거동(`hgo-laws.js:797·892·953`·`hgo-kernel.js:238` rk=0 → ½mv²). 측정 step 4개(0042·43·44·48)는 새 법칙 0 — 기존 골든 보존이 회귀 0 알리바이. 골든 250/250 일치로 0001~ 어느 해시도 불변 입증.

> **메타(라이브 융합 E 잔차)**: 0046 3.55e-11·0050 8.19e-12 는 순 핵 머신(1e-15)보다 느슨 — 라이브 다체 융합 vcom+바스 roundoff(임계 내·symplectic creep 아님·#6 watch 와 무관). 정직히 기록.

---

## §2 검증 재현 (실행 출력 인용)

전 10 step `node engine/verify.js step-NNNN` **PASS** — 닫힌 장부·결정론·가설 전 시드 통과(문서 PASS 주장을 직접 재현):

| step | ② 최대 잔차 | 핵심 수치(라이브 재현) |
|---|---|---|
| 0045 | 1.56e-13(py) | Sargent 율비 keffH/keffL 31.5≈(Q_H/Q_L)⁵·평탄 대조 180≈188 |
| 0046 | 3.55e-11(E) | Gamow 저E 18≈19.9·고E 108≈89.3·sub-barrier 18>0(고전 0)·율비 6.00 |
| 0049 | 0.00e+0 | KE(rel)=123.90=Σ(γ−1)mc²·토이 5215.13=Σ½mu²(게이트 load-bearing) |
| 0050 | 8.19e-12(E) | ⁴He+⁴He(Z₁Z₂=4) 7 ≪ ²H+²H 169(23배)·−ln 비 4.70≈4 |
| 0041·0042·0043·0044·0047·0048 | 0.00~4.55e-13 | 전부 PASS(닫힌 장부·결정론·가설) |

- **골든 재현**: `node engine/validate/verify-sim-engine.js` → **`골든 대조: 250/250 일치 PASS ✅`**(라이브 실행·추정 아님·0001~0050 전체).
- **grep 감사**: `Math.random` → `hgo-laws.js` 5건·`hgo-kernel.js` 1건 *전부 주석*("Math.random 금지 — 결정론")·실호출 0. author 분기(`isWater|isStable|.Z===상수`) → `module.exports` 외 0 hit.

---

## §3 이슈 → 후속 step 연결 (전파 고리)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| A | 0046 Gamow E_G 가 Z 무관 단일 상수(`fuseEG=9`) | ② | ✅ **해소(묶음 내)** | **step-0050 `fuseEGcharge`** — egPair=fuseEG·(Z₁Z₂)²·터널링 지수 ∝Z₁Z₂(`hgo-laws.js:795`)·게이트=0 → 상수 회귀 0. 기록만 |
| B | 0047 relCap KE 토이(운동량 상대론·에너지 ½mu² 불일치) | ④ | ✅ **해소(묶음 내)** | **step-0049 `relKE`** — KE=(γ−1)mc² 레저 게이트(`hgo-kernel.js:238`)·저속 ½mu² 회복·게이트=0 회귀 0. 기록만 |
| C | force 상호작용서 relKE 일-에너지 정합(자유 드리프트만 닫힘·법칙별 ½mv² 회계 재작성) | ②/④ | 🟡 이월(load-bearing) | STATE §3 🟡 상대론적 운동(이미 추적) → 게이트: 법칙별 상대론 에너지 회계(큰 작업) |
| D | 핵·화학 동시 무대(massDefect/fmf 장면 전역·fuse 압축 ↔ bonds 간선 인덱스) | ① | 🟡 이월 | STATE §3(이미 추적) → 게이트: 안정 id/간선 재배선(끄면 단일 트랙=회귀 0) |
| #1 | 전역 `sim.escaped` 바스 비국소(fuse E·decayRecoilPair 가담) | ③ | 🟡 이월 | STATE §3(arc Phase E 후 무르익음) → 게이트: 국소 복사장 노브(끄면 전역 바스=회귀 0) |
| #5 | 연속력 O(n²) 5개·중력 장거리 | 성능/③ | 🟡 이월(임박) | 이 묶음은 핵·드리프트라 연속력 0 — 신규 전파 아님·기록만(STATE §2·§3 이미 임박) |
| #6 | E 완화 creep | ④ | 🟡 watch | 신규 symplectic creep 0(라이브 융합 roundoff 임계 내) → STATE §3 그대로 |
| #3·#2 | scatter xMax 리터럴 · 광자 영속 자료형 | ②/① | 🔴경미/🟢 | 이 묶음 무관(Phase B 유산) — 변동 없이 이월(STATE §3) |

**전파 요지**: 이 묶음은 *자기 치유적*이다 — 0046 이 공개한 E_G Z-의존 부채를 **같은 묶음의 0050** 이, 0047 이 공개한 relKE 부채를 **같은 묶음의 0049** 가 갚았다(묶음 내 해소·원장에 안 쌓임). 원장에 *새로 이월*되는 load-bearing 은 #C(force relKE 정합)·#D(핵화학 동시 무대) 둘뿐이며 둘 다 기존 STATE §3 추적분 — 신규 척추 위반 0. **0001~0050 전 닫힌 step 감사 완료(십진 경계 정렬).**
