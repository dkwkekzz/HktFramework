# review-0031–0037 — 정합성 감사 (Phase D 핵 — 붕괴·융합·안정 골짜기)

> **묶음 감사 전용**(7 step — 마지막 묶음, 10 미만이라 그대로 닫음). 척추·verify 재현·이슈만 — *무엇을·왜*는 서사(phase-D 미작성)·[../STATE.md](../STATE.md) §7 INDEX·각 [step 문서](../steps/) 소유. 방법: `.claude/skills/hgo-atom-review/SKILL.md`.
> 판정은 PASS 주장이 아니라 **실물 코드 + verify 재현**으로. 닫힌 감사는 불변 — 단 §3 이슈는 [README.md](README.md) 원장으로.
>
> **범위**: 0031 decay(β⁻) · 0032 decayRecoilPair · 0033 fuse · 0034 측정(붕괴 통계) · 0035 측정(다단 사슬) · 0036 decayRateExcess · 0037 decayMassFormula. 핵 변환 두 방향(붕괴·융합) + 집단 통계·다단 사슬 + 율 창발 + 안정 골짜기.

---

## 1. 척추 정합성 — SPINE §5 4항 감사

| 항 | 판정 | 요지 |
|---|---|---|
| ① 단일 척추 | ✅ 견고(유보 1) | 원소 변환 = Z·N 다발 변화. 단 `a.nuc`(핵 저장고)는 *과도기 토이 필드*(M=A−B 편입 미) |
| ② 창발 환원 | ✅ **매우 견고(이 묶음의 정점)** | **0037 안정 골짜기·Q값이 결합에너지 B(Z,N)서 창발** — decayQ·문턱 author 해소 |
| ③ 국소 상호작용 | 🟡 | decay·fuse 국소(그 원자/그 두 원자). 단 방출·흡수를 *전역 바스* 적재(#1 강화) |
| ④ 닫힌 장부 | ✅ **비가역도 닫힘 입증** | Q·B·L·E·px·py 머신(0032+). 0031 만 px·py 완화(단일원자 반동→0032 해소) |

### ① 단일 척추 — ✅ 견고 (유보)
원소 변환은 *다발의 Z·N 재배치*(`decay` `hgo-laws.js:850` Z+1·N−1·e+1·lep−1)·융합은 *다발 합산*(`fuse` `:791` Z+=·N+=·e+=) — 새 자료형 0, "마그네슘"·"산소"는 측정값. **유보**: `a.nuc`(핵 저장고)·`a.lep`(렙톤)는 원래 `{Z,N,e,x,r,v}` 밖 *추가 필드*. 단 둘 다 *보존량 운반체*(ledger 가 `E+=nuc`·`L+=lep` 합산 `hgo-kernel.js:222·227`)라 다발 철학 정합. 다만 `a.nuc` 는 ΔB 를 *미리 품는 토이*(M=A−B 완전 편입 시 폐기 예정·STATE §3) — 과도기 평행 필드. → 이슈 #7.

### ② 창발 환원 — ✅ 매우 견고 (이 묶음의 정점)
author 분기 0. **0037 `decayMassFormula` 가 압권**: kernel 반경험적 질량공식 B(Z,N)(`binding` `hgo-kernel.js:32`)을 두고 β⁻ 를 *ΔB>0(발열)일 때만* 진행(`:834`) ⇒ **멈춤=ΔB≤0=안정 골짜기**(author 문턱 0)·**Q값=ΔB**(decayQ 상수 0·`:848`). 골짜기 위치(Z=8=argmax B)는 *쿨롱 vs 비대칭 경쟁*이 정함(원소표 author 0). 율도 N−Z 함수서 창발(0036 `:842`). 토이 계수(aV·aS·aC·aA)는 Bethe–Weizsäcker 물리 상수(동결 허용). **"어느 원소가 안정인가"가 코드 분기 아닌 결합에너지서 나온 것 — 굴려서 얻는다의 가장 깊은 증명.**

### ③ 국소 상호작용 — 🟡 (#1 강화)
*결정*은 국소: decay 는 *그 원자 혼자*(`:830` 순회, 이웃 0), fuse 는 *그 두 원자*(`:769` min-image, 전역 조율자 0). **단 #1 바스 강화**: decay(0032+)가 방출 입자 −Δp 를 `sim.escaped.px·py` 에(`:862`), fuse 가 흡수 KE+방출 Q 를 `sim.escaped.E` 에(`:788`) 적재 — escape·damp 에 이어 전역 바스-쓰기 가담. *결정은 국소·목적지는 전역 단일 바스*(#1 테마 반복). + **fuse 배열 압축 ↔ bonds 인덱스**(`:799~802` 죽은 원자 제거 → 간선 인덱스 어긋남, 0033 장면은 결합 없어 안전·STATE §3 disclosed).

### ④ 닫힌 장부 — ✅ 비가역도 닫힘 입증 (SPINE §2 핵심 명제)
핵 변환은 *비가역*(Z·N 못 되돌림)이나 **Q·B·L·E·px·py 전부 머신**(0032~37: 7.11e-15 ~ 0.00e+0). E 는 저장고→KE 정확 인출(`a.nuc -= q` = KE 증가분 `:855·859`). **0031 만 예외**: 단일 원자 등방 반동이라 px·py 변동(방출 입자 운동량 미추적) → *0031 장면만* 완화(px≤50). **0032 `decayRecoilPair` 가 해소**: 방출 입자 −Δp 를 바스에 적재(`:862`) → 0032~ 머신(잔차 0.00e+0). *비가역 ≠ 비보존*을 핵 변환이 입증 — SPINE §2 정합.

### 메타 — 회귀 0
게이트 전부 노브 가드: `decayRecoilPair`·`decayRateExcess`·`decayMassFormula`(`:827·842·834` 0 → 0031 author 거동 비트 동일)·`kFuse`/`kDecay`(`:758·822`). 골든 185/185(180 보존+5/step) 알리바이.

---

## 2. 검증 재현 (이 감사가 직접 실행)

```
verify 0031 px완화(단일원자) 종단Z7·멈춤N−Z≤4·KE=Q값1      PASS
       0032 잔차0.00e+0 총운동량머신0(0031 흠 해소)·Z7      PASS
       0033 잔차0.00e+0 융합Z12·운동량머신·바스E14            PASS
       0034 px3.55e-14 지수감쇠·t½34.31·전원안정              PASS
       0035 px3.02e-14 다단사슬maxZ9·딸핵4종·종점배수32        PASS
       0036 py1.42e-14 고불안정B<저불안정A·keff A0.12 B0.22    PASS
       0037 px7.11e-15 골짜기Z8=argmax B·Q=ΣΔB10.88·멈춤ΔB-0.34 PASS
node engine/validate/verify-sim-engine.js  →  골든 185/185 PASS
grep Math.random(실호출) 0 · author 분기 0
```
전 수치가 step 문서 주장과 일치. **doc-code 불일치 없음**(decay·fuse·3 게이트 정독 포함).

---

## 3. 이슈 → 후속 step 연결 (carryforward)

| # | 이슈 | 항 | 상태 | 후속 step 연결 |
|---|---|---|---|---|
| 7 | `a.nuc` 토이 저장고(M=A−B 미편입) | ① | 🟡 유보 | 결합E 가 *정지질량*에 완전 편입되면 nuc 폐기·E=Σ(M−B) 직접. **목적지 STATE §3**(이미 추적 "결합E 정지질량 편입") → 게이트: M=A−B 노브(끄면 nuc 거동·회귀 0) |
| 1 | 전역 *바스* 비국소 — **decay·fuse 가담** | ③ | 🟡 | 바스-쓰기 가담자 누적(escape·damp·decay·fuse) → 국소 복사장 무르익음↑. README 유지 |
| — | fuse 배열 압축 ↔ bonds 인덱스 | (정합) | 🟡 | 핵·화학 *동시 무대* 시 간선 재배선 필요(STATE §3 disclosed, 현 장면 안전) → 신규 전파 아님·기록만 |
| 2·3·5·6 | 광자 영속형 · xMax · 연속력 O(n²) · E creep | — | 불변 | 이 묶음(핵·이산) 무관 → README 유지 |

> **이 묶음서 author 해소**: decayQ·붕괴 문턱이 **0037 decayMassFormula** 로 결합에너지서 창발(step 내부 author → 게이트로 해소, 별도 이슈로 등록된 적 없음·기록만).

**신규 1(#7 nuc 토이)·강화 1(#1 바스)·미해소 유지(#2·#3·#5·#6)** → [README.md](README.md) §2 갱신. **새 척추 위반 0** — Phase D 핵이 *비가역도 장부는 닫힌다*(SPINE §2)를 입증하고, 0037 이 안정성을 결합에너지서 창발시켜 ②의 정점을 찍음. **0001~0037 전 닫힌 step 감사 완료.**
