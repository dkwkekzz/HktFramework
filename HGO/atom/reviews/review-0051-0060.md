# review-0051–0060 — 묶음 감사 (철 봉우리 완성 · 공간 분할 단거리 배선 · BH 트리)

> 범위: step-0051~0060(10개·전부 닫힘·십진 경계 51–60). 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 핵 물리 서사는 [phase-D-nuclear.md](phase-D-nuclear.md)(0051~53 이 철 봉우리 율·에너지면을 마무리). 본 감사는 *정합성*(척추·verify·이슈)만.
> 호(arc): **철 봉우리 완성**(0051 흡열 문턱 → 0052 환산질량 E_G∝μ → 0053 세 게이트 합작 측정) → **공간 분할 단거리 배선**(0054 cellPairs 열거기 → 0055 collide → 0056 pauli → 0057 vdw → 0058 repulse → 0059 bond·단거리 5종 완결) → **장거리판**(0060 Barnes-Hut 무게중심 트리·미배선 측정).

---

## §1 척추 정합성 (SPINE §5 4항)

두 직교 묶음이 한 십진에 든다: 핵 율의 *에너지·질량 축*(0051~53)과 *성능 인프라*(0054~60). 후자는 "법칙을 더하지 않고 *같은 결과를 가속*"이 척추 시험의 본령이다. 비판적 판정:

- **① 단일 척추 — ✅(강화)**. 자료형 0. 0051~52 는 (Z,N) 다발의 *함수*만 더한다: 흡열 비용 |ΔB_fus|(`hgo-laws.js:905`)·환산질량 μ 의존 egPair(`:890` `(egMu ? mu : 1)`). 0054~60 인프라는 *법칙이 아니다* — `cellPairs`·`bhForces` 는 LAW_ORDER·DEFAULTS 미참여 순수 열거/합산기(`hgo-laws.js:1094`·`:1140`)이고, 셀/트리 배선은 기존 force 함수 내부 *경로 분기*일 뿐(collide `:266` doPair·pauli `:590`·vdw `:627`·repulse `:550`·bond `:326`·fuse `:877`). 무게중심·전하 단극자도 다발에서 측정(`:1163` m=Z+N·q=Z−e). **알리바이**: `hgo-laws.js:1094`(cellPairs)·`:1140`(bhForces).
- **② 창발 환원 — ✅(정점)**. author 분기 0(`hgo-laws.js` 에 `Z===상수` 0 hit·전부 scenes.js 측정). 철 봉우리가 *함수 곱*서 창발: egPair=EG·(Z₁Z₂)²·μ + 흡열 문턱(`:890`·`:905`)·`if(iron)` 없음. 가속의 동치성도 author 가 아니라 *측정*: 0054 셀 쌍집합=brute(629=629)·0060 θ=0 가속=brute(maxDiff 2.22e-14·상호작용 999000=999000). **알리바이**: `hgo-laws.js:905`(흡열 문턱 부호 비교)·`:1178`(θ lump 판정).
- **③ 국소 상호작용 — 🟡(개선·바스만 잔존)**. 셀 분할은 국소성을 *강화*한다 — 제 셀+8 이웃만(`:1112-1113`)·BH 도 노드 무게중심 국소 합(`:1176`). 전역 조율자 0. 단 `sim.escaped` 단일 바스 잔존: fuse 가 흡수 KE+방출 Q 적재(`:906-908`). 0051 흡열 무대도 같은 바스(켜면 E≥0·끄면 −26.55 비물리). **#1 그대로**. **알리바이**: `hgo-laws.js:906`.
- **④ 닫힌 장부 — ✅**. 흡열 융합(비가역)이라도 Q·B·L·E·px·py 머신(0051 1.82e-12·0052/53 8.19e-12). 인프라의 핵심 정합: 연속력 컷오프가 *PE shift* 로 닫힘 — `pauliPE`/`vdwPE`/`repulsePE` 가 `uCut=U(cut)` 을 빼 r=cut 에서 0 연속화(`hgo-kernel.js:158·166`·`:182·189`·`:131·140`). 검증: 컷오프 E 잔차 ≈ brute 전쌍(0056 9.247e-3≈9.246e-3·0057 1.159e-2≈1.166e-2·0058 9.745e-3≈9.716e-3) → 컷오프가 보존을 *악화 0*. 잔차는 컷오프 탓이 아닌 연속력 고유 symplectic 진동(#6). **알리바이**: `hgo-kernel.js:164`(`if(sh&&r2>cut2)continue`)·`:166`(shift).

**회귀 0 규율(메타)**: 게이트 5개(`fuseEndo·fuseEGmu·spatialHash`[collide/pauli/vdw/repulse/bond/fuse 공용]) 전부 노브=0 → 원시 거동. 셀 배선의 비트 동일은 *근사 아님*: ① cellPairs 가 brute 와 같은 쌍 집합(0054)·② (i,j) 정렬로 처리 순서까지 brute 의 i<j 와 일치(이벤트형 collide/bond/fuse — `:283`·`:384`·`:931`). 연속력(pauli/vdw/repulse)은 force=−∇U 정합이라 sh=0 시 full U(`hgo-kernel.js:164` uCut=0·cutoff skip). bhForces 는 LAW_ORDER 불참이라 *비트가 아니라 골든 보존*서 회귀 0(0060 θ=0 ~머신 재정렬은 정직히 공개·`:1135`). 측정 step 3개(0053·54·60)는 새 법칙 0. 골든 **350/350** 으로 0001~ 어느 해시도 불변 입증.

---

## §2 검증 재현 (실행 출력 인용)

대표 step `node engine/verify.js step-NNNN` **PASS** — 닫힌 장부·결정론·가설 전 시드 통과(문서 PASS 주장을 직접 재현):

| step | ② 최대 잔차 | 핵심 수치(라이브 재현) |
|---|---|---|
| 0051 | 1.82e-12(E) | 흡열 ⁴He+⁴He E=0.1 금지 0·E=0.25 전부 200·경계 0.2<\|ΔB_fus\|=0.2327·끄면 바스 −26.55<0 |
| 0052 | 8.19e-12(E) | ³H(μ1.5)136 ≪ ²H(μ1)169 ≪ ¹H(μ0.5)225·−ln 비 1.252≈√1.5·끄면 169=169=169 |
| 0053 | 8.19e-12(E) | ¹단 169·²단 0(egPair=32)·끄면 ²단 169 회복·가열 E=22 137 |
| 0054 | 0.00e+0 | 셀 쌍=brute 629=629·검사 1798≪19900(9.0%)·wrap 40 |
| 0056 | 1.15e-2(E) | pauli 힘 maxDiff 1.15e-5·E 잔차 9.247e-3≈brute 9.246e-3·검사 24.9% |
| 0058 | 1.87e-2(E) | repulse maxDiff 2.01e-4·E 잔차 9.745e-3≈brute 9.716e-3·검사 35.6% |
| 0060 | 0.00e+0 | θ=0 maxDiff 2.22e-14·999000=999000·θ=0.2 오차 2.10%·검사 39.1% |
| 0055·0057·0059 | 5.68e-14~1.80e-2 | 전부 PASS(셀 종료 해시=brute·collide 825·vdw shift·bond 116) |

- **골든 재현**: `node engine/validate/verify-sim-engine.js` → **`골든 대조: 350/350 일치 PASS ✅`**(라이브 실행·추정 아님·0001~0070 전체).
- **grep 감사**: `Math.random` → `hgo-laws.js` 5건·`hgo-kernel.js` 1건 *전부 주석*("Math.random 금지")·실호출 0. author 분기(`Z===상수` 등) → `hgo-laws.js`(법칙 파일) **0 hit**·scenes.js 의 `a.Z===n` 은 전부 watch/assert *측정*(창발 원소 개수)이라 척추 위반 아님.

---

## §3 이슈 → 후속 step 연결 (전파 고리)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #5 | 연속력 O(n²) 5개 + 중력 장거리 | 성능/③ | ✅ **해소(이 묶음 호)** | 단거리 5종 셀 배선(0055~59·collide/pauli/vdw/repulse/bond) + BH 트리(0060) → 장거리 gravity/coulomb 배선(0061·0062·`hgo-laws.js:805·495`)으로 완결. **모든 힘 O(n log n)**. 원장서 제거·기록만 |
| #E | BH 토러스 min-image COM 토이(주기 경계 정확 합 아님) | ③/② | 🟡 **신규 이월**(#5 PM 잔여 분기) | STATE §3(이미 "토러스 min-image COM 토이·Ewald/PM" 추적) → 게이트: Ewald/PM 정밀 주기 합(farField 유지·정밀화)·θ>0 오차 2~13%는 경계 효과(`hgo-laws.js:1136`)·arc Phase E 성숙 후 |
| #6 | E 완화 creep(연속력 컷오프 무대 E≤0.03~0.04) | ④ | 🟡 부분(메커니즘 해소) | **step-0069 `symplectic`**(velocity-Verlet)이 2체 궤도 E 표류 553배 개선(메커니즘 도착)·잔여=다체 BH+fuse 적용(0070 ~3%)·적응 시간단계 → STATE §3 적분 정밀도(이미 추적) |
| #1 | 전역 `sim.escaped` 바스 비국소(fuse E 적재) | ③ | 🟡 이월 | STATE §3(arc Phase E 후 무르익음) → 게이트: 국소 복사장 노브(끄면 전역 바스=회귀 0)·`hgo-laws.js:906` |
| #C | force 상호작용 relKE 일-에너지 정합 | ②/④ | 🟡 이월(load-bearing) | STATE §3 🟡 상대론적 운동(이미 추적) → 게이트: 법칙별 상대론 에너지 회계(큰 작업)·이 묶음 무관 |
| #D | 핵·화학 동시 무대(fuse 압축 ↔ bonds 간선 인덱스) | ① | 🟡 이월 | STATE §3(이미 추적) → 게이트: 안정 id/간선 재배선(끄면 단일 트랙=회귀 0)·이 묶음 무관 |
| #3·#4·#2 | scatter xMax 리터럴 · reheat 운동량 트랩 · 광자 영속 자료형 | ②/④/① | 🔴경미/🟡/🟢 | 이 묶음 무관(Phase B 유산) — 변동 없이 이월(STATE §3 백로그) |

**전파 요지**: 이 묶음의 무게중심은 **#5(연속력 O(n²)) 해소**다 — 0054~0060 일곱 step 이 단거리 셀(5종)+장거리 BH 트리를 세우고, 곧장 0061/0062 가 장거리 힘에 배선해 *모든 힘이 O(n log n)* 가 됐다(다체 핵합성·별 무대의 enabler). 0051~53 은 *자기 완결적*으로 철 봉우리의 에너지면(흡열 문턱)·질량면(E_G∝μ)을 메워 Phase D 율 서사를 닫았다(부채 신규 0). 원장에 *새로 이월*되는 load-bearing 은 **#E(BH 주기 경계 정밀 합)** 하나뿐이며 이는 #5 의 PM 잔여 분기 — 신규 척추 위반 0. #6 은 메커니즘(0069 VV)이 도착해 부분 해소로 격하.
