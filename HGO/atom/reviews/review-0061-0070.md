# review-0061–0070 — 묶음 감사 (장거리 BH 배선 · 다체 무대 · 별 일생: 점화→사다리 등반)

> 범위: step-0061~0070(10개·전부 닫힘·십진 경계 61–70). 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 서사(왜·이론·표현·진척)는 [phase-E-star.md](phase-E-star.md)(이 묶음이 Phase E 별을 점화·등반시킴). 본 감사는 *정합성*(척추·verify·이슈)만.
> 호(arc): **장거리 BH 배선**(0061 gravity·0062 coulomb — 운동량 복원 평균 차감) → **인프라 위 다체 측정**(0063 N=600 중력 붕괴·0066 gravity+coulomb 동시) → **셀 fuse + 다체 핵합성**(0064 fuse 셀·0065 N=400 점화 시계열) → **껍질 마법수**(0067 nucShell 안정섬) → **별 일생**(0068 자가 점화·0069 velocity-Verlet·0070 사다리 등반 maxZ→12).

---

## §1 척추 정합성 (SPINE §5 4항)

이 묶음은 *척추 위반 없이 규모를 키운다* — 장거리 힘 가속(BH lump)이 운동량 보존을 깨는 위험을 *물리(등가원리·프레임 시프트)*로 복원하고, 그 위에서 별이 author 없이 점화·등반한다. 비판적 판정:

- **① 단일 척추 — ✅(강화)**. 자료형 0. BH 가속은 다발의 질량(`K.mass`)·전하(`Z−e`)로만 가중(`hgo-laws.js:807`·`:499`)·nucShell 은 B(Z,N) 의 *항* 추가(`hgo-kernel.js:51` shell truthy 면 +shellDelta)·VV 는 r·p 의 시간 전개법 교체(별도 필드 0·`:1072`). 별·원소 등반은 전부 *Z 다발의 값/위치* 측정. **알리바이**: `hgo-laws.js:805`(질량가중 g_i)·`hgo-kernel.js:38`(shellDelta=aShell·(isMagic(Z)+isMagic(N))).
- **② 창발 환원 — ✅(정점)**. author 분기 0(법칙 파일 `Z===상수` 0 hit). 별 점화가 *측정*: 중력 압축이 keRel 올려 Gamow 율 창발(`if(isStar)` 0)·끄면 0(0068 fusionsGrav 163 vs 0). 사다리 등반도 measure(0070 maxZ 1→12·끄면 1). nucShell 의 `isMagic` 는 *결합E 식의 항*이지 결과 author 아님(붕괴 종점이 ΔB 에서 굴러 마법수서 멈춤·0067 valley 10→8). **알리바이**: `scenes.js:4503`(fusionsGrav/NoGrav 대조)·`hgo-kernel.js:36`(isMagic 은 식의 일부).
- **③ 국소 상호작용 — 🟡(개선·바스만 잔존)**. BH 무게중심 lump 도 국소 합의 *측정*(전역 조율자 아님·`hgo-laws.js:1161` com 상향 합)·셀 fuse 이웃만(`:931`). 전역 조율자 0. `sim.escaped` 단일 바스 잔존(fuse 적재·`:906`). **#1 그대로**. **알리바이**: `hgo-laws.js:1176`(노드 무게중심 변위 국소).
- **④ 닫힌 장부 — ✅(운동량 복원이 백미)**. BH 의 인공 net force 를 *물리적으로* 상쇄: gravity 는 질량가중 평균 가속 차감(등가원리·균일 가속 무해·`hgo-laws.js:806-809` Σm(g−c)=0)·coulomb 은 *하전만* 차감해 중성 불변(`:498-508` Σ_q m(a−c′)=0). 측정: 0063 dpx 4.35e-14·0066 dpx 4.93e-14(대조 날것 net 0.54)·0062 중성 Δv **0.00e+0**. nucShell 세 곳(rest·decay ΔB·fuse ΔB_fus) 같은 B → Q·B·L·px·py 머신(0067 5.68e-14). **알리바이**: `hgo-laws.js:809`·`:508`·`hgo-kernel.js:51`.

**회귀 0 규율(메타)**: 게이트 4개(`farField`[gravity/coulomb]·`nucShell`·`symplectic`·`spatialHash`[fuse]) 전부 노브=0 → 원시 거동(`hgo-laws.js:803·1058`[phase 미지정 전 법칙]·`hgo-kernel.js:51`). VV 의 회귀 0 은 *비트가 아니라 경로 분기*(`symplectic`=0 → `sim.step` 옛 applyForces+integrate·무인자라 phase 분기 미진입). 측정 step 5개(0063·65·66·68·70)는 새 법칙 0. 골든 **350/350** 으로 0001~ 어느 해시도 불변 입증. VV leapfrog 는 try/finally 로 반-kick dt 오염 차단(`hgo-laws.js:1079-1081`·검토 보강 반영).

> **메타(정직)**: VV 는 *보존 연속력*만 반-kick·이벤트/소산은 1회(`:1078`). leapfrog 주석이 *미사용 조합*(symplectic=1 + 활성 bonds → bondSpring/bondAngle 1-tick 위상 지연·`:1070-1071`)을 미리 공개 — 현재 그 조합 scene 0(무해), 핵+화학/symplectic 화학 무대 열 때 선결 과제(#D 와 연동).

---

## §2 검증 재현 (실행 출력 인용)

대표 step `node engine/verify.js step-NNNN` **PASS** — 닫힌 장부·결정론·가설 전 시드 통과(문서 PASS 주장을 직접 재현):

| step | ② 최대 잔차 | 핵심 수치(라이브 재현) |
|---|---|---|
| 0061 | 2.31e-1(E) | gravity BH dpx 5.33e-15(머신)·eres 0.093≈brute 0.108·검사 35.4% |
| 0062 | 7.54e-4(E) | coulomb 중성 Δv **0**·dpx 9.99e-16·eres 4.9e-4≈brute 5.3e-4 |
| 0063 | 3.84e+1(E) | N=600 R_g 25.16→24.57(2.31%)·끄면 평탄 25.16·dpx 4.35e-14·상대 0.075% |
| 0065 | 6.82e-13(E) | ²H 400→34·⁴He 봉우리 106·Z≥3 56·B 800=800·dB 0 |
| 0067 | 5.68e-14(E) | 이중마법 보너스 1.6/1.6·비마법 0·골짜기 종점 매끈 10→껍질 8 |
| 0068 | 1.36e+1(E) | R_g 21.52→20.25(5.88%)·점화 163 vs 끄면 0·dpx 2.36e-15 |
| 0069 | 7.45e-9(E) | VV E 표류 0.0108% vs Euler 5.99%(553배)·secular 0.60 vs 16.84(28배)·dpx 0 |
| 0070 | 1.00e+3(E) | 사다리 distinct 12·²H 14·Z≥3 58·점화 310 vs 끄면 0·**relEpct 11.05%** |
| 0064·0066 | 8.53e-14·20.9 | PASS(셀 fuse 비트 동일 127·gravity+coulomb 동시 dpx 4.93e-14·중성 불변) |

- **골든 재현**: `node engine/validate/verify-sim-engine.js` → **`골든 대조: 350/350 일치 PASS ✅`**(라이브 실행·추정 아님·0001~0070 전체).
- **grep 감사**: `Math.random` → `hgo-laws.js` 5건·`hgo-kernel.js` 1건 *전부 주석*("Math.random 금지")·실호출 0. author 분기(`Z===상수`) → 법칙 파일 **0 hit**·scenes.js 의 `a.Z===n` 은 watch/assert *측정*(창발 원소·골짜기 종점).

> **발견(문서-코드 nuance·정직)**: 0070 의 *헤드라인* E 완화 "**~3%**"(STATE NOW·step 문서 §2·ledgerTol 주석)는 **라이브 120 tick** 장부(절대 ~1000/E≤1500)다. 그러나 사다리 등반(maxZ→12·load-bearing 결과)을 *실제로 보이는* **240 tick** 별 run 의 `relEpct`(=dE/|l0.E|·`scenes.js:4641`)는 **11.05%**다. 거짓 아님(11% 도 verify 출력·"적분 한계" 공개)이나 헤드라인이 *짧은·유리한* 지평을 인용 — 실제 시연 지평의 적분 비용은 ~11%. #6(적분 정밀도)로 기록(아래 §3).

---

## §3 이슈 → 후속 step 연결 (전파 고리)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #5 | 연속력 O(n²) + 중력 장거리 | 성능/③ | ✅ **해소·이 묶음서 시연** | 0061/0062 가 gravity/coulomb 을 BH 배선(`hgo-laws.js:803·493`)·0063(N=600)·0066(동시) 으로 *규모 시연* 완결. 모든 힘 O(n log n). 기록만 |
| #6 | 적분 정밀도 — E 완화(깊은 붕괴+융합) | ④ | 🟡 부분(메커니즘 0069·잔여 큼) | **0069 VV** 가 2체 secular 553배 개선(`:1072`)·잔여=다체 BH+fuse 근접조우·**240 tick 별 run ~11%**(헤드라인 ~3%는 120 tick)·이벤트형 변환 미보정 → STATE §3 적분 정밀도 → 게이트: *적응 시간단계·근접조우 정규화*(Phase E 별 일생에 load-bearing·끄면 현 거동=회귀 0) |
| #E | BH 토러스 min-image COM 토이·단극자 | ③/② | 🟡 이월 | 0061~68 전부 계승(disclosed)·중성 노드 Σq=0 → 쌍극자 누락(0062) → STATE §3(토러스 min-image·Ewald/PM) → 게이트: Ewald/PM 정밀 주기 합·멀티폴(끄면 현 min-image=회귀 0)·arc Phase E 성숙 후 |
| #1 | 전역 `sim.escaped` 바스 비국소(fuse 적재) | ③ | 🟡 이월 | STATE §3(arc Phase E 후 무르익음·복사 냉각/별 죽음과 연동) → 게이트: 국소 복사장 노브(끄면 전역 바스=회귀 0)·`hgo-laws.js:906` |
| #D | 핵·화학 동시 무대(fuse 압축 ↔ bonds 간선)·+symplectic 결합 1-tick 지연 | ① | 🟡 이월(범위 확장) | 0064 fuse 셀·0069 VV 가 *각각* 안전(현 scene 결합 없음)·동시엔 ① bonds 인덱스 ② symplectic bond 위상 지연(`:1070`) 둘 다 선결 → STATE §3 → 게이트: 안정 id/간선 재배선(끄면 단일 트랙=회귀 0) |
| #C | force 상호작용 relKE 일-에너지 정합 | ②/④ | 🟡 이월(load-bearing) | STATE §3 🟡 상대론적 운동(이미 추적) → 게이트: 법칙별 상대론 에너지 회계(큰 작업)·이 묶음 무관 |
| #3·#4·#2 | scatter xMax 리터럴 · reheat 운동량 트랩 · 광자 영속 자료형 | ②/④/① | 🔴경미/🟡/🟢 | 이 묶음 무관(Phase B 유산) — 변동 없이 이월(STATE §3 백로그) |

**전파 요지**: 이 묶음은 *규모를 척추 위반 없이 키운* 구간이다 — 장거리 BH 배선(0061/62)이 #5 를 완전히 닫고(N=600·동시 무대 시연), 그 인프라 위에서 별이 author 없이 점화(0068)·등반(0070·maxZ→12)했다. 원장에 *새로 무게가 실리는* 건 **#6(적분 정밀도)** — 0069 VV 가 2체는 553배 잡았으나 *다체 별 run 의 적분 비용이 ~11%*(헤드라인 ~3%보다 큼)로 드러나, *적응 시간단계*가 Phase E 별 일생(다음 호 — 죽음·분산)에 *load-bearing* 임이 확정됐다. 신규 척추 위반 0. **0001~0070 전 닫힌 step 감사 완료(십진 경계 정렬·atom 트랙 현재까지 전구간).**
