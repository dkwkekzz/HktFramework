# review-0091-0100 — 묶음 감사 (성간 수송·점화 + 결합 종류화 완성 + 100 step 이정표)

> 범위 0091~0100 · 호(arc): **성간 수송 닫기(snEject 0091→coreHarvest 0092→cross 0093→점화 격리 0097) + 결합 기하 네 축 종류화 완성(α 0094·r_eq 0095·차수 NL 0098·VSEPR 0099)+통합 게이트(0100)** · Phase E 서사 [phase-E-star.md](phase-E-star.md)(🟡 성숙 — 수송 기제 입증으로 SPINE §4 churn 종착에 한 발 더·*닫힌 순환은 미*, 서사 갱신은 0101 루프 닫힘 시). *무엇을 했나*는 §7 INDEX·서사 — 여기는 감사만.

---

## §1 척추 정합성 (SPINE §5 4항 — 실물 코드 알리바이)

**① 단일 척추 — ✅.** 10 step 모두 새 다양성을 *새 자료형 0* 로 실었다. 결합 종류는 Z 함수(`morseD` √ZaZb `hgo-kernel.js:205` · `morseAlpha` √ZaZb `:226` · `morseReq` (∛Za+∛Zb)/2 `:239`), 통합 게이트는 *OR 배선*일 뿐 새 필드 0(`bondKindPair` `:203·224·237`). 방출·수확은 기존 다발·바스(`snEject` `hgo-laws.js:1268`·`coreHarvest` `:746`), VSEPR θ₀ 는 *측정된 배위수* `ns.length` 함수(`:861`). c0(0093 출신 태그)·`seed`(0097)는 측정 라벨로 hash 미참여(step-0093 §척추①).

**② 창발 환원 — ✅.** `grep 'Z\s*===|isWater|Z\s*==\s*N'` over `hgo-laws.js` **0 매치** — author 종류 분기 없음. 결합 종류·각·차수 포화가 전부 Z·차수·배위수의 *함수*서 창발: D∝차수^p 멱함수(`:212`·"삼중<3×" 종류 상수 박기 0), VSEPR 선형/삼각이 `ns.length` 분기서(`:861`·분자별 각 author 0), snEject 방향이 *국소 이웃 COM 반대*서(`:1294` `ux=-cx/cl`). 0093·0097 점화/수송은 *새 법칙 0* — Gamow 율(상대 KE 함수)서 창발(분기 아님).

**③ 국소 상호작용 — 🟡(개선 없음·#1 잔존).** 신법칙은 국소다: snEject/coreHarvest 모두 `degField(sim, coolR)` + coolR 이웃만(`:1279·754`·전역 조율자 0), bondAngle 은 중심+이웃쌍만(`:855·863`). **단 전역 단일 *바스* `sim.escaped` 가 이 묶음서 가담자 +2** — snEject 가 바스서 인출(`:1273 if(!bath||bath.E<=0)return`·`:1296 draw`·`:1302 bath.E-=draw`), coreHarvest 가 바스에 적재(`:753·777 bath.E+=dKE`). 비국소 바스 결합은 그대로(#1·아래 §3).

**④ 닫힌 장부 — ✅.** Q·B·L·px·py *늘 머신*(전 step dB 0·dL 0·dpx ≤2.4e-12). E 는 연속력·라이브 붕괴서 완화 닫힘: snEject/coreHarvest/cross 0.134~0.657%(라이브 격렬 붕괴 swing·`scene.ledgerTol≤130`), 점화 0.00000%(0097), 화학 0.010~0.170%(symplectic 유계). coreHarvest 가 *코어 KE(=적분이 푼 중력 PE)* 를 바스로 옮겨 gravityPE 차분 *없이* 닫힘 — 이중계상 0(`:742` 주석·E 0.304% 검증). VSEPR 은 *힘과 PE 가 같은 θ₀* 라야 닫힘 — `bondAnglePE` 도 배위수 θ₀ 정합(`hgo-kernel.js:281` ≡ `hgo-laws.js:861`·구현 중 18% 누수→정합 후 0.13%, step-0099 §15).

> 메타(회귀 0 규율): 10 step 모두 게이트=0 → early-return/개별 게이트 환원. 0100 통합은 OR 배선이라 `bondKindPair=1` ≡ 세 sub-knob on 비트 동일(reqCC 3.6343=3.6343 검증)·새 물리 0. 측정 step(0093·0097)은 새 법칙 0 → 골든 보존이 회귀 0 알리바이.

---

## §2 검증 재현 (verify 출력 — 시드 [42,7,1234,99,2026])

대표 7 step `node engine/verify.js` 실행·전부 **PASS**(문서 수치 비트 재현):

- **0091 snEject**: maxR sn **17.2**·R_g 10.76 ≫ disp 10.0 ≫ off 6.5·churn 51>37>19·E 0.134%·dpx 5.68e-13.
- **0093 성간 수송**: cross harv **6** ≫ sn 0 = off 0(돌파 에너지=중력 붕괴 수확서만)·E 0.657%·dpx 2.39e-12.
- **0096 Otsu**: 코어 R_g Otsu **4.43** ≪ median 37.5(코어 보존)·겉 44.84·E 0.0000%·dpx 1.49e-13.
- **0097 점화**: hot consumed **9** ≫ none·cold 1·mz hot 14 ≫ cold 6(도달≠점화)·E 0.00000%·dpx 5.68e-14.
- **0099 VSEPR**: 2배위 **180.0°**·3배위 120.0° (uni off 2배위 잘못 120°)·E 0.134%·dpx 1.47e-15.
- **0100 통합**: D·α 비 **6.0**·r_eq 1.82·reqCC 3.6343 ≡ 세 sub-knob(비트 동일)·E 0.170%·dpx 4.85e-14.

**골든**: `node engine/validate/verify-sim-engine.js` → **500/500 일치 PASS** (0091~0100 각 +5 가법·495 보존 — 게이트=0/새 법칙 0 → 전 step 비트 불변).

**grep 감사**: ① `Math.random` — laws/kernel 8 매치 *전부 "금지" 주석*·실호출 0(결정론 ✅). ② author 종류 분기(`Z===`·`isWater`) — laws **0 매치**(창발 환원 ✅). ③ `#3 scatterXMax=6` 리터럴 여전(`hgo-laws.js:113`·미해소 재확인).

---

## §3 이슈 → 후속 step 연결 (전파 고리)

이 묶음은 carried 인프라 이슈(#1·#I·#C·#E·#D′·#3·#4·#2)와 *직교*(Phase E 수송·Phase C 결합) — 따라서 ✅해소 0. 단 **#1·#I 가담자 +2**, **신규 3건**.

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #1 | 전역 *바스* `sim.escaped` 비국소 — **snEject 인출(`:1273·1302`)+coreHarvest 적재(`:777`) 가담 +2** | ③ | 🟡 부분(확대) | STATE §3 → 국소 복사장 노브(끄면 전역 바스=회귀 0)·Phase E 닫힘으로 무르익음·render L-T 연동 |
| #I | degField cut=radius 가정(`:1476`) — **snEject(`:1279`)·coreHarvest(`:754`) 가 두 소비자 추가** | ③ | 🟡 라텐트 | STATE §3 백로그 → 셀 반경 ±2 확장(끄면 현 cut 가정=회귀 0)·현 scene coolR≤spatialCut 안전 |
| #J | **결합 축간 상관 미(D↔α↔r_eq·Badger 규칙)** — 0100 통합은 *합집합*일 뿐 연결 아님(`:203·224·237` 독립 OR·step-0100 §37 한계) | ②/④ | 🟡 신규(load-bearing) | **STATE §2**(이미 0101 권장 후보·arc) → 상관 게이트(k_force↔r_eq·끄면 독립 셋=회귀 0) |
| #K | **수확↔점화 tradeoff(coreHarvest 가 융합도 식힘 heavy 26<51·0092 §41·cross harv heavy 19<off 25 0093)** | ④ | 🟡 신규(tension) | STATE §3 → harvestDeg 정밀(가장 깊은 코어만)·수확/점화 분리 측정 |
| #L | **수송+점화 한 무대 닫힌 순환 미(0097 중력0 격리·도달≠점화 입증했으나 좌별→수송→우점화→우별 한 시뮬 미)** | — | 🟡 신규(arc) | **STATE §2**(이미 0101 권장 1순위) → 새 법칙 0 측정(우물 병합 회피+우 아임계 유지)·골든 보존=회귀 0 |
| #C | force relKE 일-에너지 정합 | ②/④ | 🟡 열림 | STATE §3(상대론 운동 추적)·미접촉 carry |
| #E | BH 토러스 min-image COM 토이 | ③/② | 🟡 carry | STATE §3(Ewald/PM)·미접촉 |
| #D′ | VV 결합 1-tick 위상 지연 + 안정 id | ①/④ | 🟡 백로그 | STATE §3·미접촉 |
| #3 | `scatter` xMax=6 리터럴(`:113`) | ② | 🔴 열림(경미) | STATE §3 → `scatterXMax` 노브화(기본 6=회귀 0) |
| #4 | `reheat` 운동량 일방 트랩 | ④ | 🟡 부분(#1 종속) | STATE §3 → #1 국소 복사장이 운동량도 정련 |
| #2 | 광자 영속 자료형 | ① | 🟢 설계 수용 | STATE §3 백로그 → 추적 입자화 시 |

> #J·#L 은 STATE §2 NEXT 가 이미 0101 후보로 보유 — review 는 *감사 권위*로 원장에 못 박아 step-loop 이 승급 시 근거로 삼게 한다(전파 고리: review 기록 → step 승급). #K 는 신규 tension(수송 성공이 점화량을 깎음) — 0092~93 라이브서 정량화(heavy 26<51·19<25), 닫힌 순환(#L) 설계 시 동시 최적이 게이트.
