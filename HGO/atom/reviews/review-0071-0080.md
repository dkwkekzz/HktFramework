# review-0071–0080 — 묶음 감사 (별 죽음·분산 → 일생 순환 → 세대 재점화 · 적분/융합 회계 닫힘 · 화학 해리)

> 범위: step-0071~0080(10개·전부 닫힘·십진 경계 71–80). 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 서사(왜·이론·표현·진척)는 [phase-E-star.md](phase-E-star.md)(이 묶음이 Phase E 의 *죽음·분산·순환 종착*을 그림). 본 감사는 *정합성*(척추·verify·이슈)만.
> 호(arc): **죽음·분산**(0071 disperse·0072 일생 순환·0073 fuseRebond 핵화학 동시) → **화학 해리**(0074 Morse·0075 bondBreak) → **적분/융합 E 회계**(0076 adaptSub·0077 원천 격리·0078 fuseConservePE·0079 일생 적용) → **세대 재점화**(0080 시간분리 2세대).

---

## §1 척추 정합성 (SPINE §5 4항)

이 묶음은 *척추 위반 없이* Phase E 순환을 닫고(모음→점화→사다리→**죽음·분산**→재응집), 별 ~11% E 완화의 *진단을 교정*(적분 아님·융합 회계)해 못박았다. 비판적 판정:

- **① 단일 척추 — ✅(강화)**. 자료형 0·새 필드 0. `disperse`/`bondBreak` 는 LAW_ORDER 끝에 append(`hgo-laws.js:1167`)·기질은 decay 등방반동+0032 −Δp 바스 재사용(`:1118·1126`). `bondMorse` 는 bondSpring 의 fmag *항 교체*(조화↔Morse·`:720-722`)·`fuseConservePE`/`fuseRebond` 는 바스·간선만 건드림(원자 다발 불변·`:982·964`). `adaptSub` 는 적분기 서브스텝(별도 필드 0·`sim.lastSub` 는 introspection·해시 미참여·`:1197`). 0080 "2세대"는 *측정된* maxZ 돌파(19→29)지 author "2nd star" 분기 아님. **알리바이**: `hgo-laws.js:1167`(LAW_ORDER append disperse·bondBreak)·`:720-722`(Morse term in fmag).
- **② 창발 환원 — ✅(정점)**. author 분기 0(법칙 파일 `Z===상수`·`isWater` 0 hit·grep §2). `bondMorse` 유한 해리/열팽창은 퍼텐셜서 창발(`:721` fmag=2Dα·x(1−x))·`fuseConservePE` 소멸분은 *측정*(pe0−pe1, **ledger 와 같은 커널 PE 함수**·`:886`)·author 상수 0. 0077 원천 귀속을 *측정*으로(adaptSub 켬 10.30%≈끔 9.69%·gravity-only 0.241% 대조·author 단정 0). 0080 세대 재점화도 국소 법칙 합성서 창발(중력 끄면 maxZ 2·점화 0). **알리바이**: `hgo-laws.js:886`(sumPairPE = ledger PE 함수 재사용)·grep author 0 hit.
- **③ 국소 상호작용 — 🟡(개선·바스만 잔존)**. 전부 쌍별/이벤트/셀. disperse 는 *그 원자 혼자* 판정하나 전역 `sim.escaped` 바스서 인출(`:1109`)·fuseConservePE 의 sumPairPE 도 *쌍별* PE(전역 조율자 0·O(n²) 합이나 국소 합의 측정). **#1 그대로**(바스가 이제 *소비자*[disperse]까지 가짐). **알리바이**: `hgo-laws.js:1113`(disperse per-atom)·`:1109`(bath=sim.escaped 전역·#1).
- **④ 닫힌 장부 — ✅(회계 닫힘이 백미)**. disperse: 바스−=draw·KE+=draw → E 정확·−Δp→바스 → px·py 머신(`:1125-1127`·0032 동형). bondBreak: U(r)+e[2]→바스·bondE 동기 차감 → ΔE=0·속도불변 운동량 머신(`:1154-1158`). fuseRebond: 끊긴 결합 e[2]→바스+`sim.bondE−=Eb`(`:970`). **fuseConservePE: 소멸 보편 쌍 PE (pe0−pe1)→바스**(`:985`) → 별 융합 누수 9.69%→0.224%≈적분 baseline 0.241%. 전 step Q·B·L·px·py 머신·E 는 이벤트/자유드리프트면 머신(0071/73/75)·연속력이면 symplectic 유계+누수닫힘(0078/79/80 cache net 0.262%). **알리바이**: `hgo-laws.js:985`(pe0−pe1→바스)·`:1158`(bondBreak U+e[2]→바스·bondE sync).

**회귀 0 규율(메타)**: 새 게이트 6개(`kDisperse`·`fuseRebond`·`bondMorse`·`unbondDist`·`adaptSub`·`fuseConservePE`) 전부 노브=0 → early-return/옛 경로(DEFAULTS `hgo-laws.js:13`·adaptSub=0 → M=1 단일 KDK·`:1196·1200`). 측정 step 4개(0072·0077·0079·0080)는 새 법칙 0(scene 만). 골든 **400/400**(0001~ 어느 해시도 불변). leapfrog 서브스텝도 try/finally 로 dt 오염 차단(`:1206-1208`).

> **메타(정직·이월 공개)**: leapfrog 주석이 *미사용 조합*(symplectic=1 + 활성 bonds → bondSpring/bondAngle 1-tick 위상 지연·`:1186-1187`)을 계속 공개 — 0073 fuseRebond 가 #D 의 *인덱스* 절반을 닫았으나 *VV 결합 위상* 절반은 그대로(현 그 조합 scene 0·무해). 핵+화학+symplectic 한 무대를 열 때 선결.

---

## §2 검증 재현 (실행 출력 인용 — 문서 PASS 주장을 직접 재현)

대표 step `node engine/verify.js step-NNNN` **전부 PASS**(닫힌 장부·결정론·가설 전 시드 통과):

| step | ② 최대 잔차 | 핵심 수치(라이브 재현) |
|---|---|---|
| 0071 | 0.00e+0(Q) | disperse R_g 5.66→25.70(4.54배)·끄면 5.68·바스 80→0·dpx 3.19e-15·dE 1.14e-13 |
| 0073 | 2.27e-13(E) | fuseRebond dangling 켜0/끄6·융합 51+결합 47 공존·분자 5·dE 5.68e-14·dB 0 |
| 0075 | 0.00e+0(Q) | bondBreak 결합 40→19·분자 19·해리 21회·dpx 0·dB 0·잔여 E 0.009% |
| 0076 | 6.28e-4(E) | adaptSub 순간 스윙 0.320%≪고정 331.83%(1037배)·maxM 65≫고정 1·dpx 0 |
| 0078 | 2.16e+1(E) | fuseConservePE 켬 0.224%≪끔 9.69%(43배)·maxZ 14=14·융합 314=314·dpx 1.63e-12 |
| 0080 | 2.22e+2(E) | 종단 maxZ 29>천정 19·중력끄면 2·후기 융합 3·net 0.262%·dpx 3.52e-12·dB 0 |

- **골든 재현**: `node engine/validate/verify-sim-engine.js` → **`골든 대조: 400/400 일치 PASS ✅`**(라이브 실행·추정 아님·0001~0080 전체·+30 가법, 0070=350 보존).
- **grep 감사**: `Math.random` → `hgo-laws.js` 6건·`hgo-kernel.js` 1건 *전부 주석*("Math.random 금지")·실호출 0. author 분기(`Z===상수`/`isWater`) → 법칙 파일 **0 hit**(scenes.js 의 `a.Z` 비교는 watch/assert *측정*).

> **발견(문서-코드 정합·정직)**: 직전 묶음(0061-0070)의 #6 헤드라인 "별 ~11% E 완화 = *적분* 한계" 진단을 이 묶음이 **측정으로 교정**했다 — 0077 이 adaptSub 를 켜도 별 relE 가 *안 줄고*(10.30%≈9.69%·maxM 17 작동) gravity-only 는 0.241% 임을 보여 *적분 아님·융합 이벤트 회계*로 못박았고, 0078 `fuseConservePE` 가 그 융합 누수(소멸 쌍 PE)를 바스로 환원해 **9.69%→0.224%**(≈baseline)로 닫았다. 미스어트리뷰션을 백로그가 아니라 *후속 측정*으로 바로잡은 모범 — verify 출력이 알리바이.

---

## §3 이슈 → 후속 step 연결 (전파 고리)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #6 | 적분 정밀도 — 별 ~11% E 완화 | ④ | ✅ **해소(진단 교정+회계 닫힘)** | 0077 이 *적분 아님*(adaptSub 무효·gravity-only 0.241%)으로 격리 → 0078 `fuseConservePE`(소멸 쌍 PE→바스·9.69%→0.224%·`:985`)·0079 일생 적용(11.59%→0.271%)·0080 장시간 0.262%. 별도 0076 `adaptSub` 가 2체 순간 스윙 1037배(`:1196`). 기록만 |
| #D | 핵·화학 동시 — fuse 압축↔bonds 간선 | ① | ✅ **인덱스 절반 해소** | 0073 `fuseRebond`(dangling 켜0/끄6·간선 재배선+e[2]→바스·`:964-978`). *VV 결합 위상*·*안정 id* 절반은 잔존(아래) |
| #D′ | VV 결합 1-tick 위상 지연 + 안정 id(n무관 키) | ①/④ | 🟡 백로그(비-load-bearing·scene 0) | bonds+symplectic 동시 scene 아직 0(`:1186`) → **STATE §3 백로그** → 게이트: VV 결합 위상 + 안정 id(끄면 단일 트랙=회귀 0)·핵+화학+symplectic 무대 열 때 선결 |
| #G | 핵합성 산물→분자 풍부한 무대(뜨거운 융합↔차가운 결합 척도 충돌) | ①/② | 🟡 신규(load-bearing·arc) | 0074 Morse·0075 bondBreak 가 화학 *해리* 갖췄으나 cache run 격리(라이브 자유드리프트·0074/75 §한계)·토이 covalent He 정지 → **STATE §1/§2**(이미 한계 추적) → 게이트: 융합 산물이 결합하는 라이브 scene + 결합 종류별 D·α(끄면 현 토이=회귀 0)·Phase E→물질 전환 |
| #1 | 전역 `sim.escaped` 바스 비국소 | ③ | 🟡 이월(소비자 추가) | disperse 가 바스 *소비자*로 가담(바스 E→KE·`:1125`)·#4 reheat 와 함께 → **STATE §3**(Phase E 닫힘으로 무르익음·복사 냉각/국소 복사장) → 게이트: 국소 복사장 노브(끄면 전역 바스=회귀 0) |
| #E | BH 토러스 min-image COM 토이·단극자 | ③/② | 🟡 이월 | 0080 *공간 분리* 2세대 별이 더 큰 무대 요구(토러스 wrap·0071/80 §한계)와 연동 → **STATE §3**(Ewald/PM·멀티폴) → 게이트: 정밀 주기 합(끄면 현 min-image=회귀 0) |
| #C | force 상호작용 relKE 일-에너지 정합 | ②/④ | 🟡 이월(load-bearing) | 이 묶음 무관(전 step 전가 유지) → **STATE §3 🟡 상대론적 운동** → 게이트: 법칙별 상대론 에너지 회계(큰 작업) |
| #3·#4·#2 | scatter xMax 리터럴 · reheat 운동량 트랩 · 광자 영속 자료형 | ②/④/① | 🔴경미/🟡/🟢 | 이 묶음 무관(Phase B 유산·변동 0) — **STATE §3 백로그** 이월 |

**전파 요지**: 이 묶음은 Phase E 의 *남은 두 칸*(README phase-E §3 — 죽음·분산·적분 정밀도)을 **둘 다 닫았다**. disperse(0071)~세대 재점화(0080)가 SPINE §4 순환을 *시간 분리* 2세대까지 굴렸고, #6(적분 ~11%)을 *진단 교정*(0077)+*융합 회계 닫힘*(0078/79·40배)으로 해소했으며, #D 의 인덱스 절반을 fuseRebond(0073)로 닫았다. 원장에 *새로 무게가 실리는* 건 **#G**(핵합성 산물→분자 — 화학 해리는 갖췄으나 라이브 융합-결합 동시 무대 미)와 **남은 1순위 공간 분리 2세대 별**(이미 STATE §2 step-0081 소유 — review 가 따로 이월 안 함). 신규 척추 위반 0.
