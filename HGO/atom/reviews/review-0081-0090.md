# review-0081–0090 — 묶음 감사 (별풍·층상 핵합성 → 공간 분리·중력 병합 · 핵화학 동시 무대 · 결합 종류·차수화)

> 범위: step-0081~0090(10개·전부 닫힘·십진 경계 81–90). 한 일 요약은 [../STATE.md](../STATE.md) §7 INDEX 가 SSOT — 여기 재서술 안 함. 서사(왜·이론·표현·진척)는 [phase-E-star.md](phase-E-star.md)(별풍·층상·공간 분리)·[phase-C-chemistry.md](phase-C-chemistry.md)(결합 종류·차수화). 본 감사는 *정합성*(척추·verify·이슈)만.
> 호(arc): **공간 분리 별**(0081 두 우물 각자 점화·측정) → **핵화학 동시 무대**(0082 핵합성 산물→분자·라이브·#G 해소) → **별풍·층상**(0083 coolOuter 국소 냉각→이산 분자·0084 별풍·측정·0085 disperseOuterDeg 층상·0086 중력 병합·측정·0087 disperseAutoDeg 동적 임계·0088 degField 셀 배선) → **결합 종류화**(0089 bondMorsePair D∝√Z·0090 bondMorseOrder D∝차수).

---

## §1 척추 정합성 (SPINE §5 4항)

이 묶음은 *척추 위반 없이* Phase E 의 *물질 분산*(별풍·층상 핵합성·공간 분리·중력 병합)을 닫고, Phase C 결합 강도를 *종류·차수* 두 축으로 갈랐다. 측정 step 4개(0081·0082·0084·0086)는 새 법칙 0(scene 만)·게이트 step 6개(0083·0085·0087·0088·0089·0090) 전부 노브=0 → 옛 거동. 비판적 판정:

- **① 단일 척추 — ✅(강화)**. 자료형 0·새 필드 0. `coolOuter` 는 LAW_ORDER 에 `damp` 뒤 append(`hgo-laws.js:1341-1342`)·기질은 damp 의 vcom-불변 점성 감쇠+바스 재사용(`:727-730`). 층상 게이트(`disperseOuterDeg`·`disperseAutoDeg`)는 기존 disperse 에 *밀도 한정 조건문*만 더함(`:1238` `if(deg[idx]>odegEff)continue`)·새 동역학 0. `degField` 는 deg *집계*만 셀화(deg 패스 대체·LAW_ORDER·DEFAULTS 불변·`:1474`). 결합 종류·차수 D 는 기존 Morse 의 D 를 *Z·차수 함수*로 교체(`hgo-kernel.js:200-209`)·세 사용처(force `laws:808`·PE `kernel:246`·해리 `laws:1322`)가 *같은 헬퍼* 공유. 0081/0082/0084/0086 의 "두 별"·"분자"·"별풍"·"병합 풀"은 *측정된* 클러스터/연결성분/R_g 지 author 분기 아님(`c0` 는 측정 태그·동역학 미참여). **알리바이**: `hgo-laws.js:1341-1342`(coolOuter LAW_ORDER append)·`hgo-kernel.js:200-209`(morseD = Z·차수 함수).
- **② 창발 환원 — ✅(정점)**. author 분기 0(법칙 파일 `Z===상수`·`isWater`·`=== 6/8` 0 hit·grep §2). "코어/겉껍질"은 좌표/반지름 박기 0 — *국소 밀도 deg* 서 측정(coolOuter `:709`·disperse `:1238`)·동적 임계는 deg *분포 분위수*서 자동(`:1220-1230`·author 임계 박기 0). "강/약 결합"은 `if C–C` 0 — `√(Z_a·Z_b)`(`kernel:204-205`)·"다중 결합 강함"은 `D *= order`(`kernel:207`·order=0018 측정 차수 e[3]). 0086 의 *별풍 단독으로는 못 건넌다* 발견도 author 단정 아닌 측정(disperseE 3→20 올려도 cross 0·중력 끄면 병합 0). **알리바이**: `hgo-kernel.js:204-207`(D = √(ZaZb)·order — 함수)·grep author 0 hit.
- **③ 국소 상호작용 — 🟡(개선·바스만 잔존)**. coolOuter/disperse 밀도는 각 원자+반경 coolR 이웃만(`degField:1474`)·결합 D 는 그 결합 두 원자 Z·차수만(`kernel:204`). disperse 의 *분위수 임계*만 분산 후보 deg 전체 정렬(전역 *집계*지 *조율자* 아님·각 원자 분류는 자기 deg vs 경계·국소·0087 §한계 정직). coolOuter 가 바스 *쓰기*(`:730`)·disperse 가 바스 *소비*(`:1249`) 가담 → **#1 그대로**(바스가 이제 쓰기[coolOuter]+소비[disperse] 양방). **알리바이**: `hgo-laws.js:706`(degField per-atom 이웃)·`:704·730`(bath=sim.escaped 전역·#1).
- **④ 닫힌 장부 — ✅**. coolOuter: vcom 불변(`:727-728`)·빠진 상대 KE→바스(`:730`) → px·py 머신·E 닫힘. disperse 층상: 게이트는 *누가* 부는지만 정하고 바스↔KE 순환·−Δp→바스(`:1249-1251`)는 0071 그대로 → 보존 불변. 결합 종류·차수 D: 힘·PE·해리 *같은 D* 공유(`laws:808`·`kernel:246`·`laws:1322`) → 퍼텐셜·힘·해리 환원 정합·해리 시 U(r)+e[2]→바스(0089 검증 dpx 0·dE symplectic 유계). degField 는 결과 비트 동일이라 장부도 동일. **알리바이**: `hgo-laws.js:730`(coolOuter dKE→bath)·`hgo-kernel.js:246`+`hgo-laws.js:808,1322`(morseD 세 곳 공유).

**회귀 0 규율(메타)**: 새 게이트 6개(`kCoolOuter`·`disperseOuterDeg`·`disperseAutoDeg`·`bondMorsePair`·`bondMorseOrder`·헬퍼 `degField`/`spatialHash`) 전부 노브=0 → early-return/옛 경로(DEFAULTS `hgo-kernel.js:13`). coolOuter `if(!kc)return`(`:699`)·층상 게이트 `odeg>0||autoDeg>0` 일 때만 deg 1패스(`:1217`)·morseD `bondMorsePair`/`bondMorseOrder` falsy → 균일 D(`kernel:203·207`)·degField `spatialHash` falsy → brute(`:1480`). 측정 step 4개(0081·0082·0084·0086)는 새 법칙 0(scene 만). 골든 **480/480**(0001~ 어느 해시도 불변·+50 가법). degField 셀 경로가 brute golden 과 비트 동일임을 0088 이 골든으로 *증명*(`degMaxDiff 0`).

> **메타(정직)**: degField 의 `cut=coolR≤spatialCut` 가정(`:1471·1477`)이 라텐트 게이트다 — coolR>spatialCut 무대선 셀폭<coolR → 이웃 셀 밖 누락 위험(현 scene coolR 5≤spatialCut 8 안전·0088 §한계 정직 공개). 셀 반경 확장(±2)은 후속(아래 #I).

---

## §2 검증 재현 (실행 출력 인용 — 문서 PASS 주장을 직접 재현)

대표 step `node engine/verify.js step-NNNN` **전부 PASS**(닫힌 장부·결정론·가설 전 시드 통과·라이브 실행·추정 아님):

| step | ② 최대 잔차 | 핵심 수치(라이브 재현·시드 42) |
|---|---|---|
| 0083 | 1.22e+2(E) | coolOuter 분자 6개(최대 8)≫끔 2개(최대 17)·maxZ 26 점화 유지≫전역 damp maxZ 1·dpx 7.69e-12·E 0.510% |
| 0085 | 1.20e+2(E) | 층상 maxZ 27≫균일 16·겉 R_g 5.74>완전끔 2.45·dpx 1.71e-12·E 0.214% |
| 0087 | 1.16e+2(E) | 자동(중앙값) R_g 4.60≫고정 odeg8 2.22≈끔 2.35·중앙값 deg≈17·dpx 3.41e-12·E 0.275% |
| 0088 | 1.20e+2(E) | 셀 deg − brute deg 최대차 **0**·검사 253≪n² 160000(0.16%)·dpx 1.71e-12·E 0.214% |
| 0089 | 0.00e+0(Q) | bondMorsePair C–C 생존 20/20(D 12)≫H–H 0/20(D 2)·균일 0/0·dpx 0·dE 1.564 |
| 0090 | 0.00e+0(Q) | bondMorseOrder 생존 단일 2<이중 4<삼중 6·균일 2=2=2·dpx 0·dE 0.066 |

- **골든 재현**: `node engine/validate/verify-sim-engine.js` → **`골든 대조: 480/480 일치 PASS ✅`**(라이브 실행·0001~0096 전체·이 묶음 +50 가법, 0080=400 보존).
- **grep 감사**: `Math.random` → `hgo-laws.js` 7건·`hgo-kernel.js` 1건 *전부 주석*("Math.random 금지")·실호출 0. author 분기(`Z===상수`/`isWater`/`=== 6`/`=== 8`) → 법칙 파일 **0 hit**(scenes.js 의 `a.Z` 비교는 watch/assert *측정*).

> **발견(문서-코드 정합·정직)**: 0086 의 헤드라인은 *예상의 측정적 교정*이다 — STATE §2 가 권한 "별풍 산물이 떨어진 우물 점화" 가설을 측정해 보니 **별풍 단독으로는 우물 간극(~24)을 못 건넌다**(중력 결속·바스 E 유한·disperseE 3→20 올려도 cross 0). 진짜 공간 결합은 *중력 병합*(좌10+우10 균형 풀)이고, 진짜 *수송*엔 초신성급 방출이 필요하다고 정직히 전가했다 — 그 전가가 0091(snEject)~0093(coreHarvest cross 6)에서 갚였다(아래 §3). 가설을 백로그가 아니라 *후속 측정/법칙*으로 바로잡은 모범 — verify 출력이 알리바이.

---

## §3 이슈 → 후속 step 연결 (전파 고리)

| # | 이슈 | 항 | 상태 | 목적지 → 게이트/해소 |
|---|---|---|---|---|
| #G | 핵합성 산물→분자(뜨거운 융합↔차가운 결합) | ①/② | ✅ **해소(라이브 무대+종류화)** | 0082 라이브 자가 점화 별 산물 19/21 공유결합·bondCovalent 끄면 0(`scenes`·골든 보존) + 0083 coolOuter 이산 분자(6개·`laws:697`) + 결합 종류별 D(0089 `kernel:203`)·차수 D(0090 `:207`). α·r_eq 는 0094/0095(다음 묶음)로 계속. 기록만 |
| #D | 핵·화학 동시 — fuse 압축↔bonds 간선 | ① | ✅ **라이브 입증(인덱스 0073+무대 0082)** | 0073 `fuseRebond` 인덱스 절반 + 0082 가 핵+화학 동시 무대를 *라이브로* 굴려(maxZ 28∧분자 2·골든 보존) 인덱스 무손상 실증. *VV 결합 위상*·*안정 id* 잔존분은 #D′ 로 단일 추적(중복 제거) |
| #H | 별풍 단독 우물 간극 못 건넘(초신성급 방출 필요) | ②/④ | ✅ **해소(0086 발견→0091~93)** | 0086 측정 발견(별풍 cross 0·중력 결속) → 0091 `snEject`(방향성 분출·maxR 17) + 0092 `coreHarvest`(중력 붕괴 KE→바스·maxR 37 간극 24 돌파) + 0093 성간 수송(harv cross 6≫off 0). 기록만(다음 묶음 게이트) |
| #1 | 전역 `sim.escaped` 바스 비국소 | ③ | 🟡 이월(쓰기+소비 양방) | coolOuter 가 바스 *쓰기*(`:730`)·disperse 가 *소비*(`:1249`)로 양방 가담 → **STATE §3**(Phase E 닫힘으로 무르익음·복사 냉각/국소 복사장·render L-T 연동) → 게이트: 국소 복사장 노브(끄면 전역 바스=회귀 0) |
| #I | degField cut=coolR>spatialCut 시 셀 누락(라텐트) | ③ | 🟡 신규(경미·정리성) | coolR>spatialCut 무대선 셀폭<coolR → 이웃 셀 밖 쌍 누락(현 5≤8 안전·0088 §한계) → **STATE §3 백로그** → 게이트: degField 셀 반경 ±2 확장(끄면 현 가정 = 회귀 0)·고밀도 무대 열 때 선결 |
| #E | BH 토러스 min-image COM 토이·단극자 | ③/② | 🟡 이월 | 0081/0086 공간 분리·두 우물 무대가 더 큰 토러스 요구와 연동(이 묶음 무관) → **STATE §3**(Ewald/PM·멀티폴) → 게이트: 정밀 주기 합(끄면 현 min-image=회귀 0) |
| #C | force 상호작용 relKE 일-에너지 정합 | ②/④ | 🟡 이월(load-bearing) | 이 묶음 무관(전 step 전가 유지) → **STATE §3 🟡 상대론적 운동** → 게이트: 법칙별 상대론 에너지 회계(큰 작업) |
| #D′ | VV 결합 1-tick 위상 지연 + 안정 id(n무관 키) | ①/④ | 🟡 백로그(비-load-bearing) | #D 의 VV 위상 잔존분 단독 추적·현 그 조합 scene 0(무해) → **STATE §3 백로그** → 게이트: VV 결합 위상 + 안정 id(끄면 단일 트랙=회귀 0) |
| #3·#4·#2 | scatter xMax 리터럴(`laws:113·152` 확인) · reheat 운동량 트랩 · 광자 영속 자료형 | ②/④/① | 🔴경미/🟡/🟢 | 이 묶음 무관(Phase B 유산·변동 0) — **STATE §3 백로그** 이월 |

**전파 요지**: 이 묶음은 직전 묶음이 *새 무게*로 올린 **#G**(핵합성 산물→분자)를 **해소**했다 — 0082 가 라이브 자가 점화 별 위에서 핵 산물의 공유결합을 측정하고, 0083~0085 가 국소 냉각(이산 분자)·별풍·층상 핵합성으로 *물질 분산*을 닫았으며, 0089/0090 이 결합 강도를 종류·차수로 갈랐다. 0086 은 *별풍 단독 수송 불가*를 정직히 측정해 **#H** 를 띄웠고, 그 전가가 같은 묶음 직후 0091~0093(snEject·coreHarvest·성간 수송)에서 갚였다. 원장에 *새로 무게가 실리는* 건 경미한 **#I**(degField cut 라텐트)뿐 — 나머지(#1·#E·#C·#D′)는 백로그 이월. 신규 척추 위반 0.
