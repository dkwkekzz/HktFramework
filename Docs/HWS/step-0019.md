# step-0019 — 공유(share): 개체↔대사 *차등* 결합으로 — 개체='계'를 *중립*에서 *적응*으로 (kin selection 선택압)

> [step-0018로](step-0018.md) · 실행물: [step-0019.html](step-0019.html)(3D 표준 셸) · 시뮬 로직: `engine/hws-laws.js` 의 **`share` 법칙 1개**(LAW_ORDER ⑥d — crowd 뒤·생명 앞, sim-core.js 복사 없음) · 헤드리스: `step-0019/verify.js` · 엔진 재현: `node engine/validate/verify-sim-engine.js`

step-0018 이 막/flux 결합(couple)으로 개체(다세포='계')를 *측정 윤곽*에서 *물리적 flux 결합 도메인*으로 올렸다 — kin 끼리 **필드 E 를 공유**해 개체에 **개체군 *이득***(공유 larder → carrying capacity ~69→~79·최대 개체 7→16세포)을 줬다. 그러나 그건 kin selection 의 *맹아*일 뿐 **선택압은 0**이었다(정직한 한계 #2): 모든 kin 이 *똑같이* 공유(분업 없음)라 "공유하는 유전형 vs 안 하는 유전형"의 차등 적합도가 없었다 — **개체군 이득 ≠ 개체군 선택**. 개체는 응집·공유해도 진화적으로 *중립*이었다. step-0019 은 그 larder 를 **차등**으로 만들어 개체성을 *중립에서 적응으로* 올린다(STATE §3 🟡 최우선·SPINE §다섯째 축 "개체를 진화 단위로").

이것은 **law-pipeline 의 아홉째 step** 이다 — 더한 시뮬 로직은 `engine/hws-laws.js` 의 `share` 법칙 1개(LAW_ORDER ⑥d) + 노브 1개(`kShare` 마스터) + 협동 표현형 보조 노브 2개(`coopFit0`·`coopFitStep`). 회귀는 동결 *해시*로 잠근다 — `golden-sim.json` 의 `share@` 키 신규 = step-0020 회귀 앵커. `kShare=0` → step-0018 비트 불변(`mem@`).

share 는 couple 의 **m-아날로그**다 — couple 이 *필드 E* 를 kin 끼리 공유했듯, share 는 *생물량 m*(사망 문턱 mDeath·번식 문턱 mDiv 를 직접 가르는 대사 재고)을 kin 끼리 공유한다. 단 **두 가지가 다르다**: ① 공유 대상이 E(밖의 자원)가 아니라 *m*(개체의 대사 재고) — **개체↔대사 결합**. ② 균일 균등화(couple)가 아니라 **차등**(공유 강도가 유전형의 함수 coop) + **표적 구조**(사망 직전 kin 만, 부유 kin 의 잉여로). 균등화는 부유 kin 을 mDiv 밑으로 끌어내려 번식을 깎아(협동 비용↑) 선택 우위를 지운다 — 표적 구조는 궁핍한 kin 의 *결손분*(작음)만 메워 번식을 거의 안 깎으면서(비용↓) 사망을 막는다(이득↑) → **b>c 로 협동이 *선택*된다**.

결과: **① kin selection 선택압이 켜진다(Hamilton's rule)** — 협동(coop=1)과 배신(coop=0) 유전형을 *복제 적합도와 분리*(geneFitStep=0·inheritCost=0)해 혼합 경쟁시키면(boom-bust 순환, 2×2 요인설계 혈연도×협동), 협동의 효과가 kin 이 **뭉치면** ≈0(협동 *지속*)·**흩어지면** **−0.267**(협동자 *사라짐*, 낭비된 이타). 둘의 차 = **상호작용 0.263** = "혈연도가 협동을 favor 한 폭"(kin selection 의 정량, rb>c). *두* 기준선(협동 off — 뭉침 **+0.021**·흩어짐 **+0.004**)이 모두 중립이라 효과는 *오직* 협동에서(태그·흩어짐 artifact 0). **② 회귀 0·닫힌 장부·결정론** — kShare=0 시 step-0018 비트 동일(mem@), 잔차 **4.22e-13**(m 쌍 거래 — 나간 만큼 들어옴 → sumM 불변), 해시 일치(m 재분배가 해시에 — 순차 Gauss-Seidel 결정론). **③ churn 유지** — 실제 m 재분배지만 공멸 없이 지평선(35k) 생존, 후반 출생/사망 5242/5254(≈균형), carrying capacity ~63.

## 검증 질문 (본 thesis: step-0018 의 *균일* 막 공유에 *차등 m 공유*(협동 표현형)를 더하면 kin selection *선택압*이 창발한다 — 협동의 운명이 혈연도에 의존[Hamilton rb>c], 개체성이 중립에서 적응으로. SPINE §다섯째 축, STATE §3 🟡 최우선 격차 해소)

> ① **회귀 0**: kShare=0 이면 step-0018 과 비트 동일? (`reg` — 생물량 공유 스택 해시 == golden `mem@`)
> ② **보존**: 생물량 공유(kin m 표적 구조)가 도는 내내 닫힌 장부? (`conserve`)
> ③ **결정론**: kShare on 같은 시드 2회 비트 동일(m 재분배가 해시에)? (`det`)
> ④ **kin selection(가설)**: 협동(coop=1)·배신(coop=0) 경쟁의 *2×2 요인설계*(혈연도×협동)에서 *협동의 효과*가 혈연도에 따라 갈리는가(뭉치면 ≈0·흩어지면 ≪0, 상호작용>0)? *두* 기준선(협동 off, 뭉침/흩어짐) 모두 중립? (`kin`)
> ⑤ **동역학 보존**: 생물량 공유(실제 m 재분배)가 끝없는 churn 을 *안 깬다*(공멸 없음·후반 출생≈사망>0)? (`sustain`)

모든 수치는 시드 [42, 7, 1234, 99, 2026], `node step-0019/verify.js <mode> [seed]` 로 재현. kin 은 *통제 선택 아레나*(boom-bust·번식/사망 on·협동을 복제 적합도와 분리 — adhere on/off 로 혈연도를 갈라 kin selection 격리)에서 선택압을 잰다. conserve/det/sustain 은 전체 스택(step-0018 그대로 + 생물량 공유, 균일 협동). 생물량 공유 상수(전체 스택): **`kShare 0.5`·`coopFit0 1.0`·`coopFitStep 0.0`**(균일 협동 = risk-pooling).

---

## 0. 쉽게 풀어 쓴 설명 (먼저 읽기 — 전문 기호 없이)

**이 단계가 답한 질문.** 직전 단계(0018)에서 *같은 가문(유전형)끼리 뭉친 세포들은 바깥 먹이(에너지)를 서로 똑같이 나눠* 가졌다. 덕분에 개체군 전체가 더 잘 버텼지만 — 모두 똑같이 나누니 *나누는 가문이 안 나누는 가문보다 유리할 게 없었다*. 협동이 "공짜지만 진화적으론 의미 없는" 상태였고, 그래서 "개체(뭉친 세포 무리)"는 생겼어도 진화가 *선호할 이유가 없는 중립적* 존재였다. 이번 단계의 목표는 **협동을 진화가 실제로 골라낼 만한 것으로 만들기**다.

**무엇을 바꿨나.** 세 가지다. ① *나누는 대상*을 바깥 먹이에서 세포 *자신의 몸(생물량)* 으로 바꿨다 — 몸은 그 세포가 번식하느냐 굶어 죽느냐를 직접 가르는 자산이라, 이걸 나눠야 생사가 걸린 "진짜 도움"이 된다. ② *협동을 유전 성향으로* 만들었다 — 어떤 가문은 "협동가"(굶주린 친척을 돕는다), 어떤 가문은 "이기주의자"(안 돕는다)이고 이 성향이 자손에게 유전된다. ③ *돕는 방식을 정교하게* 했다 — 잘 먹은 세포가 죽기 직전의 친척에게 *딱 살아날 만큼만 조금* 떼어 준다(자기 번식엔 거의 지장 없이 친척은 살리는 "여유분 구조").

**핵심 결과 — 협동은 친척이 뭉쳐 있을 때만 이득이다.** 협동가와 이기주의자를 섞어 경쟁시켰다. *친척끼리 뭉쳐 살면* 죽어가는 친척을 돕는 게 곧 같은 협동 유전자의 사본을 살리는 일이라 **협동가가 도태되지 않고 유지**됐다. *친척이 흩어져 살면* 협동가가 도와봤자 고립된 친척은 어차피 죽어 비용만 치르므로 **협동가가 4분의 1 넘게 줄며 사라졌다**. 이것이 생물학의 **혈연 선택(kin selection)** 이자 **해밀턴 법칙**이다 — "도움이 진화적 이득이 되려면 받는 쪽이 충분히 가까운 친척이어야 한다." 뭉치면 협동이 살고 흩어지면 죽는다. **대조 실험**으로 깨끗함을 확인했다: 나눔을 아예 끄면 두 가문이 구별되지 않았다(완전 중립) — 위 차이는 *오직 몸 나누기*에서 나온 것이다.

**흥미로웠던 점(설계 땐 예상 못 함).** ① *풍족할 땐 나눔이 무의미했다* — 환경이 꽉 찬 평형에선 굶주린 하나를 살리면 그 자리에서 다른 하나가 대신 굶는다(먹이 총량 고정). 누가 죽느냐만 바뀌지 몇이 죽느냐는 그대로다. 협동이 효과를 낸 건 **기근(흉년)** 때였다 — 흉년엔 친척을 살리는 게 남의 죽음과 맞바꾸는 게 아니라 순수히 생존자가 느는 것. *협동은 풍요가 아니라 위기의 자식*이다. ② *흩어진 협동은 중립이 아니라 해롭다* — 외톨이 협동가는 가망 없는 친척을 떠받치며 자원만 낭비한다. 협동이 좋은 전략이 되려면 *먼저* 가족이 물리적으로 뭉쳐 있어야 한다(뭉침이 협동보다 먼저). ③ *돕는 방식이 성패를 갈랐다* — 첫 시도(친척끼리 몸을 똑같이 반반 나누기)는 역효과였다. 잘 먹은 쪽을 번식 문턱 아래로 끌어내려 도움의 이득과 비용이 비겼다. "죽기 직전인 애한테 조금만 채워주기"로 바꾸자 비로소 이득이 비용을 넘었다.

**세계의 규칙은 그대로다.** 몸 나누기는 세포 사이에서 질량을 *옮길 뿐* 만들거나 없애지 않는다(장부가 정확히 맞음 — 오차 사실상 0). 같은 조건이면 항상 같은 결과(난수 없음). 이걸 더해도 개체군이 붕괴하지 않고 계속 태어나고 죽으며 굴러간다.

**왜 중요한가.** 이번 단계로 **"개체(협동하는 세포 무리)"가 처음으로 진화가 *선호하는* 대상이 됐다**. 그전엔 "어쩌다 같이 있는 세포들"이었는데 이제 "함께 협동하면 유리한 집단"이 됐다. 생명의 큰 사다리(복제분자 → 세포 → 다세포 → **사회** → 인지)에서 *다세포 개체* 칸을 넘어 *사회* 칸의 문턱에 발을 디딘 셈이다.

**솔직한 한계.** 협동이 *자리를 지키긴 하나 적극적으로 퍼지진* 않았다(강하게 번지려면 "한 번의 도움이 여럿을 살리는" 더 센 협동=공공재가 필요). 아직 *진짜 분업*(세포마다 다른 역할 — 생식 담당/몸 담당)은 아니고 "다 같이 안전망을 나눠 갖는" 수준이다. 둘 다 다음 단계 후보다.

> 아래 §1~8 은 같은 내용을 *정확한 수치·기호·척추 정합*으로 적은 정식 기록이다(이 §0 은 그 풀어 쓴 길잡이).

---

## 1. 6요소 지도

| # | 요소 | 구분 | 이 step | 문서 |
|---|------|------|---------|------|
| 1 | **터 (Ground)** | 생성자 | step-0001 그대로 | [01-ground.md](step-0001/01-ground.md) |
| 2 | **흐름량 (Quantity)** | 생성자 | step-0014 그대로(E 재고 + R 상 + A flux) | [02-quantity.md](step-0001/02-quantity.md) |
| 3 | **법칙 (Law)** | 생성자 | **생물량 공유 1항 추가** — kin m 표적 구조(차등 협동 → kin selection 선택압) | [03-law.md](step-0001/03-law.md) |
| 4 | **구동 (Drive)** | 생성자 | step-0013 그대로 (별 이산 FSM) | [step-0001/04-drive.md](step-0001/04-drive.md) |
| 5 | **자원 (Resource)** | 결과 | step-0016 그대로 — R 이 유전의 담체·생명 부트스트랩의 기질 | [05-resource.md](step-0001/05-resource.md) |
| 6 | **생명 (Life)** | 결과 | **개체가 *중립*에서 *적응*으로** — kin m 공유로 협동이 차등 적합도를 가져 kin selection 선택압 | [step-0001/06-life.md](step-0001/06-life.md) |

새 요소를 만들지 않았다 — 법칙(3)에 생물량 공유 1항을 더하고, 생명(6)의 개체가 *중립*에서 *적응*(선택압)으로 올랐다. 협동(coop)은 별도 자료형이 아니라 *유전형 a.g 의 표현형* — step-0016 의 a.g(생명 속성)를 *그대로* 공유 강도 함수에 쓴다(새 정체성 없음, 단일 척추).

---

## 2. 메커니즘 — 생물량 공유(kin m 표적 구조), m 재분배·국소·쌍 거래 보존·차등

법칙 순서의 ⑥d(crowd ⑥b 직후·생명 ⑦ 직전)에 둔다 — crowd 가 매긴 밀도세 *뒤*의 m 을 보고, 굶주린 kin 을 ⑦생명의 사망 판정 *전에* 떠받친다. 각 생명은 유전형 태그 `a.g`(0=무유전, 1..geneTypes)를 갖고, 그 표현형으로 **협동 propensity** `coop(태그) = clamp(coopFit0 + coopFitStep·(태그−1), 0, 1)` 를 갖는다. kShare≠0 일 때:

```js
// ⑥d share — 생물량 공유(kin m 표적 구조 → 차등 생존·kin selection)
danger = mDeath·SHARE_BAND                                  // 사망권(이 밑이면 "궁핍" = 구조 대상). SHARE_BAND=3 고정 상수
occIdx = {각 생명의 center → agent index}                    // 점유 그리드(생명 찾기 — m 은 *생명* 속성이라 셀 아닌 생명을 찾아야)
for (각 생명 a, 태그 t>0, coop(t)>0):
  rc = 우(+x) 이웃 center;  dc = 하(+y) 이웃 center            // 우/하만 — 쌍 한 번씩(중복 0)
  for (이웃 b in {occIdx[rc], occIdx[dc]}, b.g==t):           // kin 쌍만
    if (a 궁핍·b 안전): donor=b, recip=a;  elif (b 궁핍·a 안전): donor=a, recip=b;  else continue
    need = danger − recip.m;  avail = donor.m − danger        // 결손분 vs 잉여
    d = min(need, avail)·kShare·coop                          // 작은 쪽만큼만 — 번식 거의 안 깎음(표적 구조)
    donor.m −= d;  recip.m += d                               // m 쌍 거래(나간 만큼 들어옴 → sumM 불변)
```

다섯 설계 결정이 이 생물량 공유를 *척추 정합* + *선택 발생*하게 만든다:

1. **m(대사 재고)을 공유한다 — 개체↔*대사* 결합(STATE (a) 핵심)** — couple(0018)은 *필드 E*(개체 밖의 자원)를 공유해 개체군 *이득*만 줬다. share 는 *생물량 m*(개체의 대사 재고 — mDeath·mDiv 를 직접 가르는 양)을 공유한다. m 을 공유해야 *사망/번식*(개체 단위 적합도)이 직접 결합돼 선택압이 생긴다.
2. **차등(coop = 유전형의 함수) — "공유하는 유전형 vs 안 하는 유전형"** — couple 은 모든 kin 이 균일 공유(선택압 0). share 는 공유 강도가 유전형의 표현형 coop(a.g) — 협동 유전형(coop>0)과 배신(coop=0)이 갈린다. 다양성은 *병렬 필드 아닌 속성*(a.g 의 표현형) — 단일 척추. 협동을 *복제 적합도(fit)와 별 노브*로 분리(coopFit0/coopFitStep ≠ geneFit0/geneFitStep)해 confound 없이 kin selection 만 본다.
3. **표적 구조(targeted rescue) — 균등화 아님** — 사망 직전 kin 의 *결손분*(danger−m, 작음)만 부유 kin 의 *잉여*(m−danger)로 메운다. 균등화(couple 식)는 부유 kin 을 mDiv 밑으로 끌어내려 *번식을 깎아*(협동 비용↑) b≈c 로 선택을 지운다 — 표적 구조는 번식을 거의 안 깎으면서(비용↓) 사망을 막아(이득↑) **b>c**. (1차 구현은 균등화였고 선택 신호가 0이라 표적 구조로 바꿨다 — §정직한 한계 1.)
4. **실제 m 재분배이되 쌍 거래(닫힌 장부)** — couple(E)·inherit(대사세)처럼 *실변환*이다. 단 쌍별 거래(나간 만큼 들어옴)라 sumM 불변 — `sumE+M+R+evap+sunk+metab−injected=E0` 식이 그대로 닫힌다(잔차 4.22e-13).
5. **순차 Gauss-Seidel·결정론** — occIdx 제자리·scan=agent 배열 순서(Math.random 금지 — m 이 결정론적, 같은 시드 2회 비트 동일).

### couple(⑥c)·adhere(⑥a)·생명/번식을 안 건드린다

share 는 *m 공유 법칙*이다 — 막 공유(E)·응집(위치)·번식·상속·대사 어느 것도 수정하지 않는다. couple 이 ⑥c 에서 E 를 공유하고(막), share 가 ⑥d 에서 그 위 생명의 *m* 만 공유한다(개체 단위 생존). 직교하되 같은 kin 도메인 위에서 만난다.

---

## 3. ① 회귀 0 ② 보존 ③ 결정론 — `reg`·`conserve`·`det`, 통과

### ① 회귀 0 (`reg`)

`kShare=0` 이면 `share` 법칙이 통째로 early-return 하고 m 을 안 건드려 step-0018 의 식을 그대로 수행한다. 생물량 공유 스택 해시를 골든 `mem@`(step-0018 의 막 결합 스택, 생물량 공유 추가 전 동결)와 대조:

```
seed 42/7/1234/99/2026  hash == golden mem@ (전 시드)  OK
  (std@~mem@ 모두 불변 — share 가 LAW_ORDER 에 있어도 kShare=0·m 불변이라 비트 동일)
```

생물량 공유는 m 만 바꾸므로(m 은 이미 step-0003 이래 해시에) *해시에 새 항을 더하지 않는다* — 공유 off 면 과거 시나리오 해시 전부 불변. 새 `share@` 키(공유 on)는 step-0020 의 회귀 앵커가 된다(`verify-sim-engine.js` golden 가법 잠금).

### ② 보존(`conserve`)·③ 결정론(`det`)

생물량 공유 on(전체 스택 + R 유전 씨앗 ×2, 균일 협동), 별 6 + 생명 5, FORM 4000 + 8000 tick:

| 시드 | 잔차 | sumE | 저장체 R | 개체수 | 누적 공유 m |
|---|---|---|---|---|---|
| 42 | 3.45e-13 | 2007.4 | 3650.4 | 70 | 205.7 |
| 7 | 4.37e-13 | 2413.8 | 3891.3 | 71 | 206.5 |
| 1234 | 3.94e-13 | 2060.7 | 3981.1 | 37 | 186.7 |
| 99 | 4.02e-13 | 2177.7 | 3849.4 | 75 | 186.1 |
| 2026 | 5.34e-13 | 1924.3 | 3929.0 | 39 | 168.1 |
| **평균** | **4.22e-13** | 2116.8 | 3860.3 | **58.4** | 190.6 |

- **장부가 닫혀 있다** — 잔차 4.22e-13(step-0018 동급). 생물량 공유는 kin 쌍 m 거래(나간 만큼 들어옴) — sumM 불변, 장부 식 불변.
- **m 이 끊임없이 돈다** — 누적 공유 m ~191(churn 하는 개체군이 끝없이 kin 을 떠받친다).
- **결정론(`det`)도 비트 동일** — 같은 시드 2회 E·R·G·에이전트 x·y·**m**·a.g 해시 일치(생물량 공유는 순차 Gauss-Seidel — m 재분배가 결정론적).

---

## 4. ④ kin selection — `kin`, 통과 (개체='계'가 *중립*에서 *적응*으로, Hamilton's rule)

통제 선택 아레나(boom-bust 순환, 번식·사망 on)에 협동자(coop=1)·배신자(coop=0)를 *균등 혼합* 파종하고, 협동을 *복제 적합도와 분리*(geneFitStep=0·inheritCost=0)해 kin selection 만 격리한다. **2×2 요인설계 — 혈연도(뭉침/흩어짐) × 협동(off/on)** 네 칸(같은 시드·아레나). 800 정착 + 8×(풍년 400 + 기근 250), 협동자 비율 변화 Δ. *협동의 효과* = (협동on − 협동off)를 혈연도별로, *상호작용* = 그 둘의 차 = kin selection 정량:

| 시드 | 뭉침·협동off | 뭉침·협동on | 흩어짐·협동off | 흩어짐·협동on | 협동효과(뭉침) | 협동효과(흩어짐) | 상호작용 |
|---|---|---|---|---|---|---|---|
| 42 | +0.016 | +0.036 | −0.003 | −0.243 | +0.021 | −0.240 | 0.261 |
| 7 | +0.050 | +0.005 | +0.011 | −0.270 | −0.046 | −0.281 | 0.235 |
| 1234 | +0.023 | +0.032 | +0.007 | −0.303 | +0.010 | −0.310 | 0.319 |
| 99 | +0.016 | +0.034 | −0.011 | −0.253 | +0.019 | −0.242 | 0.261 |
| 2026 | +0.001 | −0.024 | +0.017 | −0.247 | −0.026 | −0.264 | 0.238 |
| **평균** | **+0.021** | **+0.016** | **+0.004** | **−0.263** | **−0.005** | **−0.267** | **0.263** |

- **kin selection 이 켜진다(Hamilton rb>c)** — *협동의 효과*가 혈연도에 따라 갈린다: kin 이 **뭉치면** 협동의 효과 ≈ 0(−0.005, 협동이 *지속*)하고, **흩어지면** 협동의 효과 **−0.267**(협동자가 *사라진다*, 전 시드 일관). 둘의 차 = **상호작용 0.263**(전 시드 0.24~0.32) = "혈연도가 협동을 favor 한 폭" = kin selection 의 정량. 협동은 kin 곁에서만 떠받치므로(rb>c) 혈연도가 그 운명을 가른다.
- **효과는 *오직* 협동에서(2×2 로 airtight)** — *두* 기준선(협동 off)이 모두 중립이다: 뭉침 +0.021·흩어짐 +0.004. 즉 *태그 자체*도 *흩어짐 자체*도 tag2 를 불리하게 안 한다(artifact 0). 따라서 흩어짐·협동on 의 *사라짐*은 *오직 협동(낭비된 이타)*에서 온다 — 흩어짐이라는 처리 자체가 아니라. 협동을 복제 적합도와 분리(geneFitStep=0·inheritCost=0)했으므로 confound 도 0.
- **흩어진 협동 = 낭비된 이타** — 흩어지면 협동자가 고립돼, 떠받친 kin 도 함께 죽는다(고립은 도메인 네트워크가 없어 못 산다) → 협동 비용만 치르고 이득은 증발 → rb<c. 협동이 *적응*이려면 kin 이 뭉쳐야 한다("유전이 개체보다 먼저" + "개체가 막보다 먼저"의 연장: kin 구조가 협동보다 먼저).
- **개체성이 중립에서 적응으로(STATE §3 🟡 해소)** — 0018 은 개체가 응집·공유해도 *중립*(선택압 0)이었다. 이제 *협동하는 개체*(kin 도메인 안에서 m 을 떠받치는 유전형)가 혈연도가 높을 때 차등 적합도를 가져 *선택*된다 — 개체성의 첫 *적응적* 신호(사회진화의 토대).

## 4b. ⑤ 동역학 보존 — `sustain`, 통과 (생물량 공유는 실제 m 재분배이되 churn 을 안 깬다)

전체 스택(균일 협동) + R 유전 씨앗, 별 6 + 생명 5 점화 후 pop 을 지평선(35000 tick)까지 추적:

| | 창 끝 pop | 창 끝 별 | 후반 출생 | 후반 사망 | 측정 개체 | 최대 세포 | 누적 m공유 | 공멸 |
|---|---|---|---|---|---|---|---|---|
| **step-0019**(공유 on) | **62.6** | 10.0 | **5242** | **5254** | 44.2 | **4.8** | 706 | **없음**(35k 전 시드 생존) |

생물량 공유를 더해도 step-0012 의 임계 자기조직이 유지된다 — 공멸 없이 후반 출생≈사망>0(carrying capacity ~63). 생물량 공유는 *실제 m 재분배*(0015·0016·0018 처럼 실변환)지만, *쌍 거래*(나간 만큼 들어옴)라 churn 을 안 깬다. **존재론 조각(차등 협동)을 더해도 동역학이 보존**됨을 확인했다(0013~0018 과 같은 직교성).

---

## 척추 체크 4항 (SPINE.md)

1. **단일 척추 ✅** — 협동(coop)은 병렬 *구동* 필드가 아니라 step-0016 a.g 의 *표현형*(생명 속성의 함수). 공유 대상 m 은 기존 생물량(필드는 여전히 E·R). 둘째 척추 아님.
2. **활성도 환원 ✅** — 협동을 authored type(`Cooperator{}`)으로 *만들지 않는다*. 공유 *세율*만 coop(a.g)의 연속 함수 — E 동역학·활성도는 태그로 안 갈린다(inherit 의 차등 대사세와 같은 정합). 사망/번식은 여전히 m 문턱(라벨로 안 분기).
3. **국소 문턱 ✅** — 공유는 제 4-인접 kin 쌍만 본다(전역 조율자 0). 구조 판정도 제 m·이웃 m 만(국소).
4. **순환 장부 ✅** — 생물량 공유는 쌍별 m 거래(나간 만큼 들어옴) — 닫힌 장부 불변(잔차 4.22e-13). 떠받쳐진 kin 이 죽으면 m→E 로 순환(비가역 ≠ 비보존).

---

## 5. 화면에서 직접 확인하는 법

[step-0019.html](step-0019.html) 을 연다 — 3D 표준 셸. 클릭 동작 기본 = "유전 생명 놓기":

- **kin 이 서로 떠받친다**: "혼합 군집(저·고적합 ×다수)" 를 놓고 보라 — 같은 색끼리 모여 액적(개체)으로 갈린 뒤(차등 응집), 그 액적 *안에서* 부유한 kin 이 굶주린 kin 을 m 으로 떠받친다(우측 "m공유" 누적량이 오른다). 전체 스택은 *균일 협동*(coopFit0=1)이라 모든 유전형이 kin 을 떠받친다 — 굶주린 생명이 덜 죽어 액적이 더 오래 산다. "kShare" 를 올리면 구조가 강해진다.
- **kin selection 은 verify 에서**: 협동자 vs 배신자의 *차등 적합도*(선택압)는 협동을 복제 적합도와 분리한 통제 아레나에서만 깨끗이 보인다(<code>node step-0019/verify.js kin</code>: 뭉치면 협동 지속·흩어지면 사라짐, 상호작용 0.26). 화면(전체 스택)은 *균일 협동*이라 선택이 아니라 *risk-pooling 이득*(개체 생존↑)을 보인다.
- **전체 스택**: 별 ×6 + 생명 ×5 를 놓아 보라 — 부트스트랩한 생명이 kin 액적으로 묶이고, 그 안에서 막(E)과 생물량(m)을 함께 공유한다. 공유는 churn 을 안 깬다(끝없이 생사 — <code>verify sustain</code>).

---

## 6. 의외의 발견

1. **평형에선 구조가 zero-sum이라 선택압이 0 — *비평형*(기근)에서만 켜진다.** 처음엔 자원 제한 평형 아레나에서 협동을 경쟁시켰는데 선택 신호가 0이었다. 이유: 평형(carrying capacity)에선 굶주린 하나를 구조하면 *다른 하나가 대신 굶는다*(파이가 고정 — zero-sum). 구조는 *누가* 죽나만 바꾸지 *몇이* 죽나는 안 바꾼다. 협동이 *적합도*를 주려면 **비평형 사망**(파이가 줄어 모두가 굶는 기근)이 필요하다 — 그때 구조가 사망을 *막으면* 다른 하나가 대신 죽는 게 아니라 *순 생존이 는다*. boom-bust 순환을 깔자 신호가 떴다(상호작용 0.26). **협동의 이득은 평형이 아니라 *위기*에 있다** — 사회성이 환경 변동(기근·재난)의 자식인 까닭의 게임화.
2. **흩어진 협동은 *중립*이 아니라 *해롭다*(낭비된 이타).** 혈연도가 낮을 때(흩어짐) 협동자는 그냥 중립으로 떨어지는 게 아니라 *사라진다*(Δ −0.26). 고립된 협동자가 떠받친 kin 도 함께 죽어(고립은 못 산다) 협동 비용만 치르고 이득은 증발하기 때문. **kin 구조가 협동보다 먼저**다 — "유전이 개체보다, 개체가 막보다 먼저"(0017·0018)의 연장: 협동이 적응이려면 *먼저* kin 이 뭉쳐 있어야 한다(adhere→couple→share 의 사다리 순서가 인과다).
3. **균등화는 협동을 죽이고 표적 구조는 살린다 — 협동의 *형태*가 b>c 를 가른다.** 1차 구현(couple 처럼 m 을 균등화)은 선택 신호가 0이었다 — 균등화가 부유 kin 을 mDiv 밑으로 끌어내려 *번식을 깎아*(비용↑) b≈c. *표적 구조*(궁핍한 kin 의 결손분만, 번식 거의 안 깎음)로 바꾸자 b>c 가 돼 협동이 선택됐다. **"공유하면 협동이 선택된다"가 아니라 "*어떻게* 공유하느냐가 협동의 선택을 가른다"** — 이타의 *기하*(누구에게·얼마나)가 진화 가능성을 정한다(Hamilton 의 정밀화).

---

## 7. 정직한 한계

1. **협동이 *지속*은 하나 강하게 *침투*하진 않는다(hiR Δ≈0).** 뭉칠 때 협동은 Δ +0.016(거의 중립~약상승)으로 *지속*하지 배신을 빠르게 *치환*하진 않는다(이 비용/혈연도에서 b≈c+ε). 강한 양성 선택(협동이 적극 침투)은 *시너지*(공공재 — 한 기부가 여럿을 살림, 양의 합)를 요구한다(표적 구조는 보존적 재분배라 합이 0~음). kin selection 의 *문턱*은 넘었으나(혈연도 의존 확인) *강한 침투*는 백로그 — (공공재 협동: 시너지 b>>c).
2. **협동·배신이 *고정 표현형*이지 진화하는 전략이 아니다.** coop(a.g)는 통제 아레나에서 author 한 두 전략(coop 0/1)이다 — 협동 정도가 *변이·진화*하지 않는다(inheritMu=0 으로 계통 순수). 협동 자체가 돌연변이로 *생겨나고 조율*되는 OEE(열린 진화)는 미해결(백로그 — 협동 표현형의 변이·공진화).
3. **분업(division of labor)이 아니라 risk-pooling 이다.** STATE 가 "분업·m 공유"를 묶었으나, 이 step 은 *m 공유*(굶주린 kin 떠받침 = risk-pooling)만 구현했다 — 세포가 서로 *다른 역할*(생식/체세포)을 맡는 진짜 분업·분화는 아니다. 같은 genotype 이 위치에 따라 다른 phenotype 이 되는 세포 분화는 별도 step(백로그 — (b) 세포 분화).
4. **선택압이 boom-bust 변동성에 의존(평형 zero-sum).** §발견 1 대로 평형에선 선택압이 0이라, kin 신호는 *비평형*(기근 순환) 아레나에서만 깨끗하다. 전체 스택(별 구동·정상상태에 가까움)에선 협동이 *risk-pooling 이득*(생존↑)은 주되 강한 *선택*은 약하다 — 변동성 큰 substrate 라야 선택이 날카롭다(0017 한계 4 "큰 안정 막은 정착 substrate" 의 사회진화판).
5. **공유가 *center 셀 생명만*·scan 순서 의존.** couple 과 같은 한계 — 각 생명의 center 만 kin 쌍으로 보고(footprint 전체 아님), 순차 Gauss-Seidel(scan=agent 배열 순서)이라 약한 순서 의존이 있다(0017·0018 의 4-인접 scan 이방성과 같은 부류). footprint 공유·동시 갱신은 백로그.

---

## 8. 다음 (step-0020 예고)

> 가설(잠정 — 권위는 [STATE.md](STATE.md)): step-0019 이 차등 m 공유로 개체성을 *중립에서 적응으로*(kin selection 선택압) 올려 STATE §3 🟡 최우선 격차를 닫았다. 남은 큰 미완: **(공공재 협동) 시너지 b>>c** — 한 기부가 여럿을 살리는 양의 합 협동으로 협동이 *강하게 침투*(지속을 넘어 치환). **(b) 세포 분화** — 같은 genotype 이 도메인 내 위치(중심/표면)에 따라 다른 phenotype(생식/체세포 — 진짜 분업). **(R2) 곡률 기반 표면장력** — 액적이 둥글어지고 합쳐진다(형태 사다리 둘째). **(R3) G→방향성 결정화** — genotype 이 결정 방향을 편향(R 하이트필드 형태, 형태 사다리 셋째). **(d) 표현 분열 닫기**(E·R·A·m·G 같은 원시형).

step-0019 이 개체↔대사 차등 결합(kin selection)으로 개체를 *적응 단위*로 만들었다. 남은 미완은 **강한 침투(공공재 시너지)**·**세포 분화(진짜 분업)**·**곡률 표면장력**·**방향성 결정화**·**표현 분열** 이다. step-0020 은 STATE §2 가 지정한 한 조각을 따른다.

- 검증할 것: 후속 조각의 4기둥(회귀 0·장부·결정론·가설) + 척추 체크 4항. 사회진화 조각은 *측정 가설*(협동 침투·혈연도 의존·분화 지수)로 4번째 기둥을 채운다.
- 병행 백로그: 공공재 협동(시너지) · 세포 분화(생식/체세포) · 협동 표현형 변이(OEE) · 곡률 기반 표면장력 · 방향성 결정화 · 선택 투과 막 · 표현 분열 통일 · 정착 생활사 · footprint 공유.

각 step 의 "다음" 절이 다음 한 조각을 제안하지만, **현재 위치와 다음 가설의 권위는 [STATE.md](STATE.md)** 다.
