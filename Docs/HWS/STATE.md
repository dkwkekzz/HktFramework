# STATE — 살아있는 현재

> "지금 어디까지 왔고 다음은 무엇인가"의 **단일 진실 원천(SSOT)**.
> 큰 목표·규칙은 [CLAUDE.md](CLAUDE.md) · 존재론 척추는 [SPINE.md](SPINE.md) · 각 step의 상세 기록(의외의 발견·정직한 한계 전문 포함)은 `step-NNNN.md`.
>
> **구조 규칙(에이전트 효율)**: 이 파일은 *고정 크기 대시보드*다. §1~6은 step을 닫을 때마다 **덮어쓴다(rewrite)** — 누적하지 않는다. 오직 §7 INDEX만 1행 append. 발견/한계의 *전문*은 STATE가 아니라 `step-NNNN.md`에 산다(STATE = 현재의 SSOT, step 문서 = 역사의 SSOT). 마커: ✅해소 🟡부분 🔴열림(최우선) ⬜백로그 🔧노브.

---

## 1. NOW

- **닫힌 step**: [step-0024](step-0024.md) — 형태 사다리 R1(0017 차등 응집 + 0018 막/flux 결합)이 개체를 *표면장력 액적*으로 빚어 막을 *바닥 E 하이트필드*에 새겼으나 그 액적엔 *곡률 표면장력 동역학*이 없었다(0018 한계). step-0024 는 R2 **곡률 기반 표면장력(tension)**을 더했다 — 단 *position* 곡률(셀을 옮겨 둥글리기)은 차등 응집(Steinberg DAH = 그 자체가 표면장력)과 **redundant**임을 먼저 확인했다(정직한 발견 #1: adhere on/off 가 같은 최종 형태·보존적 단일-셀 이동은 에너지 장벽도 못 넘음). 그래서 R2 의 자리는 *형태가 실리는 바닥 필드 E* 다(INTERPRET §5 "형태는 flux 의 하류"). 이 step 은 **E-막에 Young-Laplace 표면장력**을 더한다 — couple(R1)이 kin 끼리 E 를 *균등화*했다면 tension(R2)은 그 위에 *곡률 구배*를 얹어 **볼록 경계(coordination=4-근방 같은 태그 수 낮음)의 E 를 오목/속(높음)으로** 민다(ΔP=γκ 의 이산 아날로그). 액적의 *E-형태*가 *경계 식고 속 차오른 둥근 돔*으로 → 같은 해석 렌즈가 그 둥근 형태를 코드 0 으로 그린다. **law-pipeline 의 열넷째 step**(`engine/hws-laws.js` 에 `tension` 법칙 1개 + 노브 2개(`kTension` 마스터·`tensionGamma` 곡률 계수) + `LAW_ORDER` ⑥a2 한 자리(adhere 뒤·couple 앞)).
- **한 줄 상태**: **형태가 필드에 실린다(R1) → 그 형태가 둥글다(R2)** — E-막에 Young-Laplace 표면장력이 볼록 경계의 E 를 속으로 민다. 가설 수치 통과(rounding 아레나, 단일 클론 16×16 조직·adhere+couple[R1] on·tension 만 격리): 볼록 경계 E **1.93→1.62**(곡률 큰 경계의 E 가 속으로 밀려 식음 = 표면장력)·속/경계 E비 coreRatio **0.219→0.263**(속이 차오름·E-돔)·고-E 핵 원형도 **0.092→0.103**·곡률 E flux **4437**. 회귀 0(kTension=0=step-0023 비트 동일, golden `anchor@`·`curv@`+`tcurv@` step-0025 앵커 신규)·잔차 **5.7e-15**(tension 은 E 쌍 거래 — 볼록→오목, 나간 만큼 들어옴, 보존)·결정론(곡률 E flux 가 E 변경) 유지. churn 보존(공멸 없음·전체 스택 carrying capacity ~57). **핵심 발견**: 차등 응집이 곧 표면장력(DAH) — *position* 곡률은 redundant, R2 는 *바닥 필드 E* 에 산다(형태는 개체 아닌 *흐름*에서 빚인다)·표면장력은 경계를 *식히지* 속을 크게 데우진 않는다(경계는 둘레·속은 넓이)·couple(R1)↔tension(R2)은 평탄화↔곡률의 *평형*.
- **다음**: **step-0025 *sim*** (§2) — 기본 조각 = **(R3) G → 방향성 결정화**(genotype 이 결정/복제 *방향*을 국소 편향 → 등방 blob 대신 가지·결정축, R 하이트필드 형태; 형태 사다리 셋째) 또는 **(R4) E↔R 튜링 불안정**(둘째 필드 없이 — 비확산 R 자기촉매 + 확산 E 두 timescale → 반점/줄무늬) 또는 **coalescence 닫기**(막 융합 — 비보존 성장·막 이동) 또는 **안정 자원 패치**(전체 스택서 정착·곡률 발현). 렌더러 트랙(물질 표현)은 이미 ✅.

---

## 2. NEXT

### step-0025 sim 가설 (▶ 즉시 다음 *sim* step)

**척추 *공간*축(네 결정)은 별을 대표로, *시간*축(다섯째 축=유전)은 0015(R 위)+0016(생명 위)으로 닫혔다. 0017 이 개체(다세포='계')의 *공간 토대*를, 0018 이 그 액적을 *물리적 flux 결합 도메인*(막)으로, 0019 이 개체를 *적응 단위*(kin selection)로, 0020 이 협동을 *지속에서 강한 침투로*(공공재 시너지, 사회 칸), 0021 이 협동을 *균일 역할에서 분업으로*(위치 의존 세포 분화), 0022 가 분화를 *위치 동적에서 불가역 상속 계통으로*(생식세포 계통 격리, Weismann 장벽), 0023 이 *떠도는 생명을 고착하는 생명으로*(정착 생활사 — 큰 안정 조직 → 분화/격리 발현의 전제), 0024 가 *형태가 필드에 실린다(R1)에서 그 형태가 둥글다(R2)로*(곡률 기반 표면장력 — E-막에 Young-Laplace 압력) 올렸다. 남은 큰 미완은 *방향성 결정화(R3)*·*E↔R 튜링(R4)*·*coalescence 닫기*·*불가역 정착 commitment*·*안정 자원 패치*(전체 스택 정착/곡률 발현)·*연속/다중 계통*·*비대칭 분열 상속*·*표현 분열*·*완전 고정·OEE*이다.**

step-0024 가 형태 사다리 R2(곡률 표면장력)를 *E-막*에 얹어 액적의 E-형태를 둥근 돔으로 모았다(*position* 곡률은 응집과 redundant — 형태는 *흐름*에서 빚인다 재확인). step-0025 후보 갈래: **(R3) G → 방향성 결정화** — genotype 이 결정/복제 *방향*을 국소 편향 → 등방 blob 대신 가지·결정축(R 하이트필드 *형태*; 형태 사다리 셋째, R 속성이라 단일 척추·노브 `kAniso`=0→회귀0·측정 이방성 지수>1). **(R4) E↔R 튜링 불안정** — 둘째 필드 없이 비확산 R 자기촉매 + 확산 E 두 timescale → 반점/줄무늬(형태 사다리 넷째). **(coalescence 닫기)** — 0024 가 *둥글림*은 했으나 *합쳐짐*(떨어진 막 융합)은 에너지 장벽에 막혀 약함(0024 한계 #2) — 비보존 성장·막 이동으로. **(불가역 정착 commitment)** — 0023 가역 게이트 → 평생 고착(따개비·산호). **(안정 자원 패치)** — 전체 스택서 정착·곡률 발현(0023/0024 한계 — 발현 부분적). **(연속/다중 계통)**·**(비대칭 분열 상속)**·**(d) 표현 분열 닫기**(🔴 근기). **sim step 의 기본 조각 = (R3) G → 방향성 결정화**(🔴 최우선 — 형태 사다리 셋째, R2 가 등방 둥글림을 했으니 이제 genotype 이 *방향*을 주어 가지·결정축을) — 나머지(R4/coalescence/불가역 정착/안정 자원 패치/연속계통/비대칭/d)는 백로그. 시리즈 작성자가 바꾸려면 여기를 교체(메뉴 아님·단일 지정).

> **시각화 정합(형태 작업의 자리)**: 두 시각화 뷰가 *같은 실루엣*인 근본 원인 = **시뮬에 형태가 없음**(높이 `h=E+R` 하나 — [INTERPRET.md](INTERPRET.md) §5). **형태 사다리: R1 막/flux 결합 ✅닫힘(0018)** — 막 결합이 *E 자체*를 재분배해 도메인+막을 **바닥 하이트필드에 새겨**, "형태가 생기면 같은 렌즈가 코드 0 으로 받는다"(§5 명제)를 처음 실현. **R2 곡률 표면장력 ✅닫힘(0024)** — E-막에 Young-Laplace 압력(볼록 경계 E→속)으로 액적의 E-형태가 둥근 돔으로(R1 "형태가 필드에 실린다"의 R2 연장 "그 형태가 둥글다"); *position* 곡률은 응집(DAH)과 redundant 라 R2 는 *바닥 필드 E* 에 산다(형태는 *흐름*의 하류). "정합"은 두 뷰를 *다르게* 만드는 게 아니라 공유 실루엣을 *형태로* 채우는 것이었다. 남은 사다리: **R3 G 방향성 결정화**(R 덴드라이트/결정축)·**R4 E↔R 두 timescale 튜링**(반점/줄무늬, *둘째 전역 필드 금지·단일 척추*)·**선택 투과 막**(M). 형태 사다리 다음 *sim* 조각 기본 = **R3**(방향성 결정화). *물질 표현*(렌더러 절반)은 ✅닫혔다 — 두 뷰가 이제 *높이부터* 갈린다(좌 `h=E+R` ≠ 우 `h=물질[R+저활성E]`, [INTERPRET.md](INTERPRET.md) §5b).
>
> **물질 표현(렌더러) 절반은 ✅ 닫혔다([INTERPRET.md](INTERPRET.md) §5b)**: 세계 해석 뷰가 **높이=물질**(`hOf(R+저활성E)`)·**빛=에너지**(고활성 E·A)로 분해(흐르는 에너지는 안 솟고 발광)·물질을 상·조성·밀도·구조로 분화. 렌더러는 형태(실루엣)를 author 안 함(분포 재성형 금지 — §4) — *완전히 다른 형태*는 시뮬(형태 사다리)이 빚어야 렌즈가 코드 0 으로 받는다.

**큰 호 — 사람급 복잡 적응계로 가는 *각***: 0015·0016 이 다윈 동역학을 생명으로, 0017 이 개체의 *공간 토대*를, 0018 이 *물리적 도메인*(막)으로, 0019 이 *적응 단위*(kin selection)로, 0020 이 협동을 *강한 침투*(공공재 시너지)로, 0021 이 협동을 *균일 역할에서 분업으로*(위치 의존 세포 분화), 0022 가 분화를 *위치 동적에서 불가역 상속 계통으로*(생식세포 계통 격리, Weismann), 0023 이 *떠도는 생명을 고착하는 생명으로*(정착 생활사 — 큰 안정 조직 → 분화/격리 발현의 전제), **0024 가 *형태가 필드에 실린다(R1)에서 그 형태가 둥글다(R2)로*(곡률 기반 표면장력 — E-막에 Young-Laplace 압력)**. 사다리(복제분자→염색체→다세포(개체)→**사회→분화된 다세포**→인지)에서 우리는 *분화된 다세포 칸* 안에 들었고, 그 위에 *형태*를 빚기 시작했다(R1 막 → R2 둥글림) — 다음은 *방향성 형태*(R3 방향성 결정화 — 등방 blob 에서 가지·결정축으로). 정직: 곡률이 *E-형태*만(셀 배치 아님)·coalescence 약함(에너지 장벽)·전체 스택서 부분적(희소)·고정 규칙(OEE 아님)·position 곡률은 응집과 redundant(형태는 흐름에서) — *각은 있으나 도달 보장 아님*. 상세 = [SPINE.md](SPINE.md) §다섯째 축.

**검증할 것**: ① 가설 수치(후속 조각의 의도 현상 — 방향성 결정화 조각은 *측정 가설*: genotype 이 결정 방향 편향 → 이방성 지수>1) ② **회귀 0**(새 노브=0 → step-0024 비트 동일, golden 가법 잠금 — `curv@`·`tcurv@` 신규 앵커 이미 기록됨) ③ 닫힌 장부·결정론 ④ 척추 체크 4항.

**병행 백로그(블로킹 아님)**: ⬜ R3 방향성 결정화(genotype 이 결정 방향 편향 → R 하이트필드 가지·결정축) · ⬜ R4 E↔R 튜링 불안정(반점/줄무늬, 둘째 필드 금지) · ⬜ coalescence 닫기(떨어진 E-막 융합 — 0024 둥글림은 했으나 합쳐짐은 에너지 장벽에 막혀 약함, 한계 #2) · ⬜ 불가역 정착 commitment(0023 가역 게이트 → 평생 고착) · ⬜ 안정 자원 패치(전체 스택서 정착·곡률 발현) · ⬜ 연속/다중 분화 계통(계통 트리 — 0022 는 germ/soma 이분) · ⬜ 비대칭 분열 상속(부모가 자식 fate 를 비대칭으로) · ⬜ 완전 격리 빈틈(외딴 soma, 0022 한계 #4) · ⬜ 협동/분화/격리/정착/곡률 표현형 변이(OEE — 고정 전략 아닌 진화) · ⬜ 완전 고정 정량 · ⬜ 공간 assortment 공공재 · ⬜ 선택 투과 막(불투과 장벽) · ⬜ 표현 분열 통일(E·R·A·m·G·coop·soma·sessile 같은 원시형) · ⬜ 자기복제 기원(abiogenesis) · ⬜ 다봉 적합도 지형(OEE) · ⬜ 측정의 소비(A→법칙) · ⬜ 생명/결정 FSM · ⬜ 등방성/footprint export·깊은 핵 흐름(scan-order·center-셀 한계) · ✅ 막/flux 결합 R1(0018) · ✅ 곡률 표면장력 R2(0024) · ✅ 물질 표현 정합(렌더러 트랙, [INTERPRET.md](INTERPRET.md) §5b).

**빌드 인프라 — law-pipeline(0011~)**: 한 step = `engine/hws-laws.js` 에 **법칙 1개 + 노브 + `LAW_ORDER` 한 자리**(노브=0 → early-return = 회귀 0) + `step-NNNN/`(verify.js + 작은 panel.js + ~13줄 셸 HTML). 공통 sim 은 `engine/hws-kernel.js`(동결 헬퍼) · `hws-laws.js`(법칙) · `hws-sim.js`(조립), 공통 UI 는 `hws-ui.css`·`hws-ui.js`·`hws-3d.js` 한 곳. 셸: `HWS3D.bind(HWS.mount(window.HWS_SIM, HWS3D.attach(panel)))`. **회귀 앵커=동결 해시**: `node engine/validate/verify-sim-engine.js`(law-pipeline≡step-0010 비트 동일 + `golden-sim.json`: `std@`=0010 등가, `endo@`=0011 내생, `cwd@`=0012 자기제한, `fsm@`=0013 연소FSM, `flux@`=0014 계량, `gene@`=0015 R-주형 복제, `life@`=0016 생명 유전, `org@`=0017 차등 응집, `mem@`=0018 막/flux 결합, `share@`=0019 생물량 공유, `pub@`=0020 공공재 협동, `diff@`=0021 세포 분화[전체 스택·희소라 분화 거의 미커버], `tdiff@`=0021 조밀 클론 조직[differentiate 실활성], `germ@`=0022 생식세포 계통 격리[전체 스택·희소라 격리 약하게 커버], `tgerm@`=0022 조밀 클론 조직[sequester 실활성 — 계통 코드 경로 동결], `anchor@`=0023 정착 생활사[전체 스택·희소라 정착 거의 미커버], `tanc@`=0023 정착 settling 아레나[anchor 실활성 — 정착 코드 경로 동결], `curv@`=0024 곡률 표면장력[전체 스택·희소라 곡률 약하게 커버], `tcurv@`=0024 rounding 아레나[tension 실활성 — 곡률 E flux 코드 경로 동결]). 출발 템플릿 `step-0024/{panel.js,verify.js}`. 장부/해시 확장은 *미존재 시 no-op* 가법만(golden 새 키 자동 기록, 기존 키는 비교; 상태 라벨은 `state!==undefined`·활성도 A 는 `fluxInit`·유전형 G 는 `geneInit`·생명 유전형 a.g 는 `lifeGeneInit`·계통 fate a.soma 는 `germInit` 일 때만 먹임; 정착 a.sessile·곡률 E flux 는 *가법 불필요* — 정착은 매 tick 게이트라 위치·m 이, 곡률은 E 가 이미 해시에). 스펙 [engine/PANEL.md](engine/PANEL.md) · [engine/DESIGN-3D.md](engine/DESIGN-3D.md). (0001~0010 복사 코어는 동결 — 닫은 step 불변.)

---

## 3. OPEN GAPS — 열린 격차 (척추 용어, 매 step 하나씩 메움)

| 마커 | 격차 | 상태 |
|---|---|---|
| ✅ | **저장체(평형) [결정2]** | step-0008 해소 — 결정화로 R 도입. 흐름0에서 존재(보존율 0.56 vs 소산 0.14). |
| ✅ | **저장체가 무대** | step-0009 해소 — 기복(h=E+kRelief·R)으로 퇴적이 흐름을 휨. 퇴적 전선 영구 떠돎. *단 촉매는 아직 아님*(R 위 생존 문턱 불변) → 백로그. |
| ✅ | **greedy 국소 최대 함정** | step-0010 해소 — 탐사(run-and-tumble): 갇힌 굶주린 생명이 의사난수 한 칸으로 옅은 골짜기를 건넌다(escape: 트랩 생존 vs greedy 사멸, 전 시드). step-0005 이래 5 step 이월 격차 닫힘. |
| ✅ | **별이 외부 주입** | step-0011 해소 — 구동 내생화(별: R 누적 핵에서 점화하는 내생 주입원, 연료 소진까지 서행). 외부 source 없이 별이 세계를 먹인다(누적 주입 ~207k). 영구 전역 봉우리 소멸 → 측정 창 churn 634배·무게중심 떠돎 20배(G2/G3 *방향* 해소). |
| ✅ | **음성 피드백 부재 → 과증식-공멸** | step-0012 해소 — 밀도 의존 자기제한(crowd: 국소 밀도 비례 대사세 → 솎임 → carrying capacity ~73). step-0011 의 공멸(~12700 tick)이 사라지고 지평선까지 생존. 핵심: crowdR 을 *채식지 척도*(=starR 3)로 — R=2 는 국소 솎임이라 채식지를 덮어 공멸 지연뿐(carrying capacity = 개체수 아닌 *공간 분포* 문제). |
| ✅ | **생명 churn — 끝없는 churn (G2/G3)** | step-0012 해소 — 자기제한이 과증식을 묶어 *임계 자기조직*에 듦(공멸 없음·후반 출생/사망 1491/1489 ≈균형·별 10 유지·maxR>ignThresh, 단일 시드 184k 정상상태). 동결(0010·너무 적음)→공멸(0011·너무 많음)→임계(0012·그 사이) 세 위치가 *구동 구조×조절*에서 닫힘. |
| ✅ | **이산 비가역 문턱 [결정3]** | step-0013 해소(별에서) — 별 연소를 이산 FSM(living→burning→ash)으로. 문턱에서 딱 뒤집힘(연속 변조 아님)·비가역(back=0). 결정2↔3 정합 수치: labelViol=0(분기 부재)·throughput 비=livingFrac(활성도 환원)·히스테리시스 폭 0.20(anti-chatter). 회귀 0(kFSM=0=step-0012, golden cwd@)·잔차 4.48e-13. **단 별만** — 생명/결정 FSM 은 백로그(결정3 전개). |
| ✅ | **활성도(throughput) 미측정 [결정1·2]** | step-0014 해소 — 척추 변수 E 의 flux(|dE/dt|)를 활성도 필드 A 로 EMA 계량. 분류 창발: A_burn/A_store=26.1(소산/저장체, authored enum 없이 측정으로)·fracBR=1.00(흐름이 정체성보다 분류). 회귀 0(kFlux=0=step-0013, golden fsm@)·잔차 4.48e-13(비트 동일)·A 가법 해시. **단 *관찰*만** — 측정을 법칙이 *행동*에 쓰는 단계(소비)는 백로그. |
| ✅ | **유전(heredity) [다섯째 축·전략]** | step-0015 *R 위 토대* + step-0016 *생명 위 결합* — R-주형 자기복제(R)·genotype↔대사(생명)로 유전·변이·선택·적응 창발(authored 엔진 없이). R: fidelity 1.00·ratio 1.53·적응 +0.13. 생명: fidelity 1.00·ratio **3.27**(생사 churn 으로 더 날카로움)·적응 **+0.31**. **남은 미완**(별 격차로): 적합도 단봉·authored(OEE 아님)·phenotype 1차원(→ (c) `G` 방향성 결정화가 *공간/형태* phenotype 으로 첫 확장)·기원(abiogenesis) author. [SPINE.md](SPINE.md) §다섯째 축. |
| ✅ | **개체(다세포='계') 공간 토대 — 0-D 점이 다세포 도메인으로** | step-0017 해소(공간 토대) — 차등 응집(kin↑/타↓, Steinberg DAH=Schelling)이 같은 유전형 생명을 정렬 → 표면장력 액적(kin 연결 성분)이 창발하고 개체를 *측정*(author `Organism{cells}` 금지, 척추 체크 2). cell sorting kin 접촉 0.51→0.95·개체 평균 4.5·최대 21세포(통제 1.7·11). **남은 미완**: 막은 측정 선일 뿐(↓ step-0018 해소)·개체 진화적 중립(↓ 🟡)·전체 스택 개체 작음. |
| ✅ | **개체='계'가 *측정 윤곽*일 뿐 — 물리적 막/도메인 없음 [형태 사다리 R1]** | step-0018 해소 — 막/flux 결합(같은 유전형 kin 4-인접 생명끼리 *필드 E 국소 공유·재분배*, 쌍 거래)으로 액적이 *물리적 flux 결합 도메인*으로: 내부 E 균질화(|ΔE| 0.125→0.075)·경계 단차(**막**) 창발(막 지수 1.98→3.46, author 안 함). 형태가 처음 *바닥 E 하이트필드*에 실려 렌즈가 코드 0 으로 받는다(INTERPRET §5 첫 실현). **남은 미완**: 막이 부드러운 단차일 뿐(선택 투과 ⬜)·곡률 표면장력 없음(⬜)·전체 스택 막 출렁임(희소). |
| ✅ | **개체가 진화적으로 *중립* — 이득은 생겼으나 *선택압*은 0** | step-0019 해소 — 생물량 공유(차등 m 공유): kin 끼리 *m* 을 *유전형의 함수*(coop)로 + 표적 구조로 공유 → 협동이 차등 적합도(kin selection 선택압). 협동의 운명이 *혈연도 의존*(Hamilton rb>c): 협동의 효과가 뭉치면 ≈0·흩어지면 −0.267(낭비된 이타로 사라짐), 상호작용 0.263(2×2 요인설계), 두 기준선(협동 off, 뭉침·흩어짐) 모두 중립. 개체가 *중립→적응*. **남은 미완**: 협동이 *지속*만(강한 침투 아님 ↓ 0020 해소)·평형 zero-sum(비평형서만 선택)·분업 아닌 risk-pooling(세포 분화 ⬜). |
| ✅ | **협동이 *지속*만 — 강한 *침투*(치환) 아님(공공재 시너지 부재)** | step-0020 해소 — 공공재 협동(pubgood): 협동자가 제 잉여를 *기부*해 kin *여럿*에게 나눠 주되 *시너지로 증폭*(기부 m→m + (syn−1)배를 *필드 E 에서 채집*, E→m). 한 기부가 여럿을 살려(양의 합 b≫c) 협동이 *지속→강한 침투*. 침투의 세기가 *혈연도 의존*(positive assortment): 공공재의 효과가 뭉치면 +0.299(강한 침투 — 0019 의 지속 ≈0 넘어섬)·흩어지면 +0.051(~중립), 상호작용 0.248(2×2 요인설계), 두 기준선 중립. 사회 칸 진입. **남은 미완**: 완전 고정 아님(강한 침투까지 ⬜)·kin-지향(greenbeard ⬜)·분업 아닌 공동 채집(↓ 0021 해소). |
| ✅ | **협동이 *균일 역할*만 — 진짜 *분업*(같은 genotype 다른 phenotype) 아님** | step-0021 해소 — 세포 분화(differentiate): 각 생명이 제 4-근방 점유(빈칸 수)를 *위치 정보*로 읽어 — 4-근방 다 찬 *갇힌 내부*(번식 불가)는 soma(체세포)로 잉여를 4-근방 kin 에게 기부(m→m 보존), 빈칸 있는 *표면*은 germ(생식)으로 번식. genotype↔phenotype 이 1:1 을 깸(phenotype=f(genotype, 위치)). 통제 분화 아레나(단일 클론 조밀 조직): 위치가 phenotype 가른 폭 dGap +0.318(roleGap −0.366→−0.047)·표면 germ m 배증(0.254→0.529)·분업 이득 개체군 2928→3006(사장 생물량→번식). 분화된 다세포 진입. **남은 미완**: 위치 기반 *동적* 분화(불가역 계통 격리 germline ↓ 0022 해소)·germ/soma 이분(연속 분화 ⬜)·조밀 조직서만(정착 생활사 ⬜)·germ 방향이 격자 기하 산물. |
| ✅ | **분화가 *위치 기반 동적*만 — 불가역 *계통 격리*(Weismann) 아님** | step-0022 해소 — 생식세포 계통 격리(sequester): 유전형 있는 생명이 *위치 무관*하게 한 번 불가역 fate(germ/soma)를 받고(시드 의사난수<kGermline 면 soma·영원히 불변), soma 계통은 제 잉여를 4-근방 germ kin 에게 *전량* export(m→m 보존)해 m 이 mDiv 밑에 묶여 *번식 임계에 영영 못 닿는다* → 번식이 germ 전용(Weismann, reproduce 동결 — author 분기 없이 *창발*). 통제 격리 아레나(단일 클론 조밀 조직): *표면 세포 중* soma 계통 0.44(0021 위치 분화면 0 — fate 가 위치 아닌 계통)·soma 최대 m 0.758<mDiv 0.90(전 시드 Weismann PASS)·계통 할당 0.588≈kGermline·출생 3952→4506(+14%, 사장 잉여→germ 번식). 분화된 다세포 깊이 진입. **남은 미완**: 계통 둘뿐(연속/다중 계통 트리 ⬜)·할당 무작위 확률(비대칭 분열 상속 ⬜)·외딴 soma 격리 빈틈. |
| 🟡 | **분화/격리가 *조밀 조직에서만* — 전체 스택서 큰 안정 조직이 안 섬(0017 이래 반복 블로커)** | step-0023 *부분 해소* — 정착 생활사(anchor): 잘 먹은(m≥anchorM) kin-포위(같은 태그 4-근방≥anchorKin) 생명이 *고착*(이동·탐사·재정렬 보류)해 떠돌지 않으면 이웃 kin 이 둘레에 쌓여 *큰 안정 confluent 조직*으로 coalesce → 갇힌 내부 세포 늘어 0021 분화 더 발현. settling 아레나(정적 먹이): 고착 0.66·조직 평균 26.9→43.8·조직 수 18.6→10(coalescence)·갇힌 내부 0.79→0.84·분화 provision 240k→261k. 위치만(거래 0)이라 churn 보존. 블로커를 *열었다*. **남은 미완(🟡)**: *전체 스택은 여전히 희소*(carrying capacity ~71 — 정착 거의 안 켜짐, 발현 부분적)·정착이 번식·개체수와 맞바꿈(안정 자원서만 이득 — 떠도는 먹이엔 짐)·가역 게이트(불가역 commitment ⬜)·전체 스택 발현엔 안정 자원 패치 ⬜. |
| 🔴 | **표현 분열 [근기 정리]** | 연속 패턴(E·R·A)과 에이전트(생명 m·a.g·coop)와 유전형(G)·개체(연결 성분)가 서로 다른 자료형 — 척추 "개체는 같은 원시형" 미완. 0016 genotype 공유·0017 개체 측정·0019 협동을 a.g 표현형으로 *좁힘* → 근기 정리 → **step-0020 후보 (d)**. |
| ⬜ | **측정의 소비 [결정2 행동]** | step-0014 는 측정으로 분류 창발을 *관찰*했으나 그 분류를 *법칙*이 행동에 안 씀(A 읽기 전용). authored 분기를 측정 분기로 대체하는 후속(step-0017 후보 d). |
| ⬜ | **결정3 전개 — 생명/결정 FSM** | step-0013 은 *별*만 이산 FSM. 생명(living/dormant)·결정(solid/melt)은 아직 연속/가역 — 결정3 을 전 개체로 확장하는 후속(step-0017 후보 e). |
| ⬜ | **gross 통과 flux 미계량** | step-0014 활성도는 *net* dE/dt — 정상 흐름(in/out 균형, net≈0·gross 큼) 셀을 저활성으로 본다. 이 세계엔 드물어 분류 깨끗했으나 관류형 개체엔 gross 계량 필요할 수 있음(백로그). |
| ⬜ | **방향성 드리프트 추적** | step-0012 에서 *불필요*로 강등 — 자기제한만으로 끝없는 churn 이 닫혔다. 시간 감지형 run-and-tumble 은 *효율*(좌초↓)을 줄 수 있으나 지속에 필수 아님 → 백로그. |

**게임화 cross-cutting(step 진행과 별개로 누적, 상세 step 문서):**
- **G1 timescale** 🔧노브 — 폭발이 빠름(씨앗1→정점 ~33tick). 게임은 world-tick을 렌더와 분리 + rate 상수 재튜닝. 다운스트림.
- **G2 동결** ✅ — 정착 후 얼어붙음. step-0007 맥동·0009 풍경·0010 탐사·**0011 구동 내생화(전역 봉우리 제거 → churn 634배)** 거쳐, **step-0012 자기제한으로 동역학적 종결**: 공멸 없이 후반 출생≈사망>0 지속(임계 자기조직). 동결(너무 적음)도 공멸(너무 많음)도 아닌 *그 사이*에 안착.
- **G3 한 점 수렴** ✅ — greedy가 source로 뭉침. 0006~0010 완화 거쳐 **step-0011이 원인 제거**(외부 source→떠도는 필멸 별, 무게중심 떠돎 20배), **step-0012가 공멸 제거**로 그 떠돎이 *영속*한다(carrying capacity 가 수렴 없이 떠도는 개체군을 유지).

> **원리·아키텍처는 적절**(보존 장부·결정론·서버 권위·점진적 법칙·자원/생명 창발) — 게임 substrate로 타당. 부적절한 건 *절대 timescale(G1, 노브)* 과 *미완 동역학(G2·G3, 다음 step들이 메움)*.
>
> **상시 렌즈 — 척추** ([SPINE.md](SPINE.md) 필독): 개체는 *이름(별/저장체/생명)이 아니라* 활성도 연속축 + 국소 비가역 문턱으로 갈리고, 빠른 비가역 전이 + 느린 재구성으로 세계 척도에서 순환한다. 매 step은 가설 4기둥 + 척추 체크 4항(단일 척추 / 활성도 환원 / 국소 문턱 / 순환 장부)으로 판정.

---

## 4. DURABLE CONSTRAINTS — 모든 step이 지킬 정전(canonical) 사실

> 여러 step이 반복 참조하는 불변. 새 step은 이걸 어기면 척추에서 벗어난 것 — 재설계.

- **회귀 0**: 새로 더한 항을 끄면(계수=0) 직전 step과 **비트 단위 동일**(verify `reg`, maxDiff=0).
- **닫힌 장부**: E+R+T 보존 잔차 ≤ ~1e-13(미시 1e-6 / 거시 1e-7).
- **결정론**: 같은 시드 2회 → 전 셀 비트 비교 + 상태 해시 일치. **시드 결정론 — `Math.random` 금지.**
- **수치 = verify 출력**: 모든 문서 수치는 시드 [42, 7, 1234, 99, 2026] 평균으로 재현.
- **greedy 이동 = 국소 최대 함정** *(step-0010 해소)*: greedy는 옅은 골짜기 너머 더 큰 고임을 못 찾음(step-0005~0009 반복) — **탐사(run-and-tumble)가 해소**(escape): 구배가 없으면 무작위 보행만 남고(필드 E의 운동을 생물량이 물려받음) 굶주린 생명이 골짜기를 건넌다.
- **동결을 푸는 건 운동이 아니라 *비균일성/불연속*** — 매끄러운 균일 표류는 갈릴레이 불변으로 정적 세계와 동일(step-0007).
- **"능력 추가"는 정적 끌개로 수렴 → 구조를 바꿔야 한다** — 이동·비용·탐사(0005·0006·0010)를 다 줘도 정적 source에선 동결. **step-0011이 구조를 바꿔(구동 내생화) 동결을 풂** — 영구 전역 봉우리 제거 = 수렴점 소멸(churn 634배).
- **구동 내생화는 동결을 풀되 *공멸*을 부른다(step-0011) → 자기제한이 닫는다(step-0012)** — 외부 source의 *고정성*이 실은 carrying capacity(K)의 숨은 원천이었다(내생 구동엔 없어 공멸). step-0012가 생명에 *밀도 의존 자기제한*을 줘 K 를 생명 자신의 혼잡에서 창발시킨다 — **끝없는 churn = 동결도 공멸도 아닌 임계 자기조직** 완성.
- **carrying capacity 는 개체수가 아니라 *공간 분포*의 문제(step-0012)** — 자기제한을 *국소*(crowdR<채식지)로 주면 개체수는 줄어도 살아남은 생명이 채식지를 덮어 *공멸 지연뿐*. *채식지 척도*(crowdR=starR)로 흩뜨려야 채식지에 E 가 남아 결정화→연료(R) 재충전→지속. "몇 마리"가 아니라 "어떤 척도로 흩어졌나"가 지속을 가른다.
- **절제가 풍요보다 오래 산다(step-0012)** — 생명에 추가 *비용*(혼잡세)을 물려 더 죽게 만든 것이 개체군을 *영원히 살게* 했다. 솎이지 않은 풍요(0011)는 공멸, 솎인 절제(0012)는 지속. 끝없는 churn 의 maxR 은 *총 R 불변*이 아니라 *점화 가능 peak 의 정상상태*(흩어진 R 배경은 풍화돼도 활성 채식지가 peak 재충전).
- **별 연료 = 외부 질량(주입 경계)** — 별의 에너지는 닫힌 장부 밖(제 질량), 태워 E로 주입하면 `injected`로 추적(외부 source와 같은 경계). 내생화는 에너지 *원천*이 아니라 *언제·어디서*(R 누적)를 세계 상태로 결정하는 것. R은 별의 점화 신호(siting)지 소비 대상이 아니다.
- **law-pipeline 표준(0011~)** — 새 step = `engine/hws-laws.js` 법칙 1개 + 노브 + `LAW_ORDER` 한 자리(노브=0 → early-return = 회귀 0). 회귀 앵커 = 동결 *해시*(`verify-sim-engine.js` + `golden-sim.json`: std@/endo@/cwd@), 동결 파일 아님. 장부/해시 확장은 미존재 시 no-op 가법만(상태 라벨도 `state!==undefined` 일 때만 해시에 먹임 → FSM off 면 과거 해시 불변). (0001~0010 복사 코어 동결.)
- **이산 FSM = 라벨이지 타입(step-0013, SPINE 결정3)** — 상태(living/burning/ash)는 *활성도 배수의 함수*지 자유 태그가 아니다(labelViol=0: 라벨==배수 인덱스). 켜지는 국소 법칙은 *스칼라 배수 + 게이트*뿐, 별도 에너지 경로 분기 금지(결정2 정합 a). burning/ash 는 *측정 throughput(dE/dt)* 으로 환원(b). 이산성은 *문턱·timescale 분리*에서 창발 — 점화 빠른 snap·소진 느린 연료, 두 문턱 벌려(burnOn>burnOff, 폭>0) anti-chatter(c). 새 FSM 을 더할 땐 verify 가 (a)(b)(c) 를 수치로 통과해야 닫는다.
- **불은 연료가 다해야 꺼진다(step-0013)** — 스스로 선택된 burning 별은 전율 주입으로 제 핫코어를 달궈 조기 quench(burnOff) 문턱 밑으로 잘 안 떨어진다(ash 99% 연료 소진·1% 핫코어). FitzHugh–Nagumo: 빠른 변수(점화)는 snap, *느린 변수(연료)가 사이클을 끈다*. burnOff 는 히스테리시스 폭(anti-chatter)엔 필수지만 소진 경로론 미미.
- **별 문턱이 두 층(step-0013)** — *siting*(R 누적 핵에서 별이 *선다*, ignThresh — R 공간)과 *flashover*(선 별이 *전율 연소로 뒤집힌다*, burnOn — E 잔열)는 다른 문턱. 활성도 축 위 "존재"와 "활성"이 갈린다. living(kindling)은 siting 됐으나 flashover 전 — 정지해 핫코어를 쌓아야 도달(떠돌면 흩어져 못 참).
- **존재론↔동역학 독립(step-0013)** — 이산 FSM(존재론 조각)을 더해도 step-0012 의 끝없는 churn(동역학)이 보존된다(40k 전 시드 생존·후반 출생≈사망). 두 축은 직교 — 존재론 조각이 동역학을 깨지 않음을 확인.
- **활성도 = 척추 변수 E 의 flux, 분류는 측정으로(step-0014, SPINE 결정1·2)** — 활성도 A = E 의 통과 throughput(net dE/dt) EMA. 재고(E)와 흐름(A)은 다른 양 — *분류 신호는 흐름(A)*. 저장↔소산은 authored enum 아니라 *A 의 두 극단*으로 읽힌다(A_burn/A_store=26.1). 새 *측정* 필드를 더할 땐 verify 가 두 극단 분리를 수치로 보여야 닫는다(결정2). A 는 *연속축*(이분 enum 아님) — 생명·확산은 가운데.
- **측정은 부작용 0 — A 는 읽기 전용 계기(step-0014)** — 활성도 A 는 E/R/agent 동역학에 *되먹이지 않는다*(둘째 척추 금지 — 필드는 E 하나). 그래서 kFlux on/off 가 동역학을 비트 단위로 안 바꾼다(sustain 동일). 측정으로 분류를 *관찰*함과 그 분류를 법칙이 *소비*함은 다른 단계 — 0014 는 관찰만.
- **저장체의 "활성도≈0"은 R 아닌 *흐름 끊긴 자리*의 성질(step-0014)** — A_store(정착 R)≈A_quiet(정착 세계 전체)≈A_bg — 흐름이 가라앉으면 R 유무와 무관히 저활성, *소산(별)만* 활성도 축 높은 끝에 홀로. 활성도 축은 "무엇이 있나" 아닌 "흐름이 사나"를 잰다. 별 연소 셀의 100%가 R-rich(같은 substance)인데도 A 26배 — *흐름이 정체성보다 분류*(SPINE: 같은 자리가 저장·소산 동시에, 활성도가 순간 역할 읽음).
- **유전 = 저장(R)의 씨앗 끝, 이산 정보(step-0015, SPINE §다섯째 축)** — 유전은 별도 필드가 아니라 *R 의 능력*: 자기복제하는 R-배치(genotype=이산 태그 G)가 제 사본 결정화를 *촉매*한다(복제=E→R 쌍 거래+태그 복사). 유전은 *이산*을 요구한다 — 연속 필드(E·R·A)는 확산·EMA 로 번져 정보를 못 지키나(fidelity<1), 이산 태그는 복사 연산이라 세대 무한히 건너도 비트 보존(mu=0 → fidelity 1.00). 다양성(genotype)은 *병렬 필드 아닌 속성*(태그)에 — 단일 척추(필드는 E, G 는 R 의 라벨). 새 *유전* 속성을 더할 땐 verify 가 충실 복제(fidelity)·변이율(≈mu)·선택(차등 번식)을 수치로 보여야 닫는다.
- **다윈 동역학은 author 가 아니라 창발(step-0015)** — "유전·변이·선택"을 코드에 박지 않는다. 복제(태그 복사)·복제오류(±1 변이)·기질경쟁(빈칸+E 문턱)이라는 *국소 법칙*만 깔면 그 위에서 창발한다. genotype→phenotype 맵(fit=태그 함수)은 authored type 분기 아님 — E 동역학은 태그로 안 갈리고(활성도 환원 유지), 복제 *속도*만 태그의 함수. 변이+선택 함께 돌면 *적응*(평균 적합도 상승)이 창발(끌개 항상성을 넘는 첫 *방향 있는 축적*). **단 적합도 단봉·authored → OEE 아님**(다봉·공진화 지형+풍부한 phenotype 필요).
- **유전 정보는 에너지 순환의 *승객*이지 *기관차* 아님(step-0015·0016)** — 복제·표현형세는 활성도 A 의 *읽기 전용*과 달리 **실제 변환**(E→R 굳힘·m 깎음)이지만, 유전형은 이미 도는 순환에 *올라탈* 뿐이다. R 위(0015): 유전형이 substrate 절반 잠가도 churn 비트 보존. 생명 위(0016): 표현형세는 실제 대사 변환인데도 *약한 결합*(inheritCost 0.02)이라 churn 보존(0.05 는 한계 개체군을 공멸로 민다 — 선택이 carrying capacity 음성 피드백을 보탬). *선택은 churn 을 조향하지 구동하지 않는다.* 존재론 조각(유전)이 동역학을 안 깬다(0013·0014 와 같은 직교성).
- **유전↔생명 결합 = genotype 을 대사 엔진에 묶음, 단단한 자기복제(step-0016, SPINE §다섯째 축)** — step-0015 의 R-genotype 을 생명(m, 활성도 가운데)에 *세 결합*으로 묶는다: 부트스트랩(생명이 제가 선 R-genotype 을 읽음 — Cairns-Smith)·상속(분열 자식이 인접 부모 태그를 ±변이로 물려받음 — reproduce 동결, 부모 위치가 매개)·표현형→대사(fit 낮을수록 차등 대사세, m→metabolized 쌍 거래). 같은 태그·적합도 맵 공유(새 유전 시스템 아님 — genotype 담체 이동 R→생명, 표현 분열 한 칸 좁힘). 다양성은 *병렬 필드 아닌 속성*(a.g) — 단일 척추. 새 *생명 유전* 속성을 더할 땐 verify 가 상속(fidelity)·변이율(≈mu)·선택(차등 생존 ratio)·적응(평균 적합도↑)을 수치로 보여야 닫는다.
- **죽음이 있어 선택이 더 강하다 — 생명(소산 가운데)이 R(저장 극단)보다 다윈 무대로 알맞다(step-0016)** — 같은 적합도 맵인데 선택비가 R(1.53)보다 생명(3.27)에서 *두 배 날카롭다*. R-주형 복제는 빈칸을 채우면 *멈춰*(성장기 일회 경쟁) 포화 후 언다(0015 한계 6). 생명은 carrying capacity 아래서 *연속 회전*(생사 churn)해 적합 유전형이 부적합을 *지속 치환*한다(경쟁 배제가 끝까지). 지속 다윈 적응엔 *동결 안 되는 회전*(소산 동역학)이 필요 — 저장 극단은 정보를 *보존*하나 회전이 멎어 선택이 일회적, 소산 가운데(생명)는 회전이 지속돼 선택이 누적적.
- **전체 스택에선 모든 생명이 R-genotype 에서 부트스트랩(step-0016, geneCells=pop)** — 통제 아레나는 a.g 를 author 하나, 전체 스택은 생명이 *빠짐없이* 제가 선 R-genotype 을 읽어 유전형을 입는다 — 별→결정화로 빚은 R 위에 생명이 살기 때문(채식지=R 핵). 유전과 생명이 *같은 자리*(R-rich)에 사는 substrate 의 귀결 — "유전이 개체보다 먼저"(R 위 유전)가 깔려 있어 생명이 깨면 곧장 유전형을 입는다(Cairns-Smith 게임화).
- **개체='계'는 author 가 아니라 차등 응집이 빚은 표면장력 액적으로 *측정*(step-0017, SPINE 주요 전이 사다리 유전→개체)** — 다세포 개체를 `Organism{cells}` 컨테이너로 *만들면* 활성도 환원(척추 체크 2) 위반이다. 대신 차등 응집(kin↑/타↓, Steinberg DAH = Schelling 분리: 같은 유전형 a.g 선호 + 빈칸 이주)이라는 *국소 위치 법칙*만 깔면 cell sorting → 같은 태그 블롭(표면장력 액적 = flux 결합 도메인)이 *창발*하고, 개체는 그 *4-인접 kin 연결 성분*(`measureOrganisms`)으로 *측정*해 읽는다. "표면장력"·"막"을 코드에 안 박는다 — 이종 경계 최소화가 kin 선호에서 창발(개체 먼저·막은 액적 표면으로 뒤). "유전이 개체보다 먼저": a.g(kin)가 정의돼야(0016) 다세포가 묶인다. 새 *개체* 측정을 더할 땐 verify 가 cell sorting(kin 접촉↑)·개체 크기(연결 성분 ≫ percolation 배경)를 수치로 보여야 닫는다.
- **차등 응집은 위치만 — 장부 거래 0, 개체는 동역학의 승객(step-0017)** — 응집은 m·E·R 을 안 건드리고 *위치만* 재배치한다(move/tumble 과 같은 거래 0 경계 → 잔차 3.98e-13 불변·해시 새 항 0). 실제 위치 재배치(A 읽기전용과 다름)이되 장부 거래가 0이라 임계 자기조직(끝없는 churn)을 안 깬다(공멸 없음·carrying capacity ~69). 개체(존재론 조각)가 동역학을 안 깬다(0013·0014·0015·0016 과 같은 직교성). *개체는 churn 에 올라탈 뿐 구동하지 않는다* — 응집해도 생존 이득 0이라 진화적으로 *중립*(개체↔대사 결합 전까지, §3 새 🔴).
- **DAH(세포)=Schelling(사회)이 한 알고리즘·개체는 능동 정렬의 산물(step-0017)** — 차등접착(세포생물학)과 사회 분리(경제학)는 *같은 국소 법칙*(같은 종 선호 + 빈칸 이주)의 두 발현 — `adhere` 한 함수가 둘 다(이름이 아니라 역할, 척추 그대로). 또 개체는 밀도의 우연(percolation 잔클러스터, 통제 최대 11세포)이 아니라 *능동 정렬*(응집 최대 21세포, 2.6배)의 산물이라야 의미 있다 — 측정된 개체가 percolation 배경을 *넘어서야* 한다. 개체 크기는 substrate 의 *밀도·이동성*에 달림(통제 조밀·정적 평균 4.5 vs 전체 스택 희소·이동성 1.5) — 큰 다세포는 정착 생활사를 요구할 수 있다.
- **막은 author 가 아니라 측정 — kin E 공유가 빚은 창발 경계(step-0018, 형태 사다리 R1)** — "막"·"표면장력"·"장벽"을 코드에 박으면 author. 막 = 같은 유전형 kin 4-인접 생명끼리 *필드 E 를 국소 공유·재분배*(`couple`, 쌍 거래)하면 내부가 균질해지고 경계에 단차로 *창발*하는 것 — `measureMembrane`(경계/내부 ΔE = 막 지수)으로 *측정*해 읽는다(개체 먼저·막 뒤, 0017 의 연장). 결정적 정합: `couple` 은 *국소 4-인접 kin 쌍*만 보고 전역 `measureOrganisms`(연결 성분)를 *동역학에 안 쓴다* — "측정은 부작용 0" 정전 사실을 지키며 도메인은 국소 공유에서 창발. 새 *막* 형태를 더할 땐 verify 가 막 지수(경계/내부 대비 > 통제)를 수치로 보여야 닫는다.
- **막 결합은 *E 재분배*(위치만 아님)이되 쌍 거래라 장부 보존 — 개체군 *이득*을 주되 *선택*은 아님(step-0018)** — 차등 응집(0017)은 위치만(거래 0)이었으나 막 결합은 *E 를 재분배*한다(0015·0016 의 실변환 부류). 단 kin 쌍 균등화(나간 만큼 들어옴)라 sumE 불변(잔차 4.23e-13)·임계 자기조직 보존(공멸 없음). 막=공유 larder 는 개체에 *개체군 이득*(carrying capacity ~69→~79·최대 개체 7→16세포)을 주지만 — 모든 kin 이 똑같이 공유(분업 없음)라 *차등 적합도=선택압*은 아직 0(개체군 이득 ≠ 개체군 선택; kin selection 의 *맹아*지 선택 자체 아님 — 차등 결합이 다음). 존재론 조각(막)이 동역학을 안 깬다(0013~0017 과 같은 직교성).
- **kin selection 선택압 = 차등 m 공유 + 혈연도(Hamilton rb>c), 개체를 중립→적응(step-0019, SPINE §다섯째 축)** — couple(0018)은 *필드 E* 를 *균일* 공유해 개체군 *이득*만 줬다(선택압 0). share(0019)는 *생물량 m*(사망/번식을 가르는 대사 재고)을 *차등*(coop=a.g 의 표현형) + *표적 구조*(사망 직전 kin 의 결손분만 부유 kin 의 잉여로 — 균등화 아님)로 공유 → 협동이 차등 적합도. 핵심 3정합: ① *m* 공유라야 개체↔*대사* 결합(E 는 개체 밖 자원 — 선택압 안 생김) ② *표적 구조*라야 b>c(균등화는 부유 kin 번식 깎아 b≈c → 선택 0) ③ *비평형*(기근)이라야 선택(평형은 zero-sum — 구조가 누가 죽나만 바꿈). 협동을 *복제 적합도(fit)와 별 노브*(coopFit0/Step ≠ geneFit0/Step)로 분리해 confound 없이 잰다. **흩어진 협동은 중립 아닌 *해롭다*(낭비된 이타) — kin 구조가 협동보다 먼저**(adhere→couple→share 사다리 순서가 인과). 새 *사회* 협동을 더할 땐 verify 가 혈연도 의존(뭉침≫흩어짐·기준선 중립)을 수치로 보여야 닫는다.
- **생물량 공유는 *m 재분배*(쌍 거래)라 장부 보존·churn 보존(step-0019)** — share 는 couple(E)·inherit(대사세)처럼 *실변환*(m 이동)이되 쌍 거래(나간 만큼 들어옴)라 sumM 불변(잔차 4.22e-13)·임계 자기조직 보존(공멸 없음·carrying capacity ~63). 존재론 조각(차등 협동)이 동역학을 안 깬다(0013~0018 과 같은 직교성). 단 협동은 *지속*만(강한 침투 아님 — 표적 구조는 보존적 재분배 b≈c+ε; 강한 침투는 공공재 시너지 b>>c 필요 → step-0020 해소).
- **강한 침투 = 공공재 시너지(b≫c) + positive assortment, 협동을 지속→침투(step-0020, SPINE §다섯째 축 "사회 칸")** — share(0019)는 *보존적* m 재분배(b≈c)라 협동이 *지속*만 했다. pubgood(0020)은 *양의 합* — 협동자가 잉여를 *기부* c 해 kin *여럿*에게 나눠 주되 *시너지로 증폭*: 각 kin 은 기부 m 분(m→m, share 처럼 보존)에 더해 그 (pubSynergy−1)배를 *필드 E 에서 채집*해(공공재가 환경 자원을 푼다, E→m) 받는다 → 한 기부가 집단에 b=syn·c≫c. 핵심 3정합: ① *시너지는 질량 창조 아님* — 증폭분은 *필드 E*(닫힌 장부 자원)에서 채집(E 부족=기근이면 capped) ② *비용을 회수 가능하게*(기부 m→m, 클러스터선 되돌아옴)라야 b>c — 1차 구현(비용을 metabolized 소산)은 effClust −0.48 로 *정반대*(회수 못 함) ③ *kin 이 뭉칠수록* 침투 강함(positive assortment): 공공재의 효과 뭉치면 +0.299(강한 침투)·흩어지면 +0.051(~중립), 상호작용 0.248. *kin-지향(greenbeard 인식)*이라 흩어져도 *해롭진 않다*(0019 표적 구조와 다른 점 — 인식이 공간 assortment 를 일부 대신). 새 *사회* 침투를 더할 땐 verify 가 강한 침투(effClust≫0)·혈연도 의존(상호작용>0·기준선 중립)을 수치로 보여야 닫는다.
- **공공재는 *m·E 재분배*(기부 m→m·시너지 E→m)라 장부 보존·churn 보존(step-0020)** — pubgood 은 share(m)·couple(E) 처럼 *실변환*이되 두 보존 경계뿐(기부 m→m·시너지 E→m, 질량 창조 0)이라 잔차 4.28e-13·임계 자기조직 보존(공멸 없음). 공공재 채집으로 carrying capacity ~78(0019 의 ~63 보다 높음 — 협동 채집이 환경 E 를 빨리 풀어 더 먹임). 존재론 조각(시너지 협동)이 동역학을 안 깬다(0013~0019 와 같은 직교성). **공공재는 *풍요*의 자식(risk-pooling 은 *위기*에 — 두 축)**: 시너지가 *필드 E 채집*이라 풍년(E 잉여)에 빛난다(0019 표적 구조는 기근에 — 사회진화의 두 보완 국면).
- **세포 분화 = 위치가 phenotype 을 정함(genotype↔phenotype 1:1 깸), 균일 역할→분업(step-0021, SPINE 주요 전이 사다리 "사회→분화된 다세포")** — 0019·0020 은 phenotype 이 *태그만의* 함수(한 클론=한 역할). differentiate(0021)는 phenotype 을 *(genotype, 위치)* 의 함수로 — 4-근방이 다 찬 *갇힌 내부*(번식 불가)는 soma(체세포·잉여를 4-근방 kin 에 기부 m→m), 빈칸 있는 *표면*은 germ(생식·번식). 핵심 3정합: ① *번식 가능성이 역할을 가름*(soma=번식 불가)이라 분업이 *기하*에서 자기일관(임의 라벨 아님) — 격자상 germ=표면(Volvox 와 뒤집힘) ② soma 는 *손실 아니라 사장 자원의 회수*(kDiff=0 이면 갇힌 내부 m 격리·번식 못 함 → kDiff>0 이면 표면 번식으로 전환, 개체군 2928→3006) ③ 분화는 *조밀함의 자식*(전체 스택 희소라 갇힌 세포 드물어 거의 안 켜짐 — 정착 생활사가 분화의 전제). 새 *분화* 표현형을 더할 땐 verify 가 위치 의존 phenotype(dGap≫0·germM 배증·기준선 roleGap<0)을 수치로 보여야 닫는다.
- **세포 분화는 *m 재분배*(갇힌 내부 soma → kin 기부)라 장부 보존·churn 보존(step-0021)** — differentiate 는 share/pubgood 처럼 *실변환*(m 이동)이되 기부 m→m 쌍 거래(나간 만큼 들어옴)라 sumM 불변(differ 잔차 3.0e-14)·임계 자기조직 보존(공멸 없음·carrying capacity ~75). 존재론 조각(분화)이 동역학을 안 깬다(0013~0020 과 같은 직교성). 단 *위치 기반 동적 분화*지(세포가 옮기면 역할 바뀜·매 tick 재판정·commitment 없음 — 메커니즘은 share/pubgood 계열 위치형 m-기부) 불가역 계통 격리(germline·Weismann)는 아님 — 그건 백로그(→ step-0022 후보).
- **분화의 *독립* 증거는 dGap 이 아니라 *번식 이득*·골든은 두 층(step-0021 검증 정직성)** — differ 의 dGap·germM 은 measureDifferentiation 이 법칙과 *같은* occN≥4 분할을 되읽어 *기전 확인*에 가깝다(법칙이 m 을 그 분할로 옮기니 dGap>0 거의 자동·기준선 roleGap<0 도 번식 기하 산물). 분화의 *독립* 증거는 법칙이 직접 안 건드리는 *개체군·출생 이득*(2928→3006). 또 전체 스택은 희소라 분화 미커버라 회귀 골든이 두 층 — `diff@`(전체 스택, 거의 미커버)+`tdiff@`(조밀 조직, 분화 코드 경로 동결).
- **생식세포 계통 격리 = 불가역 *상속* fate(위치 무관) + Weismann 번식 격리가 *창발*(step-0022, SPINE 주요 전이 사다리 "분화된 다세포")** — 0021 differentiate 는 phenotype=f(genotype, *위치*) 라 가역·동적(옮기면 역할 바뀜·매 tick 재판정). sequester(0022)는 phenotype=f(genotype, *계통*) — 생명 속성 a.soma(불가역 fate)를 *위치 무관*하게 한 번 부여(시드 의사난수<kGermline 면 soma·영원히 불변). soma 계통은 제 잉여를 4-근방 *germ kin* 에게 *전량* export(m→m 쌍 거래)해 m 이 keep(<mDiv)에 묶여 *번식 임계에 영영 못 닿는다* → ⑧reproduce 가 자연히 skip = 번식이 *생식세포 계통 전용*(Weismann). 핵심 정합: **번식 격리를 type 분기로 박지 않고(reproduce 동결·척추 체크 2) soma 가 m 을 비워 *창발*시킨다**(somaMaxM<mDiv 전 시드). ⑦b(metabolize 뒤·reproduce 앞)에 둬야 *흡수 직후·번식 직전*에 soma 를 비운다(0021 ⑥f 는 흡수 전이라 안 됨). 새 *계통* 격리를 더할 땐 verify 가 위치 무관 fate(surfaceSomaFrac≫0·0021 이면 0)·Weismann(somaMaxM<mDiv)·계통 할당(somaFrac≈kGermline)을 수치로 보여야 닫는다.
- **계통 격리는 *m 재분배*(soma → germ kin export)라 장부 보존·fate 는 비가역 상태지 비보존 아님(step-0022)** — sequester 는 share/pubgood/differentiate 처럼 *실변환*(m 이동)이되 export m→m 쌍 거래(나간 만큼 들어옴)라 sumM 불변(germ 잔차 5.6e-13)·임계 자기조직 보존(공멸 없음·carrying capacity ~73). 존재론 조각(계통 격리)이 동역학을 안 깬다(0013~0021 과 같은 직교성). a.soma(germ/soma)는 *비가역 상태*(Weismann 장벽 — 결정3 정신)지 에너지가 아니다(비가역≠비보존, 0008). 격리의 *독립* 증거는 surfaceSomaFrac(법칙과 같은 a.soma 되읽기, 단 위치 의존성을 *반증*)이 아니라 *출생 이득*(3952→4506, +14% — 위치 분화 +4.6%보다 큼: 전량 export + germ 집중). 골든 두 층 — `germ@`(전체 스택, 약하게 커버)+`tgerm@`(조밀 조직, 계통 코드 경로 동결).
- **정착은 *운동 게이트*(위치만 — 거래 0)이고 분화는 조밀함의 자식(step-0023, SPINE 주요 전이 사다리 "분화된 다세포"의 전제)** — anchor 는 adhere/move/tumble 처럼 *위치만* 바꾸는(실은 *안 바꾸는* — 운동을 skip) 거래 0 법칙이다(잔차 2.15e-14 불변·churn 보존). 잘 먹고(m≥anchorM) kin-포위(같은 태그 4-근방≥anchorKin)된 생명이 *고착*(a.sessile 게이트로 move/adhere skip)해 떠돌지 않으면 이웃 kin 이 둘레에 쌓여 *큰 안정 confluent 조직*으로 coalesce → 갇힌 내부 세포 늘어 0021 분화 더 발현(분화는 *조밀함의 자식*, 그 조밀함을 정착이 만든다 — 0021/0022 한계 "조밀 조직서만"의 길). 정착은 *연속 활성도 축의 국소 문턱 게이트*(잘 먹고 포위된 생명이 운동을 멈춤일 뿐 — tumble 굶주림 게이트·FSM living/burning 게이트와 같은 정신, authored 개체 종류 0). a.sessile 은 *매 tick 재계산 게이트*(상속 상태 아님 — 위치·m 이 이미 해시에 들어 결정적, 가법 해시 불필요). 새 *정착·운동* 게이트를 더할 땐 verify 가 조직 coalescence(meanOrg↑·nOrg↓·interiorFrac↑)와 분화 발현(provision↑)을 수치로 보여야 닫는다.
- **정착성 생활사는 *안정 자원*의 자식 — 떠도는 자원엔 운동성이 답(step-0023)** — 정착의 양의 되먹임(고착 코어 둘레로 조직 성장)은 먹이가 *한자리에 머무는* 환경에서만 이긴다. 먹이가 *떠도는* 환경(별·srcJump)에선 외려 해롭다(고착하면 먹이를 못 쫓아 굶어 죽음 → 개체군·조직 감소). 두 생활사(정착 vs 운동)가 *자원의 공간 안정성*으로 갈린다 — 그래서 전체 스택(떠도는 별·자기제한)에선 정착이 거의 안 켜진다(carrying capacity ~71, 발현 부분적). 큰 안정 조직의 전체 스택 발현엔 *안정 자원 패치*가 더 필요(블로커를 *연* 것이지 닫은 것 아님). 정착은 *번식·개체수와 맞바꾼다*(빽빽하면 새끼 자리↓·고착하면 이동성↓) — 안정성·조밀성의 대가.
- **곡률 표면장력은 *E-막*에 산다 — *position* 곡률은 응집(DAH)과 redundant(step-0024, 형태 사다리 R2)** — *position* 곡률(셀을 옮겨 4-연결 둘레 최소화)을 먼저 구현했으나 차등 응집(0017 Steinberg DAH = 차등접착이 곧 표면장력)과 *최종 형태가 같았다*(adhere on/off 동일 원형도)·보존적 단일-셀 이동은 떨어진 액적 사이 에너지 장벽도 못 넘어 coalescence 도 못 했다 → R2 의 자리는 *셀 배치*가 아니라 *형태가 실리는 바닥 필드 E* 다. tension 은 그래서 *E-막에 Young-Laplace 압력*(ΔP=γκ)을 더한다 — couple(R1) 균등화 *위에* 곡률 구배를 얹어 볼록 경계(coordination=4-근방 같은 태그 수 낮음)의 E 를 오목/속(높음)으로 민다(kin 쌍 E 거래 — 보존, couple 과 같은 경계). 액적의 E-형태가 *경계 식고 속 차오른 둥근 돔*으로(convexE 1.93→1.62·coreRatio 0.22→0.26). 새 *형태* 조각을 더할 땐 verify 가 그 형태의 *측정 수*(곡률은 볼록 경계 E↓·coreRatio↑·flux>0)를 보여야 닫는다. couple(평탄화)↔tension(곡률)은 *평형*(γ↑ 곡률·kMembrane↑ 평탄). 표면장력의 서명은 *경계가 식는 것*(곡률 큰 곳이 압력으로 비워짐 — 경계는 둘레·속은 넓이라 속은 작게 데워짐).
- **형태는 flux 의 하류 — 0018 이 형태를 *바닥 필드*에 처음 실음, 렌즈는 코드 0(step-0018, [INTERPRET.md](INTERPRET.md) §5 첫 실현)** — 0017 의 개체 윤곽은 2D 오버레이 선(측정)이라 바닥 하이트필드엔 형태가 없었다. 막 결합은 *E 자체*를 재분배해 도메인+막을 **하이트필드에 새긴다** → 같은 해석 렌즈가 그 형태를 *코드 수정 0* 으로 받아 그린다(§5 "형태가 생기면 렌즈가 공짜로 그린다"의 첫 실현 = 렌즈가 옳다는 증거). "시각화 정합"은 두 뷰를 *다르게* 만드는 게 아니라 공유 실루엣(h=E+R)을 *의미 있는 세계 형태*로 채우는 것이었다. 남은 형태 사다리(R2 곡률 표면장력·R3 방향성 결정화·R4 E↔R 튜링)도 *시뮬*이 빚고 렌즈는 그대로 받는다(렌더러가 형태 규칙 박으면 author).
- **무작위 ≠ 비결정론**: 탐사 방향은 시드 의사난수(`tumbleHash(x,y,tick,seed)`, `Math.random` 금지) — "무작위"는 *구배와 무상관(탈상관)*일 뿐 생성은 결정론(step-0010).
- **생명은 형성기(FORM) 강고임에서 굳은 저장체를 씨앗으로 bootstrap** — 생명이 깨어나면 자유 E를 소비해 저장체가 새로 안 굳음(step-0008).
- **비가역 ≠ 비보존**: 상전이(E→R)해도 닫힌 장부는 그대로 닫힘 — "되돌릴 수 없음"은 상태의 성질이지 에너지의 성질이 아님(step-0008).
- **소산구조 ≠ 진화하는 복잡계 — 유전이 빠진 토대(이번 논의)** — 비평형 throughput + 임계 자기조직(0012)은 세계를 *지속*시키나 복잡성을 *축적*하진 않는다(임계 끌개에 맴돔 — Bénard·불꽃·눈송이는 화려하나 진화 안 함). 사람급 복잡 적응계엔 다윈 동역학(유전·변이·선택)이 필요하고, 유전 = *이산* 정보의 자기복제(연속 필드는 정보 못 지킴 — 0008 R·0013 FSM 이산성이 재료). 경로 = R-주형 자기복제(하이퍼사이클·단일 척추 유지). **유전이 개체보다 먼저**(다세포 협동은 클론 kin selection 안정화). **정직: OEE 미해결** — 각은 있으나(원리·재료) 도달 보장 아님(Tierra/Avida 정체). 사다리(복제분자→염색체→다세포→사회→인지)에서 우리는 *유전 토대 앞*. [SPINE.md](SPINE.md) §다섯째 축.
- **유전 = 저장의 *씨앗 끝*(자기복제)이지 저장 전부가 아니다(이번 논의)** — 저장(R)은 유전의 *필요조건이지 충분조건 아님*: 유전 = 저장 + 자기복제(주형). *수동적 광물(바위)은 유전 없음*(저장만), *자기복제 결정*만 가짐 — 바위→자기복제 결정→genotype 의 연속체(저장 극단 *안*에서 창발, Cairns-Smith 점토 유전자: 유전은 *전생물적*, 광물에서 난다). DNA 도 *제 사본을 베끼는 비주기 결정*=한 종류 광물. **유전은 생명 전용 아님 — 저장 극단(R)의 능력**, 별(남긴 R=원소로 *느슨한* 혈통, metallicity)·생명(자기 주형 *단단한* 복제)이 *결합 단단함*으로 갈림(유무 아님). 소산(별 불꽃, A 최고)은 흐름이 패턴을 지워 정보 못 새김 — 기억은 저장 극단에만.
- **개체는 author 가 아니라 측정 — 차등 응집→표면장력 액적(step-0017 실현, ↑ 위 정전 사실)** — 다세포 개체("계")를 `Organism{cells}` 컨테이너로 *만들면* 활성도 환원(척추 체크 2) 위반. 개체 = 차등 응집(kin↑/타↓, Steinberg)이 빚은 표면장력 액적 = *flux 결합 도메인*으로 *측정*해 읽는다(step-0017 이 *공간 토대*를 놓음 — kin 접촉 0.51→0.95). 막은 액적의 표면으로 창발(개체 먼저·막 뒤 — 아직 측정 선일 뿐, flux 결합·막 백로그). 형태는 형태발생(위치정보·대칭깨짐·경계불안정)으로 — *외계* 형태지 사람 모방 아님(VISION).
- **형태(form)는 flux 의 하류 — 렌더러가 아니라 *시뮬*이 빚는다(이번 논의, [INTERPRET.md](INTERPRET.md) §5)** — 시각화 렌즈가 필드에서 읽어도 되는 건 *색·재질·빛·흐름*(E/R/A/G/∇E 의 함수)뿐, *지오메트리(형태)는 아니다*. "물=평면, 바위=결정" 같은 *형태 규칙*을 렌더러가 박는 것은 메시 scatter 와 같은 author(인과 역전). 높이는 `h=E+R` 하나라 *두 시각화 뷰가 같은 실루엣인 건 정상* — "비출 형태가 아직 없다"는 진단이다. 형태는 현실의 다섯 원리(소산구조·튜링 불안정·상분리/표면장력·주형성장 형태발생·수송기하)대로 *flux 의 결과*로 창발해야 하며, 이는 **이후 step 의 일**(개체=차등응집→표면장력 액적·G 의 공간 phenotype·E↔R 두 timescale 튜링[*둘째 전역 필드 금지, 단일 척추*]·표현분열 통일, [SPINE.md](SPINE.md) §다섯째 축). 시뮬이 형태를 얻으면 같은 렌즈가 코드 수정 0 으로 그려낸다.
- **한 step = 한 조각**: 더 떠올라도 다음으로 전가. 새 코어는 직전 코어를 잇고 항을 하나만 더한다.

---

## 5. 6요소 진행 현황

| # | 요소 | 구분 | 상태 (현재 + 획득 step) |
|---|------|------|------|
| 1 | 터 (Ground) | 생성자 | ✅ 0001 |
| 2 | 흐름량 (Quantity) | 생성자 | ✅ 0001 + **상(phase) R** 0008 — 흐르는 E + 굳은 R(저장체), 장부에 R 항. +**flux 계량 A** 0014 — 재고(E)와 *흐름*(A=dE/dt 활성도)을 분리해 읽는다(결정1, 저장↔소산을 측정으로) |
| 3 | 법칙 (Law) | 생성자 | ✅ 0001 +응집(0002) +결정화·풍화(0008) +기복(0009) +R-주형 복제(0015) +생명 유전(0016) +차등 응집(0017) +막/flux 결합(0018) +생물량 공유(0019, kin selection) +공공재 협동(0020, 시너지 b≫c → 강한 침투) +세포 분화(0021, 위치 의존 phenotype — 갇힌 내부 soma → 표면 germ provision) +생식세포 계통 격리(0022, 불가역 상속 fate germ/soma — soma 가 germ kin 에 전량 export → 번식이 germ 전용 *창발*; Weismann 장벽) +**정착 생활사**(0023, 잘 먹은 kin-포위 생명 고착 — 이동·탐사·재정렬 게이트 skip → 큰 안정 confluent 조직 coalesce → 분화 발현; 위치만·거래 0) +**곡률 기반 표면장력**(0024, E-막에 Young-Laplace 압력 — 볼록 경계 E→오목/속 곡률 구배 → 액적 E-형태 둥근 돔; couple 균등화 위에 곡률, E 쌍 거래·보존) |
| 4 | 구동 (Drive) | 생성자 | ✅ 0001 +떠돎(0007)+내생화(0011, 별=R 누적 핵 점화 내생 주입원·서행·소진)+**이산 FSM**(0013, 별 living→burning→ash 문턱 딱 뒤집힘·비가역. SPINE 결정3 별에서 완성). 0011 공멸은 0012 자기제한이 닫음 |
| 5 | 자원 (Resource) | 결과 | ✅ 0002 →소비자(0003)·개체군 배분(0004)·생사 문턱(0006)·떠도는 고임(0007)·평형 저장체(0008)·무대(0009)·별의 연료=점화 신호(0011)·유전의 담체(0015)·**생명 유전의 씨앗**(0016, R-genotype 을 생명이 읽어 부트스트랩 — 담체 이동 R→생명, Cairns-Smith) |
| 6 | 생명 (Life) | 결과 | ✅ 0003 +번식(0004)+이동(0005)+기초대사비(0006)+탐사(0010)+밀도 의존 자기제한(0012)+유전형 획득(0016)+개체(다세포='계')(0017)+물리적 도메인·막(0018)+적응 단위(0019)+강한 침투(0020, 사회 칸)+분업/세포 분화(0021, 위치 의존 soma/germ)+생식세포 계통 격리(0022, 불가역 상속 germ/soma 계통 — 번식이 생식세포 전용, Weismann)+**정착 생활사**(0023, 잘 먹은 kin-포위 → 고착·a.sessile 게이트 → 큰 안정 조직 → 분화 발현; 가역 게이트·자기 보호)+**곡률 표면장력**(0024, 개체=kin 액적의 *E-형태*가 둥근 돔으로 — E-막 Young-Laplace; 형태 사다리 R2). *다음 큰 호: 방향성 결정화(genotype 이 형태 방향을)(STATE §2)* |

---

## 6. 빠른 참조

- 척추 체크 4항·존재론: [SPINE.md](SPINE.md) · **입문서(SPINE 쉬운 풀이 — 별·둑·봉우리·보존): [PRIMER.md](PRIMER.md)**
- 비전·게임 종착점: [VISION.md](VISION.md)(무엇을·필드→현상 사전) · **해석 레이어 [INTERPRET.md](INTERPRET.md)(어떻게 — 추상량→세계 원형 번역, 셰이더 렌즈)**
- 엔진(공통 UI/3D)·패널 스펙: [engine/PANEL.md](engine/PANEL.md) · [engine/DESIGN-3D.md](engine/DESIGN-3D.md)
- **의외의 발견 / 정직한 한계 전문**: 각 `step-NNNN.md`의 §발견·§한계 (STATE는 중복 보관하지 않음 — 위 §3·§4가 현재 load-bearing 요약).

---

## 7. INDEX — 시리즈 검증 현황 (유일하게 append, 1행/step)

| step | 더한 한 조각 | 결과 (회귀 0 전제) |
|---|---|---|
| [0001](step-0001.md) | 최소 세계 | **통과** — 닫힌 장부·결정론·비평형(잔차 1.3e-13). 고임은 부정적→0002 |
| [0002](step-0002.md) | 응집 1항 | **통과** — 고임 출현·지속·재생(평균 6.2개 persist 0.92). 자원(5) 검출 |
| [0003](step-0003.md) | 생명 1항 | **통과** — 소비 42%·항상성·기아사(잔차 2.24e-13). 생명(6) 검출 |
| [0004](step-0004.md) | 번식 1항 | **통과** — carrying capacity K=97 전 시드·구동off 전멸. 개체군 동역학 |
| [0005](step-0005.md) | 이동 1항 | **통과** — 주화성(거리 10→1.41). 단 동결 미해소(집중 강화) |
| [0006](step-0006.md) | 기초대사비 1항 | **통과** — 생존 문턱·풍요 역설(sumE 9.8→268). 단 동결 미해소(집중) |
| [0007](step-0007.md) | 떠도는 자원 1항 | **통과** — churn(맥동적). G2 부분 해소·**갈릴레이 발견** |
| [0008](step-0008.md) | 결정화·풍화 1항 | **통과** — 저장체(평형) 보존율 0.56 vs 소산 0.14. **척추 첫 적용** |
| [0009](step-0009.md) | 기복 1항 | **통과** — 무대(둑 안 E 7.21배·퇴적 전선 상관 0.039). 내생 풍경 재편·**첫 3D 셸** |
| [0010](step-0010.md) | 탐사(run-and-tumble) 1항 | **통과** — escape: 트랩 갇힌 생명 해자 건너 생존(거리 4.7) vs greedy 사멸(전 시드)·**국소 최대 함정 해소**. 회귀 maxDiff=0·잔차 2.51e-13·해시 일치. **정직(G2)**: 정적 source 끝없는 churn 미완(개체군이 전역 끌개 수렴, 후반 출생 on 1.0/off 3.8) → 구동 내생화 |
| [0011](step-0011.md) | 구동 내생화(별) 1항 *(law-pipeline 첫 step)* | **통과** — 동결 해소: 측정 창 churn 외부 9→내생 5708(634배)·무게중심 떠돎 0.24→4.80(전역 봉우리 소멸). 회귀 maxDiff=0(kIgnite=0=step-0010)·잔차 1.09e-12·해시 일치. **정직(G2)**: 끝없는 churn 미완 — 음성 피드백 부재로 과증식-공멸(붕괴 ~12700 tick 전 시드) → 자기제한/방향성 추적(0012) |
| [0012](step-0012.md) | 밀도 의존 자기제한(crowd) 1항 | **통과 — 끝없는 churn 완성**: 자기제한이 과증식-공멸(0011)을 막고 carrying capacity ~73 에 묶어 *임계 자기조직*(공멸 없음·후반 출생/사망 1491/1489 ≈균형·별 10 유지·maxR>ignThresh, 단일 시드 184k 정상상태). 회귀 maxDiff=0(kCrowd=0=step-0011, golden `endo@` 가법 잠금)·잔차 8.37e-13·해시 일치. **발견**: carrying capacity = 개체수 아닌 *공간 분포*(crowdR=채식지 척도 필수). G2/G3 동역학적 종결 |
| [0013](step-0013.md) | 별 연소 이산 FSM(combust) 1항 | **통과 — SPINE 결정3 별에서 완성**: 별 living→burning→ash 가 문턱에서 딱 뒤집히고 비가역(back=0). 결정2↔3 정합 수치: (a)분기 부재 labelViol=0·(b)활성도 환원 throughput 비=livingFrac 0.55·(c)히스테리시스 폭 0.20·3상태(LBA) 도달. 회귀 maxDiff=0(kFSM=0=step-0012, golden `cwd@` 가법 잠금)·잔차 4.48e-13·결정론(상태 라벨 가법 해시) 일치. 동역학 보존(40k 전 시드 생존·후반 출생/사망 1355/1360). **발견**: 불은 연료가 다해야 꺼진다(소진 99% 연료·1% 핫코어, FN 빠른점화-느린소진)·문턱 두 층(siting≠flashover) |
| [0014](step-0014.md) | 활성도 계량(flux) 1항 | **통과 — SPINE 결정1·2 완성**: 척추 변수 E 의 flux(|dE/dt|)를 활성도 A 로 EMA 계량 → 저장↔소산이 *측정*으로 갈림(A_burn/A_store=26.1, authored enum 없이). fracBR=1.00(별 연소 셀 100% R-rich인데도 A 26배 — 흐름이 정체성보다 분류). 회귀 maxDiff=0(kFlux=0=step-0013, golden `fsm@` 가법 잠금)·잔차 4.48e-13(비트 동일)·결정론(A 가법 해시) 일치. 측정 부작용 0(A 읽기전용 → 동역학 비트 보존, carrying capacity ~61). **발견**: 저장체 활성도≈0 은 R 아닌 *흐름 끊긴 자리*의 성질(빈 배경도 같음)·소산만 빛난다 |
| [0015](step-0015.md) | R-주형 자기복제(replicate) 1항 | **통과 — SPINE §다섯째 축(유전) 첫 토대**: 저장 R 의 씨앗 끝에서 genotype(이산 태그 G) 자기복제 → 유전·변이·선택·적응이 authored 다윈 엔진 없이 창발. 유전 fidelity=1.00(mu=0 순수 클론, *이산* 태그라 정보 보존 — 연속 필드 불가)·변이율 0.021≈mu·선택 ratio 1.53·적응 평균적합도 +0.13. 회귀 maxDiff=0(kTemplate=0=step-0014, golden `flux@` 가법 잠금 신규·`gene@` step-0016 앵커)·잔차 4.55e-13·결정론(G 가법 해시) 일치. 복제=실제 변환이되 R 순환에 올라타 churn 보존(공멸 없음·carrying capacity ~56). **발견**: 유전형이 R 절반 잠가도 churn 비트 보존(유전=에너지 순환의 승객)·이산 태그라 4083세대 fidelity 1.00. **한계**: 적합도 단봉·authored(OEE 아님)·phenotype 1차원·유전이 R 위만(생명 미결합) |
| [0016](step-0016.md) | 생명 유전(inherit) 1항 | **통과 — SPINE §다섯째 축 둘째 칸(유전↔생명 결합)**: step-0015 의 R-genotype 을 생명 대사 엔진(m)에 묶음(부트스트랩·분열 상속·표현형→대사세) → 다윈 동역학이 *생명* 위에서. 상속 fidelity=1.00·변이율 0.0196≈mu·선택 ratio **3.27**(R 의 1.53 보다 날카로움 — 생사 churn 지속 치환)·적응 생명 평균적합도 **0.50→0.81(+0.31)**. 회귀 maxDiff=0(kInherit=0=step-0015, golden `gene@` 가법 잠금·`life@` step-0017 앵커 신규)·잔차 4.48e-13·결정론(a.g 가법 해시) 일치. 표현형세=실제 대사 변환이되 약결합(0.02)이라 churn 보존(공멸 없음·carrying capacity ~63). **발견**: 전체 스택서 *모든* 생명이 R-genotype 부트스트랩(geneCells=pop)·죽음 있어 생명이 R 보다 선택 2배 날카로움. **한계**: 적합도 단봉(OEE 아님)·phenotype 1차원(대사 세율)·생명 여전히 0-D 점(개체 아님)·강결합 0.05 면 한계 시드 공멸 |
| [0017](step-0017.md) | 차등 응집(adhere) 1항 | **통과 — 주요 전이 사다리 "유전 → 개체"(개체='계'를 author 아닌 측정으로)**: 같은 유전형(kin=a.g) 생명끼리 끌리고 타는 밀어내(Steinberg DAH=Schelling) cell sorting → 표면장력 액적이 창발, 개체를 *4-인접 kin 연결 성분*으로 측정(`measureOrganisms`). sort kin 접촉 **0.51→0.95**(응집 off 0.51 동결)·개체 평균 **4.5**·최대 **21** 세포(통제 1.7·11 — percolation 잔클러스터의 2.6배). 회귀 maxDiff=0(kAdhesion=0=step-0016, golden `life@` 가법 잠금·`org@` step-0018 앵커 신규)·잔차 3.98e-13(위치만 — 거래 0)·결정론(x,y) 일치. 위치만 바꿔 churn 보존(공멸 없음·carrying capacity ~69). **발견**: DAH=Schelling 한 알고리즘·개체가 percolation 아닌 능동 정렬·전체 스택 개체 작음(희소·이동성 1.5/7세포). **한계**: 개체가 공간 군집일 뿐(분업·개체단위 선택 없음 — 진화적 중립)·막은 측정 선일 뿐·응집이 대사에 안 묶임·4-인접 scan 이방성 |
| [0018](step-0018.md) | 막/flux 결합(couple) 1항 | **통과 — 형태 사다리 R1(개체='계'를 측정 윤곽 → 물리적 flux 결합 도메인, "막은 액적의 표면으로" author 아닌 측정)**: 같은 유전형 kin 4-인접 생명끼리 *필드 E 를 국소 공유·재분배*(쌍 거래) → 액적 내부 균질·경계 단차(**막**) 창발, `measureMembrane`(경계/내부 ΔE) 측정. membrane 내부 |ΔE| **0.125→0.075**(균질화)·막 지수 **1.98→3.46**(통제는 같은 액적이되 막 없음). 회귀 maxDiff=0(kMembrane=0=step-0017, golden `org@` 가법 잠금·`mem@` step-0019 앵커 신규)·잔차 **4.23e-13**(쌍 거래 — E 재분배이되 보존)·결정론(E) 일치. churn 보존(공멸 없음). **발견**: 막=공유 larder 가 carrying capacity **~69→~79**·최대 개체 **7→16세포** 올림((a) kin selection 의 *맹아* — 개체군 이득이되 선택압 아님)·형태가 처음 *바닥 E 하이트필드*에 실려 렌즈가 코드 0 으로 받음(INTERPRET §5 명제 첫 실현). **한계**: 막이 부드러운 단차일 뿐(선택 투과 아님)·공유가 개체군 *이득*이되 *선택압* 0(분업 없음)·곡률 표면장력 없음·전체 스택 막 출렁임(희소)·center-셀/scan 순서 |
| [0019](step-0019.md) | 생물량 공유(share) 1항 | **통과 — 개체='계'를 *중립*에서 *적응*으로(kin selection 선택압, SPINE §다섯째 축 "개체를 진화 단위로")**: couple 의 *m-아날로그* — kin 끼리 *생물량 m*(사망/번식을 가르는 대사 재고)을 *차등*(coop=a.g 의 표현형) + *표적 구조*(사망 직전 kin 의 결손분만 부유 kin 의 잉여로)로 공유 → 협동이 차등 적합도. kin selection(Hamilton rb>c): 협동의 운명이 *혈연도 의존* — 협동의 효과 뭉치면 ≈0·흩어지면 **−0.267**(낭비된 이타로 사라짐), 상호작용 **0.263**(2×2 요인설계, 두 기준선 협동off 모두 중립 — 효과는 오직 협동). 회귀 maxDiff=0(kShare=0=step-0018, golden `mem@` 가법 잠금·`share@` step-0020 앵커 신규)·잔차 **4.22e-13**(m 쌍 거래 — 보존)·결정론(m) 일치. churn 보존(공멸 없음·carrying capacity ~63). **발견**: 평형은 zero-sum이라 선택압 0(*비평형*[기근]서만 켜짐 — 협동의 이득은 위기에)·흩어진 협동은 중립 아닌 *해롭다*(kin 구조가 협동보다 먼저)·*어떻게* 공유하나(표적 구조 vs 균등화)가 b>c 를 가름. **한계**: 협동이 *지속*만(강한 침투 아님 — 공공재 시너지 필요)·고정 표현형(OEE 아님)·risk-pooling 일 뿐(진짜 분업 아님)·평형 zero-sum·center-셀/scan 순서 |
| [0020](step-0020.md) | 공공재 협동(pubgood) 1항 | **통과 — 협동을 *지속*에서 *강한 침투*로(공공재 시너지, 사회 칸 진입)**: share 의 *양의 합 아날로그* — 협동자가 잉여를 *기부*해 kin *여럿*에게 나눠 주되 *시너지로 증폭*(기부 m→m + (pubSynergy−1)배를 *필드 E 에서 채집*, E→m). 한 기부가 여럿을 살려(b≫c) 협동이 *지속→강한 침투*. 침투 세기가 *혈연도 의존*(positive assortment): 공공재의 효과 뭉치면 **+0.299**(강한 침투 — 0019 지속 ≈0 넘어섬)·흩어지면 **+0.051**(~중립), 상호작용 **0.248**(2×2 요인설계, 두 기준선 공공재off 모두 중립 — 효과는 오직 공공재). 회귀 maxDiff=0(kPublic=0=step-0019, golden `share@` 가법 잠금·`pub@` step-0021 앵커 신규)·잔차 **4.28e-13**(기부 m→m·시너지 E→m — 보존)·결정론(m·E) 일치. churn 보존(공멸 없음·carrying capacity ~78>0019 ~63). **발견**: 공공재는 *풍요*의 자식(0019 risk-pooling 은 *위기*에 — 두 축)·kin-지향(친족 인식)이 공간 assortment 를 일부 대신(흩어져도 중립, 0019 는 해로움)·*비용을 회수 가능하게*(m→m) + 시너지라야 침투(metabolized 소산이면 effClust −0.48 정반대). **한계**: 완전 고정 아님(강한 침투까지)·고정 표현형(OEE 아님)·greenbeard(순수 공간형 아님)·공동 채집일 뿐(진짜 분업 아님)·center-셀/scan 순서 |
| [0021](step-0021.md) | 세포 분화(differentiate) 1항 | **통과 — 협동을 *균일 역할*에서 *분업*으로(위치 의존 세포 분화, 분화된 다세포 진입)**: 각 생명이 제 4-근방 점유(빈칸 수)를 *위치 정보*로 읽어 — 4-근방 다 찬 *갇힌 내부*(번식 불가)는 **soma(체세포)** 로 잉여(m−dangerLine)의 kDiff 를 4-근방 kin 에 *기부*(m→m 쌍 거래 — 보존), 빈칸 있는 *표면*은 **germ(생식)** 으로 번식. genotype↔phenotype 1:1 을 깸(phenotype=f(genotype, 위치)). 통제 분화 아레나(단일 클론 조밀 조직, 분화만 격리): 위치가 phenotype 가른 폭 **dGap +0.318**(roleGap=germM−somaM **−0.366→−0.047**)·표면 germ m **0.254→0.529**(soma provision 으로 배증)·갇힌 내부 비율 **0.94**·분업 이득 개체군 **2928→3006**·출생 **3952→4133**(사장 생물량→번식). 회귀 maxDiff=0(kDiff=0=step-0020, golden `pub@` 가법 잠금·`diff@` step-0022 앵커 신규)·잔차 **3.0e-14**(differ, 기부 m→m 쌍 거래)·결정론(m) 일치. churn 보존(공멸 없음·carrying capacity ~75). **발견**: germ 이 *표면*에(격자 기하가 germ-soma 방향 정함 — Volvox 와 뒤집힘)·분화는 *조밀함*의 자식(전체 스택 희소라 거의 안 켜짐 — 정착 생활사 전제)·soma 는 *손실* 아니라 사장 자원의 *회수*(번식 못 할 자원→번식할 곳). **한계**: 위치 기반 *동적* 분화(불가역 계통 격리 germline 아님 → 0022 해소)·germ/soma 이분(연속 분화 아님)·조밀 조직서만(전체 스택 희소)·germ 방향이 기하 산물·고정 규칙(OEE 아님)·center-셀/scan/깊은 핵 흐름 |
| [0022](step-0022.md) | 생식세포 계통 격리(sequester) 1항 | **통과 — 분화를 *위치 동적*에서 *불가역 상속 계통*으로(생식세포 계통 격리, Weismann 장벽, 분화된 다세포 깊이 진입)**: 두 패스 — ①commitment(불가역) 유전형 있는 미커밋 생명이 *위치 무관*하게 한 번 fate 부여(시드 의사난수 < kGermline 면 **soma**·아니면 **germ**·영원히 불변; 분열 자식은 미커밋→다음 tick 커밋 = 모든 자식이 germ 부모에서 남 = germline) ②**soma 계통**이 제 잉여(m−dangerLine)를 4-근방 *germ kin* 에 *전량* export(m→m 쌍 거래 — 보존)해 m 이 keep(<mDiv)에 묶여 **번식 임계에 영영 못 닿음** → ⑧reproduce 자연 skip = 번식이 *생식세포 계통 전용*(Weismann, reproduce 동결 — author 분기 없이 *창발*). phenotype=f(genotype, 계통) 위치 아닌 *상속*. 통제 격리 아레나(단일 클론 조밀 조직, 격리만 격리): *표면 세포 중* soma 계통 **0.44**(0021 위치 분화면 0 — fate 가 위치 아닌 계통)·Weismann soma 최대 m **0.758 < mDiv 0.90**(전 시드 PASS)·계통 할당 somaFrac **0.588≈kGermline**·roleGap germM−somaM **+0.624**(germ 0.965 vs soma 0.341)·출생 **3952→4506**(+14%, 사장 잉여→germ 번식). 회귀 maxDiff=0(kGermline=0=step-0021, golden `diff@` 가법 잠금·`germ@`+`tgerm@` step-0023 앵커 신규)·잔차 **5.6e-13**(germ, export m→m 쌍 거래)·결정론(fate a.soma·m) 일치. churn 보존(공멸 없음·carrying capacity ~73). **발견**: 번식 격리가 *창발*(soma 가 m 비워 미분열 — author 분기 없이)·계통 비율이 손잡이 그대로(상속의 안정성)·불가역 격리가 위치 분화보다 번식 더 끌어올림(전량 export+germ 집중, +14% vs +4.6%). **한계**: 계통 둘뿐(연속/다중 계통 트리 아님)·할당 무작위 확률(비대칭 분열 상속·발생 프로그램 아님)·조밀 조직서만(전체 스택 희소·정착 생활사 미구현)·외딴 soma 격리 빈틈(germ kin 없으면 번식 샐 수)·고정 규칙(OEE 아님)·center-셀/scan/깊은 핵 흐름 |
| [0023](step-0023.md) | 정착 생활사(anchor) 1항 | **통과 — *떠도는 생명*에서 *고착하는 생명*으로(정착 생활사, 큰 안정 조직 → 분화/격리 발현의 전제; 0017 이래 블로커를 *열었다*)**: 각 생명이 제 국소 상태를 읽어 *잘 먹고*(m≥anchorM) *kin 에 둘러싸이면*(같은 태그 4-근방≥anchorKin) 이 tick **고착**(a.sessile=1)해 ⑥move(주화성·탐사)·⑥a adhere(재정렬)를 *건너뛴다*(제자리 유지) → 떠돌지 않는 잘 먹은 kin 코어 둘레에 이웃 kin 이 쌓여 *큰 안정 confluent 조직*으로 coalesce → 갇힌 내부 세포 늘어 0021 분화 더 발현(분화는 조밀함의 자식 — 그 조밀함을 정착이 만든다). 가역 게이트(매 tick 재판정 — 굶거나 kin 흩어지면 풀림, 자기 보호). settling 아레나(흩어진 단일 클론 + 국소 정적 먹이·정착만 격리): 고착 비율 **0.66**(잘 먹은 코어 sessileM≈2.05)·조직 평균 크기 **26.9→43.8**·조직 수 **18.6→10.0**(coalescence)·갇힌 내부 비율 **0.79→0.84**·분화 provision **239702→260969**. 회귀 maxDiff=0(kAnchor=0=step-0022, golden `germ@` 가법 잠금·`anchor@`+`tanc@` step-0024 앵커 신규)·잔차 **2.15e-14**(정착은 위치만 — 운동 skip, 거래 0)·결정론(a.sessile 게이트가 위치 변경) 일치. churn 보존(공멸 없음·전체 스택 carrying capacity ~71). **발견**: 정착은 *coalescence*(더 큰 하나 아닌 더 고른 여럿)·정착의 양의 되먹임은 *떠도는 먹이엔 진다*(정착성 생활사는 안정 자원의 자식). **한계**: 전체 스택 발현 *부분적*(여전히 희소 — sessile≈0, 블로커를 *연* 것이지 닫은 것 아님)·번식·개체수와 맞바꿈·가역 게이트(불가역 commitment 아님)·안정 자원 패치 필요·center-셀/scan |
| [0024](step-0024.md) | 곡률 기반 표면장력(tension) 1항 | **통과 — 형태 사다리 R2(형태가 필드에 실린다[R1]에서 *그 형태가 둥글다*[R2]로 — E-막에 Young-Laplace 표면장력)**: *position* 곡률은 응집(Steinberg DAH=표면장력)과 *redundant*(adhere on/off 같은 형태·보존적 단일-셀 이동은 에너지 장벽도 못 넘음)임을 확인 → R2 는 *형태가 실리는 바닥 필드 E* 에 산다(형태는 *흐름*의 하류). couple(R1) 균등화 *위에* 곡률 구배를 얹어 — 각 kin 4-쌍서 E 를 *볼록 경계*(coordination=4-근방 같은 태그 수 낮음)에서 *오목/속*(높음)으로 옮긴다(flux=γ·Δn4·E[볼록쪽], ΔP=γκ 이산 아날로그). rounding 아레나(단일 클론 16×16·adhere+couple on·tension 만 격리): 볼록 경계 E **1.93→1.62**(곡률 큰 경계의 E 가 속으로 밀려 식음 = 표면장력)·속/경계 E비 coreRatio **0.219→0.263**(E-돔)·고-E 핵 원형도 **0.092→0.103**·곡률 E flux **4437**. 회귀 maxDiff=0(kTension=0=step-0023, golden `anchor@` 가법 잠금·`curv@`+`tcurv@` step-0025 앵커 신규)·잔차 **5.7e-15**(tension 은 E 쌍 거래 — 볼록→오목, 보존)·결정론(곡률 E flux 가 E 변경) 일치. churn 보존(공멸 없음·carrying capacity ~57). **발견**: 차등 응집이 곧 표면장력(DAH) — position 곡률 redundant, R2 는 바닥 필드 E 에(형태는 개체 아닌 흐름에서 빚인다)·표면장력은 경계를 *식히지* 속을 크게 데우진 않음(경계=둘레·속=넓이)·couple↔tension 은 평탄화↔곡률 *평형*. **한계**: E-형태 둥글림이지 셀 배치 아님(그건 adhere 몫)·coalescence(막 융합) 약함(에너지 장벽)·couple 평형에 묻힘(γ↔kMembrane)·전체 스택 발현 약함(희소)·고정 규칙(OEE 아님)·center-셀/scan |
