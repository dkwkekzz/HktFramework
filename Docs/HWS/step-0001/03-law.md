# 요소 3 — 법칙 (Law)

> 흐름량을 이웃끼리만 주고받게 하는 국소 연산자. 전역 스크립트 금지.

## 정의

법칙은 (1) **국소적**이고 — 셀은 4이웃하고만 상호작용 — (2) **보존적**이거나(이동) **장부 기록형**(증발)이다. step-0001 의 법칙은 둘:

| 법칙 | 식 | 성격 |
|---|---|---|
| **확산** | `E'[i] = E[i] + kD·(ΣE[이웃4] − 4E[i])`, kD=0.20 | 보존 이동 — 기울기를 깎는다 |
| **증발** | `d = kEvap·E[i]; E[i] −= d; evaporated += d`, kEvap=0.001 | 장부 기록 — 세계를 식힌다 |
| **응집** (step-0002) | 확산 플럭스에 업힐 보정 `+kA·agg(min(eᵢ,eₙ))·(eᵢ−eₙ)`, kA=0(기본) | 보존 이동 — *창 안에서* 기울기를 키운다 |

**응집은 새 법칙이 아니라 확산 계수의 농도 의존 확장이다**(CLAUDE.md 설계 원칙). 농도 창 `agg(m)=1−((m−mc)/w)²`(|m−mc|<w 에서만 >0) 안의 셀만 이웃의 더 높은 쪽으로 거꾸로 흐른다. 커널 인자 `min(eᵢ,eₙ)`·`agg` 가 (i,n) 대칭이고 `(eᵢ−eₙ)` 가 antisymmetric → 플럭스 보존(닫힌 장부). kA=0 이면 응집 블록을 통째로 건너뛰어 **step-0001 식과 비트 동일**(회귀 0의 출발점). 자세히는 [step-0002.md §2](../step-0002.md).

## 적용 순서 고정 (불변)

```
tick = ① 확산 → ② 증발 → ③ 주입(source) → ④ 배출(sink)
```

순서가 결과를 바꾸므로, 이후 모든 step 은 이 순서의 *사이에 끼워 넣는 것이 아니라* 계수·항 확장으로 법칙을 키운다. 순서 변경은 회귀 단절 = 새 아크.

## 왜 이렇게인가

- **국소성**: "플레이어가 한 번 배운 직관이 어디서나 통하는" 세계의 전제. 전역 규칙이 하나라도 생기면 장소마다 물리가 달라진다.
- **확산만으로는 아무것도 만들어지지 않는다**: 확산+증발은 모든 구조를 *지우는* 방향이다(엔트로피). 이것이 의도다 — 구조는 법칙이 만드는 게 아니라 구동([04-drive.md](04-drive.md))과의 긴장에서 떠오른다.
- **안정 조건**: 4이웃 확산은 kD ≤ 0.25 에서 수치 안정. 표준 0.20.
- **4이웃의 비등방성(격자 한계)**: 상하좌우만 주고받으므로 응결·확산이 *축 방향에 치우친다* — step-0002 의 고임이 둥근 방울이 아니라 격자축에 정렬된 네모/마름모(facet)로 응결하는 이유([step-0002.md §6.5](../step-0002.md)). 등방성 회복(8이웃/대각 가중)은 [step-0002 §9 백로그](../step-0002.md)의 *형태 정밀화* 과제로 둔다 — 동역학의 질이 아니라 모양만 고치는 독립 항이라 미룬다.

## 검증 현황과 수정 이력

| step | 내용 | 결과 |
|---|---|---|
| [step-0001](../step-0001.md) | 확산의 보존성(V1 잔차에 포함) · 평탄화 방향성(V3 구동 off → varE 1.8e-33) | **통과** |
| [step-0002](../step-0002.md) | 응집 항 추가 — 회귀 0(kA=0 비트 동일) · 보존(잔차 1.36e-13) · 고임 출현(평균 6.2개) | **통과** |
| [step-0008](../step-0008.md) | 결정화·풍화 항 추가(구동 직후 stage ⑤) — 문턱 위 E→R(빠름)+R→E(느림), 국소 판정. 회귀 0(kCryst=0 비트 동일)·보존(잔차 2.51e-13). ①~④ 필드 법칙 순서 불변 — 생명처럼 *뒤에 덧댐* | **통과 — 흐름량의 상전이 법칙**(소산↔저장) |
| [step-0009](../step-0009.md) | 확산(①)을 기복으로 일반화 — 흐름 퍼텐셜 h=E+kRelief·R 의 내리막 donor-제한 upwind flux(퇴적이 바닥을 올려 흐름이 비켜감). 변 flux 를 양쪽이 같은 식으로 평가해 쌍 거래 보존, f-제한으로 양수성, R=0 에선 기존 확산과 대수 동일. 회귀 0(kRelief=0 비트 동일)·보존(잔차 2.53e-13). 순서 불변(① 내부 일반화) | **통과 — 법칙이 자기 산물(퇴적)에 휘는 첫 되먹임**(무대) |
| [step-0015](../step-0015.md) | R-주형 복제 항 추가(결정화 직후 ⑤d) — 자기복제하는 R-배치(genotype 태그 G)가 이웃 E→R 결정화를 촉매하며 *이산 태그를 복사*(복제오류=변이·기질경쟁=선택). genotype→phenotype 맵(fit=태그 함수)은 복제 속도만 바꿈(E 동역학 분기 없음 — 활성도 환원). 회귀 0(kTemplate=0 비트 동일)·보존(잔차 4.55e-13, E→R 쌍 거래·태그 거래 0). 한 tick=한 세대(Gb 스냅샷) | **통과 — 법칙이 자기복제하는 첫 항**(다윈 동역학 창발, SPINE §다섯째 축) |
| [step-0016](../step-0016.md) | 생명 유전 항 추가(번식 직후 ⑧b) — step-0015 의 R-genotype 을 생명 대사 엔진에 묶음: 부트스트랩(생명이 제가 선 G[center] 읽음)·상속(분열 자식이 인접 부모 태그 ±변이, reproduce 동결·부모 위치 매개)·표현형→대사세(fit 낮을수록 차등 대사세, m→metabolized 쌍 거래). 같은 태그·맵 공유(genotype 담체 이동 R→생명). 회귀 0(kInherit=0 비트 동일)·보존(잔차 4.48e-13) | **통과 — 법칙이 유전을 생명에 묶는 첫 항**(다윈 동역학이 생명 위에서, 단단한 자기복제) |
