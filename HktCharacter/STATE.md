# STATE.md — HktCharacter 현재 상태

> 이 문서만 보면 현재 상태와 다음 할 일이 명확해야 한다. 상세는 [docs/](docs/) 링크로.

**현재 버전: v5.0 (2026-07-18)** — **뼈 → 근육 → 피부 파이프라인으로 전면 재출발.**
구 v4.2(완성 스킨 FBX 뷰어 + SDF flesh 실험)의 설계·코드·문서를 전량 폐기하고, 골격에서
살을 쌓아 올리는 방식으로 깔끔히 다시 시작했다. 설계 → [docs/PIPELINE.md](docs/PIPELINE.md).

## 지금 되는 것

- **① 뼈**: X Bot·Y Bot FBX 로드 → 구동 뼈 65개, 키 1.7m 정규화, 발 접지. 원본 스킨은 버림.
- **② 근육**: 해부학 아틀라스 31개(표재근)를 명시적 **부착 패치**(`origins[]`/`insertions[]`,
  뼈 로컬 오프셋 + 뼈-축 성분 `t`)로 정의 → 방추형 벨리를 뼈 사이에 부착. 이두근은 정지부가
  **관절을 넘어가** 팔꿈치 굴곡 시 실제로 짧아지고 굵어짐(부피 보존, 라이브 갱신).
  각 근육은 **프로필 스웹 방추형**(가는 힘줄 끝 → 중앙 벌크)이라 달걀이 아니라 가늘고 길다.
  + 뼈 패딩 캡슐로 정강이·손·발·머리 채움.
- **③ 피부**: 근육·뼈 캡슐의 음함수 필드 → MarchingCubes(64³)로 표면 추출 → 정점 용접·
  법선 재계산 → 뼈에 스키닝 바인딩 → `SkinnedMesh` 로 rest 포즈에서 **한 번 굽기**.
- **애니메이션**: 6개 Mixamo 클립(대기·걷기·뛰기·점프·공격·삼바) 순수 월드 공간 리타깃 →
  뼈가 움직이면 근육·피부가 함께 변형. 클립별 사전 측정 접지.
- **UI**: 레이어 토글(뼈/근육/피부) · 모델 드롭다운(X/Y Bot) · 애니메이션 · 속도.

## 검증 현황 (v5.0)

- `npm run smoke` — X/Y Bot 각 24개 체크 전부 통과: 뼈 65·키 1.700m·접지 0.000,
  근육 31, 피부 캡슐 83, 피부 삼각형 ~20k, skinIndex<뼈수, skinWeight 합≈1,
  피부 bbox(높이 1.80·T-포즈 스팬 1.56~1.72·전후 0.40), 정점 애니메이션 변형 Δ≈0.09~0.16m.
- `npm run render` — `eval/out/` 에 근육 정면(에코르셰)·피부 정면/측면 PNG. 사람 실루엣 확인.
- ⚠️ **브라우저 육안 확인은 사용자 몫** — 샌드박스 headless Chromium 차단.

## 다음 작업 — 세부 작업 설계 완료 → WP 시퀀스로 전환

**설계서**([docs/스켈레톤 기반 절차적 근육·캐릭터 생성 시스템 설계서.md](docs/스켈레톤%20기반%20절차적%20근육·캐릭터%20생성%20시스템%20설계서.md), v0.1 시스템 설계)를
이 트랙에서 빌드 가능한 **작업 패키지(WP) 8개**로 분해했다 → [docs/WORKPLAN.md](docs/WORKPLAN.md).
현재 v5.0 은 설계서 12단계 중 **부착 패치·경로·관절 기능·조직 분할이 통째로 비어 있음**(갭 분석 §1).

**✅ WP-01 (AttachmentPatch-lite) 완료** — 부착을 관절 원점 스냅에서 **뼈 로컬 패치**로 승격.
`anatomy.js` 를 `origins[]`/`insertions[]`(`AttachmentPatch={bone,off,t,role}`) + `architecture` 로 재작성,
`muscles.js` 에 `getAttachments()` 추가. 무손실. smoke 에 §19.1 구조 + §7.1 부착 불변 게이트 추가.

**✅ WP-02 (관절 통과 부착, Joint-Crossing) 완료 (2026-07-18)** — **측정으로 근본 결함 발견**:
근육이 인접 관절 피벗 사이에 걸려(피벗 거리=강체 뼈 길이=상수) **bulge 가 절대 발동 안 함** —
이두근이 팔꿈치 굴곡에 무반응이었다. 부착 패치에 뼈-축 성분 `t` 를 더해 정지부를 원위 뼈 아래로
내려 근육이 **관절을 넘어가게** 했다. 이제 **팔꿈치 0→135° 굴곡 시 이두 len −37%·R +13%**(부피 보존
bulge 발동). 트윈 뼈(`__dup`)가 자식으로 잡히던 버그 수정. render `2-arm-neutral↔curl.png` 로 눈검증,
smoke §19.3 기능 게이트 추가. (삼두 길항 신장은 모멘트암 필요 → WP-04 이월. wrap/관통은 WP-02b 이월.)

**◐ WP-03 ① (방추형 슬렌더화) 완료 (2026-07-18)** — 측정에서 근육 종횡비 대부분 **0.3~2:1(달걀)**,
`taper` 가 벨리 형상에 안 쓰이던 게 근인. 단일 스케일 구 → 근육마다 **프로필 스웹 `LatheGeometry`**
(가는 힘줄 끝 → 중앙 벌크 → 가는 끝)로 진짜 방추형. 피부 캡슐도 프로필 따라 테이퍼(`SKIN_CAPS=4`).
근육 상태 측정 API `getBellies()` 추가. 에코르셰의 달걀들이 **뾰족한 방추형 스핀들**로, 피부 사람 형태 유지.

**◀ 다음: WP-03 ②③ (납작화 + Fan/Sheet/MultiHead)** — 넓적한 몸통 근육(대흉근·대둔근·광배근·복직근)을
방추형 아닌 판·부채꼴로. 또는 WP-04(길항). 상세 → [docs/WORKPLAN.md](docs/WORKPLAN.md) §3·§7.

WP 시퀀스·의존성·검증 게이트는 [docs/WORKPLAN.md](docs/WORKPLAN.md) 상태표(§5) 참조. 아래는 흡수된 기존 항목:

- [ ] (WP-01 편입) 근육 오프셋 극성/T-포즈 근사 → 뼈 로컬 패치로 승격 시 재정의.
- [ ] (WP-06 편입) 체형 프리셋(마른/평균/비만) — BodyProfile-lite.
- [ ] (WP-03 편입) 아틀라스 정밀화 → 아키텍처 다양화(Fan/MultiHead/Sheet)로 흡수.
- [ ] (별도 유지) 피부 품질 향상(MC 해상도·스무딩), UE5 익스포트 경로 정리 — WP 외 트랙.

## 알려진 한계

- 근육 오프셋(off.a/off.l) 수치는 T-포즈 근사 — 극단 포즈에서 관절 관통 가능. 눈검증 필요.
- 피부는 캡슐 union 이라 얼굴·손가락 등 미세 형태 없음(뭉툭). 실루엣·볼륨 우선 단계.
- 스키닝은 거리 기반 자동 가중치 — 관절 접힘부에서 볼륨 손실 가능(추후 가중치 개선).
