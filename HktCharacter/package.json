{
  "name": "hktcharacter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HktCharacter — Skeleton → Flesh: rig-agnostic SDF flesh renderer (Mixamo rig + 동봉 로코모션 FBX). asset pipeline 프로토타입.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
