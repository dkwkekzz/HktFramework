# feature-0005 — 결정화: 에너지는 응집되어 정적인 형태(결정)가 된다

> 상태: 🟢 검증됨 (step 1~4) · 상호작용(채집·용해)은 후속 · 작업 방식: [CLAUDE.md](../CLAUDE.md)

## 명제

에너지는 **결정화**된다 — 공간에 흩어져 있는 국소장(중등급)이 국소적으로 과포화되면 그 일부가 **정적 저엔트로피 형태(결정 `I:<id>`)** 로 동결한다. 결정은 스스로 에너지 질서를 *능동적으로 유지하지 않는다*(일하지 않는다) — 잠긴 국소 최소점에 수동으로 갇혀 있을 뿐이다. 그래서 **정적**이며, 엔트로픽 확산·복사에 **면역**이다(가만두면 잔고가 변하지 않는다). 얼마나 응집했는가(응집량)와 무엇과 반응하는가(반응 상대)에 따라 채집물·장착 아이템·기체/액체/고체 같은 **복합적 세계**를 표현한다 — 현실의 화학반응과 같은 결이다.

## 저엔트로피의 두 전략 — 결정은 生命의 대척점 (feature-0004 정합)

feature-0004 는 세계를 **열린 흐름 + 등급**으로 세웠다: `태양(고) → 생명 → 국소장(중, 확산) → 심우주(저, 손실)`. 국소장은 *무질서한 중등급*이라 스스로 이웃으로 새고(diffuse) 심우주로 복사(radiate)된다. 결정은 이 엔트로픽 흐름에 **저항하는 반대 극**이다. 엔트로피의 조류에 맞서는 저엔트로피 섬에는 두 종류가 있다:

| | 질서 유지 방식 | 성질 | 엔트로픽 흐름 |
|---|---|---|---|
| **생명** | **능동** — 에너지를 계속 써서 응집 유지(이동·활동) | 動的 | 활동분을 국소장으로 소산 |
| **결정 `I:<id>`** | **동결** — 잠긴 국소 최소점에 수동으로 갇힘, 일 안 함 | **靜的** | **확산·복사에 면역** |
| 국소장 `M:<voxel>` | 무질서 | 흩어짐 | 확산·복사에 지배 |

즉 **"정적인 이유"가 곧 검증 명제**가 된다 — 결정은 능동적으로 질서를 유지하지 않기에 스스로 자라지도 못하지만, 동결된 최소점이라 **확산·복사 틱을 아무리 돌려도 잔고가 불변**이다. 반면 같은 자리 국소장은 계속 샌다. 비전문가도 한눈에 본다: *결정 잔고 = 수평선, 국소장 = 심우주로 흘러내리는 곡선.*

## 최종 목적 (측정 가능)

1. **정적성(면역)**: 결정 `I:<id>` 의 잔고는 확산·복사 틱을 N회 돌려도 불변이다. 반면 같은 위치의 국소장은 감소한다(심우주로 복사·이웃으로 확산).
2. **과포화 핵생성**: 국소장 농도가 임계(포화)를 넘을 때만 결정이 석출된다. 결정 총량은 농도(과포화도)에 통계적으로 비례하고, 석출은 **자기 제한**이다(장을 포화까지 끌어내리면 멈춘다 — 현실의 침전 평형). 희귀도 = 열역학적 improbability(에너지가 국소에 쌓인 저엔트로피 요동 자체가 드문 사건).
3. **상호작용(채집·용해)**: 생명이 결정과 근접하면 정적 질서가 풀린다 — 결정 → 생명(채집) 또는 결정 → 국소장(용해).
4. **반응(화학)**: 두 결정(또는 결정+생명)이 만나면 규칙에 따라 에너지가 재분배/방출된다 — 결합(더 큰 결정)·분해(국소장으로 방출). 조합 → 결정론적 산물 + 방출량, 보존 유지.
5. **물질 상태(gas/liquid/solid)**: 응집도(결합 에너지 밀도)로 상태를 라벨링한다 — 낮으면 기체(국소장에 가까움), 중간 액체, 높으면 고체(결정). 임계를 넘으면 상전이(석출↔용해)한다.
6. **결정론 + 보존 불변**: 같은 시드·이벤트열이면 결정 배치·잔고가 비트 단위로 동일하고, 위 어떤 흐름에도 전 풀 합계 = 10⁹ (feature-0001 회귀).

## 원장 매핑 — 보존은 공짜로 유지된다

- **결정 풀**: `I:<seq>` — 위치·종을 가진 **개별** 풀. 국소장(`M:`)과 달리 확산·복사 순회 대상이 **아니므로** 면역은 코드가 아니라 *루프 대상에서 빠지는 것*으로 자연히 성립한다.
- **다양한 생성 경로** (모두 `cause: 'crystallize'`, 결정마다 **종(species)** 부여):
  - **과포화 석출**: `국소장 → 결정`. 농도가 포화 임계(hotspot)를 넘으면 초과분의 일부가 결정으로 → 자기 제한.
  - **죽음의 분해**: `생명 → 결정`. 생명체가 죽으면 단단한 잔해가 그 자리에 결정으로 응결(나머지는 국소장으로). 채집물·드랍의 근원.
  - **반응 산물**: 반경 안 결정끼리 반응해 융합/화합한다 — 다른 종 화합은 반응열을 국소장으로 되돌린다(`결정 → 국소장`). step 3.
- **강제 지점**: 모든 흐름이 `ledger.transfer` 관문을 지난다 → 전 풀 합 = 10⁹ 는 얹어도 깨지지 않는다.
- **동기화 등급(단계적)**: step 1 은 결정을 `region=null` 서버 내부 풀 + **읽기 전용 스냅샷 방송**(`CRYSTAL`, FIELD 와 같은 성격)으로 둔다 — 공간 미러링·체크섬 편입은 상호작용이 필요한 step 2 에서 `region=key` 로 승격한다.

## step 로드맵 (점진 구축 — 한 step = 한 커밋)

| step | 목표 | 핵심 | 검증 |
|---|---|---|---|
| **1** ✅ | **결정 코어 (정적성 + 과포화 핵생성)** | 결정 풀(내부, 확산·복사 면역) · 과포화 석출(국소장→결정, 자기 제한) · `CRYSTAL` 방송 + 뷰어 결정 마커 | `crystal.test.js` + 뷰어 캡처 — 아래 step 로그 |
| **2** ✅ | **개별화 + 죽음의 결정화 + 종 (다양한 생성)** | 결정을 개별 discrete `I:<seq>`(위치·종·잔고)로 · **죽음 → 잔해가 결정으로 분해**(+국소장 거름) · **종(species)** 으로 생성 다양성 · 뷰어 종별 색 | `crystal.test.js` 9종 + 뷰어 캡처 — 아래 step 로그 |
| **3** ✅ | **반응 (화학)** | 반경 안 두 결정이 종 기반 반응 → 같은 종=융합 / 다른 종=화합(새 종)+반응열 국소장 방출 | `react.test.js` 4종 + 라이브 — 아래 step 로그 |
| **4** ✅ | **물질 상태 (기체·액체·고체)** | 국소장 밀도 regime: 저=기체(등방 확산)·중=액체(응집·중력 침강·고임·저증발)·고=고체(석출). 상태는 농도로 유도, 상전이는 임계 통과 | `phase.test.js` 4종 + 라이브·뷰어 — 아래 step 로그 |
| 5 | **상호작용 (채집·용해)** | 생명 근접 시 결정 → 생명(채집) / 결정 → 국소장(용해). 결정을 `region=key` 로 승격해 체크섬·ENTER·미러에 편입 | 근접 채집·미러 정합·보존 |

> **범위 원칙**: 각 step 은 *직관적으로 검증 가능*할 때만 닫는다(CLAUDE.md 불변 원칙). step 1 은 상호작용·반응·상태를 포함하지 않는다 — "정적 저엔트로피 형태가 과포화에서 맺히고 가만두면 불변"이라는 가장 좁고 한눈에 갈리는 핵심만 세운다.

## step 로그

| step | 한 일 | 검증 |
|---|---|---|
| 1 | **결정 코어.** 결정 풀을 확산·복사 순회(`materialKeys`) 밖에 두어 **면역**을 코드가 아닌 구조로 성립시켰다. 국소장 농도가 **포화 임계(hotspot)** 를 넘으면 초과분을 결정으로 석출 — 과포화도에 비례하되 **속도 상한**(`CRYSTAL_PRECIPITATE_MAX`)으로 확산을 이기지 않게 했다. 석출은 무음 내부 이체이고 상태는 `CRYSTAL` 스냅샷으로 방송(FIELD 와 같은 읽기 전용). 뷰어는 결정을 옥타로 그려 반투명 확산 큐브와 대비. HUD 에 `◆ 결정(정적)` 등급 추가. | `crystal.test.js` — 창세 0·포화 이하 없음·과포화 석출·**정적성**(결정은 수천 틱 불변, 국소장은 심우주로 샘)·자기 제한·결정론·방송. 보존은 **다섯 곳** 합 = 10⁹. 라이브·뷰어 캡처 확인. |
| 2 | **개별화 + 죽음의 결정화 + 종.** 결정을 복셀 버킷에서 **개별 discrete 객체 `I:<seq>`**(위치·종·잔고)로 바꿨다. **죽음 → 분해**: 생명체가 죽으면 에너지가 두 갈래로 — 단단한 잔해는 그 자리에 **결정으로 응결**(`DEATH_CRYSTAL_FRACTION`, 종을 가진 개별 결정 = 다양한 드랍/채집물), 무른 조직은 국소장으로 흩어진다(feature-0004 열린 흐름 유지). **종(species)** 을 별도 rng 스트림에서 뽑아(확산 결정론 불변) 생성마다 부여 → 색·후속 반응 규칙의 씨앗. 과포화 석출도 복셀당 "거주 결정"을 키우는 discrete 로 통일. `CRYSTAL` 방송을 `[id,x,y,z,잔고,종]` 으로, 뷰어는 **종별 색 옥타**를 제 위치에 그린다. | `npm test` **32/32** — `crystal.test.js` 9종(+죽음의 결정화: 잔해 비율만큼 결정·나머지 국소장·보존, +생성 다양성: 40죽음 → 다수 종). feature-0004 죽음 테스트를 결정+국소장 분해로 갱신. **라이브**(봇 8기 5분): 죽음마다 결정이 쌓여 15개·6종 정적 잔존. **뷰어 캡처**: 여러 위치에 **종별로 다른 색**의 결정이 흩뿌려져 선다. |
| 3 | **반응(화학).** 매 `CRYSTAL_REACT_INTERVAL_TICKS`, 반경(`CRYSTAL_REACT_RADIUS`) 안의 두 결정이 종에 따라 반응한다(결정론: seq 오름차순, 각 결정 한 틱 1회). **같은 종 → 융합**(순수 응집, 반응열 없음), **다른 종 → 화합**(`reactSpecies` 로 새 종 + `sum/CRYSTAL_REACT_RELEASE_DIVISOR` 만큼 **반응열을 국소장으로 발열**). 소진된 결정은 소멸(레지스트리·거주 인덱스·원장 풀에서 제거). 반응이 쌓인 결정을 소비해 개수를 묶고(무한 누적 방지) 종 분포를 계속 뒤섞는다(창발). | `npm test` **36/36** — `react.test.js` 4종: `reactSpecies` 가환·범위 · 반경 안 두 결정 융합/화합(종 규칙 + 반응열 방출 정확·보존) · 결정론 · 창발(한 곳 16죽음 → 개수 줄고 보존). **라이브**(봇 8기 6분): 죽음이 흩어져도 뭉친 곳에서 반응이 일어 **종이 12가지 전부로 번지고**(화합 산물) 결정 총량이 발열로 오르내린다. |
| 4 | **물질 상태 = 밀도 regime.** 기체·액체·고체를 새 물질이 아니라 국소장 에너지의 **밀도 구간**으로 표현한다. **기체**(< `LIQUID_CONDENSE`): 등방 확산(feature-0004 그대로), 잘 증발. **액체**(`[CONDENSE, SATURATION)`): ① **응집** — 등방 확산이 `1/LIQUID_COHESION`(뭉쳐 안 퍼짐) ② **중력** — `#settleLiquid` 로 아래 복셀로 침강, 아래가 `LIQUID_CAPACITY` 까지 차면 넘쳐 위로(**바닥부터 고여 수면**) ③ **저증발** — 복사 divisor 를 키워 손실↓. **고체**(≥ `SATURATION`): 결정 석출(step1). 상태는 `fieldPhase(농도)` 로 유도(서버·클라 공용), 상전이는 임계 통과로 창발. 뷰어는 복셀을 상태별 색(기체 옅은 하늘빛·액체 진한 물빛·고밀도 붉음)으로 그린다. **핵심**: 밴드를 국소장 평형(~수십)보다 위에 둬 몰린 곳만 액체가 되므로 feature-0004 million-injection 확산 시뮬은 불변(>포화 = 중력·응집 미적용). | `npm test` **40/40** — `phase.test.js` 4종: `fieldPhase` 밴드 · **중력**(위에 부은 액체가 아래로 옮겨 앉음, 기체는 등방이라 안 그럼) · **수면**(바닥부터 고여 아래가 무거운 기둥, 용량 상한) · 결정론. feature-0004·결정 회귀 전부 통과. **라이브**(봇 8기 5분): 옅은 기체가 몰리며 액체로 응축→**바닥 층으로 가라앉아**(층별합 바닥이 꼭대기의 2.5배) 일부는 결정 석출. **뷰어 캡처**: 바닥에 물빛 액체가 고이고 위는 옅은 기체, 결정이 종별 색으로 선다. |

### 구현 요지 (튜닝 근거)

- **임계는 절대량이 아니라 hotspot 신호**: 라이브 국소장 평형은 복셀당 ~수십인데(봇 배회 소산), `CRYSTAL_SATURATION=200` 을 그 위에 둬 **에너지가 국소에 몰린 뜨거운 지점에서만** 결정이 맺히게 했다(평형=균일에선 안 맺힘 = 최대 엔트로피에선 질서 안 생김). 자동 테스트는 백만 단위를 주입해 강제 재현하고, 라이브는 드물게 자연 발생한다 — 둘 다 같은 규칙.
- **속도 상한이 곧 매질 보존**: 석출을 `CRYSTAL_PRECIPITATE_MAX` 로 묶어 아무리 몰려도 확산(조류)을 압도하지 못하게 했다 — 국소장은 계속 *흩어지는 매질*로 남고, 결정은 그 위에 드물게 맺히는 *섬*이 된다. feature-0004 확산 시뮬이 흔들리지 않는 이유.

## feature-0004 와의 정합

feature-0004 의 예정 step 3(재응집·재료·아이템·`I:<id>` 결정화)를 **이 feature 로 이관**한다. feature-0004 는 *엔트로픽 조류*(확산·복사, step 1~2 검증됨)에서 닫고, *조류에 맞서 맺히는 질서 섬*(결정화)은 feature-0005 가 맡는다 — 개념이 깔끔하게 갈린다.

## 검증 (항상 가능)

```bash
npm test        # test/crystal.test.js (정적성·핵생성·자기제한·결정론·보존) + 기존 회귀 전부
./run.sh 6      # 라이브 — 에너지가 국소에 쌓이는 곳에 결정 마커가 맺히고, 국소장은 새어도 결정 잔고는 불변
```

## 상세 문서 인덱스

- (없음)
