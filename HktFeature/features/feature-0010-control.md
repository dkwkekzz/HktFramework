# feature-0010 — 제어: 플레이어/봇이 하나의 생명체를 제어한다 (욕망→이동→에너지)

> 상태: 🟡 진행 중 (step 1~4 검증됨) · 작업 방식: [CLAUDE.md](../CLAUDE.md)
> 배경: feature-0006~0009 는 생명체가 **스스로** 자기유지·채집·포식·방출을 도는 규칙을 세웠다. 이 feature 는 그 위에 **행위자(플레이어·봇)** 를 얹는다 — "누가 이 생명체를 몰고, 무엇을 원하게 하는가".
> **재층위([feature-0018](feature-0018-motive-strategy.md))**: 이후 논의로 욕구를 두 층으로 갈랐다 — 이 문서가 "욕망"이라 부르며 부여한 채집·사냥·제조 등은 0018 층위에서 **전략(수단)** 이고, 플레이어의 버튼·표적 클릭은 동기 주입이 아니라 **명령**이다(step 4 "표적이 곧 의도"가 이미 이 방향이다). 이 feature 의 검증된 핵심 — **욕망→이동(지출)→도달→획득(수입)의 에너지 순환, 수입>비용일 때만 값어치, 이동은 에너지로 지불** — 은 층 분리와 무관하게 불변이며, 동기·전략 어느 층의 수행에도 같은 회계로 적용된다. 욕구의 재정의·분류는 feature-0018 참조.

## 명제

플레이어(또는 봇)는 **하나의 생명체를 제어한다.** 제어의 핵심은 **욕망(desire)** — 생명체에 부여하는 동기다. 욕망은 무엇을 향해 에너지를 얻으러 갈지를 정하고(채집·사냥·… — 예시일 뿐, 이후 확장의 근간), 생명체는 그 욕망의 표적으로 **이동**한다. **이동은 욕망을 이루기 위한 수단일 뿐이다** — 그리고 그 수단은 공짜가 아니라 **에너지로 지불된다**(이동 소산 = 생명체→국소장, feature-0004 의 MOVE 와 같은 회계). 표적에 도달하면 기존 획득 규칙(채집 feature-0007·사냥 feature-0008)이 **수입**을 만든다. 그래서 한 욕망은 **수입 > 이동 비용**일 때만 값어치가 있다 — **제어의 전 과정이 에너지 흐름으로 닫힌다(feature-0001~0009 정합).** 욕망이 없는 소유 생명체는 주인 곁을 따른다(방향키로 카메라 방향 이동 = 수동으로 데려가는 것).

## 제어 = 에너지 흐름의 방향키 (feature-0004~0009 정합)

feature-0006~0009 는 생명체가 에너지를 얻는 세 수입(갈구·채집·강탈)과 한 지출(방출)을 세웠다. 하지만 이제껏 생명체는 **제자리**였다 — 마침 그 자리에 있는 것만 먹었다. 제어는 여기에 **행위(agency)** 를 더한다:

| | 이제껏(feature-0006~0009) | 제어(feature-0010) |
|---|---|---|
| 표적 | 그 자리에 마침 있는 것 | **욕망이 고른 표적**(결정·먹이·…)으로 향한다 |
| 이동 | 없음(정지) | 욕망의 표적으로 **한 걸음씩** 나아간다 |
| 이동의 값 | — | 나아간 거리만큼 **국소장으로 소산**(생명체→국소장, MOVE) |
| 닫힘 | 수동적 획득 | **욕망→이동(지출)→도달→획득(수입)** 의 순환 |

즉 **욕망은 에너지 흐름의 방향을 정하는 손잡이**다. 생명체는 욕망이 가리키는 에너지원 쪽으로 제 활동 에너지를 태워(이동 소산) 다가가고, 도달하면 그 에너지원을 흡수한다. **이동이 곧 "욕망을 이루려는 일"이고, 그 일에는 열역학적 대가(소산)가 붙는다** — feature-0006 의 대사(살아있음의 비용)와 같은 결이다. 그래서 먼 표적은 비싸고, 굶주린 개체는 쫓을 여력이 없다(예비 가드). 제어는 새 에너지 종류를 만들지 않는다 — **기존 흐름의 방향만 정한다.**

## 최종 목적 (측정 가능)

1. **제어 결선**: 플레이어/봇이 하나의 생명체를 소유(`owner`)한다 — SOURCE→생명체 인출(feature-0003, 보존). 떠나면 야생으로 돌아간다(소유·욕망 해제, 에너지 그대로).
2. **욕망→이동**: 욕망(채집·사냥)이 있으면 생명체가 그 표적(가장 가까운 결정·더 작은 먹이)으로 **다가간다**(거리 감소). 표적 사거리 안이면 멈춘다(도달).
3. **이동 = 에너지 지출**: 나아간 거리만큼 생명체→국소장으로 소산된다(MOVE tx). "이동은 욕망의 수단이고, 수단은 에너지로 지불된다" — 굶주리면(예비 이하) 못 쫓는다.
4. **욕망 이룸 = 수입**: 표적에 도달하면 채집(feature-0007)·강탈(feature-0008)로 에너지를 얻는다 — 이동(지출)과 획득(수입)이 하나의 순환으로 닫힌다.
5. **수동 이동**: 욕망 없는 소유 생명체는 주인(방향키로 움직이는 위치)을 따라온다 — "방향키로 카메라 방향 이동"이 곧 내 생명체를 곁에 데려가는 것.
6. **결정론 + 보존 불변**: 같은 배치·욕망·이벤트열이면 위치·잔고가 비트 단위로 동일하고, 어떤 이동·획득에도 전 풀 합계 = 10⁹.

## 욕망을 어떻게 고르는가 (부여 수단 — 사람 vs 봇)

욕망은 **부여되는 것**이고, 부여 주체가 둘이다. 둘 다 특권 없이 같은 인텐트(`DESIRE`)를 쏜다:

- **사람(플레이어)**: 뷰어의 **욕망 버튼**(`🌿 채집 · ⚔ 사냥 · ✋ 대기`, 단축키 `1·2·0`)을 눌러 고른다. 버튼은 내 생명체의 **실제 desire**(CREATURE 스냅샷)에 맞춰 강조돼 지금 무엇을 원하는지 한눈에 보인다. 대기(none)면 방향키로 직접 데려간다(수동 이동).
- **봇(AI)**: 스스로 고른다 — **제 생명체 상태를 미러(사람과 같은 관측 채널)로 보고** 결정론적 정책으로 결정한다(`tools/bots.js` `chooseDesire`). step 1 정책은 **성장 사다리**다:
  - 아직 못 봄 / 작음(`size < 2`) → **채집**: `size 1` 은 사냥(포식=강자→약자)을 못 하니 먼저 결정을 모아 큰다(feature-0006·0007).
  - 충분히 자람(`size ≥ 2`) → **사냥**: 포식자가 되어 더 작은 생명체를 뜯는다(feature-0008, 더 큰 수입).
  봇 마커는 제 생명체 곁에 머물러(관측·relevancy 유지) 계속 지켜보며 재평가한다. **이 `chooseDesire` 가 곧 확장 지점**이다 — 표적 유무·위험 회피·상위 목표·LLM 의도 같은 더 똑똑한 정책을 여기에 얹는다("이후 확장해나갈 근간"). 지금은 성장 사다리라는 가장 단순하고 한눈에 갈리는 규칙만 세운다.

즉 "욕망 선택"은 **행위자의 브레인**이 하는 일이고(사람=버튼, 봇=정책), 생명체는 그 욕망을 에너지 흐름(이동→획득)으로 실행할 뿐이다. 브레인을 교체·고도화하는 것이 이 트랙의 성장 방향이다.

## 원장 매핑 — 새 풀 없음, 생명체에 상태·이동 엣지만 추가

- **제어 상태**(에너지 풀 아님, 순수 상태 변수): `owner`(제어자 id, null=야생) · `desire`(욕망) · `moveDebt`(이동 비용 잔여 거리 누적). size·growth 처럼 보존과 무관하게 창발한다.
- **이동**(전부 `ledger.transfer` 관문 → 보존·정수): `생명체 → 국소장` (`move`) = 나아간 거리/50 만큼 그 자리 국소장으로 소산(플레이어 MOVE 와 동일 회계).
- **획득**은 기존 규칙 그대로: 채집 `결정 → 생명체`(`harvest`, feature-0007) · 강탈 `먹이 → 포식자`(`attack`, feature-0008).
- **인텐트**: `DESIRE { desire }` (클라→서버) = 내 생명체에 욕망 부여. `possessCreature`(라이브 진입점에서 접속 시) = 생명체 소유.
- **결정론**: `#pursueDesire()` 는 seq 오름차순 + 순수 클램프(rng 미사용) → 확산·성장·전투 결정론 불변.

## step 로드맵 (점진 구축 — 한 step = 한 커밋)

| step | 목표 | 핵심 | 검증 |
|---|---|---|---|
| **1** ✅ | **제어 코어 (욕망→이동→에너지)** | 소유(`owner`)·욕망(`desire`) · `#pursueDesire`(표적으로 이동, 비용→국소장 MOVE) · 채집·사냥 표적 · 예비 가드 · 수동 추종 · `DESIRE` 인텐트 · 뷰어(욕망 라벨·표적선·내 생명체 강조) | `control.test.js` 16종 + 뷰어·라이브 — 아래 step 로그 |
| **2** ✅ | **제조(craft) 욕구 — 새 욕구를 얹어 "확장의 근간" 증명** | 새 욕구(제조)를 절차 레지스트리(feature-0011 `registerDesire`)에 얹는다: 두 재료(raw) 결정을 조합해 산물 산출, 조합=열+연기 방출. 엔진 무수정 | `craft.test.js` 7종 + 뷰어·시각 검증 — 아래 step 로그 |
| **3** ✅ | **아바타 통합 + 관찰성** | 카메라가 소유 생명체를 태운다(주인공=생명체, 플레이어 점=조향 레티클) · 욕구별 강한 시각 피드백(오라·아이콘·마칭앤츠 표적선·행동 펄스·HUD 콜아웃) · 접속 시 **표적 보장 아레나**(욕구마다 눈에 보이는 표적) | `npm run shot:control` 3컷 + 라이브 — 아래 step 로그 |
| **4** ✅ | **클릭/터치 표적 지목** | 표적(결정·생명체)을 **클릭**하면 그 특정 대상으로 가서 상호작용(결정=식사·더 작은 생명체=사냥) — 자동 최근접 desire 대신 **지목한 대상 고정**(commandedTarget). 빈 곳 클릭=해제(대기→수동 이동). "표적이 곧 의도" | `TARGET` 인텐트 + 라이브(브라우저 클릭) — 아래 step 로그 |

> **범위 원칙**: step 1 은 "욕망이 이동을 부르고, 이동은 에너지로 지불된다(제어의 전 과정이 에너지 흐름)"는 가장 좁고 한눈에 갈리는 핵심만 세운다. 채집·사냥은 표적 규칙의 **예시**이고, 제조 등 새 욕망은 step 2 의 확장 지점이다(명제의 "이후 확장해나갈 근간").

## step 로그

| step | 한 일 | 검증 |
|---|---|---|
| 1 | **제어 코어(욕망→이동→에너지) + 부여 수단(버튼·봇 브레인).** 생명체에 제어 상태(`owner`·`desire`·`moveDebt`)를 붙이고, 대사·전투 앞에 새 주기(`PURSUE_INTERVAL=1틱`)로 `#pursueDesire()` 를 넣었다 — 욕망 있는 각 생명체가 제 표적(채집=감지 반경 안 가장 가까운 결정 / 사냥=가장 가까운 더 작은 먹이)으로 최대 `STRIDE(24px)` 한 걸음 나아가고, **나아간 거리/50 만큼 그 자리 국소장으로 소산**한다(`move`, 플레이어 이동과 동일 회계). 표적 사거리(채집 300·사냥 200) 안이면 멈춰(도달) 곧이어 대사·전투가 획득을 만든다 — **이동(지출)→도달→획득(수입)** 순환. 예비 이하면 못 쫓는다. 욕망 없는 소유 생명체는 주인 위치로 향한다(수동 추종=방향키). `possessCreature`(라이브 접속 시)·`DESIRE` 인텐트로 제어하고, 떠나면 야생으로(소유·욕망 해제, 보존). **부여 수단**: 사람은 뷰어 **욕망 버튼**(🌿채집·⚔사냥·✋대기, 단축키 1·2·0 — 내 생명체 실제 desire 로 자동 강조)으로, 봇은 **스스로**(제 생명체를 미러로 보고 `chooseDesire`: 작으면 채집으로 크고 size≥2 면 사냥 = 성장 사다리, 마커는 제 생명체 곁에 머묾)로 고른다 — 확장 지점. 순수 클램프(rng 미사용)라 확산·성장·전투 결정론 불변, 전부 `ledger.transfer` 라 보존 자동. `CREATURE` 스냅샷에 `desire·owner` 실어 뷰어가 **욕망 라벨·표적선·내 생명체 금색 강조**로 그린다. | `npm test` **89/89** — `control.test.js` 15종: 창세 무제어·possess·**욕망(채집)→이동**·**이동=에너지 지출**(생명체→국소장 MOVE, 보존)·**욕망 이룸**(결정 도달→채집)·**욕망(사냥)→이동→강탈**·**굶주리면 못 쫓는다**·**수동 추종**·**표적 없으면 제자리**·**야생 정지성**·**DESIRE 부여**·**제어 해제**·**방송(desire·owner)**·**결정론**·**보존 폭풍**. 기존 74 회귀 전부 통과. **시각 검증**(`npm run shot` — 실제 뷰어 3컷 캡처): 채집 욕망 생명체가 결정으로 표적선을 그리며 이동(tx [이동] 생명체→국소장)해 사거리(300px)에서 멈추고 결정을 흡수(잔고 385→997, 결정 6,016→5,344, tx [채집] 결정→생명체), 총합 10⁹·체크섬 OK. |
| 4 | **클릭/터치 표적 지목 — "표적이 곧 의도".** step1~3 은 욕망(추상 동기)을 걸면 엔진이 **가장 가까운** 표적을 자동으로 골랐다. 이 step 은 플레이어가 화면에서 **표적을 직접 클릭/터치해 지목**하게 한다 — 지목한 그 특정 대상으로 가서 상호작용한다(자동 최근접이 아니라 명시 선택). 표적 종류로 욕구를 **추론**: **결정=식사**(EAT — 날것이면 요리해 먹기)·**더 작은 생명체=사냥**(HUNT). 서버에 `commandedTarget`(표적 seq) 상태를 얹어 `#nearestCrystalFor`·`#nearestPrey` 가 지정 표적을 우선 반환(SEEK 무시=명시 선택)하고, 절차(feature-0011)는 그대로 그 표적으로 이동·수행한다(엔진 무수정). 표적이 소진·소멸하면 `#performDesire` 가 지정을 해제하고 대기로 복귀한다(**클릭 → 한 가지 수행 → 멈춤**). 빈 곳 클릭 = 해제(대기 → 방향키 수동 이동·상하 R/F). 클라: `render.#pickAt` 가 각 대상의 월드 중심을 **화면투영**(#pt)해 클릭점과의 픽셀 거리로 고르고(드래그(회전)와는 이동거리<6px 로 구분, 터치 탭 지원), `TARGET` 인텐트를 쏜다. `CREATURE` 스냅샷에 `cmd`(지정 표적) 실어 표적선·조준 고리가 지목한 것을 콕 집는다. **순수 상태·표시 계층**(에너지 무관, 표적 seq 로 결정론)이라 보존·결정론·기존 회귀 불변(commandedTarget=null 이면 종전과 동일). | `TARGET` 인텐트 서버 처리(setTarget: 표적→욕구 추론·유효성) + 라이브 시각 검증(브라우저에서 결정을 클릭하면 생명체가 굵은 표적선을 그리며 그리로 가 먹고, 작은 먹이를 클릭하면 사냥, 빈 곳 클릭하면 대기로 복귀). `npm test` 회귀(지정 없으면 자동 최근접 = 종전 동일). |
| 3 | **아바타 통합 + 관찰성 — "각 욕구가 눈에 보이는 뚜렷한 현상"을 확실히.** step1~2 는 규칙(욕망→이동→에너지·제조)을 세웠지만, 라이브에서 **욕구를 눌러도 눈에 띄는 현상이 모호**했다 — 세 원인: (a) 카메라가 보이지 않는 **플레이어 점**을 따라가 내 생명체가 화면 밖으로 벗어남, (b) 스폰 직후 사거리 안에 **표적이 없어** 아무 일도 안 일어남, (c) 피드백(옅은 점선·느린 이동)이 너무 약함. 이 step 은 셋을 닫는다: **① 아바타 통합** — `render.#camera` 가 소유 생명체를 표적으로 삼아 카메라가 내 생명체를 태운다(주인공=생명체). 플레이어 점은 은은한 **조향 레티클**로 바꾸고 금색 고리는 생명체에만(중복 제거). **② 표적 보장 아레나** — 접속 시 `seedControlArena`(index.js)가 아바타를 사냥 가능한 몸(size 2)으로 세우고 둘레에 **먹을 결정 둘(채집·식사)·붙은 재료 쌍(제조)·작은 먹이(사냥)** 를 시드한다(SOURCE→…, 보존). 자동 채집 반경(300) **밖**(~450px)·감지 반경(900) **안** — 누르기 전엔 그대로, 누르면 걸어가 수행한다. `game.spawnFood`(먹을 결정) 헬퍼 추가. **③ 강한 시각 피드백** — 내 생명체에 **욕구 오라**(승자 욕구색 맥동 고리, 사거리 도달=행동 중이면 번뜩임)·**큰 아이콘 라벨**(🌿⚔🍚🔨)·**마칭앤츠 표적선+조준 고리**(굵고 흐르는 점선)·**HUD 콜아웃**("🌿 채집 — 표적으로 이동 중 → / 도달·수행 중 ✦ / 주변에 표적 없음")을 얹었다. 순수 표시 계층(원장 미러 읽기 전용)이라 보존·결정론·기존 테스트 불변. | `npm run shot:control` — 제어 씬 3컷: (1) 채집 이동(굵은 표적선+오라), (2) 도달·수행(오라 번뜩·잔고↑), (3) 단축키 2 로 **사냥 전환**(표적선이 먹이로 갈아탐). `npm test` 회귀(표시·시드 계층은 규칙 미변경) + `npm run demo`(scene control) 라이브. |
| 2 | **제조(craft) 욕구 — 새 욕구를 얹어 "욕망은 확장의 근간" 증명.** step1 은 채집·사냥 욕구를 세웠고, 그 사이 feature-0011 이 욕구를 **절차 레지스트리**(`registerDesire`)로, feature-0012 가 **우선순위 스택**으로 일반화했다. 이 step 은 그 위에 **새 욕구 `craft`** 를 실제로 얹는 첫 사례다(엔진 `game.js` 는 이 욕구를 모른다 — ctx 만으로 실행). 절차: **[재료로 다가가기 · 조합]**. 제조 욕구를 가진 생명체가 감지 반경 안, 서로 `CRAFT_PAIR_RADIUS(220px)` 안에 붙어 있는 두 **재료(raw, 미가공)** 결정(조합 지점)으로 이동해, `#craft` 로 하나의 **산물**로 조합한다: 재료 b 를 a 로 전량 합치고(결정→결정), a 를 산물로 변형(`craftedSpecies`=재료와 다른 종, `crafted=true` 표식, `raw=false`). **조합의 일**은 `CRAFT_COST×size` 를 열(심우주)+연기(국소장)로 방출한다(요리·방출과 같은 순수 지출). **재료(raw)는 수동 반응(`#react`)에 면역**으로 둬(그 한 줄), 생명체가 올 때까지 흩어지지 않게 했다(안정 유지). 산물(crafted)은 재료로 재선택 안 돼 무한 재조합이 없다. `CRYSTAL` 스냅샷·뷰어에 `crafted` 를 실어 산물을 **굵은 외곽+겹고리+✦제조** 라벨로, 욕구 버튼에 🔨제조(4) 추가. 전부 `ledger.transfer`·rng 미사용 → 보존·결정론 불변. | `npm test` **121/121** — `craft.test.js` 7종: **레지스트리 등록**(새 욕구 인텐트 수용)·**조합 지점으로 이동→산출**(재료 2→산물 1, craftedSpecies)·**제조 방출**(열+연기, SINK↑, 보존)·**재선택 없음**(산물 혼자면 재조합 안 함)·**우선순위 정합**(제조>사냥이면 조합, feature-0012)·**결정론**·**보존 폭풍**. 기존 114 회귀 전부 통과. **시각 검증**(`npm run shot`): 제조 욕구 생명체가 **두 재료(회색 점선 옥타)**로 표적선을 그리며 다가가 조합 → **하나의 산물(✦제조, 선명·굵은 외곽)** 로 묶인다(결정 2→1), tx `[제조] 생명체→심우주 · 생명체→국소장 · 결정→결정`, 총합 10⁹. |

### 구현 요지 (튜닝 근거)

- **이동은 반드시 에너지로 지불**: 표적으로 순간이동시키지 않고 `STRIDE` 씩 나아가며 거리를 국소장으로 소산시킨 것이 핵심 — 이래야 "이동은 욕망의 수단이고 대가가 있다"가 MOVE tx·국소장 증가로 한눈에 검증되고, 먼 표적/굶주림이 자연히 제약이 된다(제어가 공짜가 아니다). 플레이어 이동(feature-0004)과 **같은 회계**를 써 흐름이 하나로 이어진다.
- **욕망은 방향만 정한다(새 에너지 없음)**: 욕망은 표적을 고를 뿐 획득은 기존 규칙(채집·강탈)이 한다 — 그래서 제어를 얹어도 보존·결정론이 그대로다. 새 욕구는 feature-0011 이후 `registerDesire` 로 절차를 얹으면 된다(확장의 근간 — step2 제조가 첫 사례).
- **(step2) 제조 = "확장의 근간"의 실제 증명**: 제조는 game.js 를 한 줄도 안 고치고(엔진은 `craft` 를 모름) 절차 레지스트리(shared/desires.js)에 얹어 동작한다 — feature-0011 의 개방성(런타임 등록 증명)이 이제 **실제 새 게임플레이(제조)**로 쓰인다. 조합·산출 로직은 ctx(`craftPair`·`craft`)에만 있다.
- **(step2) 재료(raw)를 반응 면역으로**: 제조의 재료 쌍은 수동 반응(#react, 반경 400)이 조합 전에 삼켜버려 불안정했다. `raw` 결정을 반응에서 제외하는 **한 줄**로 재료를 안정화했다("raw = 미가공 재료 = 자발적으로 반응하지 않음"). 이 규칙은 feature-0005/0011 회귀에 무영향(반응 테스트는 raw 결정을 안 쓴다). 산물(crafted)은 재료로 재선택 안 돼 무한 재조합도 없다.
- **정지성 = 기존 feature 불변**: `owner=null·desire=none`(창세·생태 생명체)는 추적하지 않는다 — feature-0006~0009 의 정지 생명체·테스트가 그대로 성립한다. 제어는 소유된 생명체에만 이동을 준다.
- **라이브 possess 는 진입점 전용**: `addPlayer`/구성자는 건드리지 않고(테스트 창세 0·무생명 불변) 라이브 진입점(`index.js`)에서만 접속 시 생명체를 쥐어준다 — feature-0006 먹이 웅덩이와 같은 결.

## 검증 (항상 가능)

```bash
npm test          # test/control.test.js(제어) + craft.test.js(제조, step2) + 기존 회귀 전부
npm run shot:control  # 시각 검증(step3 아바타·관찰성) — 채집 이동→도달·수행→사냥 전환 3컷(tools/shots/control-*.png)
npm run shot      # 시각 검증(step2 제조) — 두 재료를 산물로 조합하는 3컷(tools/shots/craftchain-*.png)
npm run demo      # 데모 서버 — http://localhost:8080 (기본 자율감정 씬. 제어 씬은 아래 PORT/scene 로)
./run.sh 6        # 라이브(봇 6기) — 각 봇이 스스로 욕망을 골라 표적으로 이동·채집·사냥. tx 피드에 [이동]·[채집]·[강탈]
```

브라우저(관전자)에서: 접속하면 **카메라가 내 생명체(금색 고리)를 태운다**(주인공=생명체, 플레이어 점은 은은한 조향 레티클). 둘레에 욕구별 표적이 시드돼 있다. **표적(결정·생명체)을 클릭/터치하면**(step4) 내 생명체가 **굵은 마칭앤츠 표적선**을 그리며 그 지목한 대상으로 이동하고, 사거리에 닿으면 **욕구 오라가 번뜩이며** 상호작용한다(결정=먹기, 더 작은 생명체=사냥). 표적을 다 쓰면 대기로 돌아온다. **빈 곳 클릭 = 대기**(방향키 WASD 수평·R/F 상하로 직접 데려간다). 클릭 대신 상단 **욕망 버튼**(🌿채집·⚔사냥·🍚식사·🔨제조·✋대기, 또는 `1·2·3·4·0`)을 누르면 **가장 가까운** 표적으로 자동 수행한다. 좌상 **HUD 콜아웃**이 "지금 무엇을 하는가"(이동 중 → / 도달·수행 중 ✦ / 표적 없음)를 한 줄로 알려준다.

## 상세 문서 인덱스

- 제조(craft, step2): [../shared/desires.js](../shared/desires.js)(craft 절차) · [../server/game.js](../server/game.js)(`#craftPairFor`·`#craft`) · [../shared/constants.js](../shared/constants.js)(`CRAFT_*`·`craftedSpecies`).
- 아바타 통합·관찰성(step3): [../client/render.js](../client/render.js)(`#myCreature`·`#camera`=카메라 태우기·`#drawSelf`=조향 레티클·`#desireLink`=마칭앤츠 표적선·`#desireActing`=행동 판정·`#creatureOrb`=욕구 오라·아이콘·HUD 콜아웃) · [../server/index.js](../server/index.js)(`seedControlArena`=표적 보장) · [../server/game.js](../server/game.js)(`spawnFood`=먹을 결정 헬퍼) · [../tools/demo-control.mjs](../tools/demo-control.mjs)(scene `control`) · [../tools/shot-control.mjs](../tools/shot-control.mjs)(시각 검증 3컷).
- 클릭 표적 지목(step4): [../shared/protocol.js](../shared/protocol.js)(`MSG.TARGET`·`CREATURE.cmd`) · [../server/game.js](../server/game.js)(`setTarget`=표적→욕구 추론·`commandedTarget` 상태·`#nearestCrystalFor`/`#nearestPrey` 지정 우선·`#performDesire` 소진 시 해제·`#flush` cmd 방송) · [../client/render.js](../client/render.js)(`#pickAt`=화면투영 피킹·클릭/드래그 구분·`#desireTargetPos` 지정 우선) · [../client/state.js](../client/state.js)(`cmd` 파싱) · [../client/main.js](../client/main.js)(`onSelectTarget`=TARGET 결선) · [../client/index.html](../client/index.html)(클릭 힌트).
