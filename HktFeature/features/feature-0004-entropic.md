# feature-0004 — 엔트로픽: 에너지는 높은 확률로 흩어질 뿐, 세계는 평형으로 향한다

> 상태: 🟡 진행 중 (step 1 검증됨 · step 2~ 예정) · 작업 방식: [CLAUDE.md](../CLAUDE.md)

## 명제

에너지의 흐름은 물리학의 법칙(엔트로피 제2법칙)을 참고한다. 에너지는 강제로 이동하지 않는다 — **엔트로픽 법칙에 따라 높은 확률로 흩어질 뿐**이다. 개별 이체는 확률적이되(오르막도 가능), 앙상블은 압도적으로 고농도→저농도(down-gradient)이며, 세계는 평형(최대 엔트로피 = 균일)으로 향한다. 되돌아오지 않는 손실(복사)이 있어 엔트로피는 단조 증가한다(시간의 화살).

## 최종 목적 (측정 가능)

1. **결정론**: 같은 시드·이벤트열이면 확산 결과가 비트 단위로 동일하다(재현 가능).
2. **엔트로픽 편향**: 이웃 국소장 사이 이체 방향 확률은 `concFrom/(concFrom+concTo)` — 고농도일수록 나갈 확률↑, 농도가 같으면 1/2(순 흐름 0 = 평형).
3. **평형 수렴**: 한 곳에 몰린 국소장은 확산으로 균일해진다 — 스프레드(최대−최소)가 급감한다.
4. **엔트로피의 화살**: 심우주(SINK)는 **단조 증가**한다(복사는 되돌아오지 않는다).
5. **보존은 불변**: 위 어떤 흐름에도 전 풀 합계 = 10⁹ (feature-0001 회귀).

## 접근 — 결정론 규칙을 확률 과정으로, 에너지를 "등급"으로

엔트로피는 양이 아니라 **품질/등급**의 문제다. 에너지를 세 등급으로 흐르게 한다:

| 등급 | 정체 | 풀 | 재응집 |
|---|---|---|---|
| 고 (저엔트로피) | 태양 — 영원한 저엔트로피 원천(스폰으로만 방출) | `W:SRC` | 원천 |
| **중** | **국소장 — 흩어진 에너지가 국소에 쌓인 것(거름·재료의 씨앗)** | **`M:<region>`** | ✅ (채집·제작·섭취는 후속 step) |
| 저 (고엔트로피) | 심우주 — 복사로 새어나간 진짜 손실 | `W:SINK` | ❌ 손실 |

흐름(전부 `ledger.transfer` 관문 통과 → 보존·정수 강제):
- **스폰**: `태양 → 생명` (저엔트로피 주입).
- **이동/죽음 = 소산**: `생명 → "그 자리" 국소장`. 활동 에너지·응집 소멸분이 국소로 흩어진다.
- **엔트로픽 확산(핵심 법칙)**: `국소장 ↔ 이웃 국소장`. 시드 동전이 방향을 정하되 **높은 확률로 down-gradient** → 균일로 수렴.
- **복사(엔트로피 세금)**: `국소장 → 심우주`. 아주 작은 일부가 영영 손실(단조 증가). 정수 유지를 위해 확률 반올림(기대율 = 잔고/`RADIATE_DIVISOR`).

## feature-0003 과의 관계 — 태양 순환(반엔트로피 텔레포트)의 삭제

feature-0003 은 `SINK→SOURCE` 태양 순환으로 세계를 닫힌 루프로 만들어 영속을 샀다. 그러나 그 순환은 **"소산된(죽은) 에너지가 태양으로 되돌아간다"는 물리적 거짓**이었다 — 실제로 시체의 응집이 풀리면 에너지는 태양으로 날아가지 않고 *주변으로 흩어져 국소적으로 재응집(거름·광물·재료)* 한다. feature-0004 는 이 텔레포트를 **삭제**하고 죽음/소산을 **그 자리 국소장**으로 돌린다.

그 대가로 닫힌 루프의 영속은 사라진다. 대신 **열린 흐름**(`태양 → 생명 → 국소장 → 심우주`)이 되고, 영속은 *태양의 방대함이 주는 실용적 영속*이 된다 — 엔트로피의 화살(심우주의 단조 증가)은 실재한다. 부정직한 가정은 "영원한 태양" 단 하나로 줄고, 나머지(확산·죽음·재응집·손실)는 전부 물리다. 이로써 feature-0003 의 여전히 참인 부분(태양=유일 원점·이동은 소산·보존)은 유지하되 "닫힌 루프 영속"만 "열린 흐름 + 실용적 영속"으로 개정된다([feature-0003](feature-0003-closed-thermo-loop.md) 개정 노트 참조).

## step 로그

| step | 한 일 | 검증 |
|---|---|---|
| 1 | **열역학 코어 개정 + 엔트로픽 확산 도입.** ① `SINK→SOURCE` 태양 순환 삭제. ② 국소장 `M:<region>` 도입(지역 컬럼마다 하나, step 1 은 `region=null` 로 두어 방송·체크섬 무관한 서버 내부 무음 이체). ③ 이동·죽음 소산을 국소장으로 리라우트(태양행 아님). ④ 이웃 국소장 간 엔트로픽 확산(시드 동전·정수 양자) + 심우주 복사 세금(확률 반올림). WELCOME·CHECKSUM 에 `mat` 실어 뷰어·봇이 세 등급을 전시. | `npm test` 21/21 — `test/thermo.test.js` 5종(창세·죽음→국소장·엔트로픽 법칙 수렴·결정론·SINK 단조) + `game.test.js`(이동/종료가 국소장으로, 네 등급 합 = 10⁹). 라이브: `[원장] … 태양 … · 국소장 … · 심우주 …(↑) · 체크섬 OK`, 총합 10⁹ 불변 |

### 예정 (step 2~)

- **step 2 — 공간화/네트워킹**: 국소장 `region=key` 로 전환해 휴면 체크섬 격자를 깨우고, 확산을 relevancy 방송에 실어 뷰어 지도에서 "잉크 번지듯" 확산을 눈으로 본다.
- **step 3 — 재응집(재료·아이템)**: 국소장에서 생명이 국소적으로 에너지를 끌어올림(채집·섭취) + 희귀 이산 아이템 `I:<id>` 결정화. **희귀도 = 열역학적 improbability**(저엔트로피 요동 = 희귀 드랍). (ㄷ) 웅덩이+이산 병행.

## 검증 (항상 가능)

```bash
npm test        # test/thermo.test.js (엔트로픽 5종) + game.test.js (소산→국소장)
./run.sh 6      # 라이브 — [원장] 줄의 국소장이 차오르고 심우주가 단조 증가(↑)
```

- **자동**: `test/thermo.test.js` 가 (a) 창세 국소장·심우주 0 (b) 죽음·이동이 태양 아닌 국소장으로 (c) `entropicOutProb` 편향 + 몰린 장이 확산으로 균일 수렴(스프레드 급감) (d) 같은 시드 → 비트 동일 (e) 심우주 단조 증가·네 등급 합 = 10⁹ 를 단언.
- **직관**: 봇/뷰어의 `태양 · 국소장 · 심우주(↑)` — 이동하면 국소장이 차오르고, 심우주가 결코 줄지 않고 오른다(엔트로피의 화살). 라이브 실측: 총 10⁹ 불변, 태양 999,998,200(=10⁹−6×스폰), 국소장 상승, 심우주 1↑, 체크섬 OK.
- **강제 지점**: 확산·복사·소산 모두 `ledger.transfer` 를 지난다 — 보존·정수는 관문의 성질이라 엔트로피를 얹어도 깨지지 않는다.

## 상세 문서 인덱스

- (없음)
