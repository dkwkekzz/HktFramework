# feature-0008 — 발산·행동: 생명체가 에너지를 발산해 다른 생명을 포식한다(전투)

> 상태: 🟡 진행 중 (step 1 검증됨) · 스킬·방출형·아이템 증폭은 후속 · 작업 방식: [CLAUDE.md](../CLAUDE.md)
> 배경: feature-0006(생명체 자기유지)에서 갈구·질서 유지를 **게임 컨텐츠(공격·스킬)** 로 해석하는 트랙. "이것이 공격하고 스킬을 사용하며 스탯이 올라가는 과정이 될 것이다".

## 명제

생명체는 내부 에너지를 **폭발적으로 발산**하여 행동한다 — 근접한 다른 생명체를 공격해 그 에너지를 **강탈**한다(전투). forage(국소장)·harvest(결정)가 *수동적 저장고*를 긁는 것이라면, 강탈은 **능동적으로 질서를 유지하는 다른 생명체**에서 뜯어내는 세 번째 free energy 수입이다. 상대가 저항하므로 (1) 먼저 일을 들여 그 질서를 무너뜨려야 하고(**발산 비용** → 열/심우주), (2) 붕괴로 풀려난 에너지도 전부는 못 붙잡는다(**효율<1**, 나머지는 국소장으로 흩어짐). 그래서 "채집"이 아니라 "전투"로 보인다 — 대상이 저항하기 때문일 뿐, 물리적으로는 **포식(predation)** 이다. **강한(큰) 개체가 약한 개체를 사냥하는 포식 위계가 창발**하고, 뜯겨 죽은 자는 결정을 남겨 생태 루프를 심화한다.

## 강탈의 물리 — 분리 근거는 명사가 아니라 **속성**이다 (불변 원칙 ② 정합)

> **핵심 — "결정이냐 생명이냐"는 진짜 분리선이 아니다.** 채집(0007)과 강탈(0008)을 가르는 물리적
> 근거는 대상이 *무엇(명사)*이냐가 아니라, 대상이 **지금 어떤 상태인가** — **수동 평형 저장고**냐
> **능동 비평형 소산구조**냐다. `I:`(결정)·`C:`(생명체)는 이 세계가 그 선을 *구현한* 현재 표식일 뿐,
> 선 자체는 **저항 여부**에 있다.

세 가지 free energy 수입은 **대상이 제 질서를 유지하려 자유에너지를 태우고 있는가(=저항하는가)** 로 갈린다:

| 흡수 | 현재 표식 | 대상의 **상태**(진짜 축) | 비용·효율 |
|---|---|---|---|
| 갈구(forage) | 국소장 `M:` | **수동** 저장고 — 무질서, 평형 | 무비용, 그냥 긁힌다 |
| 채집(harvest) | 결정 `I:` | **수동** 저장고 — 동결 질서, 평형(면역) | 무비용, 근접하면 질서 풀림 |
| **강탈(attack)** | 생명체 `C:` | **능동** 소산구조 — 대사로 질서를 유지하며 **저항**(비평형) | **일(발산)을 들여야 하고, 회수는 손실적** |

- **수동 저장고 (forage·harvest)**: 국소적 자유에너지 최소점에 앉아 제 유지에 아무 일도 안 한다(배터리·석탄·사체). 저항이 없어 이론상 손실 없이 탭할 수 있다 — 모델에선 무비용. forage↔harvest 는 같은 물리이고 **농도만 다르다**(옅은 확산장 vs 뭉친 섬).
- **능동 소산구조 (강탈)**: 대사(feature-0006 basal)로 자유에너지를 끊임없이 흘려보내며 2법칙에 맞서 제 질서를 **능동적으로 방어**하는 비평형 정상상태다(Prigogine 소산구조). 그 질서를 뺏으려면 먼저 **무너뜨리는 일**이 필요하고(발산 비용→열), 2법칙상 붕괴로 풀려난 에너지를 **전부는 회수할 수 없다**(효율<1). 이것이 생태학 **영양 전달 ~10% 법칙**(단계마다 ~90% 열로 소산)의 결이며, 포식으로 무한히 커질 수 없어 먹이사슬이 짧아지는 **창발적 상한**이 여기서 나온다.

### 왜 이 분리가 값을 하는가 — 다른 거시 법칙을 낳는다

두 흡수를 "채집" 하나로 뭉치면 **① 효율<1(~10% 법칙)** 과 **② 포식 위계(먹이사슬 상한)** 라는 창발적 거시 법칙이 사라진다. 이 둘은 수동 저장고 흡수로는 나오지 않고(누구나 사체를 값싸게 줍는다) 오직 *능동적으로 방어되는 질서를 손실적으로 부수는* 레짐에서만 나온다 — 눈으로 갈리는 다른 명제이므로(불변 원칙 ①) 분리가 정당하다.

### 상태 횡단이 곧 증거 — 같은 물질이 선을 넘는다

분리선이 명사가 아니라 상태임은 이 세계가 이미 코드로 보인다: **생명체가 죽으면 결정이 된다**(feature-0005/0006 `#decompose`). 같은 물질이 —
- 살아있을 때(능동·저항) → **강탈**해야 한다(비싸고 손실적) = 산 먹이 사냥,
- 죽어 결정이 된 뒤(수동) → **채집**으로 값싸게 흡수 = 사체 청소(scavenging).

즉 현실의 "산 먹이 사냥 vs 사체 청소" 구분 그대로다 — 대상의 종류가 아니라 **지금 저항하는가**가 회계를 가른다.

### 반례로 경계를 조인다 — "능동 결정"은 강탈 대상이 아니다

타입(`I:`/`C:`)이 아니라 속성이 기준이므로, **결정이라고 다 수동은 아니다**: feature-0009/0013 의 **과충전 결정 자폭**은 *결정인데 능동적으로 터지는* 사건이다. 이건 강탈(수동 저장고 흡수)이 아니라 **물질의 방출/폭발**(feature-0013 규칙 D)의 몫 — 주인이 다르다. 반대로 대사가 멎어 저항을 잃은 "죽어가는 생명"은 곧 결정으로 굳어 채집 대상이 된다. **분리는 언제나 "이 대상이 지금 제 질서를 능동적으로 유지/방어하는가"로 판정한다** — 명사가 애매하면 속성으로 되돌아간다(불변 원칙 ②).

## 최종 목적 (측정 가능)

1. **강탈**: 사거리(`ATTACK_RADIUS`) 안 공격은 먹이→포식자로 에너지를 옮긴다(먹이↓·포식자↑).
2. **발산 비용 = 엔트로피 화살**: 공격은 포식자 에너지를 소모해 심우주로 방출한다(SINK 단조 증가). 대상이 없으면 발산하지 않는다.
3. **손실적(효율<1)**: 포식자가 **얻는 것 < 먹이가 잃는 것** — 나머지는 국소장으로 흩어진다(세계로 방출). 2법칙·10% 법칙의 결.
4. **포식 창발**: **큰 개체가 작은 개체를 사냥한다**(size< 규칙, 강자→약자 일방). 성장(0006/0007)이 포식자가 되는 전제 — 한눈에 갈리는 위계.
5. **전투사 → 생태 루프**: 뜯겨 예비 아래로 떨어진 먹이는 죽어 결정을 남긴다(feature-0005/0006 `#decompose` 공유) → 다른 생명이 채집(0007).
6. **결정론 + 보존 불변**: 같은 배치·이벤트열이면 전투 결과가 비트 단위로 동일하고, 전 풀 합계 = 10⁹.

## 원장 매핑 — 새 풀 없음, 생명체 간 엣지만 추가

한 번의 공격(포식자 A → 먹이 V)은 **세 갈래 이체**로 회계된다(전부 `ledger.transfer` 관문 → 보존·정수 자동):

- **① 발산 비용**: `A → SINK` (`burst`) = 상대 질서를 깨는 일. 되돌아오지 않는 열(엔트로피 화살).
- **② 상대 질서 붕괴**: `damage = min(ATTACK_POWER×size, V잔고)` 만큼 풀려난다.
- **③ 손실적 회수**: `damage` 중 `CAPTURE_PCT` 만 `V → A` (`attack`, 강탈, 용량 클램프), 못 붙잡은 나머지는 `V → 국소장` (`attack`, 흩어짐).

**포식 규칙**: 각 생명체는 사거리 안에서 **자기보다 작은(`size <`)** 잔고 있는 가장 가까운 먹이 하나를 친다(강자→약자, 상호 교착 없음). **결정론**: seq 오름차순 순회 + 순수 클램프(rng 미사용) → 확산·성장 결정론 불변. 새 풀·새 상태 없이 **생명체 간 상호작용 엣지**만 얹는다. `#combat()` 은 대사(`#metabolizeCreatures`) 앞에서 돈다 — 이번 틱에 뜯긴 먹이가 곧이어 대사 순환에서 예비 아래로 떨어지면 죽어 분해된다(전투사 → 결정).

## step 로드맵 (점진 구축 — 한 step = 한 커밋)

| step | 목표 | 핵심 | 검증 |
|---|---|---|---|
| **1** ✅ | **발산 코어 (근접 강탈 · 발산 비용 · 손실적 · 포식 창발 · 전투사)** | 사거리 안 강자→약자 강탈(먹이→포식자, 효율<1) · 발산 비용→SINK · 흩어짐→국소장 · 전투사→결정 · `attack`/`burst` tx 방송 | `combat.test.js` 7종 + 뷰어(tx 피드 `[강탈]`/`[발산]`) — 아래 step 로그 |
| 2 | **아이템 강탈 증폭 (포획 계열)** | **아이템(결정)이 강탈 흡수를 증폭**(feature-0007 ④d 의 포획 측면) — 종/장착이 강탈량·효율에 영향 | 증폭·보존 |

> **범위 원칙**: step 1 은 "생명이 다른 생명을 뜯어 먹는다(손실적 포식)"는 가장 좁고 한눈에 갈리는 핵심만 세운다. step 2 는 *포획 계열*(강탈 증폭)만 다룬다. **동급(size=)·강자 상대는 강탈로 못 먹으니 방출(feature-0009)의 몫** — 강탈은 먹이(size<)에 좁힌다.
>
> **경계 — 강탈(수입) vs 방출(파괴)은 다른 원리**: 이 feature 는 **커플링된 손실적 포획(수입 스펙트럼의 끝: forage→harvest→강탈)** 만 담는다. 표적을 먹지 않고 **파괴만** 하는 발산(파이어볼·범위·원거리 = 표적 에너지를 캐스터가 아니라 세계/심우주로 흩어버림, 순수 지출)은 흐름의 위상이 근본적으로 다르므로 **[feature-0009](feature-0009-discharge-destruction.md)** 로 분리한다.

## step 로그

| step | 한 일 | 검증 |
|---|---|---|
| 1 | **발산 코어(포식).** `#combat()` 패스를 대사 앞에 새 주기(`ATTACK_INTERVAL=2틱`)로 넣었다 — 각 생명체가 seq 오름차순으로, 사거리(`ATTACK_RADIUS=200`) 안 **자기보다 작은** 가장 가까운 먹이를 친다. 한 공격 = 세 갈래 이체: ① 발산 비용 `A→SINK`(`ATTACK_COST×size`, 질서 깨는 일=열) ② 붕괴 `damage=min(ATTACK_POWER×size, 먹이잔고)` ③ 손실적 회수 — `CAPTURE_PCT(40%)` 만 `먹이→A`(강탈), 나머지는 `먹이→국소장`(흩어짐). **얻는 것 < 뺏는 것**이 코드로 보장된다(효율<1) = 2법칙·10% 법칙. 뜯긴 먹이는 곧 대사 순환에서 예비 아래로 떨어져 죽고 `#decompose`(결정+국소장)로 분해된다 — 전투사→결정→채집(0007) 생태 루프. 순수 클램프(rng 미사용)라 확산·성장 결정론 불변, 전부 `ledger.transfer` 라 보존 자동. 뷰어 tx 피드에 `강탈`/`발산` 라벨 추가. | `npm test` **67/67** — `combat.test.js` 7종: **강탈**(먹이↓·포식자↑, tx 방송)·**손실적**(강탈 < 붕괴, 강탈=damage×효율, 발산 비용→SINK, 흩어짐→국소장)·**포식 창발**(작은 개체는 강탈 못 함, 강자→약자 일방)·**전투사→생태 루프**(뜯겨 죽어 결정 남김)·**사거리**(밖이면 공격 0)·**결정론**·**보존 폭풍**(다수 난전에도 10⁹). 기존 60 회귀 전부 통과. **라이브 스모크**(포식자1 size3 + 먹이3 size1, 300틱): 포식자가 셋을 사냥해 홀로 남고(생명체 4→1) 용량 근처(2,991/3,000)로 포식·포만, 먹이 잔해가 결정으로, SINK 는 발산·대사로 단조 증가. 총합 10⁹ 불변. |

### 구현 요지 (튜닝 근거)

- **강탈은 반드시 손실적**: `victim→attacker` 100% 이동은 너무 깔끔해 비물리적이다 — 남의 질서를 부수며 그 에너지를 전부 회수하면 2법칙 위반. `CAPTURE_PCT<100` 으로 **얻는 것 < 뺏는 것**을 강제하고 나머지를 국소장으로 흘려, 포식이 세계를 먹이면서도(생태 되먹임) 무한 성장은 못 하게 했다(먹이사슬 상한).
- **발산 비용은 심우주로**: 질서를 깨는 일(cost)을 SINK(되돌아오지 않는 열)로 보낸 것이 핵심 — 대사(basal)와 같은 결이라 "공격엔 진짜 대가가 있다"가 SINK 단조 증가로 한눈에 검증되고, 굶주린 개체는 발산할 예비가 없어 사냥을 못 한다(예비 가드).
- **size< 포식 규칙**: 상호 교착(동급 난투 → 동반 자멸)을 피하고 **성장→포식** 위계를 명확히 했다. size 1 은 사냥을 못 하니 forage·harvest 로 먼저 커야 포식자가 된다 — feature-0006(성장)·0007(채집)이 0008(포식)의 전제가 되어 트랙이 자연히 이어진다.

## 검증 (항상 가능)

```bash
npm test        # test/combat.test.js (강탈·손실적·포식창발·전투사·사거리·결정론·보존) + 기존 회귀 전부
./run.sh 6      # 라이브 — 큰 생명체가 곁의 작은 생명체를 뜯어(강탈) 잔고가 차오르고, 뜯긴 먹이는 죽어 결정을 남긴다. tx 피드에 강탈·발산
```

## 상세 문서 인덱스

- (없음)
