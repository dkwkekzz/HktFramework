# feature-0012 — 욕구는 중첩되고 우선순위가 다르다 (감정=중요도로 증폭)

> 상태: 🟡 진행 중 (step 1~3 검증됨) · 기회 감정 잔여는 [feature-0018](feature-0018-motive-strategy.md) 이 흡수(기회=감정이 아니라 전략 선택 신호) · 작업 방식: [CLAUDE.md](../CLAUDE.md)
> 배경: feature-0010 은 욕구가 방향을 정하는 것을(욕구→이동), feature-0011 은 그 수행을 절차로 일반화했다(욕구→절차→방출). 두 feature 모두 욕구는 **하나**였다. 이 feature 는 욕구가 **여럿 중첩**되고 우선순위로 겨루는 것을 세운다.
> **재층위([feature-0018](feature-0018-motive-strategy.md))**: 이 feature 의 명제("차이는 신호·욕구는 방향·감정은 중요도")가 곧 0018 재정의의 뿌리다 — 이 명제를 끝까지 밀면 **욕구의 정체성 = 그것이 줄이려는 차이(appraise)** 가 된다. 여기서 세운 스택·우선순위·감정(외생 emotion + 자율 feeling)·dedup·fallback 메커니즘은 0018 에서 **동기 층**에 그대로 적용된다. 단 appraise 없는 항목(HUNT·CRAFT)은 욕구(동기)가 아니라 **전략**으로 재분류되고, step 3 잔여였던 "기회 감정(먹이 근처→사냥↑)"은 감정이 아니라 허기 동기 아래의 **전략 선택 신호**로 해소된다.

## 명제

> **차이는 신호이고, 욕구는 방향이며, 감정은 중요도다.**

생명체는 차이를 인지해 **욕구**를 품는다(방향, feature-0010). 그런데 욕구는 하나가 아니다 — 생명체는 **상황에 따라 지속적으로 여러 욕구를 주입받고(중첩)**, 각 욕구는 **우선순위(중요도)가 다르다**. **감정이 그 우선순위를 증폭**한다(유효 우선순위 = 우선순위 + 감정). 엔진은 매 틱 **가장 높은 유효 우선순위의(지금 수행 가능한) 욕구**의 절차(feature-0011)를 수행한다 — 그래서 중첩된 욕구들 사이에서 **우선순위가 행동을 정한다**. 같은 욕구를 또 주입할 필요는 없다: 주입은 **idempotent(dedup)** — 중첩되지 않고 우선순위만 갱신된다.

## 왜 "중첩·우선순위"인가 — 하나의 욕구로는 상황을 다 담지 못한다

feature-0010·0011 의 욕구는 한 번에 하나였다(`cre.desire` = 단일값). 하지만 살아있는 것은 동시에 여러 가지를 원한다 — 밥도 먹고 싶고 사냥도 하고 싶다. **무엇을 먼저 하느냐**가 곧 행동이고, 그 순서를 정하는 것이 **우선순위**이며, 우선순위를 흔드는 것이 **감정**이다.

| | feature-0010·0011 | feature-0012 |
|---|---|---|
| 욕구 수 | 하나(`desire`) | **여럿 중첩**(`desires` 스택: name→{priority, emotion}) |
| 무엇을 하나 | 그 하나의 절차 | **유효 우선순위 최고 + 지금 수행 가능한** 욕구의 절차 |
| 우선순위 | 없음 | **priority(중요도)** — 욕구마다 다르다 |
| 감정 | 없음 | **emotion** = 우선순위 증폭(낮은 욕구도 이기게) |
| 재주입 | 덮어씀 | **idempotent** — 같은 욕구는 dedup(우선순위만 갱신) |
| 상황 의존 | 절차 단계로(feature-0011) | **+ 최우선이 불가하면 다음 우선순위로 내려간다** |

세 층위가 겹친다: **차이**(무엇이 다른가 → 욕구가 생김) → **욕구**(어디로 → feature-0010) → **감정**(얼마나 급한가 = 우선순위) → 그 우선순위가 **feature-0011 절차**의 실행 순서를 정한다.

## 최종 목적 (측정 가능)

1. **중첩**: 한 생명체가 여러 욕구를 동시에 품는다(`desires` 스택에 둘 이상).
2. **우선순위가 행동을 정한다**: 두 욕구가 같은 상황에서 겹치면 **우선순위 높은** 쪽의 절차가 수행된다(사냥 우선→타격 / 식사 우선→섭취).
3. **감정=증폭**: 낮은 기본 우선순위의 욕구라도 **감정을 실으면** 유효 우선순위가 올라 다른 욕구를 이겨 **행동이 바뀐다**(감정 없으면 사냥 → 식사에 감정 → 식사).
4. **주입 idempotent(dedup)**: 같은 욕구를 다시 주입해도 스택이 늘지 않는다(하나로 유지, 우선순위만 갱신, 감정 보존).
5. **상황 의존 fallback**: 최우선 욕구가 지금 수행 불가(표적 없음 등)면 **다음 우선순위** 욕구가 수행된다.
6. **결정론 + 보존 불변**: 같은 배치·주입열이면 위치·잔고가 비트 단위로 동일하고, 어떤 중첩·감정에도 전 풀 합계 = 10⁹.

## 원장 매핑 — 새 풀 없음, 단일 desire 를 스택으로 일반화

- **욕구 스택**(에너지 풀 아님, 순수 상태): `cre.desires` = `Map<name, { priority, emotion }>`. `desire`(단일값)는 **스택의 승자를 읽는 접근자**로 남겨 feature-0006~0011 의 코드·테스트·방송이 그대로 쓴다(하위 호환). setter 는 스택을 그 욕구 하나로 교체(단일 부여 의미 보존).
- **유효 우선순위**: `desireWeight(priority, emotion) = priority + clamp(emotion)`. 순수 함수(정수·결정론).
- **주입 API**: `injectDesire(name, priority)`(스택에 얹기, dedup) · `emote(name, emotion)`(증폭) · `withdrawDesire(name)`(거둠). 인텐트 `INJECT { desire, priority?, emotion? }`.
- **실행**: `#performDesire` 는 스택을 유효 우선순위 내림차순으로 훑어 **지금 수행 가능한 첫 욕구**의 첫 적용 단계(feature-0011 절차)를 실행한다. 새 에너지 없음 — 방향/순서만 정한다(보존·결정론 불변, rng 미사용).
- **방송**: `CREATURE` 스냅샷에 `desires`(중첩 스택 `[[name,priority,emotion]]`) 를 실어 뷰어가 우선순위·감정 배지를 그린다.

## step 로드맵 (점진 구축 — 한 step = 한 커밋)

| step | 목표 | 핵심 | 검증 |
|---|---|---|---|
| **1** ✅ | **욕구 중첩·우선순위·감정 엔진** | `desires` 스택 + 하위호환 `desire` 접근자 · `injectDesire`/`emote`/`withdrawDesire` · `#performDesire` 우선순위 순회(수행 가능한 최우선) · `INJECT` 인텐트 · 뷰어 중첩 배지(승자·감정 ♥) | `priority.test.js` 10종 + 뷰어·시각 검증 — 아래 step 로그 |
| **2** ✅ | **감정의 자율 생성(차이→감정) + 포만 감쇠** | 욕구 절차의 `appraise(ctx)` 가 상황(차이)에서 감정(`feeling`)을 스스로 만든다(굶주림→식사↑) · `#appraise` 매 틱 갱신 → 우선순위 자율 재정렬 · 배부르면 0 으로 감쇠(포만→다음 욕구) | `appraise.test.js` 6종 + 시각 검증 — 아래 step 로그 |
| **3** ✅ | **다른 자율 감정원: 위협 → 회피(FLEE)** | 굶주림 말고 **위협**(더 큰 포식자 근처)이 회피 감정을 스스로 만든다 · 새 지각 `nearestThreat`·새 행동 `moveAway`·새 욕구 `FLEE`(appraise=threatFeeling)를 ctx·레지스트리에 얹기만 하면 엔진 무수정 실행(개방 재증명) · 가까울수록 회피↑, 멀어지면 감쇠 | `threat.test.js` 5종 + `shot:threat`(도망 PNG) — 아래 step 로그 |
| 3 | **다른 자율 감정원 (위협·기회 …)** | 위협(더 큰 포식자 근처)→회피/방출 감정↑, 기회→사냥 감정↑ 등 새 appraise 를 얹는다(개방 재증명) | 새 상황→감정 |

> **범위 원칙**: step 1 은 "욕구는 중첩되고 우선순위가 다르며, 감정이 그 우선순위를 정한다 + 같은 욕구 재주입은 무의미"의 핵심(두 욕구 겹침·감정으로 뒤집힘·dedup)을, step 2 는 "그 감정을 **상황(차이)이 스스로 만든다** + 배부르면 감쇠한다"의 핵심(굶주림→식사 감정 자율 상승·포만→다음 욕구)을 세운다. 굶주림 외 다른 감정원(위협·기회)은 step 3.

## 감정의 두 갈래 — 외생(emote) vs 자율(appraise) (feature-0012 step2)

step 1 의 감정은 밖에서 실었다(플레이어·봇·API 의 `emote`). step 2 는 **상황이 감정을 스스로 만드는** 길을 연다 — "차이는 신호". 유효 우선순위는 이제 세 항의 합이다:

> **유효 우선순위 = 기본 우선순위(priority) + 외생 감정(emotion) + 자율 감정(feeling)**

- **emotion(외생)**: `emote`/`INJECT` 로 밖에서 싣는 중요도(step 1).
- **feeling(자율)**: 욕구 절차의 `appraise(ctx)` 가 매 틱 상황(차이)에서 계산하는 중요도. **굶주림**(용량과 잔고의 차이)이 클수록 식사·채집의 feeling 이 오르고, 편안(용량의 절반 이상)해지면 0 으로 **감쇠**한다(포만). 그래서 외부 주입 없이도 **굶주리면 식사가 이기고, 배부르면 다음 욕구(사냥)로 넘어간다** — 상황이 우선순위를 스스로 재정렬한다.
- **개방(feature-0011 정합)**: `appraise` 는 오직 `ctx`(capacity·balance 등 지각 API)만 쓴다 — 엔진은 이름을 모른다. 어떤 욕구든 자기 감정을 `appraise` 로 정의하면 엔진(`#appraise`)이 그대로 계산한다(런타임 등록 테스트로 증명).

## step 로그

| step | 한 일 | 검증 |
|---|---|---|
| 1 | **욕구 중첩·우선순위·감정 엔진.** 단일 `cre.desire` 를 **스택 `cre.desires`**(name→{priority, emotion})로 일반화하고, `desire` 는 스택의 **승자(유효 우선순위 최대)** 를 읽는 접근자로 남겨 기존 feature(0006~0011)·테스트·방송을 그대로 보존했다(하위 호환, setter=스택 교체). 주입 API 를 셋 뒀다: `injectDesire`(스택에 얹기 — 다른 욕구를 지우지 않고 중첩, 같은 욕구는 **dedup**=우선순위만 갱신·감정 보존) · `emote`(감정으로 **우선순위 증폭**, 유효=priority+emotion) · `withdrawDesire`(거둠). 엔진 `#performDesire` 를 **유효 우선순위 내림차순 순회**로 바꿔, 지금 수행 가능한(applicable) **첫 욕구**의 첫 적용 단계(feature-0011 절차)를 실행한다 — 최우선이 불가하면 다음 우선순위로 내려간다(상황 의존). 인텐트 `INJECT{desire,priority?,emotion?}`. `CREATURE` 스냅샷에 `desires` 스택을 실어 뷰어가 **중첩 배지**(승자 ▸ 밝게·나머지 · 흐리게, 감정=♥ 개수)로 그린다. 전부 `ledger.transfer`·rng 미사용 → 보존·결정론 불변. | `npm test` **108/108** — `priority.test.js` 10종: **중첩**(두 욕구 동시)·**우선순위가 행동을 정한다**(사냥↑→타격 / 식사↑→섭취)·**감정 증폭**(식사에 감정→사냥을 이겨 행동이 뒤집힘)·**dedup**(재주입=우선순위만 갱신, 감정 보존)·**상황 fallback**(최우선 불가→다음 우선순위 수행)·**거둠**·**방송**(승자+스택)·**결정론**·**보존 폭풍**·**미등록 무시·감정 클램프**. 기존 98 회귀 전부 통과. **시각 검증**(`npm run shot`): 생명체가 **식사·사냥을 동시에 품되**(배지 `▸식사♥♥` 밝게·`·사냥` 흐리게) 감정 실린 **식사** 쪽(왼쪽 밥)으로 표적선을 그리며 이동해 먹는다(오른쪽 먹이는 두고 감, tx `[채집] 밥→생명체`), 총합 10⁹. |
| 3 | **다른 자율 감정원 — 위협 → 회피(FLEE).** step2 의 "차이가 감정을 만든다"를 굶주림 밖에서 재증명했다: **위협**(더 큰 포식자)이 가까울수록 회피의 자율 감정(`threatFeeling`)이 치솟는다. 세 조각을 얹었다 — 지각 `nearestThreat()`(나보다 큰 생명 감지, `nearestPrey` 의 대칭)·행동 `moveAway(t)`(위협 반대로 한 걸음, 이동=국소장 소산=추적과 동일 회계)·거리 `distanceTo(t)`·상수 `SEEK` 를 ctx 에 열고, 새 욕구 `FLEE`(steps=[fleeThreat], appraise=threatFeeling)를 `registerDesire` 로 등록. **엔진(game.js)은 '위협'도 '회피'도 모른다** — appraise·step 로직은 전부 레지스트리(데이터)에, 엔진은 범용 지각/행동 primitive 만 제공(어떤 미래 욕구도 재사용). 위협이 감지 반경 밖이면 `fleeThreat` 가 수행 불가(nearestThreat 없음) → 엔진이 다음 욕구로 내려가 하던 일로 돌아간다(상황이 행동을 거둔다). 순수 계산/클램프(rng 미사용) → 결정론 불변, 이동 소산은 `ledger.transfer` → 보존. | `npm test` **161/161** — `threat.test.js` 5종: **위협 감정 자율 생성**(큰 포식자 근처→회피 feeling>0, emote 없이 회피가 이김)·**회피=멀어진다**(포식자와 거리↑, 이동 tx 소산)·**멀면 감쇠**(반경 밖→회피 수행 안 됨, 다음 욕구로)·**가까울수록 큼**(단조)·**개방 재증명**(런타임 등록한 커스텀 위협 욕구를 엔진 무수정 실행). **시각 검증**(`npm run shot:threat`): 내 생명체가 곁의 큰 포식자(size4)에서 **▸flee** 로 도망쳐 거리 272→872, tx 피드에 `[이동] 생명체→국소장`(회피 지출). 총합 10⁹. **후속(기회 감정)**: "먹이 근처→사냥↑"은 HUNT 의 기저 동기를 바꿔 기존 우선순위 테스트와 얽히므로(강한 opportunity 가 작은 priority 를 압도) 별도 step 에서 HUNT 역할을 재조정해 얹는다. |
| 2 | **감정의 자율 생성(차이→감정) + 포만 감쇠.** 욕구 스택 값에 **자율 감정 `feeling`** 을 더하고(유효 우선순위 = priority + emotion + feeling), 욕구 절차에 **`appraise(ctx)`** 를 얹었다: 식사·채집은 `hungerFeeling`(용량과 잔고의 **차이**=굶주림이 클수록 오르고, 편안 임계=용량의 절반 위면 0 으로 **감쇠**). 엔진 `#appraise` 가 매 틱(수행 직전) 각 욕구의 `appraise` 를 불러 feeling 을 갱신 → 우선순위가 **외부 주입 없이** 상황에 따라 스스로 재정렬된다. `appraise` 는 오직 ctx(새로 연 `capacity()`·`balance()`)만 써 엔진과 분리 — **어떤 욕구든 자기 감정을 정의하면 엔진이 그대로 계산한다(개방, 런타임 등록으로 증명)**. 사냥은 `appraise` 없이 외생 우선순위만 → 배부르면 사냥이 이긴다. `CREATURE` 스냅샷 스택에 feeling 을 실어 뷰어가 ♥(외생+자율)로 그린다. rng 미사용 → 결정론 불변, 전부 보존. | `npm test` **114/114** — `appraise.test.js` 6종: **감정의 자율 생성**(굶주리면 식사 feeling>0·식사가 사냥을 이김, emote 없이)·**차이가 클수록 감정 큼**(단조)·**포만 감쇠**(같은 스택인데 굶주리면 식사·배부르면 사냥 — 상황이 행동을 뒤집음)·**개방성**(런타임 appraise 등록→엔진이 자율 감정 계산, 무수정)·**결정론**·**보존 폭풍**. 기존 108 회귀 전부 통과(step1 방송 테스트만 feeling 4-튜플 반영). **시각 검증**(`npm run shot`): **같은 스택(식사1·사냥2)을 품은 두 생명체**가 상황만 다르다 — 굶주린 개체는 `▸식사♥♥♥`(자율 감정 74)로 **왼쪽 밥**으로, 포만한 개체는 `▸사냥`으로 **오른쪽 먹이**로 스스로 갈라져 간다(감정을 밖에서 안 실었다). 배부른 뒤엔 식사 감정이 감쇠해 저도 사냥으로 넘어간다. 총합 10⁹. |

### 구현 요지 (튜닝 근거)

- **`desire` 를 접근자로 보존 = 무마찰 확장**: 단일값을 스택으로 바꾸되 `desire` getter(승자)/setter(교체)를 둬 feature-0006~0011 의 게이트(`desire !== NONE`)·방송·테스트를 한 줄도 고치지 않았다 — 중첩은 순전히 추가다(기존 98 회귀 그대로 통과).
- **유효 우선순위 = priority + emotion**: 가장 단순한 합으로 "감정은 중요도다"를 정수·결정론으로 구현했다. 감정을 실으면 낮은 욕구가 이기는 것이 한눈에 갈리고(테스트가 뒤집힘으로 증명), 상한 클램프로 과증폭을 막는다.
- **수행 가능한 최우선 = 우선순위 × 상황(feature-0011)의 합성**: `#performDesire` 가 우선순위 순으로 훑되 **applicable 한 첫 욕구**를 고르게 해, 우선순위(무엇을 원하나)와 절차(지금 할 수 있나)가 자연히 합쳐진다 — 최우선이 불가하면 다음으로 내려가 "상황에 따라 행동이 달라진다"가 공짜로 나온다.
- **주입은 dedup**: 같은 욕구 재주입이 스택을 부풀리지 않게 Map(name 키)로 둬, "생명체는 지속적으로 주입받되 같은 욕구를 또 넣을 필요는 없다"를 자료구조로 보장했다(감정은 보존 → 중요도 이력 유지).
- **(step2) 감정 = 외생 + 자율 두 채널**: `emotion`(밖에서)과 `feeling`(상황이 스스로)을 나눠 둬, step 1 의 외생 감정(테스트·데모)을 건드리지 않고 자율 감정을 **덧붙였다**. 유효 = priority + emotion + feeling. 이 분리 덕에 step1 회귀가 그대로 통과한다(잘 먹은 개체는 feeling 0 → step1 시나리오 불변).
- **(step2) 편안 임계로 오실레이션·간섭 차단**: 굶주림 feeling 을 "용량의 절반 위면 0"으로 둬, step1 의 잘 먹은 개체(≈95%)는 feeling 0 이 되어 우선순위 시나리오에 끼어들지 않는다. 굶주림은 임계 아래에서만 자율로 치솟는다(차이가 신호).
- **(step2) appraise = ctx 경계 = 개방**: 자율 감정도 feature-0011 의 ctx 경계를 지켜 절차 안(desires.js)에 두었다 — 엔진은 감정의 근거를 모르고 `appraise(ctx)` 를 부를 뿐이다. 그래서 새 감정원(위협·기회…)을 얹을 때 엔진을 고칠 필요가 없다(런타임 등록 테스트가 증명).

## 검증 (항상 가능)

```bash
npm test        # test/priority.test.js(중첩·우선순위·감정 증폭·dedup·fallback) + appraise.test.js(자율 감정·포만 감쇠·개방) + 기존 회귀 전부
npm run shot    # 시각 검증(step2) — 같은 스택 두 생명체가 굶주림/포만으로 갈라지는 3컷(tools/shots/appraise-*.png). 언제든 재현
npm run demo    # 자율 감정 데모 서버 — http://localhost:8080 (굶주린 개체는 밥으로·포만한 개체는 사냥으로 스스로 갈라진다)
```

브라우저(관전자)에서: 접속하면 내 생명체(금색 고리) 위에 **중첩된 욕구 배지**가 쌓인다 — 승자(▸, 감정이면 ♥)는 밝게, 나머지(·)는 흐리게. step 1 은 감정을 버튼/API 로 실어 승자를 바꾸고(외생), step 2 는 **굶주리면 식사 감정이 스스로 차올라(자율, ♥) 밥으로 가고 배부르면 감쇠해 사냥으로 넘어간다** — 감정을 밖에서 싣지 않아도 상황이 행동을 정한다.

## 상세 문서 인덱스

- 욕구 스택·우선순위·감정: [../server/game.js](../server/game.js) — `injectDesire`·`emote`·`withdrawDesire`·`#performDesire`·`#rankedDesires`·`#appraise`(자율 감정).
- 유효 우선순위·편안 임계: [../shared/constants.js](../shared/constants.js) — `desireWeight`(priority+emotion+feeling)·`DESIRE_BASE_PRIORITY`·`DESIRE_EMOTION_MAX`·`DESIRE_COMFORT_FRACTION`.
- 자율 감정(appraise): [../shared/desires.js](../shared/desires.js) — `hungerFeeling` + 절차별 `appraise(ctx)`(개방 경계).
