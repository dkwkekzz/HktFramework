# 기획서 전수 커버리지 매핑

> 목적: "분해가 기획의 모든 내용을 담는가"를 주장이 아니라 표로 검증한다. 기획서 [Design-MMO.md](../Design-MMO.md)의 45개 섹션 전부를 담당 Phase 에 매핑한다. 새 섹션이 기획서에 추가되면 이 표도 갱신한다.

| § | 내용 | 담당 Phase | 비고 |
|---|---|---|---|
| 1 | 목표·전체 흐름 | 전체 | §1 생성 목록 16항 중 "경제"는 전용 시스템이 아니라 자원(§14)·거래 행동·조직 통제의 창발로 구현 — §43이 "완성된 경제 시장"을 명시 제외 |
| 2 | 구현 원칙 (AI/코드 분리, 3계층) | README 원칙 2, P5·P8 | AI 는 정의·표현만, 진행은 코드 — `TextGenerationPort` 격리로 강제 |
| 3 | 아키텍처 7블록 | Seed Editor=P5, Compiler=P5, Validator=P6, Bootstrapper=P5, Runtime=P0~3, Interpreter=P8, Viewer=P8 | |
| 4 | WorldSeedInput | P5 | §36.1 입력 화면 포함 |
| 5 | WorldDefinition·컴파일 15단계 | P5 (표로 1:1 매핑) | `metadata` 는 P5 단계 15 저장 시 부여(제목·시드·생성 시각·입력 원문) |
| 6 | 주제 정규화 | P5 단계 1 | §6 예시 2건 스냅샷 테스트 |
| 7 | 핵심 명제 | P5 단계 2 | 명제 위반 탐지는 P6 AI 보조 검사 |
| 8 | 생존 압력 | P3 §3.5 (실행), P5 단계 3 (생성) | |
| 9 | 상태 스키마 | P1 §1.1 (검증기·updatePolicy·observable), P5 단계 4 | `derived` 쓰기 금지·파생식, `observable`↔신호 계약 — P1 §1.1 |
| 10 | 믿음 분리 | P3 §3.1 | 반향수 예시 재현이 P3 DoD |
| 11 | 규칙 구조 (트리거·조건·효과·관찰) | P2 | §11.4 예시 규칙 로드가 P2 DoD |
| 12 | 규칙 DSL (능력 10항, 확률 제한) | P2 | 확률=인과 대체 금지 검사는 P6 |
| 13 | 공간 (지역·연결·희귀도 함수) | P1 (수동 2지역), P5 단계 6 | 공간 데이터는 3D(x·y·z), 표현은 2D 투영 — 2026-07-28 기획 개정. `calculateResourceRarity` 는 AI 아닌 코드 (P5 §5.1 주석) |
| 14 | 자원 (생산·소비·변환·desiredBy) | P1 (3~4종), P5 단계 6 | 자원 6대 질문 검증은 P6 `resource.source` + AI 보조 |
| 15 | 종족 (생존 전략 절차, 번식·적응·성장) | P5 단계 7 (생성), P1·P3 (실행) | reproduction/adaptationRules/growthRules 는 전용 시스템 없이 DSL 규칙(`create_entity` 등)로 실행 — P3 콘텐츠 확장 명시 |
| 16 | 능력 체계 (발동·유지·제약·반동·mastery) | P5 단계 9 (생성), **P2 §2.7 (실행 매핑)**, P7 (mastery 성장) | 능력은 전용 실행기 없이 행동+규칙으로 분해 — 필드별 실행 주체 표는 P2 §2.7 |
| 17 | 조직 (주체성, 공개/은닉 목적, 붕괴) | P5 단계 8 (생성), **P3 §3.7 (주체화)** | 조직=faction 개체에 동일 판단 파이프라인, 위임 행동, collapseConditions interval 감시 |
| 18 | 개인 (10절차, 판단 변수 9종) | P5 (생성), P3 (traits 사용) | 조직-개인 갈등(§18-6)은 위임 목적 주입 + 활성도 충돌로 실행 |
| 19 | 목적 그래프 (노드·엣지 6관계) | P3 §3.5 | requires/conflicts/creates/reveals 엣지 처리 명시 |
| 20 | 목적 활성도 (11항, 믿음 기반) | P3 §3.5 | BeliefView 강제 — WorldState 직접 참조 린트 금지 |
| 21 | 행동 정의 (목록 21종, NPC/플레이어 비분리) | P1 (10종), P5 단계 11 (전체), P7 (비분리 검증) | "행동 위임"은 P3 §3.7 조직 행동으로 실행 |
| 22 | 행동 후보·점수·softmax | P3 §3.6 | 계수·수식 기획서 그대로 |
| 23 | 인식 (신호·채널·canObserve·믿음화 4단계) | P3 §3.2, P2 (신호 생성) | 소문·보고 채널 = 2차 신호, 조직 보고는 P3 §3.7 집약 믿음 |
| 24 | 기억 (중요도·감쇠·요약 통합) | P3 §3.3 | 요약 문장화는 P8, 상한으로 무한 증가 방지 |
| 25 | 관계 (9축+비밀+약속) | P3 §3.4 | 변화 규칙은 하드코딩 아닌 DSL 콘텐츠 |
| 26 | 런타임 (이벤트 기반, 재판단 조건) | P0 (스케줄러·루프 골격), P1 (`shouldReplan`) | 7단계 루프 훅을 P1~4 가 순서대로 채움 |
| 27 | 주체 실행 12단계 | P1 §1.2 (간이), P3 (본 구현) | |
| 28 | 사건 자동 탐지 | P4 | §28 예시 시나리오 재현이 P4 DoD |
| 29 | 사건 중요도 | P4 §4.3, P7 (playerRelevance) | 6항 계수 그대로. playerRelevance 는 조작 중인 주체가 있을 때만 0 이 아니다 — 무개입 세계의 중요도는 불변 |
| 30 | 개입 기회, 아는 것/모르는 것 | P4 §4.4, P7 §7.2 | 관찰자 시점 API `getEventViewFor` |
| 31 | 플레이어 구현 | P7 | 동일 데이터 구조, 판단만 사용자. 지식 필터는 빌더가 아니라 **코어**에서 — `PlayerKnowledgeView` 만 경계를 넘는다 |
| 32 | 성장 (7조건, 수치+선택 구조) | P7 §7.4 | NPC 도 동일 적용, `record_growth` 효과. 출처 사건이 탐지된 뒤 확정되고, 능력 갱신은 정의를 고치지 않고 원장 위에서 합성 |
| 33 | 생성 AI 역할 3분류 | 33.1=P5, 33.2=P6, 33.3=P8 | 구조화 입력만 전달, 상태 직접 변경 금지 — P8 은 `presentation/**` 가 core/viewmodel 을 import 하지 못하게 린트로 막아 **타입 수준에서** 읽기 전용을 강제하고, unknownFacts 누출을 자동 검사한다 |
| 34 | 생성 결과 검증 (필수 10규칙) | P6 §6.1 | 10규칙 각각 코드명 붙인 검사기 + 위반 픽스처 테스트 |
| 35 | 자동 시뮬레이션 테스트 (8판정, 다양성·깊이) | P6 §6.2 | 합격선은 수동 세계 기준선으로 |
| 36 | 4개 화면 | 36.1=P5·P8, 36.2~4=P8 | 명세 항목별 표시가 P8 DoD — 29항 전수 판정(`phase8Checks.checkScreenItems`). 모드(개발자/플레이어)는 렌더러 분기가 아니라 **빌더의 입력** |
| 37 | 클라이언트 구조 | P0 (트리 그대로 + `viewmodel/` 추가), P8 (rendering·app 실체) | ViewModel 은 분해 원칙 5 에 따른 확장 — 기획서 구조와 충돌 없음. P8 에서 `rendering/**` 는 `SceneViewModel` 만, `app/**` 는 ViewModel+프로토콜만 import 가능(린트 강제) |
| 38 | Worker 분리·patch | P0 §0.4, P8 (표시 재료·생성까지 경계 뒤로) | 메시지 타입 기획서 그대로 + `scene_view`/`generation_view`/`set_view`/`set_speed`/`generate_world`/`regenerate_step`. 세계 생성도 Worker 에서 돈다 — UI 스레드는 렌더링만 |
| 39 | 저장 3분리·복원·시드 재현 | P0 §0.2·§0.5 | 재현성은 전 Phase DoD 상시 항목 |
| 40 | 초기 규모 표 | P5 DoD | 생성 개수 목표로 프롬프트에 주입 |
| 41 | 첫 번째 세계 | P1 (부분집합 수동), P5 DoD (전체 생성) | "자동 생성 결과 10항" 전수 확인 |
| 42 | 구현 순서 8단계 | 분해의 골격 (README) | 재배열 없음, §37~39 만 P0 로 선행 |
| 43 | 제외 목록 | README·P8 | 제외 항목은 어느 Phase 에도 없음 |
| 44 | 완료 조건 13항 | README 매핑 표, P8 최종 게이트 | `evaluateGate44` 가 13행을 근거 수치와 함께 출력 — 13/13 통과 (Phase-8.md 관측 결과) |
| 45 | 최종 구조·생성 대상 5요소 | 전체 요약 — 상태=P1, 규칙=P2, 주체·목적=P3, 인식·믿음=P3, 현상·사건=P4·P8 | |
