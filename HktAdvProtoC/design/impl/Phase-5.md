# Phase 5 — 세계 생성 컴파일러

> 근거: §42-5, §4(입력), §5(컴파일 15단계), §6~§18(단계별 생성물), §33(생성 AI의 역할), §2.1(AI 는 정의만 작성, 진행은 코드).

## 목표

`WorldSeedInput`(§4) 몇 문장 → 검증 가능한 `WorldDefinition`(§5) 전체를 생성한다. 출력 포맷은 새로 설계하지 않는다 — **Phase 1~4 가 이미 실행하고 있는 데이터 포맷이 곧 출력 계약**이다. §41 의 첫 세계 입력으로 §40 규모의 세계가 나와야 한다.

## 산출 모듈 (§37 `generation/`)

§37 트리 그대로: `WorldSeedNormalizer` / `AxiomGenerator` / `SchemaGenerator` / `RuleGenerator` / `SpeciesGenerator` / `FactionGenerator` / `AgentGenerator` / `GoalGraphGenerator` (+ 보조: `PressureGenerator`, `SpaceGenerator`, `ResourceGenerator`, `AbilityGenerator`, `ActionGenerator`, `EventPatternGenerator`, `BootstrapGenerator` — §5 의 15단계와 1:1). `WorldValidator` 는 Phase 6.

공통 기반:
- `TextGenerationPort` — LLM 어댑터 인터페이스: `generate(taskId, systemPrompt, input, outputSchema) → 검증된 JSON`. 구현체는 Claude API 등 교체 가능. 코어·시뮬레이션은 이 포트를 전혀 모른다(§2.1).
- `CompilerPipeline` — 15단계(§5 목록 그대로)를 순차 실행하는 오케스트레이터. 각 단계의 입출력을 `generation-artifacts/<step>.json` 으로 저장 — 단계별 재시도·검토(§36.1 "생성 단계 진행 상태") 가능.

## 상세 설계

### 5.1 단계 구성 — §5 의 15단계 매핑

| §5 단계 | 생성기 | 출력(계약 스키마) | AI/코드 |
|---|---|---|---|
| 1 주제 정규화 | WorldSeedNormalizer | `NormalizedTheme[]`(§6) | AI |
| 2 핵심 명제 | AxiomGenerator | `WorldAxiom[]`(§7) | AI |
| 3 생존 압력 | PressureGenerator | `SurvivalPressureDefinition[]`(§8) | AI |
| 4 상태 스키마 | SchemaGenerator | `StateSchema[]`(§9) | AI |
| 5 세계 규칙 | RuleGenerator | `RuleDefinition[]`(§11, Phase 2 RuleSchema) | AI |
| 6 자원·공간 | ResourceGenerator·SpaceGenerator | §13·§14 구조 | AI+코드* |
| 7 종족 | SpeciesGenerator | `SpeciesDefinition[]`(§15) | AI |
| 8 조직 | FactionGenerator | `FactionDefinition[]`(§17) | AI |
| 9 능력 체계 | AbilityGenerator | `AbilityDefinition[]`(§16) | AI |
| 10 목적 그래프 | GoalGraphGenerator | `GoalGraph`/`GoalTemplate[]`(§19) | AI |
| 11 행동 정의 | ActionGenerator | `ActionDefinition[]`(§21) | AI |
| 12 사건 패턴 | EventPatternGenerator | `EventPattern[]`(§28, Phase 4 스키마) | AI |
| 13 초기 배치 | BootstrapGenerator | `BootstrapDefinition` | AI+코드* |
| 14 정합성 검증 | (Phase 6 WorldValidator) | `ValidationIssue[]`(§34) | 코드+AI보조 |
| 15 실행 데이터 저장 | WorldRepository(Phase 0) | `WorldDefinition` | 코드 |

*코드 병행: §13 의 `calculateResourceRarity` 같은 파생 계산(위험도→희귀도)은 AI 가 아니라 코드가 수행 — AI 는 조건·프로필만 생성하고 배치 수치는 결정론 함수가 계산한다. 초기 배치의 좌표 산출도 시드 RNG 코드 몫.

### 5.2 생성 호출 설계 (§33.1, §33 말미)

- **월드 상태 전체를 전달하지 않는다**(§33): 각 단계의 프롬프트 입력은 (a) 이전 단계 산출물 중 해당 단계가 참조해야 하는 것만 (b) 출력 JSON Schema (c) 개수 목표(§40 규모 표). 예: SpeciesGenerator 입력 = axioms + pressures + 공간 태그 요약, 출력 = 종족 4개.
- 대량 항목(개인 20명, 규칙 40~60개)은 **항목 단위 분할 호출**: 먼저 후보 목록(이름+한 줄 전략)을 생성하고, 개별 항목을 별도 호출로 상세화. 실패한 항목만 재시도 가능.
- 모든 단계 출력은 `outputSchema` 로 즉시 JSON Schema 검증(§34 첫 문장 "그대로 저장하지 않는다"). 검증 실패 시 오류 목록을 붙여 최대 2회 재생성, 그래도 실패면 단계 중단·사람 검토.
- **참조 무결성 사전(“심볼 테이블”)**: 파이프라인이 지금까지 생성된 모든 id(상태 키·규칙·자원·태그…)를 누적 관리하고, 각 호출에 "사용 가능한 id 목록" 으로 제공 + 출력의 미지 id 참조를 기계 검출. §34 "규칙의 대상이 실제로 존재한다" 를 생성 시점에 선방어.

### 5.3 도메인 절차의 프롬프트화

기획서가 절차를 명시한 생성물은 그 절차를 프롬프트 구조로 강제한다:
- 종족: §15 의 6단계 흐름(압력→전략 후보→종족화→장단점→감각→사회 구조)과 전략 목록(§15 의 7전략)을 단계적 출력 필드로 요구.
- 조직: §17 의 7절차(희소 자원 선택→수혜/피해 내부 집단→외부 경쟁자→공개/실제 목적 분리). 조직은 반드시 어떤 자원·규칙의 이용 전략에서 파생(§17 마지막 문단).
- 개인: §18 의 10절차 + traits 는 §18 판단 변수 9종 수치로.
- 능력: §16 의 10절차, 입력은 `AbilityGenerationContext`(§16) — 개인 생성 결과에서 채운다. 제약 강도→출력 범위 계산(절차 7)은 코드 함수로.
- 정규화: §6 "최소한의 구조만 추출, 설정을 지나치게 추가하지 않는다" 를 시스템 프롬프트 제약으로.

### 5.4 결정론과 생성물

LLM 출력 자체는 비결정적이다. 재현성 경계(§39, §44-12)는 **"같은 WorldDefinition + 같은 시드 → 같은 시뮬레이션"** 이다. 따라서 생성 결과는 항상 WorldRepository 에 고정 저장하고, 시뮬레이션 재현은 저장된 정의로부터만 한다. 부트스트랩의 난수(개체 좌표 등)는 정의에 시드로 포함.

### 5.5 UI (§36.1)

세계 생성 화면: 주제/원하는 경험/제외 요소 입력(§4 필드 그대로) → 생성 버튼 → 15단계 진행 표시(단계별 성공/실패/재시도) → 생성 구조 검토(단계 산출물 JSON 뷰) → 저장.

## 구현 스텝

1. `TextGenerationPort` + 스키마 검증 래퍼 + 오프라인 목(mock) 구현(녹화된 응답 재생 — 테스트가 AI 없이 돌게).
2. 심볼 테이블 + CompilerPipeline 골격(단계 저장·재개).
3. 단계 1~4 (정규화·명제·압력·상태 스키마) + §6·§7 예시 입력의 스냅샷 테스트.
4. 단계 5~8 (규칙·자원·공간·종족) — 출력이 Phase 2 RuleEngine 에 실제 로드되는지 확인.
5. 단계 9~13 (조직·능력·목적 그래프·행동·사건 패턴·부트스트랩).
6. §41 첫 세계 전체 생성 → Phase 1~4 런타임에 로드 → 30일 실행.
7. 세계 생성 화면(§36.1).

## 완료 조건 (DoD)

- [x] §41 의 5개 주제 입력으로 §41 "자동 생성되어야 하는 결과" 10항목이 모두 생성된다(지역 3, 흡수 생물 종, 통제 기관, 연구/밀수/보호 조직, 인간 20명, 목적 그래프, 파벌, 사건 패턴 10).
- [x] 생성물이 §40 규모 표를 만족한다.
- [x] 생성된 세계가 수정 없이 런타임에 로드되어 30일 실행된다(품질 판정은 Phase 6).
- [x] 능력 5개가 각 개인의 욕망·경험·제약에서 파생되고(§16 절차, §44-11) 전부 대가·실패 반동을 가진다.
- [x] 모든 생성 호출이 구조화 입력만 받는다(월드 상태 전체 전달 없음 — 기계 검사로 승격했다).
- [x] mock 포트로 파이프라인 전체가 오프라인 테스트된다.

---

# 구현 결과

`npm run verify` (시드 42, 30일) — Phase 5 구간 전문:

```
=== Phase 5 완료 조건 — 세계 생성 컴파일러 점검 — 시드 42, 30일 ===

✓ §41 다섯 문장 → 10항목 자동 생성 (10/10)
      ✓ 생태적으로 다른 지역 3개: 변두리 정착지(위험 12·잔재 0) / 침묵림(위험 68·잔재 240) / 공동 분지(위험 92·잔재 520)
      ✓ 능력 흔적을 흡수하는 생물 종: 반향수(organic_food,ability_residue) / 장막이끼(ability_residue) / 공동벌레(ability_residue,organic_food)
      ✓ 희귀 자원을 통제하는 국가 기관: 가장 희귀한 의지 결정(희귀도 87.2) 통제 — 왕립 결정 관리국
      ✓ 생물을 연구하는 조직: 잔재 연구회
      ✓ 자원을 밀수하는 조직: 공동 밀수단[resource.beast_organ,resource.residue_ash]
      ✓ 생태계를 보호하려는 집단: 장막 수호단
      ✓ 서로 다른 욕망과 능력을 가진 인간 20명: 20명 · 서로 다른 현재 문제 20종 · 역할 20종
      ✓ 각 인물의 목적 그래프: 20/20명이 실재하는 그래프를 가리킨다 (그래프 18개)
      ✓ 조직 내부의 파벌과 갈등: 내부 집단 10개 · 수혜/피해가 함께 있는 조직 5개
      ✓ 최소 10개의 사건 패턴: 10개
✓ §40 규모 표 충족 (11/11)
    지역 3/3 · 세부 장소 12/12 · 종족 4/4 · 조직 5/5 · 주요 개인 20/20 · 일반 개체 85/80~150 ·
    자원 15/15 · 행동 20/20 · 세계 규칙 59/40~60 · 사건 패턴 10/10 · 능력 사용자 5/5
✓ 생성 정의를 손대지 않고 런타임에 올려 30일 실행 (검증 이슈 0건)
    개체 145개 · 행동 주체 44명 · change 20000건 · 사건 13건
    [ecological_conflict:1 food_crisis:1 subjugation_muster:1 investigation:3 rumor_spread:1
     broken_promise:2 smuggling_bust:1 ability_manifestation:2 faction_standoff:1]
✓ 능력 5개가 욕망에서 파생 · 전부 제약과 실패 반동을 가짐 · 출력이 클수록 대가가 큼
    veil_mending(iri)     출력  78/대가  79/제약 1종 ← "숲이 스스로 되돌아올 자리를 남긴다"
    residue_reading(rion) 출력  85/대가  80/제약 1종 ← "저 생물이 왜 그렇게 움직였는지 끝까지 안다"
    hollow_step(tarn)     출력  89/대가  81/제약 1종 ← "잡히지 않고 사람들을 건너보낸다"
    borrowed_pain(noa)    출력 105/대가 110/제약 1종 ← "눈앞의 사람이 앓는 것을 두고 보지 않는다"
    sealed_word(sera)     출력 114/대가 114/제약 2종 ← "말이 지켜지는 자리를 만든다"
✓ 생성 호출에 월드 상태 전체가 실리지 않는다 (구조화 입력 계약)
    호출 79회 · 최대 입력 3035B (상한 12288B) · 위반 0건 · 평균 1667B
✓ 15단계 전부 오프라인 실행 · 아티팩트에서 재개하면 같은 세계
    단계 15/15 · 생성 호출 79회(재시도 0회) · 아티팩트 15개 · 재개 시 호출 0회, 정의 해시 baf2bc78 동일

  6/6 통과

합계 49/49 통과
```

`npm test` 161개(Phase 5 로 +15) · `npm run smoke` 는 실제 브라우저에서 **생성 화면**까지 확인한다 —
다섯 문장 입력 → 15단계 진행 → 정합성 통과 → "생성된 세계로 시뮬레이션 시작" 버튼 활성.

## 파이프라인 실행 로그 (`npx vite-node src/scripts/generate-first-world.ts`)

```
✓  1 주제 정규화    주제 5개 정규화 (호출 1)
✓  2 핵심 명제     명제 10개 (호출 1)
✓  3 생존 압력     압력 18종 (호출 1)
✓  4 상태 스키마    상태 71종 (호출 1)
✓  5 세계 규칙     규칙 59개 (호출 11 — 묶음 목록 1 + 그룹 10)
✓  6 자원·공간     지역 3 · 장소 12 · 자원 15 (호출 2)
✓  7 종족        종족 4 (무리 방어 / 모방 / 환경 동화 / 다산) (호출 5)
✓  8 조직        조직 5 · 내부 집단 10 (호출 6)
✓  9 능력 체계     능력 5개 (출력 85/114/89/78/105) (호출 6 — 문맥 1 + 능력 5)
✓ 10 목적 그래프    그래프 18 · 목적 30 (호출 19)
✓ 11 행동 정의     행동 20종 (호출 1)
✓ 12 사건 패턴     사건 패턴 10개 (호출 1)
✓ 13 초기 배치     개체 136 (인물 20 · 일반 85) (호출 24 — 명단 1 + 인물 20 + 배치 3)
✓ 14 정합성 검증    오류 0
✓ 15 실행 데이터 저장 world.constraint_continent 저장
```

§5.2 의 분할 호출이 그대로 보인다 — 79회 중 63회가 항목 단위 호출이다. 실패한 항목 하나만 다시 부를 수 있고,
14·15단계는 생성 호출이 0회다(코드 몫).

## 생성된 세계가 스스로 만든 사건 (시드 42, 30일 — 아무도 작성하지 않았다)

```
 0~29일 ecological_conflict    ongoing   중요도 12296 참여 36
 4~29일 promise_breach         ongoing   중요도  4973 참여 27
 0~13일 investigation          concluded 중요도  4572 참여 46
 0~29일 rumor_spread           ongoing   중요도  3085 참여 54
 0~29일 faction_standoff       ongoing   중요도  2431 참여 41
17~29일 investigation          ongoing   중요도  2251 참여 42
 0~29일 subjugation_muster     ongoing   중요도  2113 참여 46
 0~24일 ability_manifestation  concluded 중요도  1934 참여 53
 2~29일 food_crisis            ongoing   중요도  1792 참여 56
 0~ 1일 promise_breach         concluded 중요도  1230 참여 26
 0~ 1일 smuggling_bust         concluded 중요도  1230 참여 26
14~15일 investigation          concluded 중요도   856 참여 42
24~29일 ability_manifestation  ongoing   중요도   599 참여 46
```

Phase 4 가 수동 세계에서 뽑던 6종에 더해 **밀수 적발·능력 발현·조직 대치**가 새로 나온다. 이 셋은 Phase 5 가
생성한 조직(관리국↔밀수단)과 능력 체계에서만 나올 수 있는 사건이다 — 즉 생성물이 실제로 동역학에 참여한다.

## 녹화 코퍼스의 출처 (숨기지 않는다)

`src/content/first-world/recorded/*.json` 은 "생성 AI 가 §41 입력에 대해 내놓은 응답"의 녹화본이다. 그중
**규칙 44개·목적 그래프 10개·행동 14종·사건 패턴 6개·압력 9종·상태 50종은 Phase 1~4 가 손으로 쓴 데이터
그대로**다. 이것이 이 트랙의 역전 관계다 — "Phase 1~4 가 만든 실행 가능한 데이터 포맷이 Phase 5 생성기의
출력 스키마가 된다"(구현 분해 README). 나머지(지역 1·장소 8·종족 2·조직 3·자원 12·행동 6·규칙 15·
사건 패턴 4·능력 5·인물 16·일반 개체 85)는 §40 규모를 채우려고 같은 포맷으로 이어 쓴 것이다.

따라서 이 Phase 가 증명하는 것은 "LLM 이 좋은 세계를 쓴다"가 아니라 **파이프라인이 그 포맷을 조립·검증·
실행 가능한 상태로 만든다**는 것이다. 실제 LLM 어댑터는 같은 `TextGenerationPort` 뒤에 들어오고, 그 응답을
이 형식으로 녹화하면 그 순간부터 오프라인 재현이 가능하다.

## 구현 중 드러난 실패와 처방

| 관측된 실패 | 원인 | 처방 |
|---|---|---|
| 규칙 스키마 검증이 `$ref` 를 못 풀고 터짐 | 배열이 루트인데 `$defs` 는 항목 스키마 안에 있었다 | `RULE_LIST_SCHEMA` 루트로 `$defs` 를 끌어올림 |
| 정합성 검증(14단계)은 통과했는데 런타임 로드에서 죽음 | 정적 검사만으로는 "로드된다"를 말할 수 없다 | 14단계가 **버리는 런타임에 실제로 부트스트랩**해 본다 |
| 조직이 사람의 행동(순찰·밀수)을 골라 `faction.energy` 를 씀 | 조직 목적에 몸 쓰는 행동 태그를 줬다 | 조직 목적은 `delegate`/`faction_trade` 만 — 몸은 개인이 쓴다(§17) |
| 개인이 조직 전용 규칙을 발동시켜 `agent.threat_belief` 를 읽음 | 규칙에 주체 종류 가드가 없었다 | 위임·조직거래·결정신고 규칙에 `entity_type` 가드 추가 |
| 군체가 지수적으로 불어나 30일 실행이 10분을 넘김 | 번식 규칙에 개체 수 상한이 없었다 | `query_value count < 12` 상한(§8 영역 압력) |
| 어느 규칙이 터졌는지 알 수 없어 디버깅 불가 | 예외에 규칙·바인딩 정보가 없었다 | `RuleEngine` 이 규칙 id·actor·target 을 붙여 다시 던진다 |
| 능력이 30일 내내 한 번도 안 쓰임 | 능력 사용 행동의 기대 효과가 어떤 목적의 원하는 변화와도 겹치지 않았다 | 능력별 실제 효과를 발동 규칙의 효과 조건(`ability_id`)으로 분기 + 행동의 기대 효과 명시 |

## 알려진 한계 (Phase 6 로 넘긴다)

- **change 로그 상한**: 개체가 8배(18→145)로 늘면서 30일 change 가 상한 20000 에 닿는다(`MAX_CHANGE_LOG`).
  오래된 변화부터 잘리므로 초기 구간의 사후 분석은 제한된다. 탐지는 매 tick 돌아 누락되지 않는다.
- **실행 시간**: 생성 0.07초, 30일 실행 약 20초(수동 세계 4초). Phase 6 자동 시뮬레이션이 반복 실행을 요구하면
  개체 수 또는 판단 주기를 조율해야 한다.
- **의미 검증 10종(§34)** 은 여전히 Phase 6 몫이다. 14단계는 참조 무결성과 로드 가능성만 막는다.

## 이후 Phase 인터페이스

- 단계 산출물 아티팩트(`ArtifactStore`, 15개) → Phase 6 검증 대상. 단계 단위 재개도 여기서 온다.
- 심볼 테이블(435개 · 미해결 참조 판정) → Phase 6 참조 검증 재사용.
- `ValidationIssue`(§34 형식)를 이미 내보낸다 → Phase 6 WorldValidator 가 같은 배열을 채워 넣는다.
- `GenerationViewModel` → Phase 8 세계 생성 화면 고도화(단계 재시도 버튼·부분 재생성)의 소비 계약.
- `TextGenerationPort` → Phase 8 §33.3 표현 생성(사건 제목·대화·소문)도 같은 포트를 쓴다.
