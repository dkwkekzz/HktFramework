# 구현 분해 개요 — Design-MMO 단계별 상세 설계

> 원본 기획서: [../Design-MMO.md](../Design-MMO.md). 이 디렉터리의 모든 문서는 기획서 내용을 **그대로** 구현하기 위한 분해다. 기획서에 없는 기능을 추가하지 않으며, 각 설계 항목은 기획서 섹션 번호(§)로 근거를 명시한다.

## 분해 원칙

1. **기획서 §42 구현 순서를 그대로 따른다.** 8단계 순서(수동 소세계 → 규칙 DSL → 주체 판단 → 사건 탐지 → 생성 컴파일러 → 자동 검증 → 플레이어 개입 → 표현 고도화)를 재배열하지 않는다.
2. **§2.1의 3계층 분리를 코드 경계로 삼는다.** World Generation Time(`generation/`), World Runtime(`core/`), Presentation Time(`rendering/` + Event Interpreter)은 서로 직접 의존하지 않고 `WorldDefinition` / `WorldState` / `WorldEvent` 데이터로만 연결된다.
3. **결정론이 전 단계의 전제다.** §39·§44-12(같은 시드 → 같은 결과)는 마지막에 붙일 수 없으므로, 시드 RNG·이벤트 스케줄러·저장 구조를 Phase 0으로 최우선 구축한다.
4. **각 Phase는 실행 가능한 검증물(DoD)로 닫는다.** 다음 Phase는 이전 Phase의 DoD 산출물 위에서만 시작한다.
5. **렌더링 속성은 ViewModel 로 완전히 분리한다.** 파이프라인은 `세계 상태 → ViewModel 빌더 → 렌더러(속성 그대로)` 로 고정한다. 렌더러·UI 는 `WorldState`/`WorldDefinition`/사건 원본을 직접 읽을 수 없고 **오직 ViewModel 속성만** 참조한다. ViewModel 은 "무엇을 어떻게 그릴지"가 아니라 "표시 대상의 속성"만 담으며, 시뮬레이션 의미 해석은 전부 빌더에서 끝낸다. 따라서 표현 방식이 바뀌어도(텍스트→Canvas→3D) 변경은 `rendering/` 내부로 국한되고 코어·빌더·ViewModel 스키마에는 **한 줄의 변경도 발생하지 않는다**. (HktFeature 트랙 불변 원칙 ③과 동일 구조. 경계 확립: Phase 0 §0.6, 본 구현: Phase 8)

기획서 45개 섹션 전부의 담당 Phase 매핑은 [Coverage.md](Coverage.md) 에서 검증한다.

## Phase 목록

| Phase | 문서 | 기획서 근거 | 요약 |
|---|---|---|---|
| 0 | [Phase-0.md](Phase-0.md) | §37, §38, §39, §26 | 프로젝트 골격, 시드 RNG, 이벤트 스케줄러, Worker 브리지, 저장 구조 |
| 1 | [Phase-1.md](Phase-1.md) | §42-1, §9, §26, §27, §41 | 수동 정의된 작은 세계 — 시뮬레이션이 실제로 작동함을 증명 |
| 2 | [Phase-2.md](Phase-2.md) | §42-2, §11, §12 | 규칙 DSL — JSON 규칙 언어와 실행기 |
| 3 | [Phase-3.md](Phase-3.md) | §42-3, §8, §10, §19~§25 | 주체 판단 — 인식·믿음·기억·관계·목적 활성도·행동 선택 |
| 4 | [Phase-4.md](Phase-4.md) | §42-4, §28~§30 | 사건 탐지 — 상태 변화 묶음을 사건으로 해석 |
| 5 | [Phase-5.md](Phase-5.md) | §42-5, §4~§18, §33 | 세계 생성 컴파일러 — 주제 문장 → WorldDefinition |
| 6 | [Phase-6.md](Phase-6.md) | §42-6, §34, §35 | 자동 검증과 수정 — 스키마·의미 검증 + 자동 시뮬레이션 테스트 |
| 7 | [Phase-7.md](Phase-7.md) | §42-7, §30, §31, §32 | 플레이어 개입 — 동일 행동 체계로 사용자가 한 주체를 조작 |
| 8 | [Phase-8.md](Phase-8.md) | §42-8, §36, §33.3, §6블록 | 표현 고도화 — 4개 화면, Event Interpreter, 개발자/플레이어 모드 |

## 의존 그래프

```
Phase 0 (골격·결정론)
   ↓
Phase 1 (수동 소세계 + 런타임 코어)
   ↓
Phase 2 (규칙 DSL) ──── Phase 1 의 코드 규칙 20개를 DSL 로 이관
   ↓
Phase 3 (주체 판단) ─── Phase 1 의 간이 판단을 기획서 전체 모델로 대체
   ↓
Phase 4 (사건 탐지)
   ↓
Phase 5 (생성 컴파일러) ─ Phase 2~4 의 실행 데이터 포맷이 곧 생성 목표 포맷
   ↓
Phase 6 (자동 검증)
   ↓
Phase 7 (플레이어 개입)
   ↓
Phase 8 (표현 고도화)
```

핵심 역전 관계: **Phase 1~4 가 만든 "실행 가능한 데이터 포맷"이 Phase 5 생성 AI의 출력 스키마가 된다.** 생성기를 먼저 만들지 않는 이유가 이것이다(§42-1: "생성 AI를 붙이기 전에 코드로 직접 작성한다").

## 기술 기준선

- TypeScript(strict) + Vite. 시뮬레이션 코어는 DOM 비의존(§38 Worker 실행 + headless 테스트 겸용).
- 테스트: Vitest. 모든 Phase 의 DoD 는 headless 테스트로 자동화한다.
- 공간: **데이터는 3D**(§13 — 그래프 + x·y·z 좌표, 거리·반경·관찰 계산 전부 3D), **렌더링은 2D 투영**(Canvas 2D). 3D→2D 투영은 ViewModel 빌더의 몫이고 렌더러는 투영된 속성을 그대로 그린다 — 이후 3D 렌더러로 교체해도 코어 무변경(분해 원칙 5). 프레임워크 도입 없음 — §43 "시각적 완성도가 첫 목표가 아니다".
- 생성 AI 연동(Phase 5·8): `TextGenerationPort` 인터페이스 뒤에 격리. 코어와 검증은 AI 없이 항상 실행 가능해야 한다(§2.1).

## 프로토타입 완료 조건(§44) ↔ Phase 매핑

| §44 조건 | 달성 Phase |
|---|---|
| 1. 세계관 문장 입력 | 5 |
| 2. 상태·규칙·종족·조직·개인·목적 그래프 생성 | 5 |
| 3. 생성 결과의 구조화 저장 | 0, 5 |
| 4. 브라우저에서 시간 진행 | 0, 1 |
| 5. 플레이어 없이 주체가 행동 | 1, 3 |
| 6. 믿음 기반 판단 | 3 |
| 7. 세 주체 이상의 목적 충돌 사건 | 4 |
| 8. 하나의 사건에 여러 개입 방식 | 4, 7 |
| 9. 사건 결과가 세계 상태를 변화 | 2, 4 |
| 10. 후속 목적·사건 생성 | 3, 4 |
| 11. 능력이 욕망과 제약에서 파생 | 5 |
| 12. 시드 재현성 | 0 (전 Phase 유지) |
| 13. 미작성 사건의 자발적 발생 | 1~4 종합, 6 에서 자동 검증 |
