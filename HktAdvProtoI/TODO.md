# TODO — Human 이 볼 것

살아 있는 문서 하나. Cycle 마감이 여기에 쓰고, 정해진 소비자가 지운다 (회수 규칙
[Plan-Skill §3](design/Plan-Skill-CycleExecutionWorkflow.md)). 절이 비면 절을 지운다. 경위는 git 이 소유한다.
`cycles/C###/` 에는 `spec.md` 와 `shots/` 만 있다 — 원 항목 번호(C### X-##)는 그 폴더의 그림 파일명이다.

```text
§1  Human 에게          Play 단위 질문 — 굵은 한 줄이 질문 전부. Human 이 답하고 지운다
§2  Human 이 정할 것    값·규칙·방향의 결정 — 질문과 겹치지 않는 것만
§3  뒤 층 · 뒤 Play 로  지금 Cycle 이 받을 수 없는 것 — 그 층·Play 의 기획이 받는다
§4  다음 Cycle 로       열린 Play 의 다음 Cycle 명세가 읽는다
§5  공학 부채           AI 가 소비한다 — 처음 난 자리 하나만 적는다
```

## 1. Human 에게 — Play 실주행 판정

읽는 법: **굵은 줄**이 질문이다. "예 / 아니오 / 값" 으로 답하면 된다. 그 아래는 왜 묻는지와 확인 방법이다.
예면 그 질문을 지운다. 아니오면 그 줄이 DESIGN GAP 이 되어 `advprotoi-design` 에 들어간다.
그림·테스트가 이미 답한 항목은 AI 예심이 닫았다 — 여기 남은 것은 사람 눈과 사람 결정뿐이다.

공통 준비: `npm run dev` (세계 + 클라이언트). 자리를 바로 잡으려면 `HKT_SPAWN_REGION=<방> HKT_SPAWN=<x,z>`,
자율 존재가 방해하면 `HKT_NPCS=none`. 손잡이는 전부 검증용이고 세계 규칙을 바꾸지 않는다.

### RoomOfAnotherKind (C019~C021) — 세계에 갈래가 둘이 되었다 (빙결 협곡)

**Q39. 고개를 넘어 협곡을 한 바퀴 돌고 백왕령으로 돌아오는 동안 "여긴 다른 세계다" 가 느껴지는가? 마지막에 "숲으로 갈까 협곡을 더 볼까, 무엇을 들고" 가 실제로 물음이 되는가?**
- 전제: 협곡은 숲과 겹치는 것이 하나도 없다 — 바닥이 흙이 아니라 서리이고, 흔적이 색이 아니라 숨이 어는 정도이며, 위험이 짐승이 아니라 눈보라·절벽·결정면이다. 재료도 다른 것 하나(빙정석)뿐이다.
- 무엇을 묻나: 마디 하나하나(넘는다 · 앞이 안 보인다 · 막힌다 · 캔다 · 요구받는다 · 돌아온다)는 테스트와 그림이 확인했다. 그 여덟이 **한 주행에서 이어지는지**는 걸어야 안다. 이 Play 가 증명하려던 것이 그것이다.
- 확인: 백왕령 (0, 0) → 서쪽 고개 (−18, 0) → 얼음 협곡 → 오솔길 → 빙결 협곡(캐고 · 문을 읽고) → 되돌아 백왕령 산기슭. 때는 `HKT_CLOCK=SEEP`.
- 원 항목: C021 X-⑦ · C019 X-①②⑦ · C020 X-①

**Q40. 골 안에 서면 벽이 화면을 채워 "협곡" 이 한눈에 안 읽힌다 — 시점을 손볼 일인가, 벽을 낮출 일인가, 그대로 두는가?**
- 전제: 카메라는 몸을 따라다니는 고정 거리다. 협곡의 벽은 높이 20·24 이고, 땅의 모양은 위에서 본 그림(`world:observe`)에서만 읽힌다.
- 무엇을 묻나: 벽 높이는 데이터 한 줄이고 시점은 화면 레인의 일이다 — 어느 쪽인지. 그대로 두면 "협곡 안은 원래 답답하다" 가 이 방의 뜻이 된다.
- 확인: `HKT_SPAWN_REGION=ICE_CANYON HKT_SPAWN=0,-16` 으로 서 보고 `cycles/C019-beyond-the-pass/shots/ICE_CANYON.surface.png` 와 견준다.
- 원 항목: C019 X-⑨

**Q41. 협곡의 원천 넷이 "같은 것의 네 모습" 으로 읽히는가, 아니면 네 가지 다른 물건으로 읽히는가?**
- 전제: 넷 다 빙정석이고 넷 다 푸르다 — 바닥에 깔린 서리 · 벽을 가르고 선 결정면 · 눈에 섞인 가루 · 언 사체 위에 돋은 가시. 세계는 "넷이 같은 재료다" 라고 어디서도 말하지 않는다 (지목한 판은 본 모습만 말한다).
- 무엇을 묻나: 그것을 잇는 것이 관찰자의 일인데, 잇을 만큼 닮았는지. 안 닮았으면 그림 넷의 색·모양이 데이터 한 줄씩이다.
- 확인: 넷을 차례로 지목(Alt+클릭) — 얼음 협곡 (8, −16) · 빙결 협곡 (8, −15) · (−6, 6)[스밈] · (−3, −10).
- 원 항목: C020 X-②

**Q42. 백왕령 산기슭에서 「산맥이 겨우 막는다」를 보고 "저기 협곡의 추위가 여기까지 왔다" 로 이어지는가? 세계는 그 이유를 한 마디도 하지 않는다 — 그대로 두는가?**
- 전제: 스밈과 긴 밤에 산맥 조건의 줄만 옅어지고(강·거목은 그대로) 그 자락에 서리 색이 앉는다. 무엇이 그것을 약하게 했는지는 어디에도 실리지 않는다 — 협곡에 가 보고 관찰자가 잇는 것이 이 Play 의 마지막 마디다.
- 무엇을 묻나: 원인을 싣지 않은 것이 미지감인지 불친절인지. 겸해서 그 자락이 **화면에서도** 갈리는지 본다 — 철이 하늘빛으로 땅 전체를 물들이므로 그림 둘로는 판정할 수 없었다.
- 확인: 백왕령 (−16, 1) 에 고요 · 스밈 · 긴 밤으로 각각. 그림 `cycles/C021-cold-crosses-the-pass/shots/X-01-ridge-still.png` ↔ `X-02-ridge-weak-seep.png`.
- 원 항목: C021 X-①③ · C021 spec 기본형 ②

**Q43. 값 셋을 그대로 두는가 — 눈보라의 시야(낮 20 · 밤 10) · 결정면이 긴 밤에 두 배 빨리 자람(180 → 90초) · 언 사체 곁의 결정 720초?**
- 전제: 앞의 둘은 Play 확정 사항이 준 값이고, 720 은 기본형이다. 셋 다 데이터 한 줄.
- 무엇을 묻나: 걸어 보고 기다려 보고 바꿀 값이 있는지. 특히 720 은 **되돌리는 것이 시간이 아니라 그것이 다시 지나가는 것**인데 그 경로(체온을 감지하는 포식자)가 아직 세계에 없어 시간으로 대신한 값이다 — 경로가 설 때까지 아예 안 돌아오게 두는 선택도 있다.
- 확인: 눈보라 자락 안팎을 낮과 긴 밤에. `HKT_SOURCE_PHASE=CLIFF_FROST_VEIN:depleted` 로 고요와 긴 밤에 각각 기다린다.
- 원 항목: C019 X-④⑤ · C020 X-⑥⑦

**Q44. 「여기서 다시 자랐다」를 협곡의 결정면만 말하고 숲의 노두는 말하지 않는 지금이 맞는가?**
- 전제: 둘 다 캐면 **옆 자리로 옮겨** 다시 자라는 원천이다. 규칙은 원천이 그 말을 밝혔을 때만 싣게 두었고, 지금 밝힌 것은 협곡의 결정면 하나다.
- 무엇을 묻나: 숲도 말해야 하면 데이터 한 줄이다. 협곡만 말하는 것이 "여기서는 결정이 자란다" 라는 이 방의 사실이면 그대로 둔다.
- 확인: `HKT_SOURCE_PHASE=ORE_OUTCROP:recovering` 으로 노두가 옮겨 선 뒤 지목한다.
- 원 항목: C021 X-⑤

### RoomBearsLife (C022~C025) — 방이 생명을 낳는다 (허물의 주인) · **Play 가 아직 열려 있다**

이 Play 는 C025 까지 간다. 아래는 **AI 예심 전의 관찰 항목**이다 — 마지막 Cycle 뒤에 질문 대여섯으로 압축된다.
지금 답할 필요는 없고, 걸어 보다가 어긋나는 것이 있으면 그 줄에 적어 두면 된다.

```text
C022 — 허물의 주인 (아직 태어나지 않는다)
[ ] X-① 거목의 방 (9, 1) 곁에 서면 알집이 보이고, 세계 위에 그것을 가리키는 글자·아이콘·타이머가 하나도 없다
        HKT_SPAWN_REGION=RED_EYE_TREE HKT_SPAWN=6,1 HKT_NPCS=none · 그림 C022 X-01
[ ] X-② 알집을 지목하면(Alt+클릭) 「뿌리 마디에 맺힌 붉은 알집」이 지금 무엇이고 무엇이 모자란지 말한다.
        캘 수 없다는 것도 그 자리에서 읽힌다 · 그림 C022 X-02
[ ] X-③ 주인이 없는 세계에서는 결속이 서고 — 알집에 붉은 빛이 돌고, 발밑에 「땅이 낮게 떤다」가 서고,
        둘레의 흙이 한 단계 옅어진다 (재료가 알집으로 간다) · 그림 C022 X-03 · X-04 (X-01 과 나란히 본다)
[ ] X-④ 다른 방(둥지)의 균사를 캐 놓으면 결속이 서지 않고 판이 「삭은 것에서 뻗어 온 것이 끊겼다」를 말한다
        HKT_SOURCE_PHASE=NEST_FUNGUS:depleted · 그림 C022 X-05
[ ] X-⑤ 긴 밤에는 「비가 오지 않는다」 하나로 결속이 서지 않는다 — 비는 저장되는 값이 아니라 때에서 나온다
        HKT_CLOCK=LONG_NIGHT · 그림 C022 X-06
[ ] X-⑥ 결속이 **다 차기 전에는** 아무 일도 일어나지 않는다 — 뿌리혹도 균사도 개체군도 그대로다
        (다 차면 태어난다 — C023 이 그것을 세웠다. 그림 없음 · 시나리오가 증거다)
[ ] X-⑦ 회귀 — 같은 방 반대편의 뿌리혹은 한 값도 달라지지 않았고 그대로 캘 수 있다 · 그림 C022 X-07
```

```text
C023 — 태어남은 소비다
[ ] X-① 결속하던 알집이 60 세계 초에 **터진다** — 손잡이 없이 기다리면 세계가 스스로 한다.
        터진 것은 실루엣부터 갈린다(붉은 것이 한 점도 없다) · 그림 C023 X-01 ↔ X-02
        HKT_SPAWN_REGION=RED_EYE_TREE HKT_SPAWN=9,2 HKT_NPCS=none 으로 70 초쯤 기다린다
[ ] X-② 같은 순간 세계가 값을 치렀다 — 뿌리혹이 여위고, 그 곁에 **빈 껍질**이 서서 캘 수 있고,
        발밑 판의 「소란」이 0 이 아니며 「서 있는 것: 광식충 떼」가 늘었다 · 그림 C023 X-02 · X-03
[ ] X-③ 120 초 뒤 알집이 되돌아오지만 다시 결속하지 않는다 — 「이 자리에 이미 주인이 있다」
        HKT_LIFE_PHASE=ROOT_CLUTCH:dormant HKT_POPULATION=ORE_EATER:1 · 그림 C023 X-04
[ ] X-④ 뿌리 마디의 **작은 붉은 점**이 알집 없이 잇는다 — 다른 이름 · 다른 그림 · 다른 자리이고
        비도 균사도 묻지 않는다 (모자란 것이 없다) · 그림 C023 X-05
[ ] X-⑤ 뿌리혹을 캐 놓으면 계승도 서지 않는다 — 「뿌리에 쌓인 것이 모자라다」 하나로
        HKT_SOURCE_PHASE=ROOT_NODULE:depleted · 그림 C023 X-06
[ ] X-⑥ 광식충이 많을수록 **떼가 도는 자락이 넓다** — 세계는 값을 말하지 않는다
        HKT_POPULATION=ORE_EATER:1 ↔ 4 · 그림 C023 X-07 ↔ X-08 (자락이 옅어 나란히 놓아야 보인다)
[ ] X-⑦ 계승이 지난 자리에 **작은 껍질**이 남아 캘 수 있고, 뿌리혹은 그것이 먹어 비었다
        HKT_POPULATION=ORE_EATER:1 로 120 초쯤 기다린다 · 그림 C023 X-09
```

```text
C024 — 스폰이 아니라 회복이다
[ ] X-① 다 캔 허물은 **벗을 것이 없으면 돌아오지 않는다** — 길이(60)의 곱절을 기다려도 그대로다.
        같은 세계 · 같은 자리 · 같은 기다림인데 광식충이 가득하면 돌아와 다시 캘 수 있다
        (프롬프트가 「기본 스킬」 에서 **「채취」** 로 바뀐다) · 그림 C024 X-04 ↔ X-05
        HKT_SPAWN_REGION=FOREST_EDGE HKT_SPAWN=-6,6 HKT_SOURCE_PHASE=MOLT_LITTER:depleted
        HKT_POPULATION=ORE_EATER:0 ↔ 4 · HKT_NPCS=none
[ ] X-② 멎은 허물을 지목하면 판이 **「벗을 것이 없다」** 를 말한다 — 「바닥났다」 · 「되돌아오는 중」 과
        갈리는 말이다. 값이 오르면 그 말이 사라진다
        (겨냥이 몸을 집어 그림으로는 못 잡았다 — 걸어가 지목해 본다 · §5 촬영 부채)
[ ] X-③ 둥지에 **사체**가 눕고 그 곁의 자리에서 흰 것이 사체를 덮어 가다 터진다 — 세 때가 눈으로 갈린다
        · 그림 C024 X-01 → X-02 → X-03 (나란히 본다)
        HKT_SPAWN_REGION=PREDATOR_NEST HKT_SPAWN=-13,13 HKT_LIFE_PHASE=CARCASS_TO_FUNGUS:binding ↔ spent
[ ] X-④ 터진 뒤 그 자리의 흙이 **한 단계 더 짙어진다** — 판의 「흔적」이 「붉게 물들었다」 에서
        「검붉게 짙었다」 로 바뀐다. 그 자락은 그때만 서고 앞의 둘에는 없다 · 그림 C024 X-03
[ ] X-⑤ 둥지의 균사를 캐 놓고 거목균이 하나도 없으면 그것도 돌아오지 않는다 — 판이 「삭일 것이 없다」
        HKT_SPAWN_REGION=PREDATOR_NEST HKT_SOURCE_PHASE=NEST_FUNGUS:depleted HKT_POPULATION=TREE_FUNGUS:0
[ ] X-⑥ 균사와 뿌리혹을 끊어 두고 한 철을 보내면 떼의 자락이 한 겹 줄고, 다 사라진 뒤
        뿌리에 **작은 붉은 점이 아니라 큰 알집**이 다시 맺힌다 (세계가 처음으로 돌아갔다)
        — 걸어서 겪으려면 짧은 철이라야 한다 (§2 의 눈금 물음). 그림 없음 · 시나리오가 증거다
[ ] X-⑦ 회귀 — 둥지의 빛 갓 둘과 껍질 조각은 한 값도 달라지지 않았고 그대로 캘 수 있다
```

## 2. Human 이 정할 것 — 값 · 규칙 · 방향

§1 의 질문에 안 들어간 결정만. 한 줄이 결정 하나다. 정하면 지운다.

```text
Life
  기본 세계는 **t=60 에 첫 탄생을 일으킨다** — 조건 넷이 세계가 설 때부터 다 차 있기 때문이다
    (고요의 첫 비가 [0, 90) 이다). 그래서 관찰자가 거목에 걸어 닿기 전에 이미 한 번 태어나 있고,
    Play §4 의 "기다림 → 목격" 은 두 번째 탄생부터 볼 수 있다 (개체군이 0 으로 돌아오는 것은 C024).
    첫 결속을 관찰자가 볼 수 있게 늦출 것인가 — 늦춘다면 비의 자리이거나 결속의 길이다
  뿌리의 알도 **머묾 120 초**를 쓴다 (알집과 같은 값) — Design 은 알집의 것만 준다.
    밝히지 않으면 계승이 90 초마다 끝없이 돌아 상한까지 순식간에 찬다
  "결속보다 적게 먹는다" 를 **먹는 원천의 수**로 읽었다 — 결속은 둘, 계승은 하나.
    이 세계의 원천에는 "얼마나" 가 없고 캘 횟수 하나뿐이다 (뿌리혹은 1)
  탄생이 올리는 소란 **5** (타격과 같은 눈금 · 채취 10 보다 작다). 임계 300 이라 탄생만으로는 깨어나지 않는다
  **거목균(TREE_FUNGUS)이 개체군으로 섰다** — Play 확정 6 의 눈금 표에는 셋(광식충 4 · 조류 2 · 포식수 1)뿐인데,
    §5.7 이 요구한 변성형은 값을 올릴 개체군이 있어야 성립한다. Concept §4 의 사슬에 이미 있는 균류를
    그 자리에 세우고 상한을 **2** 로 두었다 (눈금 셋의 가운데). 넷째 개체군을 두는 것이 옳은가          C024
  값이 내리는 판정을 **철이 바뀌는 순간**에 둔다 — "한 철 이어지면" 을 "그 철 동안 한 번도 다 차지
    않았는가" 로 읽었다. 태어남이 잠깐 먹어 비는 것을 결핍으로 세면 값이 결코 자라지 못하기 때문이다   C024
  **눈금이 어긋난다** — 철 넷 가운데 관찰자가 "한 철 내내 모자람" 을 실제로 겪을 수 있는 것은 뒤척임(60 초)
    뿐이다. 고요는 1080 초라 캐 둔 뿌리혹(180 초)이 그 철 안에 반드시 되돌아온다. 규칙이 아니라 **데이터
    눈금**의 물음이다 — 원천의 길이를 늘릴 것인가 · 철을 짧게 볼 것인가 · 그대로 두고 뒤척임의 일로 둘 것인가  C024
  **STATE_VERSION 을 올리지 않았다** (`hkt-adv-proto-i/10` 그대로) — 저장되는 것이 둘 늘었는데도
    (적용한 철의 수 · 개체군의 metThisSeason) C022·C023 이 생명 State 를 더하고도 올리지 않은 선례를 따랐다.
    옛 스냅샷은 없는 값을 0 으로 읽어 지난 철이 한 번에 지나간 것처럼 굴러간다 (큰 걸음과 같은 답).
    저장 형이 늘 때마다 올릴 것인가, 되살릴 수 없게 될 때만 올릴 것인가                                C024
  개체군의 상한에 닿으면 **전이가 통째로 일어나지 않는다** — 재료만 먹고 아무것도 낳지 않는 자리를 만들지 않으려고
  BORN 이 **한 tick** 이다 — 눈에 보이는 "터졌다" 는 SPENT 다 (V19 가 갈리라 한 셋은 DORMANT · BINDING · SPENT)
  비의 눈금 — 하루(360초) 안의 90초 구간으로 두고 고요 하루 한 번 · 스밈 두 번 · 긴 밤과 뒤척임 0.
    Design 은 "스밈에 잦고 긴 밤에 없다" 까지만 말한다 (Play 확정 4). 결속 60초가 비 한 번 안에 다 차는 값이다
  조건이 깨지면 결속의 진행이 **멎는다** — 0 으로 지우지 않는다. Design 은 "늦어진다 · 두면 되돌아온다" 까지다.
    지우면 "늦어진다" 가 아니라 "처음부터 다시" 가 된다
  알집 둘레의 흙이 옅어지는 때 = **결속하는 동안 내내**. spec 기본형 ④ 는 "진행 절반" 을 들었으나
    진행은 관찰 결과에 실리지 않아 화면이 그것을 볼 수 없다 — 세계와 화면이 같은 단계를 내려면 phase 를 물어야 한다
  땅의 떨림을 **위험이 아니라 선 자리의 코드**로 두었다 (몸에 아무 일도 하지 않는다). 위험이어야 하면 데이터 한 줄
Rooms
  카메라 규칙 — C003 spec(동결)의 "방 크기가 시점 거리를 정한다" 는 뒤에 승인된 V4 가 뒤집었다. 이 기록으로 확정하는가
  중첩(방 안의 방)은 지금 아무 일도 하지 않는다 — 언제 · 어느 층(3층 "주체가 아는 것")에서 의미로 쓰는가
  추락에 아무 대답(문구 · 연출)이 없다 — 그대로 두는가
  거절 문구가 뜨는 시간 2.2초 고정 — 정책으로 두는가, 글자 길이 비례로 바꾸는가
  화면 아래 안내가 한 줄이라 출구가 여럿이어도 하나만 말한다 — 여럿을 한 번에 보일 자리를 언제 두는가
Land
  숲 가장자리 분지는 바닥이 보이는데 내려갈 수 없다(급경사 고리) — "내려갈 수 있는 분지" 를 원하면 깊이/falloff 값 한 줄
  이동 **진행**이 땅의 통행 가능 여부를 읽지 않는다(막는 것은 요청 판정뿐) — Play 밖이었다. 요구할 것인가
Rule
  심장에서 나올 때도 배열 P2 가 필요하다(문이 하나라서) — 남이 미로를 걸어 배열을 바꾸면 갇힌다. 허용하는가
  "돌아가기" 가 심장을 꺼내 주지 않는다 — 심장에 비상 자리(exit.emergency)를 줄 것인가
  "함께 있는 사람 수" 가 방 단위가 아니라 세계 전체 수다 — 같은 방을 뜻해야 한다면 결손이다
  걷는 도중 떠나면 남은 걸음이 압력을 더한다 — 규칙상 옳지만 회색지대. 그대로 두는가
Material
  마디가 전부 무너지면 원천이 옮겨 갈 자리가 없어 지날 수 없는 자리에 선다 — spec 기본형 ③ 유지하는가
  어귀의 원천에 코드 둘("되돌아옴이 멎었다" · "아직 그때가 아니다")이 한 줄로 이어진다 — 읽기 좋은가
  호수 바닥의 침전은 추락으로만 닿고 나가는 길은 물길 하나다 — 외길이어도 좋은가
  고갈 뒤 둘레 흙의 대비가 방마다 다르다(1→1 · 3→3 · 5→4) — 값 확정
Observe
  지목 입력을 Alt+클릭으로 정했다(spec 은 UNRESOLVED) — 승인하는가
  판의 줄 id 가 판 안에서 유일하지 않다(행동 줄이 대상마다 여럿) — 유일해야 하는가
  조건 area 위에서 "걸린 것" 과 "안전한 이유" 두 줄의 관계 — 땅에서 온 태그가 새어 들어도 되는가
  존재를 지목한 동안 내 깊이 · 안전한 이유 · 압력을 어디서 읽는가 — 지금은 띠에도 판에도 없다
  코드를 문구로 옮기는 자리를 판이 아니라 조립(app)에 두었다 — R4 "결정 표가 판 안에" 와 어긋남을 승인하는가
  "지목이 없으면 판이 없다" 를 "지목의 표식이 없다" 로 옮겼다(판은 내 자리를 기본으로 늘 선다) — 승인하는가
  V 가 표현으로 정한 것 — 존재 줄 이름표 다섯 · 행동 줄 형식 "채굴 · 너무 멀다 — 가까이 이동하자" · 기본 제목 "내가 선 자리" ·
    나이 어법 "N초 전" · 재배열 나이를 새 줄이 아니라 "지금 길" 줄에 붙임 · 광맥 잔량은 판에 안 세움. 감사 한 번
Time
  밤 시야 경계값 정확히 20 인 자리 — 포함인가 아닌가 (시나리오는 ±1 여유)
  dayIndex — "360초마다 오른다" 와 "뒤척임 값은 다음 바퀴 첫 하루" 두 문장이 뒤척임 60초에서 어긋난다. 어느 쪽이 참인가
  검사 ① 이 못 재는 방의 위험 — 위험이 놓인 방이 다섯(숲 가장자리 · 생체 광석 지대 · 숲 안쪽 · 협곡 둘). 나머지 방의 위험은 어느 Cycle 이 세우는가
  spec R6 과 R7 의 모순 — 그 철이 아니면 존재가 "서지 않는다" 인가, 존재는 서고 "이 철이 아니다" 가 "실린다" 인가 (코드는 R6)
  Connector 활성 집합의 교대표 — 어느 문이 어느 철에 교대하는가, "어느 철에도 갈 곳 하나 이상"(검사 ㉖) 규칙
  세계가 서는 그 순간이 고래의 시간표 안이다 — 첫 Tick 부터 백왕령 위로 고래가 지나 세 방의 소란이 몸 없이 90 씩 오른다.
    규칙은 옳지만 지나가는 것이 사건이 아니라 배경이 되고 C017 의 임계 뜻이 흐려진다. 시간표 한 줄(데이터)을 바꿀 것인가   C018
  소란의 임계 300 이 이 세계에서 실제로 닿는 값인가 — 확정 5 의 근거는 세계 전체 채취 합이고 소란은 방마다다
    (한 방의 원천은 하나 · 세 번 캐면 180초 대기). 값을 두는가 낮추는가                                              C017
Frost
  문의 **요구**는 표시일 뿐 활성을 판정하지 않는다 — 빙결 심층의 문은 요구를 적어 두고도 긴 밤이면 열린다.
    2층은 표시까지라는 분할선을 그대로 두는가 (실제로 여는 것은 Access 의 Lock)                                  C020
  옅어진 조건이 안전의 코드를 **대신** 선다 (곁에 함께 서지 않는다) — 「산맥이 막는다」가 지워지고
    「산맥이 겨우 막는다」가 그 자리에 선다. 둘을 나란히 세우면 판이 같은 것에 두 번 답한다                         C021
  추위가 넘는 이음을 **하나만** 밝힌다 — 실제로는 빙결 협곡에서 오솔길과 고개를 잇달아 넘어 백왕령에 닿는데,
    데이터는 백왕령에 닿는 마지막 고개 하나만 적고 그것이 두 방을 잇는지만 확인한다 (이음의 길찾기가 세계에 없다)   C021
  협곡 흔적의 단계가 셋이다 (얼음 협곡 바닥 1 · 빙결 협곡 바닥 2 · 원천 둘레 3) — 숲의 흙은 다섯이다.
    방이 둘뿐이라 얕게 두었다. 협곡이 늘면 다시 볼 자리                                                          C020
```

spec 이 침묵해 **테스트가 판정 방식을 스스로 정한 자리**(Cycle 마다 5~12 건)는 여기 옮기지 않았다 — 각 시나리오 테스트의
단언과 마감 커밋이 그 선택을 그대로 적고 있다. 결정이 걸린 것만 위에 올렸다.

## 3. 뒤 층 · 뒤 Play 로

**실주행 GAP — advprotoi-design 의 입력** (Play 실주행 판정에서 "아니오" 로 돌아온 것. 새 축·새 미지가 아니다).
아래 둘은 [play/TrailBehindClueAhead.md](content/roadmap/play/TrailBehindClueAhead.md) 초안이 받았다 — **Human 승인 대기** (STATE §1). 승인되면 지운다.

```text
RegionGraphRooms — 깊이만으로는 "어디쯤인가" 가 잡히지 않는다. Play 는 바닥 색 + 상단 문구(문명권 → 경계 → 야생 → 심부)로
  "얼마나 깊이 왔는가" 를 알게 하려 했는데, Human 의 답은 "지도도 없는데 어떻게 알 수 있다는 건지" 였다.
  세계가 위치·방향을 어떤 형태로 줄지(지도 · 이정표 · 흔적 · 깊이 감각만)는 기획이 정할 일이다             Q1 · C001~C004
RegionGraphRooms — 떨어지는 자리를 세계가 알리지 않는다. 추락은 거목 내부 세계 북쪽 끝 (0, 38) 곁에 서면 저절로
  일어나고 지금 빌드에서 그대로 작동한다(촬영으로 확인). 그런데 Human 은 절벽에서 떨어지려다 "너무 가파르다" 만 보았다 —
  그 자리가 어디인지, 거기서 무슨 일이 나는지를 세계가 한 마디도 하지 않는다("추락에 아무 대답이 없다" 는 spec 의
  결정이었다). 예고를 둘지(바닥의 갈라짐 · 소리 · 흔적) 그대로 둘지는 기획이 정할 일이다                 Q2 · C003
```

```text
살아 있는 생물(광식충 · 조류 · 포식수) — 지금 선 원천 일곱이 그때 그 생물에 매달린다 (Play A.6 ④ UNRESOLVED)   3층
협곡을 지나는 것(체온을 감지하는 포식자) — 언 사체 곁의 결정을 되돌리는 것이 그것이다. 정식 이름 표에 이름이
  없어 C020 이 경로를 짓지 않고 시간으로 대신했다 (Q43)                                                    3층 · 생명
열을 저장하는 결정이 **어디서** 나는가 — 협곡의 문이 그것을 요구하는데 협곡에 없다. 어디서 나는지는
  정하지 않았다 (Play 확정 4 — 그것이 이 Play 의 미지감이다)                                    M7 · RoomAsksForPossibilities
요구를 실제로 **채우는** 것 — 성질 Lock · 어휘 · 검사 ㉞~㊷. 2층은 요구를 표시까지 세웠다        RoomAsksForPossibilities
재료의 쓰임 — Recipe · 조합 · 효과 · 수치. 아무것도 정하지 않았다                                         4층 이후
hazard · phenomenon layer 컨텐츠 주입 — 검사 ①④ 가 그때까지 absent 다                                    컨텐츠 층 주입
둘째 흐름 · 유한 원천(FINITE_WORLD_STATE)                                                                    다음 Region 의 Play
거꾸로 된 정원의 문 저쪽 anchor 를 'MAZE_HEART_SIDE' 로 두었다 — 정원을 짓는 Play 가 가져간다               정원 Play
```

## 4. 다음 Cycle 로

```text
C025 (숲이 스스로 돈다)
  관계가 하나도 없어 ㉜ 가 absent 이고 ㉝ 가 "관계 없는 개체군 **둘**" 을 보고한다 — links 가 서면 둘 다 실제로 잰다
  populations 의 trend — Life §3.2 가 값 곁에 둔 자리다. 값을 올리고 내리는 것이 둘 이상이 되어야
    방향이 뜻을 가지므로 세우지 않았다. **C024 가 내리는 원인 하나(CONDITION_LOST)를 세웠으니 남은 것은 먹힘이다**
  개체군의 **상한** 과 값이 내리는 원인은 여전히 데이터를 읽어야 재진다 — 값이 내리는 것은 이제 행동으로
    재지만(한 철에 하나) 그 수가 4·2 라는 것과 CONDITION_LOST 라는 글자는 봉투에도 State 에도 없다
  사체(NEST_CARCASS)의 되돌아옴 원인 `nest-kill` 이 **살아 있는 것을 전제하는데** 그 개체군(포식수)이
    아직 세계에 없다. C025 가 포식수를 세울 때 그 원인을 ㉛ 의 대상 목록(LIFE_BOUND_RECOVERY_CAUSES)에
    넣고 사체에 recoveryLife 를 붙인다 — 그때 ㉛ 의 대상이 셋이 된다
  사체를 세우는 것이 **시간에서 LEAVES 로** 갈아 끼워진다 — 그러면 변성의 원천도 "되돌리는 것은 시간이
    아니라 다시 지나가는 것" 이 되어 C018·C023 이 세운 어법에 합류한다
  하네스가 못 놓아 시나리오가 `it.todo` 로 남긴 셋 (C024) — 배속 목록보다 **큰** 값 (손잡이가 상한으로 자른다) ·
    세계가 모르는 개체군을 가리킨 요구 (없는 데이터를 지어내야 한다) · 요구를 밝히지 않은 개체군
    (지금 세계의 둘 다 밝힌다). 개체군이 셋 더 서면 마지막 하나는 저절로 놓인다
```

## 5. 공학 부채 — AI 가 소비한다

처음 난 자리 하나만 적는다. 뒤 Cycle 이 같은 것을 만나도 다시 적지 않는다. 갚으면 지운다.

```text
촬영
  잠깐 뜨는 문구(거절 · 재배열 · 명령 표면)를 촬영이 못 잡는다 — 판이 대신 답해 주게 된 뒤(C028) 우회했다        C006
  촬영 하네스에서 자판 걸음(hold)이 몸을 옮기지 못한다 — 원인 미확정. 눌러서 걷게 바꿔 넘겼다(C028)                C003
  촬영이 사람의 걸음 · 떠남을 밀지 못한다 — 캔버스 둘이면 왕복이 수십 초(실측 43초)로 밀린다                      C010
  촬영의 Q 가 anchor 에서 멀면 닿지 않는다 (거리 1.6 에서도) — chooseByKey 가 눈앞의 것을 고르는 방식으로 보인다   C009
  촬영이 겨냥(화면 비율)으로 존재를 집는다 — 존재 id 로 겨냥할 수 없어 엉뚱한 것이 잡힌다                        C026
  존재가 쓰러지는 장면을 그림으로 못 잡는다 — 몸을 쓰러뜨리는 개발 명령이 없다                                  C027
  촬영의 expect 가 **늘 떠 있는 판**의 글자를 훑지 못한다 — 훑는 것이 HUD 뿐이라 판에 선 줄은 전부 "없음" 으로 적힌다  C019
  철이 하늘빛으로 땅 전체를 물들여 **철이 바꾼 자락을 그림으로 견줄 수 없다** — 두 철의 그림은 바닥색이 이미 갈린다.
    같은 철 안에 자락 안팎을 견주는 그림이 필요하다                                                              C021
  떼의 자락이 옅어(불투명도 0.12) 한 장으로는 값의 차이가 읽히지 않는다 — 나란히 놓아야 보인다.
    값마다 자락을 겹치는 방식이라 진해질수록 넓이가 아니라 짙기가 먼저 읽힌다                      C023
  촬영 하네스의 **세계 시간이 실시간보다 느리게 흐른다** (100 초 sleep 이 세계 81 초) — 60·90 초짜리
    세계 과정을 찍으려면 어림으로 넉넉히 기다려야 한다. 세계 시각을 기다리는 걸음이 없다            C023
  **판이 멎음의 사유를 말하는 것을 그림으로 못 잡았다** — 겨냥이 몸을 집어 원천이 아니라 관찰자가
    지목됐다 (C026 의 그 부채가 이 자리에서 다시 났다). 두 그림은 프롬프트가 「기본 스킬」 에서
    「채취」 로 바뀌는 것으로 대신 증언한다                                                        C024
  부푼 균사의 띠가 그림에 들지 않는다 — 그 자락은 둥지 쪽 이음에서 알집까지 몸 **뒤로** 지나가고,
    촬영은 몸을 겨냥해 서므로 띠의 있고 없음을 견줄 프레임이 없다 (판의 말로만 증거가 남았다)     C022
  그림이 항목과 어긋나는 촬영 — C002 X-⑤⑥ · C004 X-②③④ 는 세계가 뒤에 바뀌어 오늘 재현 불가(그림 · 테스트만 증거) ·
    C012 X-09 · C013 X-08 은 원천 대신 땅을 찍음 · C026 X-03 · C028 X-02 는 타이밍 ·
    C017 X-07 은 방을 건너 버려 자국의 방향·짙기가 판별 불가 · C018 X-06 · X-07 은 "캘 수 있다" 가 프롬프트에 안 보임(S-061 만 단언)
화면
  몸 뒤의 정체 모를 반투명 판 — 이름표도 모션 시트 배경도 아니다. 원인 미확정, 추측으로 손대지 않았다             C008
  카메라가 방 전체를 담지 않는다 (40×40 도 한 화면에 안 든다)                                                   C002
  pass 색 표식이 전부 경계를 가리켜 "고개 = 아직 없는 곳" 으로 읽힐 여지 — 방이 지어지면 저절로 풀린다             C002
  80×80 방의 바닥 채움 눈금이 삼각형 상한에 걸려 조금 굵다 (뜬 거리 0.020) — 큰 Region 이 오면 chunk 로 나눈다     C005
  구역 채움(방 바닥 · settlement)은 빛을 받지 않는다 — 밤에 땅은 어두워지는데 채움은 그대로다                      C015
  자국이 짙은 흙 위에서 잘 보이지 않는다 — 오래된 것(0.22)은 붉은 흙 방에서 거의 묻힌다 (흔적과 같은 색 대역)        C017
  백왕령 (-6, -8) 에서 몸 뒤의 반투명 판이 화면 대부분을 덮는다 — C008 의 그 판이 그 자리에서 커진 것. 촬영은 피해 찍었다  C008
  판이 세로로 길어지면(기록 다섯 줄) 아래끝이 몸을 가린다 — 판과 세계의 겹침을 아무도 재지 않는다                   C028
  판이 자기 크기를 계약에 밝히지 않는다 — 판과 띠의 겹침을 잰 스크립트를 남기지 않아 다시 겹쳐도 모른다             C027
  기반의 counter 위젯은 막대를 그리지 않는다 — label + progress 로 세웠다. 필요하면 ENGINE 레인                    C008
  잠깐 뜨는 알림의 자리가 기반 SceneState 에 없다 — hud.notice 로 보낸다                                         C008
  갈래가 땅을 거의 정하지 않는다 — 땅 묶음이 있는 갈래는 일곱 중 둘. 미지 셋이 등급 A 로 섰는데 그림도 원천 자리도
    같았다 (content/authoring/templates/). **Frost 가 닫혔으므로 지금 손볼 수 있다** · 확인은 world:lab 에 갈래 다른 방 둘  T6
세계 · 규칙
  이동 진행이 traversable 을 보지 않는다 (막는 것은 요청 판정뿐) — 먼 자리를 한 번에 요청하면 막힌 칸을 지난다     C006
  무너진 자리가 길을 실제로 끊는지 아무도 재지 않는다 — 우회 판정에 길찾기가 필요하다                              C006
  밤의 범위가 관찰만 자르고 상호작용 거리(2.0)는 따로 판정한다 — 둘이 같은 자리에서 나오지 않는다                   C015
  뒤척임이 철의 시작보다 한 Tick 뒤에 적용된다 — 세계 과정이 시각이 오르기 전에 돈다. 그래서 뒤척임과 자국 묻기의
    순서를 한 걸음 안에서 가려낼 수 없다                                                                           C016
  뒤척임과 때 바뀜에 연출이 없다 — 한 프레임에 값이 갈아 끼워진다 (Play V16 의 맥동)                                  C015
  깨어남이 출현을 바꾸지 않고, 소란이 방을 건너지 않는다 — 둘 다 PresenceRoute 위에 설 일이었으나 C018 이 태그·경로만 세웠다  C017
  그늘이 관찰 범위를 좁히지 않는다 (밤의 범위를 방마다 배율로 넓히는 일) · 경로가 몸을 알지 못한다 (몸에 하는 일은 3·5층)   C018
  철의 덧씌움과 깨어남의 덧씌움이 같은 자락에서 겹치는 경우 — 생체 광석 지대가 이제 둘 다 밝힌다(긴 밤 · 깨어남 →
    hazard-ore-outcrop). 겹친 자락에서 걸린 것이 둘 다 실리는지는 아직 아무도 재지 않았다                              C017
  자국의 상한 48 이 여럿이 오래 머문 방에서 무엇을 버리는지 아무도 보지 않았다                                        C017
  위험이 한 방 · 한 갈래뿐이라 "안전한 이유와 위험한 이유가 한 자리에서 읽히는가" 를 못 쟀다                      C016
  recovering 도 depleted 처럼 둘레 흙이 짙은 채 예보한다 — 캘 수 없음은 흙이 아니라 그림과 판이 말한다              C013
  같은 Tick 안의 두 요청 경합(둘이 마지막 한 번을 다툰다) — dispatch 가 한 요청씩 판정해 하네스로 놓을 수 없다      C012
  c013~c016 이 **광식충이 가득한 숲**에서 재게 되었다 — 기본 세계가 t=60 에 태어나 뿌리혹과 균사를
    먹으므로 재료 계통의 전제가 깨진다. 재는 것은 그대로이나 그 방들의 "기본 세계" 는 이제 기본이 아니다  C023
  spec 기록 — C022 는 CHANGED 없음이라 적었으나 RULE-TRACE-STRENGTH-001 이 탄생지의 자락도 읽게 되었고
    (거는 원인 둘째), R4 를 RegionPhase 덧씌움으로 적었으나 실현은 흔적 세기와 선 자리의 코드 둘로 섰다
    (덧씌움 목록은 다섯 그대로다)                                                                    C022
  c016 R-004 이 탄생지를 품은 방을 **통째로** 뺐다 — 알집 자락 **밖**의 사다리가 철에 흔들리지 않는가도
    함께 안 재게 되었다. 자락을 덮은 표본만 빼려면 검사가 생명 데이터를 읽어야 한다                   C022
  spec 기록 — C024 Observable Result ⑦ 이 ㉚ 의 답을 `방 2 · 탄생지 3` 이라 적었으나 도구의 실제 한 줄은
    `방 2 · 탄생지 **합** 3 · 방식 3 — … · 사유를 밝힌 방 1` 이다. 시나리오는 글자 대신 수 셋만 재도록 좁혔다 —
    산문의 원본은 도구 쪽이다                                                                          C024
  spec 기록 — C008 Added 절의 "op 열둘" 은 실제 열여섯 · R6 예외로 ActorState.movedThisTick 한 줄이 늘었다          C008
도구 · 빌드
  컴파일을 켤 때마다 두 번 한다(세계 한 번 · 관찰자 한 번) — *.compiled.generated.ts 를 굽지 않는다               C005
  땅을 읽는 파일이 하나 늘어 상한을 셋에서 넷으로 올렸다 — 계속 늘면 상한의 뜻이 없어진다                          C005
  world:observe 는 읽기만 한다 — 편집 · JSON · 필터 없음. world:compile 과 합칠지는 그때 정한다                    C004
  world:observe --report 는 방 하나의 땅만 낸다 — 계통 보고는 world:check 로 모였다. Play §7 의 이름과 다르다      C014
  검사 ①④ 가 absent 다 — hazard · phenomenon layer 가 없다 (§3)                                                C007
  검사 ① 이 못 재는 방이 남았다 — 위험이 놓인 방이 다섯 (§2 Time)                                          C016
  검사 ㉓ 이 **철에 매이지 않은 위상**을 보지 않는다 — 도구가 phases.seasons 만 훑어 깨어남 · 상시 · 깨진 마디의
    덧씌움이 검사에 잡히지 않는다. 협곡의 덧씌움이 이제 열이 넘는다                                             C017
  세계가 모르는 방 · 없는 area · 없는 이음을 가리킨 덧씌움을 세울 손잡이가 없다 — "끊긴 참조는 아무 일도 하지
    않는다" 를 지금 밝힌 것이 실제로 있다는 것으로만 쟀다                                                       C021
  협곡에 철 · 소란 · 지나가는 것의 덧씌움이 하나도 없어, 깨진 마디의 자락이 그 셋과 **함께** 걸리는지를
    상시 위상과의 겹침으로만 쟀다                                                                              C020
  되돌아옴이 보이기 시작하는 지점(절반)이 헤더 상수라 시나리오가 값을 손으로 적는다                                C013
  C008 이 저장한 스냅샷 fixture 가 저장소에 없어 "옛 스냅샷이 되살아난다" 를 재생으로 못 증명한다 (it.todo)          C009
  하네스가 못 놓는 것 — 관찰 결과 "형이 같다" 의 단위 · 옮겨 가는 도중의 밤 범위 · 격자 표본 7칸마다                C015
  SPEC-005(C016) 경계 ②를 세울 Given 이 없다 — 방이 onTurn 을 밝히지 않으면 "밝히지 않은 방" 과 구별되지 않는다       C016
  경로 데이터를 갈아 끼울 손잡이가 없다 — 후보 셋 이상 · 마디 하나 · 경로 없는 세계를 세울 수 없어 휨은 데이터의 후보 둘로만 잰다  C018
  관찰자가 하나도 없는 세계를 하네스가 세울 수 없다 — "그 방에 아무도 없어도" 로만 잰다                               C017
  자율 존재 셋을 같은 시간에 같은 수의 일로 세울 손잡이가 없다 — "관찰자 셋 = 자율 존재 셋" 등식을 못 세웠다             C017
  world:observe --at 은 보고만 낸다 — PNG 에는 덧씌움이 얹히지 않는다                                                C018
  검사의 끊긴 참조를 실제로 만들어 fail 을 재는 자리가 도구 쪽에 있다 (SPEC-009 경계 ① 의 뒷면)                        C018
  부르기 명령의 이름과 받는 자리 · 남겨지는 원천의 이름을 spec 이 적지 않아 시나리오가 명령 표면·데이터에서 골라 쓴다     C018
  검사 ㉖ 은 철 조건을 밝힌 문만 본다 — 소란·경로로 닫히는 문이 생기면 넓혀야 한다                                   C018
  촬영 하네스에서 클릭 한 번이 몸을 두어 걸음밖에 못 민다 (왕복 8~15초) — 자국 여럿을 남기려면 클릭을 이어야 한다         C017
  자율 존재를 미로에 놓는 손잡이(WorldSetup.npcRegion)를 시나리오 S-015 가 아직 안 쓴다 — 갈아끼우면 우회로 하나 준다 C010
  미니맵 자리가 SceneState 에 없어 "미니맵도 없다"(C011 SPEC-008) 는 참이지만 무증명                              C011
```
