# C008 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다.

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. 미로는 백왕령 → 숲 가장자리 → 숲 안쪽 → **고대 문**(Q)으로 들어간다.
바로 보려면 `HKT_SPAWN_REGION=FANTASY_MAZE HKT_SPAWN=-30,30 npm run dev`.
자동 시나리오(`content/world/tests/c008-a-room-with-a-rule.spec.ts` 35)는 전부 PASS —
`npm test` 1104 PASS / 4 todo. 그림은 `shots/`.

```text
Start   미로는 길을 외우면 된다. 지도를 그리면 된다.
End     왔던 통로가 막혀 있다. 서 있으면 아무 일도 없는데 걸으면 바뀐다 —
        길은 외울 수 없지만 규칙은 관찰할 수 있다.
```

```text
X-①  미로에 들어선다
     하기   숲 안쪽에서 고대 문을 건넌다 (Q)
     보기   방 이름과 "법칙이 낯설어지는 심부" 가 뜨고, HUD 에 **압력 0 / 120** 이 있다.
           C007 까지 "아직 갈 수 없는 곳이다" 라고 답하던 문이다 — 규칙은 한 글자도 바뀌지 않았고
           방이 지어졌을 뿐이다
     그림   shots/X-01-enter-the-maze.png
     판정   [ ]

X-②  구역과 식물
     하기   구역 넷을 돌아본다 (A 좌상 · B 우상 · C 우하 · D 좌하)
     보기   구역마다 바닥 색이 다르고 한가운데 **다른 빛깔의 고사리**가 서 있다.
           바닥에 A·B·C·D 라고 적혀 있지 않다 — 그것을 아는 길이 식물이다.
           넷이 실루엣과 색 **둘 다**로 갈리는가
     그림   shots/X-01-enter-the-maze.png · shots/X-02-clue-plant.png
     판정   [ ]

X-③  닫힌 통로가 벽이다
     하기   DEFAULT 패턴에서 한가운데 네거리로 걸어 들어가 본다
     보기   어두운 막대가 채워져 있고 몸이 선다. "길이 닫혀 있다" 가 뜬다.
           열린 통로는 **테두리만** 있고 닫힌 통로는 **채워져** 있다 — 색이 아니라
           "막대가 거기 있는가" 로 갈리는가
     그림   shots/X-03-closed-passage.png (압력 3/120 — 세 걸음 만에 섰다)
     판정   [ ]

X-④  걸으면 압력이 오른다
     하기   복도를 오간다. 그 다음 가만히 서서 본다
     보기   걸을 때만 오른다. 얼마나 찼는지가 숫자와 막대로 함께 보인다 (38 / 120)
     그림   shots/X-04-pressure-rising.png
     판정   [ ]

X-⑤  넘치면 길이 바뀐다
     하기   120 을 넘길 때까지 걷는다 (걸음 속도 6 이므로 20초 남짓)
     보기   그 순간 통로의 열림/닫힘이 뒤집히고 그 자리가 잠시 맥동한다. "길이 바뀌었다" 가 뜬다.
           **왔던 길이 막혀 있는가** — 그리고 식물은 그 자리 그대로인가
     그림   shots/X-05-rearranged.png (압력이 21 로 돌아와 있다 = 한 번 넘쳤다.
           문구는 아래 부채 ① 참고 — 4초만 뜨므로 그림에 안 잡혔다)
     판정   [ ]

X-⑥  아래 부채 ② 를 눈으로 확인한다  ← **이번 Cycle 에서 가장 중요한 판정**
     하기   패턴이 바뀐 뒤 네 구역을 다시 돌아본다
     보기   길이 **막히는가**, 아니면 **돌아가게 되는가**. 지금 데이터에서는 네 구역이 어느 패턴에서도
           서로 닿는다 (실측: 이웃 구역이 25 → 33 걸음으로 멀어질 뿐이다).
           "당황 — 왔던 통로로 되돌아가려는데 막혀 있다" 는 성립하는가?
           "길을 외울 수 없다" 가 이 정도로 성립하는가, 아니면 벽이 더 필요한가
     판정   [ ]

X-⑦  규칙 없는 방은 그대로다
     하기   미로 밖(백왕령)으로 나가 본다
     보기   압력 줄이 **아예 없고** 통로도 없다. 걸음이 C007 까지와 같다
     그림   shots/X-06-room-without-rule.png
     판정   [ ]

X-⑧  Intent — 규칙을 방이 품는가
     하기   `content/regions/fantasy-maze.ts` 의 `rule` 한 덩이를 연다. 패턴 표를 바꾸거나
           임계값(120)을 낮춰 보고 다시 띄운다
     보기   패턴·임계·통로가 전부 그 파일인가. **규칙 코드에는 방 이름이 없는가**
           (`grep FANTASY_MAZE content/world` 가 비어 있다). 방이 규칙을 품는다는 것이
           "규칙을 가진 방" 이라는 조건 하나로 서는가
     판정   [ ]
```

## 알려진 부채

```text
① 재배열 문구를 촬영이 잡지 못한다   "길이 바뀌었다" 는 4초만 뜨는데 촬영은 그 창을 놓친다.
                                C006 의 거절 문구와 같은 한계다 (도구가 잠깐 뜨는 글자를 못 읽는다).
                                넘친 사실 자체는 그림의 압력 값(21/120 로 돌아옴)과 시나리오가 증거다

② **재배열이 길을 끊지 않는다** (가장 큰 감사 자리)   방을 사등분하면 네 구역이 한 점에서 만나므로
                                열린 네거리는 언제나 넷을 함께 잇는다. P1·P2 는 대각선 둘이 늘 함께
                                열려 네 구역이 다 통한다 — 재배열이 바꾸는 것은 "갈 수 있는가" 가 아니라
                                **"어느 길로 가는가"** 다 (이웃 구역 25 → 33 걸음 · 입구→심장 문 38 → 53).
                                벽을 더 세우려면 op 가 더 필요한데 spec 이 op 목록을 동결했으므로
                                데이터로 지어내지 않았다. X-⑥ 의 판정이 이것을 정한다 —
                                지금으로 충분하면 그대로, 아니면 통로/구역 배치가 다음 Cycle 의 데이터 일감이다

③ 미로 안에 자율 존재를 정상 경로로 놓을 수 없다   `WorldSetup.npcs` 에 regionId 를 주는 길이 없어
                                기본 방에만 선다. SPEC-008(자율 존재의 걸음도 압력)은 저장·복구를
                                거쳐 세웠다. `WorldSetup` 에 한 줄이면 되는 하네스 결손이다

④ 명세의 오기                    spec 의 Added 절이 "space 의 op 열둘" 이라 적었으나 실제 구성은
                                **열여섯**이다 (cell 4 + passage 6 + clue 4 + anchor 2).
                                동결된 문서라 고치지 않고 여기 적어 둔다 — 개수를 단언한 테스트는 없다

⑤ 명세 R6 의 예외 한 줄           "그 밖의 Rule 은 하나도 바뀌지 않는다" 라고 적었으나,
                                압력은 "이번 tick 에 얼마나 움직였는가" 를 알아야 하고 그것을 아는 자리는
                                이동 진행 규칙뿐이다. `ActorState.movedThisTick` 기록 한 줄이 늘었다
                                (판정·이동·도착의 의미는 불변). 게임 명사가 없는 사실이라 전역이어도 되고,
                                그 값을 읽는 것은 미로 규칙 하나뿐이다

⑥ 기반의 counter 위젯은 막대를 그리지 않는다   Play §5.2 는 "counter, progress" 를 적었지만
                                기반의 counter 에는 progress 자리가 없다. label + progress 로 세우고
                                숫자도 함께 남겼다 (막대만으로는 임계가 얼마인지 셀 수 없다).
                                engine 을 고치지 않았다 — 필요해지면 ENGINE 레인의 것

⑦ 화면에 정체 모를 판이 하나 있다   몸 뒤에 반투명한 사각 판이 서 있다 (C006 X-04 · C008 X-01 에서
                                보인다). **아닌 것은 확인했다** — 구역 이름표가 아니고(고친 뒤에도 남는다)
                                모션 시트의 배경도 아니다(시트의 알파를 직접 뜯어 보았다: 귀퉁이 알파 0 ·
                                프레임의 24%만 불투명). 남은 후보는 구역 채움(ground-fill)을 낮은 시점에서
                                본 것이거나 terrain instance 다. **원인 미확정 — 추측으로 손대지 않았다.**
                                눈에 보이는 결손이므로 다음에 화면을 만지는 레인이 먼저 집을 자리다

⑧ 잠깐 뜨는 문구의 자리가 기반에 없다   SceneState 에 "잠시 뜨는 알림" 자리가 없어 기존 hud.notice 로 보냈다.
                                바뀐 프레임에만 부른다 — 매 프레임 띄우면 그 사이 온 거절 문구를 덮는다
```

## 다음 Cycle 로

```text
C009 — 규칙을 이용해 닿는다   패턴 조건 activation(heartAccess) + 심장 Region + 돌아가기 명령.
    이 Cycle 이 HEART_GATE anchor 를 구역 B 안에 세워 두었고, 패턴 표의 P2 가 그 자리다.
    부채 ② 의 판정이 C009 의 데이터에도 영향을 준다 — "원하는 패턴을 불러 간다" 가 성립하려면
    패턴마다 갈 수 있는 곳이 실제로 달라야 한다

spec 이 침묵해 **테스트가 판정 방식을 정한 자리** (Human 감사)
    ① rearrangedAt 이 tick 앞의 시각인가 뒤인가 — tick 하나의 폭으로 쟀다
    ② 한 tick 에 임계를 여러 배 넘겼을 때 남은 압력 — "모두 더한 뒤 한 번 판정" 으로 읽어 0 (이월 없음)
    ③ 닫힌 통로와 열린 통로가 겹치는 자리 — 판정 규칙이 없어 그런 자리를 표본에서 뺐다
       (구현은 "하나라도 열려 있으면 열림" 이고, 데이터에는 그런 자리가 없다)
    ④ anchor 가 정확히 둘인가 — spec 의 "anchor 둘" 을 그대로 단언했다
    ⑤ 문구는 코드 그대로가 아님만 쟀다 — 글자는 View 의 결정이다
```
