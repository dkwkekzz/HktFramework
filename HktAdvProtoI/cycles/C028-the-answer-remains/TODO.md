# C028 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다.

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. 미로는 `HKT_SPAWN_REGION=FANTASY_MAZE HKT_SPAWN=0,36 npm run dev`.
자동 시나리오(`content/view/tests/c028-the-answer-remains.spec.ts` 31 + todo 6)는 전부 PASS —
`npm test` 1291 PASS / 22 todo (73 파일). 기준선은 1253 / 16 (72 파일) 였다. 그림은 `shots/`.

```text
Start   거절당했다. 문구가 2.2초 만에 사라진다. 왜 안 됐는지 다시 볼 길이 없다.
End     세계가 한 말이 판에 쌓인다. 방금 것이 위에 있고, 몇 초 전 것이 그 아래 있다. 되읽는다.
```

```text
X-①  거절 사유가 남는다  ← **이번 Cycle 에서 가장 중요한 판정**
     하기   강가에서 물 쪽으로 클릭해 걸어가 본다. 거절당한 뒤 **10초쯤 아무것도 하지 않는다**
     보기   `물이 너무 깊다` 가 판의 기록에 남아 있는가. 잠깐 뜨던 문구는 이미 사라졌는데
           기록은 그대로인가 — **2.2초 뒤에도 읽히는가**
     그림   shots/X-02-refusal-remains.png · shots/X-03-two-refusals-aged.png
     판정   [ ]

X-②  여러 번이면 여러 줄이다
     하기   같은 자리로 두 번 걸어가 본다. 그 다음 서로 다른 사유로 거절당해 본다
           (닫힌 통로 · 급경사 · 곡괭이 없이 채굴)
     보기   줄이 하나씩 느는가 — **마지막 하나만 남지 않는가.** 새 것이 위인가.
           같은 사유 두 번이 `×2` 로 묶이지 않고 두 줄로 서는가
     그림   shots/X-03-two-refusals-aged.png
     판정   [ ]

X-③  얼마 전인지가 적혀 있다
     하기   거절당한 뒤 가만히 서서 판을 본다
     보기   줄마다 `N초 전` 이 붙고, 시간이 흐르면 그 값이 늘어나는가.
           **새 줄 위로 갈수록 나이가 작은가** (아래가 오래된 것이다)
     그림   shots/X-03-two-refusals-aged.png · shots/X-05-rearrangement-remains.png
     판정   [ ]

X-④  세계의 알림도 같은 기록에 남는다
     하기   게임을 띄운다 (아직 아무것도 하지 않는다). 그 다음 방을 옮긴다
     보기   방에 들어선 제목이 **이미 기록의 첫 줄**인가. 방을 옮기면 새 방의 제목이 위에 서고
           **앞 방에서 쌓인 줄들이 그대로 남는가** (기록은 방의 것이 아니라 내 것이다)
     그림   shots/X-01-entry-remains.png
     판정   [ ]

X-⑤  재배열이 기록과 함께 읽힌다
     하기   미로에 들어선다 (판의 `지금 길` 을 본다). 복도를 오래 걸어 압력을 넘긴다
     보기   들어선 직후에는 `지금 길  DEFAULT` 에 **나이가 없는가** (한 번도 안 바뀌었다).
           넘긴 뒤에는 `지금 길  P1 · 47초 전` 처럼 마지막 재배열이 얼마 전인지가 함께 실리는가.
           `길이 바뀌었다` 가 기록에도 한 줄로 남는가
     그림   shots/X-04-no-rearrangement-yet.png · shots/X-05-rearrangement-remains.png
     판정   [ ]

X-⑥  받아들여진 것은 기록을 채우지 않는다
     하기   빈 땅을 여러 번 클릭해 걸어 다닌다
     보기   걸을 때마다 줄이 늘지 **않는가.** 기록에 남는 것은 거절과 알림뿐인가 —
           걸음마다 줄이 늘면 사유가 묻힌다
     판정   [ ]

X-⑦  기록이 화면을 삼키지 않는다
     하기   거절을 여럿 쌓은 채 미로에서 판을 본다 (판이 가장 길어지는 자리다)
     보기   기록이 다섯 줄에서 멈추고 가장 오래된 줄이 밀려나는가.
           판이 몸과 세계를 얼마나 가리는가 — **이 값(다섯)이 맞는가**  ← 아래 부채 ① 이 이 판정을 기다린다
     그림   shots/X-05-rearrangement-remains.png
     판정   [ ]
```

## 알려진 부채

```text
① 상한 다섯이 지키려던 말을 밀어낸다   `ANSWER_LOG_LIMIT = 5` 는 "판이 기록판이 되지 않는 선" 으로
                                골랐는데(근거는 answer-log.ts 주석), 실제로 찍어 보니
                                **재배열 뒤 거절 다섯 번이면 "길이 바뀌었다" 가 밀려난다**
                                (shots/X-05 — 규칙 줄의 `47초 전` 만 그 일을 증언한다).
                                거절은 연달아 나고 알림은 드물게 나므로, 잦은 것이 드문 것을
                                밀어내는 구조다. 상한을 늘릴지 · 갈래마다 자리를 나눌지는
                                X-⑦ 판정이 정할 자리다 — 지금 지어내지 않았다

② 판이 세로로 길어지면 몸을 가린다      기록이 다섯 줄까지 차면 판의 아래끝이 몸에 닿는다
                                (shots/X-05). C027 이 잰 것은 판과 **상시 HUD** 의 겹침이고
                                (SPEC-008), 판과 **세계** 의 겹침은 아무도 재지 않았다.
                                C027 부채 ① 과 같은 자리다 — 판이 자기 크기를 계약에 밝히면 둘 다 잴 수 있다

③ 촬영이 자판 걸음으로는 몸을 못 옮긴다   C003 부터의 부채가 이 Cycle 에서도 걸렸다 — `hold` 로
                                복도를 걸으려 하면 압력이 27/120 에서 멈춘다. **눌러서 걷게**
                                바꾸어 넘겼다 (shots.json run 2). 원인은 여전히 미확정이다
```

## 다음 Cycle 로

```text
Play RoomAnswersWhenAsked 의 Cycle 셋이 모두 닫혔다 — 완료 확인 넷(①②③④)이 전부 섰다.
남은 것은 **Play Goal 실주행**이다: 조용한 화면에서 눈으로 찾고, 지목해 묻고, 거절당하고,
그 사유를 되읽어 조건을 고쳐 다시 시도하는 한 바퀴가 실제로 도는가.

spec 이 침묵해 **테스트가 판정 방식을 정한 자리** (Human 감사 — T 레인 보고)
    ① 기록이 판에 들어가는 이름과 한 줄의 자리 이름을 spec 이 정하지 않았다. 하네스가 여러 어법을
      동시에 넘겨 어느 쪽이어도 통과하게 두었다
    ② **코드를 문구로 옮기는 자리가 모으는 쪽인지 판인지 spec 이 말하지 않는다.** 지금 구현은
      조립이 code-text 로 옮겨 담고 판은 그 말을 그대로 세운다 — 그래야 뜨는 문구와 남는 줄이
      같은 말이 된다(SPEC-008). 그러면 R4 의 "결정 표" 는 판 안에 없는 셈이다. 이 어긋남을
      감사할 자리다 (문구는 여전히 content/view 의 표에서 나오므로 문구 반전은 지켜진다)
    ③ 상한의 크기는 단언하지 않고 "24줄 이하 + 새 것이 남는다" 로만 쟀다
    ④ 나이 어법을 모르므로 글에서 숫자를 뽑아 그 안에 나이가 있는가로 쟀다 —
      원시 세계 시각을 그대로 적으면 통과하지 않는다
    ⑤ "비면 그 자리가 아예 없다" 를 `log === undefined` 로 읽었다 (빈 배열은 통과하지 않는다)
    ⑥ SPEC-007 경계는 규칙 줄이 **이름째** 사라지지 않으므로(R5 가 CHANGED 다)
      "시간이 흘러도 그 줄이 한 글자도 안 변한다" 로 쟀다

자동으로 놓을 수 없어 Human 실주행으로 넘긴 것 (T 의 it.todo 여섯 — 전부 조립·소켓 층이다)
    받아들여진 대답이 정말 안 쌓이는가 · 한 번 일어난 일이 한 줄인가 · 넘치면 미는가 ·
    재배열 사실이 기록에 한 줄로 남는가 · 뜨는 문구가 여전히 2.2초에 사라지는가 ·
    세계로 나간 패킷이 정말 0 인가

V 가 표현으로 결정한 것 (Human 감사)
    상한 다섯과 그 근거 셋 · 나이 어법 `{}초 전`(내림) · 나이를 적는 자리를 `agoText` 하나로 모아
    기록 줄과 규칙 줄이 갈릴 수 없게 한 것 · 재배열 나이를 새 줄이 아니라 `지금 길` 줄에 이어 붙인 것 ·
    판이 아예 없을 때(내 몸을 모를 때)는 기록만으로 판을 세우지 않은 것
```
