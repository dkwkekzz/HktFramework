# C021 — TODO

## Human 판정 대기 — Experience Verification

실행: `npm run dev`. 자리는 `HKT_SPAWN="-16,1"`(백왕령 산기슭) · `HKT_SPAWN_REGION=FROST_CANYON` 과
`HKT_SPAWN="0,14"`(빙결 심층 문), 때는 `HKT_CLOCK=STILL` · `SEEP` · `LONG_NIGHT`,
원천은 `HKT_SOURCE_PHASE="CLIFF_FROST_VEIN:recovering"` 으로 민다 (전부 vite.config.ts 의 검증용
손잡이 — 세계 규칙은 그대로다). 그림은 `shots/`. 판정 칸은 비워 둔다.

- [ ] **X-① 백왕령도 언제나 안전하지는 않다.**
  하기 — 산기슭 (−16, 1) 에 고요로 한 번, 스밈으로 한 번, 긴 밤으로 한 번 선다.
  보기 — 고요에는 「산맥이 막는다 · 강이 먹인다」이고, 스밈과 긴 밤에는 **산맥의 줄만**
  「산맥이 겨우 막는다」로 바뀐다. 강의 조건은 글자 그대로다.
  그림 — `shots/X-01-ridge-still.png` ↔ `shots/X-02-ridge-weak-seep.png` ↔ `shots/X-03-ridge-weak-long-night.png`
  판정 [ ]

- [ ] **X-② 약해진 것은 산맥 하나다.**
  하기 — 같은 스밈에 거목 곁 (−5, 0) 에 선다.
  보기 — 「거목이 포식자를 물린다」가 어느 철에도 그대로다. 자락 밖에서도 달라지는 것이 없다.
  그림 — `shots/X-04-tree-unchanged.png`
  판정 [ ]

- [ ] **X-③ 그 자락이 화면에서도 갈리는가.**
  보기 — 옅어진 산맥 자락이 서리가 앉은 것으로 읽혀야 한다(채움만 갈리고 윤곽은 산맥 조건의 것 그대로).
  **그림으로는 판정하기 어렵다** — 스밈의 하늘빛이 땅 전체를 어둡게 물들여 두 그림의 바닥색이
  철 때문에 이미 갈린다. 실주행에서 같은 철 안에 자락 안팎을 견주어야 한다.
  그림 — `shots/X-01-ridge-still.png` ↔ `shots/X-02-ridge-weak-seep.png`
  판정 [ ]

- [ ] **X-④ 긴 밤에만 빙결 심층의 문이 열린다.**
  하기 — 빙결 협곡 (0, 14) 에서 문을 지목하고, 고요와 긴 밤으로 각각 본다.
  보기 — 고요에는 **잠겨** 있고 사유가 「이 철이 아니다」이며, 긴 밤에는 **열려** 있다.
  표식의 요구(「저장된 열이 있어야 한다」)는 어느 철에도 그대로다.
  그림 — `shots/X-05-depth-door-locked.png` ↔ `shots/X-06-depth-door-open.png`
  판정 [ ]

- [ ] **X-⑤ 다시 자란 자리가 그것을 말한다.**
  하기 — 결정면을 다 캐 옆 면에 다시 선 뒤 지목한다.
  보기 — 「여기서 다시 자랐다」가 읽힌다. 어디서 옮겨 왔는지도 몇 번째 마디인지도 없다.
  그림 — `shots/X-07-frost-vein-regrown.png`
  판정 [ ]

- [ ] **X-⑥ 두 Region 이 한 보고에 나란히 선다.**
  하기 — `npm run world:observe -- --report`.
  보기 — 방마다의 절에서 협곡 둘이 나란히 읽힌다. 같은 고립 조건 아래 얼음 협곡은 원천 하나
  (baseline · phenomenon), 빙결 협곡은 셋(risk · conditional · by-product / terrain · atmosphere ·
  residue), 백왕령은 **원천 0** 이다.
  판정 [ ]

- [ ] **X-⑦ Play Goal 실주행** — 이 Cycle 로 **RoomOfAnotherKind 가 닫힌다.**
  하기 — 백왕령에서 서쪽 고개를 넘어 얼음 협곡 → 빙결 협곡까지 가고, 빙정석을 캐고, 결정면을
  깨고, 빙결 심층의 문에서 요구를 읽고, 스밈에 백왕령으로 돌아온다.
  보기 — Play §4 Breath 여덟 마디(자신감 → 낯섦 → 눈멂 → 미끄러짐 → 닿음 → 결핍 → 되돌아봄 →
  갈림)가 한 번의 주행에서 이어지는가. 마지막에 **"숲으로 갈까 협곡을 더 볼까, 무엇을 들고"** 가
  실제로 물음이 되는가.
  판정 [ ]

## 알려진 부채

```text
촬영의 expect 가 늘 떠 있는 판의 글자를 훑지 못한다 — 이 Cycle 의 네 expect 도 전부 "없음" 으로
        적혔는데 그림에는 그 줄들이 다 서 있다                C027 ① → C019 → C020 → C021
옅어진 자락의 **그림**을 촬영으로 판정할 수 없다 — 철이 하늘빛으로 땅 전체를 물들여 두 그림의
        바닥색이 이미 갈린다. 같은 철 안에 자락 안팎을 견주는 그림이 필요하다              C021
R3(다시 자란 자리)가 **원천이 밝힌 것에만** 실린다 — spec R3 의 문장은 "마디를 여럿 가진 원천"
        이라 두루 읽히는데 구현은 데이터로 가린다(`regrownCode`). 그래서 뒤척임이 옮긴 숲의
        노두에는 실리지 않는다. 코드 이름이 협곡의 것(`frost-vein-regrown`)이므로 그렇게 두었고,
        숲도 말하게 할지는 Human 이 정한다                                        C021 (Human 판정)
```

## 다음 Cycle 로

```text
세계가 모르는 방·없는 area·없는 이음을 가리킨 덧씌움이 아무 일도 하지 않는지 — 그런 데이터를
        세울 손잡이가 없어 "지금 밝힌 셋이 세계에 실제로 있다" 로 대신 쟀다 (it.todo)      C021
협곡을 지나는 것의 경로 · 열 결정이 어디서 나는가 · 요구를 실제로 채우는 것
        — 이 Play 밖이다 (3층 · 그 재료를 놓는 행 · RoomAsksForPossibilities)
```
