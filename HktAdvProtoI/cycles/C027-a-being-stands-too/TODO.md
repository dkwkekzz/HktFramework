# C027 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다.

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. **Alt + 클릭**이 지목이다 (그냥 클릭은 지금까지대로 이동·채굴·건너기).
Esc 로 푼다 — 판이 사라지지 않고 **내가 선 자리**로 돌아온다. 미로는
`HKT_SPAWN_REGION=FANTASY_MAZE HKT_SPAWN=-30,30 npm run dev`.
자동 시나리오(`content/view/tests/c027-a-being-stands-too.spec.ts` 45 + todo 6)는 전부 PASS —
`npm test` 1208 PASS / 15 todo (70 파일). 기준선은 1163 / 9 (69 파일) 였다. 그림은 `shots/`.

```text
Start   자리는 물으면 답하는데 존재는 아직 아니다. 세계의 사실이 여전히 좌상단에 선불돼 있다.
End     존재를 눌러도 같은 판이 답한다. 아무것도 안 누르면 판은 내가 선 자리를 말한다.
        배울 것이 하나다 — 답은 늘 같은 자리에 온다.
```

```text
X-①  존재의 제목이 사람이 읽을 말이다  ← **C026 부채 ② 가 여기서 닫힌다**
     하기   백왕령 북쪽 출구 표식을 Alt+클릭 한다 (`HKT_SPAWN=3,17`). 광맥도 눌러 본다
     보기   제목이 `길` · `돌 광맥` 이다 — `FOREST_PATH` 같은 세계의 코드가 제목 자리에 서지 않는가.
           이름이 없는 것은 **그 종류의 이름**으로 서는가
     그림   shots/X-02-designate-a-being.png · shots/X-03-designate-a-deposit.png
     판정   [ ]

X-②  그 존재가 어떤 상태인지 읽힌다
     하기   내 몸을 Alt+클릭 한다
     보기   `종류 토끼 검사 · 하는 일 대기 · 생명 200 / 200` 이 줄로 서고 생명에 막대가 붙는가.
           **광맥·출구 표식에는 생명 줄이 아예 없는가** (0 으로 지어내지 않는다)
     그림   shots/X-04-designate-myself.png
     판정   [ ]

X-③  그 대상이 주는 것과, 못 하는 사유가 그 자리에서 읽힌다  ← **이번 Cycle 에서 가장 중요한 판정**
     하기   멀리서 광맥을 Alt+클릭 한다. 그 다음 가까이 걸어가 다시 지목한다
     보기   `할 수 있는 것  채굴 · 너무 멀다 — 가까이 이동하자` 가 뜨고, 가까이 가면 사유가 사라지는가.
           출구 표식에는 `건너기` 가 같은 방식으로 서는가.
           **지금까지는 걸어가 눌러 봐야 알던 것을 지목만으로 아는가**
     그림   shots/X-03-designate-a-deposit.png · shots/X-02-designate-a-being.png
     판정   [ ]

X-④  지목은 유지되고, 사라지면 풀린다
     하기   존재를 지목한 채 걷는다 · Esc 를 누른다 · 방을 옮긴다
     보기   새로 지목할 때까지 대상이 그대로인가. Esc·방 이동이면 **내가 선 자리**로 돌아오는가.
           (쓰러진 몸은 풀리지 않는다 — 자동 시나리오가 잡는다. 눈으로 보려면 몸이 쓰러질 상황이 필요하다)
     판정   [ ]

X-⑤  지목이 없으면 판이 내가 선 자리를 진다
     하기   아무것도 지목하지 않은 채 걸어 다닌다. 미로 안에서도 해 본다
     보기   판이 `내가 선 자리 · 방 · 깊이 · 땅 · 통행` 을 지고, 걸으면 값이 따라 바뀌는가.
           미로에서는 `구역 A · 지금 길 DEFAULT · 압력 0 / 120` 이 함께 서는가.
           **백왕령에서 같은 것을 하면 구역·지금 길·압력 줄이 아예 없는가**
     그림   shots/X-01-standing-place.png · shots/X-05-maze-standing.png
     판정   [ ]

X-⑥  좌상단에는 내 몸만 남았다
     하기   깊이가 있는 방 · 조건 area 위 · 미로에서 좌상단 띠를 본다
     보기   깊이 · 안전한 이유 · 압력이 **띠에 없다.** 소지품 · 곡괭이 · 행동 · 세계 시간 · 함께만 남았는가.
           같은 사실이 띠와 판 두 자리에 동시에 적히지 않는가
     그림   shots/X-01-standing-place.png · shots/X-05-maze-standing.png
     판정   [ ]

X-⑦  판과 띠가 겹치지 않고, 화면이 지목하는 법을 말한다
     하기   창을 좁혔다 넓혔다 한다. 오른쪽 위 조작 안내를 읽는다
     보기   판(가운데 위)과 띠(왼쪽 위)가 어느 너비에서도 서로를 덮지 않는가.
           안내 첫 줄에 `지목: Alt + 클릭` 이 있는가  ← **C026 부채 ①③ 이 여기서 닫힌다**
     그림   shots/X-01-standing-place.png
     실측   1280 · 900 · 820 · 640 · 560 · 420 · 360 폭에서 두 상자의 좌표를 재어 겹침 0 (아래 부채 ① 참고)
     판정   [ ]
```

## 알려진 부채

```text
① 겹침 판정을 저장소가 스스로 재지 못한다   SPEC-008 은 브라우저 레이아웃의 사실이라 vitest 로
                                는 잴 수 없다(T 의 it.todo 둘). 이번에는 일회성 스크립트로
                                일곱 너비를 재어 겹침 0 을 확인했지만 **그 스크립트는 남기지
                                않았다** — 다시 겹치면 아무도 모른다. 상시로 재려면 판이 자기
                                너비를 계약(SceneTargetFrame)에 밝히거나, tools/world-editor 의
                                playwright 테스트와 같은 자리에 배치 검사를 하나 두어야 한다

② 촬영이 겨냥으로 존재를 집는다   C026 부채 ④ 그대로다. 이번에도 첫 겨냥이 출구 표식 대신
                                내 몸을 집어 자리를 옮겨 다시 찍었다. 도구가 "이 존재를 집는"
                                겨냥(존재 id 로 겨냥)을 지원하면 사라질 부채다

③ 존재가 쓰러지는 장면을 그림으로 못 잡았다   확정 8 의 "쓰러진 몸은 풀리지 않는다" 는
                                자동 시나리오(S-015)가 잡지만 그림이 없다. 자율 존재를 켜면
                                촬영 중 조작이 이어지지 않는다(npcs: none 의 이유) — 세계에
                                몸을 쓰러뜨리는 개발 명령이 서면 함께 찍을 수 있다
```

## 다음 Cycle 로

```text
C028 — 답이 남는다   거절 사유와 세계의 알림이 판의 기록으로 남는다(V6) + 규칙을 품은 방의
    State 가 그 기록과 함께 읽힌다. 2.2초 뒤 사라지던 것이 다시 읽히고 촬영도 그것을 잡는다.
    위 부채 ① 은 그 Cycle 이 판을 다시 만질 때 함께 보는 것이 싸다

spec 이 침묵해 **테스트가 판정 방식을 정한 자리** (Human 감사 — T 레인 보고)
    ① 줄을 가리키는 법 — spec 이 줄의 id·문구를 정하지 않아 "한 가지만 뺀 판과 견주어 남는 줄"로
      쟀다. 그러다 **SceneFrameRow.id 가 판 안에서 유일하지 않다**는 것이 드러났다(행동 줄이
      대상마다 여럿). 유일해야 하는가를 spec 이 말하지 않는다
    ② 생명 줄 — "얼마나 남았는지가 함께 보인다" 를 최대치 표기 **또는** 막대 중 하나로 받았다
    ③ "내 몸의 것" 다섯 — spec 이 한국어로만 말해 봉투의 hud id 다섯으로 옮겨 잡았다
    ④ 안전한 이유가 땅에서 유도된 것이 아님 — 조건 area 밖에서만 쟀다. **area 위에서 땅 유래
      태그가 새어 드는가는 재지 않았다** — spec 이 "걸린 것"과 "안전한 이유" 두 줄의 관계를
      말하지 않는다
    ⑤ 지목 안내 — 문구를 모르므로 "pointerRules 가 designate 로 읽는 그 입력의 이름이 안내
      한 줄에 적혔는가" 로 쟀다. 줄 수는 강요하지 않았다
    ⑥ 존재를 지목한 **동안** 깊이·안전한 이유·압력이 어디서 읽히는가 — SPEC-006 이 말하지 않아
      "띠에 없다" 만 쟀다. 판이 존재를 지고 있을 때 그 셋을 어디서 읽는지는 정해지지 않았다
    ⑦ 경계 대상 셋(이름도 종류도 모르는 것 · 아무것도 주지 않는 존재)은 `c027-test:` 접두로
      지어냈다 — spec 이 실물을 주지 않았다

V 가 표현으로 결정한 것 (Human 감사)
    존재 줄의 이름표 다섯(종류 · 하는 일 · 생명 · 쓰러짐 · 할 수 있는 것) ·
    이름이 곧 종류인 것에는 종류 줄을 세우지 않음(같은 말을 두 번 적지 않는다) ·
    행동 줄의 형식 `채굴 · 너무 멀다 — 가까이 이동하자` 와 그 id `being.offer:<interactionId>` ·
    기본 대상 판의 제목 "내가 선 자리" 와 곁제목(내 몸의 좌표) ·
    광맥 잔량(labelValue) · attended · profile 은 판에 세우지 않음 (몸 위에 이미 있거나 R1/R2 밖)

C026 이 옛 경계로 단언하던 자리를 새 계약으로 옮겼다 (Human 감사)
    "지목이 없으면 판이 없다"(C026 S-002 · S-003 · S-024) → "지목의 **표식**이 없다".
    판이 없는 경우는 이제 하나뿐이다 — 내 몸이 어디 있는지도 모를 때.
    깊이를 재던 자리(region.spec · region-c002 · region-c003 · c004) → 상시 HUD 에서 판의 줄로
```
