# C020 — TODO

## Human 판정 대기 — Experience Verification

실행: `npm run dev`. 협곡으로 바로 가려면 `HKT_SPAWN_REGION=FROST_CANYON npm run dev`(또는 `ICE_CANYON`),
자리는 `HKT_SPAWN="6,-15"`, 때는 `HKT_CLOCK=SEEP` · `LONG_NIGHT`, 원천의 State 는
`HKT_SOURCE_PHASE="CLIFF_FROST_VEIN:depleted"` 로 민다 (전부 vite.config.ts 의 검증용 손잡이 —
세계 규칙은 그대로다). 그림은 `shots/`. 판정 칸은 비워 둔다.

- [ ] **X-① 흔적이 색이 아니라 온도로 읽힌다.**
  보기 — 협곡 어느 자리에 서든 판의 「흔적」이 흙이 아니라 **숨**을 말한다. 원천 둘레가 방 바닥보다,
  빙결 협곡이 얼음 협곡보다 한 단계 짙다. 숲에 돌아가면 다시 흙의 말이다.
  그림 — `shots/X-01-pass-rime.png` ↔ `shots/X-09-forest-unchanged.png`
  판정 [ ]

- [ ] **X-② 원천 넷이 서로 다른 것으로 보인다.**
  하기 — 넷을 차례로 지목한다(Alt+클릭). 얼음 협곡 (8,−16) · 빙결 협곡 (8,−15) · (−6,6) · (−3,−10).
  보기 — 자연 형태가 넷 다 다르고 넷 다 푸르다. 「할 수 있는 것: 채취」가 붙는다.
  그림 — `shots/X-07-dust-named.png` (눈보라의 가루). `shots/X-02-pass-rime-named.png` 는
  **겨냥이 몸에 빗나가** 서리 대신 사람을 집었다 — 아래 부채를 함께 본다.
  판정 [ ]

- [ ] **X-③ 깨진 면이 결정화 위험으로 남는다.**
  하기 — 절벽의 결정면을 두 번 캔다 (또는 `HKT_SOURCE_PHASE="CLIFF_FROST_VEIN:depleted"`).
  보기 — 그 자리의 판에 「결정면이 땅을 뚫고 올라온다」와 「결정면에 닿아 있다」가 서고,
  「통행」은 여전히 **지날 수 있다**이다 (무너짐과 갈리는 자리다). 원천은 **옆 면**에 서 있다.
  그림 — `shots/X-03-frost-vein-intact.png` ↔ `shots/X-04-frost-vein-broken.png`
  판정 [ ]

- [ ] **X-④ 눈보라의 가루는 그 철에만 있다.**
  하기 — (−6,6) 에 고요로 한 번, 스밈으로 한 번 선다.
  보기 — 고요에는 그 자리에 **아무것도 없고**, 스밈에는 서 있다. 바닥난 것도 되돌아오는 중인 것도
  아니라 아직 그때가 아니었을 뿐이다.
  그림 — `shots/X-05-no-dust-in-still.png` ↔ `shots/X-06-dust-in-seep.png`
  판정 [ ]

- [ ] **X-⑤ 협곡이 요구하는 것은 협곡에 없다.**
  하기 — 빙결 협곡 북쪽 (0,18) 의 문을 지목한다.
  보기 — 「걸린 것: 저장된 열이 있어야 한다」. 문은 **열려 있고**(요구는 표시다) 건너면
  아직 짓지 않은 곳이라 거절된다. 협곡 어디에도 그 원천은 없다.
  그림 — `shots/X-08-depth-door-requirement.png`
  판정 [ ]

- [ ] **X-⑥ 긴 밤에 결정면이 두 배로 자란다.**
  하기 — 결정면을 다 캐고 `HKT_CLOCK=LONG_NIGHT` 와 `STILL` 에서 각각 기다린다.
  보기 — 긴 밤에는 90 세계 초, 다른 철에는 180 초에 돌아온다. 데이터의 길이(180)는 그대로다.
  그림 — 없다 (기다림은 그림에 담기지 않는다). 시나리오 S-061~064 가 증거다.
  판정 [ ]

- [ ] **X-⑦ 언 사체 곁의 결정은 거의 돌아오지 않는다.**
  보기 — 한 번 캐면 바닥나고 720 세계 초(하루의 두 배)가 지나야 돌아온다. 되돌리는 것은
  **시간이 아니라 그것이 다시 지나는 것**인데 그 경로가 아직 세계에 없다.
  판정 [ ] — 이 값이 옳은지, 아니면 경로가 설 때까지 아예 돌아오지 않아야 하는지는 Human 이 정한다.

- [ ] **X-⑧ 앞의 세계는 그대로다.**
  보기 — 숲의 흔적은 여전히 흙의 말과 색이고, C019 가 세운 협곡의 땅 · 눈보라 · 절벽은 그대로다.
  그림 — `shots/X-09-forest-unchanged.png`
  판정 [ ]

## 알려진 부채

```text
겨냥이 **화면 자리**로 집으므로 몸 뒤·몸 곁의 표식을 집지 못한다 — X-02 가 서리 대신 사람을
        집었다. 존재 id 로 겨냥할 수 없다는 그 부채의 두 번째 사례다   C026 ④ → C019 → C020
촬영의 expect 가 늘 떠 있는 판의 글자를 훑지 못한다 — 이 Cycle 의 네 expect 도 전부 "없음"
        으로 적혔는데 그림에는 그 줄들이 다 서 있다                    C027 ① → C019 → C020
spec 안에서 수와 문장이 어긋났다 — SPEC-008 은 "세계에서 가장 느리다" 인데 같은 spec 의
        데이터 값 표가 240 을 주었고 그것은 같은 표의 서리(360)보다 빠르다. 둘 다 Design 이
        아니라 기본형이므로 통합에서 720 으로 세워 문장의 뜻을 세계가 지키게 했다.
        **동결된 문서는 고치지 않았다** — 어긋난 채로 남아 있고 이 줄이 그 경위다      C020
데이터 자리의 이름이 spec 안에서 두 벌이었다 — State 절은 `depletedHazardOps`(op id)라 적고
        Reuse 절은 "형은 C019 의 HazardOverlay" 라 적었다. 구현은 형이 옳은 쪽을 따라
        `depletedHazards` 로 두었다                                                    C020
검사 ㉓ 가 철에 매이지 않은 위상(깨어남 · 상시 · **깨진 마디**)을 보지 않는다 — 도구가
        phases.seasons 만 훑는다. 협곡의 덧씌움이 이제 열이 넘는데 검사에 잡히지 않는다
                                                                     C017 → C019 → C020 → tools
협곡에 철 · 소란 · 지나가는 것의 덧씌움이 하나도 없어, 깨진 마디의 자락이 그 셋과 **함께**
        걸리는지는 상시 위상과의 겹침으로만 쟀다 (it.todo)                              C020
```

## 다음 Cycle 로

```text
협곡을 지나는 것(체온을 감지하는 포식자)의 경로 — 언 사체 곁의 결정을 되돌리는 것은 시간이
        아니라 그것이다. 정식 이름이 이름 표에 없어 이 Cycle 이 짓지 않았다      3층 · 생명
철이 두 Region 사이에 선다 · 빙결 심층 문의 긴 밤 활성 · 두 Region 나란한 보고        C021
열을 저장하는 결정이 어디서 나는가 — 확정 4 가 정하지 않았다. 그 재료를 놓는 행이 답한다
요구를 실제로 **채우는** 것 (성질 Lock · 어휘 · 검사 ㉞~㊷)          RoomAsksForPossibilities
```
