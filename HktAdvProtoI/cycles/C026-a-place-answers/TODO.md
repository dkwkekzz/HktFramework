# C026 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다.

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. **Alt + 클릭**이 지목이다 (그냥 클릭은 지금까지대로 이동·채굴·건너기).
Esc 로 푼다. 미로는 `HKT_SPAWN_REGION=FANTASY_MAZE HKT_SPAWN=-30,30 npm run dev`.
자동 시나리오(`content/view/tests/c026-a-place-answers.spec.ts` 34 + todo 5)는 전부 PASS —
`npm test` 1163 PASS / 9 todo (69 파일). 기준선은 1104 / 4 (65 파일) 였다. 그림은 `shots/`.

```text
Start   화면이 이미 다 적어 두었다. 읽을 것은 많고 궁금할 것은 없다.
End     화면은 조용하다. 저 바닥만 색이 다르다 — 눌러 본다. 답이 즉시 뜬다.
```

```text
X-①  화면이 조용해졌다
     하기   백왕령에 들어선다. 방 이름이 지나가는 것을 본다
     보기   세계 위에 글자가 **하나도 없다.** 조건 셋과 도시는 색과 경계로만 갈린다.
           C006 의 `shots/X-05-safe-by.png`(글자 다섯)와 나란히 놓고 본다 — 그 다섯이 0 이 되었다.
           방 이름은 들어선 순간 한 번 지나가고 사라지는가
     그림   shots/X-01-quiet-ground.png
     판정   [ ]

X-②  지목하면 판이 선다
     하기   발밑 앞의 땅을 Alt+클릭 한다
     보기   화면 **중앙 상단**에 판이 서고 그 자리에 흰 고리가 앉는다.
           방 · 깊이 · 땅 · 통행이 줄로 읽힌다 (실측: `지목한 자리 0.0, 19.4 · 백왕령 · 문명권 · 비탈 · 지날 수 있다`).
           **기다림이 없는가** — 누른 그 순간에 찼는가 (세계로 나가는 것이 없으므로 왕복이 없다)
     그림   shots/X-02-designate-a-place.png · shots/X-03-another-place.png
     판정   [ ]

X-③  걸어가기 전에 안다  ← **이번 Cycle 에서 가장 중요한 판정**
     하기   강 쪽 물을 Alt+클릭 한다. 그 다음 실제로 그리로 걸어가 본다
     보기   판이 `땅 젖은 땅 · 통행 지날 수 없다 · 막는 것 물이 너무 깊다` 를 **미리** 말한다.
           걸어가면 같은 사유로 거절된다 — 두 답이 다르지 않은가
     그림   shots/X-04-blocked-before-walking.png · shots/X-04b-another-blocked.png
     판정   [ ]

X-④  규칙을 품은 방은 그 State 도 말한다
     하기   미로 안의 바닥을 Alt+클릭 한다
     보기   `구역 A · 지금 길 DEFAULT · 압력 0 / 120` 이 함께 실린다.
           통로 위를 누르면 통로의 열림/닫힘이 한 줄 더 선다.
           **백왕령에서 같은 것을 하면 그 줄들이 아예 없는가** (0 으로 지어내지 않는다)
     그림   shots/X-06-maze-place.png · shots/X-05-maze-quiet.png
     판정   [ ]

X-⑤  판은 판일 뿐이다
     하기   판이 선 채로 WASD 로 걷고 시점을 돌린다. Esc 로 푼다
     보기   몸이 움직이고 시점이 돈다 — 판이 자판을 붙잡지 않는가.
           Esc 로 판이 사라지고, 방을 옮겨도 사라지는가
     판정   [ ]

X-⑥  클릭이 하던 일은 그대로다
     하기   Alt 없이 빈 땅 · 광맥 · 출구 표식을 클릭한다
     보기   이동 · 채굴 · 건너기가 C008 까지와 똑같이 되는가.
           클릭의 뜻이 코드에서 정책으로 옮겨졌지만 결과는 한 글자도 다르지 않아야 한다
     판정   [ ]

X-⑦  Intent — 세계가 늘지 않았는가
     하기   `git diff 20aec7c..HEAD -- content/world content/protocol` 이 비어 있는지 본다
     보기   비어 있다. WorldState 도 봉투도 STATE_VERSION 도 한 자리 늘지 않았다.
           그런데도 화면이 전보다 **더** 말한다 — 이 Cycle 의 주장이 그것이다
     판정   [ ]
```

## 알려진 부채

```text
① 판이 좌상단 상시 HUD 를 가린다   판은 화면 중앙 상단이고 HUD 판은 좌상단인데 가로로 넓어
                                서로 겹친다 (그림 전부에서 `행동: 대기` 와 `깊이:` 가 반쯤 덮인다).
                                C027 이 상시 HUD 를 내 몸의 상태로 좁히면 저절로 줄어들 자리이지만,
                                그때도 겹치면 판의 자리나 너비를 정해야 한다

② 존재를 지목하면 제목이 원시 id   `MAZE_GATE_RETURN` 처럼 세계의 코드가 그대로 뜬다
                                (shots 의 첫 촬영에서 잡혔다). 존재 지목은 C027 의 것이라
                                이 Cycle 은 제목만 두었고 이름 표를 만들지 않았다 — C027 이 닫는다.
                                모르는 코드를 코드 그대로 두는 것은 이 저장소의 폴백 규칙이지만,
                                **제목** 자리에 그것이 서는 것은 다시 볼 자리다

③ Alt+클릭이 지목이라는 것을 화면이 말하지 않는다   조작 안내(keyHints)에 줄이 없다.
                                기반에 그 자리가 있으므로(SceneState.keyHints) 줄 하나면 되지만,
                                spec 이 정하지 않은 것이라 지어내지 않았다. C027 의 일감

④ 촬영이 겨냥으로 존재를 집는다     Alt+클릭의 자리를 화면 비율로 주므로 몸·출구 표식이 그 자리에
                                서 있으면 자리가 아니라 존재가 잡힌다 (첫 두 번의 촬영이 그랬다).
                                지금은 겨냥을 옮겨 피했다 — 도구가 "존재를 피해 바닥을 집는" 겨냥을
                                지원하면 사라질 부채다
```

## 다음 Cycle 로

```text
C027 — 존재도 같은 자리에 선다   존재 지목(이름 표 · 상태 줄) · 그 대상이 주는 행동과 불가 사유 ·
    상시 HUD 를 내 몸으로 좁히고 깊이·안전한 이유·압력을 판으로 옮긴다(V5).
    위 부채 ①②③ 이 전부 여기서 닫힌다

spec 이 침묵해 **테스트가 판정 방식을 정한 자리** (Human 감사 — T 레인 보고)
    ① 줄의 식별 — 줄 id·label 어법을 spec 이 정하지 않아 "그 의미 코드의 codeText 가 글에 실렸는가"로 쟀다
    ② "지날 수 있는가" — 긍정 문구를 강요하지 않고 막힘 사유의 있음/없음으로 쟀다
    ③ **통로 태그의 이름(AB)을 적을 것인가** — 구현은 열림 여부만 적고 이름은 안 적는다.
      구역은 이름(A)을 적으므로 둘이 어긋나 있다. spec 이 정하지 않았다
    ④ 압력 — 숫자 표기든 progress 든 하나면 통과로 보았다
    ⑤ 모르는 코드 — 봉투로 미등록 코드를 주입할 길이 없어 codeText 를 직접 불러 쟀다
    ⑥ 지목의 입력 — spec UNRESOLVED 였다. 후보 일곱을 늘어놓고 그중 하나가 designate 로 읽히는지만 보았다.
      **Alt 로 정한 것은 표현·입력의 결정**이며 근거는 pointer-rules.ts 의 DESIGNATE_MODIFIER 주석에 있다

자동으로 놓을 수 없어 Human 실주행으로 넘긴 것 (T 의 it.todo 다섯)
    ① 세계로 나간 패킷이 정말 0 인가 (소켓이 필요 — 여기서는 봉투 불변으로만 쟀다)
    ② 정책 없이 attachInput 을 걸면 요청이 안 나가는가 (브라우저 클릭 이벤트가 필요)
    ③ Esc 가 판을 푸는가 (조립 + 실제 키 입력)
    ④⑤ 방 이름 진입 제목 (진입 제목은 SceneState 에 실리지 않는다 — 조립이 이전 방 id 를 쥔다)
```
