# C019 — TODO

## Human 판정 대기 — Experience Verification

실행: `npm run dev` → 브라우저. 협곡으로 바로 가려면 `HKT_SPAWN_REGION=ICE_CANYON npm run dev`
(또는 `FROST_CANYON`)이고 자리는 `HKT_SPAWN="0,-10"`, 때는 `HKT_CLOCK=LONG_NIGHT` 로 민다
(전부 vite.config.ts 의 검증용 손잡이 — 세계 규칙은 그대로다).
그림은 `shots/`. 판정 칸은 비워 둔다.

- [ ] **X-① 고개를 넘는다.**
  하기 — 백왕령에서 서쪽 고개(−18, 0)로 걸어가 `[Q] 건너기`.
  보기 — "아직 갈 수 없는 곳" 이라는 거절이 나지 않고 몸이 **얼음 협곡**에 선다. 같은 고개로 백왕령에 돌아온다.
  그림 — 없다 (촬영 하네스에서 자판 걸음이 몸을 옮기지 못한다 · C003 부터의 부채). 시나리오 S-011~014 가 잰다.
  판정 [ ]

- [ ] **X-② 고개 너머는 다른 문법이다.**
  보기 — 골 바닥이 서리로 깔리고(판의 「땅」이 `frost`) 양옆이 얼음 절벽이다. 숲·백왕령 어느 바닥과도 색이 다르다.
  그림 — `shots/X-01-ice-canyon-floor.png` · `shots/ICE_CANYON.surface.png`(가운데 띠가 서리 697칸 · 양옆이 급경사 810칸)
  판정 [ ]

- [ ] **X-③ 절벽이 몸을 세운다.**
  하기 — 골에서 서쪽으로 걸어 벽에 붙는다 (−8, 0 언저리).
  보기 — 판의 「걸린 것」에 **절벽이 길을 끊는다**. 더 가려 하면 "너무 가파르다" 로 막힌다.
  그림 — `shots/X-02-cliff-edge.png` · `shots/ICE_CANYON.traversable.png`
  판정 [ ]

- [ ] **X-④ 눈보라가 낮에도 시야를 좁힌다.**
  하기 — 빙결 협곡 들머리(0, −18)에 서서 방 저편을 본 뒤, 북으로 세 걸음(0, −10)을 걷는다.
  보기 — 들머리에서는 걸린 것이 없고 먼 것이 실린다. 세 걸음 안에서 판에 **눈보라가 여기를 깎는다**가 서고
  20 보다 먼 몸과 원천이 실리지 않는다. 자락 밖으로 나오면 다시 실린다.
  그림 — `shots/X-03-outside-blizzard.png` ↔ `shots/X-04-inside-blizzard-day.png` (나란히 본다) ·
  `shots/FROST_CANYON.semantic.png`(들머리가 자락 밖이라는 것)
  판정 [ ]

- [ ] **X-⑤ 긴 밤의 눈보라는 그 절반의 절반이다.**
  하기 — 같은 자리에서 `HKT_CLOCK=LONG_NIGHT`.
  보기 — 낮에 보이던 가까운 몸까지 사라진다 (낮 20 · 밤 10).
  그림 — `shots/X-05-inside-blizzard-night.png`
  판정 [ ]

- [ ] **X-⑥ 결정면이 닿음을 말한다.**
  하기 — 빙결 협곡의 (0, 10)에 서고, 다시 (0, 5)로 물러난다.
  보기 — 위에서는 「눈보라가 여기를 깎는다 · 결정면이 땅을 뚫고 올라온다 · **결정면에 닿아 있다**」 셋이 함께 서고,
  물러나면 뒤의 두 줄만 사라진다. 몸의 값(HP · CP · 속도)은 한 톨도 달라지지 않는다.
  그림 — `shots/X-06-crystal-face.png` ↔ `shots/X-07-off-crystal-face.png`
  판정 [ ]

- [ ] **X-⑦ 안쪽은 한 단계 더 깊다.**
  하기 — 얼음 협곡의 (0, 18)에서 오솔길로 한 번 더 건넌다.
  보기 — 방이 **빙결 협곡**이고 깊이가 "아무도 돌보지 않는 야생"으로 읽힌다(방 바닥 색이 한 단계 차다).
  나가는 끝은 들어온 그 하나뿐이다.
  판정 [ ]

- [ ] **X-⑧ 앞의 세계는 그대로다.**
  보기 — 백왕령의 강 · 능선 · 다리 · 안전의 조건 셋이 이 Cycle 전 그대로다. 서리 규칙 한 줄이 늘었어도
  이 방의 바닥 태그는 한 값도 달라지지 않는다.
  그림 — `shots/X-08-white-king-unchanged.png`
  판정 [ ]

- [ ] **X-⑨ 협곡의 모양이 게임 화면에서 읽히는가.**
  보기 — 골 안에 서면 20~24 높이의 벽이 화면을 채워, 그림 다섯 장 어디에서도 "협곡" 이 한눈에 읽히지 않는다.
  땅의 모양은 `world:observe` 의 그림에서만 읽힌다. 시점(3층·표현)의 일인지, 벽을 낮출 일인지 Human 이 정한다.
  그림 — `shots/X-01-ice-canyon-floor.png` ↔ `shots/ICE_CANYON.surface.png`
  판정 [ ]

## 알려진 부채

```text
촬영의 expect 가 **늘 떠 있는 판의 글자를 훑지 못한다** — 여덟 run 의 expect 가 전부 "없음" 으로
        적혔는데 그림에는 그 줄들이 다 서 있다 (X-06 에 셋이 함께 선다). 훑는 것이 HUD 뿐이다.
        판이 자기 크기를 계약에 밝히지 않는 것(C027 ① → C028 ②)과 같은 뿌리다        C019 → 촬영 하네스
npcRegion 으로 놓은 자율 존재가 협곡 그림에 보이지 않는다 — 그래서 "먼 몸이 사라진다" 를
        그림으로 잡지 못했다 (판의 코드로만 잡았다). 그 방의 순회 경로가 백왕령의 자리라
        절벽 위에 서는 것도 함께 본다                                              C019
검사 ㉓ 가 **상시 위상을 보지 않는다** — tools/world-editor/check.ts 가 phases.seasons 만
        훑는다 (C017 의 awake 도 같은 상태였으므로 새 부채는 아니고, 협곡의 덧씌움 여섯이
        검사에 잡히지 않는다)                                        C017 → C019 → tools
표면 규칙 표에 줄이 늘면 **컴파일 결과의 hash**(CompiledRegion.hash)가 모든 방에서 바뀐다.
        관찰 봉투의 region.hash 는 descriptionHash 뿐이라 그대로이고, 그 값을 견주는 코드도
        테스트도 없다 — C006 이 젖음 줄을 더했을 때와 같은 일이다. 기록만 남긴다        C006 → C019
```

## 다음 Cycle 로

```text
눈보라가 **원천**도 자르는지 재지 못했다 — 협곡에 아직 원천이 없다 (it.todo)          C020
협곡을 지나는 것이 없어 "지나는 것은 잘리지 않는다" 를 놓을 자리가 없다 (it.todo)      C021 이후
자락 대 자락의 "가장 좁은 것이 이긴다" 가 실측되지 않았다 — 관찰 범위를 밝힌 자락이
        지금 눈보라 하나뿐이다 (때 대 자락은 실측했다)                                C020 이후
빙결 협곡의 phases 에 seasons·awake 가 없어 상시와 철의 **합집합**이 아직 돌지 않는다  C021
빙정석 계통 · 협곡의 Isolation/Outflow · 요구가 협곡에 없다는 문                       C020 · C021
```
