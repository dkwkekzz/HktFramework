# C006 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다
(`spec.md` 만 남은 디렉터리가 닫힌 Cycle 이다).

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. 시점 올리기는 `R`(내리기 `T`), 돌기는 `Z`/`X`, 걷기는 `WASD`.
자동 시나리오(`content/world/tests/c006-land-blocks-and-flows.spec.ts` 44 + todo 2 ·
`content/view/tests/c006-terrain-view.spec.ts` 11 · engine 366)는 전부 PASS —
`npm test` 1010 PASS / 4 todo. 그림은 `shots/`.

```text
Start   북쪽은 솟았지만 아무것도 막지 않는다. 어디로든 걸어갈 수 있는 색칠된 방이다.
End     못 넘는 산과 못 건너는 물이 생겼다. 건널 자리는 하나뿐이고, 거목 아래 도시가 있다.
        땅이 처음으로 몸에 닿았고, 화면이 "안전한 이유" 를 말한다.
```

```text
X-①  급경사가 몸을 세운다
     하기   백왕령 북서쪽 능선으로 계속 걸어 올라간다
     보기   비탈(갈색)까지는 오르고 급경사(회색) 앞에서 선다. 세계의 대답 "너무 가파르다" 가 뜬다.
           C005 에서는 색일 뿐이던 것이 이제 몸을 세우는가
     그림   shots/X-03-steep-blocks.png  (문구가 몸 위에 떠 있다)
     판정   [ ]

X-②  강이 방을 가른다
     하기   몸이 놓인 자리에서 북쪽을 본다 (R 로 지평선을 세우고 Z 로 반 바퀴)
     보기   파인 물길과 젖은 띠(청록)가 평지(초록)에서 갈린다. 방을 동서로 가로질러
           **남북을 가르는가**. 색이 밝기가 아니라 계열로 갈리는가
     그림   shots/X-01-river-crosses.png · shots/X-04-bridge-crossing.png (발밑이 젖은 띠)
     판정   [ ]

X-③  물이 막는다
     하기   강으로 곧장 걸어 들어간다
     보기   물가에서 선다. "물이 너무 깊다" 가 뜨고 몸은 그 자리에 있다.
           급경사와 **다른 문구**인가 — 무엇이 막았는지가 읽히는가
     그림   shots/X-02-water-blocks.png  (문구는 아래 부채 ① 참고 — 그림에는 안 잡혔다)
     판정   [ ]

X-④  건너는 자리는 하나다
     하기   물가를 따라 동쪽으로 걸어 다리(8, 7)를 찾아 건넌다. 그 다음 다른 자리에서 건너 본다
     보기   다리에서는 같은 강이 열려 있고 거절 문구가 뜨지 않는다. 다른 자리에서는 전부 막힌다.
           **다리를 찾는 일이 걸을 만한가** — 봉투는 다리의 자리를 알려주지 않는다
     그림   shots/X-04-bridge-crossing.png
     판정   [ ]

X-⑤  안전한 이유가 화면에 뜬다
     하기   도시 언저리(−11, 2 부근)에 선다. 그 다음 조건 밖으로 걸어 나간다
     보기   HUD 에 "안전한 이유: 산맥이 막는다 · 강이 먹인다" 가 뜨고, 겹친 자리에서는 **둘 다** 뜬다.
           밖으로 나가면 줄이 사라진다. "안전해서 도시가 선 것이 아니라 조건이 있어서 섰다"(W2)로 읽히는가
     그림   shots/X-05-safe-by.png
     판정   [ ]

X-⑥  백색 거목이 땅에 선다
     하기   도시 곁(−5, 0)의 거목을 여러 자리에서 본다
     보기   밑동이 땅에 닿아 있고 몸보다 훨씬 크다. 도시 곁의 표식으로 읽히는가 —
           **아니면 시야를 막는 흰 판으로 보이는가** (아래 부채 ② 가 이 자리다)
     그림   shots/X-06-white-giant-tree.png · shots/X-01-river-crosses.png (가까이서 본 모습)
     판정   [ ]

X-⑦  조건과 도시의 테두리
     하기   도시 언저리에서 발밑의 구역 테두리를 본다
     보기   조건 셋(산기슭 회색 · 강가 청록 · 거목 흰색)과 도시(따뜻한 인공색)가 갈리는가.
           이름표 넷이 겹치던 자리다 — 이제 겹치면 위로 쌓이고 잘리지 않는다 (고쳐졌다)
     그림   shots/X-05-safe-by.png
     판정   [ ]

X-⑧  데이터가 없는 방은 아무것도 막지 않는다
     하기   숲 가장자리로 건너가 사방으로 걷는다
     보기   평평하고 색도 하나이며 거절이 없다. 걸음이 C005 까지와 같은가
     그림   shots/X-07-flat-room.png
     판정   [ ]

X-⑨  Intent — 땅이 여전히 데이터인가
     하기   `content/regions/white-king-domain.ts` 의 C006 op 일곱을 열어 본다.
           강의 점 하나를 옮기거나 다리를 지워 보고 다시 띄운다
     보기   강을 굽히고 다리를 옮기는 것이 그 파일 하나인가.
           **다리를 지우면 강 북쪽으로 갈 수 없게 되는가** (시나리오 S-014 가 그것을 잰다).
           규칙 표(`content/regions/terrain-rules.ts`)의 값 하나로 임계·젖음 폭이 바뀌는가
     판정   [ ]
```

## 알려진 부채

```text
① 거절 문구를 촬영이 잡지 못한다        X-③ 의 "물이 너무 깊다" 가 expect 에 안 잡혔다. 문구는 몸 위의
                                    토스트라 HUD 글자 훑기에 걸리지 않고, 걸음이 물가에 닿는 시각도
                                    프레임 속도에 걸린다. 같은 문구가 X-① 의 그림(X-03)에는 찍혀 있으므로
                                    기구가 아니라 촬영의 한계다 — 도구가 토스트를 읽게 하는 것은 C007

② 거목이 시야를 막는다                 높이 17 의 sprite 하나가 가까이서는 흰 판으로 보인다 (X-01).
                                    표식이 아니라 벽으로 읽히면 값(높이)이나 그림을 손볼 자리다 —
                                    둘 다 데이터이고 코드가 아니다 (terrain-presentation · sprites)


③ 이동 진행은 traversable 을 보지 않는다  막는 것은 요청 판정(RULE-MOVE-001)뿐이다. 클라이언트의 걸음이
                                    1.6m 앞을 요청하므로 실제로는 물가에서 서지만, 한 번에 먼 자리를
                                    요청하면(명령·다른 클라이언트) 몸이 막힌 칸 위를 지나간다.
                                    Play 는 "이동 규칙이 목표 자리의 traversable 을 읽는다" 까지만 요구했다

④ 컴파일을 켤 때마다 한다              C005 에서 이어진다. 이제 세계도 컴파일하므로 두 번 한다
                                    (세계 한 번 · 관찰자 한 번). 굽는 도구 world:compile 은 C007
```

## 다음 Cycle 로

```text
world:observe 넷과 보고 (C007)
    이 Cycle 은 traversable 과 areas 를 만들었지만 그것을 **한 장으로 보는 길**이 없다.
    통행 지도·의미 지도가 C007 의 것이고, 검사 ③(settlement 가 condition 없이 섰는가)은
    이 Cycle 이 만든 `condition:` 접두사를 읽는다

시나리오가 피해 간 자리 (T 보고)
    "Description 을 모르는 region id" 를 하네스로 놓을 수 없다 — createWorld 가 던지고
    frontier 로 건너기는 region-not-built 로 막힌다 (SPEC-009 경계 · it.todo)
    HUD 문구는 세계 테스트로 잴 수 없어 view spec 으로 넘겼다 (SPEC-007 · it.todo)

Human 감사 — 데이터로 둔 값들
    강 폭 8 · 깊이 1.5 · 다리 반경 5 · 젖음 폭 6 · 조건 원의 반경 4/3.5 · 거목 높이 17.
    전부 spec 의 UNRESOLVED 절이 "데이터다" 라고 적은 것이고, 근거는 각 op 의 주석에 실측으로 있다.
    걸어 보고 강이 너무 넓거나 다리가 너무 쉽게 찾아지면 그 값 하나를 바꾼다
```
