# C005 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다
(`spec.md` 만 남은 디렉터리가 닫힌 Cycle 이다).

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. 시점 내리기는 `T`, 걷기는 방향키.
자동 시나리오(`content/world/tests/c005-land-rises.spec.ts` 48 ·
`content/view/tests/c005-terrain-view.spec.ts` 10 · engine 컴파일러·terrain 테스트)는 전부 PASS 다.
그림은 `shots/`.

```text
Start   백왕령은 색칠된 평평한 바닥이다. 왜 안전한지는 이름표가 말할 뿐이다.
End     북쪽이 솟았다. 올라가 보면 발밑 색이 바뀐다. 땅이 생겼는데 규칙은 하나도 늘지 않았다.
```

```text
X-①  땅이 솟았다
     하기   백왕령에서 시점을 내려 북쪽을 본다
     보기   평평하던 방의 북서쪽이 능선으로 솟아 있다. 하늘을 배경으로 윤곽이 선다.
           HUD 는 "깊이: 문명권" 그대로다 — 땅이 생겼다고 세계의 대답이 바뀌지는 않았다
     그림   shots/X-01-ridge-from-plain.png
     판정   [ ]

X-②  발밑 색이 경사로 갈린다
     하기   능선 쪽으로 걸어 올라간다
     보기   초록(평지) → 갈색(비탈) → 회색(급경사) 셋이 갈린다. 밝기가 아니라 색으로 갈리는가.
           "기울수록 흙과 풀이 붙어 있지 못한다" 로 읽히는가 — 풀 · 벗겨진 흙 · 맨 바위
     그림   shots/X-02-on-the-ridge.png
     판정   [ ]

X-③  급경사는 아직 막지 않는다
     하기   가장 가파른 자리(회색)로 계속 올라간다
     보기   막히지 않는다. 거절 문구도 뜨지 않는다. 능선 위에 올라설 수 있다.
           **이것이 이번 Cycle 의 의도다** — 막는 것은 C006 이다
     그림   shots/X-02-on-the-ridge.png
     판정   [ ]

X-④  몸과 표식이 땅에 붙어 있다
     하기   능선을 오르내리며 몸과 출구 표식·광맥을 본다
     보기   몸이 사면에 붙어 있다(뜨지도 파묻히지도 않는다). 출구 표식도 산기슭의 땅 높이에
           함께 올라가 있다. 숲으로 가는 문은 파묻히지 않았다 — 걸어갈 수 있는 비탈이다
     그림   shots/X-03-forest-path-foot.png
     판정   [ ]

X-⑤  데이터가 없는 방은 평평하다
     하기   북쪽 문으로 숲 가장자리에 건너간다
     보기   완전한 평면이고 색도 하나다. 같은 코드가 데이터 없는 방에서 평면을 그린다
     그림   shots/X-04-flat-room.png
     판정   [ ]

X-⑥  바닥 면이 지면을 따라간다
     하기   능선 기슭 근처에서 방 바닥의 반투명 면을 본다
     보기   면이 지면에 붙어 있다. 기울어진 판으로 떠서 지평선을 가로지르지 않는다
           (이 자리가 원래 떠 있던 자리다 — 이번에 engine 에서 고쳤다)
     그림   shots/X-06-zone-fill-drapes.png
     판정   [ ]

X-⑦  Intent — 땅이 데이터로 생겼는가
     하기   `content/regions/white-king-domain.ts` 의 stamp op 한 덩이를 열어 본다.
           값을 바꿔 보고(중심·반경·높이·falloff) 다시 띄운다
     보기   그 한 덩이가 이 땅의 전부인가. 값을 바꾸면 땅이 바뀌는가.
           **`engine` · `content/world` · `content/protocol` 은 한 줄도 바뀌지 않았다** —
           `git show --stat` 으로 확인할 수 있다. 그것이 "방이 땅이 된다" 의 뜻인가
     판정   [ ]
```

## 알려진 부채

```text
80×80 방의 바닥 채움이 조금 굵다      채움 삼각형 상한(20000)에 걸려 눈금이 1.77 로 굵어진다.
                                  지면에서 뜬 거리는 0.020 이라 눈에 띄지 않지만, 방이 더 커지면
                                  이 수가 커진다. 큰 Region 이 실제로 오면 그때 chunk 처럼 나눈다
컴파일을 켤 때마다 한다              *.compiled.generated.ts 로 굽는 도구(world:compile)가 C007 의 것이다.
                                  지금은 방마다 한 번 컴파일해 두고 다시 쓴다 — 방 아홉이 그 정도 크기다
방 바닥이 지형 굴곡에 묻힌다(C001)    이 Cycle 이 절반을 갚았다 — 면이 지면을 따라가게 됐다.
                                  남은 것은 평지에서도 면이 살짝 떠 보이는 것(ZONE_LIFT)이고 의도된 값이다
```

## 다음 Cycle 로

```text
급경사가 몸을 막는 것 (C006)
    이 Cycle 은 45° 이상을 색으로만 냈다. traversable 격자와 이동 거절은 C006 이다.
    지금 데이터에서 45° 이상은 vertex 의 5.0% 이고 전부 능선 꼭대기 쪽이다 —
    숲으로 가는 문(FOREST_PATH) 앞은 26° 라 C006 이 막아도 길은 열려 있다
    (그렇게 되도록 능선을 서쪽으로 두었다)

세계는 아직 땅을 모른다
    WorldState 에 terrain 이 없고 규칙 중 무엇도 높이를 읽지 않는다. C006 이 traversable 을
    읽어야 하는 순간, "world 와 view 가 같은 규칙 표를 읽는가"(그래야 hash 가 같다)가
    처음 문제가 된다 — 지금 규칙 표는 content/view 에 있다 (L2-World-Tool §4 의 파일 지도 그대로)

표면 임계 15° 는 근거가 약하다 (Human 감사)
    급경사 45° 는 확정 1 이 준 값이지만 평지/비탈을 가르는 각은 어느 문서에도 없어 45°의 1/3 로 두었다.
    지금 분포는 평지 75.8% · 비탈 19.2% · 급경사 5.0% 다. 눈으로 보고 비탈이 너무 넓거나 좁으면
    biome-rules 의 한 줄로 바꾼다 — 코드가 아니다
```
