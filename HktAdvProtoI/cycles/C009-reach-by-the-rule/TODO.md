# C009 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다.

## Human 판정 대기 — Experience Verification

`npm run dev` 로 띄운다. 미로는 백왕령 → 숲 가장자리 → 숲 안쪽 → **고대 문**(Q)으로 들어가고,
심장 쪽 문은 구역 B 의 (30, 30) 이다. 바로 보려면
`HKT_SPAWN_REGION=FANTASY_MAZE HKT_SPAWN=30,30 npm run dev`.
그 문이 열린 상태에서 보려면 `HKT_REGION_PATTERN=FANTASY_MAZE:P2` 를 함께 준다 (아래 부채 ⑥).
자동 시나리오(`content/world/tests/c009-reach-by-the-rule.spec.ts` 25)는 전부 PASS —
`npm test` 1129 PASS / 5 todo. 그림은 `shots/`.

```text
Start   길이 바뀐다는 것은 알았다. 그런데 심장 쪽 문은 잠겨 있고, 걸어도 걸어도 잠겨 있다.
End     문이 열리는 것은 내가 거기 있을 때가 아니라 패턴이 P2 일 때다 —
        규칙을 알면 문을 열 수 있다.
```

```text
X-①  심장 쪽 문이 잠겨 있다
     하기   구역 B 의 (30, 30) 곁에 선다
     보기   몸 오른쪽에 **채워진 자물쇠** 표식이 있다. 미로에 처음 생긴 잠긴 문이다
           (C004 가 고대 문을 연 뒤로 이 세계에 잠긴 표식은 하나도 없었다)
     그림   shots/X-01-heart-gate-locked.png
     판정   [ ]

X-②  잠긴 문의 대답
     하기   그 문에 붙어 건너기를 청한다 (Q)
     보기   "잠겨 있다" — C002 부터 쓰던 그 문구 그대로다. 새 문구를 만들지 않았다.
           몸은 그 자리 그대로인가
     그림   shots/X-02-locked-answer.png
     판정   [ ]

X-③  같은 자리 같은 몸인데 문이 열린다  ← **이번 Cycle 의 축**
     하기   `HKT_REGION_PATTERN=FANTASY_MAZE:P2` 로 다시 띄우고 같은 자리에 선다
     보기   X-① 과 **나란히 놓고** 본다. 자물쇠가 사라지고 열린 문이다.
           몸은 아무것도 하지 않았다 — 바뀐 것은 **방의 패턴**뿐이다.
           문을 여는 것이 "내가 무엇을 했는가" 가 아니라 "방이 어떤 상태인가" 로 읽히는가
     그림   shots/X-03-heart-gate-open.png (X-01 과 짝)
     판정   [ ]

X-④  건너면 심장이다
     하기   열린 문에서 Q
     보기   방이 바뀌고 **압력 줄이 사라진다** — 심장은 규칙을 품지 않은 방이다
     그림   shots/X-04-the-heart.png
     판정   [ ]

X-⑤  심장에는 규칙이 없고, 정원 쪽은 아직 없는 곳이다
     하기   `HKT_SPAWN_REGION=MAZE_HEART HKT_SPAWN=0,16` · 정원 쪽 문(0, 18)에서 Q
     보기   압력 줄이 아예 없다. 나가는 끝은 둘(왔던 문 · 정원 쪽)이고
           정원 쪽은 "아직 갈 수 없는 곳이다" — C002 가 세운 사유 그대로다
     그림   shots/X-05-heart-has-no-rule.png · shots/X-06-garden-door.png
     판정   [ ]

X-⑥  명령 표면에 돌아가기가 있다
     하기   미로에서 `/` 를 누르고 `e` 를 친다
     보기   `emergency-return` 이 걸리고 "이 방의 비상 자리로 돌아간다" 가 뜬다.
           걸면 구역 A 의 입구에 서는가 — 그리고 **압력과 패턴이 그대로인가**
           (옮겨진 것은 몸뿐이다. 이 옮김은 이동이 아니다)
     그림   shots/X-07-command-return.png
     판정   [ ]

X-⑦  비상 자리가 없는 방에서는 걸 수 없다
     하기   백왕령에서 같은 것을 해 본다
     보기   목록에는 **그대로 실리고** "이 방에는 돌아갈 자리가 없다" 가 붙는다.
           명령이 고장 난 것이 아니라 **방의 성질**로 읽히는가
     그림   shots/X-08-no-emergency-exit.png
     판정   [ ]

X-⑧  걸어서 그 패턴을 부른다  ← **가장 중요한 판정 · 그림이 없다**
     하기   손잡이 없이 미로에 들어가, 압력을 **두 번** 넘겨(DEFAULT → P1 → P2) 심장 쪽 문으로 간다
     보기   Play 가 약속한 것이 실제로 성립하는가 —
           "원하는 패턴이 올 때까지 압력을 채우고, 그 패턴에서 심장 쪽 문으로 간다".
           두 번 넘기는 동안 **지루한가, 아니면 규칙을 쓰고 있다고 느끼는가**.
           한 번 더 넘기면 문이 도로 잠긴다는 것을 알아챌 수 있는가 (세계는 패턴 이름을 말하지 않는다)
     증거   시나리오 S-005 · S-006 이 규칙 자체는 증명한다. 여기 물을 것은 **재미**다
     판정   [ ]

X-⑨  Intent — 방이 문을 여는가
     하기   `content/regions/graph.ts` 의 `CONNECTOR_ACTIVATIONS` 한 줄에서 패턴을 `P1` 로 바꿔 띄운다
     보기   그 패턴에서 문이 열리는가. 그리고 **규칙 코드에 문 이름도 패턴 이름도 없는가**
           (`grep -rn "MAZE_HEART_GATE\|'P2'" content/world` 가 비어 있다).
           "방이 규칙을 품는다" 가 C008 에 이어 **문에서도** 서는가
     판정   [ ]
```

## 알려진 부채

```text
① 심장에서 나오는 것도 같은 조건을 읽는다   문이 **하나**이므로(Play §5.4 "door Connector 하나")
                                조건도 하나다 — 패턴이 P2 가 아니면 심장에서도 나올 수 없다.
                                혼자면 갇히지 않는다(심장에는 걸음을 압력으로 바꾸는 규칙이 없어
                                패턴이 P2 에 멈춰 있다). 남이 미로를 걸어 넘겼다면 두 번 더 넘길 때까지
                                기다려야 한다. **C010 에서 관찰자가 둘이 되면 실제로 겪게 되는 자리다** —
                                지금 고치지 않은 것은 Design 이 문을 하나로 못박았기 때문이다

② 돌아가기가 심장을 꺼내 주지 않는다   비상 자리는 **같은 방 안**의 것이다 (W14 · Spec exit.emergency).
                                심장은 자기 비상 자리를 밝히지 않았으므로 거기서는 걸 수 없다.
                                Design 이 심장의 exit.emergency 를 주지 않아 지어내지 않았다 —
                                ① 과 함께 Human 이 볼 자리다

③ 명령 표면의 글자를 촬영이 읽지 못한다   `expect` 는 HUD 만 읽고 명령 표면은 다른 자리에 뜬다.
                                X-⑥ X-⑦ 은 expect 가 "없음" 이지만 **그림에는 또렷이 있다**
                                (문구도 사유도). 도구의 한계이지 세계의 결손이 아니다

④ HUD 에 방 이름이 없다             그래서 촬영이 "건넜는가" 를 방 이름이 아니라 **압력 줄의 유무**로
                                판정했다 (X-④). 심장도 미로도 깊이는 똑같이 '심부' 라 깊이로는 갈리지 않는다.
                                화면에는 방 이름이 3D 라벨로 떠 있다 — HUD 로도 낼 것인지는 view 레인의 것

⑤ C008 부채 ⑦ 이 그대로다           몸 뒤의 정체 모를 반투명 판이 이번 그림들에도 남아 있다.
                                원인 미확정 — 추측으로 손대지 않았다

⑥ 압력으로 P2 를 부르는 것을 촬영이 못 한다   임계를 **두 번** 넘겨야 하는데 소프트웨어 GPU 에서는 몇 분이고,
                                한 번 더 넘기면 문이 도로 잠긴다. 그래서 **손잡이 하나를 더했다** —
                                `WorldSetup.regionPatterns` / `HKT_REGION_PATTERN="REGION:PATTERN"`.
                                HKT_SPAWN · HKT_SPAWN_REGION · HKT_NPCS 와 같은 갈래의 초기 배치
                                손잡이이고 **세계의 규칙을 하나도 바꾸지 않는다** (세운 패턴도 그 다음부터는
                                압력이 굴린다). 규칙이 그 문을 연다는 것은 시나리오 S-005 가 증명한다.
                                남은 것은 X-⑧ — 걸어서 부르는 것이 **재미있는가**

⑦ 촬영의 Q 가 anchor 에서 멀면 닿지 않는다   표식이 크게 읽히는 거리(넷)와 건너기가 닿는 거리(2.0)가 달라
                                X-①③(표식)과 X-②④(대답)를 **다른 run 으로** 나눠 찍었다.
                                거리 1.6 에서도 Q 가 닿지 않은 적이 있다 — 원인 미확정
                                (chooseByKey 가 "눈앞의 것" 을 고르는 방식으로 보인다). 촬영 하네스의 것
```

## 다음 Cycle 로

```text
C010 — 세계는 하나다   두 관찰자가 같은 미로 State 를 본다 · 한쪽의 걸음이 다른 쪽의 압력이다 ·
    떠난 뒤에도 State 가 남는다. 이 Cycle 이 세운 **문의 활성 조건**이 거기서 처음으로
    "남이 내 문을 닫는다" 가 된다 — 부채 ① 이 그때 실제로 겪히는 자리다

it.todo 하나        C008 이 실제로 저장한 스냅샷 파일이 저장소에 없어 "옛 스냅샷이 그대로 되살아난다" 를
    재생으로 증명하지 못한다. 지금은 STATE_VERSION 동일 + 저장 형태에 C009 의 이름이 하나도 없음으로 갈음했다.
    저장소에 스냅샷 fixture 를 두는 것은 영속을 만지는 Cycle 의 일이다

spec 이 침묵해 **테스트가 판정 방식을 정한 자리** (Human 감사)
    ①  돌아가기 **거절**이 어느 rule id 로 회신되는가 — status 와 reason 만 단언하고 열어 두었다
    ②  "받는 자리 없음" 을 `parameters: []` 로 못박았다 (자리 자체의 부재가 아니라)
    ③  interaction 의 대상 자리는 `targetEntityId === undefined` 로 읽었다
    ④  돌아가기 뒤의 movedThisTick — 침묵. 단언하지 않았다
    ⑤  **심장 쪽에서 본 문의 표식** — Observable ① 은 구역 B 쪽만 말한다.
        SPEC-004 경계("돌아올 때도 같은 조건")를 근거로 심장 쪽에서도 locked 로 정했다
    ⑥  정원 쪽 문의 표식 — 침묵. C002 선례(경계를 가리키는 출구도 open)를 따라 open 으로 정했다
    ⑦  SPEC-007 의 "미로 밖의 방" 에 심장이 드는가 — 든다고 정했다 (비상 자리를 밝힌 방은 미로 하나)
    ⑧  SPEC-008 "Connector 열넷으로 건넌다" — 경계를 가리키는 문은 애초에 건널 수 없으므로
        표식 전수 확인 + 대표 경로 셋 실제 횡단으로 읽었다
    ⑨  STATE_VERSION 의 값 자체를 `'hkt-adv-proto-i/3'` 리터럴로 못박았다 (C008 마감 커밋에서 읽었다)
    ⑩  "저장 State 가 늘지 않았다" 를 스냅샷 문자열에 C009 의 이름이 없음 + regionState 키가 C008 셋의
        부분집합으로 쟀다 (rearrangedAt 유무는 강제하지 않았다)
    ⑪  SPEC-002 "몸은 아무것도 하지 않았다" 를 movedThisTick 0 + 자리 동일로 읽었다
    ⑫  여는 패턴 'P2' 를 데이터에서 읽지 않고 spec 확정값으로 리터럴 고정했다
        (관찰 계약이 "어느 패턴이 여는가" 를 투영하지 않으므로)

데이터에 남은 이름 하나   INVERTED_GARDEN_DOOR 의 저쪽 anchor 를 'MAZE_HEART_SIDE' 로 두었다.
    경계라 검사가 보지 않는 자리다 — 정원을 짓는 Play 가 가져간다
```
