# C002 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다
(`spec.md` 만 남은 디렉터리가 닫힌 Cycle 이다).

## Human 판정 대기 — Experience Verification

`npm run dev` (또는 `scripts/run.*`) 로 세계 + 클라이언트를 띄우고 순서대로 한다. 건너기 키는 `Q` 다.
자동 시나리오(`content/world/tests/c002-many-exits.spec.ts` 42 · `content/view/tests/region-c002.spec.ts` 14 ·
`engine/world-authoring/tests/*` 36 · `engine/view-kernel/tests/interaction-choice.spec.ts` 15)는 전부 PASS 다 —
여기 남은 것은 Human 의 눈만이 판정한다. 그림은 `shots/` (시나리오 `shots.json` · `npm run cycle:shot`).

```text
Start   길은 하나뿐인 줄 알았다. 나가는 곳이 셋인데 어디로 가는지는 모른다.
End     세계에는 갈 수 있는 곳, 아직 없는 곳, 잠긴 곳이 있다. 이름은 없고 종류만 있다.
        나는 그 중 몇 개만 열어 보았고 남은 것이 더 많다.
```

```text
X-①  백왕령의 출구가 셋이고 종류가 갈린다
     하기   접속 직후 시점을 내려 방 전체를 본다
     보기   표식이 셋 — 북쪽(0, 18) 하나는 모래색(길), 동·서(±18, 0) 둘은 하늘색(고개)으로
           **색이 다르다**. 어느 표식에도 목적지 이름이 없다
     그림   shots/X-01-white-king-domain.png (길) · shots/X-02-red-waste-pass.png (고개)
     판정   [ ]

X-②  고개로 건너려 하면 "아직 갈 수 없는 곳" 이라 한다
     하기   동쪽 표식(18, 0) 바로 옆까지 걸어가 `Q`. 이어서 서쪽 표식(−18, 0) 에서도 같이 한다
     보기   화면 가운데에 "아직 갈 수 없는 곳이다" 가 잠깐 뜬다. 몸은 그 자리 그대로이고
           바닥 색·방 이름·HUD 가 하나도 바뀌지 않는다
     그림   shots/X-02b-region-not-built.png
     판정   [ ]

X-③  숲 가장자리의 출구가 셋으로 늘고 깊이가 바뀐다
     하기   북쪽 길로 건넌다. 도착한 방을 둘러본다
     보기   바닥 색이 초록으로 바뀌고 HUD 깊이가 "문명의 경계를 넘었다" 다.
           표식이 셋이고 그 중 둘은 올리브색(오솔길)으로 길과 다르다
     그림   shots/X-03-forest-edge.png
     판정   [ ]

X-④  숲 안쪽은 색과 깊이가 또 다르다
     하기   숲 가장자리의 북쪽 오솔길로 건넌다
     보기   바닥 색이 앞의 두 방과 또 다르고(어두운 청록) HUD 깊이가 "아무도 돌보지 않는 야생" 이다.
           북쪽 표식은 보라색(들어감)으로 오솔길과도 다르다
     그림   shots/X-04-forest-deep.png
     판정   [ ]

X-⑤  숲 안쪽에 출구가 다섯이고 그 중 하나는 닫힌 표식이다
     하기   숲 안쪽에서 시점을 돌려 방 전체를 본다
     보기   표식이 다섯. 북서쪽(−13, 13) 하나만 **붉고 가로 빗장이 걸려** 실루엣부터 다르다.
           나머지 넷은 열린 팻말이다
     그림   shots/X-05-ancient-gate.png
     판정   [ ]

X-⑥  닫힌 문은 "잠겨 있다" 고 한다
     하기   그 붉은 표식 바로 옆까지 걸어가 `Q`
     보기   "잠겨 있다" 가 화면에 뜬다. 무엇이 잠갔는지는 말하지 않는다.
           몸은 그 자리 그대로다
     그림   shots/X-06-connector-inactive.png
     판정   [ ]

X-⑦  막다른 방에는 돌아가는 길뿐이고 방은 비어 있다
     하기   숲 안쪽에서 서쪽(둥지)·동쪽(광석 지대)으로 각각 건너갔다 돌아온다
     보기   둥지·폐허에는 표식이 하나뿐이고 방 안에 아무 것도 없다. 광석 지대만 둘이다.
           이름은 있는데 아무것도 없다는 것이 "아직 채워지지 않은 자리" 로 읽히는가
     그림   shots/X-07-bio-ore-field.png · shots/X-08-predator-nest.png
     판정   [ ]

X-⑧  Intent — Start → End
     하기   X-① ~ X-⑦ 을 이어서 한 번에 한다
     보기   Start(나가는 곳이 셋인데 어디로 가는지 모름)에서 End(갈 수 있는 곳·아직 없는 곳·
           잠긴 곳이 있고 이름은 없고 종류만 있음)로 체험이 이어진다.
           특히 **남은 것이 더 많다**는 느낌이 드는가 — 안 가 본 출구가 몇인지 셀 수 있는가
     그림   X-01 → X-02b → X-03 → X-04 → X-05 → X-06 순서로 본다
     판정   [ ]
```

## 알려진 부채

```text
pass 색 표식이 전부 경계를 가리킨다  "고개 = 아직 없는 곳" 으로 읽힐 여지가 있다. View 가 만든 의미가 아니라
                                  C002 데이터가 그렇게 생긴 결과다 — 방이 지어지면 저절로 풀린다
방 바닥이 지형 굴곡에 묻힐 수 있다   C001 이월 — 방이 평평해지는 C008 의 일이다
카메라가 방 크기에 맞지 않는다      C001 이월 — 40×40 방도 한 화면에 들어오지 않아 출구 다섯을 한 장에
                                  담지 못했다 (그래서 X-⑤ 는 시점을 돌려 본다). 80×80 이 나오는 C003 에서 필요해진다
```

## 다음 Cycle 로

```text
HUD 프롬프트가 한 줄이다        지금은 대상이 있는 것이 재주보다 먼저 서는 것으로 갈랐다
                              (engine/view-kernel/scene/interaction-choice.ts). 출구가 다섯이면
                              그 중 하나만 말한다 — 여럿을 한 번에 보여야 할 자리가 오면 그때 넓힌다
거절이 뜨는 시간이 고정이다      NOTICE_MS 2.2초. 읽는 속도·글자 길이와 무관하다
```
