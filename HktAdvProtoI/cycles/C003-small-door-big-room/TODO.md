# C003 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다
(`spec.md` 만 남은 디렉터리가 닫힌 Cycle 이다).

## Human 판정 대기 — Experience Verification

`npm run dev` (또는 `scripts/run.*`) 로 세계 + 클라이언트를 띄우고 순서대로 한다. 건너기 키는 `Q`,
시점 내리기는 `T` 다. 자동 시나리오(`content/world/tests/c003-small-door-big-room.spec.ts` 57 ·
`content/view/tests/region-c003.spec.ts` 18 · engine 검사·시점 테스트)는 전부 PASS 다 —
여기 남은 것은 Human 의 눈만이 판정한다. 그림은 `shots/` (`npm run cycle:shot`).

```text
Start   작은 문 하나가 나무 밑동에 있다. 들어가면 무엇이 있는지 모른다.
End     안이 밖보다 컸고, 끝까지 갔더니 떨어졌고, 떨어진 곳에는 돌아갈 길이 없었다.
        남은 길 하나를 건너니 아까 그 방인데 서 있는 자리가 다르다.
```

```text
X-①  붉은 눈의 거목 — 작은 방에 출구 셋, 그 중 하나는 변이 아니라 방 안쪽에 있다
     하기   숲 안쪽에서 북쪽(0, 18) 표식으로 건넌다. 도착한 방에서 시점을 내려 둘러본다
     보기   바닥이 야생 색이고 HUD 깊이가 "아무도 돌보지 않는 야생" 이다. 표식이 셋인데
           안으로 드는 문(붉은 door 색)만 변이 아니라 방 한가운데 가까이(0, 6) 서 있다 —
           나무 밑동으로 읽히는가
     그림   shots/X-01-red-eye-tree.png
     판정   [ ]

X-②  작은 문을 건너면 깊이가 또 바뀐다
     하기   그 붉은 표식 바로 옆까지 걸어가 `Q`
     보기   HUD 깊이가 "법칙이 낯설어지는 심부" 로 바뀌고 바닥 색이 야생의 청록에서
           차가운 보라·남색으로 옮겨 간다. 야생보다 더 어두워졌는가
     그림   shots/X-02-small-door.png (건너기 전) → shots/X-03-tree-inner-world.png (건넌 뒤)
     판정   [ ]

X-③  안이 밖보다 크다 — 몸이 작아진다
     하기   X-② 의 두 그림을 나란히 놓고 본다. 그 뒤 방 안을 한참 걸어 본다
     보기   같은 시점 설정인데 몸이 눈에 띄게 작다 — 시점이 물러났기 때문이다(거리 15 → 30).
           걸어 보면 바닥 테두리까지가 앞의 어느 방보다 멀다. **"이 방은 크다"** 가
           숫자가 아니라 그림으로 읽히는가
     그림   shots/X-02-small-door.png 과 shots/X-03-tree-inner-world.png 을 견준다
     판정   [ ]

X-④  요청하지 않았는데 떨어진다
     하기   거목 내부 세계에서 들어온 문의 **반대쪽 끝**(북쪽)으로 걸어간다. `Q` 를 누르지 않는다
     보기   가까이 가는 것만으로 화면이 바뀐다. 거절도 물음도 없다 —
           세계가 묻지 않고 데려갔다. 그 자리가 심장 호수다
     그림   shots/X-04-fall-to-heart-lake.png
           (이 run 은 거목 내부 세계에서 시작했고 건너기 키를 한 번도 누르지 않았다.
            촬영 하네스로는 걸어 들어가는 장면을 못 찍어 사거리 안에 서는 것으로 대신했다 —
            아래 "다음 Cycle 로" 참고. 걸어 들어가는 것 자체는 실주행으로 확인해 주기 바란다)
     판정   [ ]

X-⑤  떨어진 곳에는 돌아갈 길이 없다
     하기   심장 호수에서 시점을 돌려 사방을 본다
     보기   떨어져 선 자리에는 아무 표식도 없다. 방을 다 둘러봐도 표식은 남쪽 물길
           하나뿐이다 — 올라갈 출구가 없다는 것이 막막함으로 읽히는가
     그림   shots/X-05-heart-lake.png (물길은 이 각도의 화면 밖이다 — 시점을 돌려 확인한다)
     판정   [ ]

X-⑥  물길은 아까 그 방으로 내되 다른 자리다
     하기   그 하나뿐인 표식 옆까지 걸어가 `Q`
     보기   표식 색이 백왕령의 고개(옅은 하늘빛)와 다른 진한 청록이다. 건너면 바닥 색과
           HUD 깊이가 숲 안쪽(야생)으로 돌아오는데 **선 자리가 다르다** —
           거목으로 나갔던 자리가 아니다. 시점을 돌리면 출구 다섯과 닫힌 문 하나가 그대로다
     그림   shots/X-06-river.png → shots/X-07-forest-deep-river-mouth.png
     판정   [ ]

X-⑦  Intent — Start → End · Play Goal
     하기   백왕령에서 시작해 X-① ~ X-⑥ 을 이어서 하고, 물길로 나온 뒤 숲 안쪽 → 가장자리 →
           백왕령까지 되짚어 돌아온다 (이것이 Play 전체의 실주행이다)
     보기   ① "연결에 종류가 있다" 가 몸으로 읽히는가 — 묻고 건너는 것(길·오솔길·문)과
              묻지 않고 데려가는 것(추락)이 다른 일로 느껴지는가
           ② 돌아온 백왕령에 아직 건너지 않은 출구 둘(고개)이 그대로 남아 있는가 —
              **"남은 것이 더 많다"** 가 여전히 서는가
           ③ 방 아홉 · 깊이 넷을 지나는 동안 지도 없이도 어디쯤인지 감이 서는가
     판정   [ ]
```

## 알려진 부채

```text
방 바닥이 지형 굴곡에 묻힌다        C001 이월 — 방이 평평해지는 C008 의 일이다.
                                  C003 에서 더 두드러진다: deep 바닥(보라)이 반투명이라
                                  초록 지형 위에서 색이 씻긴다. 그림 X-03 · X-04 가 그렇다
카메라가 방 전체를 담지는 않는다     C002 이월 — C003 은 "방마다 시점이 다르다" 까지만 했다.
                                  일부러 남긴 자리다: 다 보이게 하면 큰 방이 작은 방과 같은
                                  크기로 화면에 앉아 "안이 밖보다 크다" 가 사라진다 (spec UNRESOLVED)
중첩은 아직 아무 일도 하지 않는다    세계 규칙 중 무엇도 containment 를 읽지 않고 관찰에도 실리지 않는다.
                                  지금 하는 일은 검사 ⑥ 하나다 — 부모가 누구인지를 주체가 아는 것은
                                  3층(주체가 무엇을 아는가)의 자리다
```

## 다음 Cycle 로

```text
촬영 하네스에서 자판 걸음이 몸을 옮기지 못했다
    `hold` 로 방향키를 1.5초 눌러도 몸이 그대로였다 (요청은 나갔다 — HUD 의 "보낸" 이 늘었다).
    KEY_LOOKAHEAD 1.6 은 방 안이므로 out-of-bounds 는 아니다. 소프트웨어 GPU 의 느린
    프레임에서 keyboard.direction() 이 실릴 프레임이 모자란 것으로 보이나 **원인을 확정하지 못했다**.
    그래서 X-④ 는 걸어 들어가는 장면 대신 사거리 안에 서는 것으로 찍었다.
    C002 까지는 `hold` 를 쓴 적이 없어 드러나지 않았다. 실주행(사람 손)에서는 문제가 없는지
    X-④ 가 확인해 준다 — 만약 실주행에서도 안 되면 이것은 촬영 도구가 아니라 조립(app)의 부채다

deep 색의 밝기 방향 (Human 감사)
    C002 가 세운 "깊어질수록 어둡다" 를 지켰다 — 상대 휘도로 wild 0.0588 → deep 0.0393 이다.
    다만 채널 합(192 → 214)으로 재면 반대로 보인다. 파랑이 사람 눈에 어둡게 보이기 때문인데,
    반투명으로 지형 위에 얹히면 그 차이가 줄어든다. 실주행에서 "더 깊다" 로 읽히는지 눈으로 봐 달라

추락에 아무 대답이 없다
    거절이 없으니 사유 코드도 없고, 그래서 화면에 뜨는 말도 없다 (spec 이 그렇게 정했다).
    "왜 갑자기 화면이 바뀌었지" 로 읽힐 여지가 있다 — 낙하 연출·시간은 이 Cycle 밖이다.
    X-④ 의 판정이 이것을 답한다
```
