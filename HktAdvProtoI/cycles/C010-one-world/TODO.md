# C010 — TODO

살아 있는 문서다. 항목이 닫히면 지운다 — 다 지워지면 이 파일도 지운다.

## Human 판정 대기 — Experience Verification

관찰자를 둘로 만들려면 **창을 둘 연다** — `npm run dev` 로 띄운 뒤 같은 주소를 새 창에서 한 번 더 연다.
미로에서 바로 보려면 두 창 다 `HKT_SPAWN_REGION=FANTASY_MAZE` 로 띄운 세계에 붙으면 된다
(두 번째 몸은 세계가 SPAWN_POINTS 의 다음 자리에 세운다).
자율 존재를 미로에 놓으려면 `HKT_NPC_REGION=FANTASY_MAZE`, 패턴을 골라 세우려면 `HKT_REGION_PATTERN=FANTASY_MAZE:P2`.
자동 시나리오(`content/world/tests/c010-one-world.spec.ts` 20)는 전부 PASS — `npm test` 1149 PASS / 5 todo.
그림은 `shots/`.

```text
Start   미로는 내가 걸어서 만든 상태다. 내가 부른 패턴이고 내가 연 문이다.
End     아니다 — 이 미로는 내 것이 아니라 세계의 것이다.
```

```text
X-①  둘이 같은 미로에 있다
     하기   창 둘로 같은 세계의 미로에 들어간다 (패턴 P2 로 띄우면 심장 문이 열려 있다)
     보기   둘 다 "함께: 2명" 이고, 둘의 화면에서 **같은 통로가 열려 있고 같은 문이 열려 있다**.
           길과 문은 사람마다 갈리지 않는가
     그림   shots/X-01-two-in-one-maze.png · shots/X-02-companion-sees-the-same.png
     판정   [ ]

X-②  아무도 자판을 누르지 않는데 압력이 오른다  ← **세계는 플레이어 없이도 돈다** (W9)
     하기   `HKT_NPC_REGION=FANTASY_MAZE` 로 띄우고 **손을 뗀다**
     보기   자율 존재가 미로를 걷고, 그 걸음이 내 압력이 된다. 나는 한 걸음도 걷지 않았다.
           두 그림 사이에 47 → 114 로 올랐다 — 그 사이 아무 키도 누르지 않았다
     그림   shots/X-03-pressure-before.png → shots/X-04-pressure-rose-untouched.png
     판정   [ ]

X-③  둘이 같은 압력을 본다
     하기   같은 순간 두 창을 본다
     보기   두 화면의 압력이 같은 값으로 움직인다. 압력에 **이름표가 없다** —
           누구의 걸음이 밀었는지 세계는 말하지 않는다. 그것이 "내 것이 아니다" 로 읽히는가
     그림   shots/X-05-companion-same-pressure.png
            (그림 둘은 몇 초 차이로 찍혀 그만큼 더 올라 있다 — **같은 tick 의 동일성은 시나리오 S-001 이 잰다**)
     판정   [ ]

X-④  남의 걸음이 내 길을 바꾸고 내 문을 닫는다  ← **가장 중요한 판정 · 그림이 없다**
     하기   창 둘로 미로에 들어가 **한 사람만** 걷는다. 다른 사람은 P2 에서 심장 문 곁에 서 있는다
     보기   서 있는 사람의 화면에서 통로 색이 바뀌고, 열려 있던 심장 문이 **잠김**으로 바뀐다.
           그 자리에서 건너려 하면 "잠겨 있다" — 자기는 아무것도 하지 않았다.
           그때 드는 느낌이 **부당함인가, 세계의 크기인가.** 이 Play 가 노린 것은 뒤쪽이다
     증거   시나리오 S-005 · S-007 · S-008 이 규칙은 증명한다. 여기 물을 것은 **느낌**이다
     판정   [ ]

X-⑤  떠나도 남는다 · 다시 들어와도 같은 미로다  ← 그림이 없다
     하기   한 창을 닫는다. 잠시 뒤 그 창을 다시 연다
     보기   남은 사람의 압력과 패턴이 **한 값도 바뀌지 않는다.** 떠난 이의 몸은 미로에 남아 있고
           함께 있는 사람의 수만 준다. 다시 들어온 사람이 보는 것은 처음이 아니라 **지금의 미로**다
     증거   시나리오 S-009 · S-010 · S-011 · S-012. 촬영으로는 못 잡았다 — 아래 부채 ①
     판정   [ ]

X-⑥  규칙 없는 방은 그대로다
     하기   둘이 백왕령으로 나간다
     보기   관찰자가 둘이어도 압력 줄이 **아예 없다**. 걸음도 판정도 C009 까지와 같다
     그림   shots/X-06-room-without-rule.png
     판정   [ ]

X-⑦  Intent — 관찰자의 수가 어느 규칙의 전제도 아닌가
     하기   `git show --stat` 로 이 Cycle 의 커밋을 본다
     보기   **세계 코드가 한 줄도 바뀌지 않았다.** 늘어난 것은 시나리오와 촬영 손잡이뿐이다.
           "세계에 하나" 가 앞에서 이미 참이었다는 뜻인가 — 그것이 이 Cycle 의 주장이다
     판정   [ ]
```

## 알려진 부채

```text
① 촬영이 "떠남" 을 제때 잡지 못한다      창을 닫아도 "함께: 1명" 이 그림에 오지 않는다 —
                                  캔버스 둘을 소프트웨어 GPU 로 굴리면 이어짐의 왕복이 **수십 초**로
                                  밀려(실측 43초) 관찰 결과가 그만큼 낡는다. 35초를 기다려도 2명이었다.
                                  **잘못 읽히는 그림을 남기지 않았다** — 그 run 을 빼고 X-⑤ 를
                                  실주행 항목으로 돌렸다. 떠남 자체는 S-009 · S-010 이 증명한다

② 사람의 걸음을 촬영이 밀지 못한다        같은 이유로 두 창 중 하나가 걷게 하면 18초에 압력이 1 올랐다.
                                  그래서 그림에서 압력을 미는 것은 사람이 아니라 **미로 안의 자율 존재**다
                                  (그것이 마침 Observable ⑦ 이고 W9 이다). 사람의 걸음이 남의 압력이
                                  된다는 것(Observable ②③④)은 X-④ 의 실주행 판정이 받는다

③ observers.present 는 방 단위가 아니다   "함께 있는 사람의 수" 가 **세계 전체**의 수다 — 서로 다른 방에
                                  있는 둘도 서로를 2 로 센다 (시나리오 S-002 가 그 현재 행동을 못박았다).
                                  Play 의 "함께 있는" 이 같은 방을 뜻한다면 이것은 결손이다.
                                  spec 이 그 구분을 하지 않아 지어내지 않았다 — **Human 이 정할 자리다**

④ 압력에 이름표가 없다는 것을 관찰로 증명할 수 없다   "누가 넘겼는가" 는 관찰 결과의 형에 자리 자체가 없어
                                  단언할 대상이 없다. 저장물 문자열 검사로 대신했다 (S-014).
                                  이것은 결손이 아니라 **의도**다 (spec Observable 절) — 다만 증명 방식이 간접적이다

⑤ 앞 Cycle 의 부채는 그대로다            C008 ⑦(몸 뒤의 반투명 판) · C009 ①②(심장에서 나오는 것도 P2 를
                                  요구한다 · 돌아가기가 심장을 꺼내 주지 않는다)가 이번 그림에도 남아 있다.
                                  C009 ①② 는 이 Cycle 이 "관찰자가 둘이 되면 겪힌다" 고 적어 둔 자리인데,
                                  촬영이 사람의 걸음을 밀지 못해 그림으로는 확인하지 못했다 — X-④ 가 받는다
```

## 다음 Cycle 로

```text
Play RuleBoundRoom 이 닫힌다 (C008 · C009 · C010).
**Play Goal 실주행 확인**이 그 위에 하나 남는다 — 고대 문으로 들어가 규칙을 관찰하고 이용해 심장에 닿고,
두 번째 관찰자가 같은 미로를 보는 한 바퀴. X-④ · X-⑤ 가 그 절반이다.

다음 레인은 Material (C011~C014) — Region State 와 세계 과정이 섰으므로 재료가 생애를 가질 수 있다.

spec 이 침묵해 **테스트가 판정 방식을 정한 자리** (Human 감사)
    ① "같은 tick 에 같다" 를 봉투의 tick 번호가 아니라 같은 순간 두 관찰 결과를 읽어 비교하는 것으로 쟀다
       (봉투에 tick 번호가 없다)
    ② "B 의 관찰 결과에서 통로가 바뀐다" 를 pattern 의 변화 + **B 의 이동 요청 판정 뒤집힘**으로 쟀다.
       통로의 열림/닫힘은 투영되지 않으므로(C008 의 의도) 직접 볼 자리가 없다
    ③ 두 몸이 같은 anchor 에 설 수 없어(몸끼리 밀어냄) 관찰자마다 세계를 따로 세웠다 —
       spec 은 "둘이 같은 문 앞에 선다" 를 말하지 않는다
    ④ **걷는 도중에 떠나면** 남은 걸음이 압력을 더한다 (하던 행동은 세계 시간대로 이어진다).
       규칙상 옳지만 "떠난 뒤 세계가 안 바뀐다" 의 읽기에 따라 회색지대다 — 테스트는 걸음을 끝낸 뒤 떠나게 했다
    ⑤ SPEC-006 의 "C 가 미로에 닿는 방법" 을 C001 이 세운 길(걷기 + 건너기)로 정했다
    ⑥ observers.present 가 방으로 잘리지 않는 현재 행동을 못박았다 (위 부채 ③)

시나리오가 쓰지 않은 손잡이   자율 존재를 미로에 놓는 길(WorldSetup.npcRegion)은 이 Cycle 이 세웠지만
    시나리오는 C008 의 선례(저장·복구)로 놓았다. 촬영이 그 손잡이를 쓴다 —
    S-015 를 그것으로 갈아끼우면 우회로가 하나 준다
```
