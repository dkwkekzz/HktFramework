// 의미 코드 → 플레이어 표시 문구 (결정 Layer 데이터).
// World 는 코드만 보낸다 — 불가 사유 코드, 행동 코드 등. 문구는 여기서 정한다.
// 미등록 코드는 코드 그대로 표시된다 — 표현 누락이 게임을 멈추지 않는다.
//
// 재료 계통의 코드들만은 **키를 직접 적지 않고 데이터의 이름을 그대로 받아 쓴다** (C011).
// 그 이름의 원본은 content/regions 하나이고(soilStainTag 의 주석: "데이터도 표현도 이
// 함수 하나로 이름을 짓는다"), 여기 사본을 두면 데이터가 태그를 옮겼을 때 화면이 말을
// 잃는 것이 아니라 **옛 말을 계속 한다** — 그쪽이 훨씬 나쁘다.

import {
  ASKS_WARMTH,
  ASKS_WARMTH_WEAK,
  BREATH_GLOWS,
  BIO_ORE,
  BLOCK_COLLAPSED,
  CONDITION_RIDGE,
  CONDITION_UNMET,
  EMBER_COOLED,
  FLOW_ARRIVED,
  FORM_CARCASS,
  FORM_CLUTCH_HUSK,
  FORM_CORPSE_RIME,
  FORM_DRIFT_DUST,
  FORM_EGG_HUSK,
  FORM_FROST_VEIN,
  FORM_GLOW_CAP,
  FORM_HUSK_SHARD,
  FORM_MOLT_LITTER,
  FORM_ORE_PEBBLE,
  FORM_NEST_MYCELIUM,
  FORM_FALLEN_SCALE,
  FORM_OUTCROP,
  FORM_PREY_REMAINS,
  FORM_RIME,
  FORM_RIVER_GRAIN,
  FORM_ROOT_CLUTCH,
  FORM_ROOT_EGGS,
  FORM_ROOT_NODULE,
  FORM_SEEP_CRUST,
  FORM_SILT_BED,
  FORM_SPOIL_PILE,
  FORM_WALL_EMBER,
  FROST_CRYSTAL,
  FROST_VEIN_REGROWN,
  GIANT_TREE_FUNGUS,
  HEAT_CRYSTAL,
  GROUND_TREMOR,
  FORM_CARCASS_BLOOM,
  LIFE_FUNGUS_CROWDED,
  LIFE_HAS_OWNER,
  LIFE_NEEDS_CARCASS,
  LIFE_NEEDS_DECAY,
  LIFE_NEEDS_MATERIAL,
  LIFE_NEEDS_PARENT,
  LIFE_NEEDS_RAIN,
  NO_DECOMPOSER,
  NO_MOLTER,
  ORE_EATER_MOLT,
  PRESENCE_BIG_BIRD,
  PRESENCE_ORE_EATER_SWARM,
  PRESENCE_PREDATOR,
  RECOVERY_NEXT_BIRTH,
  RECOVERY_STALLED,
  WHALE_SCALE,
  ASPECT_FLESH,
  ASPECT_HEAT,
  ASPECT_LIGHT,
  RELATION_ABSORBS,
  RELATION_EMITS,
  RELATION_GROWS_ON,
  RELATION_STORES,
  emberWarmthTag,
  frostBreathTag,
  propertyPhraseCode,
  propertyTag,
  soilStainTag,
  weakenedConditionTag,
} from '../regions/index';

const CODE_TEXT: Record<string, string> = {
  // 불가 사유
  'no-mining-tool': '곡괭이가 없다',
  'out-of-range': '너무 멀다 — 가까이 이동하자',
  // 세계가 그 원천을 알지 못한다 (C011 R1 · R2 의 거절)
  'unknown-source': '그런 원천이 없다',
  // 다 캐 간 원천이다 (C012 R1 의 거절). **몇 번 캘 수 있었는지도 언제 돌아오는지도 말하지
  // 않는다** — 실려 오지 않는 것이고(spec Observable), 관찰된 사실은 "지금 여기 없다" 하나다
  'source-depleted': '남은 것이 없다',
  // 아직 되돌아오는 중인 원천이다 (C013 R3 의 거절). **얼마나 남았는지도 무엇이 그것을
  // 되돌리는지도 말하지 않는다** — 실려 오지 않는 것이고(spec Observable), 관찰된 사실은
  // "지금은 아직 아니다" 하나다. 언제 돌아오는지는 흙과 그림이 예보할 뿐이다 (§5.6)
  'source-recovering': '아직 되돌아오는 중이다',
  // 불가 사유
  'action-busy': '지금 하는 행동이 끝나야 한다',
  'no-target': '대상이 없다',
  // 방의 끝이다 (RoomBecomesLand Q6 · 실주행 보고 "왜인지 말하지 않는다") — 막는 것이 무엇인지를
  // 말한다: 땅이 아니라 **방의 경계**이고, 나가는 길은 출구 표식뿐이다
  'out-of-bounds': '방의 끝이다 — 더 갈 수 없다. 나가는 길은 출구 표식이다',
  // 행동 코드
  idle: '대기',
  move: '이동',
  attack: '공격',
  mine: '채굴',
  hit: '피격',
  // 행동 코드
  'heavy-attack': '강공격',
  downed: '쓰러짐',
  // 불가 사유
  'insufficient-cp': '기력이 모자란다',
  // 이동 모드
  walk: '걷기',
  run: '달리기',
  // 불가 사유 — 속성 변경
  'debug-closed': '이 세계는 속성 변경을 허용하지 않는다',
  'unknown-target': '그런 존재가 없다',
  'unknown-attribute': '그런 속성이 없다',
  'value-out-of-range': '허용된 범위를 벗어난 값이다',
  // 불가 사유 — 요청 수용 경로 (세계가 이제 이 사유들도 되돌려 준다)
  'unknown-interaction': '그런 명령이 없다',
  'unknown-observer': '세계가 나를 알지 못한다',
  'missing-attribute': '무엇을 바꿀지 실리지 않았다',
  'missing-position': '어디로 갈지 실리지 않았다',
  'missing-target': '대상이 실리지 않았다',
  'missing-mode': '어떤 걸음인지 실리지 않았다',
  // 깊이 태그 (C001 — hud region.depth 의 값. L2-World-Concept §3.2)
  civil: '문명권',
  outer: '문명의 경계를 넘었다',
  // 문명권 → 경계 → 그 다음 한 걸음. 여기서부터는 아무도 돌보지 않는다 (C002)
  wild: '아무도 돌보지 않는 야생',
  // 야생 다음 한 걸음 — 기존 생물학·자연법칙으로 설명하기 어려워지는 곳 (§3.2 · C003)
  deep: '법칙이 낯설어지는 심부',
  // 불가 사유 — 건너기 (RULE-REGION-TRANSIT-001)
  'unknown-connector': '그런 길이 없다',
  'wrong-region': '여기서 갈 수 있는 길이 아니다',
  // 닫힌 Connector (C002). 여는 법은 이 세계에 아직 없다 — 그래서 무엇이 잠갔는지도 말하지 않는다
  'connector-inactive': '잠겨 있다',
  // 철 때문에 닫혀 있다 (C016 R5 · R7). **잠긴 것과 갈려야 한다** — 위의 것은 "닫혀 있다" 이고
  // 이것은 "지금이 그때가 아니다" 다: 하나는 여는 법을 찾을 일이고 하나는 기다릴 일이다.
  // **어느 철이면 열리는지 말하지 않는다** — 세계가 싣지 않는다 (spec Observable "싣지 않는다").
  // 이 한 줄이 건너기의 거절 사유이자 원천에 걸린 조건이다 — 같은 사실이므로 말도 하나다
  // (흐름의 조건이 '아직 그때가 아니다' 하나로 두 자리에 서는 것과 같은 규율)
  'not-this-season': '이 철이 아니다',
  // 낮밤을 타는 원천 (RoomBearsMaterial 실주행 판정) — 철의 것과 갈리는 말이다: 기다릴 것이 해다
  'not-this-hour': '지금은 때가 아니다 — 해가 지거나 뜨면 다시 온다',
  // 아직 짓지 않은 곳(frontier). 세계의 끝이 아니라 "아직" 이다 — 목적지는 여전히 밝히지 않는다
  'region-not-built': '아직 갈 수 없는 곳이다',
  // 불가 사유 — 돌아갈 자리가 없는 방 (C009 RULE-EMERGENCY-RETURN-001 의 ELSE).
  // 명령이 고장난 것이 아니라 **이 방에 그 자리가 없다**는 것이 읽혀야 한다 — 그래야
  // 다른 방에서는 걸린다는 것이 같은 문구에서 나온다 (비상 자리는 방의 성질이다)
  'no-emergency-exit': '이 방에는 돌아갈 자리가 없다',
  // 내 데이터와 세계의 방이 다르다 (region.hash 대조)
  'region.hash-mismatch': '세계와 다른 땅을 보고 있다',
  // 불가 사유 — 땅이 막는다 (C006 RULE-MOVE-001 의 traversable 전제).
  // 둘 다 세계의 대답이며 몸의 자리는 바뀌지 않는다. 왜 막혔는지를 **땅의 성질**로 말한다 —
  // "갈 수 없다" 가 아니라 "가파르다 · 깊다" 여야 다음에 어디로 걸을지가 화면에서 읽힌다.
  'too-steep': '너무 가파르다', // Play §4 막힘이 준 말 그대로
  // 표식의 줄기가 막는다 (RoomBecomesLand Q7) — 땅의 성질이 아니라 **거기 선 것**이 막는 첫 사유다
  'landmark-trunk': '줄기가 막는다 — 돌아가자',
  // 나아가지 못하는 몸이 듣는 말 (RoomBecomesLand Q6 · movement-reading) — 거절이 아니라
  // **진행이 멎은** 경우다. 막는 몸의 이름 · 발밑의 사유 · 그것도 모르면 막혀 있다는 것까지만
  'move.blocked-by-body': '{}이(가) 길을 막고 있다 — 비켜 가자',
  'move.stalled-on': '발밑이 막힌 땅이다 — {}. 옆으로 빠져나가자',
  'move.stalled': '나아가지 못한다 — 무언가에 막혀 있다',
  'deep-water': '물이 너무 깊다', // 같은 어법 — 땅(물)의 성질 하나로 끝나는 한 줄
  // 무너져 내린 자리 (C012 R3 · R4 — 이동의 거절이자 그 자리의 막는 것).
  // 위의 둘과 같은 어법이되 **땅의 성질이 아니라 겪은 일**이다: 가파른 곳은 언제나
  // 가팔랐지만 여기는 그렇게 된 것이다. 무엇이 무너뜨렸는지는 말하지 않는다 —
  // 그 옆에 선 원천의 그림이 이미 그것을 말하고 있고, 잇는 것은 관찰자의 일이다
  [BLOCK_COLLAPSED]: '무너져 내려앉았다',
  // ── 지목한 자리가 답하는 말 (C026) ────────────────────────────
  //
  // 판에 서는 값은 전부 **이미 있는 코드**를 옮긴 것이다 — 새 의미를 만들지 않는다.
  // 표면 태그 넷은 땅을 컴파일하는 규칙 표(content/regions/terrain-rules.ts)의 이름이고,
  // 여기서 처음 사람이 읽는 말이 된다: 화면은 이 넷을 색으로만 갈라 놓았을 뿐 여태
  // 이름을 불러 준 적이 없다 (terrain-presentation 의 SURFACE_COLORS).
  flat: '평지',
  slope: '비탈',
  steep: '급경사',
  wet: '젖은 땅',
  // 지날 수 있는가 — **사유는 따로 뜬다** (too-steep · deep-water · passage-closed).
  // 그래서 이 둘은 판정 하나만 말하고 이유를 겸하지 않는다
  'place.passable': '지날 수 있다',
  'place.impassable': '지날 수 없다',
  // 통로가 지금 어떤가. 모름을 닫힘으로 적지 않는다 — 세계가 하지 않은 말이다
  // (PASSAGE_UNKNOWN_ZONE 이 색에서 지키는 것과 같은 구분)
  'place.passage.open': '열려 있다',
  'place.passage.closed': '닫혀 있다',
  'place.passage.unknown': '알 수 없다',
  // 판의 제목 — 자리에는 이름이 없으므로 이 말 하나이고, 어느 자리인지는 좌표가 말한다
  'target.place': '지목한 자리',
  // ── 존재가 답하는 말 (C027) ──────────────────────────────────
  //
  // 아무것도 지목하지 않았을 때 판이 지는 것 — 지목한 자리가 아니라 **내 발밑**이다.
  // 제목이 그것을 말해야 판이 무엇에 대한 답인지 갈린다 (C027 SPEC-005)
  'target.standing': '내가 선 자리',
  // 이름 없는 것의 이름 ① 종류(entity.kind).
  // 세계는 이 코드들을 이미 싣고 있었고 화면은 그것을 색으로만 갈라 놓았다 —
  // 여기서 처음 사람이 읽는 말이 된다 (표면 태그 넷이 C026 에서 그랬던 것과 같다).
  // 방 사이 전이의 일곱은 region-presentation 의 TRANSITION_TINTS 가 색으로 적어 둔 뜻 그대로다.
  road: '길',
  trail: '오솔길',
  door: '문',
  pass: '고개',
  interaction: '들어감',
  falling: '추락',
  river: '물길',
  // 재료의 자연 형태 (entity.kind — C011). 같은 계통이 자리마다 다른 형태로 난다.
  // 판의 제목이 이 말이다 — 지목하면 그것이 **무엇으로 보이는가**가 먼저 온다.
  // 쓰임은 한 글자도 없다 (spec SPEC-002 경계 · S10): 관찰된 것만 적는다
  [FORM_OUTCROP]: '광맥의 노두',
  [FORM_ROOT_NODULE]: '거목의 부푼 뿌리혹',
  [FORM_MOLT_LITTER]: '나무 밑동에 흩어진 껍질 조각',
  [FORM_SPOIL_PILE]: '헐린 선광 더미',
  // 형태 셋 (C014) — 같은 표에 세 줄이 늘 뿐이다. 여기서도 쓰임은 한 글자도 없고,
  // 어귀의 알갱이와 광맥의 노두가 **같은 재료**라는 것도 적지 않는다 — 지목한 것이
  // 무엇으로 보이는가만 적는다. 그 둘을 잇는 것은 관찰자의 일이다
  [FORM_NEST_MYCELIUM]: '사체 위 흰 균사',
  [FORM_RIVER_GRAIN]: '어귀에 깔린 알갱이',
  [FORM_SILT_BED]: '호수 바닥의 침전',
  // 여덟째 형태 (C016) — 어법도 규율도 그대로다. **재료의 이름은 새로 나지 않는다**:
  // 이것도 노두·뿌리혹과 같은 생체 광석이고(resource-ecology 의 forms 다섯), 새로 필요한
  // 것은 그것이 여기서 **무엇으로 보이는가** 뿐이다. 언제 나는 것인지는 적지 않는다 —
  // 어느 철에 무엇이 나는지를 세계가 말하지 않으므로(spec Observable) 화면도 말하지 않는다
  [FORM_SEEP_CRUST]: '땅에 배어 나와 굳은 껍질',
  // 아홉째·열째 형태 (C018) — **지나간 것이 남긴 것** 둘. 어법도 규율도 그대로다:
  // 지목한 것이 여기서 무엇으로 보이는가만 적는다. 무엇이 두고 갔는지도, 언제 다시 오는지도
  // 적지 않는다 — 그것은 관찰자가 여러 번 보고 배우는 것이다 (T8)
  [FORM_FALLEN_SCALE]: '땅에 비스듬히 꽂힌 흰 판',
  [FORM_PREY_REMAINS]: '길게 끌린 껍질 조각들',
  // 열한째~열넷째 형태 (C020) — 협곡의 넷. 어법도 규율도 그대로다: 지목한 것이 여기서
  // **무엇으로 보이는가**만 적는다. 무엇에 쓰는지도, 무엇이 그것을 세웠는지도 없다.
  //
  // **넷이 같은 재료라는 것은 적지 않는다.** 네 줄에 같은 성질(푸르다 · 곁의 것을 얼린다)이
  // 되풀이해 서는 것은 그것이 넷 다에서 **관찰되기 때문**이지 넷을 묶어 주려는 것이 아니다 —
  // 묶는 것은 관찰자의 일이다 (어귀의 알갱이와 광맥의 노두를 잇게 한 C014 의 그 규율).
  [FORM_RIME]: '밟아도 녹지 않는 푸른 서리',
  [FORM_FROST_VEIN]: '절벽을 가르고 나온 푸른 결정면',
  [FORM_DRIFT_DUST]: '눈에 섞여 반짝이는 푸른 가루',
  [FORM_CORPSE_RIME]: '언 사체에 돋은 푸른 결정',
  // 열다섯째 형태 (C030) — 거목 속의 것 하나. 어법도 규율도 그대로다: 지목한 것이 여기서
  // **무엇으로 보이는가**만 적는다. 무엇에 쓰는지도, 무엇이 그것을 덥히는지도 없다.
  // 문장은 기획의 것 그대로다 (Play §5.3 Trace — "서리가 앉지 않는 벽의 자리")
  [FORM_WALL_EMBER]: '서리가 앉지 않는 벽의 잉걸',
  // 흩어진 것 셋 (RoomBearsMaterial 실주행 판정) — 방마다 여럿이 서는 작은 것들. 어법은 그대로다:
  // 지목한 것이 **무엇으로 보이는가**만 적고, 무엇과 같은 재료인지도 언제 나는지도 적지 않는다
  [FORM_ORE_PEBBLE]: '흙 위에 흩어진 붉은 자갈',
  [FORM_HUSK_SHARD]: '풀줄기에 걸린 껍질 조각',
  [FORM_GLOW_CAP]: '어둠에서 희게 빛나는 갓',
  // 둥지의 사체 (C024) — 같은 방의 '사체 위 흰 균사' 와 **갈려야 한다**: 저것은 덮인 것이고
  // 이것은 아직 덮이지 않은 것이다. 그림이 그 둘을 껍질색과 부피로 가르고, 이 말은 그
  // 그림을 옮길 뿐이다.
  //
  // **무엇의 사체인지도, 무엇이 그것을 두고 갔는지도 적지 않는다** — 세계가 싣지 않고
  // (spec Observable) 둥지에 서 본 관찰자가 스스로 잇는다 (허물의 주인을 잇게 한 규율 그대로).
  // 무엇이 삭일 것인지도 없다 — 그것은 곁에 선 것을 본 사람의 것이다
  [FORM_CARCASS]: '둥지에 눕은 껍질 덮인 사체',
  // 태어남이 남기고 간 형태 둘 (C023) — 어법은 그대로다: 지목한 것이 **무엇으로 보이는가**만
  // 적는다. **무엇이 그것을 두고 갔는지도, 무엇이 다시 세우는지도 적지 않는다** — 세계가
  // 싣지 않고(spec Observable), 터지는 것을 본 관찰자가 스스로 잇는다.
  //
  // 둘이 같은 재료라는 것도 적지 않는다 — 크기와 껍질의 색이 그것을 말하고, 묶는 것은
  // 관찰자의 일이다 (협곡의 넷을 묶어 주지 않은 그 규율 그대로).
  [FORM_CLUTCH_HUSK]: '터져 벌어진 채 남은 큰 껍질',
  [FORM_EGG_HUSK]: '뿌리 마디에 남은 작은 껍질',
  // 재료의 이름 (Material Seed 코드 — C011). 소지품 줄의 이름표가 이 말이다.
  // **무엇에 쓰는지는 여기에도 어디에도 없다**
  [BIO_ORE]: '생체 광석',
  [ORE_EATER_MOLT]: '광식충 허물',
  // 세 번째 재료 (C014) — 앞의 둘과 같은 어법이다. 어디서 나는지도 무엇에 쓰는지도 없다
  [GIANT_TREE_FUNGUS]: '거목균',
  // 네 번째 재료 (C029 ADDED — 이름이 이제야 서는 이유는 판에 '재료' 줄이 없었기 때문이다.
  // C018 이 그 원천을 세울 때 사람이 읽을 자리가 없어 코드로만 있었고, 줄이 서는 지금
  // 옮기지 않으면 판이 `WHALE_SCALE` 이라고 말한다).
  // 이 세계의 **정식 이름**이다 (L2-World-Access §9.2 · L2-World-Time) — 새로 짓지 않는다.
  // **성질은 없다** — 세계가 그 재료에 성질을 밝히지 않았으므로 판도 이름까지만 말한다
  [WHALE_SCALE]: '고래 비늘',
  // 다섯째 재료 (C020) — 이 세계의 **정식 이름**이다 (L2-World-Region §5.1 이름 표).
  // 새로 짓지 않는다: 세계관이 이미 부르는 말이 있고, 화면이 다른 말을 하면 관찰자가 본 것과
  // 이 세계가 아는 것이 갈린다 (지나가는 것들의 이름에 쓴 그 규율 그대로).
  // 앞의 넷과 같은 어법이다 — 어디서 나는지도 무엇에 쓰는지도 없다
  [FROST_CRYSTAL]: '빙정석',
  // 여섯째 재료 (C030) — 이 세계의 **정식 이름**이다 (L2-World-Region §5.1 이름 표).
  // 앞의 다섯과 같은 어법이고 새로 짓지 않는다: 어디서 나는지도, 무엇에 쓰는지도,
  // 어느 문이 그것을 기다리는지도 없다 — 잇는 것은 관찰자다 (spec SPEC-005 경계 ①)
  [HEAT_CRYSTAL]: '열을 저장하는 결정',
  // ── 재료의 성질 문장 여덟 (C029 ADDED — 판의 '성질' 줄이 이 말들이다) ──────
  //
  // 키를 직접 적지 않고 데이터의 이름을 그대로 받아 쓴다 (이 파일 머리의 규율 그대로) —
  // 색인은 `propertyPhraseCode(재료, 태그)` 하나이고 태그도 `propertyTag(축, 관계)` 로 짓는다.
  // 두 함수의 원본은 content/regions 하나다 (그 파일의 주석: "데이터도 검사도 이 함수 하나로
  // 이름을 짓는다"). 어휘가 축이나 관계의 이름을 옮기면 여기 말도 함께 따라간다.
  //
  // **문장은 지어낸 것이 아니라 기획 문서의 것 그대로다** (L2-World-Access §9.2 "문장
  // (원본 · 그대로)" 열 · RoomBearsMaterial D2 · RoomOfAnotherKind 확정 3). 재료 이름의
  // 선례와 같은 규율이다: 세계관이 이미 부르는 말이 있는데 화면이 다른 말을 하면 관찰자가
  // 본 것과 이 세계가 아는 것이 갈린다.
  //
  // **같은 태그라도 재료마다 말이 다르다** — 태그(light:emits)는 문장의 색인일 뿐이고
  // (spec R5 경계 ② · K7), 관찰되는 것은 붉게 물든 자리와 옅은 붉은 결과 푸른 빛이다.
  // 태그 그 자체는 화면 어디에도 서지 않는다 (spec Observable "투영하지 않는다").
  //
  // **쓰임은 없다** (S10 · SPEC-005 경계 ③) — 무엇으로 만드는지 이 층은 말하지 않는다.
  // 어느 축의 무슨 관계인지도 적지 않는다: 어휘는 데이터의 것이고 관찰되는 것은 문장뿐이다.
  [propertyPhraseCode(BIO_ORE, propertyTag(ASPECT_FLESH, RELATION_STORES))]:
    '살아 있는 것의 몸을 따라 옮겨 다니며 쌓인다',
  [propertyPhraseCode(BIO_ORE, propertyTag(ASPECT_LIGHT, RELATION_EMITS))]:
    '쌓인 자리를 붉게 물들인다',
  [propertyPhraseCode(ORE_EATER_MOLT, propertyTag(ASPECT_LIGHT, RELATION_EMITS))]:
    '붉은 결이 있되 옅다',
  [propertyPhraseCode(GIANT_TREE_FUNGUS, propertyTag(ASPECT_FLESH, RELATION_ABSORBS))]:
    '사체를 삭여 흙을 붉게 되돌린다',
  [propertyPhraseCode(GIANT_TREE_FUNGUS, propertyTag(ASPECT_LIGHT, RELATION_ABSORBS))]:
    '그늘에서만 산다',
  [propertyPhraseCode(FROST_CRYSTAL, propertyTag(ASPECT_HEAT, RELATION_ABSORBS))]: '열을 먹는다',
  [propertyPhraseCode(FROST_CRYSTAL, propertyTag(ASPECT_LIGHT, RELATION_EMITS))]: '푸르게 빛난다',
  [propertyPhraseCode(FROST_CRYSTAL, propertyTag(ASPECT_HEAT, RELATION_GROWS_ON))]:
    '열이 닿으면 자란다',
  // 아홉째 성질 문장 (C030) — 여덟 곁에 한 줄이 늘 뿐이고 어법도 규율도 그대로다.
  // 문장은 기획의 것 그대로다 (Play §6 V25 stores-heat).
  //
  // **쓰임은 여기에도 없다** — Play §5.3 의 관찰은 "빙결 Region 에서 체온을 유지한다" 를
  // 함께 적었으나 그것은 이 재료가 **무엇에 쓰이는가**이고, 그 층은 이 Play 가 세우지
  // 않는다 (§0 · S10 · spec Out of Scope · 기본형 ⑦). 관찰되는 것은 담는다까지다
  [propertyPhraseCode(HEAT_CRYSTAL, propertyTag(ASPECT_HEAT, RELATION_STORES))]: '열을 담는다',
  // 사람·짐승의 종류 (CharacterKind) — kind-presentation · character-catalog 와 같은 이름들
  wanderer: '방랑자',
  'rabbit-swordsman': '토끼 검사',
  // 이름 없는 것의 이름 ② 역할(entity.role) — 종류마저 모를 때 마지막으로 남는 표
  'resource-source': '재료의 원천',
  'region-exit': '출구 표식',
  // 존재가 지금 하는 일 — 사람의 행동 코드(idle · move · mine …)는 위에 이미 있고,
  // 여기 넷은 사람이 아닌 것들의 상태다. 세계가 보내던 코드에 말을 줄 뿐이다
  available: '남아 있다', // 원천이 아직 그 자리에 있다
  depleted: '바닥났다',
  // C013 — 세계가 그것을 되돌리고 있다. **언제 끝나는지 적지 않는다** (남은 시간도 진행도
  // 실려 오지 않는다). 그림과 흙이 그 다음을 말하고, 판은 지금이 어느 때인지만 말한다
  recovering: '되돌아오는 중이다',
  // 그 원천에 지금 걸린 것 (C012 R6 — entity.conditions 의 코드. 판의 '걸린 것' 줄이 이 말이다).
  // **무엇에 매달렸는지도 언제 풀리는지도 적지 않는다** — 세계가 싣는 것은 코드 하나이고,
  // 무엇이 이것을 멎게 했는지는 같은 방에서 관찰자가 잇는다 (흔적을 잇게 한 것과 같은 규율)
  [RECOVERY_STALLED]: '되돌아옴이 멎었다',
  // 탄생이 세우는 원천이 밝힌 **되돌아옴의 원인** (C023 · spec R4 CHANGED). 위의 줄들과
  // 갈리는 자리다: 저것들은 "지금 왜 안 되는가" 이고 이것은 "무엇이 그것을 다시 세우는가"
  // 다. 이 세계에서 그 답이 **시간이 아닌** 유일한 원천이므로 말도 따로 있다 —
  // 아무리 기다려도 오지 않고, 저기서 또 하나가 태어나야 여기 다시 선다.
  // **언제인지는 적지 않는다** (다음 탄생이 언제인지는 실려 오지 않는다 · spec Observable)
  [RECOVERY_NEXT_BIRTH]: '아직 그 자리에 없다 — 다음 탄생이 세운다',
  // 되돌아옴을 **멎게 한** 코드 둘 (C024 · spec R2 CHANGED — 같은 conditions 자리다. 새 자리가 없다).
  //
  // 위의 '되돌아옴이 멎었다'(recovery-stalled)와 **갈려야 한다**: 저것은 매달린 원천이
  // 비었다는 말이고 이것들은 **살아 있는 것이 없다**는 말이다. 저것은 그 원천을 두면
  // 풀리고, 이것은 두어도 풀리지 않는다 — 값이 다시 올라야 한다. 그 갈림이 이 Cycle 이
  // 놓으려는 것이므로(시간이 하는 일이 아니었다) 말도 갈려 있어야 한다.
  //
  // 어법은 조건 코드의 그것 그대로 한 마디다 — **무엇이 없는지도, 몇이면 도는지도,
  // 무엇을 두면 그것이 느는지도 적지 않는다** (개체군의 값도 그 이름도 어디에도 실리지
  // 않는다 · spec Observable "투영하지 않는 것"). 끊어 본 관찰자가 스스로 잇는다.
  //
  // 둘이 한 짝으로 읽힌다 — 벗는 것이 없으면 허물이 없고, 삭이는 것이 없으면 균사가 없다
  [NO_MOLTER]: '벗을 것이 없다',
  [NO_DECOMPOSER]: '삭일 것이 없다',
  // 흐름의 조건 둘 (C014 — 같은 conditions 자리의 코드다. 새 자리가 없다).
  // **얼마나 남았는지도 무엇이 그것을 실어 오는지도 적지 않는다** — 흐름의 주기도 다음
  // 활성까지의 시간도, 그 출발이 어느 방의 무엇인지도 실려 오지 않는다 (spec Observable).
  // 관찰된 사실은 지금이 어느 때인가 하나뿐이고, 언제 물길이 부는지는 어귀에 가서 안다
  [FLOW_ARRIVED]: '지금 실려 오는 중이다',
  [CONDITION_UNMET]: '아직 그때가 아니다',
  // 지나간 것이 있어야 한다 (C035 SPEC-006 — **같은 conditions 자리의 코드다**. 새 자리가 없다).
  // 위의 '아직 그때가 아니다'(condition-unmet)와 **갈려야 한다**: 저것은 **지금의 사실**(지금
  // 지나고 있지 않다)이고 이것은 **과거의 사실**(한 번도 지나지 않았다)이다 — 저것은 기다리면
  // 풀리고 이것은 무엇이 한 번 지나야 풀린다. 둘이 함께 실릴 수 있으므로 말도 갈려 있어야 한다.
  // 어법은 위의 한 마디 그대로다 — **무엇이 지나야 하는지도 · 어느 길인지도 · 언제 지나는지도
  // 적지 않는다** (K8 · spec Observable "투영하지 않는 것"). 흔적을 잇게 한 그 규율이다.
  // 코드 글자는 세계의 상수(content/world/semantic/condition.ts 의 NEEDS_PASSAGE)와 같다 — 이 표는
  // regions 의 이름만 들여오고 세계를 import 하지 않으므로(View 는 GameView Specification 만으로 선다)
  // 글자로 적는다.
  'needs-passage': '지나간 것이 있어야 한다',
  // 다시 자란 자리 (C021 R3 — **같은 conditions 자리의 코드다**. 새 자리가 없다).
  // 위의 줄들과 같은 한 마디 어법이다: **어디서 옮겨 왔는지도 몇 번째 마디인지도 적지 않는다**
  // (spec SPEC-005 경계 ③). 세계가 싣는 것은 "처음 자리가 아니다" 하나이고, 그 원천이 어느
  // 자리에서 여기로 옮겨 왔는지는 캐 본 관찰자가 잇는다 (흔적을 잇게 한 것과 같은 규율)
  [FROST_VEIN_REGROWN]: '여기서 다시 자랐다',
  // 다 캐 간 자리 (C030 R3 — **같은 conditions 자리의 코드다**. 새 자리가 없다).
  // 위의 줄들과 같은 한 마디 어법이다: **언제 다시 더워지는지도 무엇이 그것을 되돌리는지도
  // 적지 않는다** (실려 오지 않는다). 관찰된 사실은 "지금 이 자리가 식었다" 하나이고,
  // 무엇을 캐서 그렇게 되었는지는 캔 관찰자가 안다.
  // 문장은 기획의 것 그대로다 (Play §6 V25 ember-cooled)
  [EMBER_COOLED]: '자리가 식었다',
  // 문 앞에서 일어나는 **현상** (C029 R3 CHANGED — **같은 conditions 자리의 코드다**. 새 자리가 없다).
  //
  // C020 의 `requires-stored-heat`("저장된 열이 있어야 한다")가 서던 자리이고, 그 줄은 여기서
  // 지워졌다. 자리도 형도 그대로이고 바뀐 것은 **그 코드가 무엇을 말하는가** 하나다: 세계는
  // 요구의 이름을 말하지 않고 지금 일어나는 일만 말한다 (spec R3 · K8 — "세계는 답을 알려
  // 주지 않는다"). 그래서 이 말에는 무엇이 그것을 채우는지도, 무엇이 감지하는지도,
  // 답이 어디 있는지도 없다 (spec SPEC-004 경계 ①).
  //
  // 문장은 기획의 것 그대로다 (Play §6 V25 · Region §4.2). 이 코드가 걸렸다고 문이 잠기는
  // 것도 아니다 — 이것은 **표시**이고, 열림/잠김은 아래 두 줄이 말한다
  [ASKS_WARMTH]: '체열이 감지된다',
  // 그 문의 표식이 **완화된 것으로 대신** 실릴 때의 말 (C031 R1 — 위의 줄과 **같은 conditions
  // 자리의 코드다**. 새 자리가 없다).
  //
  // 위의 줄이 서던 자리에 **대신** 선다 — 둘이 곁에 함께 서지 않는다. C021 이 안전의 코드에
  // 세운 그 어법이고(`condition:ridge` → `condition-weak:ridge` · 아래 '산맥이 겨우 막는다'),
  // 같은 것에 판이 두 번 답하지 않게 하려는 것이다. 그래서 이 문구 **하나가 두 마디를 다 진다**:
  // 일어나는 일(체열이 감지된다)과 그것이 무디다는 것(약하다)을 한 줄이 함께 말한다.
  //
  // **무엇이 그것을 무르게 했는지 적지 않는다** — 눈보라라는 말도 그 자락의 이름도 실려 오지
  // 않는다 (spec Observable "투영하지 않는다"). 얼마나 무뎌졌는지도 마찬가지다: 정도도 수치도
  // 없고 약하다는 말 하나다. 무엇이 그렇게 만드는지는 자락을 걸어 들고 나 본 관찰자가 잇는다
  // (산맥이 겨우 막는다 · 김이 푸르게 빛난다 와 같은 규율).
  //
  // 문장은 기획의 것 그대로다 (Play §5.5 관찰의 판 문장 · §6 V25)
  [ASKS_WARMTH_WEAK]: '체열이 감지된다 — 눈보라 속에서 약하다',
  // 문 앞의 자락에 든 **몸**에 실리는 조건 코드 (C029 R2 — 같은 conditions 자리다).
  // 위의 줄이 문에 실리는 말이라면 이것은 내 몸에 실리는 말이고, 둘은 서로를 가리키지
  // 않는다 (잇는 것은 관찰자다 — 흙과 원천을 잇게 한 C011 의 그 규율).
  //
  // **무엇이 그것을 빛나게 하는지도, 그것이 무엇을 뜻하는지도 적지 않는다** (spec
  // Observable "투영하지 않는다"). 관찰된 사실은 "지금 내 몸에서 김이 푸르게 빛난다" 하나다
  [BREATH_GLOWS]: '김이 푸르게 빛난다',
  // ── 탄생지가 쓰는 말 (C022) ──────────────────────────────────
  //
  // 코드의 원본은 content/regions 의 생명 데이터(탄생지의 요구가 밝힌 모자람 코드 · 그 자락이
  // 거는 코드 · 자연 형태)와 세계가 싣는 코드(phase · 역할 · 캘 수 없는 사유)다. 데이터가
  // 이름을 가진 것은 **그 이름을 그대로 받아 쓴다** (이 파일 머리의 규율). 여기서 말이 되고,
  // **말은 게임 안의 것**이어야 한다 — 코드 이름도, 원천·개체군의 데이터 이름도 나오지 않는다.
  //
  // 이름 없는 것의 이름 — 무엇으로 보이는가 하나다 (재료 형태 열넷과 같은 어법).
  // **무엇이 태어나는지도, 언제인지도, 얼마나 찼는지도 적지 않는다** (spec Observable
  // "투영하지 않는 것"). 그것을 잇는 것이 이 Play 다: 관찰자는 허물에 주인이 없다는 것을
  // 알아채고 흔적 넷을 따라와 여기에 닿는다.
  [FORM_ROOT_CLUTCH]: '뿌리 마디에 맺힌 붉은 알집',
  // 탄생지 둘째 (C023 · SPEC-008) — **알집과 갈려야 한다.** 갈리는 것은 크기와 놓임이고
  // (큰 덩이 하나 vs 작은 점 여럿) 그림이 이미 그것을 말한다. 이 말은 그 그림을 옮길 뿐이다.
  // **어느 쪽이 결속이고 어느 쪽이 계승인지 적지 않는다** (spec Observable "투영하지 않는 것") —
  // 알집 없이 맺히는 것을 본 관찰자가 "저희끼리 잇는다" 를 스스로 잇는다
  [FORM_ROOT_EGGS]: '뿌리 마디에 맺힌 작은 붉은 점',
  // 탄생지 셋째 (C024 · SPEC-006) — 앞의 둘과 **계통부터 갈린다.** 저 둘은 뿌리에 맺힌
  // 붉은 것이고 이것은 사체에 돋은 흰 것이다. 그림이 그것을 말하고 이 말은 그림을 옮긴다.
  //
  // **무엇이 되는지 적지 않는다** (spec Observable "투영하지 않는 것") — '균류가 되는
  // 자리' 라고 적으면 화면이 세계보다 많이 아는 것이 된다. 흰 것이 사체를 덮어 가고,
  // 사체가 사라지고, 균사가 다시 서는 것을 본 관찰자가 셋을 잇는다 (허물의 주인을 잇게
  // 한 그 규율 그대로). '맺힌' 이 아니라 '돋은' 인 것도 그래서다 — 셋째 길은 맺히는 것이
  // 아니라 **있던 것이 바뀌는 것**이고, 그 갈림만은 눈에 보이는 사실이다
  [FORM_CARCASS_BLOOM]: '사체의 갈비뼈 사이에 돋은 흰 것',
  // 종류마저 모를 때의 마지막 표 (entity.role) — 원천의 '재료의 원천' 과 같은 자리다.
  // '탄생지' 라고 적지 않는 것은 무엇이 태어나는지를 세계가 싣지 않기 때문이다
  'life-site': '무언가 맺히는 자리',
  // 그 알집이 지금 하는 일 — 원천의 available · depleted · recovering 과 같은 자리의 말이다.
  // 세계가 싣는 phase 코드(dormant · binding)를 그대로 쓰지 않는 것은 앞의 글자가 **방의
  // 위상**이 이미 쓰고 있기 때문이고(위의 '방이 잠들어 있다'), 어느 말을 할지는 화면이
  // 정한다 (life-reading 의 형태 × phase 표 · 핵심 원칙 2).
  //
  // **진행을 말하지 않는다** — 얼마나 찼는지도 언제 다 차는지도 실려 오지 않는다. 갈리는
  // 것은 "지금 멎었는가 · 맺히고 있는가" 하나이고, 그것이 조건 넷과 함께 읽히면
  // 무엇을 캐면 늦어지고 무엇을 두면 오르는지가 관찰자의 것이 된다 (Play §5.5)
  'clutch-dormant': '맺힌 채 멎어 있다',
  'clutch-binding': '속에서 무언가 맺히고 있다',
  // C023 — phase 가 넷이 되었다. **눈으로 갈리는 것은 셋**이다: 맺힌 채 멎음 · 속에서
  // 맺힘 · 터진 것. 태어나는 그 한 tick(born)은 스쳐 가므로 사람이 볼 일이 드물고, 걸어와
  // 보게 되는 "터졌다" 는 언제나 그 다음(spent)이다 (spec 기본형 ⑦).
  //
  // **무엇이 태어났는지 적지 않는다** — 세계가 싣지 않는다. 무엇을 먹었는지도, 언제
  // 돌아오는지도, 다음이 언제인지도 없다. 이 말이 하는 것은 "지금 저기가 어떤가" 하나다
  'clutch-born': '알집이 터진다',
  'clutch-spent': '터진 채 비어 있다',
  // 뿌리의 알의 짝 (C023) — 같은 phase 넷이되 **그 형태의 말**이다. 알집이 터지는 것과
  // 점이 터지는 것은 같은 일이지만 관찰자가 보는 것이 다르므로, 화면도 다른 말을 한다
  'eggs-dormant': '작은 점이 맺힌 채 멎어 있다',
  'eggs-binding': '점 속에서 무언가 맺히고 있다',
  'eggs-born': '점이 터진다',
  'eggs-spent': '터진 채 비어 있다',
  // 변성지의 짝 (C024) — 같은 phase 넷이되 **그 형태의 말**이다. 뿌리의 둘이 "맺힌다" 라면
  // 이것은 "덮어 간다" 다: 맺히는 것이 아니라 **있던 것이 다른 것이 되는 중**이고, 그 갈림이
  // 셋째 탄생 방식의 전부다 (spec SPEC-006). 판이 그 말을 하면 관찰자는 알집 곁에 이것을
  // 두고 "태어나는 길이 하나가 아니다" 를 잇는다.
  //
  // **무엇이 되는지도, 무엇을 먹는지도 적지 않는다** — 세계가 싣지 않는다 (spec Observable).
  // 사체가 사라지는 것을 본 관찰자가 그 둘을 잇는다 (흔적을 잇게 한 것과 같은 규율)
  'bloom-dormant': '돋은 채 멎어 있다',
  'bloom-binding': '흰 것이 사체를 덮어 간다',
  'bloom-born': '흰 것이 부풀어 터진다',
  'bloom-spent': '삭아 내려앉은 채 비어 있다',
  // 지금 **모자란** 조건 넷 (같은 conditions 자리의 코드다 — 새 자리가 없다).
  //
  // 차 있는 조건은 실려 오지 않으므로 여기 서는 것은 늘 **모자란 것**이고, 그래서 넷이
  // 한 줄에 나란히 섰을 때 **무엇이 모자란지가 갈려야 한다**: 하나는 쌓일 것이고, 하나는
  // 뻗어 올 것이고, 하나는 때이고, 하나는 이미 있는 것이다. 넷이 각각 다른 일을 시킨다 —
  // 캐지 말고 두는 것 · 다른 방을 살피는 것 · 기다리는 것 · 어쩔 수 없는 것.
  //
  // **무엇이 그것을 채우는지 적지 않는다** — 어느 원천인지도 어느 방인지도 얼마나 남았는지도
  // 실려 오지 않는다 (spec Observable). 뿌리혹을 캐 보고 균사를 캐 본 관찰자가 잇는다.
  [LIFE_NEEDS_MATERIAL]: '뿌리에 쌓인 것이 모자라다',
  [LIFE_NEEDS_DECAY]: '삭은 것에서 뻗어 온 것이 끊겼다',
  // 비 — **지금 비가 오는가는 실려 오지 않는다**. 세계가 말한 것은 "이 조건이 모자라다"
  // 하나이고, 그 말이 곧 이 줄이다 (언제 오는지도 얼마나 오는지도 적지 않는다)
  [LIFE_NEEDS_RAIN]: '비가 오지 않는다',
  // 개체군 값도 실려 오지 않는다 — 몇인지가 아니라 **이미 있다**는 사실 하나다.
  // 이 Cycle 의 제목이 그 말이고(허물의 주인), 무엇이 주인인지는 적지 않는다
  [LIFE_HAS_OWNER]: '이 자리에 이미 주인이 있다',
  // 다섯째 (C023) — 위의 '이미 주인이 있다' 와 **정확히 반대의 자리**다. 결속은 주인이
  // 있으면 서지 않고, 계승은 이을 것이 없으면 서지 않는다 — 둘이 한 화면에 나란히
  // 섰을 때 그 반대됨이 읽혀야 관찰자가 "처음은 나무가 낳았고 지금은 저희끼리 잇는다" 를
  // 잇는다. **무엇을 이어야 하는지도, 몇이면 되는지도 적지 않는다** (개체군의 값은
  // 어디에도 실리지 않는다 · spec SPEC-005 경계 ①)
  [LIFE_NEEDS_PARENT]: '이을 것이 없다',
  // 여섯째·일곱째 (C024) — 셋째 길(변성)이 밝힌 모자람이다. 앞의 다섯과 **같은 자리**이고
  // 어법도 그대로 한 마디다. 둘이 서로 반대의 일을 시킨다: 하나는 삭을 것이 서기를
  // 기다리는 것이고, 하나는 이미 다 서서 더 할 일이 없다는 것이다.
  //
  // 위의 원천 쪽 '삭일 것이 없다'(no-decomposer)와 **한 글자 차이로 갈린다** — 그쪽은
  // 삭일 **것**(균류)이 없다는 말이고 이쪽은 삭을 **것**(사체)이 없다는 말이다. 둘이
  // 같은 방에서 나란히 읽히면 사슬의 두 끝이 관찰자의 것이 된다.
  //
  // **몇인지도 상한이 얼마인지도 적지 않는다** (개체군의 값은 어디에도 실리지 않는다 ·
  // spec Observable). '더 날 자리가 없다' 는 수를 세어 준 것이 아니라 **지금 저기가
  // 어떤가** 하나다 — 위의 '이 자리에 이미 주인이 있다' 와 같은 어법이다
  [LIFE_NEEDS_CARCASS]: '삭을 것이 없다',
  [LIFE_FUNGUS_CROWDED]: '더 날 자리가 없다',
  // 그 자락 위에 선 동안 걸린 것 (standingConditions — 안전 · 위험 · 닿음의 코드와 **같은
  // 자리**다). 그래서 어법도 그대로다: <무엇>이 <어찌한다> 한 마디이고 몸에 무엇을 하는지도
  // 얼마나 센지도 적지 않는다. 무엇이 떨게 하는지도 없다 — 그 자리에 서 본 관찰자가 잇는다
  [GROUND_TREMOR]: '땅이 낮게 떤다',
  // 캘 수 없는 사유 (알집은 원천이 아니다). 위의 '그런 원천이 없다'(unknown-source)와
  // **갈려야 한다**: 저것은 세계가 그런 것을 모른다는 말이고 이것은 여기 무엇이 서 있되
  // 캘 것이 아니라는 말이다. 그러면 관찰자는 곡괭이가 아니라 다른 것을 찾게 된다
  'not-a-source': '캘 수 있는 것이 아니다',
  open: '열려 있다', // 출구 표식 — 건널 수 있는 길이다
  locked: '잠겨 있다',
  // ── 판에 남는 기록이 쓰는 말 (C028) ──────────────────────────
  //
  // 얼마 전 일인가 — 세계 시각이 초이므로 초로 적는다 (strikes.since · rearrangedAt 이
  // 나이를 재는 그 값과 같은 단위다). 어법은 이어짐 표의 `link.since-last.value`
  // ('{}ms 전') 와 같은 계열이다 — 값 하나와 "전" 뿐이고 문장을 짓지 않는다.
  //
  // **이 한 줄이 나이를 적는 유일한 말이다.** 기록의 줄도, 판의 규칙 줄에 실리는 마지막
  // 재배열도 같은 코드를 부른다 (answer-log 의 agoText) — 같은 값이 두 자리에서 다르게
  // 적히면 둘 중 하나를 믿을 수 없다
  'ago.seconds': '{}초 전',
  // 지목하는 법 (C027 R6) — 보조키의 이름은 pointer-rules 의 원본에서 끼워진다.
  // 어법은 조작 안내의 기존 줄들과 같다 ("이름: 키")
  'hint.designate': '지목: {} + 클릭',
  // 불가 사유 — 통로가 막는다 (C008 RULE-MOVE-001 의 셋째 전제).
  // 위의 둘과 같은 어법이되 **땅의 성질이 아니라 지금의 상태**다: 가파른 곳은 언제나 가파르지만
  // 이 길은 지금 닫혀 있을 뿐이다. "갈 수 없다" 가 아니라 "닫혀 있다" 여야 다시 열릴 수 있다는
  // 것이 문구에서 읽히고, 그것이 이 Play 가 관찰시키려는 것이다.
  // 거절에 **무엇을 하면 되는지**를 함께 싣는다 (RuleBoundRoom 실주행 판정 — 힌트가 있어야 플레이가 된다)
  'passage-closed': '길이 닫혀 있다 — 걸어서 압력을 채우면 길의 배열이 바뀐다',
  // 길이 바뀐 순간 (C008). 무엇이 왜 바뀌었는지는 말하지 않는다 — 압력이 원인이라는 것은
  // HUD 의 압력 줄과 함께 보고 관찰자가 잇는 것이고, 세계가 답을 먼저 주면 이 Play 가 없다
  'maze-rearranged': '길이 바뀌었다',
  // 어느 길이 닫혔는지까지 (RuleBoundRoom 실주행 판정) — 이름은 region-presentation 의 통로 표가 준다
  'maze-rearranged.closed': '길이 바뀌었다 — 닫힌 길: {}',
  // 재배열이 임박했다 — 압력이 임계의 3/4 을 넘겼다
  'maze-pressure-high': '발밑이 울린다 — 길이 곧 바뀐다',
  // 때가 바뀌는 순간의 한 마디 (RoomNeverSame 실주행 판정 · phase-presentation)
  'clock.turned.season': '철이 바뀌었다 — {}',
  'clock.turned.night': '밤이 왔다 — 먼 것이 지워지고 다른 것이 깨어난다',
  'clock.turned.day': '날이 밝았다',
  // 안전한 이유 (C006 R4 — settlement/condition 태그). Play §4 이해:
  // "산맥이 막고 · 강이 먹이고 · 거목이 물린다". 셋은 **왜 여기에 사람이 사는가** 의 답이며
  // (Concept W2: 안전한 구역을 칠하는 것이 아니라 안전할 수 있는 조건을 적는다),
  // 겹치면 셋 다 뜬다 — 하나로 줄이면 "조건이 모여서 도시가 된다" 가 화면에서 사라진다.
  'condition:ridge': '산맥이 막는다', // 외부 생물의 이동 차단 (Concept §3)
  'condition:river': '강이 먹인다', // 식수 + 농업 (Concept §3)
  'condition:tree': '거목이 포식자를 물린다', // 백색 거목 둘레에는 포식자가 오지 않는다 (Concept §3 · §3.2)
  // 옅어진 안전한 이유 (C021 R1 — 위의 조건 코드가 서던 **같은 자리에 대신** 선다).
  // 그래서 위의 셋과 같은 주어 · 같은 갈래의 한 마디여야 한다: 여전히 **안전한 이유**이고
  // (위험의 말투로 넘어가면 어휘 일곱의 자리가 되어 버린다 — spec SPEC-003 경계 ①),
  // 막던 것이 그대로 막되 약해졌다는 것만 갈린다.
  //
  // **무엇이 그것을 약하게 했는지 적지 않는다** — 협곡도 추위도 바람도 이음도 철도 실려 오지
  // 않는다 (spec Observable "투영하지 않는 것"). 그것을 잇는 것이 이 Play 다: 관찰자는 협곡에
  // 가 보고 백왕령에 돌아와서 안다. 얼마나 약해졌는지도 언제 돌아오는지도 마찬가지다
  [weakenedConditionTag(CONDITION_RIDGE)]: '산맥이 겨우 막는다',
  // 조건 셋이 모인 자리 — 그래서 사람이 산다. 조건(왜)과 결과(그래서)를 다른 말로 둔다
  city: '사람이 사는 자리',
  // 위험한 이유 (C016 R3 — hazard 태그. 어휘 일곱은 content/authoring/contracts.ts 의 것이고
  // 데이터가 쓰는 갈래만 여기 말이 된다). **위의 안전한 이유 셋과 같은 자리에 나란히 실리므로**
  // (standingConditions) 같은 어법이어야 한다: <무엇>이 <어찌한다> 한 마디이고, 몸에 무엇을
  // 하는지도 얼마나 센지도 적지 않는다 — 관찰된 것은 "여기가 무엇인가" 하나다.
  // 안전과 위험은 그 한 물음의 두 얼굴이고, 그래서 말투를 가르지 않는다
  'hazard/creature': '짐승이 여기를 제 자리로 삼는다',
  // 협곡이 쓰는 갈래 셋 (C019 — 숲의 짐승과 하나도 겹치지 않는다). 위의 줄과 같은 자리에
  // 나란히 실리므로 어법도 그대로다: <무엇>이 <어찌한다> 한 마디이고, 셋 다 **주어가 세계**다 —
  // 나를 노리는 것이 아니라 세계가 제 일을 하고 있고 내가 그 안에 서 있을 뿐이라는 것이
  // 숲과 협곡을 가르는 것이므로(Play §4 낯섦), 그 차이가 문장의 주어에서 이미 읽혀야 한다.
  //
  // **갈래 이름을 옮겨 적지 않는다** — 기후 · 땅 · 물질은 데이터의 분류이고 관찰자가 보는 것은
  // 눈보라 · 절벽 · 결정면이다. 분류를 화면에 적으면 세계에 없는 눈금이 하나 생긴다
  // (흙의 변색이 "3단계" 를 적지 않는 것과 같은 규율).
  //
  // 셋 다 **무엇이 어떻게 되는지 말하지 않는다.** 눈보라가 앞을 지운다고 적지 않는 것이
  // 특히 그렇다 — 앞이 안 보인다는 것은 화면이 이미 보여 주고 있고, 그것을 이 자락과
  // 잇는 것이 이 Cycle 의 플레이다 (spec Observable "왜 좁아졌는지는 싣지 않는다").
  // 언제 그치는지도 적지 않는다 (같은 목록).
  'hazard/climate': '눈보라가 여기를 깎는다',
  // 땅 — 길이 여기서 끝난다는 사실이지 "못 간다" 가 아니다. 못 간다는 말은 걸어 보았을 때
  // 따로 뜬다 ('too-steep': '너무 가파르다') — 걸린 것의 줄은 발밑이 무엇인가만 말한다.
  // '막는다' 를 쓰지 않은 것은 그 말이 이미 산맥의 것이기 때문이다 — 산맥은 남을 막아 나를
  // 지키고 절벽은 내 길을 끊는다. 같은 자리에 실리는 두 말이 같은 동사를 쓰면 그 차이가 사라진다
  'hazard/terrain': '절벽이 길을 끊는다',
  // 물질 — 여기 무엇이 있는가 하나다. 닿으면 어떻게 되는지는 적지 않는다(그것은 3층의 것이고,
  // 이 줄은 내가 서지 않아도 참인 사실이다). 땅을 뚫고 올라온다는 것만으로 그 면이 무엇을
  // 하는 것인지는 아래의 닿음 줄과 함께 관찰자가 잇는다
  'hazard/matter': '결정면이 땅을 뚫고 올라온다',
  // 갈래 둘이 더 쓰인다 (RoomNeverSame 실주행 판정 — 철을 타는 방이 늘었다). 어법은 위와 같다:
  // 무엇이 어떻게 되는지 말하지 않고 여기 무엇이 있는가만 말한다
  'hazard/ecology': '붉은 곰팡이가 번져 있다',
  'hazard/phenomenon': '문 언저리에서 무언가가 일어난다',
  // 닿아 있다는 사실 (C019 R3 — 위험의 코드와 **같은 자리**에 함께 실리는 접촉 코드).
  //
  // 위의 셋과 다른 물음의 답이다 — 셋이 "이 자리가 무엇인가"(내가 서지 않아도 참)라면
  // 이것은 "지금 내가 그것에 닿아 있다"(내가 서야 참)이다. 그래서 유일하게 **주어가 나**이고,
  // 자락 밖으로 한 걸음 나가면 이 줄만 사라진다 — 그 한 걸음이 두 물음의 차이를 보여 준다.
  //
  // **닿아 있다까지만 적는다.** Play 의 말은 "결정면에 닿으면 결정화된다" 이지만 결정화는
  // 몸에 일어나는 일이고 2층이 하는 것은 말하는 것까지다 (spec R3 경계 ②) — 몸의 값은 한 톨도
  // 달라지지 않으므로, 달라진다고 읽힐 말을 적으면 화면이 세계에 없는 것을 말하게 된다
  crystallizing: '결정면에 닿아 있다',
  // 흙의 변색 다섯 (C011 — trace layer 의 soil-stain 태그. 물었을 때 판의 "흙" 줄이 이 말이다).
  //
  // **짙기의 사다리 한 벌이다** — 다섯 마디가 한 줄로 세워졌을 때 어느 쪽이 더 짙은지가
  // 말만 읽고도 갈려야 한다 (Playable Goal: 짙어지는 쪽을 따라간다). 그래서 다섯이 같은
  // 축 하나(흙이 얼마나 물들었는가)만 말하고 다른 사실을 섞지 않는다.
  //
  // **수를 적지 않는다.** 단계는 데이터의 것이고, 관찰자가 보는 것은 흙뿐이다 —
  // "3단계" 라고 적는 순간 세계에 없는 눈금이 생기고 따라갈 것이 색이 아니라 숫자가 된다.
  // 무엇이 그렇게 만들었는지도 말하지 않는다 (그것을 잇는 것이 이 Play 다).
  [soilStainTag(1)]: '흙 색이 조금 다르다',
  [soilStainTag(2)]: '흙에 붉은 기가 돈다',
  [soilStainTag(3)]: '흙이 붉게 물들었다',
  [soilStainTag(4)]: '붉은 흙이 검게 짙어졌다',
  [soilStainTag(5)]: '흙이 검붉게 절었다',
  // 협곡의 흔적 셋 (C020 — 같은 자리의 **다른 어휘**다. trace layer 도 판의 줄도 그대로이고
  // 갈리는 것은 말과 색뿐이다 · spec SPEC-002).
  //
  // **축이 색이 아니라 온도다** — 위의 다섯이 "흙이 얼마나 물들었는가" 한 축이라면 이 셋은
  // "숨이 어디까지 어는가" 한 축이다 (Play §5.1: 흔적은 색이 아니라 숨이 어는 자리로 읽는다).
  // 협곡에서 흙을 말하면 두 어휘가 한 자리에서 섞인다 (SPEC-002 경계 ②).
  //
  // 셋이 한 줄로 세워졌을 때 어느 쪽이 짙은지가 말만 읽고도 갈려야 하는 것은 위와 같다 —
  // 하얗게 서고 · 얼어 머물고 · 알갱이가 되어 떨어진다. **수를 적지 않고**(단계는 데이터의
  // 것이다) 무엇이 그렇게 만들었는지도 적지 않는다 (그것을 잇는 것이 이 Play 다).
  [frostBreathTag(1)]: '숨이 하얗게 선다',
  [frostBreathTag(2)]: '숨이 눈앞에서 얼어 머문다',
  [frostBreathTag(3)]: '숨이 알갱이가 되어 떨어진다',
  // 거목 속의 흔적 셋 (C030 — 같은 자리의 **셋째 어휘**다. trace layer 도 판의 줄도
  // 그대로이고 갈리는 것은 말과 색뿐이다 · spec SPEC-002).
  //
  // **축은 협곡과 같은 온도이되 방향이 반대다** — 협곡의 셋이 "숨이 어디까지 어는가" 라면
  // 이 셋은 "서리가 어디서 걷히는가" 다 (Play §5.3 Trace: 서리가 앉지 않는 벽 · 오르는 김 ·
  // 갈수록 따뜻함). 거목 속에서 숨을 말하면 두 어휘가 한 자리에서 섞인다.
  //
  // 셋이 한 줄로 세워졌을 때 어느 쪽이 짙은지가 말만 읽고도 갈려야 하는 것은 앞의 둘과
  // 같다 — 성기고 · 걷히고 · 김이 오른다. **수를 적지 않고**(단계는 데이터의 것이다)
  // 무엇이 그렇게 만들었는지도 어느 쪽에 그것이 있는지도 적지 않는다 (그것을 잇는 것이
  // 이 Play 다 · spec Observable "온기의 사다리가 몇 단계인가 · 원천이 어느 쪽인가")
  [emberWarmthTag(1)]: '서리가 성기다',
  [emberWarmthTag(2)]: '서리가 걷히고 공기가 미지근하다',
  [emberWarmthTag(3)]: '벽에서 더운 김이 오른다',
  // ── 방의 소란과 자국이 쓰는 말 (C017 — region.disturbance.phase · 자국의 두 단계) ──
  //
  // 위상 둘은 **세계가 싣는 값 그대로가 코드다** (spec 기본형 ⑧ — 규칙도 코드도 방의
  // 이름을 알지 못하므로 `forest-awake` 같은 말이 없다). 판의 '지금' 줄이 이 말이다.
  //
  // **무엇이 이 방을 깨웠는지 적지 않는다** — 여럿이 함께 했다는 것도, 임계까지 얼마
  // 남았는지도 세계가 싣지 않는다 (spec Observable "싣지 않는다"). 관찰된 사실은
  // "지금 이 방이 어떤가" 하나뿐이고, 왜 그런지는 소란의 값과 자국을 보고 관찰자가 잇는다.
  dormant: '방이 잠들어 있다',
  awake: '방이 깨어났다',
  // 자국의 두 단계 (track-presentation 의 TrackStage). 나이를 재는 것은 관찰자이고,
  // 지면에는 글자가 없으므로(C026 R4) 화면에서 이 둘을 가르는 것은 짙기다 —
  // 여기 말은 그 두 단계를 부르는 이름이다.
  //
  // **누가 남겼는지도 몇 사람이 지나갔는지도 없다** (Play 확정 11). 몇 초 됐는지도 적지
  // 않는다 — 자국이 말하는 것은 방향과 "방금인가 아닌가" 둘뿐이다
  'track-fresh': '갓 난 자국',
  'track-old': '오래된 자국',
  // ── 지나가는 것들의 이름 (C018 — presences[].presence 의 코드 둘) ──────
  //
  // 이 세계의 **정식 이름**이다 (L2-World-Region §5.1 의 이름 표 · Concept §9 · §11).
  // 새로 짓지 않는다 — 세계관이 이미 부르는 말이 있고, 화면이 다른 말을 하면 관찰자가
  // 본 것과 이 세계가 아는 것이 갈린다.
  //
  // **무엇인지의 설명을 붙이지 않는다** — 얼마나 큰지도, 무엇을 남기는지도, 언제 다시
  // 오는지도 없다 (spec Observable "싣지 않는다" · Time T8: 때를 맞추는 것은 관찰자가
  // 배우는 것이다). 판이 말하는 것은 "지금 여기를 이것이 지난다" 하나뿐이고, 그것이
  // 무엇을 하는 것인지는 그늘과 경로 선과 뒤에 남는 것이 스스로 말한다.
  //
  // 지나는 것이 거는 위험 코드(hazard/creature)의 말은 **이미 위에 있다** — 짐승이 제
  // 자리로 삼는다는 사실은 철이 걸든 지나는 것이 걸든 같은 사실이므로 새 말을 짓지 않는다
  // (C016 의 그 줄 · 쓰러짐 줄이 기존 문구를 그대로 쓴 것과 같은 규율).
  'sky-whale': '천공고래',
  'blind-hunter': '맹목의 사냥꾼',
  // 서 있는 떼 (C023) — **같은 자리의 코드**다 (presences[].presence). 위의 둘은 지나가고
  // 이것은 여기 산다는 것만 다르고, 판이 그 갈림을 이름표로 말한다 ('지나는 것' · '서 있는 것').
  //
  // **몇인지 적지 않는다** — 개체군의 값도 상한도 실리지 않는다 (spec SPEC-005 경계 ①).
  // 무엇을 먹는지도, 어디서 났는지도, 늘고 있는지도 없다. 늘었다는 것은 땅에 겹친 자락이
  // 눈으로만 말하고, 그것이 무엇의 떼인지는 이 한 마디가 말한다
  [PRESENCE_ORE_EATER_SWARM]: '광식충 떼',
  // C025 CHANGED — **서 있는 것이 셋이 되었다.** 늘어난 것은 이 표의 두 줄뿐이고, 위의 한
  // 줄이 지키던 규율은 한 글자도 달라지지 않는다 (같은 자리 · 같은 어법 · 같은 침묵).
  //
  // 이름은 **이 세계가 이미 부르는 말**이다 — 새로 짓지 않는다 (지나가는 것 둘에 쓴 그
  // 규율 그대로). 대형 조류는 Concept §4 숲의 생태 사슬이 부르는 말이고, 포식수는 방
  // 이름이 이미 쓰고 있는 정식 이름이다 (PREDATOR_NEST 포식수 둥지).
  //
  // **무엇이 무엇을 부르고 먹는지 적지 않는다** — 새가 광식충을 먹는 것도, 포식수가
  // 새를 줄이는 것도, 그것이 사체를 남긴다는 것도 여기 없다 (spec Observable
  // "투영하지 않는 것": 관계도 문턱도 실리지 않는다). 사슬은 자락이 넓어지고 좁아지는
  // 것과 둥지에 무엇이 서는 것을 여러 철 보아 온 관찰자가 스스로 잇는다.
  //
  // 포식수에만 '떼' 가 붙지 않는 것은 **세계가 그렇게 부르기 때문이다** — 코드가 도는
  // 것(prowl)이지 떼(swarm · flock)가 아니다. 표현이 그 갈림을 지우면 세계가 하지 않은
  // 말을 하는 것이 되고, 붙여 준다고 더 아는 것도 없다 (몇인지는 어차피 실리지 않는다)
  [PRESENCE_BIG_BIRD]: '대형 조류 떼',
  [PRESENCE_PREDATOR]: '포식수',
  // ── 방과 원천이 **기억하는** 것이 쓰는 말 (C034 — 판의 「기억」 줄) ──────
  //
  // 위의 줄들이 전부 "지금 여기가 어떤가" 라면 이 다섯은 **"여기 무슨 일이 있었나"** 다.
  // 그래서 말투가 갈린다 — 위는 현재형('지난다')이고 여기는 지나간 일('지났다')이다.
  //
  // **수를 아라비아 숫자로 적는다** — 나이를 적는 `ago.seconds`('{}초 전')와 같은 어법이고
  // (값 하나와 한 마디뿐 · 문장을 짓지 않는다), 셈에는 상한이 없으므로(Play 확정 3) 세는
  // 말의 표를 따로 두면 큰 수에서 말을 잃는다. 흙의 단계에 수를 적지 않는 것과 갈리는
  // 자리다: 저기서 수는 데이터의 눈금이지만 여기서는 **세계가 센 것 그 자체**다.
  //
  // **누가 했는지는 어디에도 없다** (spec Observable "투영하지 않는다" · T2.7) — 방이
  // 세는 것은 우리가 한 일이지 누구의 일이 아니다. **언제 다시 일어나는지도 없다** —
  // 세계는 지나간 것만 말한다.
  'memory.taken': '{}번 캐였다',
  // 마지막 고갈이 얼마 전인가 — 값은 나이를 적는 그 한 말(`ago.seconds`)이 지은 것이
  // 그대로 들어온다. **몇 번 고갈되었는지는 적지 않는다** (spec SPEC-006 이 부른 값은
  // 캐인 횟수와 마지막 고갈의 나이 둘이다). 무엇이 그것을 되돌렸는지도 적지 않는다
  'memory.depleted-last': '마지막 고갈 {}',
  // 방이 겪은 것 둘 — 이름표('기억')가 무엇을 본 것인지를 말하므로 값은 **무엇이 몇 번인지**
  // 하나씩이다. 깨어남에 시각을 붙이지 않는 것은 Playable Goal 의 문장이 그러하기 때문이고
  // (뒤척임 N번 · 깨어남 N번 · 무엇이 N번 지났다(마지막 N초 전)), 판이 세로로 길면 몸을
  // 가린다는 부채가 이미 있어 한 줄에 담을 마디를 늘리지 않는다
  'memory.turns': '뒤척임 {}번',
  'memory.awakenings': '깨어남 {}번',
  // 지나간 것 — **앞에 지나는 것의 이름이 선다** (그 이름은 바로 위의 표가 이미 옮긴다:
  // 새 말을 짓지 않는다). 이름을 이 말의 자리에 넣지 않는 것은 값이 둘(이름 · 횟수)이기
  // 때문이고, 무엇이 몇 번인지가 한 마디로 읽혀야 하므로 사이를 ' · ' 로 가르지 않는다
  'memory.passage': '{}번 지났다',
  // 그 지나감이 마지막으로 얼마 전이었나. **괄호로 묶는 것**은 한 줄에 마디가 여럿 설 때
  // (뒤척임 · 깨어남 · 지나감 여럿) 이 나이가 **바로 앞의 지나감의 것**임이 갈려야 하기
  // 때문이다 — 원천의 판은 마디가 둘뿐이라 괄호가 필요 없다
  'memory.passage-last': '(마지막 {})',
  // 태어난 것 — C037. **지나감과 같은 모양**이다 (앞에 그 탄생지의 이름이 서고, 이름과 셈
  // 사이를 가르지 않는다): 방이 세는 것이 같은 갈래의 사실이므로 어법도 한 벌이어야 한다.
  // 말이 지나간 일('태어났다')인 것도 이 여섯 줄이 함께 지키는 것이고, **무엇이 태어났는지도
  // 언제 다시 태어나는지도 적지 않는다** — 세계가 싣는 것은 어디서 몇 번인가 하나다
  'memory.birth': '{}번 태어났다',
  // 그 태어남이 마지막으로 얼마 전이었나. 지나감의 것과 **글자가 같은 것은 어법이 같기
  // 때문**이고, 열쇠를 따로 두는 것은 그 마디의 주인이 다르기 때문이다 — 한 줄에 지나감과
  // 태어남이 나란히 설 때 이 나이가 바로 앞 마디의 것임이 갈려야 한다
  'memory.birth-last': '(마지막 {})',
  // ── 그것을 **어떻게 알게 되는가** (C036 — 판의 「할 수 있는 것」 줄 끝의 한 마디) ──────
  //
  // 위의 줄들이 "지금 여기가 어떤가" 와 "여기 무슨 일이 있었나" 라면 이 넷은 **"이것을
  // 내가 어떻게 아는가"** 다. 그래서 말투가 또 갈린다 — 앞의 것들은 세계의 사실을 적지만
  // 이 넷은 그 사실이 **나에게 오는 길**을 적는다.
  //
  // 말은 **사람의 말**이어야 한다 (이 표의 규율) — 코드 글자(VISIBLE · TRACE · SIGNAL)도,
  // 기회라는 말도, 그 기회의 id(`gather:…` · `cross:…`)도 화면에 나오지 않는다. 셋이
  // 나란히 서는 한 벌이므로 어법도 한 벌이다: **주어를 밝힌 한 마디 현재형**이고, 무엇이
  // 보이게 하는지도 · 무엇이 흔적을 남겼는지도 · 어디서 신호가 오는지도 적지 않는다
  // (spec Observable "투영하지 않는 것" — 실려 오는 것은 코드 하나다).
  //
  // 셋의 갈림이 곧 이 Cycle 이 놓으려는 것이다 (spec Experience Intent): 같은 방의 두
  // 원천이 하나는 흔적으로 하나는 신호로 읽힌다. 그래서 '보인다' 한 낱말로 두지 않고
  // **무엇을 통해 아는가**를 셋 다 앞머리에 세운다 — 눈 · 흔적 · 신호.
  //
  // **discovery 는 무엇을 할 수 있는가를 바꾸지 않는다** (spec SPEC-005 경계 ②) — 이
  // 말들은 판정이 아니라 그 판정이 나에게 알려진 길이고, 거절 사유의 문구와 나란히 선다
  'discovery-visible': '눈에 보인다',
  'discovery-trace': '흔적이 말한다',
  'discovery-signal': '신호로 온다',
  // 넷째(HIDDEN)의 말은 **여기 없다** — C038 부터 세계가 아직 드러나지 않은 기회를 아예 실어
  // 보내지 않기 때문이다 (RULE-OPPORTUNITY-NAME-001 의 거름 · C038 spec SPEC-001). 그 값이
  // 화면에 닿을 길이 없으므로 닿지 않는 말을 표에 두지 않는다 (기본형 ⑤). 무엇이 그것을
  // 드러내는가와 드러난 뒤 무엇으로 읽히는가는 3층이 함께 가져온다
  // ── 때가 있는 기회가 **닫혀 있을 때** (C037 — 같은 줄의 한 마디) ──────
  //
  // 바로 위의 넷이 "이것을 내가 어떻게 아는가" 라면 이 하나는 **"지금 그것이 서 있는가"** 다.
  // 같은 줄에 나란히 서지만 말하는 것이 갈리므로 열쇠의 갈래도 갈린다 (discovery-* 는 세계가
  // 실은 값에서 열쇠가 지어지고, 이것은 참·거짓 둘에서 이 한 마디가 서거나 서지 않는다).
  //
  // 말은 **지금 하나**다 — 언제까지인지도(240 초) 무엇이 그것을 여는지도(고래가 지나감)
  // 적지 않는다. 봉투가 그것을 싣지 않기도 하지만(spec Observable "투영하지 않는 것"),
  // 그 침묵이 이 Cycle 이 놓으려는 것 자체이기 때문이다 — 되돌리는 것은 시간이 아니고,
  // "언제 다시" 는 끝까지 말해지지 않는다 (RoomNeverSame 확정 9 · spec Out of Scope).
  //
  // '없다' 인 것은 그 자리가 **비어 있다**는 사실이 관찰된 것이기 때문이다 — '못 한다'(판정)도
  // '아직'(때를 안다는 말)도 아니다. 거절 사유들과 나란히 서면서도 사유가 아니어야 한다
  'opportunity.closed': '지금은 없다',
  // ── 세계의 때가 쓰는 말 (C015 — snapshot.clock 의 코드 둘) ──────
  //
  // 세계는 코드만 싣는다 (DAY · NIGHT · STILL · SEEP · LONG_NIGHT · TURN). 여기서 처음
  // 사람이 읽는 말이 되고, 그 말은 **지금이 언제인가 하나뿐**이다 — 얼마나 남았는지도
  // 다음이 무엇인지도 며칠째인지도 적지 않는다 (spec SPEC-007 경계 ① · Time T8).
  // 철 넷의 이름은 Play 확정 1 의 것 그대로다.
  'clock.day': '낮',
  'clock.night': '밤',
  'clock.season.still': '고요',
  'clock.season.seep': '스밈',
  'clock.season.long-night': '긴 밤',
  'clock.season.turn': '뒤척임',
  // 명령이 무엇을 하는가 (Command.Effect)
  'set-attribute': '존재의 속성 값을 바꾼다',
  // 돌아가기 (C009 RULE-EMERGENCY-RETURN-001). **방을 건너지 않는다** — 같은 방 안의
  // 비상 자리로 몸만 옮기는 것이므로 "나간다" 가 아니라 "돌아간다" 여야 한다.
  // 어느 자리인지는 방마다 다른 데이터이므로 말하지 않는다 (미로의 것은 입구지만,
  // 그 사실을 문구가 미리 알려 주면 세계가 하지 않은 말이 된다)
  'emergency-return': '이 방의 비상 자리로 돌아간다',
  'collider-observe': '몸과 휘두름의 충돌체를 보인다',
  'attribute-inspect': '존재의 모든 속성을 그 몸 위에 펼친다',
  // 명령이 받는 자리 (Parameter.Id)
  'param:target': '대상',
  'param:attribute': '속성',
  'param:value': '값',
  // 비워 두면 무엇이 되는가 (Parameter.OmittedMeaning)
  'omitted:self': '내 몸',
  // ── 기반이 부르는 문구 (반전 ⑤) ────────────────────────────────
  //
  // 기반은 사람이 읽을 말을 짓지 않는다 — 코드로 부르고 팩이 말을 준다.
  // 목록의 단일 출처는 engine/view-kernel/presentation/text-codes.ts 의
  // ENGINE_TEXT_CODES 다. `{}` 자리는 기반이 값을 끼운다.
  //
  // 명령 표면
  'command.domain.entity': '존재의 이름',
  'command.domain.previous': '앞에서 고른 것이 정하는 값',
  'command.domain.value': '값',
  'command.state.on': '켜짐',
  'command.state.off': '꺼짐',
  'command.origin.world': '세계',
  'command.origin.observer': '내 화면',
  'command.unavailable': '지금은 걸 수 없다',
  'command.omitted': '비우면 {}',
  'command.omitted.nothing': '없음',
  'command.next': '다음: {}',
  'command.close': '닫기',
  'command.no-such': '그런 명령이 없다 — {}',
  'command.takes-nothing': '{} 은 아무것도 받지 않는다',
  'command.out-of-range': '허용된 범위를 벗어난 값이다 — {}',
  'command.not-here': '그 자리에 넣을 수 없다 — {}',
  'command.leftover': '받지 않는 것이 남았다 — {}',
  'command.incomplete': '아직 다 적지 않았다',
  // 겹침 표면 · 칸 띠 (기반 capability — 이 세계는 아직 쓰지 않는다)
  'surface.close': '닫기',
  'surface.empty-cell': '빈 자리',
  'surface.state.available': '가능',
  'surface.state.blocked': '불가',
  'surface.state.pending': '기다리는 중',
  'slot.key': '{} 키',
  'slot.no-key': '부를 수 없음',
  // 기반이 스스로 듣는 키의 이름
  'engine.key.command': '명령',
  'engine.key.move': '이동',
  'engine.key.turn': '시점',
  'engine.key.colliderObserve': '충돌체 관찰',
  'engine.key.attributeInspect': '속성 관찰',
  // 이어짐
  'link.state.connected': '세계와 이어짐',
  'link.state.connecting': '세계에 잇는 중…',
  'link.state.disconnected': '세계와 끊김 — 마지막으로 본 모습입니다',
  'link.round-trip': '왕복',
  'link.arrival-rate': '수신',
  'link.since-last': '마지막',
  'link.since-last.value': '{}ms 전',
  'link.sent': '보냄',
  'link.reconnects': '재연결',
  'binding.observer': '나',
  'binding.character': '내 몸',
  'binding.world': '세계',
  'binding.world.in-process': '(같은 프로세스)',
  // 자기 자원 막대를 부르는 말
  'self.health': 'HP',
  'self.energy': 'CP',
};

/**
 * 코드를 사람이 읽는 말로.
 *
 * `detail` 은 **문장에 끼울 값**이다 (친 낱말 · 범위 밖의 값 · 남은 낱말). 등록된 문구에
 * `{}` 가 있으면 그 자리에 들어가고, 없으면 값은 버려진다 — 문장이 값을 부르지 않는데
 * 뒤에 억지로 붙이면 말이 아니라 찌꺼기가 된다.
 *
 * 등록되지 않은 코드는 **코드 그대로**이며, 값이 있으면 코드 뒤에 붙는다 —
 * 표현 누락이 게임을 멈추지 않고, 무엇이 빠졌는지는 화면에 그대로 드러난다.
 */
export function codeText(code: string, detail?: string): string {
  const text = CODE_TEXT[code];
  if (text === undefined) return detail === undefined ? code : `${code}: ${detail}`;
  return detail === undefined ? text : text.replace('{}', detail);
}

/**
 * 어떻게 알게 되는가의 **코드 글자를 짓는다** — C036 ADDED (spec SPEC-005).
 *
 * 세계가 싣는 것은 자기 어휘의 값(`VISIBLE` · `SIGNAL` · `TRACE` · `HIDDEN`)이고, 위 표의
 * 열쇠는 이 함수가 그 값에서 짓는다 (`TRACE` → `discovery-trace`). 표에 그 값의 글자를
 * 그대로 적지 않는 것은 세계의 어휘와 화면의 코드가 **다른 두 이름**이기 때문이고
 * (soilStainTag 가 데이터의 이름으로 열쇠를 짓는 그 어법), 짓는 자리가 하나여야 세계가
 * 값을 하나 더 실어 올 때 화면이 조용히 옛 말을 하지 않는다.
 *
 * 표에 없는 값(자리만인 NPC · KNOWLEDGE)은 지어낸 말이 아니라 **코드 그대로** 뜬다 —
 * 표현 누락이 판을 멈추지 않고, 무엇이 빠졌는지가 화면에 드러난다 (이 파일의 규율).
 */
export function discoveryCode(discovery: string): string {
  return `discovery-${discovery.toLowerCase()}`;
}
