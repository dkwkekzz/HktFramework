// Biome Rules — 지면의 표면·통행 규칙 표를 **다시 내보내는 얇은 자리** (C006 CHANGED).
//
// C005 에서는 값이 여기 있었다. 그때는 **관찰자만** 땅을 컴파일했기 때문이다 — 표가 View 에
// 사는 것이 맞았고, "세계가 통행 임계를 읽어야 하는 순간 자리를 다시 정한다" 고 그 주석이
// 예고해 두었다. C006 이 그 순간이다: 이동 규칙(RULE-MOVE-001)이 traversable 을 읽으므로
// world 와 view 가 **같은 표**를 읽어야 하고, 값이 갈리면 세계가 막는 자리와 화면이 그리는
// 자리가 어긋난다 (SPEC-006).
//
// 그래서 값의 원본은 `content/regions/terrain-rules.ts` 로 옮겼다 — 경계 규칙 4 가 이미
// 정해 둔 "world 와 view 가 함께 읽는 데이터" 의 자리다 (regions 는 engine 만 import 한다).
// **값은 한 글자도 바뀌지 않았다. 자리만 옮겼다** (C006 spec — 규칙 표의 자리).
//
// 이 파일이 남아 있는 이유는 둘이다.
//   ① 기존 import 경로(`./biome-rules`)가 그대로 살아 있게 — 표가 이사했다는 사실이
//      View 안에서 **한 자리**로만 알려지면 된다.
//   ② View 가 표를 부르는 문(門)은 계속 View 안에 있게 — 표현(색·문구·크기)은 여전히
//      content/view 의 것이고, 그것들이 읽는 규칙 값이 어디서 오는지는 여기가 답한다.

export * from '../regions/terrain-rules';
