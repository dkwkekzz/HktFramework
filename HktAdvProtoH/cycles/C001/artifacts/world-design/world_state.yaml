# S003 / C001-SEMANTIC-001 — World State (세계의 사실만, 구현 내부 상태 금지)
world_state:
  Actor:
    Position: Position
    Inventory: Inventory        # ResourceType/Tool -> amount
    Knowledge: Knowledge        # 알고 있는 Deposit 목록
    CurrentAction: ActionState  # Idle | Mine
  ResourceDeposit:
    Position: Position
    ResourceType: ResourceType  # 이번 Cycle 은 Stone 고정
    ResourceAmount: ResourceAmount
  constants:
    InteractionRange: InteractionRange
    ExtractAmount: 1
