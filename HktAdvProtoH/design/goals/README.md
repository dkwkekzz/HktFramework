# Goals

Cycle Goal 후보 저장소. 각 Goal 은 기술 작업 목록이 아니라
**세계에서 가능한 작고 직접 플레이 가능한 경험**으로 기술한다.

예:

```
Pickaxe를 가진 Player가 Stone Deposit에 접근하여
Mine을 수행하고 Stone을 획득할 수 있다.
```

Orchestrator 가 `START_CYCLE` 시 여기서 Goal 을 가져가 `cycles/<id>/goal.yaml` 로 고정한다.
