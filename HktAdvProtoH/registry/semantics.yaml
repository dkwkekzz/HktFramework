# Semantic Registry — World 의미의 전역 Source of Truth (§12.1)
# 규칙:
#   - World Semantic Design 은 새 의미 정의 전 반드시 이 파일을 조회한다 (lookup-first).
#   - 존재하면 Reuse, 없을 때만 Semantic Delta 로 추가한다. Capability 별 중복 정의 금지 (Rule 11).
#   - 추정해서 등록하지 않는다 — 검증된 Cycle 산출물만 등록한다 (Bootstrap Step 4).
# 상태:
#   ACTIVE     — 실제 구현·검증된 semantic. 재사용 대상.
#   DRY_RUN    — 파이프라인 Dry Run 산출물. 설계만 존재, 재사용 불가.
#   DEPRECATED — Version Migration 으로 대체됨.
# C001 DRY_RUN 9종은 C002 에서 실구현·검증 후 ACTIVE 로 승격되었다 (+ 신규 2종).
semantics:
  Actor:
    kind: ENTITY
    status: ACTIVE
    defined_by: C002
  Position:
    kind: STATE
    status: ACTIVE
    defined_by: C002
  Inventory:
    kind: STATE
    status: ACTIVE
    defined_by: C002
  Knowledge:
    kind: STATE
    status: ACTIVE
    defined_by: C002
  Actor.CurrentAction:
    kind: STATE
    status: ACTIVE
    defined_by: C002
  Tool:
    kind: ENTITY
    status: ACTIVE
    defined_by: C002
  Tool.Capability:
    kind: STATE
    status: ACTIVE
    defined_by: C002
  ResourceDeposit:
    kind: ENTITY
    status: ACTIVE
    defined_by: C002
  ResourceDeposit.ResourceAmount:
    kind: STATE
    status: ACTIVE
    defined_by: C002
  InteractionRange:
    kind: RULE_CONSTANT
    status: ACTIVE
    defined_by: C002
  MoveSpeed:
    kind: RULE_CONSTANT
    status: ACTIVE
    defined_by: C002
