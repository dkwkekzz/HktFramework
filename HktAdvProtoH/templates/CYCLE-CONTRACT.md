# CYCLE-<NNN> — Cycle Contract

> 상태: `DRAFT` | `CONFIRMED`
> Stage 0 산출물. CONFIRMED 전에는 Stage 1 을 시작하지 않는다.

## Cycle ID

```text
CYCLE-<NNN>
```

## Target Horizon

```text
Persistent Open World MMORPG
```

## Capability Added

이번 Cycle 에서 세계에 추가할 **하나의** 능력. 시스템 이름이 아니라 세계 능력으로 쓴다.

```text

```

## Existing Semantics Used

기존 World Baseline 에서 **실제로 재사용**하는 의미. (첫 Cycle 이면 `없음`.)

```text

```

## New Semantics

이번 Cycle 에서 새로 추가할 의미의 **범위 선언**. 최종 정의는 Stage 2 의 몫이다.

```text

```

## Goal / Possibility Scope

```text
Goal:

Possibility:

```

이 범위 밖의 Goal / Possibility 는 다루지 않는다.

## Observable Proof

인간이 무엇을 보면 이 Cycle 이 끝났다고 인정하는가. 구체적 출력 형태로 쓴다.

```text

```

실패 경로도 함께 쓴다 — Precondition 이 거짓일 때 무엇이 보이는가.

```text

```

## Explicitly Deferred

이번 Cycle 에서 구현하지 않는 것. 비워 두지 않는다.

```text

```

## Evolution Questions

Stage 6 가 그대로 답할 수 있는 형태로 쓴다.

```text
Q1
Q2
Q3
Q4
Q5
```
