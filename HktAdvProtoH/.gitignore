node_modules/
dist/

# 세계 스냅샷 — server/world-store.ts 가 쓰는 영속 파일 (로컬 세계의 것이다)
.world/

# Master Graph 뷰어 — npm run master:graph 가 만드는 생성물 (GRAPH.md 만 커밋한다)
content/*/master/graph/graph-view.html
content/*/master/graph/graph-view.artifact.html
content/*/master/graph/concept-map.html
content/*/master/graph/concept-map.artifact.html

# Lanes 관찰판 — npm run lanes 가 만드는 생성물 (LANES.md 만 커밋한다)
content/*/LANES.html
content/*/LANES.artifact.html

# Surface Lab — npx tsx tools/surface-lab/build.ts 가 만드는 생성물
tools/surface-lab/out/
