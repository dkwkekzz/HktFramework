node_modules/
dist/

# 생성물 — motions/ 를 훑어 만든다 (tools/motion-atlas).
#
# 커밋하지 않는다. 개발 서버가 motions/ 를 지켜보며 다시 만들기 때문에, 커밋해 두면
# 서버가 도는 동안 파일이 바뀌어 working tree 가 더러워지고 pull 이 막힌다.
# 필요할 때 저절로 만들어진다 — 개발 서버·빌드·테스트가 모두 먼저 돌린다.
view/motion/motion-atlas.generated.ts
