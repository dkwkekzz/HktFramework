# Open Questions — Human 결정 대기

Master Layer 작업 중 Agent 가 **임의로 결정하지 않고 남긴 것**들이다.
Constraint 승인 · Constraint 충돌 · 설계 공백 · Trade-off 가 여기 모인다.

```text
Agent 가 쓴다      질문 · 영향 범위 · 선택지 · 각 선택의 결과
Human 이 답한다    DECISION 줄
```

답이 정해지면 해당 Node/Constraint 에 반영하고 **이 파일에서 지운 뒤 결정 내용을
[HISTORY.md](HISTORY.md) 로 옮긴다.**

미해결 **8건** — 닫힌 질문은 HISTORY.md. **어느 트랙도 막지 않는다** — 다섯 트랙 전부
후보(또는 SELECTED)가 서서 Human 선택 대기이며, 그 현황은 LANES.md 가 소유한다.

    Q29        통찰을 Graph 에서 어떻게 잡을 것인가
    Q35        **스킬(SK)** 이 남긴 것 — 그 다음 전투 Cycle 의 크기를 정한다
    Q36        이미 세계에 있는 물건의 유래 문제
    Q37 · Q38  다음 **아이템** Cycle 이 마주할 것들
    Q46 · Q67  **공정** 질문 — 게임의 의미가 아니라 두 층·병렬 레인이 굴러가는
               방식을 묻는다 (SELECTED·번호의 소유 · 레인이 공유하는 자리)
    Q66        Overlay 를 노드의 완결로 판정하는가, 플레이의 성립으로 판정하는가

번호가 띄엄띄엄한 것은 앞선 질문들이 이미 닫혀 HISTORY 로 갔기 때문이다 —
번호는 재사용하지 않는다.

## Q29. "아는 힘"(통찰)은 독립한 Capability 노드인가 — OPEN

    무엇          C016 이 세계에 세운 통찰(살펴보지 않고도 아는 힘)을 Graph 에서
                  어떻게 잡을 것인가. 지금은 MC-OBSERVE 의 **경로 하나**로 담겨 있고
                  별도 노드가 없다. C016 08 이 "성장 축으로 보면 독립한 노드일 수 있다"
                  를 보고하며 판단을 Master 로 넘겼다.

    영향          지금 당장 막히는 것은 없다 — 통찰은 MC-OBSERVE 안에서 실측으로
                  닫혔고 Frontier 판정도 그 상태로 나왔다.
                  다만 성장 축이 세계에 들어오는 순간(FR-WHAT-YOU-GATHER-CHANGES-YOUR-BODY)
                  "무엇을 올리는가" 의 목록이 필요해지고, 그때 통찰이 능력치들과 같은
                  자리에 있어야 하는지가 실제 문제가 된다.

    선택지        (a) 노드를 세우지 않는다 (현행 유지)
                      → 통찰은 MC-OBSERVE 의 경로로 남는다. SCHEMA 의 "required_by 와
                        demanded_by 가 둘 다 비면 노드가 아니다" 를 지킨다 —
                        지금 통찰을 **따로** 요구하는 Possibility 가 하나도 없다.
                        Graph 가 가벼워지는 대신, 성장 축이 올 때 다시 물어야 한다.
                  (b) MC-INSIGHT 를 신설한다
                      → 성장으로 올리는 대상이 명시적으로 잡힌다. 그러나 지금은
                        required_by 를 채울 Possibility 가 없어 노드 규칙을 어긴다.
                        세우려면 "아는 힘으로 감당한다" 는 Possibility 를 먼저
                        세워야 하고, 그것은 OPTIONS 작업이다.
                  (c) 성장 축 Cycle 이 열릴 때로 미룬다
                      → 그때는 요구가 실제로 생기므로 (b) 의 걸림돌이 사라진다.
                        Agent 판단으로는 이쪽이 "필요가 먼저" 원칙에 가장 맞는다.

    DECISION      PENDING

## Q35. "여러 가지 공격 방식" 은 층인가, 정의가 고르는 갈래인가 — OPEN

    무엇          Human 이 C020 을 닫으며 위층으로 전달을 지시했다 —
                  "추천대로 하고 여러가지 공격방식을 이후 구현해야 함 master로 전달".

                  지금 세계의 공격은 휘두름 하나의 모양뿐이었고(칼끝이 호를 그리며
                  쓸고 지나간다 — C006), C020 이 그 옆에 다른 모양 하나를 세웠다:
                  고른 것에게 거리를 두고 위력을 전한다.

    이미 데이터인 것   위력(BaseDamage · AttackRatio · DamageType) · 닿는 거리 ·
                      쓰는 시간 · 대상 요구 · 치르는 것 · 효과 갈래 — 전부 정의가 지닌다.
                      새 갈래를 더할 때 규칙이 열리지 않는 자리다.

    아직 없는 것       투사체(날아가는 것 · 비행 시간 · 도중의 차단 — C020 의 던지기는
                      즉시 닿는다) · 지속 효과 · 빗나감과 엄폐

                      **휘두름의 모양은 여기서 빠졌다** — C025 가 각 · 길이 · 굵기를
                      전역 상수에서 기술 정의로 내렸다. 아래 (a) 가 "선행 작업" 이라
                      부른 것이 그것이며, 그 일은 끝났다. 여럿을 함께 치는 것도 모양의
                      결과로 이미 일어난다 (넓은 기술이 옆까지 훑는다)

    영향          이 답이 Cycle 하나의 크기를 정한다. "갈래" 라면 스킬도 아이템과 같은
                  자리로 내려와야 하고(지금 스킬은 `SKILL_DEFINITIONS` 라는 별도 표를 쓴다),
                  "층" 이라면 전투 사다리에 칸이 하나 생기고 DC-COMBAT-ONE-LAYER-AT-A-TIME
                  이 한 번에 열 수 있는 양을 제한한다.

    선택지        (a) **정의가 고르는 갈래다** — 공격 방식은 층이 아니다.
                      → 스킬 정의와 아이템 정의가 같은 모양을 갖게 정리하고, 새 방식은
                        정의를 더하는 일이 된다. 갈래를 늘리는 것은 층을 올리는 것이
                        아니므로 ONE-LAYER-AT-A-TIME 과 부딪히지 않는다.
                        C020 이 실제로 그렇게 만들었으므로 세계의 현재 형태와 맞는다.
                      → 대신 "휘두름의 모양" 을 정의로 꺼내는 일이 선행 작업이 된다.
                        **이 선행 작업은 C025 로 끝났다** — (a) 를 고르는 데 남은
                        걸림돌이 하나 줄었다. 그러나 이 질문 자체는 열려 있다:
                        선행 작업이 끝난 것과 층이냐 갈래냐를 정하는 것은 다르다.
                  (b) **층이다** — 방식마다 새 판정이 필요하다.
                      → 투사체(비행 시간 · 도중 차단)는 실제로 새 판정이다. 그것만
                        따로 층으로 보고 나머지는 갈래로 두는 절충도 여기 속한다.
                  (c) 지금 정하지 않는다 — 다음 전투 Cycle 이 열릴 때 그 자리에서 판단한다.

    7 조건 2      **충족됐다 (2026-08-27 · Q71(b) 확장).** `MP-REACH-THE-UNREACHABLE` 이
                  `MC-PLACE-FOOTING`(딛고 설 것이 없는 자리에 딛고 설 것을 둔다)을 요구하고,
                  그 노드가 MS-SKILL-FORM 의 **공간 존재(SPATIAL-PRESENCE)** 칸에 속한다.
                  근거는 BT §8.7("정박암을 임시 발판으로 설치")과 §7.7("공기 덩어리를
                  길처럼 연결")이며, 두 대지형이 그것을 각자 요구한다.
                  그러므로 이 질문을 막던 배선의 부재는 사라졌다 — 남은 것은 **층이냐
                  갈래냐** 라는 이 질문 자체와, 세계의 축 두 가지다: 몸이 아닌 것이
                  세계의 자리를 갖는 일, 그리고 위아래(`WorldPosition` 은 x·z 뿐 —
                  그 축은 기반에 걸려 ENGINE 트랙 작업이다).

    관련           Human 이 이 질문에 답할 문서를 넣었고, 2026-08-21 그것을 최종안으로
                  확장·분할했다 — `content/proto-adventure/design/Skill/`(SK, 주입 완료. 구판 R0 은 삭제).
                  그 문서가 긋는 선은 (b) 의 절충 쪽이다: **개별 스킬은 갈래**이고
                  (파이어볼·메테오·장판은 Content 이며 시스템 타입이 아니다 — SK §7),
                  **실행 형태만 층**이다 (새 형태는 세계에 다른 생명주기·판정이 필요할
                  때만 선다 — SK §6). 그 사다리는 `graph/systems.yaml` 의
                  MS-SKILL-FORM 으로 등록되었고 지금 차 있는 칸은 접촉(CONTACT) 하나다.
                  C020 의 던지기가 SK §8 의 대상 직접 실행과 같은 모양이지만, SK 가 그
                  노드를 지목하지 않았으므로 DIRECT 칸은 비워 두었다.

                  최종안이 이 질문에 더한 것은 **층의 수가 줄었다**는 사실이다.
                  자리는 열넷에서 여섯이 되었다 — 투사체·장판·함정이 서로 다른 형태가
                  아니라 같은 공간 존재의 값 차이로 되돌아갔기 때문이다 (SK §5).
                  따라서 (b) 를 고르더라도 열어야 할 층의 총량은 전보다 작다.
                  여전히 이 질문이 열려 있는 이유는 그대로다 — 어느 Possibility 가
                  그 자리를 요구하는지를 SK 도 공급하지 않는다 (위 7 조건 2).

                  원칙 셋(IS-COMBINATION-NOT-NAME · DELIVERY-IS-NOT-EFFECT ·
                  COMBINE-BEFORE-NEW-FORM)은 Human 이 승인해 Active 이며(HISTORY Q41),
                  근거만 SK 로 옮겨 REVISED 다. 최종안이 새로 명시한 셋도 승인되어
                  Active 다 (HISTORY Q45) — 그중 ANCHOR-IS-NOT-RESOLUTION 은
                  MC-COMBAT-STRIKE 에서 SATISFIED 다.

    DECISION      PENDING

## Q46. 레인이 둘일 때 `SELECTED` 와 Cycle 번호를 누가 잡는가 — OPEN

    무엇          당시 `frontier.md`(지금은 `frontier/` 로 갈라졌다)의 "병렬 배치" 절은
                  `레인 A 하나 · 레인 B 하나` 를 권했다. 그런데 `SELECTED` 칸은 **하나만**
                  담는 형태였고, Cycle 번호를 먼저 잡는 규칙은 어디에도 없었다. 둘은 같은
                  뿌리다 — **두 레인이 공유하는 자리를 아무도 소유하지 않는다.**

    일어난 일      네 번 겹쳤다.
                      C022   C020 → C021 → C022 로 두 번 옮겼다 (HISTORY 번호 정정)
                      C025   C023 으로 잡았으나 레인 A 의 `C023-what-you-wear-changes-you`
                             가 먼저 들어와 C024 로, 다시 레인 A 의 `C024-one-slot-one-item`
                             이 먼저 들어와 C025 로 — **두 번 옮겼다.**
                             그리고 이 Cycle 은 `SELECTED` 가 아닌 후보에서 출발해야 했다 —
                             01-cycle.md 의 MASTER TRACE 가 그 사유를 적고 있다

                  **두 레인이 각자 "다음 빈 번호" 를 세면 언제나 같은 수가 나온다.**
                  번호는 레인이 스스로 정할 수 있는 것이 아니다.

    이미 선 것     **번호 쪽 절반은 규율이 아니라 구조로 답을 얻었다.** 처음에는
                  `frontier.md` 의 "병렬 배치" 절에 `Cycle 번호를 먼저 예약한다` 가
                  규칙으로 섰는데, 그 규칙이 다섯 번째 충돌을 막지 못했다 —
                  `C026-open-what-you-carry` 가 기획서의 `[DIRECT-CYCLE]` 지시로 출발해
                  Frontier 를 한 번도 거치지 않았고, 규칙이 거기에만 적혀 있어 읽을 일이
                  없었기 때문이다.

                  그 뒤 **Frontier 트랙 분할**이 예약 규칙 자체를 없애고 번호공간을
                  트랙 소유로 바꿨다 (`frontier/README.md` 병렬 규칙 · `C-<TRACK>-NNN`).
                  한 트랙 = 동시에 한 세션이므로 **두 레인이 같은 번호를 셀 자리가
                  사라졌다** — 예약도 규율도 필요 없다. `SELECTED` 도 같은 분할로
                  트랙마다 자기 파일이 되었다.

    지금 한 것     아래 (a) 가 요구하던 형태가 트랙 분할로 이미 서 있다. 이 질문에 남은
                  것은 **그 형태를 그대로 둘 것인가** 하나이며, 옛 번호공간(C001~C026)을
                  어떻게 둘 것인가는 (c) 가 묻는 것과 같은 자리다.

    선택지        (a) **트랙이 `SELECTED` 와 번호를 함께 소유한다** — 지금 선 것을
                      그대로 둔다. 트랙마다 자기 frontier 파일과 자기 번호공간이 있어
                      겹칠 자리가 없다. **가장 값이 싸며, 이미 굴러가고 있다.**
                  (b) **번호를 Cycle 이 아니라 Frontier 가 준다** — 후보마다 번호를
                      미리 달아 두고 고르면 그 번호로 시작한다. 겹칠 자리가 사라지지만
                      후보가 재배열될 때 번호가 흩어진다.
                  (c) **번호를 쓰지 않는다** — 디렉터리 이름을 뜻으로만 짓는다
                      (`the-shape-is-data`). 순서는 HISTORY 가 지닌다. 가장 근본적이나
                      기존 스물여섯을 전부 건드린다.
                  (d) 지금 정하지 않는다 — 레인을 하나로 되돌려 문제를 없앤다.

    영향          (d) 를 고르면 `frontier/README.md` 의 병렬 규칙과 트랙 번호공간이
                  사라져야 하고, 그것이 `guides/works.md` 의 레인 정의까지 닿는다.
                  그 외에는 공정 문서(`guides/` · CLAUDE.md)의 한 줄 수준이다.

    관련           C025 08-verification.md MASTER FEEDBACK ③④ 가 이 질문의 출처다.
                  경위는 feedback/C025-the-shape-is-data.md ·
                  feedback/C026-open-what-you-carry.md 가 소유한다.

## Q36. 곡괭이에 상위 정의(`IT-*`)가 없다 — OPEN

    무엇          C020 이 MASTER GAP 으로 올렸다. 세계에는 곡괭이가 있고 이제
                  정의소에도 있는데, 그것이 어느 `IT-*` 에서 왔는지 답할 자리가 없다.
                  돌은 `IT-COMMON-STONE` 까지 추적된다.

    Conflict      DC-WORLD-RESOURCE-ADAPTATION-TRACE 는 성질의 세계 유래를 요구하고,
                  곡괭이가 지닌 것은 성질이 아니라 **용도**다 (IS §3.3).
                  그래서 어긴 것은 아니지만, 세계의 아이템 중 유래를 답하지 못하는
                  것이 하나 생겼다.

    Affected      `IT-COMMON-STONE` 과 나란히 설 노드의 부재 · 이후 제작이
                  "무엇으로 만든 도구인가" 를 물을 때 답할 자리 ·
                  장착 Cycle 이 "이 도구가 채굴 용도를 준다" 를 어디에 적을 것인가

    선택지        (a) **문명권 유래의 평범한 도구 노드를 세운다** — `IT-MINING-PICK`.
                      `MW-SAFE-FRONTIER` 에서 왔고 `IP-*` 는 **없다**. 채굴은 세계압이
                      남긴 성질이 아니라 사람이 붙인 쓸모이므로 성질 노드를 만들지
                      않는다. `IT-COMMON-STONE` 이 이미 같은 형태다 — 그 노드도
                      "성질이 없는 것이 요점" 이라고 적혀 있다.
                      → 유래를 답하지 못하는 아이템이 0 이 된다. 성질 없는 종류가
                        정상이라는 것이 노드 하나로 더 분명해진다.
                  (b) **용도만 지닌 아이템은 유래를 요구하지 않는다고 명시한다** —
                      DC-WORLD-RESOURCE-ADAPTATION-TRACE 의 경계에 그 예외를 적는다.
                      → 노드가 늘지 않는다. 대신 "어디까지가 자원이고 어디부터가
                        도구인가" 를 매번 판단해야 한다.

    Agent 판단     (a) 를 권한다. 근거는 선례다 — `IT-COMMON-STONE` 이 Q22 에서 정확히
                  같은 이유(성질이 없는 것이 맞다)로 섰고, 그때 그 노드를 세운 것이
                  "평범함" 을 세계 법칙의 결과로 만들었다. 다만 (a) 는 자원이 아닌
                  종류를 `IT-*` 로 부르는 첫 사례이므로 이름 축의 결정이 필요하다.

    관련           같은 형태의 제안이 미승인 초안 하나에 이미 들어 있다 —
                  `content/proto-adventure/design/Design-Resource-Catalog-R0.md` §5 (Agent 작성 · 승인 대기).
                  그 문서를 승인하면 이 질문은 그 주입으로 함께 닫힌다.

    무게           **둘이 되었다** (C024). 손방패도 `IT-*` 가 없다 — 곡괭이와 같은
                  사정이다 (문명권에서 만든 평범한 것 · 성질이 아니라 쓸모를 지닌다).
                  Cycle 이 지어 붙이지 않고 이 질문으로 되돌렸다. 걸 수 있는 종류가
                  늘 때마다 같은 일이 반복되므로, 답을 미룰수록 소급할 항목이 는다.

    DECISION      PENDING

## Q37. 자리에 이름을 줄 것인가 — 자리 배치 조작을 별도 Frontier 로 세울지 — OPEN

    무엇          IE §48 은 자리 배치 조작(옮기기 · 맞바꾸기 · 나누기 · 정렬 · 필터)을
                  Cycle 1 행에 넣었다. C022 는 그것을 **하지 않고** 닫았다 — 세계가
                  자리를 소유하고 획득이 자리를 채우는 것까지가 "담을 자리가 유한하다" 의
                  본체이고, 그 배치를 사람이 손으로 바꾸는 것은 표면의 편의라는 판단이다
                  (C022 08 MASTER GAP ②).

    지금 상태     **자리에 이름이 없다.** `Inventory.UsedSlots` 는 수 하나이고, 무엇이
                  몇 번째 자리에 있는지를 세계가 알지 못한다. 옮길 곳도 나눌 이유도
                  아직 없다 — 세계에 종류가 둘뿐이다.

    영향          지금 막히는 것은 없다. 다만 배치를 열려면 **자리마다 신원을 주는 일**이
                  먼저이고, 그것은 되돌리기 어려운 선택이다 — 자리가 수에서 목록이 되면
                  획득·소모·덜어내기의 모든 판정이 "어느 자리에" 를 함께 답해야 한다.
                  개체(Instance)를 만들지 않는다는 DC-GROWTH-DEFINITION-INSTANCE-SPLIT 과도
                  맞닿는다 — 자리에 신원을 주는 것과 물건에 신원을 주는 것은 다르지만
                  가깝다.

    선택지        (a) **세우지 않는다 (현행 유지)** — 자리는 계속 수 하나다.
                      → 아이템 종류가 늘어 실제로 불편해질 때 다시 묻는다. 그때는
                        불편이 관찰로 있으므로 판단이 쉽다. 지금 세우면 종류 둘짜리
                        세계를 위해 구조를 늘리는 것이 된다.
                  (b) **별도 Frontier 로 세운다** — `FR-WHERE-THINGS-SIT` 같은 후보.
                      → IE §48 의 Cycle 1 을 문서대로 채운다. 다만 7 조건 2(어느
                        Possibility 를 전진시키는가)를 세우기 어렵다 — 배치는 할 수 있는
                        일을 늘리지 않는다. C022 와 같은 "노드 아님" 예외가 또 필요해진다.
                  (c) **장착 Cycle 이 함께 가져간다** — 장착은 어차피 자리를 다루므로
                      그때 배치도 함께 연다.
                      → Cycle 이 커진다. IE §48 이 둘을 가른 이유와 반대로 간다.

    Agent 판단     (a) 를 권한다. 근거는 C022 자신의 실측이다 — 종류 둘에 자리 넷인
                  세계에서 배치 조작이 없어 겪은 불편이 0건이었다. 다만 이것은
                  **IE §48 의 배치를 따르지 않겠다는 뜻**이므로 Human 이 정할 일이다.

    DECISION      PENDING

## Q38. "버리기" 와 "덜어냄" 은 같은 행동인가 — OPEN

    무엇          IE §34 의 **버리기**는 몸 → 세계 이동이고 세계 개체화(IS §6 Cycle 4)를
                  요구한다. C022 가 세운 **덜어냄**은 소모다 (IS §5.5) — 덜어낸 것은
                  세계에 놓이지 않고 없어진다. 지금은 같은 이름으로 부를 수 없는
                  다른 행동이 둘 있는 셈이다 (C022 08 MASTER GAP ③).

    지금 상태     `RULE-ITEM-DISCARD-001` 이 그 종류를 전부 줄인다. 세계에 아무것도
                  놓이지 않는다 — 놓일 곳이 없기 때문이다.

    영향          MC-TRANSFER-ITEM(세계의 아이템)이 서는 날 정해야 한다.
                  C022 는 "같은 행동의 **도착지만** 바뀐다" 로 보고 Transition 에 한 줄이
                  더해질 자리를 열어 두었다. 그 판단이 맞다면 행동은 하나이고,
                  틀리다면 플레이어에게 "없애기" 와 "내려놓기" 두 가지가 필요하다.

    선택지        (a) **하나로 본다** — 도착지가 세계로 바뀔 뿐 행동은 그대로.
                      → 규칙이 하나로 남는다. 다만 "없애고 싶은데 세계가 지저분해진다"
                        가 플레이 문제로 남을 수 있다.
                  (b) **둘로 가른다** — 없애기(소모)와 내려놓기(이동)를 다른 행동으로.
                      → 플레이어의 의도가 갈린다. 규칙과 계약과 조작이 각각 둘이 된다.

    Agent 판단     지금 판단할 근거가 부족하다. 세계에 놓인 아이템이 어떻게 사라지는지
                  (소멸 시간 · 소유권)를 MC-TRANSFER-ITEM 이 정한 뒤라야 (a) 의 비용을
                  볼 수 있다. **그 Cycle 의 Stage 1 로 미루는 것**을 권한다.

    DECISION      PENDING


## Q66. 갈래를 노드의 완결로 판정하는가, 플레이의 성립으로 판정하는가 — OPEN

    무엇          `MP-EXPLOIT-OPEN-BODY` 의 요구 넷 중 셋이 섰고 넷째
                  (`MC-AURA-ALLOCATION`)가 PARTIAL 이다. 노드로 세면 갈래도 PARTIAL 이다.
                  그런데 **플레이로는 이미 성립한다** — 상대가 물리에 무른 것을 살펴보지
                  않고 알아내고, 배분으로 물리 공격을 올려 그 무른 쪽을 친다
                  (C-COMBAT-001 08 의 WORLD SCENARIO).

    지금 상태     `overlay_missing: MC-AURA-ALLOCATION (PARTIAL)` 로 사실만 적어 두었다.
                  Cycle 이 "어느 쪽으로 판정할지는 Master(Human)의 몫" 이라 명시하고
                  올렸으므로 Agent 가 고르지 않는다.

    영향          이 판정은 이 갈래 하나가 아니라 **Overlay 를 읽는 법 전체**에 걸린다.
                  Frontier 가 "다음에 무엇을 할 것인가" 를 Overlay 의 결손에서 뽑으므로,
                  플레이로 성립한 갈래를 계속 결손으로 세면 이미 놀 수 있는 것을 또
                  후보로 올리게 되고, 반대로 성립으로 세면 semantic 의 남은 절반이
                  시야에서 사라진다.

    선택지        (a) **노드의 완결로 판정한다** — 요구 Capability 가 전부 IMPLEMENTED 일
                      때만 갈래가 닫힌다.
                      → Overlay 가 설계 문서와 1:1 로 맞는다. 다만 "이미 놀 수 있는데
                        결손으로 적혀 있다" 가 늘어난다.
                  (b) **플레이의 성립으로 판정한다** — 그 갈래로 실제 플레이가 되면 닫는다.
                      → Frontier 가 사람이 겪는 것과 붙는다. 다만 semantic 의 남은 절반이
                        어디에도 안 적히므로 그것을 담을 자리가 따로 필요하다.
                  (c) **둘을 따로 적는다** — 갈래에 "노드 완결" 과 "플레이 성립" 두 값을 둔다.
                      → 둘 다 보인다. 대신 판정할 것이 두 배가 되고 스키마가 는다.

    Agent 판단     (c) 가 정확하나 값이 비싸다. 지금 실제로 겪은 어긋남은 이 갈래 하나뿐이라,
                  스키마를 늘리기 전에 같은 형태가 한 번 더 나오는지 보는 편이 낫다고 본다.
                  그때까지는 (a) 로 두고 `overlay_note` 가 "플레이로는 성립한다" 를 적는
                  지금 방식 — 즉 **표는 (a), 산문은 사실** — 을 권한다.

    DECISION      PENDING

    번호 주      원래 Q62 로 열렸으나, 같은 시기에 전투 지식(CK) 주입이 Q62~Q65 를
                 쓰고 닫아 HISTORY 로 옮겼다 (병렬 레인의 번호 충돌). **닫힌 쪽을
                 고치는 것보다 열린 쪽을 잇는 것이 싸므로** 이 질문이 Q66 이 되었다.
                 아래 자리들이 이 번호를 가리킨다 —
                 `feedback/C-COMBAT-001-where-your-power-sits.md` ·
                 `graph/possibilities.yaml` 의 MP-EXPLOIT-OPEN-BODY overlay_note.



## Q67. 병렬 레인이 공유하는 두 자리가 어긋난다 — OPEN · PROCESS

    무엇          이번 합류에서 같은 형태의 결손이 둘 드러났다.

                  ① **Q 번호공간이 레인 소유가 아니다.** Cycle ID 는 트랙별로 갈라
                     두었으나(원칙 22) `open-questions.md` 의 Q 번호는 전역이라,
                     두 레인이 같은 번호를 동시에 쓴다. 이번에 Q62 가 충돌했고
                     열린 쪽을 Q66 으로 옮겨 해소했다.
                  ② **frontier/README.md 의 트랙 표가 트랙 파일과 어긋난다.**
                     양쪽 레인이 각자 자기 트랙 파일만 고치고 인덱스를 잊어,
                     COMBAT 과 TERRAIN 두 행이 동시에 낡아 있었다.
                     `lanes:check` 는 트랙 파일을 보므로 잡지 못한다.

    영향          둘 다 합칠 때만 아프고 평소에는 조용하다. 그래서 되풀이된다.
                  ②는 특히 위험하다 — 인덱스가 "무엇을 고를 수 있는가" 의 첫 화면인데
                  그것이 틀리면 다음 세션이 이미 닫힌 것을 고른다.

    선택지        ① 에 대해
                     (a) 번호를 레인 접두사로 가른다 (`Q-COMBAT-1` 류)
                         → 충돌이 원리적으로 사라진다. 대신 기존 예순여섯이 바뀐다
                     (b) 새 질문은 항상 "지금 최대 +1" 이 아니라 **합류 뒤에 번호를
                         받는다** → 규칙 한 줄로 끝나나 사람이 지켜야 한다
                     (c) 그대로 두고 충돌 때마다 옮긴다 (이번에 한 것)
                  ② 에 대해
                     (d) `lanes:check` 가 **인덱스와 트랙 파일의 SELECTED 를 대조**한다
                         → 재료가 이미 있다 (`tools/lanes/build.ts` 가 둘 다 읽는다)
                     (e) 인덱스에서 SELECTED 열을 **지운다** — 트랙 파일이 단일 출처이므로
                         복제를 없앤다 → 어긋날 것이 없어진다. 대신 한눈에 보기가 준다

    Agent 판단     ①은 (b), ②는 (e) 를 권한다. 둘 다 검사를 늘리지 않고 복제를 줄인다.
                  다만 이것은 PROCESS 레인의 몫이며 지금 그 레인이 비어 있다.

    번호 주      ①이 이번 합류에서 **또 났다** — main 이 BT §16 주입에 Q68 · Q69 를 쓰는
                 동안 다른 갈래가 FC 주입에 Q68~Q72 를 썼다. 둘 다 닫힌 질문이라 열린 것을
                 옮길 필요가 없어 번호를 그대로 두고 HISTORY 양쪽에 "번호 주의" 를 달았다.
                 세 번째 사례이며, (b) 나 (a) 가 정해지기 전까지는 계속 난다.

    DECISION      PENDING
