# STATE — HktAtom 현재 위치 (SSOT · step마다 §1~4 덮어쓰기 · §5 만 append)

> 권위 분리: 목표 [CLAUDE.md](CLAUDE.md) · 척추 [KERNEL.md](KERNEL.md) · 이 문서 = "지금 어디까지·다음은 무엇". 크기 예산 ≤ 20KB.

## §1 NOW — 지금 어디까지

- **계획 수립 완료 (step-0000, 프로젝트 탄생)**: 커널 정의(KERNEL §2 — 상호작용 I·측정 M·승격 ⇧/⇩)·규모 사다리(§4)·검증 5기둥(§8)·로드맵 arc A~F(§7)를 확정했다. 코드 0줄 — 아직 엔진·하네스 없음.
- 이웃 트랙 대비 정체성 확정: HGO atom(자료형 통일)·HGO flux(법칙 통일)에 이어 **규모 전이의 통일**(승격이 커널의 정식 연산자). CLAUDE.md 차별표 참조.

## §2 NEXT — 다음 한 조각 (step-0001)

**Arc A 부트스트랩 1/n — entity 자료형 + 결정론 엔진 + 닫힌 장부.**

- `engine/kernel.js`: entity `{c, r, p, u, ℓ}` 배열 + 시드 의사난수 + 상태 해시.
- `engine/interact.js`: 커널 상호작용의 뼈대 — 반대칭 힘 루프 + 문턱 교환 자리 (V₀ 는 최소형: 척력만 → 자유 운동에 가까운 무대).
- `engine/scenes.js` + `engine/verify.js`: 장면 레지스트리 + 헤드리스 검증 (기둥 2·3 — 장부·결정론). 골든 해시 앵커.
- 가설 assert: N 개 원자 자유 운동 T tick — Σc 비트 불변 · P·E 잔차 ≤ 1e-9 · 시드 재현 해시 일치.
- 뷰어(`viewer.html`)는 0001 또는 0002 에서 — 헤드리스가 먼저.

## §3 OPEN GAPS — 열린 격차

- **V₀ 의 구체형 미정** (Arc B 진입 전 결정): 쿨롱 + 단거리 척력 + 원자가(외각 전자) 문턱을 어떤 최소식으로 쓸까. 후보: 유효 쌍퍼텐셜(LJ 형) × 전하 부호 + 결합 슬롯 = 외각 전자 수. *실물리에서 직접* — 사다리 바닥의 유일한 author (KERNEL §4).
- **규모 정합의 "닮음" 지표 미정** (Arc C 진입 전 결정): 온도·밀도·구조 수 히스토그램 거리? HktFeature 의 "거시 예측의 닮음 측정" 선례를 참고할 것.
- **u 강등(⇩)의 시드 규약 미정** (Arc C): u′ 와 일관된 미시 배치 샘플링을 어떻게 결정론으로 고정하나.

## §4 DURABLE — 여러 step 이 반복 참조할 불변

- 커널 체크 5항 (KERNEL §5) · 검증 5기둥 (KERNEL §8) — 매 step 통과 의무.
- 규모 분기 `if(ℓ)` 금지 — ℓ 은 파라미터 테이블 색인일 뿐.
- 상위 규모 V 는 하위 측정 산출물만 (L0 예외). 손 튜닝 발견 즉시 격차로 등록.

## §5 INDEX — step 인덱스 (literal 1줄/step append)

- step-0000: 프로젝트 탄생 — 커널 척추(KERNEL.md)·사다리·로드맵 확정. 코드 0줄.
